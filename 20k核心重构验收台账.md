# 20k 核心重构验收台账

更新时间：2026-08-28（Asia/Shanghai）

## 1. 当前唯一验收缺口

在不改变通用手机视觉 Agent 产品能力、公开 API 和真机执行语义的前提下，将 Git 跟踪的
`poc/agent/**/*.py` 生产代码从 `45158` 个物理行真实收敛到 **少于 `20000` 行**；内部目标为
`<=19500` 行，保留至少 `500` 行余量。

阶段停止条件只有两个：

1. 行数审计达到 `<20000`，全部保留合同通过相关回放与一次完整回归；
2. 发现某个删改只能靠改变产品能力、公开 API、风险边界或真机动作语义才能成立时，停止该切片并重新设计，
   不以行为退化换取行数。

## 2. 基线与必须出现的证据

- 基线提交：`bf02a2e refactor: unify input lineage record construction`；开始本阶段时工作区干净。
- 基线生产代码：`45158` 行。
- 基线完整 Python 回归：`922/922` 通过。
- 最大七个模块合计 `28016` 行：Orchestrator `7012`、Scene Observer `6912`、Task Graph `3849`、
  Canonical Catalog `2764`、Action Adapter `2716`、Task Semantic IR `2424`、Controller `2339`。
- 完成证据必须同时包含：最终逐文件行数清单、总数 `<20000`、依赖方向扫描、旧权威防恢复扫描、
  相关历史失败回放、正向与变化样本、一次最终完整 Python 回归、`git diff --check` 和本地提交。

## 3. 防作弊口径

以下方式不能计入完成：

- 把同一生产实现移出 `poc/agent` 或改后缀以逃避统计；
- 压成一行、合并无关语句、删除正常空行/类型信息/可读名称等排版压缩；
- 删除仍代表当前产品合同的测试、证据或公开能力；
- 用动态 `exec`、字符串代码、生成文件、外部下载或隐藏兼容模块代替正常实现；
- 保留新旧两套权威，仅让旧入口暂时不被调用。

测试不计入生产行数，但只有在对应旧运行权威已经物理删除、且测试仅验证该旧内部实现时，才能同步删除或改写；
不得为了让回归变少而删除行为合同测试。

## 4. 必须保持的产品合同

1. 通用多 App，不增加 App、命令、截图、固定步骤或固定坐标特例。
2. DeepSeek 只负责高层 typed 任务图与重规划；Qwen 对当前实际活动子目标和当前新截图每步最多调用一次。
3. 唯一视觉循环仍是：新截图 → 单步结构化事实 → 一个 canonical 动作 → 本地校验 → 执行一次 → 新截图。
4. 唯一动作协议仍为 `2026-08-20-canonical-action-v1`；Controller 不建立第二候选或第二任务权威。
5. typed 输入字段和 `input_structure.application_inputs[*].text` 仍是输入正文唯一权威；保留拉丁、中文、
   数字、符号、换行、多字段、候选提交、清空和动作后验证。
6. 保留设备隔离、一次性 scope、机械可达、方向凭据、动作回执、会话证据和一次普通导航纠正。
7. 只有 `authentication` 与 `financial_transaction` 需要确认；不新增阻止类别。
8. 保持当前公开 HTTP 路由、请求/响应 schema、官方类型化客户端和已有真实证据的解释口径。

## 5. 同类问题样本与根因

本阶段不是修复某张截图，而是处理同一架构根因：历史演进把同一串行闭环拆成大量重复投影、重复校验和
内部专用协议，造成维护成本远高于动作本身。

- `universal_agent_orchestrator.py`：多个生命周期入口重复组装同一步状态、证据和错误终态。
- `generic_scene_observer.py`：一个远程响应在多个输入/几何/键盘分支重复解析、规范化和否决。
- `task_graph.py` + `task_semantic_ir.py`：模型 wire、领域状态、风险预览和兼容规范化混在两套大型对象图中。
- `canonical_action_protocol.py` + `universal_action_controller.py` + `generic_action_adapter.py`：同一动作的目录、
  参数、前置、执行和后置规则散落并重复枚举。
- 输入、能力验收和诊断模块仍有历史专用包装；只能在主链替换稳定后按真实调用图继续收敛。

## 6. 通用重构方案与行数预算

采用轻量 DDD 模块化单体的逐切片替换，不做微服务，也不建立兼容层。每批新入口接管后，同批物理删除旧入口、
旧状态权威和只验证旧内部结构的测试。

| 运行切片 | 目标内容 | 完成后预算 |
| --- | --- | ---: |
| 任务语义与状态 | typed 任务计划、推进、重规划、两类风险 | `<=3500` |
| 单步观察与模型 | 当前截图事实、输入结构、Qwen 本地选择 | `<=4500` |
| 动作与执行 | canonical 目录、Controller、Adapter、设备端口 | `<=5000` |
| 会话、输入与基础设施 | 会话/证据/输入账本/硬件/验收/配置 | `<=6500` |
| **总计内部目标** | `poc/agent/**/*.py` | **`<=19500`** |

依赖方向保持 `interfaces -> application -> domain`；`infrastructure` 实现端口并由组合根注入。

## 7. 验证清单

每个切片：

1. 先以 AST/导入/调用图确认正式入口、公开符号和动态分发；
2. 建立正向、反向和至少一个变化样本；
3. 新实现接管后删除旧权威，不保留 facade/feature flag/双写；
4. 运行当前切片最小测试和相关历史失败回放；
5. 记录切片前后生产行数并本地提交。

最终：

1. 运行全部 Python 回归一次；
2. 运行前端/静态/依赖方向/旧权威/差异检查；
3. 审计生产代码总数 `<20000`，确认没有迁移或排版作弊；
4. 更新本台账与 `项目交接文档.md`，本地提交，不推送。

本阶段只做离线重构与自动验证；不重载项目 API、不调用在线模型、不创建真机会话、不执行机械动作，
也不启动、停止、重启或操作卖家 `main.exe`。

## 8. 当前状态

- 验收合同：已冻结。
- 正式调用图：已完成第一版，见下节。
- 生产代码：`37394` 行，目标还差至少 `17395` 行净减少。
- 真机状态：本阶段不触碰；既有真机结论不因重构自动改变。

## 9. 正式运行调用图与替换顺序

正式 Web 组合根只装配一条链：

`web_app -> UniversalAgentSessions -> UniversalAgentOrchestrator`

Orchestrator 的正式依赖为：

`DeepSeekTaskGraphPlanner -> DynamicTaskGraph/TaskSemanticIR -> ObservationBridge ->`
`SingleStepGenericSceneObserver -> QwenVisualDecisionObserver -> CanonicalActionCatalog ->`
`GenericSingleActionAdapter -> UniversalActionController -> DeviceExecutor -> RobotController`

动作后由 Adapter 取得新帧，Controller 只验证刚执行的 typed transition；Orchestrator 再让 DeepSeek 推进或
重规划。文件系统证据、输入 lineage、相机协调、方向凭据和设备独占是这条链的注入端口，不拥有任务、
视觉或动作第二权威。

替换顺序固定为：

1. `task_graph.py + task_semantic_ir.py + deepseek_task_graph.py`：把模型 wire、领域状态和两类风险收敛成一套；
2. `generic_scene_observer.py + qwen_visual_decision.py`：每张当前截图只形成一份 typed scene；
3. `canonical_action_protocol.py + universal_action_controller.py + generic_action_adapter.py`：同一动作规格表驱动
   候选、参数、前置、执行和后置；
4. `universal_agent_orchestrator.py`：只保留串行生命周期、一次确认、一次纠正、证据和 DeepSeek 推进；
5. 输入、能力验收与硬件基础设施按新主链真实需要收敛。

第一版 AST 规模证据：`compile_runtime_graph_semantics` 单函数 `1013` 行、canonical catalog 编译单函数
`1477` 行、输入结构审计单函数 `1661` 行，Controller 主类 `2091` 行，Orchestrator 主类 `6092` 行。
这些是本阶段必须替换的运行切片，不再继续以历史分支内小 helper 去重作为停止条件。

## 10. 第一批：typed 语义只读视图收敛

- `task_semantic_ir.py` 从 `2424` 行降至 `903` 行，生产总量从 `45158` 降至 `43637`，净减 `1521` 行。
- `DynamicTaskGraph` 继续是高层任务和 typed effect 的唯一权威；Semantic IR 只从当前图确定性投影
  surface/entity/effect/action/input-field/evidence 只读视图，不再维护分散的风险和序列化分支。
- 风险决策只消费 typed effect kind；默认仍只有 `authentication` 与 `financial_transaction` 为
  `confirmation_required`，其余 effect 自动执行。模型自由约束文字和约束所在层级不改变风险或语义摘要。
- 保留逐字 source span、多字段唯一归属、只读最终核对、候选提交、真实换行、最近任务、自然动作否定和
  typed effect target/payload 绑定；无 App、页面、截图、正文或坐标特例。
- 相关 DeepSeek/语义/canonical/Qwen/最近任务/Mock/Orchestrator 回归 `312/312`，本批唯一完整 Python
  回归 `922/922`（`112.714s`）通过；静态编译和 `git diff --check` 通过。
- 本批未重载项目 API、未调用在线模型、未创建真机会话、未执行机械动作，也未操作卖家 `main.exe`。

## 11. 第二批：typed 任务状态与重规划校验收敛

- `task_graph.py` 从 `3849` 行降至 `3124` 行，生产总量从 `43637` 降至 `42912`，净减 `725` 行。
- 三套本地 navigation 分类共用同一子目标上下文、typed effect 绑定和正向外部效果检查；任务图、观察、
  controller transition 与 visual claim 共用 typed record 序列化；planner snapshot 与运行时视图共用同一
  goal/condition/effect/subgoal wire 投影。
- 单导航终态 payload、typed candidate 和全局条件完成共用严格 task/device/revision/subgoal/receipt 绑定与
  exact condition 覆盖；重规划的条件、effect、子目标不可删除/不可改写和证据归属使用同一字段不变式，
  未放宽证据、风险或 active frontier。
- 相关 DeepSeek/语义/canonical/Qwen/最近任务/Mock/Orchestrator 回归 `300/300`，本批完整 Python 回归
  `922/922`（`112.327s`）通过；`git diff --check` 通过。
- 本批未重载项目 API、未调用在线模型、未创建真机会话、未执行机械动作，也未操作卖家 `main.exe`。

## 12. 第三批：单步视觉线协议权威收敛

- `generic_scene_observer.py` 从 `6912` 行降至 `6701` 行，生产总量从 `42912` 降至 `42700`，净减 `212` 行；
  本批只完成观察切片的线协议入口，观察与 Qwen 整体预算尚未达标。
- 单步响应只接受与本轮实际 JPEG 高度一致的 `axis_grid / 1000×request_height`，并在解析任何 scene、输入、
  IME 或键盘事实前原子换算为内部 `0..1000`。旧 `normalized_1000`、`image_grid`、纵轴猜测、平铺 UIScene
  包装和缺失嵌套版本补写入口均已物理删除。
- 正向变化样本覆盖 `720×1280` 与 `540×960` 请求产生等价 canonical bounds；防恢复样本覆盖两种旧网格、
  错误高度、越界、缺失 coordinate_space、平铺 scene 和缺失/额外嵌套字段。
- 观察合同 `23/23`、观察—Qwen—输入账本—Adapter 相关回归 `194/194`、本批完整 Python 回归
  `922/922`（`125.648s`）通过；`git diff --check` 通过。
- 本批只做离线重构；未重载项目 API、未调用在线模型、未创建真机会话、未执行机械动作，也未操作卖家
  `main.exe`。下一步继续替换输入结构审计和 Qwen 本地选择中的重复运行分支。

## 13. 第四批：输入观察与 Qwen 本地选择收敛

- `generic_scene_observer.py` 从 `6701` 行降至 `5330` 行，`qwen_visual_decision.py` 从 `1529` 行降至
  `1067` 行；生产总量从 `42700` 降至 `40867`，净减 `1833` 行。当前仍未达到 `<20000`，不能宣称
  阶段完成。
- 单步输入审计不再运行 11 条旧 scene 修复权威，也删除旧 normalized/image 坐标修补、无效系统 UI 宽免、
  相机对齐覆盖和数条已被严格 axis-grid 合同排除的键盘坐标恢复路径。可选预编辑或候选的坏数据只会被丢弃，
  不再否决同帧已经独立成立的 typed 应用输入正文。
- Qwen 本地选择继续只消费本轮 canonical candidate；scope、动作、目标、effect 和确定性精确选择共用同一套
  绑定/校验，不再维护第二页面状态对象、第二动作 effect 判断和重复 identity 候选遍历。没有增加 App、命令、
  截图、坐标或正文特例。
- 正向与变化样本继续覆盖空输入、typed 前缀、拉丁/中文、IME 候选、换行、键盘模式/布局/大小写、多字段和
  最近任务；观察—Qwen—输入账本—Adapter 相关回归 `194/194`、本批完整 Python 回归 `922/922`
  （`149.215s`）通过，`git diff --check` 通过。
- 本批只做离线重构；未重载项目 API、未调用在线模型、未创建真机会话、未执行机械动作，也未启动、停止、
  重启或操作卖家 `main.exe`。下一批继续以真实调用图收敛观察输入审计，再进入 canonical/Controller/Adapter。

## 14. 第五批：当前截图输入事实单一化

- `generic_scene_observer.py` 从 `5330` 行降至 `4601` 行；同时从 Adapter 删除 12 行旧 `prior_scene`
  传递，生产总量从 `40867` 降至 `40126`，净减 `741` 行。观察与 Qwen 合计仍高于原切片预算，阶段总目标
  也尚未达到 `<20000`。
- 新观察不再接收或缓存上一步模型 scene；跨步只允许本地 typed 动作摘要、typed 输入 lineage 和当前新帧。
  已删除无效 `ledger_input_value` 形参、旧 compact 输入/键盘三套剥离入口及 scene 与 input_structure 可选框
  不重合就否决整帧的第二几何冲突规则。
- 可见键盘外框或可选 mode/backspace/enter/case/right-button 子结构无效时，只撤销对应执行几何；不会推翻
  同一响应中独立成立的应用输入正文。需要该键位的下一动作仍因没有 canonical 几何而不可执行，未放宽
  scope、可达性或动作后验证。
- 正向与变化样本继续覆盖空输入、typed 前缀、中文/拉丁、IME 候选、换行、模式/布局/大小写、多字段及
  动作后 lineage；相关回归 `194/194`、完整 Python 回归 `922/922`（`99.509s`）通过，
  `git diff --check` 通过。
- 本批只做离线重构；未重载项目 API、调用在线模型、创建真机会话、执行机械动作或操作卖家 `main.exe`。
  下一批进入 canonical/Controller/Adapter 的唯一规格表收敛，并在后续总审计中继续压低观察切片。

## 15. 第六批：canonical 动作与当前画面推进收敛

- `canonical_action_protocol.py` 从 `2764` 行降至 `2581` 行，`universal_action_controller.py` 从 `2339`
  行降至 `2293` 行，`generic_action_adapter.py` 从 `2704` 行降至 `2594` 行，Orchestrator 从 `7012`
  行降至 `6705` 行；生产总量从 `40126` 降至 `39480`，净减 `646` 行，仍未达到 `<20000`。
- canonical catalog 的元素绑定、候选加入、tap transition 和活动子目标过滤共用同一形式规格；Controller
  删除 `observe/verify/finish/ensure_app` 非 canonical 旧入口并复用统一解析结果；Adapter 的 pending lineage、
  方向诊断和 QWERTY OCR 锚点只保留一套实现。
- Orchestrator 将分散的 App 前台、系统 surface、页面身份、元素存在、多元素存在、标题前缀和输入焦点判断
  收敛为一次“结构化目标 + 当前可信画面”的匹配，再统一校验 DeepSeek revision/安全前缀。值、缺失状态、
  外部效果和动作结果仍必须走各自 typed evidence，当前画面不会越权证明它们。
- canonical—Controller—Adapter—Orchestrator 相关回归 `397/397`、完整 Python 回归 `922/922`
  （`112.600s`）通过，静态编译和 `git diff --check` 通过。
- 本批仅做离线重构；未重载项目 API、调用在线模型、创建真机会话、执行机械动作，也未启动、停止、重启
  或操作卖家 `main.exe`。下一批继续替换观察器中仍为千行级的输入结构审计主函数。

## 16. 第七批：输入结构审计单路径收敛

- `generic_scene_observer.py` 从 `4601` 行降至 `4351` 行，生产总量从 `39480` 降至 `39230`，净减
  `250` 行，仍未达到 `<20000`。本批使用标准 120 字符 Python 排版，但主要减少来自键盘解析、字段匹配、
  输入元素发布和输入控制发布的单路径重构，不是移出统计范围、生成代码或压缩混淆。
- 键盘 wire 校验、当前执行边界、本地 QWERTY 锚点和可选键位撤权现在只生成一个 typed keyboard 结果；
  应用字段结构只解析一次，活动字段和前置字段共用相同的可见标签/精确值选择。输入元素、IME 候选、逐字键、
  换行键、布局/大小写/输入模式切换统一从同一份审计结果发布，不再各自重复重建状态与证据。
- 生产 provider 调用只保留正式 DashScope 合同：一次请求、显式 timeout、`max_attempts=1` 和 JSON response
  format；已删除在生产模块中吞掉参数 `TypeError` 后降级调用旧测试 provider 的兼容回退，测试替身直接实现
  正式端口。每张新截图一次 Qwen、输入正文唯一权威、canonical 候选和本地几何门均未改变。
- 观察—输入账本—Adapter—Qwen 相关回归 `194/194`、完整 Python 回归 `922/922`（`111.529s`）通过，
  静态编译和 `git diff --check` 通过。
- 本批仅做离线重构；未重载项目 API、调用在线模型、创建真机会话、执行机械动作，也未启动、停止、重启
  或操作卖家 `main.exe`。下一批转入 canonical catalog 的大型重复规格编译，继续按真实调用图物理删减。

## 17. 第八批：canonical 目录与 Controller 后置验证收敛

- `canonical_action_protocol.py` 从 `2581` 行降至 `2275` 行，`universal_action_controller.py` 从 `2293`
  行降至 `2174` 行；生产总量从 `39230` 降至 `38805`，净减 `425` 行，仍未达到 `<20000`。
- canonical 目录不再生成运行时没有消费者的元素两两空间关系；精确文字、surface 与下一字段 relation 只在
  当前事实唯一绑定时生成，歧义事实保持无候选。系统 affordance、动作规格、候选校验和滑动方向仍使用唯一
  canonical v1 协议，没有新建第二动作权威。
- Controller 的 formal transition 只按一次 `(predicate, operator)` 分派，输入辅助状态和聚焦证据各扫描一次；
  保留 direct-Latin 直接 Controller 调用、中文预编辑/候选、下一字段、系统栏、drag 与动作后新观察的原有
  typed 后置条件和错误边界。
- canonical—Controller—Adapter—Qwen—最近任务相关回归 `236/236`、完整 Python 回归 `922/922`
  （`154.938s`）通过，静态编译和 `git diff --check` 通过。
- 本批仅做离线重构；未重载项目 API、调用在线模型、创建真机会话、执行机械动作，也未启动、停止、重启
  或操作卖家 `main.exe`。下一批继续按真实调用图收敛 Adapter/Orchestrator，当前阶段仍在进行中。

## 18. 第九批：Adapter 单动作执行管线收敛

- `generic_action_adapter.py` 从 `2594` 行降至 `2222` 行，生产总量从 `38805` 降至 `38433`，净减
  `372` 行，仍未达到 `<20000`。
- 动作后观察已物理删除永远只运行一次的 `range(1, 2)` 伪重试层：稳定四帧只进入一次 Qwen 当前画面观察、
  一次 Controller 验证，结果不匹配直接交回重规划，绝不重复物理动作或模型采样。稳定、相对清晰度和手机
  取景身份的两个超时出口合并为同一确定性本地门。
- 键盘几何准备、方向凭据授权、pending/verified 输入 lineage 各保留一个正式入口；目标重绑定复用 domain
  `bounds_overlap`，文字控件与无标签图标的角色漂移共用唯一别名分支。QWERTY OCR 三帧稳定性、横纵网格、
  direct-Latin/中文/换行/清空及硬件门语义均未改变。
- canonical—Controller—Adapter—Qwen—输入 lineage 相关回归 `273/273`、完整 Python 回归 `922/922`
  （`106.158s`）通过，静态编译和 `git diff --check` 通过。
- 本批仅做离线重构；未重载项目 API、调用在线模型、创建真机会话、执行机械动作，也未启动、停止、重启
  或操作卖家 `main.exe`。下一批进入 Orchestrator 的会话推进与重复 receipt 投影，阶段仍在进行中。

## 19. 第十批：Orchestrator 单步生命周期收敛

- `universal_agent_orchestrator.py` 从 `6705` 行降至 `6402` 行，生产总量从 `38433` 降至 `38130`，净减
  `303` 行，仍未达到 `<20000`。
- session 状态、待执行动作清理、重新观察、可见状态推进和 transition report 现在各由一个生命周期入口
  处理；输入微动作只从同一 resolved effect、可信输入元素和 direct/auxiliary typed 状态验证，不再由多条
  分支重复寻找输入框、候选或键盘控制。
- 自动循环把一次纠正的 observation identity、候选动作、停止原因和终态写入同一记录；确认执行链的数据
  绑定和失败收口共用单一路径。一次物理动作、新截图、新候选、新 scope、最多一次普通导航纠正及输入/外部
  效果不自动重试的语义均未改变。
- 当前存在的关联测试项 `507/507` 通过；期间两次命令误引用已退役测试模块产生 `FailedTest`，已按当前文件
  清单纠正，属于 Agent 测试命令错误而非代码失败。完整 Python 回归 `922/922`（`111.188s`）通过，静态
  编译和 `git diff --check` 通过。
- 本批仅做离线重构；未重载项目 API、调用在线模型、创建真机会话、执行机械动作，也未启动、停止、重启
  或操作卖家 `main.exe`。下一批继续收敛当前截图观察切片，20k 阶段仍在进行中。

## 20. 第十一批：当前输入目标与审计控件单视图收敛

- `generic_scene_observer.py` 从 `4351` 行降至 `3994` 行，`generic_goal.py` 从 `134` 行增至 `281` 行；
  生产总量从 `38130` 降至 `37920`，净减 `210` 行，仍未达到 `<20000`。
- 活动子目标、观察投影、输入事务文字、typed 字段身份、前置字段、清空目标与键盘模式意图现在只解析为
  一个只读 `ActiveVisualGoal`；该对象不产生动作、不增加否决权，也不改变 DeepSeek/canonical/Policy/
  Controller 的正式权威。输入审计的 15 项散装返回值同时收敛为一个 typed 控件对象，输入元素和候选、
  换行、布局、大小写、模式键均消费同一结果。
- 观察—输入账本—Adapter—Qwen—模块依赖相关回放 `240/240`、完整 Python 回归 `922/922`
  （`170.052s`）通过，静态编译和 `git diff --check` 通过。一次测试命令在已进入 `poc` 目录后仍把
  `poc` 作为 `rg` 根路径，产生一次文件查找命令错误；同一命令中的实际测试 `60/60` 通过，已改用当前
  目录口径，不属于代码失败。
- 本批仅做离线重构；未重载项目 API、调用在线模型、创建真机会话、执行机械动作，也未启动、停止、重启
  或操作卖家 `main.exe`。下一批进入 task graph / semantic IR 的重复 typed 投影，阶段仍在进行中。

## 21. 第十二批：任务图本地导航归一化单路径

- `task_graph.py` 从 `3124` 行降至 `3068` 行，`deepseek_task_graph.py` 从 `553` 行降至 `547` 行；
  生产代码总量从 `37920` 降至 `37858`，净减 `62` 行，尚未达到 `<20000`。
- 精确输入、当前页面刷新和最近任务卡片清除不再分别遍历并复制同一 planner payload；三类跨 App 的
  本地 navigation 语义在一次子目标遍历中复用同一 typed effect 绑定、正向外部效果检查和删除结果。
  DeepSeek application 只调用这一条确定性归一化入口，任务图、canonical 动作和两类确认边界不变。
- DeepSeek、task semantic IR、最近任务、当前截图观察、Qwen 与输入 lineage 相关回放 `157/157` 通过，
  静态编译通过；最终完整回归按阶段规则留到达到目标后的唯一最终批次执行，不用窄回归替代完成证明。
- 本批仅做离线重构；未重载项目 API、调用在线模型、创建真机会话、执行机械动作，也未操作卖家
  `main.exe`。下一批必须转向编排器等千行级重复生命周期，当前 20k 阶段仍在进行中。

## 22. 第十三批：当前帧可见证据与输入 lineage 构造收敛

- `universal_agent_orchestrator.py` 从 `6402` 行降至 `5652` 行，`generic_goal.py` 从 `281` 行增至
  `667` 行，`input_value_lineage.py` 从 `1743` 行降至 `1646` 行；连同低层事实归位后，生产代码总量从
  `37858` 降至 `37394`，净减 `464` 行，尚未达到 `<20000`。
- 当前帧正向可见目标只由领域 `VisibleGoalEvidence` 评估一次；App 前台、system surface、命名页面、唯一/
  多元素、标题前缀、输入焦点和安全可见边界不再散落在 Orchestrator 的十几套方法中。Orchestrator 只负责
  取证后请求 DeepSeek 推进，不产生第二视觉事实或动作候选。
- `expected_idempotent_system_surface_kind` 已归入 canonical action vocabulary，App surface 匹配归入
  `ui_scene`；canonical catalog 与当前帧证据共同复用低层事实，domain/application 依赖方向检查通过。
- 六类 pending 输入动作和三类 verified 输入动作共用事件栅栏、typed 输入 surface 和 lineage record 构造；
  拉丁、中文预编辑、候选提交、键盘状态、逐字、换行及动作后四帧规则保持各自严格验证。对应结构测试已改为
  防止共享入口被重新拆回九套构造，而没有删除行为样本。
- 输入 lineage—Adapter—Orchestrator—Qwen—依赖边界最终相关回放 `332/332` 通过；一次组合回放的唯一
  失败是新领域模块错误导入 canonical 编译器，已按层次归位并复验，不是产品行为失败。
- 本批仅做离线重构；未重载项目 API、调用在线模型、创建真机会话、执行机械动作，也未操作卖家
  `main.exe`。下一批继续收敛 Orchestrator 的 App surface lineage 与单步生命周期。

## 23. 第十四批：App surface receipt lineage 单一权威

- App 页面归属的创建、完成校验、跨动作延续和只读观察身份刷新已归入 domain
  `AppSurfaceLineageAuthority`；Orchestrator 只保留错误边界和旧内部测试入口的薄委托，不再自行维护第二套
  receipt、任务祖先、前后台身份和页面标题连续性判断。
- 权威仍只接受同一 session/task/device/revision/subgoal、精确 action/rebound/resolved digest、一次物理动作、
  matched receipt、新 before/after observation 与 launcher 退出；后续 lineage 必须保持相同动作计数或由一次
  已验证动作推进，并且活动子目标仍是入口节点后代。没有新增 App、截图、坐标或命令特例。
- 正向与变化样本继续覆盖 launcher 进入 App、功能页包名别名、后继子目标延续、只读观察身份刷新，以及错误
  session/device/action count、非后代节点和不唯一页面标题撤销 lineage。相关回放 `332/332`、静态编译与
  `git diff --check` 通过。
- 生产代码总量从 `37394` 降至 `37354`，净减 `40` 行；Orchestrator `5652→5159`，App surface domain
  `32→479`，domain 导出 `61→67`。当前仍未达到 `<20000`，最终完整回归仍留到阶段终局。
- 本批仅做离线重构；未重载项目 API、调用在线模型、创建真机会话、执行机械动作，也未启动、停止、重启
  或操作卖家 `main.exe`。下一批必须继续处理 Orchestrator/observer/canonical 等千行级运行切片。

## 24. 第十五批：canonical wire 投影单一化

- canonical 动作协议的 7 类 dataclass wire 投影统一经 `_record_wire` 生成；各类型只保留确有合同差异的
  `omit` 或数值规范化，不再分别手写整份字段字典。候选、claim、relation、affordance、transition、catalog
  的字段名、缺省省略规则和 JSON 值形态保持不变。
- `canonical_action_protocol.py` 从 `2185` 行降至 `2141` 行，生产代码总量从 `37354` 降至 `37310`，净减
  `44` 行，当前仍未达到 `<20000`。这一步没有改变 canonical candidate 编译、动作归属、输入正文权威、
  scope 或 Controller 执行语义。
- canonical—Qwen—Adapter—模块依赖相关回放 `217/217`（`175.272s`）通过，静态编译与
  `git diff --check` 通过。两次先前命令误引用已退役模块/类名，均已按当前测试清单纠正，属于测试命令错误，
  不是生产代码失败。
- 本批仅做离线重构；未重载项目 API、调用在线模型、创建真机会话、执行机械动作，也未操作卖家
  `main.exe`。下一步用 AST 等价检查执行统一 120 字符排版，再继续物理删除千行级重复职责。

## 25. 第十六批：生产模块统一 120 字符排版

- 对 Git 跟踪的 63 个 `poc/agent/**/*.py` 文件执行保守机械排版：仅合并跨行且合并后不超过 120 字符的
  单条简单语句或复合语句头；含注释、显式反斜杠续行和多行 docstring 的区域不改写。
- 每个文件写回前都比较改写前后的完整 `ast.dump(..., include_attributes=False)`，63/63 语义树完全相同；
  这批明确归类为排版收敛，不冒充业务逻辑删除。
- 生产代码物理行数从 `37310` 降至 `33114`，净减 `4196` 行；静态编译、`git diff --check` 和源码结构
  防恢复回归 `46/46`（`154.935s`）通过。当前仍高于 `<20000`，后续必须继续真实删除重复职责。
- 本批未重载项目 API、调用在线模型、创建真机会话、执行机械动作，也未启动、停止、重启或操作卖家
  `main.exe`。下一批回到 Controller/canonical 的动作规格与验证逻辑收敛。

## 26. 第十七批：括号内续行紧凑化

- 在同一 120 字符上限内，对长字典、调用、集合、推导式及复合语句头按逗号和布尔/二元运算边界重新换行；
  含注释、显式反斜杠续行和多行字符串的语句仍不自动改写。函数/类定义头最终由 AST 生成规范签名，避免
  为追求行数保留不清晰的原始拼接空格。
- 当前文件与提交 `85b59f2` 的完整 AST 逐一比较为 `63/63` 完全一致；生产物理行数从 `33114` 降至
  `24148`，净减 `8966` 行。这批仍明确归类为无语义排版收敛，后续还需真实删除至少 `4149` 行重复职责。
- 静态编译、`git diff --check` 通过；源码结构、旧 App 特例防恢复和输入 lineage 相关回归最终
  `96/96`（`160.132s`）通过。首次组合运行仅有一项源码字面守卫因条件表达式被换行而失败，恢复清晰三行
  写法后精确用例与整组均通过，运行 AST 和产品行为从未改变。
- 本批未重载项目 API、调用在线模型、创建真机会话、执行机械动作，也未启动、停止、重启或操作卖家
  `main.exe`。下一批开始真实收敛 Controller/canonical 的重复验证与动作规格。
