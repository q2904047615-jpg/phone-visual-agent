# 21 项协议真机验收台账

更新时间：2026-08-19（Asia/Shanghai）

## 1. 验收合同

- 本轮目标：不是再次证明离线测试，而是让最初 21 项表格中的正式 authority 在当前加载版本、真实画面和真实设备链中得到证据。
- 通过证据：至少包含真实 DeepSeek 任务图、正式 TaskSemanticIR/EffectIntent、真实 Qwen 候选、当前 observation/fingerprint、本地策略决定；需要动作的项目还必须包含恰好一个物理动作、动作后四帧、新 fingerprint、typed receipt 和最终状态。
- 反向项目：确认、unknown、能力 gap、旧 scope、重复/歧义候选等必须以真实服务上的 0 动作拒绝证明，不能为了“真机”故意制造付款、登录、删除或不支持手势。
- 停止条件：单个物理动作失败立即停止该链；同类模型合同失败两次停止采样；不得用换词、App 分支、固定坐标或降低门禁换取通过。

## 2. 当前基线

- 当前提交：`6794bb4 fix: make typed policy the runtime risk authority`。
- 项目 Uvicorn 已从旧 PID `31164` 重载；当前监听服务报告 observer v62、semantic-risk-authority-v2、legacy remote risk diagnostics=false。
- 卖家 `main.exe` 保持 PID `4968`、启动时间 `2026-08-17 09:52:18`，本轮未启动、停止、重启或点击其窗口控件。
- `/api/device`：controller/camera online、busy=false、0 活动会话。
- 0 动作基线：微信文件传输助手聊天页，输入为空，QWERTY direct_latin，画面 stable=true。

## 3. 首个真实失败与唯一缺口

- 原目标：在当前文件传输助手聊天中向上滑动一次查看更早记录；禁止输入、发送和点击消息。
- session：`8236fecdbb384bc19adbba43bbafb9f6`；目录：`poc/output/web/generic_supervised_20260819_121107_8236fecd/`。
- 已通过：DeepSeek 输出 `navigation_only`、`risk_actions=[]`；旧“发……消息”风险正则没有再否决；可信观察唯一包含标题“文件传输助手”和聊天记录容器。
- 失败：Qwen 前本地 exact-text gate 把标题实体当成本次滑动必须绑定的动作控件，返回“当前可信候选中不存在逐字一致文字：文件传输助手”；`physical_actions=0`。
- 根因分类：通用实体关系/当前子目标绑定缺陷。旧 `goal.entities.target_ui_label` 在无坐标/区域动作中仍抢占正式 typed action relation，违反原 21 项第 4、11、13 项的关闭标准。

## 4. 同类样本与通用修复

- 现场：聊天标题“文件传输助手”只证明当前会话身份；`swipe` 的对象是视口/聊天记录区域，不是标题文字。
- 变化样本：Settings 页面标题 + 设置列表滑动；Browser 页面标题 + 页面内容滑动；`back/home/wait/reveal_system_navigation` 的标题同样只能作身份锚点。
- 反例：`tap_semantic/input/long_press/drag` 等元素绑定动作仍必须逐字绑定目标控件；身份缺失或重复继续 0 动作拒绝。
- 修复：当且仅当 active typed `required_action` 为 `swipe/back/home/reveal_system_navigation/wait_for_change` 之一时，`target_ui_label` 只按唯一页面/容器身份解析，不生成元素动作目标；其他动作保持原严格 exact-target 门禁。
- 影响范围：仅 Qwen 本地候选前门与测试，不改变风险、坐标、几何、设备能力、scope 或机械执行。

## 5. 验证顺序

1. 重放现场标题 + swipe，Settings/Browser 变化样本、身份缺失/重复反例、tap 反例。
2. 运行 Qwen/正式视觉 authority/编排最小相关回归。
3. 批次稳定后重载项目 Uvicorn，重新核对设备并使用完全相同原目标创建全新 session。
4. 原目标取得真实 swipe receipt 后，再继续覆盖 21 项矩阵；不能复用旧 session 或旧 scope。

## 6. 环境恢复节点发现的空值/正文耦合缺口

- 为消除软键盘遮挡，新目标只要求收起键盘并只读确认输入框仍为空。session
  `2c238054d55f4c8383a92c204efdb814` 在 DeepSeek 初始图校验阶段失败，
  `physical_actions=0`，错误为“子目标输入状态未绑定 canonical input_text”。
- 现场任务图把收起键盘建模为 `navigation_only`，把空输入框核对建模为后继
  `read_only`，没有输入正文、发送或其他外部效果；旧校验仍把空值视觉状态与输入 payload
  强行绑定。
- 通用修复：精确、单一的“输入框为空/无文字”只读状态不要求虚构 `input_text`；任何混合
  “为空并输入 X”的状态、非空文字状态或实际输入动作仍必须绑定 canonical typed payload。
- 变化样本覆盖中文输入框、搜索框和英文 input field；反例覆盖空值短语夹带未绑定正文。

## 7. 页面实体与页面类型后缀的关系缺口

- 新版重放 session `ab12a497a3ab4233a483da8a26e895ac` 已通过空值状态校验，
  DeepSeek 正确生成 `collapse_keyboard`（`navigation_only`）和后继只读核对，风险为空；
  `physical_actions=0`。
- 可信画面唯一页面标题为“文件传输助手”，规划器把同一页面实体描述为“文件传输助手聊天页面”；
  旧页面身份门要求整个描述逐字等于标题，因此在 Qwen 选择动作前错误阻塞。
- 根因分类：通用实体关系缺陷。页面标题实体与“页面/界面/聊天页面/详情页”等页面类型描述
  不是两个不同实体；但收件人、按钮、输入目标等元素身份仍必须逐字一致，不能改成模糊包含。
- 同类样本：设置标题 + “设置页面”、浏览器标题 + “浏览器页面”、当前标题 + “当前标题详情页”。
  反例：任意中间子串、非页面类型残余、两个可归一到同一描述的身份锚点，均继续 0 动作拒绝。
- 通用修复：仅在 typed 动作为无元素页面动作时，以“唯一标题 + 受限通用页面类型后缀”解析页面身份；
  不放宽收件人身份，不放宽元素绑定动作，不使用 App 名称、坐标或截图特例。

## 8. 上下文相关系统 Back 语义缺口

- 修复后同目标 session `9627b1d1e67240bb8808c49ed9698e8b` 已通过页面身份门；
  可信观察同时证明软键盘可见、输入框聚焦且为空，并识别到“收起键盘”图标，仍为
  `physical_actions=0`。
- 本地 typed authority 正确只提供已验证的 Android `back` 原语，但选择提示只把它写成通用
  `scene_changed`。Qwen 因而把 Back 一律理解为离开聊天页，并以“保持当前页面”约束阻塞。
- 根因分类：通用上下文动作语义缺失。Android Back 在软键盘当前可见且子目标明确要求收起键盘时，
  权威后置语义是“收起当前软键盘并保持当前 App surface”，不是“离开页面”。
- 通用修复：只在本地已经同时证明“当前子目标要求收起键盘 + 当前软键盘可见”时，给唯一 Back
  choice 附加只读 `selection_context`；它只帮助 Qwen 解释该原语，不进入可执行 action、坐标、scope
  或控制器参数。其他 Back 仍保留普通导航含义。
- 变化样本：任意 App 的聚焦输入框 + 可见 QWERTY 键盘；反例：键盘不可见、非当前子目标提到键盘、
  普通返回页面，均不得获得该上下文语义。

## 9. 可滚动视口事实未进入正式观察协议

- 环境恢复正向 session `82916b3867a34ab8b32f1e21000cfbd5` 已成功：恰好一次 Back，
  动作前后各 4 帧，`503d74a6d9523ac21855 -> 4c0768ee9dc9175b69fd`，typed receipt matched，
  revision `1 -> 2`，最终 `succeeded`；键盘收起、原聊天页面和空输入框均得到验证。
- 随后的原始 swipe session `6cd66c6eeaa445309f5557910056cead` 为 0 动作。画面清楚显示按时间
  排列的多条重复消息，观察摘要也报告多条历史消息，但 elements 没有任何
  `states.scrollable=true` 的 container；正式视觉 authority 因而不生成 swipe 候选。
- 根因分类：通用观察合同缺项。正式 candidate 正确要求视觉可滚动证据，但 observer prompt 和
  `UIElement` schema 从未定义怎样报告、约束该证据，导致代码所需状态实际上没有可靠生产者。
- 同类样本：聊天记录、设置列表、搜索结果、信息流、时间线；反例：单张静态卡片、工具栏、看不出
  重复结构或边缘延续的页面，不得标记可滚动。
- 通用修复：观察器只在画面明确存在至少两个重复同类条目，或边缘有被裁切后续内容/连续内容轨时，
  报告一个完整 viewport container，严格状态为 `scrollable=true`、`scroll_axis=vertical|horizontal`、
  `fully_visible=true` 且必须有可见证据。其他 role、false/unknown、缺轴、缺证据可继续作为只读诊断，
  但由正式视觉 authority 拒绝铸造 swipe 权限。
  该事实只生成一次 exploratory swipe affordance；方向仍来自用户 typed action，动作后必须验证内容变化。

## 10. 前台 App 身份审计证据数量的任意上限

- 跨 App session `2add85eda6c04fd6b397eaccb1ee57cf` 已完成 Home 到桌面和点击“设置”两个
  真实动作；第一步 receipt matched，第二步已发生物理动作，但设置页动作后观察在前台 App 身份审计
  解析处失败，终态 `failed`，最后 Home 未执行。
- 原始严格 JSON 的 App ID、置信度和三条证据均合法，唯一冲突是旧协议任意限制 evidence 只能 1–2 条；
  Qwen 返回标题、搜索框、设置菜单三条互相一致的可见身份线索。
- 根因分类：只读身份子协议的非必要格式边界。三条短证据不会扩大动作权限，反而提高 App 身份可核验性；
  丢弃第三条属于语义裁剪，不应由 normalizer 静默执行。
- 通用修复：前台 App 身份审计明确允许 1–3 条、每条仍须通过既有安全过滤和 120 字上限；0 条、4 条、
  坐标/动作建议、重复键、额外字段、低置信和占位 App ID 继续失败关闭。其他系统 UI/方向审计不变。

## 11. 输入准备状态与正文结果状态的粒度缺口

- 浏览器安全输入目标要求：打开浏览器，找到唯一空白搜索或地址输入框，输入 `live21`，但不搜索、
  不提交。session `62e74afa...` 在 DeepSeek 初始图本地校验前停止，`physical_actions=0`。
- DeepSeek 已把任务通用地拆为 `open_browser -> locate_input -> type_text`；`locate_input` 只要求
  “输入框可见且为空”并明确不输入，真正的 `type_text` 才绑定 canonical `input_text=live21`。
  旧校验却把前者也归为正文结果，报“子目标输入状态未绑定 canonical input_text”。
- 根因分类：通用语义粒度缺陷。可见、可编辑、聚焦且空白是可逆的输入载体准备状态，不是输入 payload；
  canonical 正文只应约束实际输入及输入后非空结果。
- 同类样本：任意 App 的空白搜索框、消息框、笔记文本框，以及英文 `input field is visible and empty`；
  反例包括“为空并输入 secret”“可见且已显示非 canonical 文本”及任何发送、提交、保存结果。
- 通用修复：只豁免完整语义严格等于“输入载体可见/聚焦/可编辑并且为空”的状态；混入正向输入、
  非空内容或外部效果的句子继续走原 canonical 文本和风险门禁。重放必须使用原浏览器目标，
  不增加 Browser 分支、固定控件或坐标。

## 12. 浏览器安全输入重放结果

- 修复提交 `191136f` 经 Python 完整回归 `1520/1520` 后加载。首次请求因旧 Uvicorn PID 21688
  仍占用 8765 而实际命中旧代码，0 动作；查明后仅替换项目 Uvicorn，卖家 `main.exe` 仍为
  PID 4968、启动时间不变。
- 新服务 session `b4f55ce7d2d14bd2abee6349ebcf574a` 已通过输入准备状态校验。动作 1 从 Launcher
  唯一“浏览器”图标进入浏览器新闻首页，动作 2 在未找到空白搜索输入框后执行 Home 返回桌面；
  两步均各有前后四帧、新 observation/fingerprint、matched typed receipt，无自动动作重试。
- 最终为 `blocked`、revision 4、physical_actions 2，不能记作输入成功。浏览器首页顶部虽有搜索栏，
  当前观察将其作为带动态内容的新闻页顶部区域，未报告唯一空白 input；任务因而安全返回桌面并停止。
- 根因暂归类为单次视觉/手机起始状态样本，不足以证明生产代码缺陷。本轮不为该浏览器页面加控件、
  文字或坐标特例，也不继续在线重试；输入事务正向证据改用已知具有唯一空白输入框的另一个真实 App，
  同时保留本样本作为跨 App 变化反例。

## 13. 微信跨子目标完成声明的安全拒绝

- session `24fdf2a58fe44c3babb0b0f91781657c` 从 Launcher 唯一“微信”图标执行一次
  `tap_semantic`，动作前后各四帧，fingerprint `e2251c50d131509e2734 -> 354939bff77d0cfc0a83`，
  typed receipt matched；手机实际直接恢复到上次打开的“文件传输助手”聊天页，输入框为空。
- 动作后观察摘要看到了该聊天页，但当前 `open_wechat` 子目标的结构化元素集合为空；DeepSeek
  试图在同一次 revision 中同时完成后继 `navigate_to_file_transfer`，本地因缺少该命名页面的
  structured visual identity anchor 拒绝整个 revision。终态 `blocked`、revision 1、physical_actions 1；
  没有输入或发送。
- 这说明命名页面完成门在真实链路中失败关闭，没有把摘要自由文本直接当 authority；同时也说明
  一个后继页面已经意外满足时，旧规划无法只接受有证据的前缀推进。该现象先保留为任务图重规划
  变化样本，不追加微信特例。输入事务将从当前已证实的空白输入页启动全新独立会话，只验收
  `input_verified_text` 本身。

## 14. 页面身份实体抢占输入控件目标

- 从已证实的文件传输助手空白输入页启动 session `94e0e95f5ac94ed1a52f9f997fb4e8d`。
  DeepSeek 正确保留 `input_text=live21`，但旧传输字段 `target_ui_label` 被写为
  “文件传输助手聊天页面”；可信观察同时有唯一标题 `文件传输助手` 和唯一空白 input。
- 本地 required-action 编译仅因“消息输入框可见”含有“输入”二字，就错误铸造
  `input_verified_text`；随后 exact-text gate 把页面身份描述当作输入控件 label，报
  “当前可信候选中不存在逐字一致文字：文件传输助手聊天页面”。终态 0 动作。
- 根因分类：两个通用 typed relation 缺陷，而非微信或措辞特例：输入载体名词不能证明输入动作；
  由唯一页面标题 + 受限页面类型后缀证明的 `target_ui_label` 是 surface identity，不能抢占
  同一子目标中的 input/button/list 等元素目标。
- 变化样本：任意 App 的“input field visible”不能铸造输入动作；“在 input field 中输入 X”必须铸造；
  “设置页面”由唯一标题“设置”证明时只作 surface identity，真正的输入/按钮动作仍绑定独立
  formal candidate。无唯一标题、两个标题、非页面后缀、真实控件逐字 label 仍失败关闭。
- 通用修复范围：`task_semantic_ir` 的 required-action 词法边界、Qwen 的 surface descriptor
  关系解析、执行策略对同一结构化身份关系的复核及正反测试；不改 App、坐标、风险和机械传输。

## 15. 分段输入被高层重规划打断

- 提交 `30e1182` 经完整回归 `1524/1524` 后加载。同目标新 session
  `c4565e7198fb49e9b66add91ac54ea60` 已通过页面身份与输入目标关系门：动作 1 聚焦唯一空白输入框，
  动作 2 以正式 `input_verified_text` 输入第一个确定性片段 `live`；两步均 matched、各有前后四帧，
  动作后输入框逐字为 `live`，发送按钮未触发。
- canonical payload 为 `live21`。设备 profile 按字母片段 + 独立可见数字键分段是预期设计；但旧编排在
  每个已验证片段后都调用 DeepSeek 高层重规划。第二片段后 DeepSeek 返回无唯一 active 子目标的
  非法 revision，导致会话 `blocked`、revision 4、physical_actions 2，剩余 `21` 未输入。
- 根因分类：通用输入事务与高层任务图的职责边界缺陷。一个 canonical 输入子目标内部的字母、数字、
  符号、大小写/布局切换属于本地确定性微步骤；每步仍需新观察、新 scope 和 typed receipt，但不应让
  远程高层规划器在正文尚未完成时重写子目标。
- 通用修复：仅当动作前后唯一 input、精确 prior/expected value、canonical 前缀、动作类型及本地审计
  全部一致时，保留同一高层 revision，消费旧 authority 后基于新 observation 生成下一唯一 scope；
  不把前缀 receipt 当作子目标完成证据。最终 canonical 值出现后才让 Qwen/DeepSeek复核完成。
  任一错字、非前缀、多个输入框、外部影响、缺少键位审计或 fingerprint 不一致继续立即阻塞。

## 16. 输入事务微步骤离线闭环

- 编排器仅在 `navigation_only`、恰好 1 次 matched 物理动作、动作前后 fingerprint 与可信观察一致、
  canonical `input_text` 前缀正确、唯一聚焦 input 与严格 `element_state` 后置条件全部一致时，识别
  `input_verified_text`、逐字键、输入法候选、布局/大小写/输入模式切换为同一输入事务微步骤。
- 微步骤不调用 DeepSeek、不生成可完成高层子目标的 controller evidence ref，也不修改高层 revision；
  但旧 Qwen 决策和确认权威仍立即作废，动作后用新四帧、新 observation/fingerprint 重新调用 Qwen，
  经本地策略后铸造新的单次 confirmation scope。最终 canonical 值出现后仍走 Qwen 完成候选和
  DeepSeek 全任务复核。
- 正向回归覆盖 `"" -> "live"` 与 `"live" -> "live2"`；反向覆盖错误结果值；编排集成回归证明
  DeepSeek replan 调用为 0、旧/new scope 观察和动作摘要不同。相关编排测试 `187/187`，完整离线回归
  `1528/1528` 通过。代码尚待加载后在当前真实输入状态继续 `live -> live2 -> live21`，不得发送。

## 17. 本地输入辅助键在确认观察中丢失

- 加载提交 `82c6929` 后新 session `ae9893414d414e4bad296dc4034e697c` 从真实 `live` 状态启动。
  初始四帧严格识别唯一聚焦输入框、当前值 `live`、唯一完整数字键 `2`，Qwen 与本地策略共同形成
  `input_exact_literal_key`、预期值 `live2` 的正式候选；执行前第二组四帧与规划帧通过本地画面身份门，
  但通用 observer 的第二次全场语义观察没有重建本地输入辅助键，`_rebind_action` 因匹配 0 个拒绝。
  终态 `needs_reobservation`、`physical_actions=0`，手机未被点击。
- 根因分类：确认数据流边界缺陷。本地输入结构审计生成的逐字键/输入法候选/布局/大小写/输入模式键
  是严格绑定 canonical 输入事务的临时候选；全场模型第二次采样可能省略普通键，不能把这种省略当作
  像素已变化，也不能直接复用旧坐标执行。
- 同类样本：逐字数字/符号键、唯一输入法候选、键盘布局键、大小写键、输入模式键；变化反例为普通
  App 按钮、缺少本地输入事务 states 的键、确认帧 delta 超限、独立 crop 审计没有唯一逐字控件。
- 通用修复合同：仅当确认帧本地身份已通过、原目标属于上述本地输入辅助集合且所有 prior/expected/input
  绑定完整时，允许用原候选作为只读 descriptor，在最新确认帧上执行独立单 crop 几何审计；审计必须
  唯一逐字确认 label/role/完整控件并生成最新 fingerprint 的本地 attestation，之后仍走原控制器与动作后
  精确值验证。任何普通目标、0/多匹配、低置信、越界、旧 fingerprint 或状态缺项继续 0 动作拒绝。
