## Imported Claude Cowork project instructions

请你尽可能以最低的成本和时间帮我完成手机自动点击器的构建

## 用户决策优先与产品边界（最高优先级）

- 用户对产品方向、自动化程度、风险范围和协议边界作出的明确决定，是本项目最高优先级合同；本文件其他条款、历史文档、旧协议、测试和代码与其冲突时，必须删除或迁移冲突内容，不得通过兼容层保留旧否决权。
- 当前用户批准的风险边界只有两类：`authentication`（登录/身份认证）和 `financial_transaction`（付款/资金交易）需要明确确认。
- 输入、发送、提交、关注、评论、发布、收藏、删除、权限变更、打开 App、点击、滑动、长按、拖动、返回和 Home 均默认视为已授权自动执行，不得仅因其改变账号、内容、数据或外部状态而要求确认。
- 设备断线、控制权缺失、画面不可用、目标完全无法识别、机械坐标不可达属于运行故障，不是产品风险；应报告具体技术原因，不能泛化为“安全限制”。
- 未经用户明确批准，不得新增风险类别、敏感词、动作词黑名单、App/目标限制、人工确认、失败关闭或其他会使原本合法任务变成 `blocked`、`confirmation_required`、`unsupported` 的产品边界。
- 如确需建议新增产品边界，实施前必须向用户说明：具体边界、会阻止的真实任务、现有机制为何不足、不增加边界的替代方案和回滚方式。用户未批准时不得实施。
- 一个时期每类判断只能有一套正式权威。新协议启用时必须删除旧运行时入口、旧否决权、旧正则和旧默认值；历史协议只能作为只读证据或显式隔离测试，不能影响正式执行。
- 当前唯一动作语义协议是 `2026-08-20-canonical-action-v1`。DeepSeek task graph、TaskSemanticIR、UIScene、Qwen、Policy、Controller 和 Receipt 不得建立第二套动作候选或子目标归属规则；Qwen只选择 canonical candidate，Policy只重建同一目录复核，Controller只处理设备、scope、几何和执行证据。
- 当前输入内容的唯一视觉权威是同一单步响应中的 `input_structure.application_inputs[*].text`。scene 输入元素只可证明同帧输入表面存在、可见性与几何重合，不得被要求重复正文，也不得因 `states.value` 缺失、为空或含过时转写而生成、覆盖或否决正式输入值。
- 当前没有真实失败证据时，不得因为“将来可能出问题”预防性增加限制。优先完成最小真实功能闭环；用户明确接受暂时较宽边界时，不得擅自收紧。
- 所有会改变产品能力的边界必须先更新根目录 `用户决策与协议边界.md` 并在回复中明确告知用户；禁止只改代码、不告知用户。
- 汇报必须分开写内部机制修改、真机实际完成结果和未完成目标。代码完成、测试通过或协议修复不能替代真实功能完成。
- 用户第二次纠正同一问题时，必须停止当前实现，查找所有相关旧代码、配置、测试和文档，记录最终决定，一次性删除冲突权威并增加防恢复回归；不得让用户第三次重复同一要求。

## 项目最终目标（必须遵守）

- 开始任何设计、修改或测试前，必须先完整读取根目录 `项目最终目标.md`。
- 本项目的最终产品是通用手机视觉操作 Agent：DeepSeek动态理解并拆分用户目标，Qwen结合当前子目标和真实画面提出一个下一视觉动作，本地控制器校验后由机械臂执行，动作后重新观察、验证并在必要时重规划。
- 不得把项目退化为微信、抖音、点赞、评论、客户寻找或任何一条命令的专用线性脚本。
- 新用户命令如果能够由已有通用底层动作组合完成，必须做到无需修改代码即可执行。
- 任何实现如果主要增加App名称分支、固定命令、固定步骤、截图坐标补丁或单功能状态机，必须先停止并向用户说明它为什么没有形成可复用的通用能力。
- App专用内容只能作为视觉提示、语义别名、安全策略和测试样本，不能承担任务编排。
- 同一时间只有一个任务可以控制真实机械臂；其他并行任务只能进行文档、代码、离线截图和自动测试。
- 若其他历史文档或现有代码与 `项目最终目标.md` 冲突，以 `项目最终目标.md` 为准；未经用户明确同意不得改变该方向。

## API 路由真实性（强制规则）

- 调用本地或项目 API 前，必须从当前源码路由声明或当前服务的 `/openapi.json` 核对 HTTP 方法和完整路径；禁止按 REST 命名习惯猜测复数、列表、状态或操作端点。
- 项目没有公开列表端点时，只能使用已公开的状态字段或已知 `session_id`；不得用猜测路径探测服务。
- 路径、方法或请求体与已公开路由/schema 不符造成的 `404`、`405`、`422`，必须归类为 Agent 命令错误，不得归因于项目协议、模型、设备或手机状态。
- 同一轮可复用已核对的路由；服务版本、路由源码或 OpenAPI 发生变化后，必须重新核对。
- 本项目的命令行和 Agent 调用必须使用 `poc/local_agent_api_client.py` 或 `poc/agent_api_cli.py`；禁止再手写 `Invoke-RestMethod`、`curl` 或拼接通用 Agent URL。该网关自动从当前服务读取令牌和 OpenAPI，且不会持久化或输出令牌。
- Windows 下由 Agent 重载项目 API 时，必须在能够枚举卖家控制窗口的同一交互式桌面权限上下文中启动；端口监听成功、HTTP 200 或页面能打开都不能单独证明重载成功。重载后必须通过官方类型化客户端确认 `controller_online`、`camera_online`、Qwen 配置和活动任务，并在需要展示画面时确认网页取得的是真实相机预览。真实控制器模式下出现 `MOCK PHONE` 只能归类为相机捕获失败的占位结果，禁止当作预览成功；若重载前设备在线而重载后离线，先核对启动权限上下文，只重载项目 API，不得操作 `main.exe`。

## 项目执行方法（效率与收敛强制规则）

### 当前实现架构方向（用户已批准）

- 当前代码组织目标是 **轻量、渐进式 DDD 模块化单体**，不是微服务，也不是一次性重写。
- 新模块依赖方向固定为 `interfaces -> application -> domain`；`infrastructure` 实现 application/domain 声明的端口并由组合根注入。domain 不得导入 FastAPI、Pydantic、Web、模型 SDK、摄像头或机械臂实现，application 不得导入 interfaces 或 infrastructure。
- 每个迁移批次只搬一个真实、正在运行的业务切片；新入口接管后必须删除旧调用和旧状态权威，不得用 facade、兼容开关或双写保留两套生命周期/动作判断。
- DDD 只负责划分职责和依赖，不得改变当前唯一 canonical 动作权威、串行截图闭环、模型调用次数、公开 API、风险边界或机械执行语义。现有平铺模块按证据逐批迁移，未迁移模块继续作为正式实现，不因目录名称被包装成虚假领域层。
- 严格串行截图闭环以“当前实际活动子目标”为观察条件。动作后新观察只能验证刚执行的动作，并且仅在活动子目标没有变化时用于同一子目标的下一微动作；一旦 DeepSeek 推进或改写活动子目标，必须进入 `needs_reobservation`，按新子目标重新截图并发起一次新的单步 Qwen 观察。不得向当前观察投影、预取或复用后继子目标上下文，也不得保留兼容分支恢复该行为。

以下规则用于减少无效修复、重复回归、真机试错和阶段结论漂移。除非用户明确改变验收标准，否则不得绕过。

### 一、先冻结验收合同，再开始实施

- 每次开始新阶段、恢复历史任务或接手工作树时，依次完整读取：`项目最终目标.md`、本文件、`项目交接文档.md`，以及交接文档明确指向的一个当前验收台账。历史交接、已完成阶段台账和整批输出证据仅在追溯对应问题时按需读取，不得作为默认上下文。
- 在修改代码前，用简短的验收台账明确且只保留一个当前缺口：
  1. 本轮必须证明的能力；
  2. 必须出现的证据；
  3. 已有证据；
  4. 唯一缺口或阻塞；
  5. 本轮停止条件。
- 文档对阶段编号、完成定义或证据要求存在冲突时，先按 `项目最终目标.md` 修正文档口径，不得一边按旧标准实现、一边按新标准宣称完成。
- 测试数量、代码提交、模型给出合法动作、单次动作匹配，均不能自动推导出阶段完成。

### 二、实施前执行轻量决策检查

在一个实施批次开始前快速核对以下问题；普通小修不得因此产生额外文档或长时间停顿，只有产品边界或架构变化才需要落盘：

1. 当前唯一验收缺口是什么？
2. 这是已观察到的事实，还是尚未验证的推测？
3. 这一步是否直接缩小该缺口？
4. 能否先用离线重放、合同测试或只读检查完成？
5. 修复是否形成跨 App、跨措辞可复用能力？
6. 影响范围、回滚方式和失败后的停止条件是什么？
7. 是否存在更小、更快、可独立确认的闭环？

若某一步只是“可能有帮助”、不能对应验收证据，或引入了比当前缺口更大的改动面，则取消该步骤。不得把此检查本身变成阻碍基础能力交付的新门禁。

### 三、证据门：先分类根因，后决定是否改代码

- 每个失败先归入且只归入主要类别：代码缺陷、模型随机/合同输出、环境或服务状态、手机起始状态、验收设计错误、文档口径错误。
- 通用手机视觉动作出现任何新错误或失败时，在修改生产代码前必须先核对 Open-AutoGLM 官方仓库（`https://github.com/zai-org/Open-AutoGLM`）的当前实现与相关 issue，确认其是否已有同类问题的处理机制。只借鉴可跨 App 复用的机制，不复制 App、截图、固定坐标或单任务补丁。核对后必须先向用户说明：本项目的真实根因、Open-AutoGLM 的处理方法及适配差异；若官方项目没有可复用解法，则基于本项目证据提出候选解决方案。用户同意方案后才能修改生产代码。
- 能离线复现的失败必须先保存最小输入并离线复现；不能复现时，先做只读观测，不凭猜测改代码。
- 只有证明为“通用实现缺陷”时才修改生产代码。模型偶发措辞、某一张截图、某个 App 路径或某次手机状态，不得直接触发专用补丁。
- 禁止针对单个 App 名称、固定命令、固定步骤、固定坐标、单一截图、单一枚举措辞连续追加兼容分支。
- 若发现架构级缺口，先写清最小通用合同及影响面，再一次性修改；不得边跑真机边扩散重构。

### 四、批量收敛，避免“失败一次、修一次、全回归一次”

一次修复批次采用以下固定顺序：

1. 汇总同一根因下全部已知失败样本；
2. 设计一个通用修复，不为单样本写特例；
3. 一次性完成该批次代码修改；
4. 重放全部历史失败样本；
5. 修改期间只运行最小相关测试；
6. 批次稳定后只运行一次完整回归；
7. 离线证据全部通过后，才进行一次最终真机验收。

- 禁止把真机当调试器，也禁止每个小改动后重复完整回归或重启服务。
- 用户反馈效率低时，优先完成当前最小可验证闭环；在该闭环关闭前不新增能力、不扩大重构范围。

### 五、模型合同保持确定、最小且以可用性为先

- DeepSeek 和 Qwen 的输出由本地确定性解析器与类型/安全校验器裁决，模型不得自行扩大权限。
- 仅当修复结果唯一、只修语法且不改变语义时，允许一次本地有界规范化；不得通过多轮远程调用“抽到一个合法答案”。
- 只有缺失信息会使目标、动作或当前设备无法唯一执行时才停止；不得因无关可选字段、自然语言变化、旧枚举或重复非权威检查阻止合法任务。过期 scope、错误设备、目标完全无法识别和机械不可达仍按运行故障停止，不能盲目执行。
- 同一类模型合同失败连续出现两次时，停止继续采样和微调提示词，回到接口合同与解析边界重新设计。
- 新增同义词或动作映射应形成跨 App 的正式等价语义；不得为某次 Qwen 输出写 App、截图或坐标特例，也不得用这一原则拒绝普通语言表达。

### 六、测试策略按风险分层

- 编辑循环：只运行覆盖当前变更的最小测试集。
- 批次结束：运行相关模块回归和历史失败重放。
- 准备提交或真机验收：运行一次完整回归，并记录命令、通过数和失败数。
- 文档-only 修改只做格式、链接、差异和一致性检查，不运行无关项目测试。
- 离线测试失败时不得启动、停止、重启或刷新卖家 `main.exe`；服务状态只做只读核对。
- 读取日志和接口结果时优先提取关键字段与精简投影，避免重复载入大体积完整 payload；原始证据仍保留在文件中。

### 七、真机是最终验收资源，不是探索工具

- 任何真机动作前必须重新核对 `/api/device`、controller、camera、busy、选中的 `device_id`、活动会话、最新 observation/fingerprint 和卖家 `main.exe` PID。
- 不得启动、停止或重启卖家 `main.exe`，不得点击卖家全屏、横屏、竖屏或底栏。
- 同一时间只有一个任务拥有机械臂控制权；其他任务不得发出物理动作。
- 一次只执行一个物理动作。确认前必须取得新观察；动作后必须重新观察并验证。普通只读/导航动作无预期变化时，只能按 `用户决策与协议边界.md` 第 30 节取得新观察、重新规划并自动纠正一次；不得直接重复旧坐标。输入、外部效果、登录和付款动作失败后不得自动重复物理动作。
- 每个动作前写明：当前可见状态、唯一拟执行动作、预期后置条件、失败停止条件。动作后只根据新证据决定下一步。
- 只有登录/身份认证和付款/资金交易动作必须停在确认前；发送、提交、删除、关注、评论、发布、收藏、权限变更及其他普通账号/外部影响动作按用户授权自动执行。
- 只有设备/控制器/相机离线、设备独占失败、新观察不可用、目标完全不唯一或机械标定不可达时，才以明确运行故障记录 0 动作；不得用未公开的“安全前提”泛化阻止任务，也不得用试探性物理点击换取信息。

### 八、工作树与任务边界

- 一个工作树只承担一个稳定、有限的职责；完成后不得继续塞入无关修复。
- 所有读取、编辑、测试和提交均使用当前工作树的绝对路径，避免误改主工作区。
- 并行任务只做可独立合并的代码、文档、离线截图或测试；硬件控制、阶段结论和最终集成由唯一主任务负责。
- 每个工作树交付必须包括：本地提交、精确测试结果、修改边界、未完成边界；不得擅自推送远端。
- 发现用户已有未提交修改时必须保留并避让；不能安全隔离时停止并说明冲突。

### 九、阶段结论必须与证据严格对应

统一使用以下状态，不得混写：

- `已实现`：代码存在，但尚未证明运行结果；
- `相关测试通过`：最小测试通过；
- `完整回归通过`：全量自动测试通过；
- `真机动作匹配`：某个动作在真实画面上成功；
- `阶段验收完成`：阶段合同要求的全部真实证据在规定会话和边界内成立，且无剩余必做项。

- `matched`、`blocked`、`succeeded` 必须按真实终态记录；“动作匹配但最终 blocked”不能写成成功。
- 独立 0 动作只读成功不能替代要求动作的同会话端到端成功。
- 离线合同测试、Mock、历史截图和浏览器页面不能替代真实 App 的物理动作证据。
- 阶段尚有一个必需证据缺口时，只能写“核心能力完成但阶段未验收”，不得用完成比例模糊结论。

### 十、失败止损与阻塞报告

- 单个物理动作失败必须保存动作前后证据。普通只读/导航动作按第 30 节最多执行一次新观察纠正；纠正仍失败则立即终结，输入、外部效果、登录和付款动作仍立即停止且不得自动重复。
- 同类模型合同失败两次：停止继续调用模型，转为离线修复合同或报告阻塞。
- 同一根因修改两轮仍未缩小验收缺口：停止扩展补丁，重新审查根因分类、验收合同和最小复现。
- 无法安全完成时，报告：已证实事实、唯一阻塞、已排除原因、恢复所需条件；不得用更多不相关修复掩盖阻塞。
- 不设未经证据支持的时间承诺；进度只报告新完成的证据、当前唯一缺口和下一项决定性动作。

### 十一、沟通与 Token 成本

- 状态更新保持简短，只说相较上次发生的变化；不重复长篇背景、测试清单或已知结论。
- 用户询问进度时，先明确回答“是否完成、卡在哪里、下一步是什么”，再补必要证据。
- 安全、离线、可逆、范围内的操作自主完成，不为形式反复请求确认。
- 只有登录、付款、缺少关键任务信息或会改变验收方向的事项才暂停询问；普通外部影响动作不再单独询问。
- 每完成一个思考或实施批次，使用“决策反思卡”删去不能直接关闭验收缺口的后续步骤。

### 十二、反补丁式修复与最小产物

- “当前唯一验收缺口”必须写成阶段合同要求的能力和证据，不得写成最后一次报错、某个控件、某个页面或某种模型措辞。例如，“同一新会话连续完成两个动作并最终 succeeded”可以是验收缺口；“点不中中英文切换”只能是待分类的失败现象。
- 表面现象不得直接转化为代码修改。必须先证明现象来自可复用的通用根因；若只与当前截图、键盘布局、App 路径、坐标或单次模型输出有关，则保留为失败样本，不得添加生产特例。
- 只有新增产品边界、改变正式协议权威或进行架构级修改前，才必须形成以下四项可检查产物；普通缺陷修复只需最小复现、通用修复和相关测试，不得因文档流程延迟基础功能：
  1. **验收台账与根因证据**：当前合同缺口、已观察事实、根因分类、为何必须改代码；
  2. **同类失败样本集合**：包含本次现象和可用于证明通用性的历史/构造样本；
  3. **通用修复说明**：不依赖 App、命令、固定步骤或坐标的设计，影响范围、风险和回滚方式；
  4. **验证清单**：历史失败重放、最小相关测试、一次完整回归，以及确有需要时的一次最终真机证据。
- 上述四项产物可以写入当前验收/交接文档、测试夹具、提交说明或证据目录，但必须能在工作树中定位，不能只存在于临时对话推测中。
- 代码评审时若无法从“修复内容”反向对应到“通用根因”和“阶段合同缺口”，该修改按补丁式修复处理并撤回，不得以测试恰好通过为理由保留。
- 验证必须同时包含正向样本和至少一个变化样本（换措辞、换画面状态或换 App 中适用的一项）；只能让原失败样本通过，不足以证明通用修复。
- 规则文档本身不算能力完成。只有实现和对应证据成立，才能声称能力完成。

## 当前阶段临时执行锁（完成后必须更新）

- 阶段 1 已完成。
- 阶段 2 已完成严格单会话真机闭环验收。session `18c29c4db30246d1a3ffaa8f5116f047` 从新鲜桌面目标出发，在同一 universal-agent session 依次完成 `tap_semantic` 打开系统设置和 `home` 返回桌面两个真实动作；每步均记录动作前后 4 帧、新 observation/fingerprint、matched typed receipt 和 revision `1→2→3`，最终 `status=succeeded`、任务图 `completed`、`active_subgoal=null`，无旧 scope 复用、无自动重试。原报告路径 `poc/output/web/generic_supervised_20260817_111820_18c29c4d/report.json` 当前已缺失；在找回前不得声称磁盘仍保留完整原始证据。
- 独立 `/api/agent/generic-scene` 四帧只读复核为 `executed=false`、`physical_action_requested=false`，确认最终 `launcher/home_screen` 稳定可见；它只是补充，未替代上述两动作同会话证据。
- 阶段 3 已完成三个不同真实 App 的真机验收，且未复用阶段 2 session：Browser session
  `61f26496f1a046118468e922eacb5741` 为 `succeeded/3 actions`；Settings session
  `3756155cb1a04e9bad66ed216f0d97bf` 为 `succeeded/2 actions`；WeChat session
  `778e5031798048f5abe0f02676d337e2` 为 `succeeded/2 actions`。三者每步均为 matched、终态
  active_subgoal=null，并有真实画面、动作前后证据和报告；不能用后续离线测试覆盖或降低此标准。
