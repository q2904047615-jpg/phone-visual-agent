## Imported Claude Cowork project instructions

请你尽可能以最低的成本和时间帮我完成手机自动点击器的构建

## 项目最终目标（必须遵守）

- 开始任何设计、修改或测试前，必须先完整读取根目录 `项目最终目标.md`。
- 本项目的最终产品是通用手机视觉操作 Agent：DeepSeek动态理解并拆分用户目标，Qwen结合当前子目标和真实画面提出一个下一视觉动作，本地控制器校验后由机械臂执行，动作后重新观察、验证并在必要时重规划。
- 不得把项目退化为微信、抖音、点赞、评论、客户寻找或任何一条命令的专用线性脚本。
- 新用户命令如果能够由已有通用底层动作组合完成，必须做到无需修改代码即可执行。
- 任何实现如果主要增加App名称分支、固定命令、固定步骤、截图坐标补丁或单功能状态机，必须先停止并向用户说明它为什么没有形成可复用的通用能力。
- App专用内容只能作为视觉提示、语义别名、安全策略和测试样本，不能承担任务编排。
- 同一时间只有一个任务可以控制真实机械臂；其他并行任务只能进行文档、代码、离线截图和自动测试。
- 若其他历史文档或现有代码与 `项目最终目标.md` 冲突，以 `项目最终目标.md` 为准；未经用户明确同意不得改变该方向。

## 项目执行方法（效率与收敛强制规则）

以下规则用于减少无效修复、重复回归、真机试错和阶段结论漂移。除非用户明确改变验收标准，否则不得绕过。

### 一、先冻结验收合同，再开始实施

- 每次开始新阶段、恢复历史任务或接手工作树时，依次完整读取：`项目最终目标.md`、`项目交接文档.md`、当前阶段交接/验收文档，以及本文件。
- 在修改代码前，用简短的验收台账明确且只保留一个当前缺口：
  1. 本轮必须证明的能力；
  2. 必须出现的证据；
  3. 已有证据；
  4. 唯一缺口或阻塞；
  5. 本轮停止条件。
- 文档对阶段编号、完成定义或证据要求存在冲突时，先按 `项目最终目标.md` 修正文档口径，不得一边按旧标准实现、一边按新标准宣称完成。
- 测试数量、代码提交、模型给出合法动作、单次动作匹配，均不能自动推导出阶段完成。

### 二、每次实施前执行“决策反思卡”

在每一次代码修改、模型调用或真机动作前，先回答以下问题；答案不清楚时不得实施：

1. 当前唯一验收缺口是什么？
2. 这是已观察到的事实，还是尚未验证的推测？
3. 这一步是否直接缩小该缺口？
4. 能否先用离线重放、合同测试或只读检查完成？
5. 修复是否形成跨 App、跨措辞可复用能力？
6. 影响范围、回滚方式和失败后的停止条件是什么？
7. 是否存在更小、更快、可独立确认的闭环？

若某一步只是“可能有帮助”、不能对应验收证据，或引入了比当前缺口更大的改动面，则取消该步骤。

### 三、证据门：先分类根因，后决定是否改代码

- 每个失败先归入且只归入主要类别：代码缺陷、模型随机/合同输出、环境或服务状态、手机起始状态、验收设计错误、文档口径错误。
- 能离线复现的失败必须先保存最小输入并离线复现；不能复现时，先做只读观测，不凭猜测改代码。
- 只有证明为“通用实现缺陷”时才修改生产代码。模型偶发措辞、某一张截图、某个 App 路径或某次手机状态，不得直接触发专用补丁。
- 禁止针对单个 App 名称、固定命令、固定步骤、固定坐标、单一截图、单一枚举措辞连续追加兼容分支。
- 若发现架构级缺口，先写清最小通用合同及影响面，再一次性修改；不得边跑真机边扩散重构。

### 四、批量收敛，避免“失败一次、修一次、全回归一次”

一次修复批次采用以下固定顺序：

1. 汇总同一根因下全部已知失败样本；
2. 设计一个通用修复，不为单样本写特例；
3. 一次性完成该批次代码修改；
4. 重放全部历史失败样本；
5. 修改期间只运行最小相关测试；
6. 批次稳定后只运行一次完整回归；
7. 离线证据全部通过后，才进行一次最终真机验收。

- 禁止把真机当调试器，也禁止每个小改动后重复完整回归或重启服务。
- 用户反馈效率低时，优先完成当前最小可验证闭环；在该闭环关闭前不新增能力、不扩大重构范围。

### 五、模型合同保持严格、确定、失败关闭

- DeepSeek 和 Qwen 的输出由本地确定性解析器与类型/安全校验器裁决，模型不得自行扩大权限。
- 仅当修复结果唯一、只修语法且不改变语义时，允许一次本地有界规范化；不得通过多轮远程调用“抽到一个合法答案”。
- 缺字段、歧义、过期 scope、观察不一致或不安全动作一律失败关闭，不得自动猜测或自动重试。
- 同一类模型合同失败连续出现两次时，停止继续采样和微调提示词，回到接口合同与解析边界重新设计。
- 新增同义词或动作映射前，必须证明它是跨 App 的正式等价语义；不得为某次 Qwen 输出单独放宽协议。

### 六、测试策略按风险分层

- 编辑循环：只运行覆盖当前变更的最小测试集。
- 批次结束：运行相关模块回归和历史失败重放。
- 准备提交或真机验收：运行一次完整回归，并记录命令、通过数和失败数。
- 文档-only 修改只做格式、链接、差异和一致性检查，不运行无关项目测试。
- 离线测试失败时不得启动、停止、重启或刷新卖家 `main.exe`；服务状态只做只读核对。
- 读取日志和接口结果时优先提取关键字段与精简投影，避免重复载入大体积完整 payload；原始证据仍保留在文件中。

### 七、真机是最终验收资源，不是探索工具

- 任何真机动作前必须重新核对 `/api/device`、controller、camera、busy、选中的 `device_id`、活动会话、最新 observation/fingerprint 和卖家 `main.exe` PID。
- 不得启动、停止或重启卖家 `main.exe`，不得点击卖家全屏、横屏、竖屏或底栏。
- 同一时间只有一个任务拥有机械臂控制权；其他任务不得发出物理动作。
- 一次只执行一个物理动作。确认前必须取得新观察；动作后必须重新观察并验证；失败不得自动重试。
- 每个动作前写明：当前可见状态、唯一拟执行动作、预期后置条件、失败停止条件。动作后只根据新证据决定下一步。
- 发送、提交、删除、关注、评论、支付或其他账号/外部影响动作，只能准备到确认前；没有用户明确确认不得执行。
- 如果安全前提、控制权或新观察任一项不成立，记录为 0 动作阻塞，不用试探性点击换取信息。

### 八、工作树与任务边界

- 一个工作树只承担一个稳定、有限的职责；完成后不得继续塞入无关修复。
- 所有读取、编辑、测试和提交均使用当前工作树的绝对路径，避免误改主工作区。
- 并行任务只做可独立合并的代码、文档、离线截图或测试；硬件控制、阶段结论和最终集成由唯一主任务负责。
- 每个工作树交付必须包括：本地提交、精确测试结果、修改边界、未完成边界；不得擅自推送远端。
- 发现用户已有未提交修改时必须保留并避让；不能安全隔离时停止并说明冲突。

### 九、阶段结论必须与证据严格对应

统一使用以下状态，不得混写：

- `已实现`：代码存在，但尚未证明运行结果；
- `相关测试通过`：最小测试通过；
- `完整回归通过`：全量自动测试通过；
- `真机动作匹配`：某个动作在真实画面上成功；
- `阶段验收完成`：阶段合同要求的全部真实证据在规定会话和边界内成立，且无剩余必做项。

- `matched`、`blocked`、`succeeded` 必须按真实终态记录；“动作匹配但最终 blocked”不能写成成功。
- 独立 0 动作只读成功不能替代要求动作的同会话端到端成功。
- 离线合同测试、Mock、历史截图和浏览器页面不能替代真实 App 的物理动作证据。
- 阶段尚有一个必需证据缺口时，只能写“核心能力完成但阶段未验收”，不得用完成比例模糊结论。

### 十、失败止损与阻塞报告

- 单个物理动作失败：立即停止该动作链，保存动作前后证据，不自动重试。
- 同类模型合同失败两次：停止继续调用模型，转为离线修复合同或报告阻塞。
- 同一根因修改两轮仍未缩小验收缺口：停止扩展补丁，重新审查根因分类、验收合同和最小复现。
- 无法安全完成时，报告：已证实事实、唯一阻塞、已排除原因、恢复所需条件；不得用更多不相关修复掩盖阻塞。
- 不设未经证据支持的时间承诺；进度只报告新完成的证据、当前唯一缺口和下一项决定性动作。

### 十一、沟通与 Token 成本

- 状态更新保持简短，只说相较上次发生的变化；不重复长篇背景、测试清单或已知结论。
- 用户询问进度时，先明确回答“是否完成、卡在哪里、下一步是什么”，再补必要证据。
- 安全、离线、可逆、范围内的操作自主完成，不为形式反复请求确认。
- 需要用户授权、会产生外部影响或会改变验收方向的事项才暂停询问。
- 每完成一个思考或实施批次，使用“决策反思卡”删去不能直接关闭验收缺口的后续步骤。

### 十二、反补丁式修复硬门禁

- “当前唯一验收缺口”必须写成阶段合同要求的能力和证据，不得写成最后一次报错、某个控件、某个页面或某种模型措辞。例如，“同一新会话连续完成两个动作并最终 succeeded”可以是验收缺口；“点不中中英文切换”只能是待分类的失败现象。
- 表面现象不得直接转化为代码修改。必须先证明现象来自可复用的通用根因；若只与当前截图、键盘布局、App 路径、坐标或单次模型输出有关，则保留为失败样本，不得添加生产特例。
- 每次生产代码修改前必须形成以下四项可检查产物，缺少任一项不得开始修改：
  1. **验收台账与根因证据**：当前合同缺口、已观察事实、根因分类、为何必须改代码；
  2. **同类失败样本集合**：包含本次现象和可用于证明通用性的历史/构造样本；
  3. **通用修复说明**：不依赖 App、命令、固定步骤或坐标的设计，影响范围、风险和回滚方式；
  4. **验证清单**：历史失败重放、最小相关测试、一次完整回归，以及确有需要时的一次最终真机证据。
- 上述四项产物可以写入当前验收/交接文档、测试夹具、提交说明或证据目录，但必须能在工作树中定位，不能只存在于临时对话推测中。
- 代码评审时若无法从“修复内容”反向对应到“通用根因”和“阶段合同缺口”，该修改按补丁式修复处理并撤回，不得以测试恰好通过为理由保留。
- 验证必须同时包含正向样本和至少一个变化样本（换措辞、换画面状态或换 App 中适用的一项）；只能让原失败样本通过，不足以证明通用修复。
- 规则文档本身不算能力完成。只有四项产物和对应证据在后续实际任务中被执行，才能声称工作方式已从补丁修复转为合同驱动修复。

## 当前阶段临时执行锁（完成后必须更新）

- 阶段 1 已完成。
- 阶段 2 已完成严格单会话真机闭环验收。session `18c29c4db30246d1a3ffaa8f5116f047` 从新鲜桌面目标出发，在同一 universal-agent session 依次完成 `tap_semantic` 打开系统设置和 `home` 返回桌面两个真实动作；每步均有动作前后 4 帧、新 observation/fingerprint、matched typed receipt 和 revision `1→2→3`，最终 `status=succeeded`、任务图 `completed`、`active_subgoal=null`，无旧 scope 复用、无自动重试。报告位于 `poc/output/web/generic_supervised_20260817_111820_18c29c4d/report.json`。
- 独立 `/api/agent/generic-scene` 四帧只读复核为 `executed=false`、`physical_action_requested=false`，确认最终 `launcher/home_screen` 稳定可见；它只是补充，未替代上述两动作同会话证据。
- 真正阶段 3 现在允许开始，但尚未完成。阶段 3 仍必须以至少 3 个不同真实 App 的陌生命令、真实画面和物理动作证据完成；不能复用阶段 2 session，也不能用离线测试或同一浏览器中的多个页面替代。
