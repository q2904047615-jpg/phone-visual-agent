# 通用手机视觉 Agent 真机验收活动

日期：2026-08-12
依据：根目录 `项目最终目标.md`
用途：只定义验收输入和证据，不参与任务规划，不包含任何 App 专用执行流程。

## 2026-08-13 真机能力验收模式

- 已新增独立、通用的真机能力验收入口。它按 `device_id + candidate_action` 工作，不含
  App 名称、固定坐标或固定步骤。
- 正式控制器不会临时获得未验证能力。每次 trial 使用一个不登记、不热更新的临时控制器，
  只比正式能力集合多开放本次唯一候选动作。
- 启动和风险确认必须保持 `physical_actions=0`；具体动作确认绑定
  `trial/session/task/device/revision/subgoal/risk/observation/fingerprint/action`，最多执行
  一个物理动作，并保存动作前四帧与动作后四帧。
- 通过报告仍不能自动启用能力。第二次晋级确认绑定 report 与 registry 的 SHA-256，成功时
  只原子更新一个设备的一项 `verified_actions`，并返回 `requires_restart=true`；运行服务不热更新、
  不自动重启。
- 动作后观察、证据读取或报告校验失败时，trial 固定为失败并保存已产生的动作数和证据，释放
  设备、禁止重试和晋级。服务重启后只能只读恢复 trial；任何一次性确认权限都不会恢复。
- 网页控制台已加入“真机能力验收”面板。浏览器契约证明：成功路径只有一次动作确认和一次
  独立的零动作晋级确认；失败路径没有晋级按钮，也没有第二次机械臂请求。
- 截至 2026-08-13，这些属于代码与模拟验收。`input_verified_text`、`long_press` 和 `drag`
  仍没有新的真机通过报告，不得据此把它们标成真实可用。

## 2026-08-13 Windows 150% 缩放适配

- 当前桌面账户已写入并读回 `LogPixels=144`、`AppliedDPI=144`，即 Windows 全局缩放
  `150%`。只读提取卖家 PyInstaller 入口后确认，`main.exe` 会在 `get_windows_scale()!=100`
  时主动提示并退出；现为它单独设置 `~ DPIUNAWARE`，让其检测到96 DPI而不改变Windows
  其他程序的150%缩放。
- 本地控制器已启用每显示器 DPI 感知；摄像区高度、卖家动作下拉框、动作按钮、连点次数和
  输入按钮都按控制窗口实际客户区宽度从 540×960 基线换算，不再依赖 Windows 必须为100%。
- 150% 离线反例证明：810×1515 完整窗口映射为 810×1440 摄像区；若窗口只剩 810×1392，
  控制器会在任何机械臂动作前拒绝，因为摄像区或底部操作栏已被裁切。
- `~ HIGHDPIAWARE` 不能绕过卖家自己的DPI检测，改为 `~ DPIUNAWARE` 后主窗口恢复为
  `智联新途AI机械臂控制端v1.0.1018`。150% 零动作 `doctor` 实测客户区 `810×1515`、摄像区
  `810×1440`、窗口DPI 96、项目尺度150%，底部操作栏完整；诊断截图为
  `poc/output/doctor_window.png`。本轮没有执行机械臂，截图中的手机当前为黑屏/熄屏状态。

## 2026-08-13 手机点亮后的只读控制台验收

- 手机点亮后再次运行零动作 `doctor`，真实画面为 Android 桌面；控制端客户区仍为
  `810×1515`、摄像区 `810×1440`，证明150%缩放下画面恢复且底部操作栏完整。
- 本机安全触点服务 `http://192.168.0.137:8770/` 已启动并返回 HTTP 200。手机仍停留在
  桌面时执行 `run_xy_calibration.py probe`，按预期以“未检测到稳定校准靶点”失败，机械臂
  动作数保持为0；未在桌面或真实 App 上替代试点。
- 当前提交 `017ce1a` 的网页控制台已重新启动。只读 `/api/device` 返回
  `controller_online=true`、`camera_online=true`、客户区 `810×1515`、活动任务0，且
  DeepSeek 与 Qwen 均已配置。控制端窗口最小化时接口会安全报告 `0×0/离线`；零动作
  `doctor` 恢复窗口后状态立即恢复，未重启卖家控制端或发送机械命令。
- 真实页面只读浏览器验收显示通用自然语言入口、动态计划、实时画面、逐步确认以及
  `device-local-01` 的真机能力验收面板。截图为
  `poc/output/live_console_readonly.png`；没有点击任务启动、动作确认、晋级或停止按钮。
- 同一代码的前端精确确认协议 `17/17`、浏览器契约 `6/6` 通过。浏览器契约仍属于模拟 API
  验证；上述运行页面截图只证明控制台与真实预览可用，二者都不能替代安全触点的一次真机
  点击回传证据。

## 2026-08-13 150%画面的第一次安全触点回传

- 用户打开安全触点页后，零动作预检稳定识别第1/9点：画面坐标 `[195,343]`、视觉网格
  `[241,238]`、画面尺寸 `810×1440`，`physical_actions=0`。
- 用户对该观察明确确认后，`probe --execute` 只执行一次产品 `vision_tap_relative`。手机回传
  `sequence=0`，页面推进到第2/9点，证明触控笔接触有效；没有自动补点。
- 实际触点相对目标约向右偏 `8.89 CSS px`、向下偏 `2.28 CSS px`，总误差
  `9.1786 CSS px`，超过单点 `8 px` 通过线，因此报告固定为 `passed=false`。证据位于
  `poc/output/xy_calibration/probe_20260813_124922/`。
- 根因不是旧九点标定质量差：现有 `540×960` 标定已经过九点独立验证，RMS 为
  `1.6326 CSS px`；加载器因150%后画面变为 `810×1440` 而仅按绝对尺寸把归一化标定停用，
  第一次探测实际使用了未校正坐标。
- 通用修复允许已验证的归一化仿射标定跨严格等比画面缩放复用，非等比缩放继续拒绝。新增
  等比/非等比反例后，标定与150%定向 `17/17`、生产虚拟环境 Python 全量 `683/683`
  通过。
- 第2/9点新的零动作定位为画面 `[406,342]`、视觉网格 `[502,238]`；修复后的标定会给出
  校正网格约 `[495,237]`。这只证明坐标计算已经进入产品路径，仍需新的逐动作确认和真实
  回传通过后，才能把150%下的点击闭环标记为通过。
- 用户随后授权由系统自行完成安全校准页上的矫正；该授权不扩展到真实 App、账号影响或发送
  类动作。每个校准点仍分别执行一个动作、动作前核对触点会话序号、动作后等待手机回传，任意
  一点失败都会停止后续点。
- 第2/9点使用修复后的归一化标定，命令画面坐标 `[400,341]`，触点误差从首点的
  `9.1786 px` 降到 `0.4984 px`。第3至第9点继续覆盖右上、左中、中心、右中、左下、下中、
  右下区域，单点误差依次为 `1.5635`、`2.7208`、`3.0654`、`1.6373`、`2.4308`、
  `1.8492`、`1.6624 px`，全部通过且均未重试。
- 排除首个未校正样本后，8个校正点整体 RMS 为 `2.0696 px`、最大误差为 `3.0654 px`；
  手机页面明确显示“九点采集完成 · 等待电脑验证”。150%等比缩放下的真实点击坐标闭环
  因此通过。
- 网页控制台已安全重启到提交 `5e06d82`，重启前后均无活动任务；当前控制端、摄像头、
  DeepSeek、Qwen在线，后续通用 Agent 动作将使用修复后的等比标定加载逻辑。
- 新增无 App 专用逻辑的 `/actions` 安全验收页，分别提供 `swipe`、`back`、
  `input_verified_text`、`long_press`、`drag` 五种单动作模式和本机事件回传。桌面无头浏览器
  已逐项得到5个 `passed` 事件，随后调用重置接口把模拟事件清空；当前事件数为0。
- 该页面和事件回传属于代码/浏览器夹具验证，尚未证明五种动作已由真实机械臂执行。后续仍需
  在手机打开对应模式，由Qwen依据真实画面提出唯一动作，并沿真机能力验收入口分别保存前后
  证据；不得用桌面模拟事件晋级设备能力。

## 2026-08-13 通用自然语言向上滑动真机闭环

- 输入原句为 `在当前本地通用动作验收页向上滑动一次，让蓝色终点进入画面；不要点击其他控件。`；
  DeepSeek 没有生成固定步骤，而是把结果状态定义为“蓝色终点在当前页面可见”，保留“不要点击
  其他控件”和“页面内容只允许向上移动一次”两项约束，并用 `current_foreground` 表示未命名的
  当前前台应用。
- Qwen 基于实时四帧观察返回唯一 `swipe/up/screen` 动作，决策置信度 `0.85`；本地控制器判定
  `allowed=true`，确认作用域绑定 session/task/device/revision/subgoal/observation/fingerprint，
  确认前 `physical_actions=0`。
- 会话 `96b497956fde443086e361b281737847` 只执行一次机械臂动作。本机验收事件记录唯一
  `swipe/up/passed`，且 `endpoint_visible=true`；没有点击其他控件，也没有自动重复动作。
- 动作后重新采集四帧，Qwen 识别到“swipe 验收通过”和“蓝色终点 · 滑动通过”。DeepSeek 随后
  生成 revision 2，把完成条件和子目标标记为已完成；最终报告为 `status=succeeded`、
  `task_graph.status=completed`、`physical_actions=1`。
- 前后八帧、两版任务图、视觉决策、控制器判定和验证结果位于
  `poc/output/web/generic_supervised_20260813_140900_96b49795/`。该目录是本机运行证据，不用于
  声称其他底层动作、其他 App、连续多步或多手机已经完成。
- 本轮同时修复了几项通用问题：低层措辞任务图的一次有界修复、当前前台占位、风险审计理由与
  控制字段隔离、Qwen 重复相同 JSON 的窄容错、确认前真实画面身份校验、只读观察最多两轮重试，
  以及高置信目标元素不再仅因自由命名的 `screen_id=unknown` 被低置信精查结果覆盖。冲突 JSON、
  画面变化、陈旧确认和第二次格式失败仍在任何机械臂动作前停止。
- 修改后的生产虚拟环境全量自动测试 `698/698` 通过。该数字属于代码与模拟回归；上述唯一事件、
  前后真机画面和终态报告才是本次滑动能力的真机证据。

## 2026-08-13 连续闭环夹具与系统返回零动作预检

- 通用安全验收页新增 `tap_semantic` 单动作模式，以及不包含任何 App 名称、账号数据或固定业务
  流程的三阶段夹具：让目标进入画面、打开只读详情、返回上一层。它只用于验证通用
  `swipe → tap_semantic → back` 闭环，不参与 DeepSeek 规划或 Qwen 动作选择。
- 桌面 Playwright 契约从空事件会话开始，实际滚动页面、点击唯一测试按钮并调用浏览器返回；
  服务端严格收到 sequence `0/1/2`、kind `swipe/tap_semantic/back`、status 均为 `passed`。
  该事件属于桌面夹具验证，完成后已经重置为0，不能冒充机械臂证据。
- 夹具补充了滚动后目标可见比例的确定性复核：只有黄色按钮至少75%进入视口才记录第1步，
  同一个阶段的重复观察不会重复写事件。动作事件白名单只增加通用 `tap_semantic`，未知动作和
  重复 `client_event_id` 仍被拒绝。
- 修改后的生产虚拟环境全量自动测试 `699/699` 通过。
- 当前真实手机仍显示上一轮“swipe 验收通过 / 蓝色终点 · 滑动通过”画面。零动作提交新命令
  `返回当前浏览器页面的上一页，确认上一页可见；不要点击页面控件，也不要修改任何数据。` 后，
  会话 `12e7ab1172e64304bcddbea61380877e` 生成唯一 `back` 候选，Qwen 置信度 `0.85`，
  控制器 `allowed=true`，风险集合为空，`physical_actions=0`。
- 待确认作用域绑定 task `caf2a6e359fe4dbf9d529e1a98bd89bf`、device `device-local-01`、
  revision `1`、subgoal `navigate_back`、observation
  `obs_9697dae47a764949981761d98a3c7d1d`、fingerprint `02eb87945cdc7d76250e`。未经用户对
  这个当前动作的新确认，不执行系统返回，也不把它记为真机能力通过。
- 用户随后明确确认上述精确作用域。机械臂只执行一次 `back`，动作前后本地 fingerprint 从
  `70caa5136db145631c8c` 变为 `3004326d69ec5e920a98`；动作后四帧稳定画面显示百度搜索结果页、
  “未找到相关结果”和热搜列表。控制器验证 `matched=true`、`physical_actions=1`，没有第二次
  机械臂动作。真机证据位于
  `poc/output/web/generic_supervised_20260813_142247_12e7ab11/`。
- 原始运行的 DeepSeek revision 2 已把 `navigate_back` 标为 completed，但又新增一个
  `read_only` 的 `confirm_previous_page`；旧逻辑继续请求 Qwen，Qwen 错误提出第二个 `back`，
  本地控制器因“物理动作不能用于 read_only 子目标”拒绝，因此原始报告真实保持 `blocked`。
  不得把后续修复倒写成该原始会话已经 succeeded。
- 通用修复规定：`read_only` 子目标只能使用现有可信观察完成、阻塞或推进到后续非只读子目标；
  Qwen 对 read_only 提出的物理动作会在解析层触发一次有界修复；DeepSeek 的只读完成复核若仍
  保留 read_only 活动节点，也会触发一次有界协议修复，禁止形成重复视觉动作循环。该规则不含
  App 名称、固定坐标或固定业务步骤。
- 使用同一份真实动作后观察进行零机械动作在线复验，DeepSeek 生成 revision 3，状态为
  `completed`，`previous_page_visible`、`navigate_back` 和 `confirm_previous_page` 均绑定到上述
  百度结果页可见证据；语义风险审计无 unknown 来源。复验不重新拍摄、不连接执行器、不改写
  原始报告，累计真实机械臂动作仍为1。
- 修改后的生产虚拟环境核心定向测试 `202/202`、全量自动测试 `703/703` 通过。该测试和在线
  零动作复验共同证明修复路径；真机返回能力本身仍以原始目录中的一次动作和前后画面为证据。
- 为进入下一项安全能力验收，零动作会话 `616ca7ec05574fc5a83ad8294300020c` 要求从当前百度页
  前进到浏览器历史中的下一页。Qwen从真实四帧唯一识别 `nav_forward`、标签 `>`、置信度
  `0.92`，但旧策略因没有通用 `forward` 分类而阻止，终态 `blocked`、`physical_actions=0`。
  证据位于 `poc/output/web/generic_supervised_20260813_145908_616ca7ec/`。
- 本地通用策略随后增加 `forward/next/前进/下一页` 导航类别并升级为 policy v3；动作仍须逐字
  复用可信候选的 element/role/label/states/bounds，账号影响和确认语义仍优先禁止。Python全量
  `705/705`、前端协议 `17/17`、真实浏览器契约 `7/7` 通过；尚需重启服务后重新建立新的零动作
  会话，旧 blocked 会话不能复用。
- 服务重启到 policy v3 后，同一句目标建立全新会话 `b74dc8693b604edebe192af2a8676621`。
  Qwen从新的真实四帧识别唯一 `tap_semantic` 候选 `nav_forward`，语义为浏览器前进按钮、标签
  `>`、置信度 `0.88`；本地策略返回 `allowed=true`、canonical class `forward`。确认作用域绑定
  task `fbf74d7207794b6585fdaed44b8e8481`、revision `1`、subgoal `navigate_forward`、observation
  `obs_d7f2e465960348e4814f076cb08a35eb`、fingerprint `f4819511ab76ccdffe59`，物理动作数为0。
  用户随后只确认该精确作用域，服务器重新采集并绑定同一 `nav_forward`
  候选后，机械臂只执行1次物理点击。
- 动作后前后指纹从 `3455d2bdf572836997da` 变为 `65669c0ac5c55b7cd89e`；
  `verification_step_1.json` 记录 `matched=true`、`physical_actions=1`。新四帧画面显示
  “swipe 验收通过”和“蓝色终点 · 滑动通过”；Qwen 返回 `finished`，DeepSeek revision 3
  将完成条件标记为已满足，会话终态 `succeeded`。证据目录为
  `poc/output/web/generic_supervised_20260813_150610_b74dc869/`。未执行第二次点击。

## 完成判定

以下所有项目都有当前可读取的真实证据后，才可以声明最终目标完成：

1. 每个物理动作前有新的稳定四帧观察、Qwen 唯一动作、控制器允许记录和未消费的确认作用域。
2. 每个物理动作后有新的稳定四帧观察，`observation_id` 和非等待动作的 `fingerprint` 均已变化。
3. 每个动作最多产生一个物理动作；执行后 DeepSeek 生成更大的 revision，或用当前可见证据结束。
4. 外部状态动作先确认风险范围，再确认绑定当前画面的具体动作；两个确认均不能跨 revision、设备、子目标或画面复用。
5. 代码测试、模型离线测试、模拟硬件测试与真机结果分别记录；其中任何一项不能替代另一项。
6. 三个 App 的验收命令只作为陌生输入样本；执行仍必须走同一个通用编排器，验收期间不得新增 App 名称分支、固定坐标或固定步骤。
7. Qwen 的预期结果必须先归一为控制器可验证的 `scene_changed/content_changed/app_id/screen_id/element_state`；协议外结果不得在动作后被当作普通页面变化通过。

## A. 当前单动作闭环

- 设备：当前已连接手机。
- 输入：`打开浏览器首页，仅查看，不搜索、不登录`。
- 当前状态：用户明确确认后执行过 1 次点击；机械臂报告的归一化目标点为 `[0.249, 0.094]`，但动作后仍是同一桌面，浏览器没有打开，因此本项未通过且没有自动重试。
- 失败证据：动作前 `f7a89af49da849d7e8e4`，动作后 `a7f9b74d9c5b769aa872`；两次场景语义等价。前后截图位于 `poc/output/web/generic_supervised_20260812_180711_acc53be0/`。
- 缺陷修复：通用控制器不再把摄像头 fingerprint 轻微变化直接等同于成功；语义无变化会写成 `mismatched`，交给 DeepSeek 重规划，并停止有界自动循环。
- 在线重建：会话 `fbe331637189465c852fbdc77734f83b` 在确认后重观察阶段因同一图标的模型同义措辞漂移而安全停止，机械臂动作数为 0；旧确认已失效。
- 通用修复：确认重绑定只接受同一低风险导航语义类别、相同标签/角色/状态和高度重叠区域；风险语义变化仍拒绝。预期结果别名已归一到控制器可验证字段，冗余动作 `bounds` 只允许逐项复用可信候选。
- 最新会话 `1565af53bbcb463cbf61952e05365f11` 已在 0 次物理动作下生成新的点击确认门；当前 observation 为 `obs_560060a8606d4e619fc9ea1ebfd228e2`，fingerprint 为 `d82edb72783b6a813cc3`，预期结果为 `app_id=browser`、`screen_id=browser_home`。
- 用户随后明确确认该会话的当前“点击浏览器”动作。确认前重新观察仍绑定同一个
  `browser_app_icon`，机械臂只执行 1 次，校正后的相机坐标为 `[115, 90]`；动作后两轮
  四帧观察仍是 Android 主屏，结果为 `mismatched`，没有自动重复点击。证据位于
  `poc/output/web/generic_supervised_20260812_224931_1565af53/`。
- 该真实运行版本随后因 DeepSeek 任务图只接受旧触发名 `action_mismatch`、而编排器发送
  `action_result_mismatch`，把会话置为 `blocked`。新代码已统一接受真实触发名，并为重规划
  输出增加一次有界协议修复；模型若仍在 blocked 图中请求低层重试，本地只会改写为
  “继续原目标还是停止任务”的高层澄清，不会产生第二个物理动作。
- 使用上述真实任务图和动作后画面进行零机械动作在线复放后，DeepSeek 保持原
  `task_id/device_id`，生成 revision 2，触发记录为 `action_result_mismatch`，状态为
  `blocked`，`active_subgoal_id=null`；证明失败可以进入新 revision 且旧动作不会自动重试。
- 通过证据：点击前四帧、点击后四帧、浏览器首页可信场景、一次物理动作、DeepSeek 新 revision、最终 `succeeded` 报告。

## B. 通用底层动作

每一项都从实时画面由 Qwen 选择语义目标，不手填手机坐标。

| 能力 | 安全验收场景 | 必须证明 |
| --- | --- | --- |
| 点击 | 打开当前唯一可见的 App 入口 | 一次点击后页面变化 |
| 四向滑动 | 可滚动的只读列表 | 方向正确且新内容可见 |
| 返回 | 从详情页返回上一层 | 上一层页面可见 |
| 输入 | 已聚焦的测试输入框中输入精确草稿，不提交 | 内容与 `goal.entities.input_text` 逐字一致 |
| 长按 | 长按桌面 App 图标并只打开菜单 | 菜单可见，未触发删除或移动 |
| 拖动 | 用户确认的无破坏性可恢复控件 | 起终点均绑定可信候选，变化可见，可恢复 |

拖动在当前设备的 `verified_actions` 中仍为关闭；只有这项真机验收成功并保存证据后才能启用。

## C. 连续闭环与重规划

- 低风险导航：从非预期起始页面完成至少三个动作；每轮都重新观察，最多执行本次确认允许的有界动作数。
- 弹层变化：在起始页面人为保留一个可关闭弹层，确认系统先处理当前画面而非照搬旧步骤。
- 失败恢复：让一个预期页面没有出现，确认系统停止旧动作并由 DeepSeek 产生新 revision；禁止机械臂自动重复同一点。
- 安全边界：一旦下一步变成输入、长按、拖动、外部状态或未知影响，连续推进必须暂停。

## D. 三个 App 的陌生命令

验收前把下列句子作为新输入提交，不为它们修改代码：

1. 浏览器：`打开浏览器，只让我看到首页，不搜索也不登录。`
2. 设置：`去系统设置里找到设备信息页面，把当前可见的型号信息展示出来，不改任何设置。`
3. 微信：`打开微信的搜索入口，把“通用Agent验收草稿”填进输入框，但不要发送，也不要加好友。`

随后从每项中选一个语义等价但措辞不同的版本再次执行，证明不是固定命令匹配。微信若继续测试发送，必须新建外部状态子目标，并重新完成风险范围确认和具体发送动作确认。

## E. 多手机调度

- 每台设备必须有不同 `device_id`、控制窗口和标定文件。
- 设备 A 与设备 B 分别启动会话，两个会话可以同时处于观察或等待确认状态。
- 两台设备各确认一个安全动作，证据目录、动作计数、revision 和画面不得串线。
- 第三个会话再次请求设备 A 时必须失败关闭，且不能调用相机、Qwen 或机械臂。
- 当前只有一套已连接硬件，因此跨进程锁已由自动测试证明；双真机并行仍等待第二套手机和机械臂控制窗口。
- 离线新增反例证明：不同 `device_id` 的活动会话和硬件协调锁可以并存；同一 `device_id` 的第二会话或第二硬件锁会失败关闭。该证据只证明调度代码，不替代双真机证据。

## 当前外部依赖

- 当前点击坐标已由卖家控制端记录，控制端进程、摄像头、COM4 和 CH340 设备均在线，但
  前后真实画面证明手机没有收到这次触摸。因此“窗口在线”不能等同于“触控笔接触有效”；
  已新增 `run_xy_calibration.py probe`：默认只定位稳定靶点且 `physical_actions=0`，显式
  `--execute` 后才复用产品 `vision_tap_relative` 路径执行最多一次并保存前后图、触点回传和
  坐标误差；不会修改现有标定或自动重试。仍需用户在手机打开安全触点页后完成真实探测。
- 诊断时对卖家 WCF `8082` 端点发送普通只读 GET 会导致其 `WindowsService1.exe` 在
  `dll1.dll` 中崩溃；服务已由系统自动拉起，控制端和摄像头已复核在线。该端点不是健康
  检查接口，后续禁止用普通 GET 探测，也不能把 8082 监听状态当作执行器健康证明。
- 多手机真机验收需要第二套手机、机械臂控制端窗口和独立标定文件；缺少硬件时不得以两个 mock 设备替代完成证明。

## 2026-08-13 运行服务保持状态

- 为避免破坏旧会话，本轮没有重启当前 `127.0.0.1:8765` 服务，也没有热加载新验收面板。
- 只读复核显示旧服务会话 `8835c281a3b14c32a6abf1e5b739b791` 仍为
  `awaiting_confirmation`，当前候选是 `tap_semantic / browser_app_icon`，控制端和摄像头在线、
  设备不忙；本轮没有消费该确认，也没有执行、取消或覆盖它。
- 新验收模式只有在当前旧会话安全处理完并重启到已提交代码后才会进入真实测试。重启后仍需
  为每一个动作取得新的精确用户确认，不能复用当前会话的确认。
