# 动作结果绑定候选：离线实现与验收边界

更新时间：2026-09-07 17:26（Asia/Shanghai）

## 结论

后续验证已于2026-09-07 17:34停止：授权10次中第一组2次即出现反例，正确发送动作ID仍配上更早输入成功的理由。候选不集成；其余8次未用。详见 [识别报告](../output/bound_action_result_20260907/report.md)。下文为17:26识别前记录。

尚未验证模型效果，未接入生产。用户批准的方案先在隔离候选中实现；没有修改正式协议、增加本地完成清单或改变手机动作。

唯一能力缺口：模型应根据当前图与本会话已执行事实，判断刚执行动作的实际效果，并正确继续或结束整任务；不能把更早的输入成功、旧内容或执行回执当成本次发送成功。

## 根因证据与适配边界

- 实际反例来自 `output/web/generic_supervised_20260907_165714_719dd258/0df6ccf437914203937c50b78d9c7721_model_response.json`：最后历史为发送点击，模型却用更早输入成功解释 matched；新图仍显示草稿。不是没有传最后动作。
- `generic_action_adapter.py` 的 action_outcome=matched 是执行层回执；视觉结果另存于 model decision/visual_outcome。候选明确将已审查的该生产者回执显示为 executed，不改变历史视觉结果、不编造触达证明。
- Open-AutoGLM 官方 [handler.py](https://github.com/zai-org/Open-AutoGLM/blob/main/phone_agent/actions/handler.py) 的动作执行返回和 [agent.py](https://github.com/zai-org/Open-AutoGLM/blob/main/phone_agent/agent.py) 的模型 finish 并不是独立的视觉成功证明；其统一千分坐标用于直接设备点击，本项目还有相机和机械标定。不能宣称上游已解决语义误判，也不能直接加固定偏移。

## 本批改动

- `bound_action_result_candidate.py`：仅供离线构造请求。扁平结果为 evaluated_action_id/action_result/action_result_reason；ID绑定当前任务、最后实际步骤及完整动作语义，下一动作与整任务 finish 仍由原 canonical 解析器消费。
- 候选拒绝引用错误动作或混用旧结果字段；这只是引用/类型检查。编号正确、理由却矛盾仍能通过本地检查，必须在真实模型对照中判为失败，不能用新的理由字段充当“已修复”的证明。
- 坐标是独立候选开关：仅将响应坐标改为 X/Y 均 0..1000，保留 JPEG 实际尺寸、用户正文和其他合同，不与结果绑定实验混为单变量对照。尚未做真实模型坐标对照，也未修改机械映射。
- 模块没有设备、网络、服务或会话写入入口；不被生产运行导入。实际截图不会由此模块上传。

## 验证结果

- 修改前相关基线：135 tests，8.166 秒，通过。
- 最终相关回归：147 tests，8.090 秒，通过；其中新增 12 项。
- 覆盖精确正文/Unicode、不变的动作 schema、最后动作身份、初始空历史、旧字段/错误ID、语义矛盾不能本地证明、各动作参数保真、canonical 导航/finish、独立坐标候选，以及原失败、既有发送成功、旧气泡未发送、已空清空、发送后 Home 的证据夹具。
- 第一次扩展测试有 1 个新测试错误：误将 cases() 的二元返回值当字典。已修正测试解包，未改生产校验；不是原项目功能退化。
- 本批修改前后 `poc/agent` 下 60 个 `.py`/`.txt` 文件 SHA256 清单一致。其他既有未提交修改未覆盖。
- 未跑完整全项目/前端回归；未加载代码；0 远程模型调用、0 手机动作、未操作卖家 main.exe、未提交或推送。

## 下一决定性动作与停止条件

需新的明确授权后，最多 10 次阿里云 Qwen 保存截图识别：五类状态各原版/候选一次（发送后仍为草稿、真实发送成功、旧内容未执行、已空清空、发送后仍需 Home）。同模型参数和当前图片，不传旧截图、不重试、不操作手机；本轮不叠加坐标变量。

候选若仍以较早动作作理由、把草稿/旧内容判完成，或导致既有成功/空清空/Home 样本退化，停止，不接入生产、不继续加提示词采样。通过这组识别也只证明局部样本，需要正式接入后的相关回归、加载核对及另行授权的完整真机证据。

回滚边界：本批只有两个新实验/测试源码及本说明、交接/台账新增段；可精确撤回这些内容，不回退整个工作树，不碰原有实验或用户修改。
