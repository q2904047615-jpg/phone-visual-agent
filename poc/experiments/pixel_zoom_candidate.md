# 显式原图像素坐标候选：尚未验证模型效果

2026-09-08 02:42（Asia/Shanghai）。当前仅本地候选，不是正式修复，不改变生产协议。

## 依据与反证

上一轮zoom_grounding_20260908首轮ROI在明确要求双轴0..999时输出[97,1093,153,1153]。仅证实最终输出违反合同；不能唯一认定这个数组是像素、混合单位或错误估计，不能倒改旧响应单位来通过测试。

候选假设：不要求模型进行归一化，明确声明image_pixels，仅对实际传入图像定位，由本地处理裁剪和放大反算，可能减少单位混用。该假设尚无新的模型证据。

重要反证：[阿里云官方视觉文档](https://www.alibabacloud.com/help/en/model-studio/vision/)说明Qwen3.7通常返回0..999归一化坐标。原始像素不是本模型的官方默认定位合同，可能更难遵守，不能包装成上游已验证修复。Open-AutoGLM执行器直接换算模型坐标，没有提供此类模型估计错误的通用修正。此前规范化坐标的多个失败样本也不等于原始像素方案必然更好。

## 实现与范围

pixel_zoom_candidate.py是纯离线函数模块，没有网络、设备、项目API和文件写入入口。

- 图片尺寸从请求内实际编码图片解码取得，不从模型自报或历史配置取得。
- ROI与放大图落点都必须明确coordinate_space=image_pixels；禁止裸数组、旧normalized响应或猜测单位回退。
- 本地只按实际宽高检查数值、裁剪，再按实际2倍放大关系反算像素中心；不按参考按钮框纠正点、不读思考候选，不恢复OCR/颜色/框中心点击。
- 输入/发送/Home/finish、正式观察与执行链、设备标定、模型配置不修改。旧失败批次完整保留且仍停止，不增加运行时兼容开关。
- 单位标签本身不证明准确：模型可声明像素却给出合法范围内的错误点，测试明确保留这一限制。

## 最小验证提议（尚未授权或执行）

新批最多4次：同一保存JPEG的语音、更多两个失败目标，各1次全图ROI+1次放大图取点。只发送已有截图及其模型指定裁剪；任何单位/范围错误、目标被裁掉或最终点仍偏离原参考区域即停止，不能改框补采。不续用旧批取消的7次额度。

该筛选只检验最先失败的接口及两类定位，不能替代发送/表情对照、跨画面、同请求重复、完整正式合同集成及真机验收。即便通过，也不直接接生产。若再出现同类单位错误，应停止这条像素变体，不叠提示重试。

其他疑点：ROI可选错目标或裁剪过紧；放大不创造原图细节；服务可能内部缩放图像；两轮延迟会影响未来真实截图新鲜度；模型输出正确单位也可能错误定位。任何一项均可推翻本候选的适用性。

未采用：猜测/钳位旧坐标、改读分析字段、固定偏移、手工指定框（改变或泄露目标答案）；直接生产切换（缺模型与回归证据）。

回滚：本候选没有正式入口，保持不接入即可，无生产恢复操作。是否删除实验文件需按用户范围决定，不删除原始失败证据。

## 本次结果

- 新增候选10项离线测试通过；与上一轮实验测试合计21项通过（0.107秒，无失败/错误/跳过）。覆盖横竖图、实际编码尺寸、单位拒绝、旧错误数组不可重解释、越界/NaN、裁剪及反算。
- 68项生产文件哈希与上一轮冻结值相同，旧实验源码/输入哈希相同，旧stopped.json仍1/8停止。
- 本次0远程调用、0手机动作、0API请求/重载，未提交/推送。未核对实时设备，不引用旧在线结果。
- 状态：候选已实现、相关离线测试通过；模型效果尚未验证、正式定位问题未修好。
