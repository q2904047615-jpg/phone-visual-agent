# 仓库减负与旧链退役验收台账

更新时间：2026-08-21（Asia/Shanghai）

## 本轮必须证明的能力

正式服务只加载通用多 App Agent 主链；旧固定 App 工作流不再注册 API、不再初始化运行时、不再参与规划、风险、动作或任务状态判断。

## 必须出现的证据

1. 默认交接只需读取一份精简当前快照和一个当前台账；历史材料有索引但不会被默认加载。
2. 已核实无引用的临时目录和缓存被删除，真实验收证据、回归夹具、虚拟环境和用户录屏被保留。
3. 旧固定 App 路由、旧任务编排和旧运行时 imports 从正式服务消失。
4. 被通用链路实际使用的文字分段或硬件能力先迁入通用模块，再删除旧模块。
5. 相关回归与一次完整回归通过；服务 OpenAPI 静态/测试检查不再暴露旧执行入口。

## 已有事实

- 精简前 `项目交接文档.md` 为 1228 行、约 192 KB，并混合 58 个历史阶段更新。
- `tmp` 约 523 MB，`poc/__pycache__` 约 5 MB；`.superpowers`、`.tmp_order_analysis`、`.tmp_orders_analysis` 和空的 `.git.invalid-empty-backup-20260811` 均无生产引用。
- `poc/output/web` 约 795 MB，包含当前阶段 3 与复杂输入真实证据；本轮不批量删除。
- `poc/web_app.py` 仍导入并注册旧工作流；多数路由返回 410，但 `/api/agent/plan-preview` 仍可进入旧规划链。
- `operation_specs.py` 的文字规范化/分段函数仍被通用输入链路使用，不能直接删除。
- `robot_gui_poc.py` 仍承载通用硬件/window/camera 原语，不能在未拆分前直接删除。

## 当前唯一缺口

在不破坏通用 Agent、当前 API 客户端和硬件适配层的前提下，完成旧固定 App 运行链退役，并用自动回归证明正式入口只剩通用链路。

## 根因分类

架构历史债务：旧路径虽然被功能开关关闭，仍在进程启动时导入、初始化和注册，且部分通用工具寄生在旧模块中。这不是产品风险、模型失败或真机问题。

## 通用修复边界

- 删除旧固定 App 的正式 API 和初始化链，不新增兼容层。
- 共享通用能力迁到名称和职责都通用的新模块。
- 不改 canonical 动作语义、不增加确认类别、不增加 App/命令/坐标特例。
- 删除与退役运行时一一对应的专属测试；保留或迁移仍证明通用能力的测试。

## 停止条件

- 发现某旧模块仍是通用硬件或输入链的唯一实现时，先迁移并通过相关测试，之后再删除。
- 相关测试出现非预期回归时，不继续扩大删除范围。
- 本轮不执行真机动作，不操作卖家 `main.exe`，不删除真实报告与用户文件。

## 完成结果

### 交接体系

- `项目交接文档.md` 从 1228 行、约 192 KB 精简为 96 行当前快照；旧过程只通过 `历史文档索引.md` 和 Git 历史按需读取。
- `AGENTS.md` 的默认读取合同改为：最终目标 → AGENTS → 当前交接 → 一个当前台账，不再默认读取所有历史阶段材料。
- 本阶段提交：`147b518 docs: slim active project handoff`。

### 临时文件

- 已删除：`tmp` 约 499.26 MB、`poc/__pycache__` 约 5.19 MB、`.superpowers`、`.tmp_order_analysis`、`.tmp_orders_analysis` 和空的 `.git.invalid-empty-backup-20260811`。
- 删除总量约 504.45 MB，不可恢复；这些目录均已核实无生产引用。
- 已保留：`poc/output/web` 全部真实报告、`poc/evals`、`poc/.venv`、根目录 `录屏复核副本.mp4` 和所有用户文件。

### 旧固定 App 运行链

- `web_app.Runtime` 不再创建旧 `TaskStore`、后台 worker、旧 HybridAgent、旧 GenericTaskOrchestrator、旧 StateGraphRunner 或旧 supervised session。
- 正式 OpenAPI 从 46 条路由收敛到 25 条；`/api/tasks*`、`/api/agent/parse`、`generic-goal`、`plan-preview`、旧 supervised、dry-run、旧 ensure/observe/tap-heart 和旧 events/evidence/report 路由均不可注册，且无环境变量或运行开关可恢复。
- `/api/device` 不再转发旧微信/抖音 readiness，控制器公开身份改为 `universal_action_controller`；旧业务就绪状态不再伪装成当前产品能力。
- 正式服务冷启动不再加载 `operation_specs`、`task_orchestrator`、`state_controller`、`douyin_page_signals`、`supervised_semantic_runtime`、`semantic_action_adapter`、`live_semantic_dry_run`、`semantic_executor` 和 `generic_intent`。
- 通用文字规范化/分段/恢复迁入 `text_input_utils.py`；canonical 动作载体迁入 `semantic_action.py`；typed 任务图只读投影迁入 `generic_goal.py`。
- `generic_goal.py` 不再含旧的低层动作词字段黑名单，也不强制普通任务确认；正式风险和候选权威仍分别是 typed risk policy 与 canonical catalog。
- `vision_agent.py` 与 `robot_gui_poc.py` 暂时保留，因为它们仍承载 Qwen 客户端和通用卖家窗口/相机/机械传输原语；其中旧业务方法没有正式路由、实例或调用入口，不再构成运行权威。后续若拆文件，必须先迁移这些通用原语，不能为追求文件名干净破坏硬件链。
- 本阶段代码提交：`571bf1c refactor: retire fixed app runtime paths`。

### 验证

- 退役合同、typed goal 与文字工具定向测试：`28/28`。
- 通用主链相关回归：实际存在的 `409/409` 通过；命令中误写的 3 个不存在测试模块产生 loader error，已归类为 Agent 命令错误，不是项目失败。
- 完整 Python 回归先后两次均为 `1340/1340`；最终一次耗时 51.732 秒，`skipped=54` 为已明确隔离的旧固定 App 历史测试。
- `py_compile`、正式服务冷启动导入、OpenAPI 路由检查和 `git diff --check` 通过。
- 本轮没有真机动作，没有启动、停止、重启或操作卖家 `main.exe`。

## 结论

本轮合同已完成：旧固定 App 代码仍可在 Git/隔离测试中追溯，但不再进入正式服务运行权威；通用 Agent 主链、能力验收、设备状态与预览接口保持可用。代码与自动测试完成不代表复杂输入真机闭环完成。
