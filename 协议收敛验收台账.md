# 通用视觉 Agent 协议收敛验收台账

更新时间：2026-08-18（Asia/Shanghai）

## 1. 当前唯一验收缺口与根因证据

当前默认产品协议尚未同时满足以下合同：用户可以直接表达点击、滑动、输入、长按、拖动等自然动作意图；DeepSeek 负责结构化目标、完成条件和风险；Qwen 只从当前真实画面选择一个下一动作；本地控制器只校验类型、作用域、候选身份、几何、设备能力和外部影响；安全动作在每次重新观察后可自动推进；外部影响在具体目标和草稿明确时只确认一次。

已确认的主要类别是架构与协议边界错误，不是单个 App、页面、截图或模型措辞：

1. 公开后端仍并存固定 App 白名单/状态机、GenericIntent v1 和 DeepSeek 动态任务图三套入口。
2. 高层任务图把自然动作词与裸坐标、Shell、ADB 混在同一禁令中，导致合法用户目标在动作层之前失败。
3. 本地控制器使用大量中英文关键词和页面语义重新理解任务，形成第二个业务规划器。
4. 安全动作也要求逐动作人工确认，自动循环被固定为零；外部任务还在观察前先确认风险，之后再次确认具体动作。
5. 视觉候选、身份、输入和手势能力存在硬编码上限、平面文字唯一性和设备无关常量。
6. DeepSeek 合同失败会远程重采样，观察与几何证据在同一 fingerprint 下缺少统一预算和复用。

生产代码必须修改，因为这些边界直接阻止最终目标要求的跨 App、跨措辞、无需新增代码的默认闭环。

## 2. 同类失败样本集合

正向样本：

1. “点击设置并查看标题后返回桌面”。动作词必须保留为意图，但每个执行动作仍由新观察和本地 scope 授权。
2. “向上滑动找到隐私设置”。DeepSeek 可保留方向约束，Qwen 只能在当前可继续浏览的表面选择一次 swipe。
3. “在当前输入框输入 Agent 8，你好。”。输入内容必须逐字保存，键盘布局和模式按观察动态切换。
4. “长按当前唯一项目后拖到目标区域”。手势参数来自设备能力和结构化意图，不来自固定 App 或坐标。
5. “给指定收件人发送指定消息”。安全导航和草稿自动推进，最终具体收件人、正文和发送控件只确认一次。
6. 计算器、系统设置、浏览器和任意第四个 App 的等价目标换措辞，不增加本地关键词或 App 分支。

变化样本：

- 同一动作使用“点一下/选择/打开”“划到下方/继续浏览”“键入/填写”等不同措辞；
- 当前页面已经满足一部分步骤、存在弹层、从中间页面开始；
- 收件人名称在标题和历史内容中重复，但标题角色唯一；
- 无文字的返回、更多、附件或发送图标具有唯一视觉签名；
- 数字、大小写、中文、空格和常用标点混合输入；
- 不同设备提供不同文字长度、手势时长、拖动距离和已认证动作能力。

必须失败关闭的反向样本：

- 模型输出裸坐标、Shell、ADB、卖家控制命令或连续自由动作；
- stale session/device/revision/observation/fingerprint/decision/action digest；
- 多个无法通过角色、容器和区域消歧的目标；
- 画面变化、目标移动、几何不重叠、低置信或未认证设备能力；
- 未授权的发送、支付、删除、关注、评论等外部影响；
- 物理动作失败后的自动重试；
- legacy 固定 App API 在默认产品模式下继续创建可执行任务。

## 3. 通用修复说明

### 3.1 单一默认协议

- DeepSeek 动态任务图是唯一默认产品协议；旧 App 白名单、固定计划、固定状态机和固定 ROI 只保留为显式 legacy 回归资料，默认 API/控制台不得执行。
- GenericIntent 仅作为只读预览或迁移输入，不再拥有独立执行确认语义。

### 3.2 意图与执行权限分离

- 允许自然语言目标、子目标和完成条件出现点击、滑动、输入、长按、拖动等动作词。
- 继续严格拒绝裸坐标、按键码、Shell、ADB、main.exe 和绕过控制器的字段。
- 文字动作声明不产生机械权限；权限只来自当前 typed action、可信观察、本地策略、设备能力和精确 scope。

### 3.3 职责收敛

- DeepSeek 负责语义、风险和完成条件；本地风险审计只能提高风险或拒绝，不能靠关键词证明安全业务语义。
- Qwen 从本地生成的、与当前结构化实体和动作能力兼容的候选中选择一步。
- 控制器不再用中英文关键词决定业务导航，只校验 action kind、impact、元素原样绑定、几何、状态、能力和风险确认。

### 3.4 自动安全循环

- 原始用户目标授权同一 session 内的 `read_only/navigation_only` 安全动作自动推进。
- 每个内部迭代仍只执行一个物理动作，动作后重新观察、验证、生成 receipt 和新 revision；失败不重试。
- `external_state` 在导航、身份和草稿准备完成后暂停；具体外部动作只接受一次绑定目标、正文、观察和动作 digest 的确认。
- `unknown` 允许只读观察和重新分类，不允许物理动作。

### 3.5 视觉、身份和能力

- compact 场景改为更大的有界候选集，并优先用角色/容器/区域而不是全画面文字计数消歧。
- 无文字图标可使用非空视觉签名做只读几何定位；仍要求全场语义唯一、fresh rebind 和独立几何审计。
- 本地不得根据模型框自动补造 `fully_visible`。
- App 前台身份只接受规范化精确身份或显式别名，不使用任意 token 交集。
- 空 `target_apps` 只在正式 `target_surface=device/system/current_surface` 时允许。
- 未知 entity 只作为非权威上下文；能影响执行的实体必须属于正式 typed key。

### 3.6 输入、手势和成本

- 文本、手势时长、拖动距离和动作集合来自设备能力描述；不支持内容必须返回结构化 capability gap。
- 输入模式允许按下一确定性分段双向切换并保留已验证前缀；清空文字不依赖 QWERTY 或 direct_latin。
- 长按/拖动回执区分 GUI transport barrier 与真实接触 ACK，最终成功仍以动作后可见结果为准。
- DeepSeek 每个 plan/replan 只允许一次远程语义调用；仅唯一、无语义变化的本地规范化可修复格式。
- 同 fingerprint 的只读观察、身份和几何凭据受模型调用预算约束并可复用；画面变化立即失效。

影响范围：DeepSeek 任务图、Qwen选择、观察器、控制器、适配器、Web API/前端和设备能力报告。回滚方式是整体撤销本协议收敛批次；不得只恢复 legacy 默认执行或动作词禁令。

## 4. 验证清单

1. DeepSeek：动作词正例、裸坐标/Shell反例、单次远程调用、unknown只读重分类、typed entity/target surface。
2. Qwen：动作候选按结构化能力裁剪、无字图标、角色化收件人、重复文字、滑动表面和无合法动作反例。
3. 控制器：不依赖自然语言关键词放行；所有 action kind、risk、scope、geometry、fresh observation 正反测试。
4. 自动循环：安全多动作自动推进、每步一动作和新 receipt；外部动作停在一次具体确认；任何失败无物理重试。
5. 输入/手势：双向模式切换、非空前缀、清空布局无关、设备能力差异、未认证手势失败关闭。
6. Legacy：默认控制台和 API 不再创建固定 App 可执行任务；显式测试模式仍可读取历史夹具。
7. 历史失败重放：动作词误拒绝、Launcher入口误完成、重复收件人、相邻文字框、键盘模式与物理门失败样本。
8. 编辑循环只跑最小测试；批次稳定后运行相关回归、Python完整回归、前端协议与浏览器合同、静态编译和 `git diff --check`。
9. 本批不加载服务、不调用在线模型、不操作相机、机械臂或 `main.exe`。完整离线通过后另开一次真机验收批次。

## 5. 本轮停止条件

- 需要 App 名称、固定页面、固定步骤、截图身份或固定坐标时停止。
- stale scope、几何、风险、设备独占或一动作一观察任一门禁被削弱时停止。
- 两轮通用实现仍无法让动作词变化样本和跨 App 变化样本同时通过时，停止扩展并重新审查协议。
- 完整回归存在断言失败时，不加载服务或进行真机验收。

## 6. 完成证据

本批代码与协议文档已实现，且没有增加 App 名称分支、固定步骤、截图坐标或单命令状态机。

- DeepSeek 任务图回归：`200/200`。
- 通用观察器回归：`194/194`。
- Qwen 视觉决策回归：`101/101`。
- 独立元素几何审计：`33/33`。
- Universal Agent 编排器与本地策略：`161/161`。
- 语义适配、文字事务与能力验收相关回归：`112/112`。
- Web 平台在显式 legacy 兼容开关下：`170/170`；另有默认关闭固定工作流的 410 反向测试。
- Python 完整离线回归：`1380/1380`。仅出现既有测试子进程 `ResourceWarning`，无断言失败。
- 前端协议：`27/27`；离线 Playwright 浏览器合同：`11/11`。
- 9 个修改后的生产 Python 文件 `py_compile` 通过；4 个相关 JavaScript 文件 `node --check` 通过；`git diff --check` 通过（仅 CRLF 转换提示）。

当前结论：协议收敛批次已完成代码与完整离线验证。它没有加载项目服务、调用在线模型、相机、机械臂或卖家 `main.exe`，因此不能替代阶段 3 的三个真实 App 正式验收。

## 7. 真实消息目标风险类型收敛补充台账

- **当前合同缺口与根因证据**：真实原样目标“打开微信，进入文件传输助手，输入‘你好’，然后发送”在目录 `poc/output/web/generic_supervised_20260818_174410_2d790bd9/` 以 `physical_actions=0` 停止。DeepSeek 图已逐字绑定 `recipient=文件传输助手`、`input_text=你好`，且唯一外部子目标和风险均为 `message_or_communication`；独立审计却把复合原句保守归为 `unknown_external_effect`，全局风险集合因此不相等。主要类别为 typed message 合同与独立审计聚合之间的通用映射缺口。
- **同类样本**：正例包括“给张三输入并发送今晚八点见”“向文件传输助手发你好”等同时具备逐字收件人、正文、唯一发送子目标和通信风险的复合目标。反例包括缺收件人、缺正文、多种外部风险、支付并发送、删除后通知、关注并私信，以及任一子目标为 `unknown` 的目标。
- **通用修复**：只在 recipient/input_text 完整、全图外部风险恰好只有 `message_or_communication`、全部外部子目标都精确关联该风险、且本地可证明发送通信语义时，将全局审计附带的 `unknown_external_effect` 视为已被更具体的通信风险覆盖。它不删除风险、不降低 external_state、不授权动作，也不读取 App 名称、固定页面、步骤或坐标。回滚为删除该单一风险类型收敛谓词。
- **验证清单**：增加上述正例和缺字段/混合风险/unknown 子目标反例；运行 DeepSeek 专项、完整 Python 回归、前端协议/浏览器合同、静态编译和 `git diff --check`。全部离线通过后只重载 Uvicorn；第二次在线仍在同类风险合同失败时立即停止，绝不继续采样或操作真机。
