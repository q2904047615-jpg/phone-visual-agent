# 单一动作协议迁移验收台账

更新时间：2026-08-20（Asia/Shanghai）

## 1. 本轮唯一验收缺口

每个物理动作只能由一份本地、确定性的正式动作协议生成和裁决。DeepSeek、TaskSemanticIR、Qwen、策略层和控制器可以分别承担任务描述、语义数据、视觉选择、作用域核验和物理执行职责，但不得各自重新解释“这个动作是否属于当前子目标”。

本轮完成必须同时证明：

- 正式运行链只公开一个动作协议版本；
- 当前子目标之外的动作不会进入候选目录，而不是先进入再由另一套规则否决；
- Qwen 只能从当前正式目录选择，不能创造候选、后置条件或动作权限；
- 策略层只复核同一目录的 scope、摘要和候选身份，不再二次解释输入、App surface 或 EffectIntent；
- 控制器继续保留设备独占、新鲜观察、目标唯一、几何、标定、一次一动作和动作后验证，这些是物理执行门，不是第二套动作语义协议。

## 2. 已观察事实与根因分类

主要根因：代码缺陷（动作语义权威重复）。

现场失败样本：`poc/output/web/generic_supervised_20260820_212229_65c5045c/`。

- DeepSeek revision 1 的 active subgoal 是 `navigate_to_input`，后续 `type_last_char` 尚为 pending；
- 可信画面已经证明输入框可见、已聚焦，内容为 `longinputvalidation2026:1`；
- 旧视觉 authority 仍从整份 TaskSemanticIR 生成了字符 `2` 的正式候选；
- Qwen 正确选择该候选，并逐字绑定期望值 `longinputvalidation2026:12`；
- 策略层随后用独立的 `input_transaction_action` 子目标归属规则拒绝，0 物理动作、session blocked。

这证明旧链路不是“一个协议被严格执行”，而是“宽候选协议先授权、第二套语义协议再否决”。

同类代码样本：

- `visual_action_shadow` 同时存在 shadow report、authority report、claim protocol 和 selection protocol；
- Qwen choice builder、orchestrator candidate rebound、policy formal decision 分别重新编译或匹配候选；
- Qwen 的 drag 匹配元组曾把 `source_element_id` 重复两次，说明重复候选组装已经发生实现漂移；
- policy formal decision 在选中正式候选后，仍独立重算 input transaction、EffectIntent 和 target surface 归属。

## 3. 通用迁移设计

唯一动作协议命名为 `2026-08-20-canonical-action-v1`。

正式数据流：

1. DeepSeek task graph：只决定当前 active subgoal、依赖和完成条件；不是动作协议。
2. TaskSemanticIR：只把任务图转换成 typed surface/entity/input/effect/subgoal 数据；不是动作协议。
3. UIScene：只提供当前真实画面事实；不是动作协议。
4. CanonicalActionCatalog：唯一动作协议。它在本地同时读取 active subgoal、typed 语义、可信 scene 和设备能力，只生成当前一步允许的候选及其 typed transition。
5. Qwen：只返回一个本地 `choice_id` 或 finished/blocked；本地用选择结果还原 catalog 中原候选，模型不能写候选身份、摘要、transition 或权限。
6. Policy：只重建同一 catalog 并核对 scope、digest、candidate、action kind 和参数完全相等；不再二次解释业务语义。
7. Controller/adapter：只处理几何、标定、设备、单动作和动作后 transition 验证；不重新分类任务或风险。
8. Receipt：只记录已经发生的执行证据；不是动作协议，也不能反向授予下一动作。

候选目录按 active subgoal 编译：

- 输入、清空、候选字和键盘切换仅在当前 active subgoal 绑定对应 `InputFieldIntent` 时出现；
- effect 控件仅在当前 active subgoal 绑定对应 EffectIntent 时出现；
- App 首页入口仅绑定当前 subgoal 的 surface；
- 具名字面目标仅绑定当前 subgoal 的 typed entity refs；
- Home、Back、Swipe、系统导航栏和 wait 只作为当前 navigate/observe 子目标的通用表面动作出现；
- 不属于 active subgoal 的候选直接不进入 catalog，后面没有另一套补充否决。

## 4. 影响范围、回滚与停止条件

影响范围：`task_semantic_ir.py`、动作目录模块、`qwen_visual_decision.py`、`universal_agent_orchestrator.py`、Web 状态、相关测试和正式协议文档。

不改变：机械标定、卖家控制工具、摄像头、几何审计、动作后稳定采帧、typed receipt、登录/付款确认政策。

回滚：本批形成单独本地提交；失败时可整体回退该提交，不保留半套兼容入口。

停止条件：任何历史失败样本无法由 active-subgoal catalog 唯一解释，或相关回归出现不能归因的失败时，不操作真机，先停止并报告。

## 5. 验证清单

- 现场重放：`navigate_to_input` 时目录不包含字符 `2`；Qwen 能 finished，不能越过 active subgoal直接输入。
- 正向变化样本：revision 推进到 `type_last_char` 后，同一画面目录唯一包含字符 `2`，选择后 policy 允许。
- 跨动作正向：Home、Back、Swipe、长按、拖动、清空、直接输入、键盘切换和普通 effect 均由同一 catalog 生成。
- 反向：pending/其他 subgoal 的 input、effect、App surface 和字面目标不进入目录。
- Qwen 伪造 candidate/digest/transition、旧 revision、旧 observation、错 device 全部拒绝。
- 静态扫描：生产运行链不存在旧 shadow/selection/action-authority 协议常量和旧候选编译入口。
- 运行相关最小测试、历史失败重放、一次完整 Python/前端回归和 `git diff --check`。
- 离线全绿并本地提交后，才重载项目服务；重新核对设备状态，再以全新 session 验收最后字符 `2`，不得复用旧 session，失败不得自动重试。

## 6. 实施结果

- 旧 `visual_action_shadow.py`、旧 fixture 和只验证旧候选协议的测试已删除；生产源码/JSON 对旧 visual action shadow/authority/selection 符号的扫描结果为 0。
- 新 `canonical_action_protocol.py` 统一生成候选、typed transition、catalog digest，并提供动作结果的确定性投影；App 身份只允许完整 package 相等，或一侧明确为未限定短名时的叶节点相等，不再把两个都以 `.app` 结尾的不同包误判为同一 App。
- TaskSemanticIR 为每个子目标写入 typed `entity_refs`；InputFieldIntent 通过 typed entity、期望状态或输入 action 绑定所属子目标，不再要求 DeepSeek 必须写某个低层动作词。
- Qwen 旧的独立动作枚举和 expected-result 组装代码已物理删除。Qwen choices 现在逐项机械投影 canonical candidates；模型只能返回一个 choice ID。
- Policy 只重新编译同一 catalog 并校验 report digest、candidate ID、action kind、元素/方向参数和原 transition；旧 input transaction、EffectIntent、App surface 二次语义裁决已删除。
- Web 状态把 `action_protocol` 明确公开为 `2026-08-20-canonical-action-v1`，机械执行合同单列为 `controller_protocol`，不再把两者混称为动作协议。

## 7. 离线证据

- canonical catalog `9/9`；TaskSemanticIR `25/25`；Qwen `114/114`；orchestrator `124/124`；mock loop `6/6`；Web/API `174/174`。
- Python 全量回归 `1301/1301`；前端协议和离线浏览器合同 `22/22`；生产模块 `py_compile` 与 `git diff --check` 通过。
- 正向变化样本覆盖：当前目标 App 的普通控件、跨 App 先 Home、active 输入子目标最后字符、键盘模式切换、ordinary effect、长按和拖动。
- 反向样本覆盖：pending 输入不泄漏、导航子目标不取得未绑定 effect 控件、不同 qualified package 不因公共后缀误绑定、伪造/过期 catalog digest 与 candidate ID 拒绝。

当前结论仅为“代码迁移完成且完整回归通过”。项目服务尚未在本节记录后重载，最后字符 `2` 的全新真机会话尚未执行，因此不能把本节写成新的真机闭环完成。

## 8. 首次加载后的现场失败与同根修正

- 提交 `b01a1f0` 加载后，服务正确公开 canonical action v1，controller/camera online、busy=false、0 活动会话；卖家 `main.exe` PID 2904 未变化。
- 全新 session `a418253b6d3440829afd79e7d9afa995` 清晰观察到输入值 `longinputvalidation2026:1`、numeric/direct-latin、唯一字符键 `2`；Qwen 选择正确字符，但 Policy 在动作前以 catalog digest 不一致阻塞，`physical_actions=0`，手机未改变。
- 根因不是模型、坐标或风险边界，而是遗漏的旧 `_precondition_eligible_action_kinds`：Qwen 先用它从完整设备能力中删掉 input/clear，再编译目录；Policy 用完整能力重建，因此两份 report digest 不同。
- 该旧筛选器、清空/收键盘文本正则及辅助动作过滤已物理删除。清空、键盘收起、输入事务、字符键和键盘切换的前置条件统一由 canonical catalog 的 active typed required action 和可信 scene 处理。
- 新增回归要求：Qwen 决策携带的 report digest 必须等于 Policy 以同一 scene、semantic IR 和完整设备能力重建的 digest。修正后 Qwen `114/114`、canonical/orchestrator/mock/Web `313/313`、Python 全量 `1301/1301`、静态编译和旧筛选器扫描通过。

仍未执行第二次真机会话；必须先提交并重载本次修正。旧失败 session 不得复用，第二次仍只能使用全新 session，失败不重试。
