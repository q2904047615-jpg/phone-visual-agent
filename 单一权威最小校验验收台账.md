# 单一权威最小校验验收台账

更新时间：2026-09-02（Asia/Shanghai）
状态：三项通用修复、相关回归、最终完整回归、本地提交和项目 API 加载已完成；真实功能闭环成功，但最新一次性护栏尚缺自然新任务中的无重复真机证据

## 本轮唯一能力缺口

当前唯一缺口是**最新护栏的自然真机证据**，不是仍待修复的生产代码缺口：在加载 `da3f4a2` 后的一条自然输入/效果任务中，证明同一 typed 字段不会发生第二次物理聚焦点击，同一 typed effect scope 不会发生第二次物理动作。

历史失败链已经定位并修复：会话 `9110e19bd9584d0893f77371e7181280` 暴露无可见键盘时没有把肯定式光标规范化为焦点；会话 `0987cbb1122e483c91904cce4d71d0d3` 暴露 Qwen 在无聚焦证据时跳过聚焦。`a92af6a` 与 `9a62f44` 分别修复焦点归一化、防重复基础和“先聚焦、后输入”的唯一单步合同。

真机会话 `85f1c3b573c64f368ffa7375e7e91507` 随后实际完成 Companion 输入与普通发送，同时证明旧守卫会被可选视觉状态变化绕过，并对同一 `send_message_effect` 物理尝试两次。`da3f4a2` 已将同字段身份收敛为子目标与 `input_field_id`，并将同一 typed effect scope 限制为最多一个物理动作；真实回放、变化样本、相关回归和完整回归均已通过。为避免重复外部消息，尚未再次执行相同真机目标。

## 必须证明的能力

1. `cursor`、`caret`、`光标`、`插入符`及肯定式变化措辞在键盘隐藏时可使唯一 audited typed 输入框成为 `focused=true`。
2. `soft_keyboard_visible=false` 与 `focused=true` 可以同时成立；键盘隐藏不得推翻光标焦点。
3. 无光标、普通 border/outline、`no cursor`、`caret hidden`、`光标不可见`不得误建焦点。
4. 同一子目标、同一 `input_field_id`、同一 `tap_semantic` 且结构化输入状态无推进时，只执行第一次物理点击；元素 ID 和 bounds 轻微漂移不能绕过守卫。
5. 第一次重复只触发一张新截图和一次 Qwen 纠正；纠正后仍重复时在第二次物理点击前停止。
6. 焦点成立后 Qwen 切换到 `input_verified_text` 时正常继续；普通非输入导航点击、字段 ID、prior/expected、Companion scope 与 ACK 硬校验均不放宽。
7. 不增加 App、截图、固定坐标、候选词点击或 Companion 字段选择特例。
8. 无 `keyboard.visible=true`、肯定式 caret/cursor cue 或合法 `caret_line_index` 时，Qwen 对 input/clear/enter 目标先选择一次同字段 `tap_semantic`，不得把聚焦与文字动作合并。
9. 聚焦后 `input_verified_text` / `clear_verified_text` 引用该 typed 输入框；`press_enter` 引用以 `states.input_element_id` 绑定该输入框的明确 Enter 键。
10. 光标闪烁、focused 漏报、键盘显示/隐藏、布局/模式、正文、元素 ID 或 bounds 变化都不能为同一 `input_field_id` 重新签发第二次聚焦点击。
11. 当前 `ConfirmationAuthority.effect_ids` 非空且已执行 1 个物理动作后，新截图仍为 action 时停止且不签发第二次 scope；新截图 finish 时正常成功。普通导航与 0 动作等待不受影响。

## 已有证据

代码：

- `generic_scene_observer.py` 只把同一输入审计中的肯定式 caret/cursor 事实规范化为焦点；否定式 cue 保持非聚焦。
- `runtime_session.py` 只保存单会话、一次性的输入聚焦字段身份，不再保存可绕过的一组视觉状态摘要；不持久化、不选动作、不形成第二视觉权威。
- `universal_agent_orchestrator.py` 只按同一子目标与 `input_field_id` 拦截重复聚焦，并复用既有 typed effect scope 限制一次物理尝试；它不会生成替代动作。第一次聚焦重复后仍由新截图上的 Qwen 选择下一动作。
- `single_step_observation.txt` 在唯一 Qwen 响应合同中明确“无焦点先点同一输入框，执行后等待新截图，再输入/清空/换行”；本地仍只校验 Qwen 的选择，不生成替代动作。

正向、变化与负向样本：

- 跨 App 空白输入框、键盘隐藏、四类中英文光标 cue：建立焦点。
- 无 cue、边框、outline 和三类否定 cue：不建立焦点。
- `input_field_id` 不变、元素 ID 重编号、bounds 轻微漂移且状态不变：物理动作保持 1 次，第二次执行前停止。
- 动作后 `focused=true` 且 Qwen 改选 `input_verified_text`：进入文字动作确认，不被防重复守卫误伤。
- 动作后已 focused 但 Qwen 仍重复聚焦：只读纠正一次，仍重复则停止，物理动作保持 1 次。
- `caret_line_index=0` 且键盘隐藏：仍建立焦点；无任何焦点证据的输入、清空和换行目标：合同均要求先点击同一 `input_field_id` 的输入框。
- 换行合同明确引用绑定 typed 输入框的 Enter 键，不把 `press_enter` 错绑到输入框本身。
- 真实 step 1/2 的光标闪烁、键盘状态、元素编号和 bounds 漂移已变成防恢复样本：第二次聚焦前停止，物理动作保持 1 次。
- 自动 `send_message`：新截图 action 时以 failed 停在 1 个物理动作；新截图 finish 时 1 个动作成功。`publish_content` 后改选 swipe 的变化样本同样停止，证明不是 App、按钮或重复同动作特例。

自动验证：

- 观察器：49/49。
- 单循环编排：22/22。
- 输入/观察/执行相关回归：273/273。
- Python 最终完整回归共运行 710 项：709 通过，1 项按既有条件跳过。
- 前端完整回归：33/33（unit 17、浏览器合同 12、离线控制台 4）。
- `py_compile` 与 `git diff --check` 通过。

## 尚未完成的证据

- `da3f4a2` 加载后，同一 typed 字段与同一 typed effect scope 均未发生第二次物理动作的自然新任务真机证据。

## 下一决定性动作与停止条件

下一动作：

1. 不重跑会再次发送相同消息的原目标；
2. 下一次用户自然提交含输入或外部效果的任务前，用官方类型化客户端重新核对 OpenAPI、设备与活动会话；
3. 逐动作记录最新护栏是否保持同字段聚焦最多 1 次、同 effect scope 最多 1 次；若该一次动作后新截图未 finish，按运行失败停止，不自动重输或重发。

停止条件：

- 项目 API 无法安全重载到当前提交；
- controller/camera/device 离线或设备被占用；
- 新观察不可用、目标不唯一、scope/fingerprint 失配或机械坐标不可达；
- 登录/付款缺少确认；
- 最终真机动作出现输入或外部效果失败。发生后不自动重输或重发。
