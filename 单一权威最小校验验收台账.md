# 单一权威最小校验验收台账

更新时间：2026-09-02（Asia/Shanghai）
状态：聚焦归一化与防重复已提交；补充单步聚焦顺序修复及完整回归已通过，等待提交、项目 API 重载与一次最终真机验收

## 本轮唯一能力缺口

失败会话 `9110e19bd9584d0893f77371e7181280` 中，机械点击已经落入当前 typed 输入框，动作后画面也出现插入光标；但 observer 只在软键盘可见时发布 `focused=true`。Companion IME 不占用可见键盘画面，因此后续合法 `input_verified_text` 被聚焦门拒绝，Qwen 又连续选择同一输入框聚焦点击，最终产生 8 次无效重复动作。该分支已由 `a92af6a` 修复并加载。

修复后首次复验会话 `0987cbb1122e483c91904cce4d71d0d3` 在 `physical_actions=0` 时停止：当前帧没有聚焦证据，Qwen 却跳过聚焦直接选择 `input_verified_text`，被本地聚焦硬校验正确拒绝。当前唯一能力缺口因此仍是同一个 typed 输入聚焦状态转换，而不是机械落点、Companion transport 或发送。

本轮必须证明：当前唯一 audited typed 输入框内的肯定式光标证据能够建立焦点；无聚焦证据时 Qwen 先点击同一 typed 输入框；新截图确认聚焦后才选择输入、清空或绑定的换行键；同一字段无进展时最多执行一次聚焦点击，并只允许一次额外只读观察让 Qwen 自行纠正。

## 必须证明的能力

1. `cursor`、`caret`、`光标`、`插入符`及肯定式变化措辞在键盘隐藏时可使唯一 audited typed 输入框成为 `focused=true`。
2. `soft_keyboard_visible=false` 与 `focused=true` 可以同时成立；键盘隐藏不得推翻光标焦点。
3. 无光标、普通 border/outline、`no cursor`、`caret hidden`、`光标不可见`不得误建焦点。
4. 同一子目标、同一 `input_field_id`、同一 `tap_semantic` 且结构化输入状态无推进时，只执行第一次物理点击；元素 ID 和 bounds 轻微漂移不能绕过守卫。
5. 第一次重复只触发一张新截图和一次 Qwen 纠正；纠正后仍重复时在第二次物理点击前停止。
6. 焦点成立后 Qwen 切换到 `input_verified_text` 时正常继续；普通非输入导航点击、字段 ID、prior/expected、Companion scope 与 ACK 硬校验均不放宽。
7. 不增加 App、截图、固定坐标、候选词点击或 Companion 字段选择特例。
8. 无 `keyboard.visible=true`、肯定式 caret/cursor cue 或合法 `caret_line_index` 时，Qwen 对 input/clear/enter 目标先选择一次同字段 `tap_semantic`，不得把聚焦与文字动作合并。
9. 聚焦后 `input_verified_text` / `clear_verified_text` 引用该 typed 输入框；`press_enter` 引用以 `states.input_element_id` 绑定该输入框的明确 Enter 键。

## 已有证据

代码：

- `generic_scene_observer.py` 只把同一输入审计中的肯定式 caret/cursor 事实规范化为焦点；否定式 cue 保持非聚焦。
- `runtime_session.py` 只保存单会话、一次性的输入聚焦纠正状态，不持久化、不选动作、不形成第二视觉权威。
- `universal_agent_orchestrator.py` 只拦截 Qwen 对同一 typed 输入字段的无进展重复聚焦；它不会生成替代动作。第一次重复后仍由新截图上的 Qwen 选择下一动作。
- `single_step_observation.txt` 在唯一 Qwen 响应合同中明确“无焦点先点同一输入框，执行后等待新截图，再输入/清空/换行”；本地仍只校验 Qwen 的选择，不生成替代动作。

正向、变化与负向样本：

- 跨 App 空白输入框、键盘隐藏、四类中英文光标 cue：建立焦点。
- 无 cue、边框、outline 和三类否定 cue：不建立焦点。
- `input_field_id` 不变、元素 ID 重编号、bounds 轻微漂移且状态不变：物理动作保持 1 次，第二次执行前停止。
- 动作后 `focused=true` 且 Qwen 改选 `input_verified_text`：进入文字动作确认，不被防重复守卫误伤。
- 动作后已 focused 但 Qwen 仍重复聚焦：只读纠正一次，仍重复则停止，物理动作保持 1 次。
- `caret_line_index=0` 且键盘隐藏：仍建立焦点；无任何焦点证据的输入、清空和换行目标：合同均要求先点击同一 `input_field_id` 的输入框。
- 换行合同明确引用绑定 typed 输入框的 Enter 键，不把 `press_enter` 错绑到输入框本身。

自动验证：

- 观察器：49/49。
- 单循环编排：19/19。
- 输入/观察/执行相关回归：270/270。
- Python 最终完整回归共运行 707 项：706 通过，1 项按既有条件跳过。
- 前端完整回归：33/33（unit 17、浏览器合同 12、离线控制台 4）。
- `py_compile` 与 `git diff --check` 通过。

## 尚未完成的证据

- 补充单步聚焦顺序修复的本地 Git 提交。
- 项目 API 已加载补充提交的只读证明。
- 当前 controller/camera/device/busy/活动会话状态。
- 修复后同一会话真实完成“聚焦 → Companion 输入 → 发送”的真机证据。

## 下一决定性动作与停止条件

下一动作：

1. 检查最终差异并创建补充本地提交，不推送；
2. 从当前源码或 `/openapi.json` 核对路由，用官方类型化客户端读取当前服务与设备状态；
3. 仅重载项目 API 加载当前提交，不操作卖家 `main.exe`；
4. controller、camera、设备均在线且空闲时，在网页仅运行一次原目标，逐动作取得新截图，记录真实终态。

停止条件：

- 项目 API 无法安全重载到当前提交；
- controller/camera/device 离线或设备被占用；
- 新观察不可用、目标不唯一、scope/fingerprint 失配或机械坐标不可达；
- 登录/付款缺少确认；
- 最终真机动作出现输入或外部效果失败。发生后不自动重输或重发。
