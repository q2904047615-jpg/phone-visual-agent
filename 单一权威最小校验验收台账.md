# 单一权威最小校验验收台账

更新时间：2026-09-02
状态：DeepSeek 重复 effect 归属否决已从正式 Python 链删除并由项目 API 加载；原失败与跨 App 变化样本、
相关回归 `438/438`、最终完整回归 `837/837`（`skipped=1`）通过。同原文网页复验已越过旧计划门并执行
2 个真机动作；整体任务随后因 Qwen 选中的唯一输入元素 `e2` 被本地输入审计处理移除而失败，未发送正文
正式边界：`用户决策与协议边界.md` 第 55、58、59、60 节

## 1. 本轮唯一能力缺口

Qwen 在同一次当前截图响应中选择的唯一 scene 元素必须在同响应 `input_structure` 审计投影后继续保持
可引用、可执行。当前失败中 `e2` 是唯一合法空白输入框，Qwen 已明确选择 `tap_semantic(e2)`；本地不得先
删除该 scene 元素，再因引用消失反向否决同一个模型动作。修复必须保持 `input_structure` 为正文唯一权威，
不恢复 scene 正文双写或第二套视觉投票，也不得增加微信、截图、正文或坐标特例。多个候选、非法 bounds、
设备/scope/fingerprint、唯一 canonical、坐标和机械可达硬门仍必须保留。

## 2. 根因与参考实现

- 当前新失败已由 `before_step_3_frame_attempt_1_qwen_failure.json` 证明：同一 Qwen 响应的 scene 中只有
  一个 `element_id=e2` 的输入元素，decision 也只引用 `e2`；`_strip_preliminary_input_elements()` 在
  decision 引用完整性校验之前移除该 scene 输入元素，`input_structure` 投影没有保留其引用身份，最终
  本地把唯一目标误报成“不唯一或不可执行”。这是代码/合同缺陷，不是 Qwen 没看见输入框。
- 最新失败发生在 DeepSeek 已返回合法 JSON 之后、Qwen 调用和设备动作之前：`input_message` 被旧
  `execution_class` 副本写成 `effect`，正式 `send_message_effect.source_subgoal_ids` 只绑定实际发送步骤，
  旧 `_derive_subgoal_effect_links()` 因两处不一致把整个计划拒绝。该失败属于代码/合同缺陷；Qwen 请求数
  和物理动作数均为 0，不是视觉、设备、坐标或机械故障。
- Open-AutoGLM 不要求模型在两个位置重复声明 effect 归属；其单循环直接消费当前截图中的一个动作或
  finish。本项目继续保留 typed effect、登录/付款确认、canonical、scope、坐标和机械硬门，但不得保留
  Open-AutoGLM 主循环不存在且会在 0 动作处否决普通计划的重复关系副本。
- 主要根因是代码/合同架构缺陷，不是设备、相机、机械臂或用户目标故障。
- 旧 `QwenVisualDecisionObserver` 的模型调用数为 0，本地 deterministic selector 实际拥有动作选择权；
  旧编排器又在动作后用多套状态推进/重规划入口改变完成结论。
- Open-AutoGLM 的主循环是当前截图、一次模型 `do(...)` 或 `finish(...)`、ActionHandler 执行、下一截图。
  本项目只借鉴这一循环形状，继续保留 canonical、机械标定、Companion IME、可信包名直启以及仅登录/
  付款确认；不复制裸坐标、ADB Keyboard、任意 Shell 或其确认策略。
- 两次真实失败 session `1445d5e36f3141b9af823583eb4ab1fb` 与
  `46f58f975b19450ab95695f7190b276e` 都已证明同一根因：Qwen 正确理解需要先离开当前 App，且第二次
  已收到真实可用的 `home`，但旧 schema 允许它因 `home` 不能一步直接进入设置而用第三种状态否决路径。

## 3. 已实施的通用修复

- DeepSeek schema、初始提示和隔离重规划提示不再要求 `execution_class`。实际效果只由
  `effect_intents[*].source_subgoal_ids` 声明；模型夹带的历史 `execution_class/effect_ids` 被计划定义投影
  忽略，不能恢复否决权。
- `_derive_subgoal_effect_links()` 只从唯一 effect 引用派生反向 `risk_action_ids` 和运行时
  `external_state`；未绑定步骤派生为 `navigation_only`。输入待发送正文因此是准备步骤，实际发送仍是
  `send_message` effect；登录和付款仍由本地 effect kind 策略进入确认。
- TaskSemanticIR 的输入字段来源不再仅因正文在任意子目标说明中出现就建立所有权；只有明确 typed 输入/
  清空动作或精确字段值验证可以绑定字段，避免结果核对子目标被重新解释成输入动作。
- `SingleStepGenericSceneObserver` 的唯一响应现在包含 `scene`、必要 `input_structure` 和严格 `decision`；
  decision 只允许 `action/finish`，并绑定当前 fingerprint。提示词明确中间动作只需推进路径。
- `QwenVisualDecisionObserver` 只映射模型明确给出的 action/元素/方向；零个或多个匹配按具体模型合同
  故障停止，本地不改选。`finish` 只能引用同一 scene 的 summary 或 element 证据。
- `UniversalAgentOrchestrator` 已缩为一次观察、一次决定、一次执行、下一张截图的循环。DeepSeek 只在
  start 生成初始计划；`matched` 不自动完成高层目标，Qwen `finish` 才推进本地进度。
- 普通合法动作由自动循环执行；只有 `authentication` 与 `financial_transaction` 等待效果确认。
- 旧 deterministic selector 测试文件已删除；旧动作后 replan、可见推进、输入微步骤和重复效果完成
  运行入口已从编排器物理删除，没有兼容开关或隐藏回退。
- 旧 `AppSurfaceLineageAuthority` 领域模块、会话状态字段、导出入口和专属测试已物理删除；为保持公开
  API 形状，历史快照键 `verified_app_surface_lineage` 固定返回 `null`，不再承载任何运行判断。
- Qwen 的普通语义第三状态已从响应 schema、提示词、解析器、`GenericStepProposal`、决策对象、指标、
  编排分支和离线评估成功口径中物理删除；历史输出只保留为必须拒绝的防恢复样本。

## 4. 验证结果

### 4.0 2026-09-02 DeepSeek 单一 effect 权威批

- 原失败四步样本逐字覆盖：打开微信、进入文件传输助手、输入 `aaazjie？你好`、发送。即使历史副本把
  输入和发送都写成 `execution_class=effect`，计划仍成功建立；输入步骤派生为 `navigation_only`，只有
  `send_message` 被 `send_message_effect` 绑定并派生为 `external_state`。
- 变化样本改为浏览器表单“填写 alpha 后提交”，并故意交换历史 execution class 提示；结果仍只按
  `submit_effect.source_subgoal_ids` 绑定提交步骤，证明没有增加微信、正文、页面或坐标特例。
- 登录和付款仍为 `awaiting_confirmation`；effect 引用未知子目标、重复 ID、依赖环、错误正文结构和已有
  device/scope/坐标/唯一 canonical/机械硬门继续拒绝。旧“effect 必须与 execution_class 双写一致”的
  成功测试已删除，防恢复测试同时证明提示 schema 不再要求该字段。
- 原失败、变化、恢复性重规划与核心 DeepSeek 最小回放 `18/18`；DeepSeek、TaskSemanticIR、Qwen 单循环、
  Web/API 相关回归 `152/152`；视觉、canonical、输入 lineage、Adapter、直启、最近任务和 Web 扩展相关链
  `438/438`。最终完整 Python 回归使用不存在的 Companion registry 隔离真实 DPAPI 配对，结果
  `837/837`、`skipped=1`、`85.005s`；静态编译和 `git diff --check` 通过。
- 提交 `985e67b fix: derive deepseek effects from one authority` 已由项目 API 加载。官方类型化客户端确认
  OpenAPI 有效、controller/camera 在线、设备空闲、无旧任务/会话、DeepSeek 与 Qwen 已配置、doctor
  `ready=true`；当前 `127.0.0.1:8765` 监听 PID 为 `18672`，卖家 `main.exe` PID `14500` 未被操作。
- 网页以完全相同原文“打开微信，给文件传输助手输入 aaazjie？你好，然后发送”建立新 session
  `b425cdadf6084dc8be3fc33c05400a0d`。旧“effect 子目标必须被一个 effect_intent 绑定”没有再次出现；
  DeepSeek 计划成功进入 Qwen 单步循环；`qwen_usage.json` 记账 4 次模型请求和 4 次成功网络尝试，并真实
  执行 2 个动作，证明本批前置缺陷已在线关闭。报告：
  `poc/output/web/generic_supervised_20260902_002941_b425cdad/report.json`。
- 整体任务没有成功：第三步新截图中 Qwen 明确识别微信文件传输助手、唯一空白输入框 `e2`，并选择
  `tap_semantic(element_id=e2)`；本地输入审计处理后却找不到该引用，终态网页报“当前Qwen决策引用的元素
  不唯一或不可执行：e2”。失败证据：同目录
  `before_step_3_frame_attempt_1_qwen_failure.json`。`report/session.json` 最后持久化快照是
  `needs_reobservation/physical_actions=2`；随后 Web 聚合任务在同一第三步的下一次观察解析中失败，不能把
  report 快照误读为任务成功。本轮未输入或发送正文，也没有自动重试。

### 4.1 2026-09-01 普通合法任务最小校验批

- 真实根因样本为 session `9abbf182eea84c8dbe14badb6974e072`：前两个动作 matched，第三次同帧响应
  只漏写固定的 `input_structure.protocol_version`，且旧合同没有承认底部唯一空白编辑面，因格式门在
  canonical 前停止；不是设备、坐标、机械臂、风险确认或用户目标故障。
- action/finish 已改为真正的最小 tagged union。缺失固定版本可唯一补齐，中性额外字段被投影忽略；
  action/actions/plan/step/steps/Shell/裸坐标和跨分支冲突引用仍拒绝。重复 finish evidence 本地去重，
  同帧至少一个有效完成证据仍为必需。
- `input_structure.application_inputs[*].text` 继续是输入正文唯一视觉权威。当前 typed 输入事务及 active field
  已唯一确定时，唯一合法 bounds 即可建立空白字段；scene 重复输入、placeholder、caret、evidence、field
  label、confidence、完整键盘和候选词均不再是 Companion IME 输入/清空或聚焦动作的前置条件。多个可执行
  输入面和被选字段非法 bounds 仍不能执行；未选中的坏 scene/input/IME/keyboard 项只撤销自身。
- Qwen scene/element/decision/camera alignment confidence 与 evidence 现只作诊断，不参与候选过滤、
  canonical 重复对象选择、动作后验证、typed 连续性或物理授权。同帧 Qwen `finish` 也不再被本地 App
  lineage 二次否决。本地四帧稳定性/清晰度、当前帧方向绑定、device/scope/fingerprint、visible/enabled、
  被选目标唯一合法几何、精确 canonical 映射、机械可达与一次性执行门保持不变。
- 核心正反与变化样本 `346/346`、扩展相关链 `554/554`；最终完整 Python 回归使用明确不存在的临时
  Companion registry 隔离真实 DPAPI 配对状态，结果 `832/832`、`skipped=1`、`90.208s`。全量测试没有
  读取、删除或替换真实配对状态；静态编译和 `git diff --check` 通过。首次扩展相关命令因漏设隔离变量
  在收集期触发 DPAPI 错误，未执行测试；补齐隔离后即得到上述通过结果。
- 当前协议版本为 observation `2026-09-01-single-step-qwen-action-finish-v6`、observer
  `2026-09-01-single-step-scene-action-finish-v7`、Qwen decision
  `2026-09-01-qwen-same-response-action-finish-v9`、input audit
  `2026-09-01-input-structure-audit-v12`。本批未重载项目 API、未调用在线 Qwen、未创建真机会话、未执行
  机械动作，也未操作卖家 `main.exe`；因此这里只能写“完整回归通过”，不能写成真机任务完成。

- 观察合同：`24/24`。
- Qwen 同响应 action/finish 正反与变化样本：`6/6`。
- 最近任务 canonical：`9/9`。
- 简化编排器：`7/7`。
- 使用真实通用 adapter 的离线 Mock 闭环：`6/6`。
- Web/API：`30/30`。
- 删除 lineage 与无调用可见完成裁决器后的相关回归：`83/83`。
- 最终完整 Python 回归：`789/789`，`skipped=1`，`107.112s`。
- 静态编译通过；`git diff --check` 通过；生产源码扫描不存在旧 selector、动作后 `.replan()`、
  `_try_advance_visible_subgoal` 或 `TaskGraphTransitionReport`。
- 当前 Git 跟踪的 `poc/agent/**/*.py` 为 `70` 个生产文件、`17,516` 行；主编排器本批约从
  `2,591` 行降至 `510` 行。
- 单一权威重构已形成本地提交 `6c22bb0 refactor: use qwen same-response action loop`。项目 API
  当次重载后的监听 PID 为 `25756`；官方类型化客户端确认 OpenAPI 有效、controller/camera 在线、
  `busy=false`、无活动任务/会话，observer 为 `2026-08-31-single-step-scene-decision-v3`，卖家
  `main.exe` PID `14500` 未被操作。
- 新真机目标“打开系统设置，看到设置主页面后结束”的第一次 start 因模型服务连接被断开而在
  `physical_actions=0` 结束，未建立 session；第二次建立 session
  `fec087196a0f46438c4f85944988a398`，Qwen 正确识别当前是微信主列表，但因提示只枚举系统动作、
  没有说明 `home` 可用于从非目标 App 返回启动器，返回 `blocked/physical_actions=0`。这不是设备、
  canonical catalog 或机械臂故障；本地实际已经为该画面生成唯一 `home` candidate，且没有越权改写
  Qwen 的 blocked。
- 本轮通用修复只补充 `home`、`back`、`open_recent_apps`、`reveal_system_navigation` 和
  `wait_for_change` 的适用语义，不含 App 名称、固定命令或坐标。正向样本证明“非目标 App + 目标
  App surface”时 Qwen 的 `home` 可精确映射；变化样本证明 Qwen 明确 blocked 时本地即使有
  `home` 也不会代选。最小观察/Qwen 测试 `32/32`、相关链回归 `81/81`、完整 Python 回归
  `791/791`（`skipped=1`，`109.871s`）通过。
- 上述首轮提示修复已形成提交 `65e930a fix: teach qwen generic system navigation` 并由项目 API
  监听 PID `27576` 加载；官方客户端确认 controller/camera 在线、camera stable、设备空闲，卖家
  `main.exe` PID `14500` 未被操作。新 session `1445d5e36f3141b9af823583eb4ab1fb` 的 Qwen
  单次响应已明确判断“需退出当前 App 或切换至 Launcher/系统层”，但仍返回
  `blocked/physical_actions=0`；调用 `1` 次、网络尝试 `1` 次。该结果证明首轮只增加“可以 home”
  的说明仍未闭合合同，不能算真机成功。
- 二次离线核对发现同一提示内仍残留冲突权威：旧规则把“当前画面不能直接执行最终目标”定义为
  blocked，同时 Qwen 只看到协议动作全集，未收到当前设备本轮实际可用动作。当前修复已删除该旧
  blocked 口径，把 adapter 的唯一运行时动作集合送入同一次 Qwen 请求；若必须离开当前 App 且
  `home` 可用，唯一合法输出就是 `action=home`。观察缓存也绑定该动作集合，防止设备能力变化后复用
  旧决策；observer 版本提升为 `2026-09-01-single-step-scene-decision-v4`。观察合同 `25/25`、
  相关链回归 `260/260`、完整 Python 回归 `793/793`（`skipped=1`，`98.249s`）通过。
- 运行时动作合同已形成提交 `07cb491 fix: bind qwen to runtime action contract`，项目 API 监听
  PID `26924` 已加载 observer v4；官方客户端再次确认 controller/camera 在线、camera stable、设备
  空闲，卖家 `main.exe` PID `14500` 未被操作。新 session
  `46f58f975b19450ab95695f7190b276e` 的请求明确携带包含 `home` 的真实运行时动作集合，Qwen 仍返回
  `blocked/physical_actions=0`，理由是“home 只能返回主屏幕，不能直接进入设置”。调用 `1` 次、网络
  尝试 `1` 次。该结果证明问题不再是动作缺失、缓存、设备能力、canonical 或机械臂，而是当前通用
  Qwen 在这份合并 scene+decision 合同下，把“推进一步”错误收紧成“单步直接完成高层目标”。
- 最新 action/finish 合同最小测试 `25/25`、视觉—canonical—编排—通用 Adapter—Web/API 相关回归
  `373/373` 通过。最终完整 Python 回归首次暴露一个与本批无关的旧测试对已删除 `poc/output` 文件的
  硬依赖；该测试已改为仓库内自包含的 retired runtime 字段夹具，针对用例 `1/1` 通过。修复后的最终
  完整 Python 回归 `794/794`，`skipped=1`，耗时 `98.934s`。
- 本轮协议版本为 `2026-09-01-single-step-qwen-action-finish-v5`，observer 版本为
  `2026-09-01-single-step-scene-action-finish-v6`，Qwen 决策版本为
  `2026-09-01-qwen-same-response-action-finish-v8`。离线测试使用不存在的临时 Companion registry，
  没有读取、删除或替换真实 DPAPI 配对状态。
- 用户批准的前台 App 身份窄能力已实现：Android Companion 优先使用当前输入连接的系统校验
  `EditorInfo.packageName`，否则只在内存中选出最新 `ACTIVITY_RESUMED` 包名/事件时间；桌面端通过独立
  `2026-09-01-foreground-app-identity-v1` 签名消息校验 device、pairing、新鲜度和 nonce。新鲜系统事实
  存在时覆盖 Qwen 的 App 字段，但不覆盖 Qwen 的 screen、控件或动作；无权限、无事件或超过 6 秒时
  明确使用当前 Qwen 视觉身份，二者不投票。
- 旧“桌面 screen_id 一律把具体包名改写为 launcher”归一化已收窄：不带包名的视觉别名仍规范为
  `launcher`，可信 `com.*` 包名被保留；桌面/最近任务类型仍由同一 `screen_id` 结构规则判断。
- 目标 App 能由同一可信 launch 注册表解析为包名时，具体当前包名不同的 `finish` 会作为同帧自相矛盾
  拒绝，本地不代选动作。微信 `com.tencent.mm` 对系统设置 `com.android.settings` 的历史失败形态、匹配
  目标包名、系统身份覆盖错误模型字段、无系统身份视觉回退、身份变化导致缓存失效、签名篡改、重放、
  跨设备与过期样本均已覆盖。
- 本批最小相关回归 `205/205`、场景/canonical/直启/最近任务/输入/编排变化回归 `316/316`、Web/API
  `86/86`、模块与配对相关回归（扣除一次错误测试模块名）`69/69` 通过；最终完整 Python 回归
  `801/801`，`skipped=1`，耗时 `99.588s`。两次先从错误目录/错误模块名启动测试属于 Agent 命令错误，
  均已用当前正式命令重跑，不归类为项目失败。
- 用户明确允许使用 `D:\Android` 的 JDK、SDK 与 Gradle 缓存后，Android `0.2.0` 已完成
  `testDebugUnitTest + lintDebug + assembleDebug`：`42/42` JVM 测试通过，lint 为 `0` error / `15`
  warning，Gradle `BUILD SUCCESSFUL in 1m 16s`。新 APK 为
  `android/companion-ime/app/build/outputs/apk/debug/app-debug.apk`，大小 `53,627` 字节，SHA-256 为
  `7E7EBC75E0CA9F672A5634F1FED6A7E82A1BBDB4EEE39386667B88D34259145E`。

### 4.2 Qwen 视觉能力离线验收集

- `poc/evals/qwen_visual_decision/cases.json` 已升级为
  `2026-09-01-qwen-visual-capability-benchmark-v5`：共 `24` 个“真实截图＋目标”用例，复用 `6` 组已保存
  画面，覆盖桌面、反光变化桌面、系统设置、符号键盘输入、拼音预编辑和裁切候选画面。
- 每个用例保存 `page/target/input/finish` 标准答案，报告新增 `capability_metrics`，分别统计页面理解、
  同帧目标绑定、输入状态、同帧完成证据和最终 `action/finish` 决策。输入正文仍只来自正式
  `input_structure` 审计投影，不建立第二套视觉正文权威。
- 离线 runner 已补齐正式 `TaskSemanticIR` 编译，并把 `SingleStepGenericSceneObserver` 作为
  `QwenVisualDecisionObserver` 的同响应 decision source。端到端假 provider 回放证明每个用例只有 `1`
  次模型调用，decision 复用该响应并进入正式 canonical 映射；本地没有另选动作。
- 最小与相关回归 `51/51`；最终完整 Python 回归在使用明确不存在的 Companion registry 隔离真实
  DPAPI 配对后为 `806/806`、`skipped=1`、`104.139s`。首次全量命令因读取真实配对密文且当前测试
  账户无法 DPAPI 解密而出现 `2` 个 Web 导入错误，归类为测试环境隔离错误；真实配对文件没有被修改。
- 本批没有调用在线 Qwen、相机、Controller、Companion IME 或机械臂，没有重载项目 API，也没有操作
  `main.exe`。因此当前只证明验收集和评分器可运行，不代表 Qwen 已取得任何在线准确率或新增真机成功。

## 5. 尚未完成的证据

- 本批已由项目 API 加载，并已用完全相同原文在线证明 DeepSeek 可以越过旧重复 effect 计划门。当前唯一
  新缺口是：Qwen 同一响应选中的唯一 scene 输入元素必须在 `input_structure` 审计投影后继续保持可引用，
  不能先移除再用“引用不唯一或不可执行”否决。本次整体任务仍为失败，不能声明普通发送闭环成功。
- DeepSeek 单一 effect 权威代码、测试和文档已由本地提交
  `985e67b fix: derive deepseek effects from one authority` 固化并加载。当前项目 API 监听 PID 为 `18672`；
  卖家 `main.exe` PID `14500` 未被操作。复验结束后官方客户端确认 controller/camera 在线、
  `busy=false`、`stop_requested=false`、0 活动任务/会话。
- APK `0.2.0` 已产出但尚未安装，Usage Access 尚未授予，因此还没有新鲜系统包名真机证据。
- 本批仍未取得新的 `action → 新截图 → action/finish → succeeded` 真机端到端成功；上述两次历史尝试均为
  `physical_actions=0`，session `1445d5e36f3141b9af823583eb4ab1fb` 与
  `46f58f975b19450ab95695f7190b276e` 都是 0 动作 blocked；历史 session 不能替代这一证据。
- 24 用例在线 Qwen 运行尚未执行；当前没有页面、目标、输入或完成判断的真实模型准确率数字。
- 离线测试使用不存在的临时 Companion registry，避免读取当前桌面账户的真实 DPAPI 配对密文；没有
  修改、删除或替换真实配对状态。

## 6. 下一决定性动作与停止条件

本批的重载与同原文网页复验已经完成，DeepSeek 重复 effect 否决的在线关闭证据已成立。新的唯一故障为
Qwen 已选中的 scene 输入元素 `e2` 在同响应 `input_structure` 投影后被本地移除，随后又被引用完整性门
否决。已经按输入/外部效果失败不自动重试的规则停止，没有再次运行或发送。下一批若继续修复，必须先以
本次 `qwen_failure.json` 做最小离线复现，核对 Open-AutoGLM 当前单响应动作引用处理，向用户说明通用
修复、影响范围和回滚方式并获得同意；不得增加微信、截图、正文或坐标特例。修复后先重放变化样本和相关
回归，最后只允许一次新的真机复验。
