# 单一权威最小校验验收台账

更新时间：2026-09-02（Asia/Shanghai）
状态：代码、Python 与前端完整回归已通过；等待本地提交和项目 API 只读核对

## 本轮唯一能力缺口

把正式普通任务运行链真实收敛为：

```text
DeepSeek 首次计划一次
→ 当前新截图
→ Qwen 同响应 scene/input_structure + 一个 action 或 finish
→ 必要硬校验
→ 执行一次
→ 下一张新截图
```

不得存在第二候选、动作重绑、动作后 DeepSeek replan、Qwen 普通 blocked、App/输入持久 lineage、旧 decision source、可选模型字段否决或前端旧协议执行兼容。

## 必须证明的能力

1. 普通自然语言任务只调用一次 DeepSeek。
2. 每个新截图只消费该次 Qwen 同响应的 scene 与 action/finish。
3. Qwen 已选元素只因 ID/角色/语义/bounds 等必要硬错误停止；malformed 可选 states 不停止。
4. 空白输入框不因可选 `goal_relevant=false` 被否决。
5. Controller 不按 label、role、states 或历史画面重选目标。
6. 动作后只取得新截图和下一次 Qwen 决策，不修改任务图 revision 进行 replan。
7. typed 文字保持当前 field ID、scope、expected 和精确结果；不写持久视觉 lineage。
8. 前端只接受 Qwen v9 `action/finish` 为可执行协议。
9. 当前公开 API、设备独占、机械可达、登录/付款确认和一次一动作语义不变。

## 已有证据

代码结构：

- 已删除 `task_semantic_ir.py`、`post_action_observation.py`、三份 input lineage 生产文件和 `deepseek_replan.txt`。
- 旧 selector、replan、decision source、目标重选和 persistent lineage 生产符号扫描为 0。
- Git 跟踪 `poc/agent/**/*.py` 当前 70 个文件、13,775 行。
- 当前截图观察直接返回 `(scene, model_decision)`；adapter 与 orchestrator 以值传递，scene-only observer 会被拒绝。
- 前端协议适配层只认 `2026-09-01-qwen-same-response-action-finish-v9`；旧 Qwen 协议只在负测试中出现。

正向与变化样本：

- 所选普通按钮带格式错误的可选 states：仍保留合法 ID/语义/bounds 并生成当前动作。
- 同一空白输入框 scene 标记 `goal_relevant=false`：仍只签发一次聚焦目标。
- 当前正文包含单行和换行变化：`input_structure.text` 均覆盖 scene 旧值。
- DeepSeek 空 subgoals + 可选澄清问题：派生一个可运行目标；缺少目标 scope 仍拒绝。
- 旧 scene-only observer、旧 Qwen 协议和已删除 lineage 模块均有防恢复测试。

自动测试：

- 主链相关回归：337/337。
- Web/API 与能力回归：141/141。
- Qwen 离线评估：13/13。
- Python 最终完整回归：702/702，另 1 项按既有条件跳过。
- 前端完整回归：33/33（unit 17、浏览器合同 12、离线控制台 4）。

## 尚未完成的证据

- 本地 Git 提交。
- 项目 API 已加载该提交的只读证明。
- 当前 controller/camera/device/busy/活动会话状态。
- 本轮没有新真机动作；不把代码和测试结果写成真机任务成功。

## 下一决定性动作与停止条件

下一动作：

1. 跑一次最终 `npm test`；
2. 扫描旧生产符号并执行 `git diff --check`；
3. 更新本台账结果；
4. 本地提交，不推送；
5. 从当前源码或 `/openapi.json` 核对路由，再用官方类型化客户端只读核对项目 API 与设备状态。

停止条件：

- 任一相关或完整回归失败；
- Git diff 含未解释的用户修改或旧生产权威；
- 项目 API 不能安全重载到当前提交；
- controller/camera/device 离线或设备被占用；
- 需要操作卖家 `main.exe`。

发生停止条件时只报告具体代码或运行故障，不执行真机动作。
