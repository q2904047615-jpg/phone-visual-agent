# 单一权威最小校验验收台账

更新时间：2026-09-03（Asia/Shanghai）
状态：本地提交 `64ed6e3` 和 Controller v23 已加载；Python 727/727、前端 33/33、受影响模块 296/296 通过，官方 `doctor.ready=true`。一次最终真机会话已反证“当前通用 Qwen 粗 bounds 中心可直接作为可靠 tap point”；当前唯一缺口是确定并验证新的单一 Qwen 点击坐标合同

## 本轮唯一能力缺口

当前唯一能力缺口是：**Qwen 语义目标正确时，当前元素 bounds 仍可能整体偏移到相邻元素，Controller 直接取中心会真实误点**。会话 `867418b8ffdb4dc38d0bc02403a4589a` 中，Qwen 正确选择“文件传输助手”，但 bounds 中心 `[0.5,0.301]` 对应机械像素约 `[396,427]`，实际打开下一行“微信团队”；动作后新截图成立，`physical_actions=2` 后系统停止，没有清空、没有 Home、没有重跑。旧本地表面分割已退役且不得恢复为第二权威；当前应改为由 Qwen 同帧显式给点，还是采用其它单一坐标合同，必须先经用户决定和离线反证。

最近任务向左移除的机械问题按用户决定延后到明天现场核对；会话 `3412a7008d8f4c8aa8add2aa6b68dddd`、`42029a32637246eb8f7000a243cd907f`、`56ce624b70aa414d9a29260ea0dbad7b` 及对应“连续按住—移动—释放未被手机识别”的结论保留为上一主线证据，不与本轮输入焦点缺口并列。

此前 v3 通用点击安全内点只保留为历史证据，其生产入口和相应否决测试已退役；不得恢复为运行权威，也不得转化为合法任务的确认或人工前置条件。

ADB Keyboard 已不再是当前缺口：固定无线设备上的 `input_verified_text` 与 `clear_verified_text` 均已各自取得动作后新截图证据。Open-AutoGLM 的 Base64 输入机制继续只作为通用文字 transport 参考；发送仍由本项目视觉选择和机械点击链完成。

## 必须证明的能力

1. 当前 typed 输入框没有 `keyboard.visible=true`、肯定式 caret/cursor 证据或合法 `caret_line_index` 时，Qwen 和本地都不得签发 `clear_verified_text`；必须先对同一 `input_field_id` 只聚焦一次。
2. 聚焦动作后必须取得新截图；只有新的同帧输入结构证明当前字段已聚焦，才能签发一次固定 `ADB_CLEAR_TEXT` 广播。
3. 清空动作后必须以新截图中同一 `input_field_id` 的精确正文 `""` 通过硬校验；广播返回、旧焦点或 Qwen `matched` 不能替代。
4. 清空失败后不得自动重清、切换 transport 或继续把依赖清空成功的 Home 包装成完成；清空成功后才可进入下一子目标并由新截图选择 Home。
5. 修复必须是跨 App 的 typed 输入前置门禁，不得包含微信、文件传输助手、正文、坐标或固定任务步骤特例。

## 已有证据

已有证据：

- 真实失败会话 `66144bb7c8614de3a5d39f68d4050c92`：一次 `open_recent_apps` 后七次旧 `swipe(up)`，系统最近任务网格发生列表滚动但微信卡片始终存在；共 8 个物理动作。失败主类为代码/协议缺陷：页面滚动与目标元素直接滑动共用一个语义，且没有通用重复轨迹预算。
- Open-AutoGLM 当前通用机制由模型为 `Swipe(start,end)` 提供轨迹；公开 issue #61 记录了相同动作无限循环风险。本项目只借鉴跨 App 轨迹机制，没有复制 App、卡片或坐标特例。
- 当前生产源码唯一协议为 action `2026-09-03-canonical-recents-home-clear-v5`、DeepSeek task graph `2026-09-03-deepseek-required-action-v6`、Qwen `2026-09-03-qwen-required-action-v14`、single-step observation `2026-09-03-single-step-required-action-v10`。
- `swipe_element` 已覆盖坐标空间换算、元素内部安全起点、屏幕终点、合法距离/唯一主方向、同目标近似轨迹一次只读纠正、纠正后仍重复的 1 动作停止、改变轨迹后最多 2 动作且禁止第三次；完整 Python 回归 715/715（121.655 秒）、前端 33/33 通过，`git diff --check` 无空白错误。
- gesture v2 已在交互桌面权限上下文加载；重载后官方 `status`/`doctor` 确认 controller、camera 在线，设备空闲，`ready=true`，公开动作包含 `scroll` / `swipe_element` 且不再包含旧 semantic `swipe`；本轮没有启动、停止、重启或点击卖家 `main.exe`。
- 网页首次运行 `generic_supervised_20260903_032741_3e06deb1` 在 0 动作前停止：Qwen 返回长度为 1 的数组，唯一对象本身包含完整 scene/decision；解析器以“模型返回值必须是 JSON 对象”拒绝。
- 网页重试会话 `58af313f130d470793859d493602ffa9` 共 3 个物理动作：两次 `open_recent_apps` 后，Qwen 在最近任务新截图唯一绑定微信卡片 `e1`，签发 `swipe_element` 起点 `[0.265,0.547]`、终点 `[0.265,0.859]`；动作后四帧均为 Launcher 桌面。动作后 Qwen 再次返回长度为 1 的对象数组，系统停止且未重试；`status=failed`，不能将桌面推断为微信卡片已移除。
- 同类单元素数组合同错误曾连续出现两次，按停止规则停止采样后已完成有界修复；Open-AutoGLM 只提供有限格式清理的参考原则，没有直接照搬数组解包实现。
- 用户批准的单元素数组修复已限于单步观察入口：长度恰好为 1 且唯一元素为对象时只解包一次；空/多项/非对象、重复字段、协议和语义错误继续拒绝，其它 JSON 对象入口保持严格。新增 4 个边界测试通过，两份真实失败响应离线重放分别得到 `finish` 与 `open_recent_apps`，相关回归 204 项、完整 Python 回归 719/719（130.282 秒）通过。
- 修复加载后的真机会话 `0d1141df46134629ae2a529e9cdbd105` 共执行 5 个物理动作：步骤 1/3/5 为 `open_recent_apps`；步骤 2/4 均为绑定微信卡片 `e1` 的向下 `swipe_element`，轨迹分别为 `[0.265,0.527] -> [0.265,0.859]` 和 `[0.265,0.520] -> [0.265,0.859]`。两次动作后均为 Launcher，再开后台后微信卡片仍在；会话以同目标两次预算停止，未执行第三次滑动，`status=failed`。
- 该会话证明数组归一化和两次硬上限有效，也证明当前近似轨迹纠正只覆盖紧邻的动作后决策：`open_recent_apps` 中间导航使纠正记录未建立/未保留，`corrective_retry_history=[]`，第二次近似向下轨迹因此被执行。项目 API 当前空闲，doctor ready；本轮未操作卖家 `main.exe`。
- 用户于 2026-09-03 明确决定最近任务页“移除向左、上下只滚动寻找”。Qwen 提示、本地 Controller 和测试已统一到该单一权威：最近任务页只接受向左 `swipe_element`，上/下/右元素滑动拒绝；`scroll` 只接受上/下，水平滚动拒绝。旧“方向完全由模型自由选择”的冲突权威已从生产合同和正向测试删除。
- recents gesture v3 的正向、反向和协议版本测试通过，最终 Python 完整回归 721/721（121.487 秒）通过；当前源码和协议测试确认 Qwen v11，项目 API 重载后官方 `status`/`doctor` 确认 action v3、Controller v21 已生效，controller/camera 在线、设备空闲、`ready=true`、blockers 为空。本轮未启动、停止、重启或点击卖家 `main.exe`。
- 网页真机会话 `8bc4754b3c604e72ada0168e4bb7eb72` 共执行 2 个物理动作：打开后台后，Qwen 绑定微信卡片 `e1`，模型轨迹 `[0.265,0.559] -> [0.065,0.559]`，本地合法化为 `[0.265,0.559] -> [0.05,0.559]`，机械客户端路径 `[198,797] -> [17,797]`；新方向合同确已生效。
- 同一会话的动作后原始截图仍可见微信卡片，机械 swipe 的 `hardware_receipt=null`。随后 Qwen 动作后观察发生一次 `SSL: UNEXPECTED_EOF_WHILE_READING`，会话 `status=failed`、`physical_actions=2`，没有重复动作。SSL 中断阻止了模型终态判断，但卡片未消失已由动作后截图独立证明。
- Open-AutoGLM 官方当前 `phone_agent/adb/device.py` 直接使用 Android `input swipe`，默认持续时间按距离计算并限制为 1000–2000ms；本项目非文字动作必须使用机械控制器，不能照搬其 ADB transport。该官方事实也不支持“把本项目 0.8 秒路径改短即可修复”的结论。
- 用户批准后，机械 `swipe` 已从 `drag_client_path` 分离为独立 `swipe_client_path`：沿用已验证点击的 0.35 秒触点建立窗口，随后 0.30 秒内完成 6 段连续轨迹，释放后执行卖家坐标状态栅栏；`drag` 仍保持 0.8 秒/16 段。回执包含按下、全部插值、抬起、起终点、参数和栅栏，且明确 `mechanical_contact_ack=false`。异常路径测试证明中途失败也会释放右键。
- swipe/drag、设备执行器、方向门、网页和编排相关回归分别为 35/35、304/304、109/109；最终 Python 完整回归 726/726（121.276 秒）通过，`git diff --check` 无空白错误。
- 项目 API 仅重载已核实的 uvicorn 父/子进程，重载后官方 OpenAPI、`status`/`doctor` 确认新 profile 已加载：`relative_path_receipt=seller_position_barrier`、`touch_down_ms=350`、`movement_ms=300`、`steps=6`，controller/camera 在线、设备空闲、`ready=true`。卖家 `main.exe` PID `20172` 未被操作。
- 最终网页会话 `3412a7008d8f4c8aa8add2aa6b68dddd` 从已经打开的系统后台页开始，只执行一次向左 `swipe_element`，没有额外打开后台动作。Qwen、本地和机械路径均向左，机械回执完整；动作后 Qwen 仍看到同一微信卡片并给出同一轨迹，系统先做一次 0 动作纠正观察，仍近似后在第二次物理动作前停止。会话 `status=failed`、`physical_actions=1`，四次 Qwen 请求均成功，没有网络错误或重复滑动。
- 该会话证据目录为 `poc/output/web/generic_supervised_20260903_043236_3412a700`；动作前后第 3 帧人工复核均显示微信与设置卡片位置不变。因此本轮失败不再能归因于 Qwen 方向、数组合同、SSL 中断、重复预算或 drag/swipe 共用时序。
- 用户要求“将微信卡片移到最左边”后，新网页会话 `42029a32637246eb8f7000a243cd907f` 从当前后台页只执行一次 `swipe_element`。Qwen 轨迹 `[0.265,0.520] -> [0.05,0.520]`，机械客户端路径 `[198,741] -> [17,741]`，水平向左 181 像素、垂直 0 像素；完整回执仍为 `mechanical_contact_ack=false`。动作前后第 3 帧人工复核显示微信卡片没有可见位移，Qwen 新截图仍绑定同一微信卡片并再次给出近似轨迹，系统在第二次物理动作前停止。会话 `status=failed`、`physical_actions=1`、三次 Qwen 请求均成功；证据目录为 `poc/output/web/generic_supervised_20260903_044445_42029a32`。
- 用户要求再试一次后，会话 `56ce624b70aa414d9a29260ea0dbad7b` 从全新后台截图绑定微信卡片 `e1`，执行轨迹 `[0.265,0.559] -> [0.05,0.559]`，机械客户端路径 `[198,797] -> [17,797]`，仍是水平向左 181 像素且只执行一次。回执完整，动作后 Qwen 与人工复核均确认微信卡片仍在且没有可见位移；会话按外部效果单次预算停止，`status=failed`、`physical_actions=1`、两次 Qwen 请求均成功。证据目录为 `poc/output/web/generic_supervised_20260903_045922_56ce624b`。
- 新自然任务会话 `a28f3f30a4f84bd89df4b82f5698ac00`：从系统后台点击微信卡片后，新截图正确证明文件传输助手已前台；输入观察发布正文 `aaazjie？你好`、`soft_keyboard_visible=false` 且无真实焦点证据，Qwen 仍直接选择 `clear_verified_text`，Controller 允许执行。动作后硬校验得到实际正文仍为 `aaazjie？你好`、预期 `""`，会话 `status=failed`、`physical_actions=2`，没有重清且没有执行 Home。证据目录为 `poc/output/web/generic_supervised_20260903_050352_a28f3f30`。
- 第一轮焦点硬门禁加载后的会话 `7ff74e465ce140628898fe991e20ae20`：Qwen 在无焦点证据时再次选择 `clear_verified_text`，canonical 绑定层以“文字动作必须引用当前画面明确已聚焦的输入框”拒绝，`status=failed`、`physical_actions=0`、没有 ADB 广播；证据目录为 `poc/output/web/generic_supervised_20260903_052321_7ff74e46`。该结果证明执行门有效，也证明模型动作可用集合仍存在提前开放冲突。
- 第二轮通用修复已在 `universal_agent_orchestrator.py` 将 typed transport 动作绑定到本会话、同 revision、同 `input_field_id` 的一次已匹配聚焦动作回执；`generic_action_adapter.py` 区分动作前与动作后可用集合，保证聚焦动作后的新截图请求才看到输入/清空动作。canonical 协议与 Controller 仍分别要求 `focused=true`，不存在本地自动改选动作、App 分支、坐标补丁或额外模型调用。
- 真实失败正文、软键盘隐藏、未聚焦清空样本及“明确聚焦但软键盘隐藏”的变化样本均已固化；相关回归 374/374、最终 Python 完整回归 729/729（123.560 秒）通过，`git diff --check` 无空白错误。第二轮未重载项目 API、未再次执行手机动作。
- 用户已明确批准替换；Open-AutoGLM 官方实现与 ADBKeyboard 官方接收端已核对。
- 正式组合根只加载 `AdbKeyboardRuntimeRegistry`；Companion bridge/transport/配对/setup Python 文件与对应测试已删除，端口 `18766` 不再监听。
- ADB Keyboard profile 固定绑定 `device-local-01`、serial `aea36ffc` 与 `com.android.adbkeyboard/.AdbIME`；输入只签发 `ADB_INPUT_B64`，清空只签发 `ADB_CLEAR_TEXT`，有 0 动作 preflight 与 nonce 防重放。
- 正向、中文/英文/标点、离线设备、未选中 IME、独立清空和重复 scope 测试已覆盖；外部效果无变化门禁、终态租约释放及相关回归保持覆盖。
- Controller v23 对点按类动作直接使用 Qwen 同帧唯一元素 bounds 中心；`LocalPointGrounding`、`stable_visual_point_grounding`、`point_grounder` 和 `local_point_grounding` 已从生产域、适配器与组合根删除，并有防恢复源码扫描测试。
- v23 受影响模块 296/296、最终 Python 完整回归 727/727（91.791 秒）、前端 33/33、语法检查和 `git diff --check` 通过。本批尚未重载项目 API、尚未操作手机，也未启动、停止、重启或点击卖家 `main.exe`。
- 无线 ADB serial `192.168.0.162:5555` 已在 USB 拔除后独立在线，设备现有 ADB Keyboard 已启用并选为默认输入法，Companion IME 已禁用；卖家 `main.exe` PID `20172` 由用户自行启动并保持运行。
- 2026-09-02 21:02 官方 status/doctor 确认 controller、camera、无线 ADB Keyboard 均 ready，无活动任务、`physical_actions=0`、blockers 为空。
- 官方 `status`/`doctor` 曾确认 controller、camera、ADB Keyboard ready 且无活动任务。偏航会话 `79256e54a60940f48caddc457f42a66c` 从 Launcher 错用 `exact_input_text` 后执行 9 次 `home`，没有输入、清空或发送；项目 API PID `14508` 随后被停止。生产守卫现会在已处于 Launcher 时以 0 新动作阻止 `home`。
- 官方 APK 已下载并校验：17,374 字节、ZIP magic `504B0304`、SHA-256 `E698ADEA5633135A067B038F9A0CF41BAA4DE09888713A81593FB2B9682CDC59`。
- 真机会话 `12ad565455184bafb4c3848f0257d87b`：一次 `ADB_CLEAR_TEXT` accepted，动作后 `primary_input=""`，`physical_actions=1`，没有发送。
- 真机会话 `1796b7a954d849caa349348b88247bbf`：一次聚焦后，一次 `ADB_INPUT_B64` accepted，动作后 `input_structure` 精确值为 `aaazjie？你好`；随后只执行一次发送点击。发送前后输入框均保留正文、没有新增气泡，会话为 `failed`、总 `physical_actions=3`，没有重输或重发。
- 当前失败发送的真实帧变化凭据：`global_median_delta=0.723`、`max_tile_median_delta=0.988`、阈值 `2.0`、`material=false`。两张人工复核原图也显示手机 UI 未改变，因此旧气泡没有再把新任务误判为成功。
- 真机会话 `a7fa03bb77bc4f59b5d66d7ca564732f`：Qwen 唯一选择 `send_message` 元素；本地把模型中心 `(0.88,0.9295)` 校正到稳定表面内点 `(0.8839506,0.8892361)`，命令像素 `[719,1274]`，人工复核确认位于可见按钮内部。卖家 GUI 回执为一次左键事件、位置往返成立，但这些字段与 `mechanical_contact_ack=false` 均只作诊断，不裁决任务是否完成。
- 同一会话动作后新截图仍显示输入框正文 `aaazjie？你好` 和发送按钮，没有新增气泡；Qwen 继续给出动作，系统按同一 effect 单次预算停止。最终 `status=failed`、`physical_actions=1`，没有重输、重发或切换 transport。
- v3 对该会话四张动作前帧离线重放：表面框约 `[650,1226,757,1284]`，安全点 `[704,1256]`，四向内缩约 28 至 54 像素；v2 视觉点约 `[716,1280]`，距表面下边界仅约 4 像素。不同颜色/位置、邻近框外和相邻双候选拒绝样本均通过。
- 历史会话 `6abcb05a18494ac2b67abfe48731c560` 的“多个同等可信表面”是现已退役的 v3 本地表面分割结果，不再代表当前生产停止条件。
- 2026-09-03 01:42 后官方 `doctor`：ready、controller、camera、无线 ADB Keyboard 均为 true，`physical_actions=0`，设备独占可用，blockers 为空；卖家 `main.exe` 仍为用户原进程 PID `20172`，本轮未操作。

## 尚未完成的证据

- 尚未取得“打开微信 → 只聚焦一次 → ADB Keyboard 清空一次 → 新截图正文为空 → Home 一次 → 新截图为主屏幕”的同会话真机闭环。
- v23 已加载且运行条件成立，但真机任务因第二步相邻行误点失败；不能记录阶段完成。
- 卖家连续滑动协议和机械反馈仍是上一主线未完成证据；用户已决定明天现场核对，当前不再真机试参。

## 下一决定性动作与停止条件

下一动作：

1. 向用户说明 v23 真机反证、Open-AutoGLM 专用模型直接 tap point 合同、官方同类偏移 issue、适配差异、失败路径和回滚方式；在用户批准前不继续修改生产坐标协议。
2. 推荐方案获批后，让 Qwen 同帧为点按动作显式输出绑定当前唯一元素引用的 `tap_point`，本地只校验坐标范围、元素引用字段一致性、scope/fingerprint 和机械标定；不得从 bounds 中心推导，也不得恢复 OCR/颜色表面第二候选。
3. 先离线重放本次相邻行失败帧和至少一个不同 App/不同布局变化样本，再跑相关与完整回归；通过后只做一次最终真机任务。任一步失败不重复点击、不自动重清或重跑。

停止条件：

- ADB 不在线、serial 不唯一、ADB Keyboard 无法安装/启用/选中；
- 项目 API 无法安全重载到当前工作树；
- controller/camera/device 离线或设备被占用；
- 新观察不可用、目标不唯一、scope/fingerprint 失配或机械坐标不可达；
- 登录/付款缺少确认；
- 任一真机输入、清空或外部效果失败。发生后不自动重输、重清或重发。
- Qwen 再次返回不能唯一、纯语法地归一化的顶层结构，或有界归一化后仍不满足完整单步协议。
