# 单一权威最小校验验收台账

更新时间：2026-08-31
状态：最小版本内部实现与完整回归完成；尚未加载项目 API，未产生新真机任务证据
正式边界：`用户决策与协议边界.md` 第 55 节

## 1. 本轮唯一能力缺口

DeepSeek 当前必须同时返回计划定义、任务/子目标运行状态、活动指针、完成证据、效果双向关联和效果结果副本；本地随后把这些重复副本逐项比对。模型只要漏写或写出语义等价但字面不同的副本，合法任务就会在 0 动作处失败。必须改为“模型只定义计划，本地只派生一份运行状态”，且不改变公开任务图展示、canonical 动作和一次一动作真机执行语义。

## 2. 已证实事实与根因分类

- 主要类别：代码/合同架构缺陷，不是设备、相机、Qwen、机械臂或用户目标故障。
- 最新失败 session `e497d7d7` 中，DeepSeek 返回 `status=ready`、`active_subgoal_id=open_wechat`，但所有子目标状态均为 `pending`；本地以“必须且只能有一个活动子目标”拒绝，`physical_actions=0`，Qwen 未调用。
- 同类历史证据还包含效果 `source_subgoal_ids` 与子目标 `effect_ids` 双写冲突、以及 `expected_results` 与完成条件逐字副本冲突；这些字段表达同一事实，却分别拥有否决权。
- Open-AutoGLM 可复用的是单一串行形状：当前截图只产生当前一个动作，执行后再以新截图决定下一步。其模型不维护本项目这组重复任务图运行字段。本项目保留 typed canonical 与设备执行适配，但不保留额外的重复语义否决层。

## 3. 通用修复合同

- DeepSeek 正式输出只含计划定义字段；历史响应若仍夹带旧运行字段，本地不读取、不修补、不比对，也不让它们影响计划结果。正式运行只投影计划定义字段。
- 初始活动子目标由本地按数组顺序选择第一个依赖已满足的节点；其它节点为 pending。
- 重规划时，本地保留已经发生的完成事实；`subgoal_completed` 由当前视觉证据推进，`action_result_matched` 由严格绑定当前 revision/子目标/新观察的一次回执推进，mismatch 不推进。
- 效果反向关联和预期结果均从一个方向的正式定义派生，不再让模型重复填写。
- 只有 `authentication` 与 `financial_transaction` 进入确认状态；其余合法效果自动执行。
- 不增加 App、页面、截图、文字、坐标或单任务特例；回滚仅需恢复本批提交。

## 4. 验证清单

- [x] 最新“指针为 active、节点全 pending”失败形态不再可由正式 schema 产生，等价计划由本地唯一激活一个节点。
- [x] 两个独立可运行根节点的变化样本确定性选择计划顺序中的第一个节点。
- [x] effect 正向 source 关系自动派生子目标反向关系和正向结果。
- [x] 旧 `status/active_subgoal_id/satisfied/evidence/completion_evidence/effect_ids/expected_results` 不再被读取、修补、校验或拥有否决权，且无生产运行入口。
- [x] matched、mismatch、只读完成、登录确认、付款确认和普通发送自动执行均有相关测试。
- [x] 历史失败重放与相关回归通过。
- [x] 一次最终完整 Python 回归、静态编译、旧入口扫描和 `git diff --check` 通过。
- [x] 更新交接并本地提交，不推送。

## 5. 已完成证据

- 最新失败 `generic_supervised_20260831_140234_e497d7d7` 的原始 DeepSeek 响应已直接回放；旧响应中顶层指针为 `open_wechat`、全部节点为 pending，当前实现忽略模型运行副本并本地得到唯一 `open_wechat/active`，不再以 0 动作失败。
- 正常 `action_result_matched` 和 `subgoal_completed` 不再调用 DeepSeek；本地消费当前新观察的一次结果后直接推进初始计划中的下一节点。只有 mismatch、路径失效或确实需要改写剩余路径时才远程重规划。
- DeepSeek 初始输出只保留计划定义；重规划输出只保留剩余 `subgoals` 与必要 `clarification_questions`。旧运行态别名修补、模型证据比对、双向效果关系比对和重复结果比对的生产函数已删除。
- 旧 Planner 测试文件由 43 个复杂协议用例收敛为 13 个当前最小合同用例；本批总 diff 当前净删除约 1,500 行，生产和测试不保留旧成功路径。
- 最小 Planner `13/13`、Planner + TaskSemanticIR + Orchestrator `209/209`、Web 隔离配置 `86/86`、模块化边界 `47/47` 通过。
- 本批唯一完整 Python 回归：`952/952`，`skipped=1`，耗时 `93.103s`。测试进程使用不存在的隔离 Companion IME registry，避免读取另一个 Windows DPAPI 上下文下的真实配对密文；没有修改、删除或替换现有配对状态。
- `compileall`、旧生产入口扫描和 `git diff --check` 通过。

## 6. 当前证据边界

以上只证明内部最小版本与自动回归。尚未重载项目 API、未调用在线 DeepSeek/Qwen、未建立新任务、未执行机械臂或 Companion IME 动作，也未操作卖家 `main.exe`；因此不能把本批写成新的真机任务成功。

## 7. 停止条件

若本地派生仍需要模型重复提交同一运行事实，或为了通过单一样本新增 App/截图/坐标特例，本批停止并重新审查设计；离线稳定前不重载项目 API、不执行真机动作、不操作卖家 `main.exe`。
