# 复杂输入完整批次验收台账

## 本轮必须证明的能力

本批一次性收口四项通用能力，但每项分别形成可判定证据，不能用一项成功替代另一项：

1. **长文本分段**：同一个全新会话把一个超过单次 20 字符 direct-Latin 上限的 canonical 文本分成至少两个物理输入步骤，最终唯一输入框值逐字相等，session `succeeded`。
2. **真实换行**：目标文本包含真实 `\n`；系统只在当前多行字段存在唯一、完整可见且明确表示“换行”的 Enter 键时执行一次 `press_enter`，最终值逐字包含真实换行。Send/Search/Done/Next 等动作键不得冒充换行。
3. **多字段**：同一个全新会话在至少两个具有不同字段身份的输入框中分别写入各自 canonical 值；每一步必须绑定当前 typed `InputFieldIntent`，不得把正文写入标题字段或把前一字段值继承到后一字段。
4. **普通发送**：用一份新的收件人/正文任务形成恰好一次普通 `send_message`，无需风险确认；动作后新鲜视觉证据逐字证明发送结果，session `succeeded`。登录与付款确认边界保持不变。

四项可以使用独立的全新 session；这是为了隔离证据和失败边界，不是把通用能力拆成 App 专用脚本。生产实现不得出现 App 名称、固定步骤或固定坐标分支。

## 必须出现的证据

- 每个物理步骤前后各四帧、新 observation/fingerprint、一次性 confirmation scope、`physical_actions=1`、`matched` typed receipt。
- 每个 session 不复用旧 scope、不自动重试；失败后立即停止该 session。
- 长文本至少两个文字分段，最终值长度大于 20 且逐字相等。
- 换行步骤的 before scene 同时证明：唯一聚焦多行输入框、精确 prior value、唯一可见 Enter 键、Enter 的本地语义为 newline；after scene 精确等于 `prior + "\n"`。
- 多字段每个动作同时绑定 logical `field_id`、对应 payload、当前可见字段身份和唯一聚焦 input；最终两个字段的值分别逐字相等。
- 普通发送只有一个 effect receipt；若首轮遗漏结果，只允许既有 `needs_effect_verification` 的一次 0 动作刷新，绝不产生第二次发送。

## 已有证据

- 设备能力声明 canonical input 最多 4000 字、direct-Latin 单物理步骤最多 20 字、最多 32 字段/目标；Typed IR 已能表达 `input_fields[*].multiline` 和 `press_enter`。
- session `d1b687525f6c4e559bb261dea8957bd9` 已在同一会话完成两个逐字数字步骤并 `succeeded`，证明当前单步目录和跨步 exact value 连续性可用；它没有证明超过单段上限的文字、换行或多字段。
- session `c0de1d2d3eb84cf2840336e6e5ab26e6` 已以一次普通发送动作最终 `succeeded`，证明发送与结果复核主链已存在；本批仍需以新正文做回归性真实闭环。
- `TaskSemanticIR` 的多字段/多行结构测试存在，但没有多字段目标身份、正式动作目录和真机证据。

## 当前缺口与根因分类

### A. 换行只有语义声明，没有执行链

- `press_enter` 已在能力枚举和 Typed IR 中出现，但设备 profile 固定 `enabled=false`。
- 唯一正式 canonical action protocol、Qwen 单步协议、controller、adapter、observer 都没有 `press_enter` 完整路径。
- input structure prompt 还明确排除 Enter，因此当前无法建立“可见 Enter → 精确换行值”的本地权限。
- 分类：**代码/架构缺口**，不是模型随机或某个 App 页面问题。

### B. 多字段缺少字段身份到当前输入框的正式绑定

- `requested_input_text` 能从 active subgoal 选出唯一 payload，但 `InputFieldIntent.field_id` 尚未成为当前可见输入框的身份约束。
- canonical catalog 对唯一聚焦 input 可直接挂载当前 payload；若页面同时有多个字段或焦点错误，缺少 typed field identity 交叉验证。
- 分类：**代码/架构缺口**。不能靠“刚才点过哪个字段”或 App 页面顺序猜测。

### C. 长文本和发送主要缺真实新证据

- 长文本分段、typed lineage、动作后稳定+相对清晰双门均已实现并有部分真机证据，但还缺一个从空白输入面开始、长度大于 20、至少两段且最终 `succeeded` 的正式会话。
- 普通发送已有一次正式成功；本批无需为它重构，只需用新正文回归验收。
- 分类：**验收证据缺口**；除非离线重放发现新的通用代码问题，否则不改生产代码。

## 同类失败样本集合

- 换行正例：多行文本框中的明确 newline/return 键；变化正例为中文“换行”和回车箭头。反例：发送、搜索、完成、下一项、键盘隐藏、非多行字段、两个候选 Enter、低置信或裁切键。
- 多字段正例：`subject` 与 `body` 两个字段分别绑定不同 payload；变化正例为任意另一组字段 ID/可见标签。反例：重复字段 ID、活动子目标绑定 0/2 个字段、字段标签缺失/重复、焦点位于错误字段、payload 交叉、前一字段 lineage 被后一字段继承。
- 长文本正例：两个或更多 direct-Latin 分段；变化正例为中文拼音分段后选择唯一候选。反例：当前值不是目标前缀、动作后缺 exact value、模糊帧、真实换行被当软折行。
- 发送正例：普通消息发送一次且新气泡逐字出现；反例：发送按钮不唯一、effect target/payload 漂移、结果不可见、旧 receipt 重放、第二次发送。

## 通用修复说明

1. 把 `press_enter` 接入唯一 canonical action protocol，而不是另建第二条旁路。Enter 必须由 input structure audit 作为独立、严格、可见的 `input_exact_enter_key` 元素产生；本地仅在 active multiline field 的下一字符恰为 `\n` 时铸造候选。
2. `press_enter` 走与其他局部输入辅助键相同的 fresh 观察、独立几何审计、标定、单击事件栅栏、稳定+清晰采帧和 exact input verifier；执行前后值必须是 `prior → prior+"\n"`。
3. 为 `InputFieldIntent` 增加模型不可伪造的字段身份投影：active field 的 `field_id` 必须与当前子目标引用的字段身份及当前可见 input 的本地身份唯一匹配。多个字段时禁止单字段 fallback；不同字段的 lineage 不能互相复用。
4. 扩展本地安全验收页只作为测试载体，提供长文本、多行和两个具名字段；页面不参与生产编排，也不向生产代码提供 App/坐标特例。

## 影响范围、回滚与停止条件

- 预计影响 observer 输入结构协议、Typed IR 字段身份、canonical catalog、Qwen action schema、controller、adapter、lineage 和相应测试；不改变登录/付款风险政策，不改变普通发送 effect 合同，不改变卖家 `main.exe`。
- 回滚为撤销本批单一提交；历史 canonical 动作和已完成阶段证据保持不变。
- 先完成历史/构造样本重放、最小相关测试和一次完整回归；全部通过后才重载项目 Uvicorn。
- 真机按长文本、换行、多字段、普通发送依次验收。任一物理动作失败立即停止该 session，不自动重试；同类模型合同失败连续两次则停止真机采样并回到离线合同。

## 本批离线实施结果（2026-08-21）

- `press_enter` 已接入唯一 canonical action protocol、Qwen 单步选择、controller、adapter 和设备能力快照；底层仍复用经过验收的 `vision_tap_relative`，但正式动作身份保持为 `press_enter`。
- input structure audit 现在独立报告 `enter_key.key_action`。只有 `newline`、active field 为 multiline、下一事务字符恰为 `\n` 三项同时成立时，才本地铸造 `input_exact_enter_key`；`send/search/done/next/unknown` 均不生成换行候选。
- `InputFieldIntent` 已增加可选 `field_label`；orchestrator 仅从当前 active typed field 本地投影 text/field_id/field_label/multiline。observer 用逐字可见字段标签从最多四个输入框中选择唯一目标，canonical catalog 再核对 field_id/field_label。单字段旧任务保留兼容，多个字段不回退到位置或顺序猜测。
- 测试页新增 `input-long`、`input-multiline`、`input-multifield` 三个安全测试模式；它们只生成验收事件，不参与生产任务编排。
- 正反测试已覆盖：两个字段选择正文而非主题；newline 键生成 `press_enter`；同一位置改报 `send` 时无换行候选；canonical/Qwen/controller/adapter 的正式动作、单击事件栅栏和动作后精确 `prior + "\n"` 全链。
- 相关回归：`645/645` 通过。
- 完整离线回归：`1308/1308` 通过；无失败、无错误。

当前唯一缺口已经从代码/架构缺口收敛为四项真机证据缺口。离线结果不能替代真实手机 session 的 `succeeded` 终态。

## 首次长文本真机门与新增通用根因（2026-08-21）

- 新代码提交 `aceaafb` 已加载到交互桌面 Uvicorn PID `24856`；加载后 controller/camera online、`busy=false`、0 活动会话，卖家 `main.exe` 保持 PID `2904` 和原启动时间，未被启动、停止、点击或重启。
- 0 动作观察精确确认文件传输助手当前草稿为 `longinputvalidation2026:1234`、输入框已聚焦、numeric/direct-latin 键盘可见；未发送。
- 全新 session `68736de1ff6542adad028f9b3f931edf` 的目标是在同一输入框先清空，再输入 37 字符 `longinputvalidation2026:1234567890ABC`。任务图正确生成 active `clear_input` 和 pending `enter_text`，但动作目录为空，最终 `blocked/physical_actions=0`；手机没有变化，也没有重试。
- 已观察的通用根因：`TaskSemanticIR.input_fields[*].source_subgoal_ids` 只把最终 `input_verified_text` 子目标绑定到输入字段，没有把同一输入事务中先行的 `clear_verified_text` 子目标绑定到该字段。canonical catalog 虽构造了清空 affordance，随后因 active clear 子目标没有 `active_input_payload_refs` 而将其删除。视觉模型此时看到的字符键不是根因，且不能绕过目录执行。
- 同类样本集合：单字段“先清空再输入”必须把 clear 与 input 绑定到同一字段；多字段场景只能在可见 `field_label` 唯一对应时绑定 clear，不能把正文清空动作落到主题字段；无输入 payload、标签缺失/重复或 clear 子目标未逐字关联字段时继续失败关闭。
- 最小通用修复：编译 typed input-field ownership 时，把单字段事务中的正式 `clear_verified_text` 子目标加入同一 `InputFieldIntent`；多字段只允许按唯一非空 visible field label 绑定。canonical catalog、Qwen、Policy 和 adapter 不增加第二套规则，继续只消费这一个字段归属。同步修正 `/api/device` 的公开动作列表，使其与实际 capability snapshot 一致显示 `clear_verified_text` 和 `press_enter`，不改变执行权限。
- 验证清单：现场任务图离线重放；单字段 clear+input 正例；多字段唯一标签正例及缺失/重复标签反例；canonical 目录只公开清空动作；相关测试、一次完整回归；重新加载后仅建立一个新的长文本 session。若仍在 0 动作处失败，保存新证据并停止，不换措辞抽样。
- 修复已实现：单字段 clear 与后续 input 共享同一字段；多字段 clear 只按唯一非空 `field_label` 绑定；缺失或同时命中多个标签时不建立归属。`/api/device` 的公开 enabled/protocol action 列表也改为从同一 capability profile 显示 `clear_verified_text`、`press_enter`，执行权限没有变化。
- 新增定向正反 `3/3`、受影响模块 `453/453`、完整 Python 回归 `1311/1311` 通过；生产模块静态编译和 `git diff --check` 通过。真机仍停在原草稿，0 新动作。

### 修复加载后的清空动作结果

- 提交 `14853de` 已加载到交互桌面 Uvicorn PID `27140`；`/api/device` 同时列出 `clear_verified_text` 与 `press_enter`，controller/camera online、`busy=false`、0 活动会话，卖家 PID 未变。
- 全新 session `de6e19d07d4141c0a1b737be05e3240a` 正确生成唯一 `clear_verified_text` candidate 并消费一次 fresh scope，证明 typed 字段归属修复已进入真实运行链。
- 该次物理清空没有误点发送，但未完整删除：动作前值为 `longinputvalidation2026:1234`，动作后独立只读预览显示只剩 `longinp`。动作后采帧在 10 秒截止时最大差异 `46.8 > 38.0`，报告为 `failed/physical_actions=1/post_action_failure`；没有继续输入或重试清空。
- 本次主要归类为 **clear 多退格 transport 未完成/动作后证据失败**，不是 typed 字段归属、Qwen 目标、风险边界或发送误触。它保留为独立 clear 能力缺口；不能写成清空成功，也不在本轮为单次真机样本直接修改生产传输。
- 四项复杂输入主验收改用既有本地安全验收页的全新空白字段，避免把 clear 的独立失败混入长文本、换行和多字段证据。长文本仍必须在同一新 session 从空值完成 20+17 分段，不能从当前 `longinp` 续写。

### 跨 App 回到主屏幕的 typed surface 同义词缺口

- 临时验收页仅绑定当前 Wi-Fi 地址 `192.168.0.137:8770`，不监听其他网卡；它不控制机械臂。随后全新导航 session `0520b14b619a453da287b04df0fac2c9` 正确生成 active `go_home`、pending `open_browser/reload_page/verify_final_page`，但在 0 动作处 blocked，手机未改变。
- 落盘真机帧清楚显示文件传输助手，Qwen 将其摘要成黑屏/加载并不是本地动作目录的根因。离线重放证明 typed IR 只把“主桌面/桌面/主页/home screen/launcher”识别为 launcher，并只把“回到主页/回到主桌面”识别为 `home`；DeepSeek 使用标准 Android 说法“回到手机主屏幕”时，IR 只建立 `current_surface`，active subgoal 无 required action，因此 Home 不可能进入目录。
- 最小通用修复：把逐字“手机主屏幕/系统主屏幕”加入 launcher surface 词汇，把“回到手机主屏幕/回到系统主屏幕”映射到唯一 canonical `home`；不接受泛化的“某 App 主屏幕”，避免把 App 内首页误当系统 Launcher。验证包括现场任务图重放、英文/现有同义词正例和 App 主屏幕反例，修复后未知当前页面也应只产生 Home，不依赖 App、坐标或截图。
- 修复后现场任务图离线重放得到 `surface=launcher`、`required_action=home`、唯一 candidate=`home`；“浏览器主屏幕”反例不生成 launcher/Home。新增定向 `2/2`、相关 `166/166`、完整 Python 回归 `1313/1313` 通过。
- 提交 `bfba1bd` 加载后，相同目标的新 session `794bbed276da436c9bea4b2ef5518ab1` 已由 Qwen 正确选择唯一 Home，但 Policy 在动作前以 catalog digest 不匹配拒绝，`physical_actions=0`。现场两边 candidate ID 相同，Qwen report digest 为 `8c12...`，Policy 重建为 `68bc...`。
- 根因是 Qwen 的隐私视图分支仍在 canonical catalog 之前把完整设备能力再次筛成 `{home}`；Policy 按完整 capability snapshot 重建。它没有产生第二个候选，却改变了 report 中的 affordance/source digest，属于单一动作协议迁移后遗漏的第二筛选器。
- 最小修复是删除该能力集合改写；隐私分支只裁剪发送给模型的图像，不能改变 canonical catalog 输入。正式 typed launcher 子目标本身已经保证目录唯一为 Home。新增回归必须证明 privacy Home 使用完整能力时 Qwen 与 Policy report digest 完全相等；模型伪造非 Home 仍由 canonical catalog 拒绝。
- 修复已实现。隐私 Home digest 正向、隐私图片不泄露、非 Home 伪造三项 `3/3`，Qwen/canonical/Policy 相关 `253/253`，完整 Python 回归 `1314/1314` 通过。

### 只读验收标题被误判为输入动作（2026-08-21）

- 加载提交 `527f18f` 后，controller/camera online、`busy=false`、0 活动会话；卖家 `main.exe` 仍为 PID `2904`，未被操作。新导航请求在 session 建立前以 `409/physical_actions=0` 拒绝：`sg_refresh_until_title` 被判定需要 `input_verified_text`，但没有 `InputFieldIntent`。
- 现场目标中的该子目标只是“刷新当前页面，直到看到标题为长文本输入验收且有空白输入框”。误判来自旧的低层动作词正则：它把名词“输入验收”开头的“输入”当作输入命令；“输入框”本身已有反例保护，因此这不是同一措辞的简单漏词，而是动作识别没有先要求 canonical 输入 payload。
- 根因分类：**通用语义归属缺陷**。没有 typed `input_text` 实体时，本地不可能执行可验证输入，也不应仅凭界面标题、模式名或能力名中的“输入”铸造输入动作。
- 同类样本：长文本输入验收、输入测试、输入能力、input validation 等只读名词均不得生成输入动作；“输入‘你好’”“Type hello”且存在 typed payload 的实际输入步骤仍必须生成 `input_verified_text`；只写输入动词但缺 payload 的任务不能获得可执行输入目录。
- 通用修复：正式 required action 仍使用唯一动作协议，但 `input_verified_text` 的动作词命中必须同时存在 typed `input_text` 实体。此门只决定动作归属，不放宽 payload 精确值、字段身份、动作后验证或风险策略；不添加 App、页面、固定标题或坐标分支。回滚范围仅 `task_semantic_ir` 的动作归属和对应合同测试。
- 验证清单：现场刷新任务图重放；中文/英文只读输入名词反例；中英文真实输入正例；相关语义回归；一次完整回归。全部离线通过后才重新加载，再创建一个全新导航 session；若仍为同类合同失败则停止真机采样。
- 修复提交 `0fe2c86` 已完成：定向 `3/3`、相关 `283/283`、完整 Python 回归 `1315/1315`，静态编译和 diff 检查通过。项目 Uvicorn 仅重载为交互桌面 PID `23040`，controller/camera online；卖家 PID `2904` 与临时验收页 PID `10032` 均未改变。重载过程中最初的任务脚本因 Windows PowerShell 5 将 UTF-8 无 BOM 中文路径按旧代码页解码而启动失败；改用纯 ASCII 临时脚本并 Base64 还原 Unicode 路径后恢复，计划任务和临时脚本均已删除。

### 验收页导航会话的独立模型格式失败

- 全新 session `de8ba3fffc974d6b9b9b18b5ac7094af` 成功完成 Home 与打开浏览器两个真实动作：两步均 `matched`、各 1 个物理动作、旧 scope 作废并产生 typed receipt；累计 `physical_actions=2`。
- 浏览器打开后为新闻聚合首页，底部“窗口”显示已有 2 个标签页；它没有直接恢复本地验收页。进入刷新子目标前的 0 动作重观察失败：目标精查模型返回了严格 schema 之外的顶层字段，session 最终 `failed`，没有执行刷新或其他动作。
- 本次归类为 **单次模型合同输出失败 + 手机起始标签页状态不符**。它不是长文本、换行、多字段动作本身的失败，也没有达到同类连续两次门槛；不据此修改生产 parser 或为浏览器首页增加专用分支。
- 下一最小闭环：在全新导航 session 中只打开浏览器现有标签页列表并寻找本地验收页。若标签页不存在，则通过通用地址栏输入本机验收 URL；不得假设刷新首页能进入验收页。任何动作失败立即停止对应新 session。

### 连续两次目标精查坐标 shorthand 失败的通用收敛

- 新 session `b99ff5c4ba7240e8b41fbe73247a51f1` 以“标签页列表”作为控件逐字目标，0 动作 blocked；这是目标结果与入口控件名不一致，未改代码。显式通用视觉命令的新 session `431d0dda04b94548b5cab02a407c6d56` 已建立唯一“窗口”按钮和 fresh scope，但确认前 fresh 几何复核再次以相同目标精查 schema 错误停止，`physical_actions=0`，按钮没有被点击。
- 两份脱敏 raw 证明报错文字“包含额外顶层字段”不准确：顶层都严格只有四个字段。真正失败都是 `bounds` 返回了 `{x,y,w,h}`，且 y 使用了大于 1000 的纵向画布坐标；本地拒绝把未知坐标系猜成 full-frame normalized bounds。第一份返回新闻列表容器且没有现有同身份目标；第二份返回“窗口”按钮，与同一 fingerprint 的 compact scene 中唯一高置信、完整可见 `label=窗口/role=button/meaning=window_button` 元素严格同身份。
- 根因分类：**通用模型合同输出 + 有界规范化缺口**。不得按 1920、2000 或画面比例猜测坐标，也不得因标签相似绑定；但当同一观察的 base scene 已有唯一逐字 label、role、meaning、高置信、完整可见且 goal-relevant 元素时，targeted delta 的无效坐标不应覆盖该本地已有粗几何。动作前仍必须通过独立单 crop geometry audit，base bounds 不能直接成为执行点。
- 同类样本：唯一同身份按钮的 invalid xywh 可丢弃无效 shorthand 并复用同 fingerprint base 粗框；无逐字标签、0/2 个同身份项、role/meaning 不同、base 低置信/裁切/非目标，以及没有 base 对应项的新闻容器都必须继续失败关闭；合法 normalized array 和合法 0..1000 xywh 行为保持不变。
- 通用修复：在 targeted delta 严格 parser 中增加一次确定性 identity reattachment，只替换 `element_id+bounds` 为唯一 base 元素的原值，不换算模型坐标、不新增语义、不改变风险/动作权限；后续 adapter 继续对 planned 与 fresh 两组四帧分别做 geometry audit 并比较 IoU。回滚仅撤销这一本地规范化函数。
- 验证清单：重放两份现场 raw（窗口正例、新闻容器反例）；唯一/重复/裁切/低置信变化样本；既有 xywh 正反；observer/geometry/adapter 相关回归；一次完整回归。离线全绿后才重载并创建新 session，绝不复用已消费 scope。
- 修复已实现：无效坐标本身永不换算；只有逐字 label、role、meaning 唯一一致，且同 fingerprint base 候选已经高置信、完整可见、goal-relevant 时，才复用 base 的 element_id 和粗 bounds。动作前双组独立 crop geometry audit 未被移除或放宽。窗口现场 raw 正例与新闻容器反例、重复/裁切/低置信变化样本及既有 xywh 合同均通过。
- 定向测试 `2/2`、相关观察/geometry/adapter/编排回归 `426/426`、完整 Python 回归 `1316/1316` 通过；无测试断言失败。一次相关回归命令误列了不存在的 `test_generic_action_adapter.py`，产生单个 loader error，随后依据文件清单用真实 `test_semantic_action_adapter.py` 补跑 `31/31`；该命令错误不计入通过数，也未触及运行服务或手机。

### 可见列表项前缀定位未推进的通用缺口

- `e1cb6f6` 加载后，全新 session `844129575c7743e8b0cccd4950092354` 已越过原 targeted schema 阻塞，并以一次真实 `tap_semantic` 打开浏览器标签页卡片界面；动作后画面明确显示两个卡片，右侧标题为“通用动作真机验…”。session 的动作 receipt 为 matched，但 DeepSeek 将页面命名为“多任务/最近应用”，没有完成“标签页列表可见”，最终 `blocked/physical_actions=1`；不把它记为成功，也未重试。
- 新定位 session `88a23ad1e9d4493b8318c4640740c55e` 的 DeepSeek 图正确拆成 read-only `locate_card` → navigation `click_card` → read-only verify。0 动作观察已经返回唯一 `list_item`，逐字可见 label=`通用动作真机验...`、高置信、完整可见、goal-relevant，但本地 presence gate 只允许页面 identity 的 text/container 先证明命名对象，且把“标题”同时误当成目标控件 role 要求，导致 locate 阶段 blocked、0 动作。
- 根因分类：**通用只读定位证据绑定缺口**。目标是“标题开头为 X 的卡片”时，标题是列表项的可见 label selector，不要求该列表项自身 role=text/title；唯一可信 list_item 的 label/evidence 与 X 的有界文字项交集足以证明定位，但仍不能证明点击已执行或页面结果。
- 同类样本：列表项/卡片 label 以前缀或截断省略号显示时可完成只读 locate；按钮、菜单项等唯一可见对象也可由自身 label/meaning/evidence 命名。0/2 个 goal element、无词项交集、裁切、低置信、冲突元素、要求 exact value/外部结果/不可见或消失状态仍不得零动作完成。
- 通用修复：presence 的第一层 grounding 允许同一 scene 的唯一可信 goal element 以自身 label/meaning/evidence 证明命名；当完成条件中的“标题”只是 selector 且 candidate 有非空 label 与目标词项交集时，不再额外要求 candidate role=title。后续安全内框、surface class、唯一性、DeepSeek 逐字 evidence、revision+1 和前缀推进门全部保留。
- 验证清单：现场 list_item 前缀正例；重复、裁切、低置信、无词项交集反例；既有输入框/页面/多元素 presence 回归；编排相关与完整回归。离线通过后才重载并新建定位会话。
- 修复已实现：显式中文“标题开头/起始为”和英文 `title starts/begins with` 被解析为 bounded prefix selector；唯一 candidate label 必须逐字以该 selector 开头。通用“卡片/列表”等词项本身不能建立身份，重复、裁切、无 selector 匹配继续 0 动作 blocked。selector 命中后仅完成 read-only locate，仍不直接执行点击。
- 定向 `2/2`、相关实际测试文件合计 `193/193`、完整 Python 回归 `1317/1317` 通过。两次相关命令曾误列不存在的测试模块名，只产生 loader error，随后均由 `rg --files` 确认真实文件名并补跑；生产断言与完整回归均无失败。

### 页面刷新被 DeepSeek 错标为 effect

- `b23cf63` 加载后，前缀 locate 已在 session `83095591ad2447df918bf0d94ae66ed0` 以 0 动作推进至 revision 2；随后 0 动作新 fingerprint 重规划因模型改写冻结完成条件而 blocked。未改代码，改用当前可见完整标题的直接动作 session `dae3bdf484e340198491712ecabf7a0f`，一次点击打开本地验收标签并最终 `succeeded/physical_actions=1`。
- 正式长文本 session `924ce8987fc547ee869dd0e98feb04e3` 的 0 动作观察证明该标签仍是缓存的“通用动作真机验收页”，不是服务器当前 `input-long` 模式，因此没有目标输入框，正确 blocked。刷新准备请求在 session 建立前失败：DeepSeek 将“点击刷新图标，重新加载当前页面”标成 `execution_class=effect`，同时 `effect_intents=[]/effect_ids=[]`，typed parser 以 `effect 子目标必须引用 effect_intent` 拒绝，0 动作。
- 根因分类：**模型 execution class 与本地 typed effect 权威冲突**。当前页面 reload/refresh 只改变本机临时可见页面，属于 navigation；模型标签不能把它变成外部效果。反过来，提交、发送、保存、发布、删除、关注、评论、登录、付款等词出现时不能被该规则降级。
- 同类样本：当前页刷新/重新加载/重新载入，且无 effect intent/ref，为 navigate 正例；“刷新后提交表单”“刷新并登录”“刷新后支付”、已有 effect intent/ref、未知同步/写入语义均保持 effect/unknown 或严格拒绝。
- 通用修复：在严格 JSON payload 进入 graph dataclass 前做一次局部确定性归类，只把同时满足 `execution_class=effect`、零 effect intent、零 effect_ids、目标子句明确为当前页面 refresh/reload 且无外部效果词的节点改为 navigate。它不根据 App 名、页面、坐标或模型风险标签授权，后续 formal semantic IR/canonical/Policy 仍完整校验。
- 验证清单：现场 raw 重放；中英文 refresh 正例；混合 submit/login/payment 和有 effect ref 反例；DeepSeek transport、typed IR、canonical、orchestrator 相关回归和一次完整回归。全绿后才重载并新建刷新 session。
- 修复已实现：仅在全局 `effect_intents=[]`、节点 `effect_ids=[]`、语义明确为当前 page/tab/view refresh/reload 且没有任一外部效果词时，把模型的 effect enum 归一化为 navigate。中英文正例通过，刷新+提交/登录/付款反例仍以 unbound effect 拒绝。
- 定向 `1/1`、相关 DeepSeek/typed IR/canonical/orchestrator `193/193`、完整 Python 回归 `1318/1318` 通过；静态编译与 diff 检查随后执行。

### 刷新语义被拆到总目标与子目标后的漏判

- 加载 `b371ac1` 后的新请求仍在 session 建立前 `409/physical_actions=0`：总目标包含“重新加载当前页面”，但 active 子目标仅写“点击刷新图标/刷新图标已被点击”。旧归一化只扫描子目标，因而没有看到“当前页面”，原始 `execution_class=effect` 再次被严格 parser 拒绝。
- 根因分类：**同一 typed 语义跨 goal/subgoal 投影时的本地归一化缺口**，不是风险政策、视觉定位、窗口状态或机械臂问题。
- 通用修复：刷新归一化同时读取总目标和当前节点，但要求节点自身仍逐字包含 refresh/reload 动词；总目标或节点任一处出现发送、提交、保存、发布、删除、关注、评论、登录、付款等外部效果词即不降级。这样不会把总目标中的“刷新”错误传播给无关的提交节点。
- 验证新增现场拆分样本正例和“总目标刷新后提交、当前节点提交表单”反例；离线全绿并重载后只创建一个新的刷新 session。
- 修复后现场定向 `1/1`、相关 DeepSeek/typed IR/orchestrator `177/177`、完整 Python 回归 `1318/1318` 通过；生产模块静态编译通过。一次测试命令从仓库根目录按 package 名导入时因项目测试使用 bare module import 产生 loader error，随后从规定的 `poc` 目录重跑通过；该启动错误不计入通过数，也未触及手机或运行服务。

### 无文字刷新图标与任务文字别名无法绑定

- 归一化修复加载后，新 session `24af57111a7a4029ac53584f2fb5a7ac` 已正确生成 navigation task graph，但在 0 动作 Qwen 本地门 blocked。可信场景实际只有一个 `meaning=reload` 的 icon，且具备本地 `reload_visual_audit=true`、`independent_geometry_verified=true`、`fully_visible=true`；其 label 为空。任务图的 `target_ui_label=刷新图标` 被 exact-text 门当成必须出现在控件文字中的字面字符串，因此唯一已审计图标仍无法绑定。
- 根因分类：**文字实体与无文字本地语义控件之间的 typed identity 缺口**。不是候选真的重复，也不是坐标、风险或窗口问题。
- 通用修复：只有固定 refresh/reload 语义别名可以绑定到本地严格审计过的唯一 reload icon/button；仍要求三项本地状态全部为真，且后续 canonical catalog、Policy、fresh geometry audit 完整执行。未审计 reload、书签/扩展图标、任意其它文字（例如支付图标）均不能借此通过。
- 验证清单：现场状态正例；缺本地 audit 反例；非 reload 文字反例；Qwen/canonical/Policy 相关回归与一次完整回归。全绿后只重载并创建一个新的刷新 session。
- 修复后定向 `1/1`、Qwen/canonical/orchestrator 相关 `255/255`、完整 Python 回归 `1319/1319` 通过；生产与测试模块静态编译通过。

### 描述性“可点击”被几何审计误判为动作指令

- 为进入模式选择页建立的新 session `16f799d101c440529e97cc2222615b67` 已生成唯一文字按钮、fresh scope 和 allowed controller decision；确认前独立几何审计在 0 动作处拒绝，原因是元素原始 evidence 含“白色可点击链接文本”。
- 根因分类：**几何证据控制语言分类过宽**。中文正则把描述可见 affordance 的“可点击”与命令式“点击该链接”混为一类；英文 `clickable` 原本不会命中 `click` 单词边界，形成中英文不对称。
- 通用修复：证据校验只把逐字“可点击”视为描述性可见属性并在控制词扫描前去歧义；“点击该项/点击该链接”、click/tap/press/drag/swipe、坐标与执行建议仍严格拒绝。修复不改变控件身份、几何、动作权限或外部效果政策。
- 验证清单：现场 evidence 正例、命令式点击反例、geometry observer/adapter 相关回归与一次完整回归；全绿后新建会话，不复用已消费 scope。
- 修复后定向 `1/1`、geometry/observer/adapter 相关 `302/302`、完整 Python 回归 `1320/1320` 通过；生产与测试模块静态编译通过。

### canonical catalog 把完整长文本误当成单步后置条件

- 正式长文本 session `5df986ee03f94f38ac9e37e25b982d0c` 已完成一次输入框聚焦并 `matched/physical_actions=1`；随后场景严格证明唯一输入框为空、已聚焦、direct-Latin QWERTY 可见。第一段输入仍在 0 动作处 blocked，错误为“精确文字输入的后置条件没有绑定下一确定性分段”。
- 根因分类：**唯一 canonical action protocol 的分段投影缺口**。catalog 已用 `plan_from_input_states` 判断该输入动作可执行，却把 transition expectation 固定为完整 typed payload；控制器则正确按同一 planner 得到最多 20 字符的下一 direct-Latin 分段，两者必然冲突。
- 通用修复：direct-Latin `input_verified_text` candidate 的 `element.state.value` expectation 改为同一个 deterministic input step 的 `expected_value`。短文本行为不变；超过单段上限时，第一动作只承诺第一段前缀，动作后新观察再生成下一段 candidate。Qwen 仍只选择 choice_id，不能改写文字或后置条件。
- 验证清单：37 字符空值正例必须得到长度 20 且非完整目标的 expectation；短文本延续保持完整值；Qwen/controller/adapter/input-lineage 相关回归及一次完整回归。全绿后刷新当前验收页并从空框建立一个全新 session，不复用失败 session。
- 验收页原目标 `longinputvalidation2026:1234567890ABC` 虽总长 37，却混合了小写、数字、冒号和大写；事务会按键盘类型拆成小写批次、逐字数字/符号与大小写切换，不能单独证明“超过单段上限的同类文字连续分段”。验收载体改为 37 个纯小写字符 `abcdefghijklmnopqrstuvwxyzabcdefghijk`，使本轮能力和证据边界保持单一；数字、符号和大小写仍由各自既有动作合同承担。
- 设备能力快照先前写 `max_chars_per_physical_step=30`，而实际 deterministic planner 的 direct-Latin 上限为 20；本批将公开元数据改为引用 planner 的唯一常量 20，避免验收、UI 和执行器继续宣称不真实的单步上限。底层机械输入函数仍可接受更短的合法片段，未扩大动作权限。
- 修复后定向 canonical+验收页 `2/2`、实际相关模块 `520/520`、完整 Python 回归 `1321/1321` 通过；生产与测试模块静态编译通过。一次相关测试命令误列不存在的 `test_universal_action_controller.py`，产生单个 loader error；随后按实际文件清单补跑真实控制器覆盖并通过，该错误不计入通过数。

### 2026-08-21 新版长文本真机尝试（外部连接阻塞）

- 页面刷新 setup session `a9c4a364ea404846bd3ecce91613abb6` 的刷新点击 `matched/physical_actions=1`；后续 0 动作重观察逐字看到了新版 37 字符目标 `abcdefghijklmnopqrstuvwxyzabcdefghijk` 和空白“长文本”输入框。该 setup session 最终被取消，不能写成正式验收成功。
- 正式长文本 session `f41fb859dc794b24b5e361f05e3fc8eb` 先以 0 动作定位唯一空白“长文本”输入框，随后只执行一次聚焦点击；动作后四帧已保存，但 Qwen 动作后观察连接连续两次收到 `[SSL: UNEXPECTED_EOF_WHILE_READING]`，因此报告为 `failed/physical_actions=1/post_action_failure`，没有执行任何文字输入，也没有自动重试。
- 失败主因归类为 **模型服务/网络连接状态**，不是 canonical 分段协议、坐标、风险边界或输入 transport。失败后本地状态接口进一步显示 controller/camera 离线；按一次动作失败即停止规则，本批在取得用户恢复方案前不得建立新会话或重试聚焦。

### 2026-08-21 只读恢复诊断：DashScope 被当前 Clash GLOBAL 出口中断

- 用户明确允许只读诊断后，核对发现项目 Uvicorn 和临时验收页进程均已退出；卖家 `main.exe` 仍由用户环境运行，但 PID 已变为 `2144`。本轮没有启动、停止、重启或点击卖家程序。
- 仅恢复本项目 Uvicorn 为 PID `15940` 后，官方类型化客户端重新验证 `/api/session` 与 OpenAPI；`/api/device` 显示 controller/camera online、`busy=false`、0 活动任务/会话，Qwen 仍为 `qwen3.7-plus`、思考关闭、密钥已配置，DeepSeek 请求正常。
- 新建的只读观察请求在 `physical_actions=0` 阶段再次收到 Qwen 两次 `[SSL: UNEXPECTED_EOF_WHILE_READING]`，目录为 `poc/output/web/generic_supervised_20260821_131116_ed46a150/`。四帧已保存；最后一帧人工只读确认新版长文本验收页、37 字符目标和空白长文本框仍可见，手机没有发生动作。
- 独立的无鉴权 `/models` TLS 探测同样在握手阶段 EOF，证明错误与图片、prompt、schema、API key 和 canonical action 无关。DNS 将 `dashscope.aliyuncs.com` 映射为 Clash fake-IP `198.18.0.138`；Clash 当前 `mode=global`，日志确认相关连接均走 `GLOBAL`。显式使用本地 mixed proxy `127.0.0.1:7897` 仍同样 EOF，而其他 HTTPS 域名可以正常建立连接。
- 当前主要根因归类为 **外部网络/代理出口故障**。按同类模型合同连续失败止损规则，不再采样 Qwen，也不执行任何物理动作。恢复条件是经用户批准把 Clash 临时切到现有 `rule` 模式（配置中 `aliyuncs.com` 已有 direct 规则）或由用户提供其他可用 DashScope 网络出口；先用无鉴权 TLS 探测证明连接恢复，再建立全新长文本 session，旧失败 session 不复用。

### 网络恢复后的“当前页面”定位误判修复

- 用户批准临时切换后，Clash 运行态已为 `rule`；DashScope 无鉴权 `/models` 探测由 SSL EOF 恢复为预期 HTTP `401`，证明 TLS 与路由已恢复。项目服务保持 PID `15940`，controller/camera online、设备空闲。
- 网络恢复后的全新请求目录 `generic_supervised_20260821_131921_2138899d` 在 `physical_actions=0` 阶段停止。Qwen 已正确观察唯一空白、标签为“长文本”的输入框；DeepSeek replan 也引用了当前 typed visual claim，但本地命名页面门把“找到当前页面中的输入框”误读成名为“找到”的页面，报“命名页面完成声明缺少结构化画面身份锚点”。
- 通用修复仅将“找到/定位/观察等任务动词 + 当前/同一/目标页面”识别为位置描述；系统设置页、订单详情页、具名聊天页等真实命名页面仍必须有结构化身份。没有修改输入候选、动作目录、几何、风险或执行权限。
- 新增现场完整 replan 正例和中英文变化样本；真实命名页面反例继续拒绝。定向与相关回归 `149/149`、完整 Python 回归 `1323/1323`、生产文件静态编译和 `git diff --check` 通过。首次新增测试误用了不存在的 trigger `start_observation`，随后改用正式 `observation_changed` 并通过；该测试命令错误没有触及生产服务或手机。
- 修复已提交为 `8eb8e30`。用户两次明确批准只重载项目 Uvicorn PID `15940`，但 Codex 自动审批审查的响应流均在完成前断开，进程命令被平台拒绝且未执行。源码与 OpenAPI 中没有公开的优雅 reload/restart/shutdown 路由，因此不能通过项目 API 安全替代；当前运行服务仍是加载修复前代码，手机 0 新动作。
- 审批通道恢复后，项目 Uvicorn 已从 PID `15940` 只重载为 PID `1736`；卖家 `main.exe` 保持 PID `2144`。全新 session `8bf211e67a4e446cbfdc79929cc04f1e` 的 0 动作观察成功把 `locate_input` 推进为 completed，证明 `8eb8e30` 已加载；随后唯一聚焦动作 `matched/physical_actions=1`，输入框为空、`focused=true`、QWERTY direct-Latin lower 可见，receipt `receipt_e560ab11d5f645b1bfddb04092adc250`。
- 同一 session 在动作后下一选择处 `blocked`，没有开始输入。确定性根因是 Qwen 旧逐字目标验证已经确认动作绑定唯一“长文本”输入框，却又只允许 `tap/dismiss`，把 canonical `input_verified_text` 二次否决。该重复动作类型否决已删除：逐字要求现在只约束动作必须绑定正确元素，或绑定到该输入元素的本地审计键盘辅助控件；非元素动作仍不能忽略逐字目标。
- 精确标签输入、键盘 literal/mode 辅助关系、清空旧行为和错误目标反例均固化；相关回归 `256/256`、完整 Python `1323/1323`、静态编译和 `git diff --check` 通过。旧 blocked session 不复用；加载后必须从当前已聚焦空框建立全新 session，不能重复聚焦。

### 重新加载后的粗观察键盘重复权威修复

- `f096ca3` 加载后的全新 session `e2ce66f6becf48eb829540290c40baf0` 在 0 动作观察阶段失败，`physical_actions=0`。第一轮 compact 把键盘模式键写成超出完整画面的 bounds；第二轮专用 input audit 又把整套 QWERTY anchors 整体向下错估，本地布局校验因此拒绝，手机没有执行输入或其他动作。
- 保存帧只读确认真实页面仍是唯一空白“长文本”输入框、已聚焦、完整 direct-Latin QWERTY 可见。不能放宽 anchors 阈值，否则会把错行坐标送给控制器；第二轮属于应被拒绝的模型几何错误。
- 通用根因是 explicit text-input 任务仍允许 compact observation 保留一份键盘容器/键盘专用控件几何，而后续 input-structure audit 才应是唯一键盘结构权威。前者的坏坐标会在专用审计运行前浪费整轮观察。
- 修复删除 explicit text-input 路径中的 compact 键盘重复权威：只隔离 typed meaning 明确属于专用键盘目录、字段形状完整且不含动作字段的元素；键盘模式切换专门任务不受影响，动作字段注入、含糊元素和非输入目标仍由 compact parser 严格拒绝。专用 input audit 继续完整校验输入框、键盘模式和真实 anchors，未放宽几何阈值。
- 现场坏 bounds 正例和动作字段反例 `2/2`、generic observer `233/233`、planner/Qwen/canonical `233/233`，合计相关 `466/466`；完整 Python 回归 `1324/1324` 通过。首次定向命令误从仓库根目录运行导致两个 import loader error，随后从正式 `poc` 目录按同一测试名重跑通过；该命令错误未触及生产服务或手机。

### 空输入审计不再伪装成候选协议阻塞

- 加载 `64dc14f` 后的全新 session `aca22a9f5bd1464790d9845863bc029f` 完成两次 0 动作观察并推进到真实 `input_text` 子目标，但第二次专用 input audit 返回空 application_inputs；旧路径把空场景交给 Qwen，最终误报“当前可信候选中不存在唯一逐字：长文本”。session 为 `blocked/physical_actions=0`，手机无动作。
- 通用根因是 dedicated input audit 的一次合法空列表只有在用户目标自带 top/bottom 等方位词时才会获得高清局部复核；即使 compact 已唯一看见输入框，没有方位词也会丢弃这份只读定位提示。该错误属于观察分辨率升级缺口，不是风险、动作边界或真实目标缺失。
- 修复后，explicit active input transaction 可以把 compact 中唯一且 bounds 合法的 input 仅用作裁剪提示：裁剪覆盖该框上方余量至完整键盘底部，所有可执行 input/keyboard geometry 仍必须由独立 crop-local input audit 重新识别、映射并通过现有严格校验；compact bounds 本身永不进入动作候选。用户方位 ROI 仍优先，顶部近全屏裁剪不会重复采样。
- 若完整审计和唯一一次高清局部审计都未建立专用本地目标，现改报明确观察故障，不能再下沉为 Qwen 候选/产品协议 `blocked`。中文 IME 精确候选等专用本地目标按审计目录自身唯一性判断，粗观察候选仍由既有撤权流程处理。
- 当前现场空审计、变化标签/文字、双空审计故障和中文候选回归 `5/5`；observer+planner 相关 `337/337`、完整 Python 回归 `1326/1326` 通过。测试夹具初次有一处裁剪坐标期望计算错误和一处断言插入位置错误，均只修改测试并重跑通过；生产服务和手机未受影响。

### 第一段真实输入成功，但动作后相机归位超出 10 秒预算

- 加载 `33a5cd0` 后的全新 session `25ea287294234b6998ab0c79b91861cc` 通过 0 动作定位与两级输入审计，唯一候选为已聚焦空白“长文本”输入框；正式动作是 `input_verified_text`，完整目标 37 字符，但 canonical transition 只承诺确定性第一段 `abcdefghijklmnopqrst`（20 字符）。
- 确认一次后 `physical_actions=1`，机械输入真实完成；动作后采集在默认 `post_action_settle=1.5s`、`post_action_timeout=10s` 内持续拍到偏离手机的机械结构，外圈变化 `58.1 > 38.0`，因此 session 正确终止为 `failed/post_action_failure`，没有自动重试，也没有执行第二段。
- 随后的独立 0 动作只读 session `307724c2500e41a7a29a4b131a71af00` 证明相机已自行回到手机画面，输入框逐字显示 `abcdefghijklmnopqrst`，页面显示当前长度 20；该证据证明第一段物理输入完成，但不能改写前一 session 的 failed 终态，也不能替代同会话 37 字符最终 succeeded。
- 当前根因归类为 **动作后相机/机械归位时间超过固定采集预算**，不是输入落点、分段协议、Qwen、风险边界或手机页面。下一修复必须等待用户选择：推荐为输入/清空/拖动等机械持续动作使用更长的动作后稳定预算，并在本地同时要求“连续四帧稳定 + 回到动作前同一手机取景/清晰度”，一旦满足立即返回；不得重复物理动作，也不得只放宽稳定阈值。

### 用户选择方案 1：持续动作自适应等待与手机取景身份门

- 现场帧离线校准：动作前到机械结构帧的 outer-band 取景差异最低 `60.5`，动作前到相机归位后手机帧仅 `5.7`；正式默认阈值取 `45.0`。该门比较动作前后四帧外圈物理取景并取中位最近差异，避开大部分中央输入/滚动内容变化，单帧不能建立归位权威。
- 生产默认普通动作仍为 10 秒；`input_verified_text`、`clear_verified_text`、`drag`、`long_press` 使用最多 45 秒的动作后预算。采集循环满足连续四帧稳定、输入连续性动作的相对清晰度以及可比较真实帧的手机取景身份后立即返回；等待只采集画面，绝不重复动作。
- 只有动作前真实帧达到既有最低清晰度时才启用取景身份比较；低纹理帧无法证明取景身份，继续保留稳定性和后续 Qwen/Controller 校验，避免把纯色测试或真实低信息页面错误当成相机偏移。稳定但仍在非手机取景的画面会以明确运行故障停止，不能进入模型观察。
- 调用方显式传入短 `post_action_timeout` 时，未显式覆盖的持续动作预算继承该短值，保留测试/受控运行兼容；生产无显式覆盖时才是 `10s/45s`。现场标定变化样本、错误取景超时、动作分类与兼容预算 `4/4`，adapter+observation images 相关 `121/121`，完整 Python 回归 `1329/1329` 通过。一次初始相关回归因旧测试的纯色 drag 帧触发默认 45 秒等待而人工中止；修复为“仅清晰可比帧启用取景身份”后同一测试及全回归通过。

### 占位文字消失后的 typed 字段身份连续性修复

- 现场失败 `6f30239f00cf48009dc90992c57a1f7c` 已离线精确复现：可信 scene 中唯一输入框为 `local_audited_input_1/application_text_input`，`input_field_id=input_field_1`，当前值与可见 label 均为 `abcdefghijklmnopqrst`，它是授权 payload `abcdefghijklmnopqrstuvwxyzabcdefghijk` 的精确前缀；旧逐字门仍只寻找已经消失的占位标签“长文本”，因此在 Qwen 前以 `exact_text_missing` 阻断，`physical_actions=0`。
- 根因分类为 **Qwen 前置逐字身份桥接的通用代码缺陷**。typed input transaction 已经拥有字段 ID、payload 和活动子目标归属，但逐字目标门没有在占位文字被真实值替换后继续使用这些正式事实；这不是 App、截图、坐标或模型随机问题。
- 通用修复新增 `_active_input_transaction_exact_candidate_ids()`。它只接受活动子目标唯一 typed input field、合法 input payload、唯一可信 `application_text_input`、逐字相同 `input_field_id`、必要时逐字相同 `input_field_label`、`goal_relevant=true`、`fully_visible=true`、当前非空值为授权 payload 精确前缀且元素 label 逐字等于当前值。任何一项漂移都不能建立目标身份。
- 空字段继续由原可见占位标签匹配。桥接是否接管只看同一 typed `field_id`，不会因多字段页面中另一个输入框非空而错误阻断当前空字段；也不会把另一字段或任意同名元素提升为当前目标。
- 样本集合包括：现场 `input_field_1` 正例；错误字段 ID、错误前缀反例；变化样本 `field_label=备注/input_field_id=notes_field/payload=releasecandidatecontinuation`；以及“当前备注字段为空、另一标题字段非空”仍沿用占位标签的多字段正例。
- 验证结果：最小 `2/2`、Qwen/canonical/typed input/observer/orchestrator 相关 `546/546`、完整 Python `1331/1331`；`git diff --check` 通过。完整回归只出现既知测试子进程 `ResourceWarning`，无断言失败。
- 当前只能声明 **通用修复已实现、相关测试通过、完整回归通过**。尚未加载运行服务或取得第二段真机动作；严格长文本验收仍要求恢复设备后，在同一全新 session 连续完成两个输入动作、最终逐字值为 `abcdefghijklmnopqrstuvwxyzabcdefghijk`、`status=succeeded`、`active_subgoal=null`。

## 横向裁剪长文本后置验证的通用修复（2026-08-21）

- 上一节修复已本地提交为 `7c1d039 fix: preserve typed input identity across prefixes`，未推送。项目 Uvicorn 只重载为 PID `15740`；官方 CLI 启动检查显示 `service_version=0.2.0`、`openapi_verified=true`。卖家 `main.exe` 始终保持 PID `2144` 和启动时间 `2026-08-21 12:47:52`，未被启动、停止、重启或操作。
- 修复加载后，全新 session `bf6041e0777045b993132721edbd554f` 在 0 动作观察中逐字确认同一 `input_field_1` 当前值为 `abcdefghijklmnopqrst`，唯一下一动作是追加 `uvwxyzabcdefghijk` 的 `input_verified_text`。只消费一次 fresh scope，执行一个物理动作；没有自动重试、发送或其他动作。
- 正式会话终态仍必须记录为 `blocked/physical_actions=1/action_outcome=mismatched`，不能改写为成功。receipt 为 `receipt_e4b7b1f9db704307b89afaef0bccf8b8`；before/after observation 为 `obs_57a7bf659d4c4743b9f23e322e0a23ea → obs_c0b1463537304fec821860a3d86c5ce0`，fingerprint 为 `ed6f9056af2d4a401f1c → 0e433a9d3af1a2c6bae2`。正式报告位于 `poc/output/web/generic_supervised_20260821_151741_bf6041e0/report.json`。
- 动作后四帧同时显示验收页“验收通过（回传失败）”和“当前长度：37”，单行输入框因光标位于末尾而只露出 `nopqrstuvwxyzabcdefghijk`。页面自身严格比较的完整 DOM 值已经等于 37 字符目标，但通用观察合同把横向滚动后的可见尾段写入 `states.value`，控制器又把它当完整值比较，形成假 mismatch。页面文案只作为本次根因证据，生产修复不读取这些文案。
- 同一动作后页面新增结果提示使输入框整体下移；原几何重叠门不能单独证明它仍是同一输入框。动作前后观察均保留相同 typed `input_field_id=input_field_1`，因此本批把“横向尾段值恢复”和“布局变化后的字段身份”作为同一次精确输入交易的一个通用验证闭环处理。
- 通用修复只在以下条件全部成立时恢复完整值：正式 canonical candidate；`direct_latin` 的精确 `prior + fragment = expected`；聚焦、完整可见的单行 `application_text_input`；动作前后逐字相同且非空的 typed `input_field_id`；QWERTY/直输模式一致；可见值是 expected 的真后缀、完整包含授权 fragment，并至少带有 4 个字符的旧前缀尾部跨段重叠；元素 evidence 逐字包含该可见尾段；App/页面身份仍兼容。恢复后的 scene 同时保留 `visible_value_suffix` 与 `value_visibility=horizontal_suffix`，控制器只有在这个已验证标记和 typed field ID 同时成立时才允许布局位移。
- 错字段 ID、只看见新 fragment 而没有旧前缀重叠、错误尾段、缺少逐字可见 evidence、非正式 candidate、换行/多行或非 QWERTY/直输场景全部保持 mismatch。没有增加 App、验收页、截图、固定坐标或页面文案特例。
- 正向样本包含本次生产形状，以及变化样本 `notes_field: releasecandidate + continuation`、可见尾段 `didatecontinuation`；四类拒绝样本与它们同批验证。历史正式报告离线重放后得到完整值 `abcdefghijklmnopqrstuvwxyzabcdefghijk` 并通过控制器后置校验，但这只证明修复可重放，不能回写原 session 的 blocked 终态。
- 验证结果：新增定向 `1/1`（两个正向子样本及四类反例）；相关 observer/lineage/adapter/controller/canonical/Qwen/orchestrator 回归 `679/679`；完整 Python 回归 `1332/1332`，耗时 `66.518s`；`git diff --check` 通过。完整回归仅出现既知测试子进程 `ResourceWarning`，无断言失败。
- 最新官方只读状态为 controller/camera online、`busy=false`、0 活动任务/会话，卖家 PID `2144` 未变。按单动作失败止损，本轮没有继续任何真机动作。严格同会话两段长文本、换行、多字段和新的普通发送同会话成功证据仍全部未完成；新代码必须先本地提交并加载，之后才可用全新会话重新验收。

## 2026-08-21 长文本严格重验：代码闭环完成，真机仍未验收

### 通用修复与验证

- 旧固定 App 运行链退役后，通用 start 入口仍访问已删除的 `runtime.store`，导致正式 API 在会话建立前 HTTP 500。提交 `5cb91b9` 删除该残留旧队列依赖并增加防恢复回归。
- 输入结构审计已逐字识别附着字段标签，却在用 `local_audited_input_1` 替换粗场景输入元素时丢弃 `field_labels`。提交 `d6142df` 将已审计字段标签保留为输入候选的字面证据；多字段变化样本证明只保留被选中字段标签，不串到其他字段。
- DeepSeek 两次输出了唯一可无歧义修复的 transport 形状：把 `target_surface` 放到 `goal` 直辖层，以及把内部 `navigation_only` 写入正式 `execution_class`。提交 `35be67c` 只对已知一一映射做确定性字段归位/枚举归一化；冲突字段和未知枚举继续严格拒绝。
- 清空已有草稿不需要新输入 payload，但 canonical 目录旧逻辑把 `clear_verified_text` 与 `input_verified_text` 一样要求 `active_input_payload_refs`。提交 `e819e96` 只在子目标明确要求清空/删除输入框文字、且当前唯一聚焦输入候选值非空时暴露 `clear_verified_text`；“删除”作为跨 App 普通同义语义进入 typed required action，其他动作不放宽。
- 同期正式基线还包含 `ea1810f` 与 `41bbde8`，删除重复字面否决并记录边界；它们由并发任务独立提交，本批完整保留并在合并磁盘状态上回归。
- 最小/相关证据包括：旧就绪依赖 `1/1`、固定链退役/官方客户端 `15/15`、字段标签变化样本 `1/1`、observer/Qwen/orchestrator `479/479`、DeepSeek transport `26/26`、清空目录与同义词 `16/16`、Qwen 最新基线 `119/119`。最终完整 Python 回归为 `1345/1345`，`skipped=54`，无断言失败。

### 真机动作与严格结论

- 清空准备 session `c850fdcee747422494d16083547176e5` 正确生成唯一 `clear_verified_text`，绑定 fresh observation `obs_aba5c05c11224fb7b213c556317d3de4` / fingerprint `91a8ed128ffcd26254c5`。只执行一次物理动作；动作后仍有 `abcdefghijklmno`，正式回执为 `mismatched`。系统虽给出第二次清空 scope，但未确认、未重试，随后取消会话。报告：`poc/output/web/generic_supervised_20260821_163223_c850fdce/report.json`。
- 该失败证明：没有 typed lineage 的独立清空会话只能按横向可见后缀计算退格数，不能把可见后缀当成完整长值连续重试。本批没有为该现场页面或坐标新增补丁。
- 独立恢复 session `539b9fc0b32c42f5b46feffaeeacc9e7` 的系统 `back` 只用于收起键盘，动作 matched；新观察仍显示 15 字符。DeepSeek 随后把“看见 reload 图标”误写成 reload 已完成，session 最终 blocked，未执行第二个动作。报告：`poc/output/web/generic_supervised_20260821_163613_539b9fc0/report.json`。
- 新刷新 session `4286a4ad0d8c4a39983c1dc7b8677bc1` 从键盘已收起状态建立唯一 `local_audited_reload_control_1`，具备 `reload_visual_audit=true`、`independent_geometry_verified=true`，只执行一次 `tap_semantic` 且物理回执 matched。动作后真实画面变为 `browser/web_error_page`，显示网页无法访问且没有长文本输入框；后续 0 动作重规划也 blocked。报告：`poc/output/web/generic_supervised_20260821_164034_4286a4ad/report.json`。
- 最新官方状态：controller/camera online、`busy=false`、0 活动任务/会话；项目 API PID `23912`，卖家 `main.exe` PID `2144` 未变化且未被操作。当前阻塞是验收网页不可访问的运行故障，不是产品风险。
- **严格长文本两段同会话没有开始，更没有 `succeeded`。** 换行、多字段和新的普通发送仍不得开始。恢复条件是验收页重新可访问，并由新观察证明唯一空白长文本输入框；不得从错误页试探点击，也不得把上述 matched 准备动作写成阶段完成。

## 2026-08-21 typed 输入字段身份统一修复

### 根因与通用修复

- 复核现行代码后确认仍有一个重复权威：即使观察已经提供 typed `input_field_id`，控制器动作后仍先按元素临时 ID、文字标签和坐标绑定输入框；只有横向裁剪尾段这一种情况才允许 typed 身份越过布局变化。因此输入框因结果提示、键盘或页面布局正常移动时，同一缺陷仍可能换一种画面复发。
- 修复后，只要动作前存在非空且非 `unknown` 的 typed `input_field_id`，动作后必须唯一找到同 ID 的 `application_text_input`。元素 ID、占位文字、当前值标签和几何不再拥有并行否决权；没有 typed ID 时才沿用旧的元素 ID → 语义标签 → 几何退化顺序。
- 页面身份、App 身份、聚焦状态、键盘布局/输入模式、单行/多行类型、精确目标值仍独立验证。错误字段 ID、重复字段 ID和明确冲突字段标签均拒绝，没有加入 App、页面、文字或坐标特例。
- 横向尾段合同只负责完整值恢复，持续动作 45 秒合同只负责相机归位；它们不再承担字段身份判断。

### 离线验证

- 新正向变化样本：输入框元素 ID、可见 label、坐标和兼容 screen ID 同时变化，但唯一 typed 字段 ID 与精确输入值连续，动作后验证通过。
- 反例：错误 typed 字段 ID、重复 typed 字段 ID、字段标签冲突继续拒绝。
- 最小样本 `4/4`；与后续审计裁剪修复合并后的相关回归为 `577/577`、完整 Python 回归 `1348/1348`，`skipped=54`，`git diff --check` 通过。

### 真机边界与误启动收尾

- 本轮曾误把用户要求的通用修复理解为立即执行两段长文本闭环。恢复 session `4f68370ee30f4786af1f2d19bcdfe196` 只执行一次 audited reload，正式回执 `matched`、终态 `succeeded`，将错误页恢复为空白长文本验收页。
- 长文本 session `d066e2bd13154c34ba971a2cca399df7` 的聚焦点击随后真实执行一次，`physical_actions=1`；动作后四帧显示唯一长文本字段仍为空、已聚焦且键盘完整可见，但专用审计返回 `application_inputs=[]`，正式终态为 `post_action_failure`，没有输入字符。
- 新 session `07f293e3ff2043c4b96512a4aee6859b` 从当前已聚焦空框开始，在 `physical_actions=0` 再次得到同类空审计并失败。连续两次门触发后停止在线采样；上述恢复和聚焦动作都不能替代严格两段同会话验收。

### 聚焦后页面上移时的审计裁剪修复

- 两次失败的最终 crop-local 响应都只看见完整键盘。动作后保存帧清楚显示输入框因键盘出现而上移到旧粗框之上；旧重试裁剪仍按粗框 `top-120`，现场从约 `0.47` 开始，因而完整排除了当前输入框。这是通用观察裁剪缺口，不是 App、固定文字、输入 transport 或字段身份问题。
- 提交 `fe82737` 在第一次审计已经独立证明完整键盘时，用键盘大框的高度和顶边扩大重试裁剪选择；该提示只决定只读 crop，不生成字段、状态、候选或坐标。重试仍须在自己的 crop-local 坐标系独立重建完整输入框与键盘；无效、过小或不可解析的键盘提示继续沿用原 fail-closed 行为。
- 正向现场形状、`notes_field` 与不同键盘几何变化样本、无效键盘大框和非 JSON 反例均已固化。与 `094b4db` 合并后的相关回归 `577/577`、完整 Python `1348/1348`，`skipped=54`，`git diff --check` 通过。
- 最新只读状态为 controller/camera online、`busy=false`、0 活动任务/会话；当前项目 Uvicorn PID `23912` 尚未加载两个新提交，卖家 `main.exe` PID `2144` 未改变且未被操作，`8770` 当前无监听。

## 2026-08-21 长文本正式完成与换行批次状态

### 长文本严格同会话证据

- 正式 session `e2134caef5d7425c8b71a07a5eb5bddc` 从唯一空白长文本框开始；第 1 个输入 fragment 为 `abcdefghijklmnopqrst`，第 2 个为 `uvwxyzabcdefghijk`，两步均为一次物理动作、matched typed receipt 和新 observation/fingerprint。
- 最终逐字值 `abcdefghijklmnopqrstuvwxyzabcdefghijk`、`physical_actions=2`、任务图 completed、`active_subgoal=null`、`status=succeeded`。报告：`poc/output/web/generic_supervised_20260821_175502_e2134cae/report.json`。长文本合同据此完成。

### 换行合同修复与验证

- 第一份换行 session `07dbde6449e74965b68530e2a8d3eabd` 实际仍显示旧长文本页；输入 `first` 后专用事务审计无法建立目标，终态 failed。它只证明验收页面切换错误，不能作为换行失败或成功证据。
- 正确多行页加载后，相同目标连续两次在 start 前生成 `input_text="first line\nsecond line"`、`effect_intents=[]`，却把输入子目标标为 `effect`；均为 0 动作。脱敏 raw 保存于 `poc/output/web/generic_supervised_20260821_182917_c9f254b5/deepseek_initial_task_graph_step_1_deepseek_failure.json`。
- 提交 `b8f6e1f` 在 DeepSeek payload 建图前确定性归一化精确未提交本地输入：必须存在 typed `input_text` 或非空 `input_fields`，子目标 `effect_ids=[]`、未被 EffectIntent 绑定、局部语义为输入/填写/换行，且没有未被否定的发送、提交、保存等正向外部效果。否定短语“不要提交或发送”“未提交或发送”“without submitting”不会再冒充正向效果；“输入并发送”“不提交直接发送”、缺 typed 载荷仍拒绝。
- 现场两子目标 raw、中文/英文草稿、真实换行、多字段变化样本和外部效果反例共定向 `3/3`；DeepSeek 相关 `33/33`；最终完整 Python `1354/1354`，`skipped=54`；`git diff --check` 通过。一次错误地在仓库根目录运行 `discover -s poc` 导致子进程无法 import `web_app`，归类为测试命令 cwd 错误；在 `poc` 目录的有效完整回归全绿。

### 最新真机止损与剩余缺口

- 修复加载后，正式 session `2513efc8e1fb4f1c90c752e12f4941d4` 成功建立；0 动作完成 locate 并生成唯一 `tap_semantic(local_audited_input_1)` 聚焦候选，绑定 observation `obs_6f932d7dc3d343b28aa78160ab300fff`、fingerprint `b9c3e9eb5a16fc5253e8`。
- 聚焦只执行一次。动作后保存画面肉眼可见字段高亮、QWERTY 和真实换行键，但正式采集在限定时间内的参考/候选清晰度为 `15.335/12.174`，相对值 `0.794 < 0.800`，因此 `post_action_failure`、`status=failed`、`physical_actions=1`。该动作不能写为 matched；没有重试。
- 当前唯一缺口仍是一个全新 session 在稳定画面下逐字得到 `first line\nsecond line` 并最终 `succeeded`。多字段与新的普通发送尚未开始，必须等换行成功后依次进行。
- 最新官方状态：controller/camera online、`busy=false`、0 活动任务/会话；项目 Uvicorn PID `22068`；卖家 `main.exe` PID `2144` 未改变且未被操作；临时 `8770/8771` 验收服务已停止。

## 2026-08-21 长文本 direct-Latin 批量 transport 提速

### 已有事实与通用根因

- 用户再次指出长文本闭环耗时过长后，复核正式成功 session `e2134caef5d7425c8b71a07a5eb5bddc`：37 个纯小写字符被拆为 20+17 两个 canonical fragment，每个 fragment 从动作前四帧到动作后四帧约 70 秒，整场约 4 分 24 秒。Qwen 单次决策约数秒，主要耗时不在提示词，而在 fragment 内逐字符向卖家控制端发送机械点击并等待相机归位。
- 历史卖家文字对话框批量 transport 的真实失败样本是 `Agent123 -> agent.com`：它证明大小写、数字和符号模式切换不精确；其中连续小写字母 `agent` 本身逐字正确。旧修复把所有 direct-Latin 一并永久降级成逐键，扩大了失败边界，导致纯小写长文本也承担每字符一条 PC 点击指令和机械队列开销。
- 提交 `009bbe9` 清理旧固定 App 栈时还误删了仍由 `configure_single_click_count()` 调用的 `type_unicode_digit()`，留下真机路径潜在 `NameError`。这是清理回归，与长文本语义合同无关，但会在输入动作进入硬件前阻断现行链路。

### 最小通用修复

- canonical 规划、20 字符分段、typed `input_field_id`、授权 fragment、QWERTY 几何、一次一动作、动作后新四帧和 exact value verifier 均不改变。只把已经同时满足 `direct_latin + lower + 1～20 个 ASCII 字母` 的 fragment 交给卖家控制端现有文字对话框一次排队；Qwen、Policy 和 Controller 没有新增第二套候选或字段身份规则。
- 新 transport 位于通用卖家桥和 `RobotController`，不包含 App 名称、手机页面、用户命令、截图或手机坐标分支。控制端输入按钮是卖家通用工具栏控件，不承担手机目标定位；对话框没有出现、没有关闭或 Esc 中止均立即失败，禁止在结果不明时自动退回逐键重做。
- 控制端对话框关闭只表示接收队列，不表示机械臂完成。现行通用配置沿用既有卖家批量速率 `0.8 秒/字符` 等待有界队列，再由 adapter 取得新四帧和逐字验证，避免过早采到旧画面。大写 direct-Latin、数字、符号和中文拼音继续走当前可见键位/逐键合同，不从失败样本外推。
- 用 `type_unicode_text()` 替代已被误删的单数字 helper，同时服务单击次数输入和批量对话框；新增防恢复测试证明每个字符产生 Unicode down/up、单击次数仍输入 `1`。

### 变化样本、回归与未完成边界

- 正向：20 字符 `abcdefghijklmnopqrst` 只消费一个 `input_verified_text` 物理作用域，并只调用一次 seller batch；短小写 `agent` 同样走 batch。
- 变化样本：大写 `M` 仍使用经过审计的逐键 QWERTY 路径；`Agent`、`agent1`、`agent.com`、中文和 21 字符越界批次均在任何硬件访问前拒绝 batch；无效等待配置也在查找卖家窗口前拒绝。
- 定向新增/变化测试 `9/9`；相关输入、adapter、能力合同、旧固定 App 防恢复和 Web/控制器回归 `197/197`；最终完整 Python 回归 `950/950`，耗时 `34.812s`；`git diff --check` 通过。完整回归仅出现既知测试子进程 `ResourceWarning`，无断言失败。
- 代码提交后只重载项目 Uvicorn `22068 -> 5932`；官方 CLI 重新核对 `service_version=0.2.0`、`openapi_verified=true`、controller/camera online、`busy=false`、0 活动任务/会话，并已公开 `direct_latin_transport=seller_text_dialog_batch`。卖家 `main.exe` 保持 PID `2144` 和原启动时间，未被启动、停止、重启或操作；没有执行真机动作。当前只能声明 **通用提速代码已实现、相关测试通过、完整回归通过、运行服务已加载**；纯小写 batch 的真实耗时和逐字结果仍需验收页就绪后用一个全新长文本 session 验证，不能用历史逐键成功报告冒充新 transport 的真机证据。

## 2026-08-21 换行继续验收与模型合同止损

### 新增通用缺陷与离线修复

- 官方类型化客户端已证明真实 LF 能进入 HTTP 请求；旧会话入口和 DeepSeek planner 随后用 `split()` 折叠内部空白，导致 session `raw_goal` 与 typed `input_text` 退化为空格。两处入口现只裁剪首尾空白，保留内部真实 LF 和 source span。
- Qwen 两份现场输入审计把完整键盘报告为高度 `170/1000`，低于本地最小 `180/1000`，且按键锚点明显压到画面底部。本地几何门保持不变；prompt 现显式要求键盘完整 bounds、最小宽高以及包含所有按键/anchors，错误几何仍失败关闭。
- session `56f103cafd074231baa711d10b00e98e` 的全帧审计后 crop 重试把多行框上沿裁掉，报 `application_inputs[0].bounds` 接触 crop 内部边界，`physical_actions=0`。通用 retry ROI 现按粗字段高度保留 `240～360/1000` 上方上下文，crop 合同要求内部边缘至少 `15/1000` 余量；旧边缘反例仍拒绝。
- DeepSeek 在 session `bdeb930dd5554438883068a2bc5195f3` 中保留了 session 原始 LF，却把 `goal.entities.input_text` 返回成两个字符 `\\n`，长度 23 而非正确 22。新增的本地有界规范化只在 `\\n -> LF` 后能逐字命中未改写原始用户目标时修复；不命中的变化样本保持 planner 原值，不能凭该规则扩大 payload。
- 最终相关回归 `529/529`，完整 Python 回归 `951/951`（`35.518s`）；此前 crop 批次相关 `344/344`，完整回归同样 `951/951`。仅有既知测试子进程 `ResourceWarning`，无断言失败。

### 真机事实、停止条件与顺序门

- session `fe32d7db94bb41ce82b145343df9235b` 与 session `56f103cafd074231baa711d10b00e98e` 均在动作前观察失败，`physical_actions=0`；它们分别暴露原始 LF 折叠/键盘 bounds 和过窄 crop 缺陷，不能记作换行证据。
- 最新 session `bdeb930dd5554438883068a2bc5195f3` 的 `raw_goal` 已保留真实 LF，页面仍是唯一空白“正文”多行框；但两轮 Qwen 审计依次因键盘 bounds 高度不足和 QWERTY anchors 本地布局异常被拒绝，终态 `failed/physical_actions=0`。报告：`poc/output/web/generic_supervised_20260821_205315_bdeb930d/report.json`。
- 该 session 已达到“同类模型合同连续失败两次”的硬停止条件。本轮不再调用 Qwen、不执行任何物理动作，也不以放宽 anchors 几何门换取通过。恢复条件是先有离线可验证的通用观察合同修复或模型输出恢复，再建立全新 session；任何旧 scope 均不得复用。
- 当前换行合同仍未验收；多字段与新的普通发送严格按顺序门尚未开始。长文本历史严格成功不受影响，但不能替代这三项新证据。

## 2026-08-21 Qwen 键盘几何与多行字段合同继续收敛

### 通用修复与离线证据

- 旧本地 OCR 行复核器先调用完整 QWERTY 校验，导致 Qwen 行坐标恰好发生垂直压缩时无法进入纠偏；同时纠偏只在动作适配器阶段发生，输入结构解析器会更早拒绝。现将三帧稳定 OCR 行中心前移到输入审计签发阶段，Qwen 仍须提供完整七点和合法水平结构；缺少两行稳定字母证据、错误水平结构和漂移过大的样本继续拒绝。
- 两次相同 `locate_input/read_only` 0 动作失败证明：当前输入定位节点没有 `active_input_transaction_text` 时，旧 retry ROI 不会使用 compact 阶段唯一输入框粗框。修复后当前子目标自身确为输入目标即可使用该粗框做一次 crop 选择；刷新等非输入当前节点不能借用未来输入 payload 的粗框，crop 仍须独立重建完整字段和键盘。
- session `2a1a2bf08eb04c7cb73fa867cc04a6fd` 的脱敏 Qwen 响应已精确证明唯一“正文”框为 `[130,240,870,590]`、高度 350；旧解析器一律按单行最大高度 180 丢弃。现仅在 typed `active_input_multiline=true` 或正式输入 payload 含 LF 时启用多行高度合同，单行高区域和与键盘重叠区域仍拒绝。
- session `ce45f1882b894a3db8a666f4066d260a` 的第二轮响应已由本地 OCR 修复键盘主体，但未消费的 mode/case/enter/layout 可选控件被 Qwen 写成 1160～1270 坐标。现只撤销“字段形状完整但坐标无效”且当前确定性分段不消费的 mode/case 控件；合法只读控件继续保留，额外动作字段、语义矛盾及当前真正需要的控件继续严格校验。退格、Enter 和 layout 的既有无效候选撤权行为不放宽。
- 新增/变化样本覆盖垂直压缩行、OCR 不完整反例、无效键盘 bounds 的本地恢复、只读定位 crop、非输入节点隔离、typed 多行高字段、单行高区域反例和无效未消费控件。相关回归 `425/425`、完整 Python 回归 `960/960`（`35.704s`），仅有既知测试子进程 `ResourceWarning`，无断言失败；`git diff --check` 通过。

### 真机边界与当前唯一缺口

- `v66` 全新 session `ba9ec9a281274fb68b0e6abf28e66a71` 成功建立唯一空白、聚焦、typed `input_field_1` 多行框；键盘为 direct-Latin lower，本地 OCR anchors 为 `q/p=708`、`a/l=780`、`z/m/backspace=853`。唯一候选为第一确定性分段 `first` 的 `input_verified_text`，controller allowed，证明原键盘/多行字段阻塞已越过。
- 确认一次前，系统按规则采集新四帧并执行独立目标几何审计；Qwen 返回 `whole_control=true` 但 `fully_visible=false`，明确指出输入框顶边被 crop 边界截断。本地以 `geometry match 未证明完整可见的整个控件` 拒绝，authority 已消费，`request_physical_actions=0`、`physical_actions=0`；随后用官方客户端取消会话，设备恢复 0 活动任务/会话。报告：`poc/output/web/generic_supervised_20260821_215149_ba9ec9a2/report.json`。
- 当前唯一缺口是确认前独立输入框几何 crop 对高多行字段缺少上方上下文；下一批必须先离线重放本次四帧并形成通用 crop 修复，再允许一个全新 session。换行仍未验收，多字段和普通发送仍未开始。卖家 `main.exe` PID `2144` 与启动时间未变，未被启动、停止、重启或操作。

## 2026-08-21 输入链单一权威收敛

### 最终决定与通用根因

- 用户第二次明确要求把输入链全部清理并解决耗时。本轮停止继续叠加现场补丁，完整扫描生产代码、测试与当前文档。最终确认同时存在三项冲突：正式事务规划只把连续小写字母合批、空格仍退化为独立键；`text_input_utils.py` 保留一套无人使用的旧分段/重输状态机；Qwen 当前运行时只接受最小 `choice_id`，文件和测试却仍维护一整套无人调用的完整动作 JSON/重试提示词。
- 卖家 `main.exe` 的文字输入对话框是 `Qt6110QWindowIcon` 单 HWND，不存在原生 `IDOK` 子控件。session `626f2ed204b64e5ab65c576f72b555e8` 因旧实现只接受 `IDOK` 而失败；这属于 transport 实现缺口，不是 Qwen、手机键盘或产品风险。
- 最终唯一规划权威为 `verified_text_transaction.py`：一个 direct-Latin 批次只允许 1～20 个小写 ASCII 字母或空格；真实 LF 永远是独立 `press_enter`；大写、数字、标点和中文继续走各自既有可见键位合同。`first line\nsecond line` 因此精确收敛为 `first line`、LF、`second line` 三个动作。

### 物理清理与防恢复

- `text_input_utils.py` 只保留文字规范化和可编辑字符计数；旧 `split_input_segments`、`InputAttemptState`、`InputRecoveryCoordinator`、`MAX_FULL_RETYPES` 及其专属测试已物理删除。
- `qwen_visual_decision.py` 只保留当前运行时实际调用的最小候选选择提示词；旧 `_decision_prompt`、`_decision_retry_prompt` 以及六个只维护旧完整动作输出的提示词测试已物理删除。Qwen只选择 canonical choice，不再拥有第二份文字分段或后置条件。
- `is_direct_latin_batch_segment()` 是 planner、controller、能力验收和卖家 transport 共用的唯一批次判定；防恢复测试扫描上述旧符号不得重新出现。历史空格逐键测试已删除，数字/标点逐键正反样本继续证明非批次路径。
- 卖家原生对话框仍直接点击精确 `IDOK`；Qt/单 HWND 对话框只向该精确 HWND 投递 `WM_KEYDOWN/WM_KEYUP Return`。不调用全局 `press_virtual_key(VK_RETURN)`；投递失败或对话框超时只关闭该精确对话框并停止。

### 同类样本、验证与真机边界

- 正向变化样本覆盖 `agent ready`、`at eight`、`first line\nsecond line` 和 20 字符上限；反例覆盖大写、数字、标点、中文、21 字符、错误 Qt 投递和只有一帧完整 OCR。输入框真实首尾空格/LF 不再被 `.strip()` 破坏，typed 多行字段不会退回旧视觉 lineage 值。
- 全部改动面相关回归 `399/399`；发现并删除一个仍要求空格逐键的旧测试后，观察/事务/Qwen/防恢复相关回归 `264/264`；最终完整 Python 回归 `971/971`，耗时 `35.151s`，`git diff --check` 通过。仅有既知测试子进程 `ResourceWarning`，无断言失败。
- session `978ca475c37747d48a4cd6b361306c51` 的聚焦动作已真实执行一次，但旧相对清晰度比较把键盘弹出后的新画面判为 `0.794 < 0.800`，终态 failed；本轮只对会改变大块画面的聚焦撤销该不可比的相对门，稳定帧和绝对清晰门仍保留。
- session `626f2ed204b64e5ab65c576f72b555e8` 的第一文字批次真实执行一次后因 `IDOK` 假设失败，终态 `failed/physical_actions=1`。Qt 窗口定向确认和空格合批尚无新的真机成功证据；换行、多字段、普通发送仍未验收。下一次只允许在本批本地提交并加载、官方客户端重新核对全部运行状态和新观察后，从全新换行 session 一次一动作继续。
- 提交 `f2d012d` 加载后，session `ada8aac9ebc6473a87d66b1e230ebe73` 已生成正式后置条件为 `first line` 的唯一批次候选；确认前最后一条旧 `[A-Za-z]` 字符检查仍二次否决空格，`physical_actions=0`、authority 已消费。该检查已从 `robot_core.validate_verified_text()` 删除，因为同函数此前已经用唯一 planner 精确重建并验证 `step.segment`；防恢复测试锁定旧错误文本不得返回。相关 `166/166`、完整 Python `972/972` 通过。
- 提交 `a0f4d5f` 加载后，session `f0caeec256084889a22340af7287ff0d` 从唯一空白 `input_field_1` 出发，只执行一次 `first line` 批次。动作后四帧肉眼和页面当前值均显示 `first\n,line\n`，正式 verifier 同样记录该值，故 `mismatched/physical_actions=1/status=blocked`。报告：`poc/output/web/generic_supervised_20260821_234400_f0caeec2/report.json`。
- 失败主类为 **卖家批量 transport 不支持空格串**，不是视觉误读、Qwen、字段身份或换行键。该真机证据推翻本节先前的空格合批可用性假设；不得把 972 个离线测试包装成该 transport 已验收。动作失败后未重试，未执行正式 LF 或 `second line`，设备恢复 online/idle 且无活动会话。

## 2026-08-22 卖家文字对话框永久退役

### 用户最终决定

- 卖家 `main.exe` 的电脑端文字输入对话框永久退出正式产品能力。本节覆盖本台账此前所有把该对话框当作当前 transport、修复对象或候选优化的口径；旧记录只用于解释历史失败。
- 后续任何普通文字、长文本、空格、换行、中文、数字或符号输入均不得调用该对话框，也不得通过配置、环境变量、备用入口、失败回退或兼容层恢复。

### 物理删除与防恢复

- `robot_core.py` 删除批量 transport 方法、能力声明、等待配置与分流；`robot_gui_poc.py` 删除 owned-window 查找、对话框填字、Qt/IDOK 确认和关闭逻辑；`controller_config.json` 删除专属配置。
- `verified_text_transaction.py` 仍是唯一 planner：连续小写词使用真实手机可见键位序列，空格和真实 LF 分别生成独立可见键动作。变化样本 `first line\nsecond line` 确定性生成 7 步。
- 删除 8 个只证明旧对话框行为的测试，新增 1 个生产文件物理缺失防恢复测试；静态扫描中退役符号只存在于该测试黑名单。

### 验证与真机边界

- 相关回归 `569/569`；完整 Python 回归 `965/965`（`35.284s`）；最后删除两个无引用的对话框专属常量后，退役/硬件适配定向回归 `89/89` 再次通过。JSON 解析、Python 编译与静态扫描通过。完整回归仅出现既知测试子进程 `ResourceWarning`，无断言失败，提示的 PID 随后自然退出。
- 本批没有重载服务、调用模型或执行真机动作，也没有启动、停止、重启或操作卖家 `main.exe`。因此只能声明对话框 transport 已内部永久退役，不能声明换行、多字段或普通发送真机完成。
- 当前唯一能力缺口仍是按顺序取得换行、多字段、普通发送的全新同会话 `succeeded` 证据。后续提速只能在真实手机可见键位和逐动作新观察合同内设计，不能恢复电脑端文字窗口。

## 2026-08-22 QWERTY 外框重复门删除批次

- 当前合同缺口仍只有换行同会话 `succeeded`。session `757fdc5a32eb4bacb015f749897cfc8c` 的唯一 audited reload 物理动作 matched，动作后页面已恢复为空白多行框；该 session 的后续任务图终态 blocked，不计为换行成功。
- 全新换行 session `76818b986b4f493c9ca2f3fd4e40cbeb` 只执行一次聚焦动作并 matched。随后两次独立观察都得到 direct-Latin QWERTY 的稳定七点 `q/p=708`、`a/l=780`、`z/m/backspace=852`，但模型均给出 `keyboard.bounds=null`；旧解析器因此在第一段 `first` 确认前以“可见键盘必须提供有效 bounds”拒绝，`physical_actions` 保持 1，没有输入字符。达到同类合同两次停止门后已取消会话。
- 根因分类为通用实现缺陷：本地三帧 OCR 七点已经是键盘行几何的独立权威，整体模型外框仍拥有重复否决权。修复只在七点完整、水平结构合法且跨帧稳定时由键距/行距重建键盘执行包络；无本地七点、非 QWERTY 和结构错误继续拒绝。
- Enter、退格、字面键、模式/布局切换键仍须各自的标签、bounds、置信度、完整可见性与动作语义。新增正常位置、整体上移变化样本、换行键正例和换行键自身不可见反例；没有 App、页面、截图或固定坐标分支。
- 最小正反 `5/5`、相关回归 `424/424`、完整 Python 回归 `967/967`（`35.818s`），`git diff --check` 与 Python 编译通过。完整回归只有既知测试子进程 `ResourceWarning`，无断言失败。
- 本批提交后已启动项目 Uvicorn PID `13200`；官方客户端确认 `service_version=0.2.0`、`openapi_verified=true`、controller/camera online、`busy=false`、0 活动任务/会话。没有重新执行真机动作，也没有向卖家 `main.exe` 发送任何操作。因此只能声明 **通用修复已实现、相关测试通过、完整回归通过、运行服务已加载**；换行、多字段、普通发送均仍未完成真机验收。

## 2026-08-22 第一行 matched 后任务图未推进修复

- 修复加载前的全新 session `d5c0c38e5e2145c29d1b2f73df710978` 首次确认前输入审计临时未重建唯一字段，`physical_actions=0`；唯一一次允许的新观察恢复空白、聚焦、direct-Latin QWERTY 和 `input_field_1`。
- 第二次确认只执行一个 `input_verified_text`，新画面严格显示 `value="first"`，回执 `matched`、`physical_actions=1`。同一新观察也独立公开右下角 `↵` 为 `input_exact_enter_key`，`key_action=newline`、后置值 `first\n`。
- 旧编排仍把该 matched 动作视为整段 `first\nsecond` 的内部微步，任务图继续停在已完成的 `input_first_line`；同时 typed 动作语义只识别“回车/Enter”，没有把“换行键”编译为 `press_enter`，因此 canonical 只剩无关模式切换候选，Qwen最终 blocked。会话随后取消，未点击换行、未输入 `second`。
- 通用修复新增“换行键/new line key”到正式 `press_enter` 等价语义；当下一确定性片段是 LF，且任务图存在唯一直接 `press_enter` 后继时，当前 matched 输入片段退出微步并走一次性 receipt/replan。单一长文本子目标和无匹配后继变化样本继续保持原微步行为。
- 最小 `4/4`、相关 `182/182`、完整 Python `969/969`（`35.552s`）通过；仅有既知测试子进程 `ResourceWarning`，无断言失败。当前只声明 **通用修复已实现、相关测试通过、完整回归通过**，修复后的同会话换行证据仍待取得。

### 重置页连续两次 0 动作合同失败

- 修复 `3f49364` 加载后，两个相同重置请求分别留下 `generic_supervised_20260822_011600_462d2894` 与 `generic_supervised_20260822_011638_2fee3d97`。两次均为 `physical_actions=0`，DeepSeek 把唯一刷新子目标写成 `status=pending` 却同时指定相同 `active_subgoal_id`，并错误绑定无 payload 的 `data_mutation clear_input` effect，导致“可推进任务图必须且只能有一个活动子目标”。
- 本地已有“当前页面 reload 是 navigation”权威，但旧实现只处理 `effect_intents=[]`，且把“不要发送或提交”中的否定词误当正向外部效果。本轮扩展同一通用归一化：唯一当前页刷新绑定的无 payload `data_mutation` 被物理撤权，否定约束不阻断归一化；刷新后真实提交等正向 effect 反例继续拒绝。
- 定向现场/变化样本 `1/1`、相关 `212/212`、完整 Python `969/969`（`35.807s`）通过。尚未加载或重试真机，手机仍保留上一成功动作产生的 `first`。

## 2026-08-22 Qwen 键盘纵向坐标漂移的本地几何权威修复

### 新失败与唯一根因

- reload 准备动作已恢复空白多行页。严格换行 session `cd6f1173140b430f832710db114662c4` 只执行一次聚焦；保存画面证明输入框已聚焦、完整 QWERTY 可见。两轮动作后输入审计分别返回键盘 `bounds=[50,870,950,1000]` 和 `bounds=[60,1160,940,1900]`，均以“可见键盘必须提供有效 bounds”失败。终态 `failed/physical_actions=1`，没有输入 `first`、换行或 `second`。
- 同三帧 Windows OCR 实际稳定读到顶行 `q/w/e/r`、底行 `c/v/b/n/m`。旧 `stable_qwerty_ocr_anchors()` 先检查模型纵坐标必须在 `0..1000`，导致第二类响应在 OCR 前返回 `None`；第一类响应则因模型压缩行与真实行距离略超旧门而丢弃本地证据。分类为 **通用本地观察实现缺陷**，不是机械不可达、网络、字段身份、风险边界或某张截图问题。

### 通用修复

- Qwen 继续识别键盘布局、输入模式和 Enter 当前语义，不再提供执行纵坐标权威。三帧本地 OCR 同时拟合顶/底字母行的水平键距与垂直中心，至少两帧一致后本地重建七点；错误字母排列、行距或跨帧漂移继续拒绝。
- 本地七点成为 QWERTY 整体执行包络的唯一几何来源。Enter 仅在 Qwen 逐字报告 `newline` 且标签为通用 Enter/Return/回车/换行符号、水平位置与本地 backspace 行一致时，按本地行距重建；Send/开始等反例不生成 `press_enter`。
- 当模型 X 使用 `0..1000`、Y 使用更高 portrait grid 时，不接受 `1920/2000` 等猜测。只有模型三行与本地三行能拟合出低残差单一仿射变换时，才把该变换原子应用于整份审计；任一结构越界则不规范化。
- 同一 fingerprint 的粗 scene 只有在唯一完整 goal input、typed `field_id` 不冲突且可见标签逐字一致时，才为专用审计提供本地字段粗框；重复、缺失或冲突字段不重绑。该粗框不能直接成为点击点，动作前独立 geometry audit、Controller 和 receipt 门保持不变。

### 样本与验证

- 现场两份 raw 均离线恢复为同一 `input_field_1`，anchors 为 `q/p=708`、`a/l=780`、`z/m/backspace=852`；当前值为空时生成第一段文字动作，模拟当前值为 `first` 时生成唯一 Enter，后置值逐字为 `first\n`。
- 变化正例覆盖整体键盘上移；反例覆盖错误水平字母排列、重复同名字段、Send/开始键、缺少本地稳定行与非 QWERTY。没有 App、页面、截图或固定坐标分支。
- 最小正反/变化 `5/5`；observer/planner 与 canonical/Qwen/orchestrator/lineage 相关回归 `537/537`；完整 Python 回归 `996/996`，耗时 `36.153s`；`git diff --check` 与 Python 编译通过。完整回归仅出现既知测试子进程 `ResourceWarning`，PID 随后自然退出。
- 当前状态只能声明 **通用修复已实现、历史失败重放通过、相关测试通过、完整回归通过**。尚未加载项目 API、未执行新的真机动作；换行合同仍未验收，多字段与普通发送仍不得开始。

## 2026-08-22 执行确认层 typed 输入框续接批次

### 真机失败与分类

- session `47cf47c3cd134e498b1ce3e49e260582` 的 `first` 与可见 `↵` 两个真实动作均 matched；同一 `input_field_1` 的值依次为 `first`、`first\n`，observation/fingerprint 分别更新为 `obs_e63d4ec10d37497baf9f0d29bd8d1cf5 / 29897dd2cb1aeb9aae07` 与 `obs_2c4ba09aa1d4496e84f953c7de459f96 / 574e8c2cfaf7229f8d98`。
- 第三步 `second` 没有执行。确认前四帧仍显示同一聚焦多行框和 `first\n`，但执行确认重绑以 `application_text_input` 0 个匹配拒绝，`request_physical_actions=0`、总动作数仍为 2、终态 `needs_reobservation`。报告：`poc/output/web/generic_supervised_20260822_102932_47cf47c3/report.json`。
- 主类为 **通用执行确认身份缺陷**：占位消失后，确认层未使用已验证 typed 字段身份和授权 payload 前缀；不是机械不可达、键盘坐标、网络或风险边界。

### 通用修复与验证

- `input_verified_text` 仅在 formal candidate、稳定同帧、唯一聚焦输入、非空 typed `input_field_id`、当前值和本步后置值均为授权全文精确前缀时恢复同一字段描述；随后仍须 fresh crop geometry audit 才能执行。错误授权前缀、重复或冲突输入字段继续 0 动作拒绝。
- 正向覆盖占位完全消失；变化样本覆盖 fresh scene 仍有唯一输入但暂缺 typed ID 且几何漂移；负样本覆盖错误授权前缀和重复字段。集成样本证明只追加 `second`，不会重输 `first\nsecond`。
- 最小 `2/2`、相关 `367/367`、最终完整 Python `998/998`（`36.211s`）通过；`git diff --check` 通过，测试 `ResourceWarning` PID `12856` 随后退出。当前仍未加载、未取得全新 session 成功证据；换行验收未完成，多字段和普通发送不得开始。

### 加载后运行阻塞

- 提交 `7a61c46` 加载后，独立刷新 session `6df6d89b225747b4a1872fe157dfde4a` 的唯一 reload 动作 matched，`physical_actions=1`，新观察 `obs_5ad5aaeb934645ebbe356567a8e014f9 / 261b77568d184b53178c` 证明页面恢复为唯一空白输入框。动作后 DeepSeek 重规划返回第一次 `HTTP 402 Insufficient Balance`，因此 session blocked；刷新动作事实仍成立。
- 全新严格 session `e424c727b3fa4f728d0ffdbc0d024662` 从空白 `input_field_1` 建立。第一次聚焦确认在物理动作前因本地帧差 `6.60 > 6.00` 停止，`physical_actions=0`。随后唯一一次官方 reobserve 得到 `obs_d7e5e4e8477044e8a472bbc6d019097b / c0f7389d6d4384ce13cf`，画面仍为空白唯一输入框；重规划再次返回相同 402，终态 blocked/0 actions。报告：`poc/output/web/generic_supervised_20260822_105148_e424c727/report.json`。
- 这是外部 DeepSeek 账户余额运行故障，不是代码回归、字段身份、键盘几何或机械不可达。已达到相同外部模型故障两次停止门，未继续采样或动作。当前设备 online/idle、0 活动会话，卖家 PID 2144 未变；余额恢复后必须另建全新 session，从聚焦开始按一次一动作取得完整证据。

## 2026-08-22 动作后 typed 字段身份权威批次

### 新真机事实与根因

- 全新严格 session `6e8883c3b6474c34a530462e155d6e24` 从唯一空白 `input_field_1` 开始。聚焦、输入 `first`、点击右下角 `↵` 三个真实动作依次 matched；字段值依次为 `""`、`"first"`、`"first\n"`。第一次 Enter 确认在 0 动作处因 geometry 模型没有逐字复用 label+role 停止，官方一次新观察后同一 Enter 成功；没有自动重试已执行动作。
- 第四个真实动作追加 `second` 后，动作后 observation `obs_6f70403062e24aa990ff13064cd65828 / a69e17af7e7e98a55cd6` 已逐字证明同一 `input_field_1` 的值为 `first\nsecond`，但正式回执为 `mismatched`、session 终态 blocked。报告：`poc/output/web/generic_supervised_20260822_164614_6e8883c3/report.json`。
- 唯一不一致是模型描述的 `screen_id` 从动作前“通用动作具机验收页”改写为动作后“通用动作真机验收页”；前后 `foreground_app_id=unknown`、typed 字段 ID、多行属性、聚焦状态、QWERTY/direct-Latin 和相机方向均一致，后置文字也逐字正确。旧 Controller 在计算 typed 字段身份前先让自由文本页面名否决，因此属于 **通用身份权威顺序缺陷**，不是输入、机械坐标、网络、App 切换或风险边界问题。

### 通用修复与验证

- 动作后校验现先重建非空 typed `input_field_id` 权威；只有同为 `application_text_input`、同多行属性、无冲突字段标签、完整可见时才成立。模型页面描述不兼容时，该权威可维持同一输入框身份；随后仍必须通过唯一字段绑定、授权后置值逐字一致、聚焦、键盘布局/模式和相机方向检查。
- 没有增加 App、页面、错别字、截图或坐标特例。变化样本覆盖任意页面措辞漂移；不同 typed 字段 ID、两个不同真实包名和键盘输入模式变化继续拒绝。
- 最小现场正反/变化 `4/4`、输入控制器/canonical/Qwen/lineage/observer/orchestrator 相关回归 `560/560`、完整 Python 回归 `999/999`（`36.944s`）通过；`git diff --check` 与 Python 编译通过。完整回归仅出现既知测试子进程 `ResourceWarning`，PID `18968` 随后自然退出。
- 当前只能声明 **真实手机内容已逐字形成、通用修复已实现、相关测试通过、完整回归通过**。由于该 session 正式终态仍是 blocked，换行阶段尚未验收；加载后必须先把当前正确草稿独立恢复为空白，再用一个全新 session 取得 `first → ↵ → second` 全部 matched 且最终 `succeeded/active_subgoal=null` 的证据。多字段和普通发送仍不得开始。

## 2026-08-22 exact tap 唯一本地目标直推批次

- 提交 `4f180cb` 加载后，恢复 session `ed86b30deb3845d6aa22a0c672457eda` 只执行一次系统 `back`，正式回执 `matched/succeeded`，新观察 `obs_cf4232892ef646b9b6e4615120088b6c / 5bce19ababdff03bec48` 证明软键盘已收起且当前验收结果页保留。
- 随后两个全新刷新 session `6764154de82746cd902c3353ad919d01`、`8a17997adfdc441c8941aacfcc867da1` 均在 0 动作处 blocked。两份独立观察都唯一公开 `local_audited_reload_control_1`，具备 `meaning=reload`、`goal_relevant=true`、`reload_visual_audit=true`、`independent_geometry_verified=true`；Qwen 却两次选择了别的 tap choice，最后由“必须绑定当前画面唯一语义目标”门拒绝。达到同类合同两次停止门后没有继续真机采样。
- 根因是结构化 `exact_tap_semantic` 的本地直推只按 action 种类过滤；页面同时存在验收按钮等普通 tap choice 时，它未进一步消费已经唯一成立的 `target_local_candidate`，错误退回随机模型选择。修复只对显式 exact tap 生效：存在唯一本地语义目标时，唯一 canonical choice 还必须逐字复用其 `element_id`；目录缺失该目标继续返回 `None`，普通非 exact 视觉任务仍由 Qwen 选择。
- 最小正反/变化 `3/3`、Qwen/canonical/observer/controller/orchestrator/input 相关回归 `562/562`、完整 Python 回归 `1001/1001`（`36.666s`）通过；`git diff --check` 与 Python 编译通过。完整回归仅有既知测试子进程 `ResourceWarning`，PID `2820` 随后自然退出。
- 当前仅能声明该通用合同修复已实现并通过离线验证，尚未加载或重新点击刷新。加载后只允许建立一个新的 exact reload session；若其唯一动作 matched 并恢复空白页，才可另建全新严格换行 session。

### 加载后真机结果与停止门

- 提交 `c1d9d35` 加载后，exact reload session `2d3ef3e5cf73459182582821edba2f8e` 已确定性直推并逐字绑定 `local_audited_reload_control_1`，唯一真实动作 matched；动作后 observation `obs_2e023da879fc48f7bc1093ff7bea8664 / 09427382e59858269fa0` 证明正文恢复占位符。该独立 session 后续因 DeepSeek 重规划活动子目标数量错误 blocked，不能记为 session 成功，但刷新物理事实成立。
- 第一次严格换行 session `522ab89f80664d4283b4ff7067a3325b` 从空白页开始，聚焦与输入 `first` 两个真实动作均 matched。Enter 在 0 新动作处因 `geometry match 没有逐字复用 label+role` 进入 needs_reobservation；唯一一次官方 `next` 又因“专用输入结构审计没有建立当前输入事务的唯一本地目标”终止。正式终态 failed/physical_actions=2，手机保留 `first`。
- 独立 `back` session `e31bb5da321740a9b5846ca722c498b1` matched/succeeded 收起键盘，随后 reload session `14e0177fb3274f6897aeeb50cc6f845e` matched/succeeded 恢复空白页。第二次严格 session `102c8260eea343da9199d1e141a42c11` 再次从空白页完成聚焦和 `first` 两个 matched 动作，但 Enter 确认前再次得到完全相同的 geometry label+role 合同失败，0 新动作；会话随后由官方客户端取消并释放 authority，手机仍为 `first`。
- 两个全新严格 session 在相同阶段连续出现同一 geometry 模型合同失败，已达到停止门。当前 controller/camera online、busy=false、0 活动任务/会话，卖家 `main.exe` PID 2144 与启动时间未变。不得继续在线采样、`next` 或重建 session；下一批只能离线重审 geometry 输出接口合同及两份 `confirmation_failure_step_3.json`，不能加 App、页面、截图、键盘坐标或验收页特例。
- 换行正式验收仍未完成；多字段与普通发送未开始。提交 `4f180cb` 和 `c1d9d35` 的内部修复、相关回归与完整回归结论不变，不能用它们替代新 session 的 `succeeded` 证据。
