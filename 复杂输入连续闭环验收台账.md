# 复杂输入连续闭环验收台账

更新时间：2026-08-20（Asia/Shanghai）

## 1. 本轮必须证明的能力

在一个全新的 Universal Agent session 中，从真实已聚焦输入框的精确值
`longinputvalidation2026:12` 出发，连续完成两个不同可见字符 `3`、`4`，最终精确值为
`longinputvalidation2026:1234`，并由同一 session 以 `succeeded`、`active_subgoal=null`
结束。

本轮只验证多动作输入值连续性，不混入换行、多字段、输入法切换或发送。

## 2. 必须出现的证据

- start 的新鲜四帧观察逐字证明初值为 `longinputvalidation2026:12`、输入框已聚焦、键盘模式明确；
- 第一步唯一候选为可见字符 `3`，动作后精确值为 `longinputvalidation2026:123`；
- 第二步在新 observation/fingerprint 和新一次性 scope 下唯一候选为可见字符 `4`，动作后精确值为 `longinputvalidation2026:1234`；同一 typed input 子目标的已验证内部微步允许保持原高层 revision，达到 canonical 全文后必须由 DeepSeek 将 revision 精确推进 `+1` 并完成任务；
- 两步各自 `physical_actions=1`、`action_outcome=matched`，各有动作前后四帧和独立 typed receipt；
- 总物理动作恰好为 2，没有发送、提交、第三动作或自动重试；
- 最终同一 session 为 `succeeded`、任务图 completed、active subgoal 为空。

## 3. 已有证据

- session `cd2fe3e9ad3049a892ecc0f9c4ce17d9` 已证明 canonical action v1 能从
  `longinputvalidation2026:1` 单步输入 `2` 并以 `succeeded` 结束；
- 当前项目服务已加载唯一动作协议，网关与 OpenAPI 校验正常，controller/camera online、busy=false，
  没有活动会话；
- 卖家 `main.exe` PID 2904 未变化。

## 4. 唯一缺口与根因状态

唯一缺口：尚无当前代码下“同一 session 连续两个字符、两次动作后均精确续接并最终 succeeded”的
真机证据。当前没有已证实的代码缺陷，不修改生产代码。

## 5. 通用性与停止条件

- 使用两个不同的真实可见数字键，证明按新观察与精确前缀继续，而不是复用上一动作或固定同一键；
- 不增加微信、页面、文本、步骤或坐标分支；
- 任一步若出现模型合同错误、目标歧义、超时、设备状态变化、动作不匹配或精确值不符，立即停止；
- 写请求不自动重试；确认调用使用网关默认 180 秒，不再人为缩短；
- 失败后只保存证据并分类根因，不清空、不补输、不发送。

## 6. 首次 0 动作失败与根因证据

- 全新 session `90d59bbc9e784d5fb32e60a329f93925` 的新鲜四帧观察逐字证明当前值为
  `longinputvalidation2026:12`、输入框已聚焦、键盘为 `numeric/direct_latin`，并建立唯一可见字符键
  `3`，其后置值为 `longinputvalidation2026:123`；
- canonical 目录同时错误公开了当前 numeric 键盘上不可执行的 `input_verified_text` 候选；Qwen 选择
  该正式候选后，控制器才以“精确文字输入要求当前画面确认 QWERTY 键盘”拒绝；
- session 为 `blocked/physical_actions=0`、无 confirmation scope，手机未变化。证据目录为
  `poc/output/web/generic_supervised_20260820_234258_90d59bbc/`；
- 根因分类为代码缺陷：唯一 canonical 动作目录只按“input 已聚焦”生成批量输入 affordance，没有把
  typed 输入事务的下一分段类型、键盘布局、输入模式、大小写和 preedit 状态纳入候选生成；执行层仍在
  目录选择后承担第二次语义前置否决。

## 7. 同类样本与通用修复

同类样本集合：

- 本次 numeric 键盘、下一字符为 `3`：应只公开本地审计的 literal-key tap，不得公开批量输入；
- 上一次成功的 numeric 键盘、下一字符为 `2`：Qwen恰好选对 literal-key，但旧目录同样可能公开无效批量输入；
- 变化正例：QWERTY/direct-latin 且下一段为英文字母时，应继续公开 `input_verified_text`；
- 变化反例：输入已达目标、当前值不是目标前缀、模式/大小写不匹配或存在 IME preedit 时，不公开批量输入。

通用修复：canonical 目录调用同一 typed 输入事务规划器，只在下一分段是可批量输入的英文/拼音，且当前
QWERTY、输入模式、大小写和 preedit 事实全部匹配时生成 `input_verified_text`；下一分段是数字、空格或
符号时只保留经本地输入审计签发的 literal-key 候选。目录只读取 active input field 的 payload，不从
其他 pending 字段借用正文。控制器继续独立复核物理执行条件，但不得再成为正常候选的首个语义否决者。

回滚方式：本批只修改 canonical 目录及其正反测试，可整体回退，不改变设备、标定、观察或执行 transport。

## 8. 本批验证清单

- 精确重放本次 numeric/`3` 和历史 numeric/`2` 形状，目录中无 `input_verified_text`；
- Qwen choices 只含可见 literal-key，不再能选到无效批量输入；
- QWERTY/direct-latin 英文续写正例仍有唯一批量输入候选；
- 已完成、错误前缀、非 QWERTY、模式/大小写不匹配及 preedit 反例均无批量输入候选；
- canonical、Qwen、编排器相关回归后只运行一次完整 Python 回归和 `git diff --check`；
- 离线全绿、本地提交并只重载 Uvicorn 后，重新核对设备，再用全新 session 做一次最终两字符验收。

## 9. 最终离线与真机结果

- 通用修复提交为 `1e6bb6b`。canonical/Qwen/编排/Web 整链 `429/429`，Python 完整回归
  `1303/1303`，生产文件 `py_compile` 和 `git diff --check` 通过；
- 只重载项目 Uvicorn，父进程 PID 28108、实际监听 PID 14820；controller/camera online、busy=false，
  卖家 `main.exe` 保持 PID 2904 和原启动时间；
- 全新 session `d1b687525f6c4e559bb261dea8957bd9` 首轮新鲜观察为
  `value="longinputvalidation2026:12"`、numeric/direct-latin，目录只向 Qwen公开唯一可执行字符键 `3`；
- 动作 1 只点击一次 `3`，receipt `receipt_11c4ef3a8d8543b19c65c917dbb889d9`，前后 observation
  `obs_2d70b22cb7c44567bd88b72e0db7d65d → obs_4cce32947ba842f98d1197aebd7fa945`，fingerprint
  `5a743df2ccf09061397c → f29a13b898c59d5e83e6`，`matched`，值精确变为
  `longinputvalidation2026:123`；
- 同一 typed input 子目标在新画面上本地推进微步，未伪造高层 revision；新 scope 只公开唯一字符键 `4`。
  动作 2 只点击一次 `4`，receipt `receipt_5e7e0471910c4289918c0968eddea1be`，前后 observation
  `obs_4cce32947ba842f98d1197aebd7fa945 → obs_3478e08837ae45d790df6a4e64635fab`，fingerprint
  `f29a13b898c59d5e83e6 → 61712d8f085c3f07c104`，`matched`，值精确变为
  `longinputvalidation2026:1234`；
- 达到 canonical 全文后 DeepSeek revision `1→2`，transition disposition=`task_completed`。最终同一
  session `succeeded/physical_actions=2/active_subgoal=null`，没有发送、提交、第三动作或自动重试；
- 正式报告为
  `poc/output/web/generic_supervised_20260820_235434_d1b68752/report.json`，同目录含两步动作前后各四帧、
  两份 verification、两份 post-action transition 和最终 revision 2 task graph；
- 最终设备 controller/camera online、busy=false、0 活动会话，卖家 `main.exe` 未被操作。

结论：本轮“同一新会话连续两个不同字符、精确输入值连续性、最终 succeeded”真机闭环完成。该结论不
扩大为换行、多字段或发送验收；下一轮应单独冻结并完成下一个最小合同。
