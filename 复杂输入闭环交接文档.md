# 复杂输入闭环交接文档

更新时间：2026-08-21（Asia/Shanghai）

## 1. 本轮目标与真实状态

当前主线是把以下能力全部完成正式真机闭环：

1. 超过单段上限的长文本连续输入；
2. 换行；
3. 多字段输入；
4. 新的普通发送任务在同一新会话最终 `succeeded`。

阶段 1、2、3 的既有权威验收结论不变；本文件只交接上述复杂输入批次。代码、离线测试和真机结果必须分别汇报。

## 2. 已完成并提交的当前基线

当前分支：`codex/integrate-phase2-stage3`。

最近提交：

- `f096ca3 fix: bind exact input targets to canonical actions`
- `64dc14f fix: isolate keyboard geometry to input audit`
- `33a5cd0 fix: refine empty input audits from compact hints`
- `60edfd4 docs: record post-input camera recovery gap`
- `2ee8b74 fix: wait for camera return after continuous actions`

`2ee8b74` 已实现用户选择的方案 1：普通动作后稳定等待仍为 10 秒；输入、清空、拖动和长按等持续动作最多等待 45 秒，并要求连续四帧稳定、输入连续性清晰度以及可比较时回到动作前手机取景身份。满足条件立即结束等待，绝不重复物理动作。

离线验证结果：相关回归 `121/121`，完整 Python 回归 `1329/1329`。这些结果只证明该提交的代码与测试，不代表当前复杂输入批次已完成真机验收。

## 3. 当前真机证据

### 第一段输入

会话 `25ea287294234b6998ab0c79b91861cc` 只执行了一次 `input_verified_text`，真实输入了第一段：

`abcdefghijklmnopqrst`

旧的 10 秒动作后采集预算内相机未归位，因此该会话终态为 `failed/physical_actions=1`。后续独立 0 动作会话 `307724c2500e41a7a29a4b131a71af00` 从真实画面逐字证明输入框当前值确实为上述 20 字符。该只读证据不能改写原失败会话，也不能替代同一新会话完整输入 37 字符并最终 `succeeded` 的严格验收。

### 占位文字消失后的错误阻断

全新会话 `6f30239f00cf48009dc90992c57a1f7c` 在 0 物理动作阶段正确观察到：

- 唯一可信输入框 `local_audited_input_1`；
- `meaning=application_text_input`；
- `input_field_id=input_field_1`；
- 当前值和元素 label 均为 `abcdefghijklmnopqrst`；
- 已聚焦、QWERTY、`direct_latin`、lower；
- 下一确定性分段应为 `uvwxyzabcdefghijk`。

但会话被 Qwen 前置逐字门错误阻断为“当前可信候选中不存在逐字一致文字：长文本”。根因是输入后占位文字“长文本”自然消失，而旧逐字候选匹配只认当前可见 label，没有使用同一 typed 输入字段 ID 和正确前缀维持目标身份。该会话为 `blocked/physical_actions=0`，不得复用。

报告目录：

`poc/output/web/generic_supervised_20260821_142828_6f30239f/`

## 4. 本批通用修复与离线验证

接手时工作区已有两个未提交代码草案：`poc/qwen_visual_decision.py` 与 `poc/test_qwen_visual_decision.py`。本批完整保留并审查了它们，没有 reset、checkout 或覆盖；本文件与 `项目交接文档.md` 的交接修改也一并保留。

`_active_input_transaction_exact_candidate_ids()` 现只有在以下事实同时成立时，才允许在占位文字消失后继续把当前输入框绑定为原逐字目标：

1. 当前正式 `TaskSemanticIR` 只有一个属于活动子目标的 input field；
2. typed field label 为空或逐字等于任务要求标签；
3. 当前元素是唯一可信 `application_text_input`；
4. 当前元素的 `input_field_id` 逐字等于 typed field ID；
5. `goal_relevant=true`、`fully_visible=true`；
6. 当前非空值是授权完整 payload 的精确前缀；
7. 元素可见 label 逐字等于当前值。

空输入框继续使用原始可见占位标签匹配；判定只看同一 typed `field_id`，不会因为另一个字段已有非空值而误接管当前空字段。错误字段 ID、错误前缀、错误字段标签和其他元素仍阻断。没有加入 App 名称、固定命令、固定步骤或坐标特例。

验证已覆盖：

- 现场生产形状 `input_field_id=input_field_1`、当前值 `abcdefghijklmnopqrst`、授权 payload `abcdefghijklmnopqrstuvwxyzabcdefghijk`；
- 错误字段 ID 与错误前缀反例；
- 变化样本 `field_label=备注`、`input_field_id=notes_field`、不同 payload `releasecandidatecontinuation`；
- 当前 typed 字段为空、另一字段非空时，仍由当前字段可见占位标签建立唯一匹配。

最小测试 `2/2`、Qwen/canonical/typed input/observer/orchestrator 相关回归 `546/546`、完整 Python 回归 `1331/1331` 全部通过，`git diff --check` 通过。代码尚未加载到运行服务，也没有新增真机证据；只能声明通用修复与完整离线回归完成。

## 5. 当前实时运行状态

2026-08-21 最新只读检查：

- 官方类型化 CLI `agent_api_cli.py status` 可访问项目 API；
- `busy=false`；
- `active_tasks=[]`；
- `active_sessions=[]`；
- `controller_online=false`；
- `camera_online=false`；
- 卖家 `main.exe` PID `2144`，本轮没有启动、停止、重启或操作它。

因此当前不能确认手机仍处于较早画面，也不能执行任何物理动作。设备/摄像头离线属于运行故障，不是产品风险或协议边界问题。

## 6. 新对话唯一推进顺序

1. 完整读取 `项目最终目标.md`、`项目交接文档.md`、本文件和 `复杂输入完整批次验收台账.md`。
2. 保留当前两个未提交修改，不得 reset、checkout 或覆盖。
3. 本批已完成草案审查、现场 `input_field_1` 复现和不同标签/字段/payload 变化样本。
4. 本批已完成相关回归、一次完整 Python 回归和 `git diff --check`。
5. 本批完成验收台账更新并创建本地提交，不推送 GitHub。
6. 提交后只读核对项目服务、卖家 PID 和官方 CLI 状态。若 controller/camera 仍离线，保存精简证据并停止真机步骤；不得启动、停止或重启 `main.exe`。
7. 设备恢复后，只重载项目 Uvicorn 以加载新提交，再用官方类型化 CLI 重新核对 OpenAPI、设备、busy、活动会话、新 observation/fingerprint。
8. 创建全新会话，不复用 `6f30239f...`。先用 0 动作证明当前字段、精确前缀和下一分段，只在唯一动作确认为 `input_verified_text` 且分段为 `uvwxyzabcdefghijk` 时执行一次物理动作；动作后最多自适应等待 45 秒并取得四帧新证据。
9. 上述跨会话续写只用于安全恢复和验证连续性修复。严格长文本验收仍需恢复空白验收页，在一个全新会话内连续完成两个输入动作、最终逐字值为 `abcdefghijklmnopqrstuvwxyzabcdefghijk`、终态 `succeeded`、`active_subgoal=null`。
10. 长文本严格闭环完成后，才依次验收换行、多字段和新的普通发送同会话成功证据；不要扩大重构。

## 7. 停止条件

- 任一物理动作失败：立即停止该动作链，不自动重试。
- controller/camera 离线、新观察不可用、目标不唯一或机械不可达：记录具体运行故障，0 动作停止。
- 同类模型合同错误连续两次：停止在线采样，返回本地合同修复。
- 任何新发现必须先归类为通用根因；不得针对当前 App、截图、文字或坐标增加特例。
- 登录/身份认证和付款/资金交易才要求确认；普通输入、发送、提交等自动执行。
