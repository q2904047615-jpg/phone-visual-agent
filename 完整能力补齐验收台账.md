# 完整能力补齐验收台账

更新时间：2026-08-19（Asia/Shanghai）

## 当前批次：旧边界正式退役

### 本轮必须证明的能力

正式 Universal Agent 运行链只能由 `TaskSemanticIR + EffectIntent + 本地版本化风险策略`
决定语义与确认政策；旧远程自由文本风险审计、旧文本风险归一和旧固定 App 工作流不得再通过
配置重新进入生产裁决。

### 必须出现的证据

1. `DeepSeekTaskGraphPlanner` 不再创建或调用旧 `SemanticRiskAuditor`，也不再暴露启用旧诊断的构造参数。
2. plan/replan 不再让旧自由文本逻辑否决任务、要求确认或授予动作；迁移期 transport 清理只能删除模型自相矛盾的旧风险标记，随后必须立即进入 typed 投影。
3. `PHONE_AGENT_ENABLE_LEGACY_WORKFLOWS` 即使存在也不能重新开放旧微信/抖音/队列执行入口；旧路由固定只读退役并返回 410。
4. 普通发送、关注、评论等继续按新本地策略 automatic；登录、付款、敏感权限和不可逆删除仍需确认。
5. 坐标、ADB、Shell、keycode、`main.exe` 直控、过期 scope、重复目标和未验证画面继续失败关闭；这些是新执行安全合同，不属于待删除旧边界。

### 已有证据与唯一缺口

- 自由文本 canonical-input 否决已由 `ade42d8` 退出，生产旧远程风险诊断当前配置也为 false。
- 代码审计发现两个可恢复旧权威：`DeepSeekTaskGraphPlanner` 默认仍可启用旧远程风险审计；Web 仍允许环境变量重新开启旧固定工作流。另有一段旧图 transport 清理同时承担迁移职责，不能误删成模型图自相矛盾后直接阻塞。
- 本轮唯一缺口是移除两个旧权威入口，并把迁移清理明确降为“只删除矛盾标记、无否决/确认/执行权”的 typed 投影前转换，再用历史 swipe/input 误判样本和新策略正反样本证明正式裁决只剩新边界。

### 影响、回滚和停止条件

- 不删除历史报告读取器，也不删除名为 `legacy` 的卖家 GUI 传输模块；二者不参与语义或风险裁决。
- 不修改 App 名称、页面步骤、截图坐标或机械动作实现，不操作真机和 `main.exe`。
- 回滚范围限定为本批 planner/Web 入口与对应测试；若移除后正式类型化 authority 无法独立通过历史样本，停止并补齐新 typed 合同，不恢复旧文本否决权。

### 本批结果

- 正式 `DeepSeekTaskGraphPlanner` 已删除 `risk_audit_provider`、`enable_legacy_risk_diagnostics`、`SemanticRiskAuditor` 实例、调用计数和 plan/replan 审计调用；旧开关参数现在直接 `TypeError`，无法重新取得生产裁决权。
- `PHONE_AGENT_ENABLE_LEGACY_WORKFLOWS` 启动配置已删除，固定 App/旧队列生产路由固定为 410。测试若需读取历史实现，只能在测试进程内显式 monkeypatch，不能通过部署配置恢复。
- 旧图清理已重命名为 `_normalize_legacy_transport_for_typed_projection`，只能删除模型自相矛盾的旧 risk 标记，不能拒绝目标、要求确认、调用 Qwen 或授予动作；清理后立即进入 `TaskSemanticIR + 本地策略`。
- 历史远程审计语料由测试专用 planner wrapper 显式运行；生产模块不再顶层导入旧审计模块。正式普通发送继续 automatic，unknown effect 继续在 Qwen 前拒绝，登录/付款等策略未放宽。
- 验证：语义/任务图 `250/250`、新增 Web 入口定向 `3/3`、Python 完整回归 `1542/1542` 通过。本批代码提交后只重载项目 Uvicorn；新运行态 controller/camera online、busy=false、0 活动会话，并报告旧工作流和旧远程风险审计均关闭。未调用在线模型或机械臂，未操作卖家 `main.exe`。

## 当前批次唯一验收缺口

普通外部效果尚未形成“唯一效果控件 → 一次物理动作 → 新观察验证 → 同会话 succeeded”的真实闭环。当前先关闭 `send_message` 这一通用效果类型；完成前不并行扩展多字段或复杂手势组合。

## 当前子批次：拼音候选确定性续步

- **合同缺口**：`input_verified_text` 键入拼音后，新画面已经出现唯一、完整可见、逐字绑定 canonical 文本的 IME 候选时，必须保持同一个 typed input 子目标，由下一轮 Qwen 选择 `ime_exact_candidate`，点击后验证输入框值；不得在候选点击前让 DeepSeek 重新解释未完成的确定性输入事务。
- **根因证据**：真实 session `b4bf55988325464bb32990bb3993917b` 的 fresh before fingerprint 为 `fd1c4d7ea0304b8a0eab`，计划观察为 `a4256e20332cc652bd1f`；动作结果已严格绑定 fresh before 且 after 画面本地生成唯一 `local_audited_ime_candidate_1/你好/nihao`。`_verified_input_transaction_microstep` 却错误要求计划观察 fingerprint 等于 fresh confirmation scene，因正常的执行前重观察而返回 false，随后触发不必要的 DeepSeek replan 并因多 active 阻塞。
- **同类样本与通用修复**：覆盖中文拼音 preedit、英文分段、数字/符号 literal key 和键盘模式切换。微步身份改为校验 `resolved_action.before_fingerprint == fresh before_scene.fingerprint`；计划观察仍由 confirmation authority 在执行前校验，但不再被错误要求与 fresh scene 指纹相同。错误 fresh 绑定、错误 preedit/candidate、重复候选、错 canonical 正文继续失败关闭。
- **验证清单与停止条件**：新增“计划帧与 fresh 帧不同但 resolved 精确绑定 fresh 帧”的正例及 wrong fresh digest 反例；重跑现有 IME candidate Qwen/策略正反、编排器相关回归和一次 Python 全量。离线全绿后只重载项目 Uvicorn，再从当前真实候选画面建立全新会话；旧 blocked session 不复用，候选点击失败不自动重试。
- **离线结果**：微步校验现绑定 `resolved_action.before_fingerprint` 与 fresh confirmation scene，不再错误要求 planned observation 与 fresh scene 指纹相同；wrong fresh 绑定继续拒绝。现场形状定向 `4/4`、编排/Qwen 关联 `299/299`、Python 完整回归 `1543/1543` 通过。
- **首次新会话的第二个 0 动作缺口**：session `1f6cb61edb9d4de189e8109aefe50af1` 在 Qwen 前以“逐字一致文字目标不唯一：你好，共2个”阻塞。scene 同时保留模型粗元素 `e1/button/select_candidate/你好` 与本地输入审计元素 `local_audited_ime_candidate_1/button/ime_exact_candidate/你好`；两框 IoU `0.6203`，实际是同一个候选控件的两种描述，却被当成冲突。
- **通用重复对象修复**：同 role、逐字同 label、语义兼容且 IoU≥0.5 的框折叠为同一视觉对象；本地严格输入审计控件优先成为 canonical，保留原审计 bounds，不平均或发明坐标。不同 label 的重叠控件继续冲突，空间分离的同 label 控件继续保持两个并阻塞歧义。
- **第二批离线结果**：现场重复候选、分离同名控件、异义重叠控件及 IME 正式选择 `4/4`；observer/Qwen/编排关联 `513/513`；Python 完整回归 `1545/1545`。新会话全程 `physical_actions=0`，没有试探点击。
- **第二个新会话的第三个 0 动作缺口**：加载提交 `e14c0c2` 后，全新 session `abab00d558fd4acf832595ef48c2e555` 已能生成唯一“你好”点击动作，但确认前 fresh 观察把同一控件的粗语义从 `select_candidate` 改写为 `candidate_word`；adapter 因 meaning 漂移正确拒绝，`physical_actions=0`、手机未被点击。初始观察没有运行独立输入结构审计，因此动作后置条件也只有宽泛 `scene_changed`，不能作为正式输入事务权限。
- **第三批根因与同类样本**：`_goal_requests_input` 只扫描活动子目标的自由文本是否含“输入框/键盘”等词，没有取得正式 `TaskSemanticIR` 已生成的 `input.value_equals` desired state。所以“选择唯一逐字候选”虽已把 canonical 正文作为输入值后置状态，却被当成普通按钮目标。正样本为任意 App 中活动子目标的 typed desired state 逐字绑定 canonical 输入正文；变化样本为另一候选文字/另一 App；反样本为任务仍有 `input_text`，甚至控件标签偶然与正文相同，但当前 typed desired state 是结果列表、发送或其他非输入状态。
- **第三批通用修复说明**：`ObservationBridge` 只从本地编译且当前活动的 `TaskSemanticIR` 中投影一个只读 `active_input_transaction_text` 标记；仅接受 `input.value_equals`、subject 为 canonical `input_text` 实体且 value 与实体正文相同的唯一状态。观察器据此强制独立输入结构审计；审计仍须证明唯一应用输入框、匹配的拼音 preedit、唯一完整可见逐字候选，才本地生成 `ime_exact_candidate` 及精确 `expected_input_value`。模型/用户同名字段会被本地覆盖，普通发送、搜索结果或标签巧合均不获得权限。不增加 App 名称、坐标、固定页面或别名放宽。
- **第三批验证清单与停止条件**：先固化现场形状和“input_text 存在但活动标签为发送”的反例；运行 observer/Qwen/adapter/编排相关回归与一次 Python 全量。全绿后本地提交，只重载项目 Uvicorn；重新核对设备、相机、busy、活动会话和卖家 `main.exe` PID，再做 0 动作观察。只允许全新会话点击“你好”一次，动作后必须看到输入框 `value="你好"`；不点击发送，失败不重试。
- **第三批离线结果**：typed marker 正向、同名但非输入 desired state、发送节点不重启输入审计共 `3/3`；observer/Qwen/adapter/编排关联 `606/606`；Python 完整回归 `1548/1548`；四个修改文件 `py_compile` 与 `git diff --check` 通过。尚未加载服务或执行候选点击。
- **第三批加载后的第四个 0 动作缺口**：加载提交 `50c9061` 后，0 动作观察已稳定生成唯一
  `local_audited_ime_candidate_1/button/ime_exact_candidate/你好`。全新 session
  `f19b4f233ea2402f9bfbea02bbc04c6f` 也已产生精确的 `input.value_equals("你好")` 后置条件，但确认前
  以 `physical_actions=0` 拒绝：计划审计框 `[0.10,0.61,0.24,0.65]` 与 fresh 审计框
  `[0.09,0.626,0.192,0.654]` 的 IoU 为 `0.153`、较小框覆盖率 `0.274`、中心偏差仅
  `(0.009,0.020)`。两组第四帧目视内容相同，候选仍在第一位；没有发生手机动作。
- **第四批根因分类与样本集合**：主要类别为通用代码缺陷——短小输入辅助控件的独立 crop 几何审计会在
  静止画面上产生宽窄框抖动，旧全局 `smaller_coverage>=0.50` 合同没有利用“同一 strict local element
  id + 同一 meaning/label/states + 相同确认帧 + 两次独立 geometry attestation”的更强身份事实。正样本为
  当前 IME 逐字候选；变化样本为 strict local literal key 与键盘模式切换；反样本为普通按钮、非本地
  element id、缺少独立几何凭据、框完全不相交，以及历史 ee017 大幅纵向漂移。
- **第四批通用修复说明**：不修改全局 IoU/覆盖阈值。只对 `tap_semantic` 的本地严格输入辅助控件，在
  fresh 四帧身份已验证、计划与 fresh element id/meaning/label/role/states 均相同、两侧均有
  `element_geometry_audit` 私有凭据、框真实相交且中心仍落在紧邻范围时，允许较小框覆盖率最低 `0.25`；
  执行落点必须使用 fresh 审计框中心。任何普通控件、无凭据、无相交或大中心漂移继续 0 动作拒绝。
  回滚只撤销这一窄分支及其测试，不加入 App、正文、截图或固定坐标分支。
- **第四批验证与停止条件**：新增现场数值正例、缺 fresh frame identity/非本地控件/不相交与 ee017 反例；
  运行 adapter 相关最小测试、observer/Qwen/编排关联回归和一次 Python 全量。全绿后只重载项目
  Uvicorn，重新核对 controller/camera/busy/活动 session/main.exe，只建立一个全新 session 点击候选一次；
  动作后必须看到输入框 `value="你好"`，不发送，失败不自动重试。
- **第四批离线结果**：现场宽窄框、本地身份缺失、非本地控件、不相交框和历史大漂移定向 `4/4`；
  adapter 文件 `92/92`、observer/Qwen/adapter/编排关联 `608/608`、Python 完整回归 `1550/1550`
  通过。全局阈值未改，下一步只提交、重载项目 Uvicorn并执行上述唯一一次真机候选点击。
- **第四批真机结果**：提交 `c98ce33` 加载后，全新 session `992af6d8e14541438430aa88f78e7cd1`
  从唯一 strict local “你好”候选执行恰好一次 `tap_semantic`，fresh 落点来自重新审计框中心；
  `physical_actions=1`、`action_outcome=matched`，before/after fingerprint 分别为
  `c151296f158c338be665` / `0934cbb575e38ddc0295`，动作后四帧和控制器均逐字确认输入框
  `states.value="你好"`。随后独立 0 动作观察 fingerprint `c256feadc3d188ae5da9` 再次确认同一值；
  发送按钮未触发。
- **剩余终态缺口**：该会话在动作匹配后没有直接收口为 succeeded，而是再次对画面逐字“你好”做目标枚举，
  因输入框正文与候选栏同名而以“逐字一致文字目标不唯一，共2个”标为 blocked。候选点击与精确输入能力已取得
  真机 matched 证据，但普通外部效果的完整同会话 succeeded 闭环仍未完成；下一批应只离线修复“已满足
  `goal_complete_on_success` 后的终态收口”，不得重做输入、点击发送或扩大几何改动。
- **终态收口根因与样本集合**：`_verified_input_transaction_microstep` 在验证 fresh after input 后只判断该状态是
  合法输入步，没有区分 `expected_value` 是 canonical 全文还是未完成前缀。因此拼音预编辑、逐字候选落值和最后
  一个 literal key 全都被视为“继续同图并再次调用 Qwen”。正样本为 preedit `nihao` 后继续候选、长文本前缀与
  数字前缀继续下一段；终态样本为 candidate/literal/direct input 后 fresh value 已逐字等于 canonical；反样本为
  wrong value、mismatched、缺 controller evidence、错误 before/after fingerprint，均不得提前完成。
- **终态通用修复与验证计划**：微步函数仅在唯一 fresh input 精确满足本步后置状态且 `expected_value != canonical`
  时返回 true；当 `expected_value == canonical` 时返回 false，让现有 typed receipt/controller transition 进入
  DeepSeek `action_result_matched` 修订。只有 revision 严格递增、绑定旧 navigation subgoal 的 controller evidence
  被消费且新图 completed，session 才 succeeded；否则保持 blocked。新增 candidate/literal/direct final 正例、
  unfinished prefix 正例、wrong value/伪造 `goal_complete_on_success` 反例及“完成后 Qwen 不再被调用”的集成回归。
  不按 App、正文、控件标签或坐标分支，回滚只撤销微步终态判断与测试。
- **终态离线结果**：实现只改变 microstep 的最终布尔条件；candidate、final literal key、direct input 三种
  canonical 完成正例与未完成前缀/wrong value 反例均通过，既有 typed receipt 一动作完成且 Qwen 不二次调用
  的集成回归继续通过。编排器 `190/190`、observer/Qwen/adapter/编排关联 `609/609`、Python 完整回归
  `1551/1551` 通过。尚未加载服务；手机当前已是“你好”，本批没有再次操作真机。
- **加载后只读核对**：提交 `c852f64` 已加载到新 Uvicorn listener PID `3628`；controller/camera online、
  `busy=false`、0 active task/session，卖家 `main.exe` 仍为 PID `18236` 与原启动时间。独立 0 动作观察
  `executed=false/physical_action_requested=false`，fingerprint `5d3743d1575fe9b1e149`，再次逐字确认
  `local_audited_input_1.states.value="你好"`。没有为了制造同一前置状态而清空、重输或再次点击候选。
- **证据边界**：代码、历史现场形状回归、完整离线回归和加载后真实只读状态均已完成；由于当前手机已经是
  canonical 终态，本批没有再造一次“候选点击→post-action succeeded”物理会话。不得把旧 blocked session
  改写为 succeeded；下一次自然发生的最终 input step 应按新合同产生 typed receipt 后完成，届时再补同会话
  succeeded 报告即可，不需要为本修复主动重复真实输入动作。

## 必须出现的证据

1. 当前任务的 typed `EffectIntent` 同时绑定目标对象、正文和当前外部效果子目标。
2. 正式视觉 authority 只给唯一且语义一致的效果控件绑定 `effect.applied`；收件人行、输入框、返回键和其他邻近控件均无此权限。
3. 动作前重新观察，最终动作恰好一次；动作后取得四帧、新 observation/fingerprint 和 typed receipt。
4. 最终画面逐字显示已发送正文，任务图 `completed`、`active_subgoal=null`、session `succeeded`。

## 已有证据

- 真实微信场景 `778e5031798048f5abe0f02676d337e2` 已识别唯一 `meaning=send_message`、`label=发送` 的按钮；该会话目标明确要求不发送，因此没有点击。
- 旧真实发送目标 `0606355d` 在旧任务图动作词禁令处以 0 动作失败，未进入 Qwen 或机械臂。
- 本地风险策略已将普通 `send_message` 归为 automatic；登录、付款、敏感权限和不可逆删除仍需确认。
- 离线最小复现证明旧正式视觉 authority 会把同页收件人、输入框、返回键和发送键都绑定为同一个 `send_message` 效果，属于通用实现缺陷。

## 根因分类与同类失败样本

主要类别：代码缺陷——效果对象、效果载荷与“执行效果的控件”没有分开建模。

- 正样本：聊天页唯一 `meaning=send_message` 的按钮应取得 `send_message` effect authority。
- 变化样本：发布评论的唯一 `meaning=comment` 控件应绑定 `publish_content`；关注按钮的 `meaning=follow` 应绑定 `relationship_change`。
- 反样本：收件人行、正文输入框、返回键、关闭键、两个重复效果控件均不得取得最终效果 authority。

## 通用修复说明

- 在正式视觉候选编译层新增确定性的 effect-control 绑定，只读取 typed `EffectIntent.kind`、可信场景中的结构化 `role/meaning/states` 和唯一性。
- `binds_effect_target` 只表示效果对象，不再等同于“点击后效果已发生”；`binds_effect_payload` 只表示载荷。
- 只有通用效果类型与规范化控件 meaning 对应、角色为 button/icon/toggle、完整可见且全场唯一时，才生成 `effect.applied` transition。
- 不使用 App 名称、固定页面、固定步骤、标签文本或坐标；回滚为撤销本批提交。

## 验证清单

1. 固化上述正反样本并运行视觉 authority 最小测试。
2. 运行任务语义、Qwen、控制器、编排器相关回归。
3. 批次稳定后运行一次 Python 完整回归与前端合同回归。
4. 重新核对服务、设备、相机、busy、活动会话、最新观察及卖家 `main.exe` PID。
5. 用全新会话准备真实普通发送目标；最终外部动作前取得用户明确确认，只执行一次，然后按上述证据验收。

## 后续排队（当前批次完成后依次开启）

1. 多字段、多目标、长文本的真实同会话验收；协议上限 32 字段、32 目标、4000 字不能替代真机证据。
2. 长按、拖动、清空输入、关闭弹层在复杂页面组合中的跨 App 验收；已有专用验收页成功证据保留，但不冒充复杂组合完成。

## 当前离线结果

- 通用 effect-control 唯一绑定已实现；没有增加 App 名称、固定标签、固定步骤或坐标。
- 初始 effect-control 修复的视觉 authority 专项 `29/29`、语义/Qwen/编排关联 `355/355`、Python 完整回归 `1536/1536` 通过。
- 本批仍未加载服务、调用在线模型、相机或机械臂；普通发送的真机 succeeded 证据仍待完成。

## 首次真机准备失败样本

- 全新 session `f2fa0c35717b4adb95c3dcade73936df` 从 Launcher 开始，依次真实完成打开微信、进入文件传输助手、清空旧 `live21`，前三步均 `matched`。
- 第四步在输入框已经 `focused=true`、`value=""` 时仍生成并选择 `tap_semantic`，造成一次无状态变化的重复聚焦点击，结果 `mismatched`；没有输入“你好”，没有触发发送。会话已取消，设备恢复空闲。
- 通用根因：正式视觉 authority 对已聚焦输入框仍同时生成“聚焦点击”和 `input_verified_text` 候选；前者的后置条件在动作前已经满足，却仍被授予动作权限。
- 修复边界：已聚焦 input 不再生成 tap-to-focus 候选；未聚焦 input 仍可生成一次聚焦候选。正向测试要求 focused input 仅保留 typed input candidate，变化测试要求 unfocused input 仍能聚焦。
- 修复后专项 `30/30`、关联 `356/356`、Python 完整回归 `1537/1537` 通过；尚未重载服务或开始第二个真机会话。

## 第二次真机准备的 0 动作合同失败

- 全新发送前准备目标在 initial task graph 阶段 `physical_actions=0` 失败，目录 `generic_supervised_20260819_151354_73b87720`；没有创建会话、调用 Qwen 或点击手机。
- 原始 DeepSeek 图把 `focus_input` 定义为“输入框已聚焦、内容为空”，把实际输入“你好”放在后续 `type_text`，语义与依赖关系正确。
- 本地 `_describes_only_exact_empty_input_state` 只接受少数固定词序，并要求每条输入状态条件都同时包含空值；因此“输入框可见且聚焦”与“输入框内容为空”拆成两个完成条件时被误拒。
- 第一轮修复曾把空白准备状态改为条件集合正则；现场正向/反向 `3/3`、DeepSeek/语义/编排关联 `436/436`、Python 完整回归 `1539/1539` 通过，但它仍依赖少数词序，不能作为最终修复。

## 第三次真机准备仍为 0 动作：旧文本门禁必须退出正式路径

- 服务重载后的全新任务目录 `generic_supervised_20260819_151855_c10068dc` 再次在 initial task graph 阶段以 `physical_actions=0` 失败；原始 DeepSeek 图把 `focus_input` 写成“消息输入框可见且已聚焦”“输入框为空”，后续 `type_text` 才逐字输入“你好”，依赖和职责仍然正确。
- 根因证据：`Subgoal.validate()` 仍用 `_describes_only_exact_empty_input_state` 和 `_state_description_binds_canonical_input_text` 从自由文本推断输入载荷归属；“可见且已聚焦”没有正文值，却被旧门禁要求绑定最终正文。该门禁与正式 `TaskSemanticIR.input_fields`/`required_action` 重复，且会因自然语言变化产生误拒。
- 同类样本：两次现场失败分别采用“输入框可见且聚焦 / 输入框内容为空”和“消息输入框可见且已聚焦 / 输入框为空”；变化样本覆盖聚焦、可见、空白的不同组合；反样本为实际 `input_verified_text` 子目标输入非 canonical 正文或缺少 typed input field。
- 通用修复：彻底删除 `Subgoal.validate()` 中旧自由文本 canonical-input 否决权；正式输入权限只由 `TaskSemanticIR` 的 typed `InputFieldIntent.payload_ref`、`source_subgoal_ids` 与 typed `required_action=input_verified_text` 绑定裁决。聚焦/可见/空白仅是视觉状态，不拥有正文；真正输入动作必须唯一绑定 canonical payload，错正文或缺 payload 继续失败关闭。
- 回滚范围：只涉及旧输入文本门禁退出和 typed 输入绑定校验；不改变 App、页面、步骤、控件标签、坐标、外部效果或付款/登录风险规则。
- 验证清单：重放两份现场 raw graph；聚焦/空白措辞变化均通过；错误正文和缺 payload 的实际输入动作均拒绝；运行 DeepSeek、TaskSemanticIR、Qwen/编排相关回归及一次完整回归；最后只做一次“输入你好、停在发送前”的新会话真机验收。

## 旧输入文本门禁退出后的离线结果

- 已删除 `Subgoal.validate()` 中自由文本 canonical-input 否决分支及其四组固定词序正则；正式规划器现已进一步删除旧风险审计构造参数和调用，正式风险权威只认 typed `EffectIntent` 与本地策略。
- `TaskSemanticIR` 现在要求每个 `required_action=input_verified_text` 至少绑定一个 typed `InputFieldIntent`，并校验 payload 必须是 `role=input_text/type=text`；单输入字段任务按 typed 动作绑定 canonical payload，不从子目标正文提取替代值。
- `read_only` 子目标不会因“输入完成后”等历史描述被投影为新动作；两份现场失败图均重放通过，输入字段来源都唯一为 `type_text`，不含 `focus_input` 或 `verify_no_send`。
- 专项正反 `5/5`、DeepSeek + TaskSemanticIR `249/249`、Python 完整回归 `1539/1539` 通过。当前代码尚未提交、服务尚未重载；真机发送前输入验收仍待完成。

## 旧门禁退出后的首次新会话停止点

- 提交 `ade42d8` 已通过交互桌面方式加载；生产状态明确为 `legacy_remote_risk_diagnostics_enabled=false`，controller/camera online、`810x1515`、busy=false、0 旧会话，卖家 `main.exe` PID/启动时间未变。
- 全新 session `9c8843dcb2e04b13be0386e7027cc6b3` 不再触发 canonical-input 文本错误：任务图从 revision 1 推进到 revision 3 的 `type_text`，证明旧门禁已退出正式路径。
- 会话随后在物理动作前以 `physical_actions=0` blocked：可信观察证明唯一空白聚焦输入、QWERTY 和 typed 输入字段，并产生 `switch_keyboard_input_mode` 候选，但本地 choice 编译器把该候选的 expected result 错写为通用 `scene_changed`；控制器因此正确拒绝“输入辅助键缺少精确输入值后置条件”。
- 根因分类：代码缺陷——`switch_keyboard_layout` 与 `switch_keyboard_case` 已有确定性的输入框后置条件分支，`switch_keyboard_input_mode` 遗漏了同类分支；不是 App、坐标、模型随机或风险边界问题。
- 通用修复：choice 编译器从可信候选的 `input_element_id/prior_input_value/current_mode/target_mode` 本地构造 `application_text_input` 后置状态 `{value: prior_input_value, keyboard_input_mode: target_mode}`；控制器对模式集合、当前输入框状态、方向、文本保持和 exact expected state 做同等校验。禁止由 Qwen自由填写或从 App 文案推断。
- 同类样本：`direct_latin -> chinese_pinyin` 与 `chinese_pinyin -> direct_latin` 均应生成确定后置条件；缺 prior value、目标模式无效、方向不变、输入框当前模式不匹配或后置状态缺字段均失败关闭。
- 停止条件：离线正反与完整回归通过前不重载服务、不创建第二个真机会话；通过后只允许一次新的发送前输入验收，失败仍不自动重试。

## 输入模式切换后置条件修复的离线结果

- 本地 choice 编译器已补齐 `switch_keyboard_input_mode` 分支；正式候选只有在带 typed `keyboard_input_mode_switch` 状态时才生成，expected result 固定绑定原输入框、原 value 与目标模式。
- 控制器现把输入模式切换纳入输入辅助键完整校验，并验证 QWERTY、当前模式、目标模式、方向变化、输入框当前 value 及 exact postcondition；缺字段、方向不变和当前模式不一致均拒绝。
- 正向覆盖 `direct_latin -> chinese_pinyin` 与反向切换；反向样本覆盖缺 value、错误目标模式和输入框当前模式不一致。相关 Qwen/UI/编排回归 `347/347`，Python 完整回归 `1540/1540`，`py_compile` 通过。
- 当前修复尚未提交和重载，未创建第二个真机会话；session `9c8843dcb2e04b13be0386e7027cc6b3` 保持 `blocked/0 actions`，没有被续跑或重试。

## 普通发送闭环：当前视觉 claim_id 的唯一规范化

- **当前唯一验收缺口与必须证据**：在当前文件传输助手已准备正文“你好”的真实状态中，用一个全新
  universal-agent session 先以当前 typed visual claim 完成只读输入前置条件，再只执行一次唯一
  `send_message`，动作后四帧必须看到新消息气泡逐字显示“你好”；必须有新 observation/fingerprint、
  typed effect receipt、任务图 `completed`、`active_subgoal=null` 和 session `succeeded`。失败后不得自动
  重试物理动作。
- **验收台账与根因证据**：全新 session `de4ab0c9e83d41e19491c015af40dd4b` 在 start replan 阶段
  `physical_actions=0` 失败。DeepSeek 为 `verify_input` 返回裸 64 位 claim id
  `291e26d9...6485`；离线按当前 `obs_baf2610e3ed7458496239e8ec5a09b0a` 重建证明它恰好等于当前唯一
  `local_audited_input_1` 元素快照的 `VisualClaimEvidenceRef.claim_id`，但正式校验只接受带
  `visual_claim:<scene_id>:` 前缀的完整 `ref_id`。这是 typed evidence 传输的格式兼容缺口，不是事实伪造、
  App 特例、坐标问题或物理动作失败。
- **同类失败样本集合**：正样本为当前观察中唯一存在的裸 claim id；变化样本为另一 App/另一元素的当前
  唯一 claim id。反样本包括旧观察的 claim id、未知 64 位摘要、非 64 位文本、完整 ref_id，以及人为构造的
  同一 claim id 多义映射；反样本均不得获得当前视觉权限。
- **通用修复说明**：只在现有一次本地 evidence canonicalization 中，将裸 64 位十六进制字符串按
  `VisualClaimEvidenceRef.claim_id` 查找；仅恰好一个当前观察 ref 匹配时替换为其完整 `ref_id`。完整 ref、
  自然语言、未知/旧/多义摘要保持原样并继续由既有校验失败关闭。不增加 App、控件标签、固定步骤、坐标，
  不降低风险、fresh scope、唯一目标或一次一动作边界；回滚范围仅该规范化及测试。
- **验证清单与停止条件**：先重放现场正样本及上述变化/反样本；运行 DeepSeek 与编排相关测试，再运行一次
  Python 完整回归和 `py_compile`/`git diff --check`。全部通过后才提交并重载项目 Uvicorn，重新核对
  `/api/device`、controller、camera、busy、活动会话和卖家 `main.exe` PID。随后只建立一个全新发送会话；
  若物理动作失败立即停止，若 0 动作合同仍失败则保留新证据且不得把它写成发送成功。
- **离线结果**：唯一当前 claim id 与未知/多义反例 `2/2`；DeepSeek `229/229`；编排器 `190/190`；
  Python 完整回归 `1553/1553` 通过。生产改动仅为当前观察 claim id 的唯一映射；尚未提交、重载或执行
  发送动作。
- **加载与真实动作结果**：提交 `63281a5` 已只重载项目 Uvicorn，监听 PID `7620`；卖家 `main.exe`
  保持 PID `18236` 和原启动时间。全新 session `6f5b862feb9b4c65a9bed3be3c334474` 先以完整 current
  visual ref 完成 `verify_input`，再为新 `send_message` 子目标取得 observation
  `obs_1105dc6c6e7d4763935b7b86e0455c01` / fingerprint `a7b3f54d547ae44aecd9` 和一次性 scope。
  唯一一次 `tap_semantic` 执行后为 `physical_actions=1` / `matched`，typed receipt
  `receipt_7128e017638846f1b0be345033177e79`；before/after fingerprint 为
  `530172073ca19714c274` / `33df6d3c57f5444c4d0d`，输入框由“你好”变为空，未自动重试。
- **真实结果复核与证据边界**：动作后保存的第四帧
  `qwen_visual_revision_3_b82ef416f18346488b19b42b1116548f_after_attempt_2_4.jpg` 清楚显示最底部新的
  绿色我方气泡“你好”。随后独立 `/api/agent/generic-scene` 以 0 动作返回
  `executed=false`、`physical_action_requested=false`、fingerprint `479f84fe965c36a3ab63`，唯一
  `latest_green_bubble/chat_message_bubble` 的 label 为“你好”、`is_sender_message=true`。因此真实普通发送及
  可见结果已经完成。
- **仍未满足的正式同会话终态**：动作后的首轮通用观察虽然在原始帧中包含新气泡，但有效 scene 只报告
  “输入框为空、发送按钮消失”，漏掉气泡。DeepSeek 随后把已执行的 external_state 子目标改成 blocked，
  session 最终为 `blocked`、revision 4、`active_subgoal=null`，不是 `succeeded`。不得把独立 0 动作复核
  拼接成同会话 succeeded，也不得重新输入或再次发送。下一唯一通用缺口是：严格绑定的 effect receipt 已
  证明效果动作只执行一次、但视觉结果暂未被首轮观察覆盖时，系统必须保留该 receipt 并进入只读结果复核，
  而不是阻塞后询问是否重复效果动作。

## 外部效果一次执行后的延迟视觉结果复核

- **当前唯一验收缺口与必须证据**：任意 `external_state` 效果动作已经产生严格绑定、`matched`、恰好
  1 次物理动作的 `VerifiedActionTransition` 后，如果第一次动作后视觉场景没有覆盖最终结果，session 必须
  保留一份只读待复核状态。下一次 `/next` 只能采集新的四帧和运行 DeepSeek 结果复核，不能调用 Qwen、
  不能生成确认 scope、不能再次执行效果动作。新的 typed visual claim 能证明结果时同一 session 才
  `succeeded`；仍无证据时以 0 个新增物理动作 `blocked`。
- **验收台账与根因证据**：真实 session `6f5b862feb9b4c65a9bed3be3c334474` 已有唯一发送动作、matched
  receipt、变化后的 observation/fingerprint 和包含新气泡的原始第四帧；首次有效 scene 只报告输入框清空，
  DeepSeek 因此返回无 active 的 blocked 图。随后独立 0 动作观察立即唯一识别最新绿色“你好”气泡。这证明
  根因是代码缺少“已执行效果的只读结果复核”运行态，不是发送按钮、微信坐标、风险政策或动作执行失败。
- **同类失败样本集合**：正样本为消息发送后结果气泡延迟进入语义场景；变化样本为评论/发布/保存等普通
  typed effect 已执行、结果标记在下一次稳定观察才出现。反样本包括 outcome 非 matched、0/2 次物理动作、
  wrong session/task/device/revision/subgoal/action digest、未绑定当前子目标的 EffectIntent、旧或未知 receipt、
  新观察没有匹配视觉 claim，以及高风险效果未经过原有确认 scope；均不得进入成功或产生第二动作。
- **通用修复说明**：在 session 中保存本地构造的 `pending_effect_verification`，严格绑定原
  `VerifiedActionTransition`、唯一当前 `EffectIntent`、EffectPreview digest 和首次动作后 observation。
  receipt 只证明该效果动作已经执行一次，不能证明页面结果；最终 `effect.result_visible` 仍只允许当前新观察的
  typed visual claim 完成。首次 replan 未完成外部效果时，不接受“重发”路径，而进入
  `needs_effect_verification`。该状态的 refresh 是一次只读终局复核：不进入 Qwen/控制器，成功后清除 pending，
  失败则终止且不重试。无 App 名称、固定文案、固定步骤或坐标；回滚范围为该 session 状态、只读分支及测试。
- **语义编译同根修正**：同一外部效果子目标的完成条件即使逐字包含 payload，也必须先按其唯一 effect 归类；
  “结果可见/显示”编译为 `effect.result_visible`，允许来源为 visual claim + effect receipt，而不是误编译成
  `input.value_equals`。effect receipt 仍不能单独证明可见结果。
- **验证清单与停止条件**：先用真实失败形状和另一 typed effect 变化样本做离线正例；覆盖所有绑定错误、无
  visual claim、receipt 重放和非 matched 反例；断言 pending refresh 的 Qwen/adapter.execute 调用数均不增加、
  physical_actions 恒定。运行 TaskSemanticIR、DeepSeek、编排器相关回归及一次 Python 完整回归，最后提交。
  本批禁止再次操作手机或发送；已有真实效果足够作为失败样本，代码测试通过只标为实现/回归通过，不伪造旧
  blocked session 的终态。
- **离线实现与结果**：session 已新增严格绑定 receipt、唯一 EffectIntent 和 EffectPreview digest 的
  `effect_verification` 状态。matched external effect 首轮未完成时进入 `needs_effect_verification`；一次 fresh
  refresh 只捕获四帧并调用 DeepSeek，成功必须让原外部效果子目标使用当前 observation 的完整
  `visual_claim` ref 完成。复核失败、preview 摘要篡改和无结果分支都保持 1 次总物理动作、1 次总 Qwen 动作
  选择且无新 scope。effect 子目标包含 payload 文字时现优先编译为 `effect.result_visible`；只有明确“动作已
  执行/效果已触发”才是 receipt-only `effect.applied`。定向 TaskSemanticIR `24/24`、编排器 `192/192`、
  DeepSeek `229/229`、前端协议与离线浏览器 `39/39`、Python 完整回归 `1557/1557`，`py_compile`、JS
  语法和 `git diff --check` 均通过。未重载服务、未调用相机或机械臂；旧真实 session 仍诚实保留 blocked。
- **提交与加载状态**：实现已本地提交为 `435f783`。确认 `busy=false`、0 活动会话后，只重载了项目
  Uvicorn，监听 PID `7620 -> 15440`；加载后 controller/camera online、`busy=false`、0 活动会话，视觉模型
  仍为 `qwen3.7-plus`、observer v62。没有启动、停止或点击卖家 `main.exe`，没有调用相机观察或机械臂，
  也没有为了制造证据再次发送。

## 复杂输入：确认前输入框状态抖动的通用重绑定

- **当前唯一验收缺口与必须证据**：在真实 App 的唯一空白且已聚焦输入框中，输入一段超过单次 30 字符
  上限的中英文、数字和标点混合长文本；必须按正式输入事务分段，每个物理步骤前后都有新四帧、
  observation/fingerprint 和 matched receipt，最终输入框值与 canonical `input_text` 逐字一致，页面仍停在
  发送或提交之前。本轮不证明多字段，禁止发送、提交、保存和自动重试。
- **验收台账与根因证据**：session `8c73e7b014be49a092593562b92234f1` 在文件传输助手的真实空白聚焦
  输入框上得到 `input_verified_text` 正式候选，但确认前以
  `确认时目标语义不再严格唯一：application_text_input，匹配0个` 停止，`physical_actions=0`。计划场景的
  本地输入审计将 `keyboard_case_mode` 记为 `unknown`；同一页面的既有成功输入样本
  `992af6d8...` 将其记为 `lower`，且还可能附加 `ime_preedit_text/ime_exact_candidate_text`。当前
  `_rebind_action` 把这些观察丰富度字段连同输入框身份状态一起传给 `find_elements` 做精确子集匹配，因而
  同一输入框会在动作前被误判为不存在。这是代码缺陷，不是 App、坐标、风险边界或长文本本身失败。
- **同类失败样本集合**：现场样本为 `keyboard_case_mode: unknown -> lower`；历史变化样本为同一输入框在
  两次观察间增加或省略 IME 预编辑/候选提示。正例还包括当前观察增加 `fully_visible=true` 的可信丰富化。
  反例必须覆盖输入值改变、失焦、键盘布局改变、输入模式改变、当前目标不可见、两个兼容输入框同时存在、
  几何不重叠和正式 meaning 真正改变；这些情况仍须 0 动作失败关闭。
- **通用修复说明**：把“输入框身份/动作前置状态”和“可变观察元数据”分离。输入类动作先按空标签、input
  role、兼容的已知前置状态及原区域重叠筛选唯一当前输入框；`goal_relevant`、键盘几何、大小写未知值和 IME
  提示不参与身份匹配。原 value、focused、keyboard layout、已知 input mode、fully-visible 安全条件仍必须
  单向满足；新增观察字段允许丰富但不能覆盖原前置条件。正式候选 meaning、几何重叠、fresh observation、
  一次一动作和动作后逐字验证保持不变。不增加 App、文本、控件名称或坐标分支；回滚范围仅输入重绑定兼容
  函数及测试。
- **验证清单与停止条件**：先增加上述正例、变化例和全部反例，运行 adapter/输入事务/编排器最小相关测试；
  稳定后运行一次 Python 完整回归、`py_compile` 和 `git diff --check`。全部通过并本地提交后，取消旧的 0 动作
  session，只重载项目 Uvicorn；重新核对 device/controller/camera/busy/main.exe，再用一个全新 session 做
  一次长文本真机闭环。任何物理动作失败立即停止，不重复输入，不把单字段结果写成多字段完成。
- **离线实现与结果**：输入重绑定现把原 `keyboard_case_mode=unknown` 视为未观察事实，只允许 fresh 本地
  审计将该字段丰富为已知大小写；原本已知的 `lower/upper` 改变仍严格拒绝。`goal_relevant` 与键盘几何继续
  不作为稳定身份，其余 value、focused、layout、input mode、fully-visible 约束未放宽。现场形状正例和已知
  case 改变反例 `2/2`，adapter `77/77`，相关规划与编排 `287/287`，Python 完整回归 `1559/1559`，
  `py_compile` 与 `git diff --check` 通过。尚未提交、重载或产生物理动作。
- **首次真实分段结果与第二个同根缺口**：提交 `0227541` 并只重载 Uvicorn 后，全新 session
  `122a265e0ed7440baacbd5900087a754` 的第一步正确输入拼音 `fuzashuru`，只产生 1 个物理动作；第二轮 fresh
  四帧唯一证明原输入框仍为空、已聚焦、中文拼音模式，并同时出现逐字候选“复杂输入”。但动作后第 1 次
  观察未唯一绑定输入框而暂态失败，第 2 次观察已经通过 `verify_after_action` 时，adapter 仍把第 1 次的
  `verification_errors` 原样带入最终结果，将正确的第 2 次结果记为 `mismatched`。session 因此停在
  `awaiting_confirmation`，未点击候选、未发送。根因是有界复观察成功后的错误累计代码，不是候选、App、
  坐标或机械输入失败。
- **第二批通用修复与反例**：动作后最多两次只读观察保持不变；仅当后一次观察和控制器完整验证成功时，
  该次物理动作按最终 fresh 证据记为 matched，前一次暂态不匹配不再充当最终 verification error。两次都失败、
  第二次采集/观察失败、最终 value/preedit/candidate/App/page/输入框绑定不符时仍 mismatched 或失败关闭；动作
  绝不重复。正例用“第 1 次缺输入框、第 2 次唯一输入框+精确拼音候选”，变化例用普通页面第 1 次旧场景、
  第 2 次目标后置状态；反例保留现有两轮均错与第二次采集失败测试。修复范围仅成功出口的最终错误归类及
  回归测试。
- **第二批离线结果**：现场同形正例证明第 1 次暂态遗漏、第 2 次唯一输入框及逐字拼音候选时最终为
  `matched`、`verification_errors=()` 且 robot 只调用一次；两轮都错和第二次采集失败仍保留首轮错误并停止。
  三项定向 `3/3`，adapter+编排相关 `271/271`，Python 完整回归 `1560/1560` 通过。当前真机 session 仍停在
  正确但未提交的拼音预编辑状态，没有点击候选或发送；修复尚未提交、重载。
- **第二次真实分段与第三个通用缺口**：提交 `bf728dd` 并只重载 Uvicorn 后，新 session
  `0086a6d8b04a4009a267b88e559f0d24` 先用 1 次 `tap_semantic` 将既有唯一候选“复杂输入”写入输入框，
  outcome=`matched`；随后 1 次 `input_verified_text` 正确产生 `yanshou` 和唯一候选“验收”。但动作前后同一
  页面被模型分别命名为 `微信/聊天界面` 与 `文件传输助手/chat_input`，控制器即使已验证输入框 value、
  focused、QWERTY、中文模式、区域重叠和方向全部稳定，仍因 raw app/screen 字符串不同将两轮均记为
  `输入动作后 App 或页面身份发生变化`。session 停在 `awaiting_confirmation/2 actions`，未点击“验收”、
  未发送。这是通用视觉身份字段把“应用名/当前表面标题”混写及中英文页面族未规范化，不是动作失败。
- **第三批通用修复与反例**：将输入页 `chat/conversation/compose/editor/form/input/search` 及对应通用中文
  UI 类别规范成确定页面族。raw app id 不同只能在 before/after 都属于同一非占位输入页面族时继续验证，
  且仍必须唯一输入框区域稳定重叠、聚焦不变、已知 keyboard layout/input mode 一致、相机方向一致；App
  相同仍保留原快速路径。跨页面族、unknown 占位伪匹配、输入框移动、失焦、键盘模式变化、方向变化和多个
  输入框均拒绝。实现不得包含 App 名、联系人、固定坐标或目标文字；变化样本至少覆盖聊天与编辑/搜索两类
  页面族，现有 `page-a -> page-b` 反例必须继续失败。
- **第三批离线结果**：通用中英文输入页面族已本地规范化；当 app 字段是展示名/当前表面标题时，仅同一明确
  页面族及全部本地输入稳定条件可续接。形如 `com.example.real` 的明确包标识一旦变化仍无条件拒绝，完整
  回归首次发现的跨包同 chat 反例没有被放宽。现场同形正例、跨族/unknown/跨包/键盘模式变化反例均通过；
  相关 action/scene/orchestrator `339/339`，Python 完整回归 `1562/1562` 通过。修复尚未提交、重载；当前
  session 保留正确的 `yanshou` 与唯一“验收”候选，未点击候选、未发送。
- **数字阶段与第四个通用缺口**：提交 `8be8efb` 并只重载 Uvicorn 后，session
  `85e00b103f5a4ff89f39f9cb04a97881` 用 1 次 matched 点击将“验收”写入输入框；下一次物理动作正确把键盘
  切到符号/数字布局。动作后两轮模型场景都把可见“符”键自声明为
  `keyboard_input_mode_switch=true/current_mode=symbol/target_mode=unknown`，严格 scene parser 因方向无效在
  本地输入结构审计前拒绝整个场景。session 终止为 `failed/2 actions`，输入框仍为“复杂输入验收”，没有
  输入数字或发送。根因是无效的模型自声明权限阻塞了随后本地 typed 审计，不是布局切换动作失败。
- **第四批通用修复与反例**：模型只能提供可见按钮事实，不能用不完整方向字段铸造键盘模式切换权限。对
  `keyboard_input_mode_switch=true` 但 current/target 不在正式模式集合或方向相同的模型状态，仅撤销该私有
  switch claim 及其方向字段，不改 label/role/meaning/bounds，不创造目标；随后必须由现有独立输入结构审计
  重新铸造 layout/mode/case/literal-key typed 候选。带 action/坐标/额外协议字段仍严格报错，合法且方向明确的
  结构保持现有行为；本地审计失败则 0 新动作阻塞。变化样本覆盖 symbol/unknown、模式相同和合法双向切换。
- **第四批离线证据**：模型无效 switch claim 的直接撤销、完整 scene parse、同模式变化样本、合法已知模式别名/
  大小写规范化、协议额外动作字段失败关闭均已覆盖；观察器回归 `217/217`，Python 完整回归 `1565/1565`
  通过，两个修改文件静态编译通过。尚未将本批代码加载到正式服务，也尚未执行新的真机动作。
- **第五个通用缺口与根因证据**：第四批提交 `5a14e29` 加载后，新 session
  `8e9d6d05fb7c4f8fb22fe4282fcacfa1` 连续完成 5 个 matched 动作，将输入框从“复杂输入验收”逐字推进到
  “复杂输入验收2026”；准备输入全角冒号前，确认帧的严格输入结构审计把当前符号键盘写成
  `keyboard.layout=symbol_grid`。本地协议只接受 `symbol`（已接受精确旧别名 `symbols`），因而在动作前以
  `keyboard.layout 无效` 停止，本请求新增物理动作 0、总动作 5、未发送。失败样本集合为现场
  `symbol_grid`、已有等价 `symbols`，变化反例 `symbols_custom`；根因是跨模型层对同一正式 symbol 布局缺少
  精确、有限、单义的别名规范化，不是按键坐标或 App 特例。
- **第五批通用修复与验证清单**：仅把大小写/外层空白处理后的精确 token `symbol_grid` 与 `symbols` 映射为
  正式 `symbol`，同时用于普通 scene 键盘枚举和独立 input-structure audit；不接受前缀、子串、任意
  `*_grid` 或 `symbols_custom`，不改 literal key、布局切换方向、geometry 或动作权限。回滚仅删除这两个精确
  alias。验证要求覆盖两个正向等价 token、`symbols_custom` 反例、普通 scene 与 input audit 两层、相关观察器
  回归和一次完整离线回归；全部通过后才重新加载并从“复杂输入验收2026”新建续输会话。
- **第五批离线证据**：`Symbols`/`symbol_grid` 两个精确正例、普通 scene/input audit 双层映射和
  `symbols_custom` 反例均通过；观察器回归 `218/218`、Python 完整回归 `1566/1566`、两个修改文件静态编译
  通过。尚未加载第五批代码或执行新真机动作。
- **第五批同根遗漏与修正范围**：提交 `38e63a0` 加载后的 0 动作新会话证明，顶层 `keyboard.layout` 已被正确
  规范化，但同一输入结构中的 `layout_switches[*].current_layout` 仍保留 `symbol_grid`，两轮动作前审计均以
  “layout_switch 方向或 bounds 无效”停止。原始两份响应都同时给出有效冒号 literal key、目标
  `target_layout=qwerty` 和当前 `current_layout=symbol_grid`；这是同一个正式布局 token 在嵌套方向字段中遗漏
  规范化，不是新的语义。修正必须复用同一精确 alias helper 同时规范化 source/target，再做原有方向、label、
  bounds、confidence 和键盘内包含关系校验；`symbols_custom`、同向切换、错误 label/越界仍全部拒绝。只有嵌套
  正反例、观察器回归和完整回归通过后才再次加载。
- **第五批修正后离线证据**：现场形状 `layout=symbol_grid`、`current_layout=symbol_grid`、
  `target_layout=qwerty`、`label=返回` 已作为集成样本通过，非目标“返回”键被安全忽略而不阻塞合法 literal key；
  精确 `symbol_grid→symbol` 的方向正例和 `symbols_custom` 反例通过。观察器 `218/218`、Python 完整回归
  `1566/1566`、静态编译和 diff 检查通过。尚未加载本次同根补全，也未执行新真机动作。
- **可选键盘能力互相阻塞的通用缺口**：`dc56b56` 加载后的新会话在 0 动作动作前观察中，两轮都识别出唯一
  全角冒号 literal key，但同一 payload 还把邻近退格图形读成 `✘`，并把“常用/中文/英文/返回”等符号面板
  分类标签报告为超出正式 qwerty/numeric/symbol 集合的 layout switch；旧验证器让任一无效可选候选否决整个
  输入结构，因此一个不可授权的旁路误识别反而阻塞了独立有效的冒号键。样本集合包含现场 `✘`、已有空
  backspace label、合法 `⌫`，以及现场 `frequent/chinese_symbols/english_symbols/previous`、合法三布局方向和
  协议额外字段反例。
- **候选级失败关闭方案与验证清单**：顶层 input-structure 和每个子对象的精确字段集合、类型、数量、置信度
  格式、协议额外/action 字段继续全局严格拒绝；仅对精确 schema 下但字面图形不构成退格，或 source/target
  不是正式布局、同向、与当前布局不一致、bounds 无效的可选能力候选执行“撤销该候选/列表项”。被撤销项
  不得生成 element、geometry 或动作；若任务本身需要退格/布局切换，因缺少唯一候选仍 0 动作阻塞；独立合法
  literal key/输入框事实可继续。验证必须含现场冒号集成正例、合法退格/布局切换正例、错误 label/方向/bounds
  不授权反例、额外字段仍报错、清空任务缺退格仍阻塞、观察器与完整回归，之后才重新加载。
- **候选隔离离线证据**：现场 `symbol_grid + 冒号 + ✘ + frequent/previous` 集成样本生成且只生成可信冒号
  literal-key 目标；无效 backspace 没有写入通用 keyboard geometry，无效 layout-switch 项没有生成 element。
  合法 numeric backspace、合法 `symbol→qwerty` 方向仍可用；错误 label/非正式方向不授权，嵌套 action 和顶层
  协议外字段仍报错。观察器 `219/219`、Python 完整回归 `1567/1567`、静态编译通过。尚未加载或执行新动作。
- **冒号动作真实结果与验收口径**：`71bd715` 加载后，session `78777ec4510a436ea4d3fa5a21d0c964`
  对唯一可见冒号键执行 1 次动作，键盘从符号面板回到数字页；独立 0 动作复核明确输入值为
  `复杂输入验收2026:`，即键面视觉被模型声明为全角 `：`，实际应用写入半角 `:`。因此该动作不能计入原全角
  文本的 matched 证据，也不通过修改验证器把两种 Unicode 字符当相等。用户本轮只要求“复杂输入任务”，未指定
  必须使用全角标点；后续另建验收合同，保持当前真实半角前缀并将目标冻结为
  `复杂输入验收2026: 长文本, 数字与标点均需逐字保持; 本次内容只留在输入框内.`，继续验证长中文、数字、
  空格和多种 ASCII 标点。全角标点能力仍明确记为未验收，不用降级口径伪造完成。
- **长文本续输的当前能力边界**：ASCII 长文本新合同在 0 动作观察时连续两次收到缺少
  `confidence/fully_visible` 的 literal-key 项，而且当前 numeric 页面没有可信的空格或 qwerty 目标；即使把
  这些不完整项候选级丢弃，也不会产生下一安全动作，所以本轮不为“让请求继续”而给缺失权限字段补默认值。
  长中文+空格续输仍记为未完成。为直接完成用户未限定具体内容的“复杂输入任务”，最终真机合同改为保持当前
  已真实存在的中文+数字+冒号前缀，并只使用当前独立审计可见的数字/符号键，目标冻结为
  `复杂输入验收2026:123+45-6@7.`；它同时覆盖中文既有内容、连续数字、加减号、@ 和句点，最多 11 个新动作，
  不含发送/提交。只有完整逐字值和 `succeeded` 才算本轮通过；长文本、空格和全角标点不会被该结果覆盖。
- **literal-key 输出膨胀的通用根因与修复计划**：混合目标 session `d012956e87df44de9f9ea1a7d3080bb1`
  已在初始观察选中下一唯一字符 `1`，但确认帧审计仍把目标全文中可见的 `2/0/6/1/3/+/4` 一并枚举，且
  每项都遗漏 `fully_visible`，最终 0 动作停止。根因是 `_input_audit_literal_key_targets` 以完整 target_text 构造
  最多 8 字符白名单，没有利用同一 preliminary scene 已逐字观察到的唯一当前 input value；提示范围远大于“一次
  一个物理动作”的合同。样本集合包含当前前缀到 `1`、前缀推进后到 `2`、下一步为字母/拼音时空白名单、
  当前输入缺失/重复时不得猜测的回退样本。修复仅在 preliminary scene 恰好有一个字符串 input value，且
  `plan_next_verified_input(target,current)` 成功时，把审计白名单缩为下一 literal-key 单字符；下一步是
  direct_latin/chinese_pinyin 时为 `[]`。无唯一当前值、前缀不一致或规划失败时保持原有有界白名单，不从目标猜
  当前值。parser 对目标字符本身的 confidence/fully_visible/label/bounds 仍完全严格，不补默认值。验证覆盖 prompt
  与 observer 调用链、模糊回退、现有全量回归；通过后才加载并重跑冻结混合目标。
- **单字符审计白名单离线证据**：compact 输入几何继续被完全隔离，仅把恰好一个 preliminary input 的字符串
  value 作为审计选择 hint；`当前前缀→1`、`推进后→2`、下一步字母/拼音时 `[]`、不一致前缀回退旧有界集合
  均通过。完整 observer 调用链实际 prompt 只含 `whitelist=["1"]`，独立 audit 再确认后才生成 `1` 候选；
  目标候选的 confidence/fully_visible/label/bounds 未放宽。观察器 `220/220`、Python 完整回归 `1568/1568`、
  静态编译通过。尚未加载或执行新动作。
- **第六个通用缺口与根因证据**：提交 `7db3e09` 并只重载项目 Uvicorn 后，全新 session
  `2da8357c71cb488ea85d987a6df0446c` 已把确认审计收敛为唯一下一字符 `1`，但在任何物理动作前停止；
  planned 与 fresh 四帧分别通过独立 `element_geometry_audit`，语义、逐字 label、element id、输入前后值和
  本地来源均一致，矩形仍相交且覆盖较小框 `0.464`，仅因中心漂移 `(0.046, 0.025)` 超出现有局部横向
  `max(0.03, 0.25*width)` 门而被拒绝。主类别为通用几何门禁对低矮键帽的两次模型框抖动容差不足；不是
  App、固定坐标、按键选择、机械标定或风险规则问题。
- **同类失败样本集合**：现场正例为同一 `local_audited_literal_key_1` 的双独立审计、强相交、
  `coverage=0.464`、中心差 `(0.046,0.025)`；已有变化正例为窄 IME 候选框的双审计抖动。反例包含相邻数字键
  约 `0.20` 的横向位移、相邻行约 `0.07` 的纵向位移、完全不相交、缺任一独立几何凭据、确认帧身份不一致、
  label/meaning/states 改变，以及普通非本地控件。反例必须继续 0 动作失败关闭。
- **通用修复说明**：只调整 `stable_audited_local_input_target` 特例：仍要求确认帧身份一致、
  `tap_semantic`、相同 `local_audited_*` element id、相同 meaning/role/label/states、两端均由
  `element_geometry_audit` 本地铸造，并保持实际相交与原有 `smaller_coverage>=0.25` 下限，同时把
  中心容差改为横向 `max(0.05,0.30*max_width)`、纵向 `max(0.03,0.75*max_height)`。普通控件仍使用全局
  IoU/coverage 门，任一语义或凭据不一致不适用特例；不增加 App、字符、键盘坐标或页面分支。回滚只需恢复
  该局部布尔门三个阈值。
- **验证清单与停止条件**：先用现场数值正例和相邻键/相邻行/缺 frame identity 反例覆盖 `_rebind_action`，
  复跑已有普通控件与不相交反例；再运行 adapter/规划/编排相关测试、一次 Python 完整回归、静态编译和
  `git diff --check`。全部通过并本地提交后，取消旧 0 动作 session，只重载项目 Uvicorn，重新核对
  `/api/device`、controller、camera、busy、device id、活动会话与卖家 PID；只用一个全新 session 续验冻结文本
  `复杂输入验收2026:123+45-6@7.`。任何物理动作 mismatch/failed 立即停止，不自动重试，不点击发送。
- **第六批离线证据**：现场数值同形正例、相邻键/相邻行、缺 frame identity、普通控件和不相交反例
  `4/4` 通过；完整 adapter 类 `81/81`、Python 完整回归 `1569/1569` 通过。实现只改变双重独立审计的
  `local_audited_*` 输入辅助目标中心容差，未改变全局几何门、语义绑定或动作权限。静态编译和差异检查
  随提交前检查记录；尚未重载或产生新的物理动作。
- **第六批真机结果与同根二次证据**：提交 `5b5d972` 并只重载项目 Uvicorn 后，全新 session
  `cce32522c94c48dab68be67454e3fdae` 的第 1 个字符 `1` 已用一次物理动作逐字验证为 matched，输入框从
  `复杂输入验收2026:` 推进为 `复杂输入验收2026:1`；第 2 个字符 `2` 在动作前 0 动作停止，误差为
  `iou=0.257, smaller_coverage=0.418, center_delta=(0.080,0.016)`。真实帧显示 `2` 位于中间数字键，fresh
  几何审计框向左覆盖到 `1/2` 之间；因此不能继续采用“放宽后直接使用 fresh 框中心”，本轮已取消，未点击
  `2`、未发送。该证据把根因进一步收敛为两次独立模型框无确定性共识点，而非阈值单纯过小。
- **同类样本与通用共识方案**：现场正例要求 planned/fresh 同一 `local_audited_*` 目标、相同逐字 label、
  meaning/role/states、相同确认画面身份、两端独立几何凭据，且两框覆盖较小框至少 `0.40`、中心差横向不超过
  `0.10`、纵向不超过 `0.03`；执行区域必须改成本地计算的两框交集，不能采用任一模型框。原窄候选路径保留
  已验证的 `coverage>=0.25/x<=0.05/y<=0.03`，同样改用交集。相邻键约 `0.20` 横移、相邻行约 `0.07` 纵移、
  无交集、覆盖不足、缺凭据/画面身份、label/meaning/states 变化和普通控件全部拒绝。交集由本地确定性计算，
  不增加 App、键值、坐标或键盘布局分支；回滚只涉及 adapter 的局部交集共识分支。
- **二次验证清单与停止条件**：增加现场 `0.418/0.080/0.016` 正例并断言 rebound 使用精确交集 bounds；
  增加同样中心差但覆盖不足、相邻键、相邻行、无 frame identity 反例，保留普通控件和无交集回归。完成
  adapter、相关规划/编排、一次完整 Python 回归、静态编译和差异检查后本地提交；取消旧 session、只重载
  Uvicorn，再从当前真实前缀 `复杂输入验收2026:1` 新建一次冻结目标续验。任何新物理动作 mismatch 立即停止，
  不重复动作，不点击发送。
- **交集共识离线证据**：现场同形共识正例及覆盖不足/相邻键/相邻行/无 frame identity/普通控件/不相交反例
  `4/4` 通过，adapter 完整类 `81/81`、Python 完整回归 `1569/1569` 通过。生产执行路径会先把 fresh scene
  中唯一目标替换为本地计算的 planned/fresh 交集，再由现有控制器从交集中心解析物理点；模型不能指定该共识
  区域。新增端到端 adapter 测试进一步证明 FakeRobot 收到的是交集中心而非任一模型框中心；最终 Python
  完整回归再次为 `1569/1569`。静态编译和差异检查随提交前检查记录；尚未加载本批代码或产生新动作。
- **交集共识真机结果与第七个通用缺口**：提交 `a687334` 并只重载 Uvicorn 后，全新 session
  `fa005636788a41719d74990ec221f2c7` 在同一会话连续完成 `2`、`3`、`+` 三个 matched 动作，输入框从
  `复杂输入验收2026:1` 精确推进到 `复杂输入验收2026:123+`，证明交集中心实际落点有效。第 4 个动作把真实
  画面推进到 `复杂输入验收2026:123+4`，但两轮动作后观察均在本地 parser 失败，session 最终
  `failed/4 actions`，未继续、未发送。两份原始审计都只返回下一完整可见键 `5`，字段、置信度、可见性和
  bounds 合法；提示层白名单也是 `5`，但 parser 仍按旧全文前 8 个非字母字符集合
  `2/0/6/:/1/3/+/4` 校验，错误认定 `5` 越界。根因是单字符白名单只接入 prompt、遗漏 parser，不是模型、
  几何、机械动作或 App 问题。
- **同类样本集合与通用修复**：现场正例为目标 `…:123+45-6@7.`、唯一当前值 `…:123+4`、合法 literal key
  `5`；变化正例覆盖当前前缀继续到 `-`、`@` 等目标全文前 8 个字符之外的位置。反例覆盖当前应为 `5` 却返回
  `6`、唯一输入缺失/重复、当前前缀与目标不一致、literal key 缺字段/额外字段/低置信/不可见。parser 在得到
  唯一可信 `trusted_input` 时，必须把该输入的逐字 `text` 原样传给现有 `_input_audit_literal_key_targets`，与
  prompt 使用同一 `plan_next_verified_input` 结果；无唯一可信输入时保留旧有界集合且不能猜前缀。不得补默认
  字段、不得丢弃错误目标键、不得增加 App/文本/字符特例。回滚仅恢复 parser 的一个参数。
- **第七批验证清单与停止条件**：增加现场 `5` parser 集成正例、错误 `6` 反例、后续符号变化例，并调整旧
  “同时枚举多个目标键”样本为严格单下一键合同；运行 observer 最小测试、完整 observer、相关 adapter/编排、
  一次 Python 完整回归、静态编译和差异检查。本地提交并只重载 Uvicorn 后，必须先用独立 0 动作观察确认
  当前真实值 `复杂输入验收2026:123+4`；仅当成立才新建一次剩余冻结续验。任何新物理动作失败立即停止，
  不自动重试，不点击发送。
- **第七批离线证据**：现场 `5` 与后续 `-` 的 parser 集成正例、错误 `6` 反例、旧白名单外键反例和提示层
  共享函数测试 `4/4` 通过；观察器完整回归 `221/221`、Python 完整回归 `1570/1570` 通过。parser 仅在
  `trusted_input` 唯一时传入其逐字当前值，无唯一输入仍使用原有有界回退；没有放宽 literal key schema、
  置信度、可见性或额外字段。静态编译与差异检查随提交前检查记录；尚未加载或产生新动作。
- **第七批加载后的真机续验与第八个通用缺口**：提交 `0edd5dc` 加载后，全新 session
  `0d12ef651da44c17b66dc9378fd6f724` 在同一冻结目标中连续完成 `5`、`-`、`6` 三个 matched 动作，输入框
  精确推进到 `复杂输入验收2026:123+45-6`。下一字符 `@` 的 planned/fresh 独立几何审计和本地交集共识均
  通过，但共识框约为 `[0.802,0.798,0.988,0.860]`，中心 `x≈0.895` 超出实测标定凸包右边界
  `0.88257`，机械标定层在真正点击前拒绝外推；`@` 未被触碰、未发送。真实截图显示同一键帽在标定凸包内
  仍有约 `43%` 的二维面积，根因是右侧目标视觉框偏宽而执行点固定取整个框中心，不是风险规则、字符白名单、
  App 分支或标定失效。
- **第八批同类失败样本集合**：现场正例为同一 `local_audited_*` 输入辅助目标已具备双独立几何凭据、确认帧
  身份一致、语义/逐字 label/states 相同，且其本地共识框与实测凸包面积覆盖约 `0.43`、横纵轴覆盖均至少
  `0.40`；变化正例覆盖屏幕左、上、下边缘的同形局部目标。反例包含目标框完全在凸包外、任一轴或面积覆盖
  低于 `0.40`、交集中心离原中心过远、交集中心贴近目标/凸包边缘、缺任一独立审计、普通按钮、宽框跨多个
  控件、过期帧及无有效标定。反例必须保持 0 物理动作失败关闭。
- **第八批通用修复说明**：保留 `corrected_grid_point` 的凸包拒绝权，不对任意坐标做 clamp。仅在 adapter 已经
  证明目标是同一确认画面上的双独立 `element_geometry_audit`、同一 `local_audited_*` 输入辅助目标且已形成
  planned/fresh 本地交集后，允许机械层在动作前计算“该共识目标矩形 ∩ 实测标定凸包”的凸多边形。原中心在
  凸包内时完全不变；原中心在外时，只有面积覆盖和每轴覆盖均 `>=0.40`、交集质心距原中心不超过
  `max(0.03,0.30*目标轴长)`，且质心距目标框与凸包边缘均至少 `0.01` 时，才把动作前落点替换为该交集质心。
  普通控件、单次模型框、低覆盖和无标定不获得该恢复路径；最终点仍再次通过原 `corrected_grid_point`。回滚只
  删除此局部输入目标的动作前解析与一个纯标定几何函数，不影响全局标定或其他动作。
- **第八批验证清单与停止条件**：先为凸包裁剪、面积/轴覆盖、边缘距离、位移上限和原中心不变编写纯离线正反
  测试；再做 adapter 端到端测试，证明只有双审计局部输入目标在物理计数前使用交集质心，普通按钮/缺凭据/
  低覆盖仍拒绝或沿用原中心并被标定层关闭。运行标定、adapter、相关编排回归及一次完整 Python 回归、静态编译
  和差异检查。全部通过并本地提交后只重载项目 Uvicorn，先用独立 0 动作确认真实前缀仍为
  `复杂输入验收2026:123+45-6`，再用一个全新 session 仅续输 `@7.`；任一动作 mismatch/failed 立即停止，
  不自动重试，不点击发送或提交。
- **第八批离线证据**：现场 `@` 数值样本解析为标定凸包与目标框交集质心 `(842,829)`，并由原
  `corrected_grid_point` 再次接受；原中心在凸包内保持不变，低覆盖和过大位移反例均失败关闭。adapter 端到端
  证明只有双审计局部输入共识目标会把精确交集 bounds 交给机械标定解析器，普通语义点击不调用该恢复路径；
  标定+adapter `132/132`、action/orchestrator `224/224`、Python 完整回归 `1573/1573` 通过，五个修改文件
  静态编译与 `git diff --check` 通过。尚未加载本批代码或产生新的物理动作。
- **第八批加载后的 0 动作门与第九个通用缺口**：`7eb177a` 加载后，设备在线空闲且活动会话为 0，卖家
  `main.exe` 仍为原 PID `18236`。连续两次独立四帧观察均为 0 物理动作并被输入结构协议关闭：第一份模型把
  全屏纵坐标写成最高 `1920`，违反明确的 full-frame `0..1000` 合同；第二份已使用合法 `0..1000` 几何并逐字
  观察到当前值 `复杂输入验收2026:123+45-6` 与唯一 `@` 键，但 literal-key 只输出 `label/bounds/key_kind`，
  漏掉 parser 强制的 `value/confidence/fully_visible`。根因主类别是输入审计提示接口没有把 literal-key 的六字段
  形状放进最终 JSON 样例，文字段落虽提到各语义，模型仍稳定仿照最终 `literal_keys:[]` 样例省略字段；不是
  手机状态、字符白名单、标定或风险问题。按两次同类合同失败规则已停止继续采样。
- **第九批同类失败样本集合**：现场反例一为像素/传统屏幕高度式纵坐标，反例二为缺
  `value/confidence/fully_visible` 的 literal-key；历史合法样本为六字段 `@`、`5`、`-` 单键。变化样本覆盖
  whitelist 为空时必须仍为 `[]`、空格键的 `value=" "/key_kind="space"`、普通字符动态逐字 label，以及
  额外字段、错误 value、低置信、不可见和越界 bounds 仍由 parser 拒绝。不得从缺失字段补默认值，也不得把
  1920 坐标自动缩放为 1000。
- **第九批通用修复说明**：不放宽 parser，只修正模型接口的唯一形状矛盾。输入审计 prompt 明确要求每个
  literal-key 恰好六字段，并在 whitelist 非空时把当前唯一目标字符动态写入最终 JSON 形状样例；whitelist
  为空仍给 `[]`。坐标合同补充“屏幕宽高/长宽比不改变统一 0..1000，底边永远是 1000，禁止源像素或传统
  屏幕高度”，但本地仍逐项严格拒绝越界，不做缩放、clamp 或字段推断。该修复跨 App、字符和键盘布局复用，
  回滚只恢复 prompt 文本，不改变执行权限。
- **第九批验证清单与停止条件**：测试非空 `@` prompt 含动态六字段最终样例、空白/普通字符和空 whitelist
  变化样本，以及明确的统一坐标句；保留 parser 缺字段/额外字段/越界/低置信反例。运行 observer 相关测试、
  一次完整 Python 回归、静态编译和差异检查，提交并只重载 Uvicorn 后再做一次独立 0 动作观察。若仍违反
  input-structure 合同则停止；只有逐字前缀和唯一目标键均可信，才新建冻结目标 session 输入 `@7.`。
- **第九批离线证据**：非空 `@` prompt 现把当前 whitelist 与完整六字段 literal-key 对象写入同一个最终 JSON
  样例，空 whitelist 仍严格为 `[]`；统一坐标段明确屏幕底边恒为 `1000`。parser 未改，缺字段、额外字段、
  低置信、不可见和越界仍按原合同拒绝。观察器 `221/221`、Python 完整回归 `1573/1573`、静态编译与
  `git diff --check` 通过。尚未提交、重载或再次调用模型。
- **第九批加载后的真实动作与第十个通用缺口**：输入审计接口提交 `f72b325` 并加载后，独立 0 动作观察成功
  逐字确认当前值 `复杂输入验收2026:123+45-6`、唯一完整 `@` 键和 fingerprint
  `3c132ffcc9226508e2c2`。全新 session `d6aa55cc6c5a4fb0969e20a1a9fc0425` 的唯一动作经过新标定解析后真实
  写入 `@`；动作后截图明确显示输入末尾为 `…-6@`，未发送。但两轮输入结构审计都把窄输入框内的视觉自动
  换行转写成实际 `\n`，得到 `复杂输入验收\n2026:123+45-6@`，本地精确 verifier 正确拒绝并终止为
  `blocked/1 action`，没有继续 `7` 或 `.`。主类别是视觉呈现软换行与真实字符值未分层，不是落点或动作失败。
- **第十批同类失败样本集合**：现场正例为动作前精确值无换行、唯一非换行 literal-key `@`、typed expected
  等于 prior+key、动作后唯一高置信 input 的可见字形去除 `\r/\n` 后逐字等于 expected，且 evidence/label
  同时保留该换行转写。变化正例覆盖长数字/字母在窄输入框的二行或三行软换行。反例包含目标文本本身要求
  换行、动作不是 literal-key、prior/expected 链不成立、去换行后仍不等、多个 input、缺可见 evidence、低置信
  或失焦；这些均不得规范化。
- **第十批通用修复说明**：输入审计 prompt 明确“控件自动折行不是 `\n`，应把连续可见字形还原为一个字符
  串；无法证明实际换行时不得猜”。同时在动作后 verifier 前增加一个仅由本地 typed literal-key 回执授权的
  软换行消歧：仅当 prior 不含换行、key 不是换行、expected 精确等于 prior+key、唯一动作后 input 的 raw value
  含换行且删除 CR/LF 后精确等于 expected，并有同值可见 evidence 时，才在该动作的 trusted after scene 中把
  value/同值 label 还原为 expected并附加本地来源说明。它不用于初始观察、普通输入、换行动作或近似文本，
  不删除空格/标点，也不改变物理动作。回滚只删除该局部重建函数和 prompt 句子。
- **第十批验证清单与停止条件**：增加软换行正例、错误字符/真实换行目标/非 literal-key/缺 evidence 反例，
  证明重建后的 scene 可通过原 exact verifier；增加 prompt 断言。运行 adapter、observer、controller/编排相关
  测试和一次完整回归、静态编译、差异检查。全部通过并提交后只重载 Uvicorn，先做一次独立 0 动作观察；若
  当前值不能逐字还原为 `复杂输入验收2026:123+45-6@` 则停止。成立后只能用新 session 续输 `7.`，不得重按
  `@`，任何失败停止且不发送。
- **第十批离线证据**：现场同形 `li\nve2 → live2` 正例经本地逐键回执消歧后通过原 exact verifier；错误字符、
  缺同值 evidence 和目标含真实换行反例均保持原 scene，不会被规范化。初始输入审计 prompt 已明确软折行不等于
  `\n`。observer/adapter/controller/orchestrator 相关回归 `544/544`、Python 完整回归 `1574/1574`、四个
  修改文件静态编译和 `git diff --check` 通过。尚未提交、重载或进行新的模型/物理动作。
- **第十批加载后停止结论**：提交 `34b8367` 加载后，卖家 `main.exe` 仍为原 PID `18236`，设备在线空闲且
  活动会话为 0。唯一一次独立 0 动作观察仍被 input-structure parser 关闭；原始模型结果实际逐字识别到
  `复杂输入验收\n2026:123+45-6@`，并合法返回唯一下一键 `7` 的完整六字段对象，但 preliminary scene 中的
  视觉软折行已使 `_input_audit_literal_key_targets` 无法把当前值认作目标前缀，parser 因此按旧有界回退集合拒绝
  `7`。该请求 `physical_action_requested=false`，没有触碰手机。当前已有真实截图证明 `@` 动作写入成功且未
  发送；冻结目标只完成到可见末尾 `…-6@`，`7.` 尚未输入，复杂输入任务未完成。
- **止损与下一架构缺口**：同一软折行根因已经历“提示接口澄清”和“动作后 typed 回执消歧”两轮；后者能保护
  今后的同会话动作，但项目服务重载后只剩无动作来源的视觉场景，单张画面无法安全区分真实换行与控件自动折行。
  继续按目标文本直接删除 `\n` 会把真实换行误当软折行，属于用目标污染观察事实；继续放宽 literal-key parser
  则修错层。按反补丁门禁停止生产修改与真机采样，不清空、不重按 `@`、不发送。下一步必须先设计跨服务/跨会话
  的本地 typed input lineage：只有由逐次已验证输入回执形成的 exact value、当前画面去折行字形与该 value 完全
  一致、设备/输入表面/fingerprint 链绑定且未过期时，才可给新会话恢复精确当前值；无 lineage 继续失败关闭。
- **第十一批验收缺口与根因证据**：当前唯一缺口是让已经由一次真实逐键动作写入的精确输入值，在项目服务重载后
  仍能被新的只读观察安全识别，从而继续同一复杂输入；不是让目标文本反向决定观察值。现场 session
  `d6aa55cc6c5a4fb0969e20a1a9fc0425` 已保存完整 typed action：动作前唯一 input 值为
  `复杂输入验收2026:123+45-6`，目标键是唯一 `input_exact_literal_key` 的 `@`，expected 严格等于 prior+key，
  `physical_actions=1` 且 GUI event barrier/round-trip receipt 成立；动作后唯一高置信聚焦 input 的可见转写含
  `\n`，删除 CR/LF 后逐字等于 expected，四帧证据和新 fingerprint 均在。服务重载后的独立 0 动作观察再次
  逐字得到同一软折行值，并返回合法下一键 `7`，但 preliminary scene 不能证明其为目标前缀而回退到旧有界字符集。
  根因分类为“已验证输入值没有跨 session/服务持久化的类型化连续性合同”，而非模型随机、键盘布局、落点或 App。
- **第十一批同类失败样本集合**：正例一为现场 `prior + @ → expected` 的持久执行快照与重载后同输入框软折行画面；
  正例二为任意 App 中逐键回执形成的长字母/数字/标点值，在窄输入框二至三行视觉折行后，删除 CR/LF 与回执 exact
  value 完全相等。变化样本包括普通无折行的精确值和服务未重载时的新一键回执。反例包括设备不一致、记录过期、
  非 literal-key 动作、expected 不是 prior+key、physical_actions 不是 1、GUI barrier 无效、before/after 指纹未变、
  四帧证据缺失、动作后不唯一/低置信/失焦 input、删除 CR/LF 后仍不等、真实目标值本身包含换行、当前输入框几何/
  语义表面不兼容，以及仅凭用户目标文本猜测当前值；反例全部必须继续失败关闭。
- **第十一批通用修复说明**：新增本地版本化 `TypedInputLineage`，只记录严格验证的逐字符输入交易：device、精确值、
  input 语义与几何、before/after observation fingerprint、typed action/receipt digest、生成时间和来源。记录本身不授予
  任何动作权限；新观察仍需四帧稳定、Qwen 独立看到唯一高置信聚焦 input，且其 raw value 删除 CR/LF 后与未过期
  lineage exact value 完全相等、输入语义和几何兼容，才可在调用 input-structure audit 前把该 exact value作为
  “已验证当前值”选择器，并在审计后本地还原同一 input 的 value。每次新的 literal-key 动作经原 controller exact
  verifier 成功后原子更新 lineage。为恢复已发生但旧版本未记录的 `@`，提供通用离线导入器：只从持久 session
  execution 中重验上述 typed 链、GUI receipt、四帧文件、唯一 before/after input 与 fingerprint 后生成同一类型记录；
  导入不生成 confirmation authority，也不执行动作。无记录、绑定不一致或任何证据缺失时保持原有严格行为。
- **第十一批验证清单与停止条件**：先为 lineage 严格 schema、原子存储、TTL、设备/几何/语义绑定、现场同形 execution
  导入和所有反例编写离线测试；再补 observer 正例 `li\nve2 + lineage live2` 能枚举下一键、真实换行/错值/错设备/
  过期记录保持关闭，以及 adapter 成功动作更新记录的测试。运行 observer、adapter、controller/orchestrator 相关回归，
  再运行一次 Python 完整回归、静态编译和 `git diff --check`。全部通过并本地提交后，只重载项目 Uvicorn，不触碰
  `main.exe`；用通用导入器重验现场 `@` 交易并写入 lineage，先做一次独立 0 动作观察，必须确认 exact current value
  为 `复杂输入验收2026:123+45-6@` 且唯一下一键为 `7`。随后只用一个全新 session 依次输入 `7`、`.`，每次一动作、
  新四帧、新回执和新 fingerprint；任一失败立即停止，不自动重试，不点击发送/提交/搜索/换行。最终以独立 0 动作
  逐字确认完整冻结目标作为闭环证据。
- **第十一批离线证据**：新增类型化连续性 7 个正反测试通过，覆盖严格逐键链、事件栅栏、原子存储、TTL、设备/
  App/页面/输入框几何绑定、错误值和历史四帧缺失；现场 session `d6aa55cc6c5a4fb0969e20a1a9fc0425`
  已由通用导入器离线重验为 exact `复杂输入验收2026:123+45-6@`，未执行动作。动作后时序也已纳入同一合同：有效
  点击栅栏先形成不落盘的一次性 expected lineage，仅用于下一次只读画面逐字比对；原 controller exact verifier 成功
  后才更新持久记录。lineage 不能生成 confirmation scope、不能跳过四帧/模型/几何/策略门。observer、adapter 相关
  回归 `327/327`，Python 完整回归 `1581/1581`，四个生产文件和测试文件静态编译、`git diff --check` 通过。
  尚未提交、重载、写入正式 lineage 状态或产生新物理动作。
- **第十一批首次 0 动作门与绑定修正**：`c4e1a94` 加载并导入现场回执后，独立观察仍以 0 动作关闭。原始审计
  再次逐字报告 `复杂输入验收\n2026:123+45-6@` 和合法唯一 `7`；本地数值复核确认同设备、同 App、同页面、
  同 exact value 均成立，但模型将同一两行输入框从历史 `[0.13,0.54,0.69,0.61]` 重画为
  `[0.13,0.59,0.68,0.69]`，中心纵向差仅 `0.065` 且仍相交，较小框覆盖 `0.281`，被初版 `0.45` 几何下限
  拒绝。该门槛把视觉模型对软折行高度的正常漂移误当成不同输入表面。通用修正仅把 coverage 下限改为 `0.25`，
  仍要求实际相交、横向中心差 `<=0.10`、纵向 `<=0.08`，并保留设备/App/页面/唯一聚焦输入/精确文本/TTL 全部
  绑定；现场高度变化为新增正例，纵向继续移动到中心差超限、相邻横向框和不相交框继续为反例。此次请求没有产生
  物理动作；修正后必须重新完成相关与完整回归，再做且只做一次 0 动作门。
- **第十一批第二次 0 动作门与身份漂移样本**：几何修正 `a5fe23a` 加载后，独立 0 动作观察成功得到 exact
  `复杂输入验收2026:123+45-6@`、唯一下一键 `7`、`input_lineage_used=true`。随后全新 session
  `5c0d5b95bb604b47862e080d0ede5910` 在动作前以 `blocked/0 actions` 停止；同一画面这次被 compact 观察写成
  `app_id=unknown, screen_id=chat_input_composing`，因此初版严格等值身份绑定未使用 lineage，最终 scene 只有带软
  折行的 input、没有逐键候选，Qwen 如实 blocked。根因是把模型生成且会退化/细化的 App/页面名字当成稳定本地身份。
  通用修正只允许以下降级：当前 App 必须是原明确 App 或 `unknown`，screen 必须与原 screen 相等或互为下划线边界
  的细化前缀；只有 App 为 unknown 时还额外要求 exact value 长度 `>=8`。明确的其他 App、无关/unknown screen、
  短值、错值、错设备、错几何继续拒绝。新的已验证逐键若当前视觉身份退化，只有上一 lineage exact 等于本轮 prior、
  before 输入几何兼容且页面细化关系成立时，才能继承上一条明确身份；否则不写持久记录。该会话未产生物理动作，
  修正后必须重新完成相关与完整回归，再建立全新 session，禁止复用 blocked session 或旧 scope。
- **第十一批最终真实闭环**：身份连续性提交 `9c805eb` 相关回归 `330/330`、Python 完整回归 `1584/1584`、
  静态编译和差异检查通过后，只重载项目 Uvicorn；卖家 `main.exe` 始终保持 PID `18236`、启动时间
  `2026-08-19 14:36:18`。全新 session `c6debd2edc5b44ce85981d72446e3793` 在同一会话按顺序执行且仅执行
  两个 `tap_semantic`：第1步 exact prior `…-6@` → expected `…-6@7`，fingerprint
  `cfaff0b55a371c40c568 → b9a0a9d8eb2baa69d2c0`；第2步 exact prior `…-6@7` → expected `…-6@7.`，
  fingerprint `b9a0a9d8eb2baa69d2c0 → ca9a938fac0a74ec1c02`。两步均 `matched/physical_actions=1`，最终
  `status=succeeded`、`active_subgoal=null`、总物理动作2，无自动重试、无发送/提交/搜索/换行。持久 lineage 已由
  实时逐键回执更新为 exact `复杂输入验收2026:123+45-6@7.`，来源 `verified_live_literal_action`。
- **最终独立只读补充与边界**：动作后另一次独立 `/api/agent/generic-scene` 为 `executed=false`、
  `physical_action_requested=false`，逐字可见 `复杂输入验收\n2026:123+45-6@7.`，`literal_key_count=0`，发送控件
  `goal_relevant=false`；它再次把视觉软折行保留为 raw `\n` 且本轮 `input_lineage_used=false`，因此不能用这份独立
  观察单独声称 canonical exact。正式成功证据来自同一真实会话两次 controller exact verifier、新 fingerprint 与
  最终实时 lineage。复杂输入 `7.` 闭环已经完成；剩余边界只是“模型再次给出与已知页面无关的身份名称时，独立
  无动作预览是否展示 canonical 无换行值”，不得为此回退已完成动作或继续真机采样。
- **第十二批验收缺口与根因证据**：本轮只关闭上一条剩余边界：持久 lineage 已逐字保存
  `复杂输入验收2026:123+45-6@7.`，独立只读画面也看到删除 CR/LF 后完全相同的唯一聚焦输入框，但模型再次改写
  `app_id/screen_id` 后 `input_lineage_used=false`。已证实正文、设备、输入框和真实页面都未变化；不稳定的是模型生成
  的页面名称。根因分类为“把模型语义名称当成本地输入表面主键”，不是输入、键盘、落点或物理动作失败。
- **第十二批同类失败样本集合**：正例覆盖同一真实输入表面被模型命名为明确 App、`unknown`、页面名细化以及完全
  不同的任意名称；它们必须同时具备同设备、未过期 lineage、唯一高置信聚焦 input、删除 CR/LF 后逐字相同、兼容
  几何和与已验证动作后四帧相符的本地局部画面描述。变化正例使用轻微相机噪声和同一控件软折行高度漂移。反例
  覆盖其他画面、错值、错设备、过期、失焦/多输入、不兼容几何、真实换行目标、无动作后四帧凭据以及局部描述超
  阈值；即使模型声称同一 App/页面也不得用局部描述绕过正文与控件门。
- **第十二批通用修复说明**：升级为版本化 lineage v2。每次逐键动作通过原 exact verifier 后，除 typed action/
  receipt/fingerprint 外，从同一动作后四帧按已验证 input bounds 生成本地固定算法的灰度局部描述并原子持久化；不把
  App 名称或目标文字写入描述算法。新会话仍先要求 exact 可见正文、设备、TTL、唯一聚焦 input 和几何全部成立；
  页面名称不兼容时，只有当前真实帧与任一持久动作后局部描述通过确定性距离门，才把该表面认作同一表面。审计后
  使用同一真实帧再校验一次。无描述的旧记录不能走此降级；历史导入器必须从已存在的四帧重新生成 v2 描述。该凭据
  只消歧当前值，不生成 action authority、不跳过模型/四帧/策略，也不允许近似正文。
- **第十二批验证清单与停止条件**：补 v2 严格 schema、局部描述确定性、相机噪声正例、任意模型身份名正例，以及
  不同画面/无描述/错正文/错几何反例；验证实时记录接收动作后四帧、历史导入从四帧重建、observer 在审计前后均
  使用同帧凭据。只运行相关测试后运行一次完整回归、静态编译和差异检查。本地提交后只重载项目 Uvicorn，保持卖家
  `main.exe` 不变；从已成功 session `c6debd2edc5b44ce85981d72446e3793` 重验最后一步生成 v2，再且只做一次独立
  0 动作观察。必须得到 canonical 无换行 exact、`input_lineage_used=true`、`physical_action_requested=false`；失败
  立即停止，不采样第二次、不产生物理动作。
- **第十二批离线证据**：lineage v2 专项 `11/11` 通过，观察器、编排器、mock loop 与 v3 scope 相关回归合计
  `440/440` 通过，Python 完整回归 `1585/1585` 通过；四个生产/测试文件静态编译和 `git diff --check` 通过。
  另外用真实成功 session 的最后四帧离线重建 v2，得到 exact `复杂输入验收2026:123+45-6@7.`、四份局部描述；
  在当前只读摄像头帧上把模型身份替换为任意无关名称后，仍仅凭 exact 正文、兼容几何和局部描述得到唯一匹配。
  尚未提交、重载或执行最终 0 动作模型复核。
- **第十二批最终只读闭环**：实现提交 `8e4a6d0`。只重载项目 Uvicorn 后，首次受限进程不能枚举交互桌面，按 0
  动作门停止；随后仅以交互桌面权限重启项目服务，卖家 `main.exe` 全程保持 PID `18236`、启动时间
  `2026-08-19 14:36:18`，未启动、停止或点击。用成功 session 最后一笔交易及其四帧重建正式 v2 后，唯一一次
  `/api/agent/generic-scene` 返回 `executed=false`、`physical_action_requested=false`、`input_lineage_used=true`；
  当前唯一 input 为高置信聚焦状态，canonical value 逐字等于 `复杂输入验收2026:123+45-6@7.`，不含视觉软折行，
  发送控件 `goal_relevant=false`。本轮没有物理动作、没有第二次模型采样。模型本次给出明确 App 和相关页面名；完全
  任意身份名的降级分支由同一真实当前帧的离线替换样本证明，不虚称模型本轮恰好产生了该名称漂移。

## 未完成真机闭环集中收口

- **当前唯一验收缺口**：先关闭“普通外部效果延迟视觉复核”的正式终态。当前文件传输助手中唯一聚焦输入框已由
  lineage v2 与独立只读观察共同证明，正文逐字为 `复杂输入验收2026:123+45-6@7.`，唯一发送控件可见且此前
  `goal_relevant=false`。本轮新目标明确要求把这段现有正文发送到当前文件传输助手，只发送一次、不改正文、不
  切换会话。必须在一个全新 universal-agent session 中形成恰好一次 `send_message` matched receipt；若首轮漏检
  结果，只允许进入 `needs_effect_verification` 后进行一次 0 动作 fresh refresh，最终必须 `succeeded`、输入框为空、
  新我方消息气泡正文逐字一致。任何 mismatch、第二个物理动作、重新输入或页面漂移立即停止。
- **根因与已有证据**：旧 session `6f5b862feb9b4c65a9bed3be3c334474` 已真实完成一次发送并看到绿色“你好”气泡，
  但首轮动作后 Qwen 漏掉气泡，导致旧合同把会话写成 blocked。提交 `435f783` 已实现通用延迟结果复核且完整离线
  回归通过；缺少的是加载后下一次自然外部效果任务的同会话真机 succeeded 证据，不需要再改代码。
- **同类样本与通用边界**：正例是任意普通外部效果只执行一次，随后由 fresh typed visual claim 证明结果；变化
  正例覆盖发送、评论、关注、发布和收藏。反例为结果仍不可见、effect preview/target/payload/digest 漂移、旧 receipt
  重放或 refresh 试图生成第二动作。最终结果不可见时必须 0 新动作 blocked，不能把 receipt 单独当效果成功。
- **本轮验证和停止条件**：动作前重新核对 controller/camera、busy、device、活动会话、最新画面和 `main.exe`；
  start 接口使用全新 session，普通发送按当前本地风险政策 automatic，不创建付款/登录权限。动作后只读取报告和
  新画面；若状态为 `needs_effect_verification` 只调用一次 `/next`。完成后再依次开启多字段/多目标/长文本与复杂
  手势组合，不能并行占用机械臂。
- **首次新会话的 0 动作根因证据**：session `8145bcfecd0442aab1f1c3727eea744e` 在完成只读正文核对并切换到
  `send_message` 后，`/next` 以 `blocked/physical_actions=0` 停止。落盘 revision 的正式 risk 已是
  `confirmation_required=false`，本地 policy 也只确认登录、付款、敏感权限和不可逆删除；但 DeepSeek 重规划的旧
  transport 候选在正式投影前仍可携带模型 `confirmation_required=true + status=ready`。`candidate.validate()` 先于
  `apply_formal_semantic_risk_policy()` 执行，于是以“本地风险策略要求确认”误拒，新的唯一风险权威根本没有机会生效。
- **同类样本、通用修复与验证清单**：正例覆盖发送/评论/关注等模型旧字段要求确认、但 typed policy 为 automatic
  的重规划；变化正例覆盖付款模型漏确认、由本地 policy 反向提升为 confirmation。反例覆盖缺 risk 对称引用、unknown
  effect、非法 active frontier，不能被规范化掩盖。修复只在 replan 的迁移 transport validate 前，把“唯一 active 外部
  子目标 + 已声明且模型标为确认的 risk”状态规范为 `awaiting_confirmation`，使旧对象内部自洽；随后立即编译 typed
  authority，本地 automatic 会投影回 `ready`，高风险则保持/提升确认。补 DeepSeek 正反测试、编排回归、完整回归后
  才重载并建立全新 session；旧 blocked 会话不复用，不为本样本添加发送或微信分支。
- **普通发送最终真机闭环**：重规划迁移态修复提交 `bc2e59e`；普通 effect 重规划正例与付款反向确认测试通过，
  DeepSeek `231/231`、编排器 `193/193`、相关语义/确认 `37/37`、Python 完整回归 `1587/1587`，静态编译与
  `git diff --check` 通过。只重载项目 Uvicorn 后，卖家 `main.exe` 保持 PID `18236` 和原启动时间。全新 session
  `c0de1d2d3eb84cf2840336e6e5ab26e6` 的动作前输入值逐字为 `复杂输入验收2026:123+45-6@7.`，当前会话标题为
  文件传输助手，唯一发送候选经确认帧独立几何重绑定后只执行 1 次 `tap_semantic`。动作后输入框为空，新绿色气泡
  正文完全匹配，before/after fingerprint 为 `19a38b8280ce86c41bcb → 5fcf7f2c9b7eedde02d8`，outcome=`matched`、
  revision `3→4`、最终 `status=succeeded`、`active_subgoal=null`、总物理动作 1；未使用延迟刷新、未重发。
- **长文本续验停止点**：跨 App 准备 session `dceb5a0260304ac09fec398a29530797` 真实完成 Home、打开微信和
  `chinese_pinyin→direct_latin` 三个 matched 动作，随后首个输入动作在确认前因 planned/fresh 输入区域无交集而
  0 新动作停止。后续全新 0 动作会话的可信 scene 只证明唯一空 input 和 QWERTY，但没有 `focused=true`，模式又
  报告为 `chinese_pinyin`；因此本地拒绝把 `navigate_to_chat→focus_input` 两个节点同时完成是正确行为。曾根据
  scene summary 中“已激活”误判为结构化 focus 事实并实现的补证据提交 `8efadbe` 已撤回，不保留与真实根因无关的
  生产改动。当前没有输入 39 字符、没有清空重输、没有发送；长文本、多字段和多目标仍未完成正式真机闭环。
- **第十三批验收缺口与根因证据**：当前只关闭“稳定页面中的长文本首个输入动作”这一项。失败 session
  `dceb5a0260304ac09fec398a29530797` 的计划场景是唯一高置信、已聚焦、空值、`direct_latin` QWERTY 输入框；
  确认前新四帧与计划四帧的本地灰度差异仅 `0.246`，远低于页面变化阈值 `6.00`，两组持久帧肉眼也为同一微信
  会话、同一空输入框和同一键盘。机械动作仍为 0；唯一失败是两次独立视觉审计给出的 input bounds 无交集，触发
  `iou=0`。根因分类为“把输入框模型边框当成实际逐键动作坐标”，不是页面移动、键盘变化或机械坐标变换。逐键
  动作实际只使用确认帧中新签发的 `keyboard_geometry`，不使用输入框中心作为落点。
- **第十三批同类失败样本集合**：正例为任意 App 中像素稳定、唯一输入语义/标签/角色/焦点/当前值/键盘布局/
  输入模式/大小写均不变，但模型把同一输入框重画为不相交边框；动作必须继续使用 fresh 输入结构审计的键盘几何。
  变化正例覆盖短文本与 39 字符长文本。反例包括本地帧差异超阈值、输入框不唯一、失焦、当前值变化、布局/模式/
  大小写变化、缺少本轮键盘几何或本地 QWERTY 行复核失败，均保持 0 动作。`tap_semantic`、长按和拖动仍以控件
  几何作为真实执行坐标，原有 planned/fresh 几何漂移反例必须继续拒绝。
- **第十三批通用修复说明**：仅当 adapter 已用原始计划帧与确认帧证明本地画面身份不变，且动作种类为
  `input_verified_text` 时，最终语义重绑定不再要求“输入框模型边框重叠”。唯一输入、正式 meaning、标签、角色、
  焦点、精确 prior value、键盘布局/模式/大小写仍须逐项一致；确认帧仍须重新观察，输入结构仍须在确认帧签发
  `keyboard_geometry` 并通过本地 QWERTY 行复核。实际逐键坐标只取 fresh keyboard geometry。无计划帧、页面像素
  变化、清空、点击、长按和拖动均不走此例外。回滚只需恢复最终 rebind 的 geometry 参数，不影响模型协议或控制器。
- **第十三批验证清单与停止条件**：先增加正例“相同四帧、同一唯一输入、input bounds 完全漂移仍用 fresh 键盘
  几何输入一次”，反例“同样边框漂移但 fresh 输入模式/值变化时 0 动作”，并保留点击几何漂移 0 动作测试。运行
  adapter 相关测试、历史长文本/输入合同回归和一次 Python 完整回归，静态编译及 `git diff --check` 全部通过后本地
  提交。随后只重载项目 Uvicorn，不触碰卖家 `main.exe`；重新核对设备、控制器、相机、busy、活动 session 与新鲜
  observation 后建立一个全新 session，只输入未发送的长文本。必须恰好一次输入动作、matched、新四帧和新
  fingerprint，最终输入框逐字等于目标；任一失败立即停止，不自动重试、不发送。
- **第十三批离线证据**：新增稳定帧 input bounds 完全漂移正例、输入模式变化反例，并重跑既有点击几何漂移反例，
  三项 `3/3`；adapter 文件 `101/101`；文字事务、lineage、观察器、编排器和 mock loop 相关回归 `439/439`；Python
  完整回归 `1589/1589`。两个修改文件静态编译和 `git diff --check` 均通过。正例实际调用一次 fresh QWERTY 几何，
  只输入确定性首段 `longinput`；反例与点击漂移均为 0 动作。尚未提交、重载服务或进行本轮真机动作。
- **第十三批首次加载与 0 动作阻塞**：修复提交 `1b0ae88`，只重载项目 Uvicorn 至 PID `25244`；卖家
  `main.exe` 保持 PID `18236` 和原启动时间。独立 0 动作观察确认文件传输助手中唯一空输入框为
  `focused=true/direct_latin/lower`，但随后全新 session `71d4597895a04e03af37d46d7f3cc8d0` 的另一组新四帧只把
  输入框结构化为“空值 + QWERTY + chinese_pinyin”，没有 `focused=true`。DeepSeek 合理完成当前“会话可见”节点，
  却又仅凭 summary 的“已激活”文字把直接后继“输入框聚焦”同时标为 completed；本地结构化 focus 门正确拒绝，
  会话在首个机械动作前 `failed/0 actions`。这不是输入几何修复失败，也不得通过重复模型采样或把 summary 当焦点事实。
- **第十四批验收台账与同类样本**：当前缺口是“模型一次返回的安全可见前缀中，前一节点有本地权威证据、后一
  节点只有 summary 推断时，不应让整个 0 动作会话失败”。现场样本是页面可见→输入聚焦；历史变化样本包括列表
  可见→第二项可见，以及页面可见→结构化 focused=true，两节点各有本地证据时应继续允许同 revision 一起完成。
  反例包括当前节点本身无权威证据、后继为 external_state/unknown、依赖不满足、非直接连续节点、模型增加/删除/
  重排节点或改写目标/条件，均不得被本地收窄掩盖。
- **第十四批通用修复说明**：DeepSeek revision 先按现有严格 schema、证据来源和图身份完成验证；随后本地逐节点
  复核可见完成前缀。若且仅若当前节点已经以权威可见证据成立、紧邻的第一个 pending 后继依赖已满足但缺少其专用
  本地状态事实，则对该 revision 做唯一的“权限收窄投影”：保留已证明的 completed 前缀，把第一个未证明后继恢复为
  active，后续节点恢复原 pending/skipped 状态，清除未采纳证据并将图保持 ready。它不新增完成事实、不生成动作
  authority、不改变目标/风险/完成条件；外部状态、未知影响和非连续图仍失败关闭。下一轮必须用新观察为新 active
  节点取专用结构化事实或 Qwen 动作，不能复用 summary。
- **第十四批验证清单与停止条件**：新增现场同形正例（summary 声称聚焦但 states 无 focused）必须只完成页面节点、
  激活 focus 节点、0 Qwen/0 robot；保留双节点均有 structured evidence 时一起完成；增加外部影响后继和当前节点
  无证据反例继续抛错。运行编排器相关回归和一次 Python 完整回归，静态编译与差异检查通过后本地提交，再只重载
  项目服务。重新读取同一手机状态，只允许一个全新 session；若 focus 仍无结构化事实，应先获得新的只读观察或唯一
  focus 动作，之后才进入长文本分段。任一 0 动作合同失败立即停止，不换措辞重试。
- **第十四批离线证据**：现场同形安全收窄、structured focus 双节点完成、external_state 后继禁止收窄三项 `3/3`；
  编排器 `195/195`；DeepSeek 任务图、mock loop 与 adapter 相关回归 `338/338`；Python 完整回归 `1591/1591`。
  新正例最终只保留 `chat-visible=completed`、`focus-input=active`、后续 pending，Qwen/robot 均 0 次；原 structured
  focus 正例仍完成两节点；external_state 返回 `None` 并保持原失败关闭。尚未提交、重载或新建第二个真机会话。
- **第十四批提交与真实停止点**：提交 `2df3bc8` 加载后，全新 session
  `da4597b498494faf807163549790121a` 成功完成第 1 个确定性分段：空值→`longinput`，1 动作 matched，fingerprint
  `e79cb9ec671747201e3d → 78de1e3dff67cbdb46bb`。第 2 动作目标是下一字符 `2`，但输入结构审计把英文 QWERTY
  的 W 键上方小号数字提示“2”误签为可直接点击键；审计初始 bounds 中心与 q-p 字母行几乎同高，独立几何后落点
  `[0.174,0.686]`，机械点击实际得到 `longinputw` 而非 `longinput2`。动作后两轮均 mismatched，会话按合同暂停，
  总动作2；没有继续、重试、发送或提交。根因是键帽主字符与长按/次要提示字符未区分，不是坐标变换或本批 input
  bounds 修复。
- **第十五批验收台账与同类样本**：当前缺口是“非字母逐键字符只能绑定直接点击就会插入该字符的完整独立键，
  不能绑定字母键上的小号长按提示”。现场样本为 W/2；变化反例覆盖 QWERTY 字母键上的数字、标点或其他次要
  角标。正例包括 numeric/symbol 布局中的主字符键、QWERTY 中独立空格/标点键，以及确有独立数字行且键中心与
  字母行分离的完整数字键。布局切换键应在目标字符当前不可直接点击时成为唯一安全后继。
- **第十五批通用修复说明**：输入结构 prompt 明确要求 literal key 的主/中央大号 glyph 必须逐字等于 value，任何
  小号角标、上滑字符、长按提示、候选文字均不得报告为 direct literal key；若目标字符只以提示存在，则应报告
  方向明确的 numeric/symbol layout switch。除此之外，本地使用同一帧已验证 q/p、a/l、z/m anchors 重建三条字母
  键中心栅格；QWERTY 下任何 literal candidate 若中心落入任一字母键单元，确定性丢弃，模型高置信也不能授权。
  独立数字行与 numeric/symbol 键盘不在该排除区域。该规则只撤销可疑键，不补造布局切换或动作权限。
- **第十五批验证清单与停止条件**：用现场 anchors 与 `[170,690,250,750]` 构造 W/2 反例，断言不生成 literal
  action；同画面存在明确 `123→numeric` 时只生成 layout switch；numeric 布局数字2、QWERTY 空格和分离的独立数字
  行保持正例。补 prompt 断言并保留白名单、唯一键、独立几何和动作后 exact value 回归。完成观察器、adapter、编排器
  相关与一次完整回归后本地提交、只重载项目服务。当前错误草稿必须先用现有 verified clear 能力在全新会话清空并
  逐字确认，再重建长文本会话；不得从 `longinputw` 猜测恢复，也不得在旧 awaiting session 上确认下一动作。
- **第十五批离线证据**：prompt 主字符/次要提示合同、现场 W/2 字母单元反例、`123→numeric` 唯一后继、独立数字
  行与 numeric 数字键正例均通过；白名单检查被固定在提示过滤之前，白名单外字符仍严格报错。观察器+adapter
  `323/323`、编排器+mock `201/201`、Python 完整回归 `1592/1592`。本地栅格仅撤销与 qwerty 字母中心重叠的
  literal 候选，不改变 numeric/symbol、空格、独立数字行或布局切换权限。尚未提交、重载或清理真机错误草稿。
- **第十六批验收缺口与根因证据**：`20f746d` 加载后的四帧只读观察再次明确看到当前输入框正文
  `longinputw`，但最终结构化 input value 被第二个 input-structure 模型调用写成空值，且退格键未形成可信元素；
  直接启动清空会被误判为已经为空。持久状态文件仍停在更早的逐键值
  `复杂输入验收2026:123+45-6@7.`，没有记录本轮已 matched 的 `input_verified_text` 交易
  `""→longinput`。代码确认 lineage 只接受 `tap_semantic/input_exact_literal_key` 和 GUI 点击 receipt，整段
  `input_verified_text` 即使经动作后 exact verifier、四帧、新 fingerprint 全部通过也不会写记录。根因是类型化输入
  连续性覆盖的动作集合不完整，不是当前 App、字符串或退格坐标。
- **第十六批同类样本集合**：正例覆盖 direct_latin 任意非空分段，均要求 before 唯一聚焦 input
  精确等于 prior、expected 精确等于 prior+input_fragment、resolved expected_effect 同值、一次物理事务调用正常返回、
  动作后唯一聚焦 input/evidence 精确等于 expected、前后 fingerprint 不同且有四帧局部描述。变化正例覆盖跨服务
  读取视觉软折行。反例覆盖 mismatch、0/2 动作、expected 链错误、错误/缺失动作后值或 evidence、同 fingerprint、
  四帧缺失、错设备/表面/TTL、clear、拼音预编辑或普通 tap 冒充已提交文字分段；全部不得写 lineage。中文值仍由
  原有“拼音预编辑后点击唯一逐字候选”的 literal receipt 持久化；成功清空则只撤销该设备旧 lineage，不能写空值记录。
- **第十六批通用修复说明**：沿用 `TypedInputLineage` v2，不新增 App/命令分支。为已执行返回的
  `input_verified_text` 建立一次性 pending exact lineage，只能帮助紧接着的同帧软折行核对；controller 原 exact
  verifier 成功后，再以 resolved action、before/after exact scene、四帧、fingerprint 和本地 verified-transaction
  receipt digest 原子持久化。记录仍只消歧视觉折行，不生成动作权限、不跳过四帧/Qwen/几何/策略。任何 verification
  error 不写记录；`clear_verified_text` matched 后删除旧记录，避免清空后继承非空值。
- **第十六批验证清单与停止条件**：补 direct_latin pending 与持久化正例、拼音预编辑拒绝、错误链/错误动作/缺帧反例、
  adapter 仅 matched 后写入及 clear 后撤销测试；保留原 literal-key receipt 全部测试。运行 lineage、observer、adapter
  最小回归，再运行相关编排器与一次完整回归、静态编译和差异检查。本地提交并只重载项目 Uvicorn 后，不从当前
  `longinputw` 猜测恢复；改用全新空白安全输入表面完成长文本，再在下一全新会话用刚形成的 exact lineage 完成清空。
  任一物理动作 mismatch 立即停止，不自动重试、不触碰发送/提交。
- **第十六批离线证据**：lineage 专项新增 direct_latin pending/持久化、错误 chain、缺四帧、拼音预编辑拒绝和
  单设备撤销正反例；adapter 实测 matched 后写 `verified_live_text_action`、mismatched 不写、clear matched 后删除。
  lineage+observer+adapter+文字事务 `347/347`、编排器+mock loop `201/201`、Python 完整回归 `1597/1597`；
  完整回归仅出现历史子进程清理 ResourceWarning，无失败断言。尚未提交、重载或执行新的物理动作。
- **第十七批验收缺口与根因证据**：设置导航 session `4c056b185a1c43969730d575744ea295`
  已 matched 完成 Home 和打开设置两个动作，另一个全新 session `373e5a5be8f5435f974528f99f91c07d`
  对顶部唯一搜索框执行 1 次 tap 后，真实画面已出现搜索记录、取消按钮与完整软键盘，但动作后 targeted delta 因一个
  越出 0..1000 的 goal-relevant 键盘容器关闭；随后独立 0 动作观察再次因一个越界且明确
  `goal_relevant=false` 的键盘切换提示关闭。两次 raw response 均完整、无截断、顶层四字段正确、其余元素可做原有
  evidence/xywh 唯一规范化。当前唯一缺口是 targeted delta 在 schema 前没有复用正式 scene 已有的“只丢弃明确
  非目标且越界的外围元素”规则；不是搜索框落点、机械坐标或 App 专用问题。
- **第十七批同类样本与通用修复**：变化样本覆盖任何 App 的目标元素旁同时出现一个明确
  `goal_relevant=false`、非 input、非目标逐字控件且 bounds 越界的模型外围项；应只删除该项后继续严格 schema。
  目标元素越界、input 越界、没有明确 false、目标逐字同名或包含动作字段的项必须继续使整个 delta 失败。实现只在
  targeted evidence/xywh 有界规范化后、严格 schema 前调用既有 `_drop_out_of_range_non_goal_elements`；它不能改写
  bounds、不能增加元素或权限。现场第一次 goal-relevant 越界键盘仍必须失败，第二次 non-goal 越界提示应唯一修复。
- **第十七批验证与停止条件**：用两份现场同形 payload 做一正一反，保留 legacy authority、重复键、>12元素和所有
  无效目标 bounds 反例；运行 targeted observer 最小测试、observer+adapter 相关回归和一次完整回归。本地提交并只
  重载项目 Uvicorn 后仅做一次独立 0 动作观察；必须确认搜索输入为空、已聚焦且键盘可见。若同类格式再次失败，
  停止，不再采样或点击；成功后才在全新 session 开始长文本。
- **第十七批离线证据**：两份现场 raw response 原样重放：包含 goal-relevant 越界键盘容器的动作后响应仍失败；
  仅含 non-goal 越界切换提示的独立观察响应通过并只保留 `search_input_clipped` 与 `btn_cancel`。专项正反
  `4/4`、observer+adapter `325/325`、Python 完整回归 `1598/1598`；完整回归仍只有历史子进程清理
  ResourceWarning。尚未提交、重载或追加物理动作。
- **第十八批验收缺口与根因证据**：当前只关闭“输入目标的 compact 初步观察不得与后续专门输入结构审计争夺
  键盘整体容器的类型/状态权威”。全新 0 动作会话目录
  `poc/output/web/generic_supervised_20260820_003804_a9b3b1f6` 的两次 compact 原始响应均包含唯一真实输入框，
  同时把被动软键盘整体报告为 `role=container`、`meaning=soft_keyboard_area`，并在该容器 states 中放入仅允许
  正式输入元素携带的 `keyboard_layout`/`keyboard_input_mode`。严格 scene schema 因此连续两次在任何机械动作前
  失败；两次 `physical_actions=0`。后续独立 input-structure audit 本来才是输入框、键盘模式、布局、逐键几何的
  唯一权威，compact 被动容器没有可执行语义，也不应提前阻断该审计。根因归类为两个通用观察阶段的职责重叠，
  不是微信、当前草稿、某个控件或机械坐标问题。
- **第十八批同类失败样本集合**：正例覆盖任意 App 的 active input goal：compact 元素必须使用标准八字段、
  `role=container`、meaning 明确为 keyboard/soft_keyboard_area，states 含 keyboard_layout 或
  keyboard_input_mode，bounds/states/evidence 结构合法且整个元素无 action/plan/tap/swipe/command/coordinates
  等动作字段；该容器可被撤销，随后必须由同一新鲜帧的独立 input audit 重建正式输入和键盘事实。反例包括
  role=input、非键盘 container、缺少键盘状态、非标准字段、非法 bounds/states/evidence，以及任意层级包含动作字段；
  这些项目必须保留给原严格 parser 失败关闭，不能被清洗。
- **第十八批通用修复说明**：在 compact JSON 提取完成、严格 scene parse 之前增加一个只撤销权威的本地隔离步骤，
  仅移除上述完整匹配的被动键盘容器；不修 bounds、不改 states、不增加元素、不创建动作权限。它和现有“active
  input goal 隔离 compact input geometry”共同将 `compact_input_geometry_isolated=true`，从而禁止 targeted
  refinement 抢先介入，并让后续 full-frame input-structure audit 成为唯一输入/键盘 authority。非输入目标和所有
  反例路径完全不变；回滚只需移除该纯预处理函数及一次调用。
- **第十八批验证清单与停止条件**：新增一项端到端正例，compact 输入框加被动键盘容器后必须仅调用一次独立
  input audit 并得到本地审计输入；增加动作字段、非键盘容器和 input-role 反例，确认仍由严格 schema 拒绝或保留。
  对现场两份 redacted raw compact response 做原样离线重放，确认只移除键盘整体而保留唯一输入值提示；运行
  observer 最小测试、输入/adapter/编排器相关回归和一次 Python 完整回归，静态编译及 `git diff --check` 通过后
  本地提交。随后只重载项目 Uvicorn，不触碰卖家 `main.exe`；建立一个全新会话先收起键盘并导航到新的空白安全
  输入表面，再执行未发送长文本和下一全新会话清空。若同类模型合同失败连续两次，立即停止，不采样第三次、不
  追加 App 或页面特例。
- **第十八批离线证据**：两份现场 `redacted_raw_response` 已原样重放；每份均先提取精确值提示
  `longinputw`，再只撤销 compact input geometry 与被动 keyboard container，严格 scene parse 后仍保留普通非目标
  发送按钮，未补造任何输入/键盘元素。端到端输入审计正例与动作字段、input role、非键盘、缺状态、越界 bounds
  反例 `2/2`；observer、lineage、adapter/step planner 相关回归 `341/341`；Python 完整回归 `1599/1599`。
  两个 Python 文件静态编译和 `git diff --check` 通过；完整回归仅出现既有子进程清理 ResourceWarning，无失败断言。
  尚未提交、重载服务或执行新的物理动作。
- **第十八批提交与在线结果**：通用修复提交 `abc21db`，项目 Uvicorn 已单独重载为 PID `11984`；卖家
  `main.exe` 保持 PID `18236` 和原启动时间。首次带本地控制令牌的全新会话
  `0f5bcb15e4b84c02a501a33fe313ed53` 在 DeepSeek 初始任务图阶段 0 动作失败；新 compact 观察尚未发生，因而没有
  重现被动键盘容器冲突。新的唯一阻塞是模型把用户明确禁止的“发送消息”风险同时关联到“进入普通会话”和“空输入框
  已聚焦”两个 navigation_only 子目标，本地 transport 一致性校验在正式类型化风险投影前拒绝。
- **第十九批验收缺口与根因证据**：当前只关闭“模型从否定约束虚构的旧 risk_action 不得在正式本地风险引擎之前
  阻断纯 read_only/navigation_only 子目标”。现场原始图中 `risk_send_message` 的唯一正向效果是发送消息，用户目标和
  相关约束均明确“不得发送”；被关联的两个子目标正向结果分别只是进入本机临时会话页面、确认空输入框聚焦与软键盘
  可见，均不包含未否定的发送/发布/登录/付款/数据变更结果。现有 `_subgoal_is_safe_without_forbidden_risk` 除了检查
  “没有未否定外部效果”，还要求命中特定 transient-navigation 文本正则；“输入框已聚焦”没有命中该枚举，因此本可由
  现有 `_purely_forbidden_initial_risks` 唯一删除的虚构风险被保留并提前关闭。根因是旧 transport 清理仍依赖页面状态
  词枚举，不是本地类型化风险策略或用户目标真的要求发送。
- **第十九批同类失败样本集合**：正例覆盖任意 App 的 read_only/navigation_only 子目标，只要所有关联风险动作锚点
  都被目标/约束直接否定、该风险不被任何正向目标或完成条件要求，且子目标 objective/completion_conditions 不含任何
  未否定外部效果，就允许在正式 typed projection 前撤销该旧 risk 引用；变化样本包括打开普通页面、聚焦空字段、显示
  键盘、收起弹层、切换本机标签。反例包括“不要忘记登录”“发送后再查看”“付款完成”“数据已删除”、只有泛化的“注意
  安全”而没有直接否定、unknown risk 无动作锚点，或任一全局/子目标正向完成条件需要该风险效果；全部必须保留并由
  正式本地策略确认/阻塞。
- **第十九批通用修复说明**：不删除风险协议、不放宽 graph.validate，也不让模型决定安全。只收窄
  `_subgoal_is_safe_without_forbidden_risk` 的旧迁移判定：read_only 保留原有只读风险控制状态合同；其余 transport
  影响枚举可能正是由同一虚构风险派生，因此不把该枚举单独当作权威，也不再要求命中有限页面词正则，而是要求其
  正向 objective+completion_conditions 中不存在任何本地外部风险模式的未否定命中。它仍只是
  `_purely_forbidden_initial_risks` 的一个必要条件；动作锚点逐项直接否定、全图无正向需求、
  引用对称和后续 TaskSemanticIR+版本化本地策略全部继续执行。共享同一风险的多个子目标还必须处于同一个无向依赖
  连通分量；两条不相干任务链不能借同一否定约束相互消除风险。该修改跨 App、跨页面措辞，只撤销模型虚构的禁做
  风险，不生成动作权限。
- **第十九批验证清单与停止条件**：用现场同形的“进入普通会话+聚焦空字段”共享 send risk 做正例，断言风险和两处引用
  被移除并保留 navigation_only；增加正向发送结果、正向登录/付款以及未直接否定反例继续失败关闭。运行 DeepSeek
  风险/语义最小测试、编排器相关回归和一次 Python 完整回归，静态编译、差异检查通过后本地提交并只重载 Uvicorn。
  之后只允许一个全新会话继续原准备目标；若同类初始任务图错误再次出现，立即停止，不换措辞采样。
- **第十九批离线证据**：现场 `deepseek_initial_task_graph_step_1_deepseek_failure.json` 中完整
  `redacted_raw_response` 已原样重放为 `ready`，原五个子目标和 active frontier 保持不变，两个纯否定虚构风险及全部
  引用被撤销；没有新增确认或动作权限。现场同形共享 send risk、既有纯否定草稿与只读“发送按钮未触发”正例通过；
  正向发送结果、付款确认、没有直接禁止和断裂依赖链反例继续失败关闭。DeepSeek、TaskSemanticIR 与编排器相关回归
  `451/451`，Python 完整回归 `1600/1600`；两个 Python 文件静态编译和 `git diff --check` 通过。完整回归只出现既有
  子进程清理 ResourceWarning，无失败断言。尚未提交、重载或追加物理动作。
- **第十九批提交与在线结果**：提交 `58cb6ff` 单独重载项目服务至 PID `22460` 后，全新 session
  `56b1e50dc54f4d148c198b4c26220e08` 已成功生成 `ready/5 subgoals/0 risks` 的初始正式任务图，证明纯否定风险阻塞
  已关闭。随后第一组四帧 targeted refinement 在 0 物理动作时失败，原因是元素 bounds 使用完整单词
  `{x,y,width,height}`，而现有唯一有界 shorthand 只接受 `{x,y,w,h}`；同一响应的 evidence 字符串已有正式唯一规范化，
  顶层四字段、协议版本、元素八字段、数值和目标语义均完整。没有重试模型或操作手机。
- **第二十批验收缺口与根因证据**：当前只关闭“targeted delta 对两种严格等价的矩形宽高键名应执行唯一、本地、
  无语义变化的坐标规范化”。现场原始响应只把既有 `{x,y,w,h}` 中的 `w/h` 写成 `width/height`；四个值均为有限数字，
  且 `x+width/y+height` 落在 0..1000。当前 parser 因键名集合不相等而把整个最小增量误报为顶层协议错误。根因是
  JSON 几何 shorthand 的同义结构未完整覆盖，不是输入语义、App 页面或低置信问题。
- **第二十批同类样本与通用修复**：正例覆盖任意角色和 App 的 targeted element，bounds 必须严格等于
  `{x,y,width,height}`，四值为非布尔有限数、width/height>0、结果完整落在 0..1000；本地唯一改写为
  `[x,y,x+width,y+height]`。原 `{x,y,w,h}` 继续支持。混用 `w+height`、额外字段、缺字段、字符串/布尔/NaN、非正宽高、
  越界结果全部不改，随后由现有最小 delta schema 失败关闭。不得推断 left/right、像素缩放或裁剪坐标。
- **第二十批验证清单与停止条件**：新增 width/height 正例及混合键、额外键、无效数值反例；用现场原始 targeted
  response 原样重放，确认只发生 evidence 单字符串数组化和 bounds 唯一转换，随后严格 delta parse 成功。运行 observer
  最小与相关回归、一次 Python 完整回归、静态编译和差异检查；通过后本地提交并只重载 Uvicorn。之后只建立一个
  全新会话；若 targeted 合同同类失败再次出现，立即停止，不采样第三次。
- **第二十批离线证据**：现场 `before_step_1_frame_attempt_1_qwen_failure.json` 的完整 targeted raw response 已原样
  重放；唯一转换为 bounds `{x:130,y:540,width:580,height:50}` → `[130,540,710,590]` 和 evidence 字符串→数组，
  严格 delta parse 后得到 normalized bounds `(0.13,0.54,0.71,0.59)`，value 仍逐字 `longinputw`。新正例和混合键、
  额外键、字符串、布尔、非正宽高、越界反例通过；observer `224/224`，Python 完整回归 `1600/1600`，静态编译和
  `git diff --check` 通过。完整回归仅出现既有子进程清理 ResourceWarning，无失败断言。尚未提交、重载或追加动作。
- **第二十批提交与在线停止点**：提交 `c509365` 单独重载项目服务至 PID `22232` 后，同一验收目标的全新初始图再次
  在 0 动作时出现“纯否定风险先于正式本地投影阻塞”，session 目录
  `generic_supervised_20260820_010155_2936985b`。新变体把 `risk_alter_draft` 关联到 read_only 的“原草稿保持不变”
  节点；约束逐字禁止输入、发送、删除、清空，但没有复述模型风险描述中的“修改”一词，旧迁移层因要求每个自由文本
  action anchor 都被同词直接否定而未删除风险。另一个 send risk 的子目标约束只描述应进入的安全状态，也没有重复全局
  “不得发送”，同样暴露 transport 风险清理仍依赖模型如何分配/复述约束。controller/camera 在线、busy=false、0 活动
  任务/会话，卖家 PID/启动时间未变，手机没有新增物理动作。
- **止损后的架构结论**：第十九批已处理的原始图可通过，但同一目标的等价第二图仍失败，说明继续增加“保持不变≈不修改”
  或把全局禁止复制到每个子目标只会形成措辞补丁，必须停止。下一批应把 legacy `risk_actions/risk_action_ids` 明确降为
  非权威 transport：在 `Subgoal.validate` 的风险/impact 对称校验前，先按全图正向 objective/completion semantics 编译
  本地 typed effect；只有正向结果确实包含 effect，或 risk_type=unknown 且无法归类时才保留旧 risk 供正式本地策略裁决；
  纯由禁止/保持不变/未触发等非正向结果产生的 risk 及引用统一撤销。登录、付款、权限和删除等只要是正向结果仍保留；
  不再要求用户或模型逐子目标重复风险动词。需要以本轮两份原始图、正向发送/登录/付款/删除和 unknown 反例形成新的
  架构批次，再实施一次，而不是继续扩充自由文本同义词。

## Typed v4 长文本真机续验：功能性页面描述误作专名

- **当前唯一验收缺口与必须证据**：从新的空白安全输入表面完整输入超过 30 字符的混合长文本，逐段获得 fresh 四帧、
  observation/fingerprint 和 matched receipt，最终唯一输入框值逐字等于 canonical 文本且不选择结果、不提交、不发送、不保存。
- **现场事实与根因分类**：全新 session `9d18a3c9589d45dc9e9cbb8a76dc4de3` 从 Launcher 只执行一次打开“设置”的
  `tap_semantic`；动作 matched、前后各四帧、receipt `receipt_3f7b8e4457fb48afbf9b21e2a4e11b2b`，新画面明确为
  `system_settings/settings_main`，并含唯一空白 `local_audited_input_1`（label=`搜索系统设置项`）。随后在 0 新动作处以
  `命名页面完成声明缺少结构化画面身份锚点：subgoals.navigate_to_search` 阻塞。触发文本是“可输入搜索内容的页面可见”；
  当前 `_named_visual_identity_anchor()` 把功能性关系从句“可输入搜索内容的”当作页面专名。主要类别为通用代码缺陷，
  不是模型随机、App 路径、视觉坐标或机械臂失败；旧 session 不续跑、不改写、不换措辞重试。
- **同类失败样本集合**：现场正例为“可输入搜索内容的页面可见”；变化正例包括“用于填写订单编号的界面可见”、
  “能够编辑草稿的视图出现”，这些描述容器能力/用途，不是稳定名称。反例保留“系统设置搜索页面可见”、
  “订单详情页面可见”和“张三的聊天页面可见”，它们仍必须由结构化 App/screen/title/recipient 事实锚定；逐字引号标题门禁保持不变。
- **通用修复说明**：只在页面身份 anchor 提取阶段识别位于 `页面/界面/屏幕/视图/面板/卡片` 之前、以“的”收尾且
  含可/能/用于/供/允许/支持/包含/显示/存在等功能谓词的关系从句，并将其判为无专名容器描述。它只取消不适用的
  “专名一致性”门，不新增完成证据；当前 visual claim、唯一输入、typed surface、依赖、revision、effect policy 和动作 authority
  仍由原合同逐项验证。真实专名、收件人聊天页和引号逐字标题继续失败关闭。回滚只需撤销该 anchor 分类与对应测试。
- **验证清单与停止条件**：先补上述现场正例、两种换措辞正例及三个真实专名反例；运行 typed v4 transport 与编排器最小回归，
  再运行一次完整 Python 回归、静态编译和 `git diff --check`。通过后本地提交，只重载项目 Uvicorn，并从当前设置主页面建立
  一个全新 session；不得复用 `9d18...`。若同类页面身份错误再次出现，或真实专名反例被放宽，立即停止，不继续真机动作。

## Typed v4 长文本真机续验：方向证据被卖家调试浮层污染

- **当前唯一验收缺口与必须证据**：保持上一节的长文本输入闭环不变；本批只需证明卖家画面边缘的 `PX/MM` 调试文字
  不再使整张动作后场景失效，同时该文字绝不能成为方向、坐标或动作 authority。
- **现场事实与根因分类**：新 session `f900395175b0451b85aad4ac18365f9d` 已通过上一批页面身份门，机械臂只执行一次
  聚焦搜索框动作；真实后帧显示设置搜索记录页和软键盘。第2轮 compact 响应把左上角卖家调试浮层分别写成
  `camera_alignment.evidence=["PX: (264, 151)", "MM: (44.8, 44.4)"]`，解析器以“包含坐标或控制指令”拒绝整张场景。
  主要类别为通用代码缺陷：现有 `_drop_forbidden_camera_alignment_evidence()` 仅在至少还剩一条安全方向证据时删除污染项，
  两条全污染时没有降为未知的路径。动作链已经停止，不重用该 session，不再操作手机。
- **同类失败样本集合**：现场设置搜索页为“全部方向证据均被 PX/MM 污染”；历史真实 session
  `f2125adc5a8f4999ac78bfc56038cd9b` 在新闻资讯类 App 也记录同一错误，证明与 App 和目标无关。变化正例为“一条 PX/MM +
  一条手机文字正立”的混合证据，继续只保留安全项。反例包括布局方向与本地帧尺寸冲突、非法枚举、非数值 confidence、
  缺少 camera_alignment 或非字符串 evidence，仍必须失败关闭。
- **通用修复说明**：只对字段形状完全合法且 evidence 全为字符串的 payload 做确定性降级。若存在安全证据，沿用现状只删除污染项；
  若全部证据均为坐标/控制文字，则删除全部证据，并把 `phone_content_rotation` 设为 `unknown`、confidence 设为 `0.0`。
  `camera_layout_orientation` 仍必须与本地稳定帧尺寸一致。该场景不能产生方向 authority；后续任何物理动作仍需独立方向审计。
  不接受、改写或利用 PX/MM 数值，不放宽元素坐标、目标几何、typed 后置状态或动作策略。
- **验证清单与停止条件**：重放当前两条全污染证据和混合污染证据；验证降级值精确为 unknown/0/空数组；保留本地布局冲突、
  非法枚举、非法 confidence、缺字段和非字符串 evidence 反例；运行 observer 相关回归与一次完整回归。离线全绿后本地提交并只重载
  Uvicorn。真机只允许一个全新 session；若聚焦后仍不能建立唯一可信输入目标，立即停止，不以方向降级冒充 focused 成功。

## Typed v4 长文本真机续验：更换完整可见的安全输入载体

- **当前唯一验收缺口与必须证据**：仍只验证同一个能力——从空白应用输入框分段写入超过单步 30 字符的 canonical 长文本，
  最终逐字相等且保持未发送；不新增代码能力。
- **已观察事实与根因分类**：方向降级修复加载后，对设置搜索页的 0 动作观察成功，页面与键盘均可信，但应用输入框本体被卖家画面
  上沿固定遮挡；input structure audit 没有签发任何 input element。此处主要类别为验收设计错误/摄像头可见性限制，而不是继续放宽
  typed input authority 的理由。设置载体停止使用，不在未知焦点上输入。
- **验收载体与通用性**：改用文件传输助手的空白消息输入框，只作为真实 App 样本；任务编排仍由 DeepSeek/Qwen 和通用
  `home → tap_semantic → input_verified_text` 组合动态产生，生产代码不得出现微信名称、固定步骤或固定坐标分支。目标明确禁止发送，
  因此本轮没有账号外部效果。
- **必须证据与停止条件**：从当前页面开始建立一个全新 session；每步必须有 fresh 四帧、新 observation/fingerprint、单动作 matched
  receipt。最终唯一输入值必须精确等于 `longinputvalidation2026:123+45-6@7.`，session 为 succeeded、active_subgoal 为空、发送按钮未触发。
  任一动作不匹配、出现未知输入目标、外部 effect 或模型合同失败时立即停止，不自动重试，也不换第三个 App。

## Typed v4 长文本真机续验：几何审计安全证据数量上限

- **当前唯一验收缺口与必须证据**：长文本闭环不变；本批只修复第三步执行前、0 物理动作的几何审计格式阻塞。
- **现场事实与根因分类**：session `8323f306bcbe404aa07e92bc4ef0db30` 已获得 Home 与打开微信两个 matched receipt，
  当前稳定停在微信消息列表。点击“文件传输助手”前的独立 crop 审计返回唯一同名 `list_item`、confidence=1、完整可见、whole control，
  但 evidence 为 3 条安全视觉事实；解析器把任意的“最多2条”当成硬安全边界而拒绝，第三步 physical_actions=0。主要类别为通用模型合同
  容量缺陷，不是目标歧义、坐标失败或外部效果风险。
- **同类失败样本集合**：现场为 3 条安全证据；构造变化样本为 1、2、4 条安全证据。反例为 0 条、5 条、任一条含坐标/点击等
  控制信息、单条超长、非字符串、重复/额外字段，继续失败关闭。
- **通用修复说明**：把正式 `element_geometry_audit` evidence 容量从 1–2 调整为 1–4，并在 prompt 明写边界；每条仍逐项执行
  200 字符上限和控制/坐标词拒绝。唯一 match、逐字 label、role、source_ref、置信度、fully_visible、whole_control、crop-local bounds、
  内部边缘和本地映射门禁完全不变。证据数量不直接授权动作，只补充已经通过几何门禁的可见理由。
- **验证清单与停止条件**：用保存的 3 条现场响应离线重放；补 1/2/4 正例及 0/5/污染/非字符串反例；运行几何模块、观察器、
  编排器相关回归和一次完整回归。全绿后本地提交、只重载 Uvicorn，再从当前微信消息列表建立一个全新 session；若仍在同一步出现
  几何、语义或动作不匹配，立即停止，不重做已经 matched 的 Home/打开微信。

## Typed v4 长文本真机续验：初始命名 App 与通用前台类别的身份绑定

- **当前唯一验收缺口与必须证据**：长文本闭环不变；本批只修复从已经打开的命名 App 启动任务时，首个“App 主界面可见”子目标
  无法由 0 动作当前画面推进的问题。
- **现场事实与根因分类**：新 session `dea2beb6230e44799a93ce3e32d19357` 在 0 动作处 blocked。任务图首节点为
  `open_wechat`，完成条件“微信主界面可见”；可信场景结构化 `screen_id=wechat_main_chat_list`，且唯一“文件传输助手”条目可见，
  但 compact `foreground_app_id=messaging`。现有 `_needs_foreground_app_identity_audit()` 只在 unknown、占位符或 Android 包名不匹配时
  触发，通用类别 `messaging` 与命名目标 `wechat` 的不一致没有独立复核，导致已有 visible-presence 0 动作推进被前台身份门拒绝。
  Qwen 正确拒绝了破坏现状的 Home 候选，手机未被点击。主要类别为通用身份合同缺口。
- **同类失败样本集合**：现场 `messaging + wechat_main_chat_list → wechat`；构造变化样本为
  `system_utility + settings_main → settings` 和结构化 page title 逐字出现命名 App。反例为 `calculator/main → settings`（页面结构不含目标）、
  审计低置信/unknown、页面只在普通业务文本中提到目标名、目标与前台明确为不同 App，均不得重绑定。
- **通用修复说明**：保留现有 unknown/占位符/运行时包名审计；新增一个窄触发条件：前台类别与命名目标不一致，且 `screen_id` 或
  page/title/container 的结构化身份字段逐词包含目标 app_id/app_name 时，才调用目标无关的单图前台 App 身份审计。审计 prompt 不披露目标，
  优先返回画面中明确可见的真实产品/App 标识；无法确定则 unknown。只有审计返回值与任务图目标一致时，现有本地 presence/revision 门才能
  0 动作完成旧节点；screen_id 本身不直接签发动作 authority。
- **验证清单与停止条件**：补上述两种命名 App 正例、明确不同 App 与低置信反例、prompt 不泄露目标检查；运行 observer、task graph、
  orchestrator 最小回归和一次完整回归。全绿后本地提交、只重载 Uvicorn，再从当前微信列表建立一个全新 session。若首节点仍无法
  0 动作推进或审计返回不一致，立即停止，不通过别名表或 App 专用分支绕过。
- **离线验证结果（2026-08-20）**：新增正反用例 4/4；观察器与编排器相关回归 414/414；完整离线回归 1348/1348；
  `py_compile` 与 `git diff --check` 通过。真机结论仍以服务重载后的全新 session 为准。

## Typed v4 长文本真机续验：跨子目标字面实体角色去重

- **当前唯一验收缺口与必须证据**：同一新会话已经以一个 matched 动作进入文件传输助手；当前缺口是输入子目标应把收件人标题仅作为
  页面身份，把唯一空输入框作为动作目标，随后才能进入分段长文本事务。必须证明输入子目标不会继续要求输入框逐字标为收件人名称，
  同时仍要求页面存在唯一逐字收件人身份，且导航子目标继续严格绑定收件人入口。
- **现场事实与根因分类**：session `28e07c7d431a4dc7899cd8a4451c426b` 为 `blocked/1 action`；唯一物理动作
  `tap_semantic(文件传输助手)` matched，前后 fingerprint `0564642c24a5ba0d41f2→c5bc939638192c4b04ca`。当前可信场景为
  `wechat/chat_window_file_transfer_helper`，含唯一 `page_title=文件传输助手` 与唯一空 `role=input`。任务图已激活 `type_input`，但
  `QwenTaskContext.exact_text_requirements` 无条件先加入全局 `target_ui_label=文件传输助手`，即使同值 `recipient` 已被本地归类为
  `identity_text_requirements`，从而在模型调用前误报“当前可信候选中不存在逐字一致文字”。主要类别为通用 typed 实体角色投影缺口；
  不是 Qwen、坐标、机械臂或手机状态失败。
- **同类失败样本集合**：现场 `recipient==target_ui_label` 且当前为未标名输入框；变化样本为任意 App 的具名对象/页面标题与未标名输入框；
  多收件人各自输入子目标使用对应身份。反例为导航/选择子目标仍须逐字绑定收件人入口，以及与 recipient 不同的显式字段标签
  （如“备注”“用户名”）仍须保留为本轮逐字控件目标；身份缺失或重复仍必须 0 动作失败。
- **通用修复说明**：在 `QwenTaskContext` 的本地、只读任务投影中先计算当前子目标的身份文字；若 `target_ui_label` 与其中一个身份文字
  完全相同，则不再把同一字面量重复加入动作目标集合。它仍保留在 goal/typed IR 中，页面身份门继续逐字唯一校验；导航子目标因该
  收件人仍属于控件目标而继续进入 exact-text 集合。不同值的显式控件标签不受影响。不得删除逐字门禁、不得依赖 App 名称或坐标。
- **验证清单与停止条件**：重放现场 `type_input` 上下文和可信场景，证明 exact-text 不再误拦、identity 仍唯一、选择集合只提供输入框
  聚焦动作；补导航正例、身份缺失/重复反例、不同显式字段标签反例和多收件人变化样本；运行 Qwen、语义 IR、编排器相关回归和一次完整
  回归。离线全绿后本地提交；本批不继续真机串行试错，先完成整条输入链离线合同审计，再决定下一次唯一真机验收。
- **离线验证结果（2026-08-20）**：现场形状、导航变化、身份缺失、不同字段标签与多收件人正反样本通过；输入链相关回归
  357/357，完整离线回归 1349/1349，`py_compile` 与 `git diff --check` 通过。当前运行中的服务尚未加载本批修复，且按停止条件
  未再次操作真机；因此本条状态是“代码与完整回归通过，等待下一次唯一真机闭环”，不是长文本真机验收完成。

## Typed v4 长文本真机续验：输入事务后的采帧运动模糊停止点

- **加载与动作前证据**：提交 `7b83f4d` 已只重载到项目 Uvicorn PID `19832`；卖家 `main.exe` 始终保持 PID `2904` 和原启动时间。
  新鲜四帧只读预览为 `executed=false/physical_action_requested=false`，稳定识别
  `wechat/chat_session_file_transfer_helper`、唯一标题“文件传输助手”和唯一完整可见空输入框，fingerprint
  `d28a0fc26401907135e5`。controller/camera online、busy=false、0 活动会话。
- **唯一新会话结果**：全新 session `8019c38f34b646c3bda7b989d69f1bab` 正确通过字面实体角色去重；第 1 个
  `tap_semantic` 聚焦输入框并形成 matched receipt `receipt_adcb64797e8647b4b42d4d0b5d83e1d0`，revision `3→4`。
  第 2 个动作是正式 `input_verified_text`，本地计划的首段为 `longinputvalidation`；动作后首次四帧观察失败，session
  最终 `failed/physical_actions=2/active_subgoal=type_text`，没有发送、提交或第三个动作。
- **根因证据与诚实边界**：第 2 动作后的前三张持久帧发生大幅运动拖影，手机内容和键盘无法稳定辨认；第 4 张才部分回稳并可见
  输入框已有英文前缀迹象。输入结构审计原始响应为合法 v7 形状，但因模糊返回 `application_inputs=[]`、
  `keyboard.visible=false`，观察器以“页面不稳定或整体置信度不足”失败关闭。由于没有建立可信 after scene，不能把可见前缀迹象
  写成逐字输入成功，也不能把旧 session 改写或续跑。主要类别暂定为环境/动作后采帧稳定性，而不是实体角色、风险、App、坐标或发送边界。
- **停止条件与下一步**：本轮不重试真机、不清空、不继续输入、不修改生产代码。下一批先离线比较本次与既有成功
  `input_verified_text` 的动作返回至四帧采集时序，证明是否存在跨 App 可复现的通用 settle/capture 缺口；只有形成同类样本、通用合同和
  正反测试后才允许修改。当前手机草稿的精确值保持 unknown，下一次任何物理动作前必须先取得新的稳定四帧并决定安全恢复路径。
