# 接口调用统一网关验收台账

日期：2026-08-20  
状态：实现、完整回归和在线只读验证完成，尚待后续真机任务使用。

## 1. 当前合同缺口

此前命令行直接拼接 HTTP 请求，已出现两个可避免错误：猜测不存在的
`/api/agent/generic-supervised/sessions`，以及写请求遗漏 `X-Control-Token`。
它们属于 Agent 调用层错误，不是项目协议、模型、服务或手机故障。

必须证明所有后续通用 Agent 调用只经过一个类型化入口，该入口从当前运行服务取得
OpenAPI 和内存令牌，在写请求发出前校验方法、路径、请求体、版本和最新作用域。

## 2. 同类失败样本

- 猜测复数或列表路径；
- 错 HTTP 方法；
- 缺失、过期或错误控制令牌；
- 请求体缺字段、多字段或类型错误；
- 运行服务版本与 OpenAPI 版本不一致；
- session 不存在、终态 session 或 confirmation scope 缺失/跨会话；
- 写请求已经发出后等待响应超时；
- 服务、模型、设备和动作后验证错误混为同一种“接口错误”。

## 3. 通用修复

- 新增 `poc/local_agent_api_client.py`：只暴露命名操作，不暴露任意 URL 请求；
- 启动握手读取 `/api/session` 和 `/openapi.json`，核对版本并将令牌仅保留在内存；
- 所有项目请求先根据当前 OpenAPI 做严格 JSON schema 校验；
- `confirm_once()` 总是重新 GET 当前 session，只消费当前最新完整 scope；
- 写请求不自动重试，避免响应超时后重复物理动作；
- 新增 `poc/agent_api_cli.py`，作为命令行唯一入口；
- 错误分类为 `client_contract_error`、`service_contract_error`、
  `session_state_error`、`resource_not_found`、`network_timeout`、
  `network_error`、`model_error`、`device_error`、`verification_error` 和
  `service_error`。

该修复不读取 App 名称、不改变任务协议、动作协议或风险政策，也不增加产品边界。

## 4. 验证清单

- [x] 模块与 CLI 语法编译通过；
- [x] 路由缺失时目标请求为 0；
- [x] schema 多字段、scope 缺失和版本漂移在请求前阻断；
- [x] 当前令牌自动携带且不出现在结果/错误输出；
- [x] confirmation 从当前 session 读取并只 POST 一次；
- [x] 写请求超时不自动重试；
- [x] 最小离线测试 `10/10`；
- [x] 当前运行服务在线 bootstrap 和 status 均通过，手机物理动作 `0`；
- [x] 完整 Python 回归 `1392/1392`；
- [ ] 通过新网关完成下一次真实单步输入并取得动作后证据。

## 5. 停止条件

任何接口、模型、设备、验证错误或超时立即停止，不自动重试，不再绕过网关改用临时 HTTP 命令。
