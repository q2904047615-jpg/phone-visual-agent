# 手机自动点击器｜新对话接手文档

更新时间：2026-09-12（Asia/Shanghai）

> 状态说明：本文中的模型状态是 2026-09-12 历史快照。2026-09-15 用户已明确将正式视觉模型切回 `qwen3.7-plus`；当前状态以根目录 `项目交接文档.md` 的最新条目和运行配置为准。

## 1. 项目目标

这是一个跨 App、跨自然语言表达、可扩展到多手机的通用视觉操作 Agent。不能退化为微信、抖音或固定任务脚本。

当前正式运行方向是 Qwen 单视觉整任务循环：Qwen 接收原始任务、本会话已执行事实和当前新截图，每次只返回一个 canonical 动作或整任务 `finish`。

## 2. 当前运行链

网页/API → Application 会话 → Qwen 当前截图判断 → canonical 解析器 → 一次设备动作 → 新截图 → Qwen 判断继续或 `finish`。

- Qwen 负责视觉理解、下一动作选择和整任务完成判断。
- 本地负责 schema、设备/scope、坐标范围和一次执行约束。
- Controller/机械臂负责点击、滑动、Home、返回、后台等动作。
- ADB Keyboard 仅负责文字输入、清空和换行；按 device_id 独立配置。
- 未配置 ADB 的设备仍可使用机械点击、滑动和导航，不借用其他设备的 ADB。

## 3. 主要代码分层

- `poc/web_app.py`：网页/API 入口和运行时组合根。
- `poc/agent/domain`：canonical 动作、任务状态、设备能力、协议和校验。
- `poc/agent/application`：Qwen 请求、会话和整任务循环。
- `poc/agent/infrastructure`：摄像头、机械臂、ADB Keyboard、App 直启、Qwen HTTP、设备注册表和证据存储。
- `poc/agent/infrastructure/generic_scene_observer.py`、`generic_action_adapter.py`：观察和一次动作执行。
- `poc/agent_api_cli.py`、`poc/local_agent_api_client.py`：官方类型化客户端入口。

## 4. 已验证状态

- 当前项目 API 已按授权重载，使用工作树 `C:\Users\Administrator\Documents\Claude\Projects\手机自动点击器\poc`。
- 最近一次官方 status：controller 在线、camera 在线、busy=false、无 active task/session。
- Qwen：`qwen3-vl-plus`，思考开启（2026-09-12 历史状态，已被 2026-09-15 的 `qwen3.7-plus` 决策覆盖）。
- 1 号设备 ADB Keyboard ready；ADB 不是机械动作前提。
- 本批相关回归：117 项通过。
- 基线仍有 1 项既有架构测试失败：测试当时硬编码 `qwen3.7-plus`，当时正式模型为 `qwen3-vl-plus`；这是 2026-09-12 历史结论。
- 卖家 `main.exe` 未操作。

## 5. 多机位现状

用户要求共用一个可物理移动的摄像头，网页任务中指定目标手机；不要求每个机位都配置 ADB。

只读核对卖家目录发现：

`D:\main软件发客户-20260705\main实时点击控制软件\me_config`

目前只有一个设备目录，包含 `config.json` 和 `1.json`；未发现 `2.json` 到 `10.json`。当前登记一个 `USB Camera2` 和 `COM4`。

因此尚不能实现多机位切换。不能把 1 号的绝对物理坐标复制给其他手机，也不能猜卖家窗口按钮坐标。当前设备注册表仍禁止多个设备共享同一个控制窗口，避免没有硬件锁和画面归属时发生串机。

## 6. 本批已撤回的错误方向

本对话曾加入未经验证的 `seller_position` 参数、固定机位按钮坐标和任务开始时隐式切位点击；这些已精确撤回，并保留快照：

`C:\Users\Administrator\AppData\Local\Temp\phone-adb-optional-245b16ce0df64ff3b895bc8ec7978a83`

不能用整仓 `git reset` 或 `git checkout` 覆盖用户已有修改。

## 7. 当前唯一主线和停止条件

下一步先取得真实的 2 号及其他目标机位标定文件，再设计最小共享摄像头锁、切位顺序和画面归属验证。没有真实标定文件时停止生产代码扩展，不操作卖家程序、不移动机械臂、不猜坐标。

## 8. 接手纪律

- 先读取根目录 `项目最终目标.md`、`AGENTS.md`、本文件和验收台账当前段。
- 修改前说明根因、文件范围、验证和回滚方式。
- 调用 API 前先通过当前源码或 `/openapi.json` 核对路由。
- 每次只执行一个设备动作；动作后重新截图。
- 区分代码已实现、测试通过、API 已加载、真实单步成功和完整任务完成。
- 不启动、停止或操作卖家 `main.exe`。

## 9. 相关证据

- `poc/output/optional_adb_20260911/report.md`
- `项目交接文档.md`
- `单一权威最小校验验收台账.md`
- `用户决策与协议边界.md`
