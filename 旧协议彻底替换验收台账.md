> 历史/参考材料，不是当前运行权威。现行结论见[项目交接文档](项目交接文档.md)，产品方向见[项目最终目标](项目最终目标.md)。正文保留用于追溯，不据此恢复旧代码或执行旧步骤。

# 旧协议彻底替换验收台账

更新时间：2026-08-20（Asia/Shanghai）

状态：**已完成可执行协议切换与完整离线回归。**

> 本文记录 2026-08-20 切换时的历史政策，不再定义当前确认集合。当前只有登录和付款需要确认；敏感权限、账号/数据删除等普通效果自动执行。唯一边界见 `用户决策与协议边界.md`。

## 本轮唯一验收缺口

生产 DeepSeek 规划链仍在正式 `TaskSemanticIR / EffectIntent / 本地风险策略` 之前解析并校验模型提供的
`risk_actions / risk_action_ids / external_impact / confirmation_required`。这些旧字段及其自由文本迁移器仍可在新权威
运行前拒绝合法任务。必须完成一次真正的权威切换：旧字段不得再作为模型输入、前置校验、风险来源、确认来源或重规划
不变量；后续问题只能在新类型化协议中修复。

## 必须出现的证据

1. 正式 plan/replan 提示词和严格原始响应 schema 不再包含旧四字段；模型返回它们时按“退役字段”拒绝，不能悄悄恢复。
2. 原始规划先解析正向 `effect_intents`、实体绑定、子目标 effect refs 和 execution class，再构造正式 `TaskSemanticIR`；
   本地版本化策略是唯一确认权威。
3. `DynamicTaskGraph` 若因现有 Qwen、控制器或前端接口仍需风险投影字段，这些字段只能由新 IR 本地生成；模型值、旧正则、
   旧远程风险审计和旧兼容开关均不能写入或否决它们。
4. 旧远程自由文本风险审计不在生产导入图中；旧 App 固定流程继续固定 410；仓库不再保留可被误用为正式输入的 v2/v3
   协议夹具，只在负向测试中构造最小退役字段，证明它们不能进入 session、Qwen 或机械臂。
5. 两份现场等价失败图在不增加“保持不变≈不修改”等同义词补丁的情况下都进入新 IR；普通导航、聚焦空输入、未提交输入
   不产生 effect。正向发送、登录、付款、权限和不可逆删除必须形成对应 typed effect；未知或缺少绑定的 effect 在 Qwen 前阻塞。
6. 相关回归、历史失败重放和一次完整离线回归通过；本轮不启动/停止卖家 `main.exe`，不调用真机动作。

## 已观察事实与根因分类

- `DeepSeekTaskGraphPlanner.plan()` 目前先调用 `_normalize_legacy_transport_for_typed_projection()`，随后 `graph.validate()`，最后才
  调 `_apply_formal_semantic_authority()`。
- `Subgoal.validate()` 仍强制旧 `external_impact` 与 `risk_action_ids` 对称，能在新 IR 之前拒绝任务。
- 当前 `compile_formal_semantic_authority()` 实际调用 `compile_legacy_graph_shadow()`；`EffectIntent` 主要由旧 `risk_actions` 映射，
  因此现有“新协议”仍以旧协议为语义来源。
- 交接文档曾把“删除旧远程审计和旧固定入口”误写成“旧边界全部退役”，但随后又保留旧 transport 转换；验收口径错误。
- 根因分类：架构切换不完整 + 验收设计错误，不是模型随机、当前 App、手机状态或某个同义词缺失。

## 同类失败样本集合

- 现场样本 A：禁止发送，进入普通会话并聚焦空输入；旧模型把 send risk 关联到导航节点。
- 现场样本 B：保持原草稿不变且禁止输入/发送/删除/清空；旧模型把“修改草稿”风险关联到只读节点。
- 变化正例：换 App、换字段名称、把禁止事项放在全局或子目标约束，均不得产生正向 EffectIntent。
- 正向效果：发送消息、关注、评论、发布和一般数据修改形成 automatic typed effect。
- 需要确认：登录、资金交易、敏感权限、不可逆账号删除、不可逆数据删除形成 confirmation-required typed effect。
- 反例：effect 缺目标/正文绑定、source subgoal 不存在、expected result 不属于绑定子目标完成条件、未知 effect kind、旧字段混入、
  旧 v2 payload、模型自报 confirmation 均在 Qwen/机械臂前失败。

## 通用修复说明

1. 新增严格的 DeepSeek 规划传输对象：只携带 goal、constraints、completion conditions、typed effect intents、subgoals、frontier
   和 clarification；不携带旧风险字段。
2. typed effect 必须引用用户原文实体角色、存在的 source subgoal，并逐字引用该子目标的正向完成结果；禁止/未发生/保持不变
   只能成为 constraint/desired state，不能成为 effect。
3. 先由新传输对象直接生成 `TaskSemanticIR` 和本地风险决定，再确定性投影到当前运行图；投影字段仅是下游兼容视图，不能反向
   参与 IR 编译或风险裁决。
4. 删除生产旧迁移器、旧风险/impact 前置对称校验、旧远程审计入口及其生产帮助函数。历史 JSON 只保留为“正式入口拒绝旧协议”
   的负向夹具；不会提供运行时 fallback。
5. 不改变 Qwen 单动作、fresh scope、唯一视觉目标、设备能力、一次一动作和动作后证据门；回滚以本批单独提交为单位。

## 验证清单与停止条件

1. 先实现并运行新规划传输 parser/IR/projector 的最小正反测试。
2. 原样重放两份现场失败，断言旧风险内容不能进入正式 authority，且不依赖新增自由文本同义词。
3. 运行 DeepSeek、TaskSemanticIR、Qwen 上下文、编排器、Web/前端协议相关回归。
4. 删除或改写只验证退役旧审计行为的测试；不得删除仍覆盖设备、证据、scope、revision 或动作安全的测试。
5. 最后运行一次完整离线回归、静态编译、JS 语法、`git diff --check` 和生产导入扫描。
6. 若新 IR 仍读取模型旧字段、任何旧字段可阻止合法 typed plan、或普通外部效果/高风险效果分类出现无解释变化，立即停止，
   不加载服务、不操作真机、不追加措辞补丁。

## 最终实施结果

- 当前唯一正式高层协议为 `2026-08-20-deepseek-typed-task-graph-v4`；DeepSeek 原始响应严格使用
  `effect_intents / effect_ids / execution_class`，模型风险字段和确认布尔值均不在 schema 中。
- 删除旧 transport 迁移器、旧远程风险审计模块、旧监督 runtime、旧 v2/v3 协议夹具与只验证旧协议的测试。
- Qwen、编排器、Web API 和前端只接受 typed v4；旧版本和伪装成 v4 的旧字段均为
  `unsupported-protocol`，且不会产生确认按钮、scope 或动作。
- 效果确认统一为 `effect_ids`、`EffectConfirmationAuthority`、`/approve-effect` 和
  `2026-08-20-typed-effect-confirmation-v1`；`/approve-risk` 已删除。
- 普通发送、关注、评论、发布、收藏、订阅和一般数据修改由本地策略自动处理；登录、付款、敏感权限与不可逆删除才要求
  效果确认；`unknown` 仍在 Qwen 前阻塞。
- 已删除未被正式链使用、仍携带 `risk_ids` 的聊天专用 `CanonicalMessageIntent` payload。收件人逐字绑定辅助函数保留为
  通用实体消歧，不拥有协议、风险或动作权限。
- Python 内部仍有少量历史属性名承载 typed v4 确定性生成的运行时投影。它们不接受模型旧 payload、不调用旧审计、不能改变
  effect 分类或产生确认权限，因此不是旧协议兼容入口；后续仅可做不改变行为的命名清理。
- 历史报告、交接记录和 2026-08-12 设计文档保留证据价值，但全部标记为归档/不可执行；当前协议说明与 Qwen 评估说明均已
  改为 typed v4。

最终验证：

- 核心协议、Qwen、编排与 Web：`514/514`；其中 typed v4 transport/重规划不变量为 `15/15`；
- Python 全量：`1341/1341`；
- Node 协议及两套离线浏览器合同：`22/22`；
- 新 JSON 夹具：`4/4`；
- 生产文件 `py_compile`、两份 JavaScript `node --check`、`git diff --check` 和退役生产导入扫描均通过。

首次全量发现的 `2 failures + 3 errors` 全部来自旧测试仍寻找 `risk_audit_revision` / `last_semantic_shadow`；测试改为新
`effect_policy_revision` / `last_semantic_authority` 后，定向 `36/36` 与最终全量均通过。另将旧 232 项测试中仍有效的自然动作、
直控拒绝、跨 App、依赖环、唯一 frontier、身份/约束/revision 保持、effect 不可删除/改型和单次模型调用不变量迁移为新的 typed v4
正反测试；没有为旧断言恢复生产兼容。

本批最终只重载了本项目 Uvicorn，没有调用在线模型、相机、机械臂或卖家 `main.exe`，不会改变既有阶段 1/2/3 真机结论。运行中 `/api/device` 已只公开 `typed_effect_authority`：语义 IR 为 `2026-08-20-task-semantic-ir-v3`、效果权威为 `2026-08-20-typed-effect-authority-v1`、视觉动作权威为 `2026-08-19-visual-action-authority-v1`；旧 `semantic_risk_authority` 属性不存在。当前隐藏服务会话不能访问卖家交互窗口，因此 controller/camera 不可用，本轮按 0 动作停止，未把协议切换冒充真机验收。
