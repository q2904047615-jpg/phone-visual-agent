# 架构减法重构验收台账

更新时间：2026-08-26

## 本轮必须证明的能力

正式运行链只保留一套可执行动作与完成判定权威：DeepSeek 维护任务图和完成条件；一次 Qwen 单步观察建立可信画面与 canonical candidate；本地确定性选择器只从该目录选择一个动作；Controller 只校验当前设备、scope、时效、可达性和动作后证据。

## 必须出现的证据

1. 正式 Web 与评测入口不再实例化带在线二次选择能力的 Qwen selector。
2. Qwen selector 不再包含模型调用、重试、选择提示词、`finished` 或完成证据字段。
3. Orchestrator 不再接受 Qwen 提出的完成候选；任务完成只来自 DeepSeek 任务图、正式完成谓词和动作后验证。
4. 正式单步观察器之外的旧多轮 targeted/App/system/input 审计入口被物理删除，不能通过配置恢复。
5. 同类动作只保留一套 canonical candidate 构建；Controller 不重建候选，只验证执行条件和回执。
6. 输入正文身份只由 typed input ledger 维护；分段规划器只计算下一段，不成为第二状态权威。
7. 相关历史失败重放、最小相关测试和一次完整回归通过；代码扫描确认旧符号不存在。

## 已有证据

- `poc/web_app.py` 和正式评测已经接入单步观察器，每步只允许一次 Qwen 视觉调用。
- `2026-08-20-canonical-action-v1` 已是唯一动作语义协议。
- 阶段 1、阶段 2 和阶段 3 的既有验收结论不依赖本轮被删除的休眠分支。
- 本轮修改前基线：Python `1156/1156`、前端 `15/15` 通过，已保存本地回滚提交 `3c0e334`。

## 已证实根因与同类样本

根因：新的单步正式链已经启用，但旧实现仍以类、配置开关、提示词和测试的形式留在生产模块中，形成不可见的备用权威和维护负担。

- `allow_online_selection=True` 可恢复第二次 Qwen 选择调用。
- `_selection_decision_prompt` 允许 Qwen 输出 `finished` 和完成证据。
- Orchestrator 的 `_review_completion_candidate` 接受 Qwen 完成候选后再次调用 DeepSeek。
- `GenericSceneObserver.observe` 仍保留旧多轮视觉审计链，尽管正式入口只实例化 `SingleStepGenericSceneObserver`。
- 旧几何、App 身份、输入身份和完成检查在不同层重复表达同一结论，导致合法动作被后置规则再次否决。

## 通用修复

1. 把 Qwen selector 收敛为纯本地 canonical 选择器，删除联网二次选择与完成判定能力。
2. 删除 Orchestrator 的 Qwen 完成候选复核分支，只保留任务图完成和正式回执验证。
3. 删除正式调用图不可达的旧多轮观察入口及其专属测试；保留单步观察所需的通用解析、输入结构和候选构建函数。
4. 逐项把 App 身份、几何、输入值和完成判定改为直接调用各自唯一权威；确认无正式调用者后删除旧包装器和旧默认值。

本轮不增加 App 名称分支、固定坐标、命令特例、确认项或风险类别。

## 影响、回滚与停止条件

- 影响范围：视觉观察后的本地动作选择、完成候选处理以及正式链不可达的旧实现。
- 不影响：登录/付款确认边界、机械臂驱动、canonical action 类型、阶段 1–3 的既有证据。
- 回滚点：本地提交 `3c0e334`。
- 停止条件：若删除项仍被正式 Web/评测调用，或相关回归显示它承担独立且必要的通用能力，立即停止该项删除并重新分类；不以兼容开关把旧权威接回正式链。

## 完成结果

- 已删除 `poc/generic_step_planner.py`、`poc/element_geometry_audit.py`、`poc/test_element_geometry_audit.py`，并把仍有价值的 adapter 回归从 `test_generic_step_planner.py` 改名为 `test_generic_action_adapter.py`。
- `GenericSceneObserver` 的旧多轮 targeted/App/system/input/geometry 审计链已删除；正式观察只剩 `SingleStepGenericSceneObserver`，每步至多一次远程 Qwen 请求。
- Qwen 在线二次动作选择、重试提示词、`finished`、完成候选和 Orchestrator 完成复核已删除；本地 selector 只能从同一 canonical 目录选择。
- Controller 的发送、输入、关注、评论、删除、长按、拖动等动作词黑名单及相关重绑定特判已删除；普通动作不再产生第二风险权威。
- App 身份与输入身份的重复私有判断已分别收敛到 canonical surface 合同和 typed input lineage；Controller 只处理 fresh scope、唯一目标、几何、机械可达和回执。
- 相关 adapter/Controller 变化样本 `166/166` 通过；完整 Python 回归 `820/820`（`24.462s`）通过；前端回归 `30/30` 通过；静态编译、`git diff --check` 与旧生产符号扫描通过。

本轮验收缺口已关闭。没有执行在线模型或真机动作；阶段 1–3 的既有真机结论保持不变，但本轮没有新增或重做真机证据。

## 2026-08-26 单一权威链重新打开

### 当前唯一缺口

文档虽已声明 canonical 单一权威，但当前运行时仍对同一个已选动作依次运行 Qwen 内部 Controller 预检、`CanonicalActionPolicy` 初检、确认后的 Policy 复检和带 3 秒 TTL 的物理执行门。必须把正式链收敛为一次 canonical 选择、一次 scope 消费、一次 Controller 执行和一次 typed receipt；其他组件只能消费，不得再次裁决。

### 已观察事实与根因分类

- `QwenVisualDecision.validate()` 当前直接调用 `UniversalActionController().resolve_one()`；随后 Orchestrator 又运行 Policy，实际执行时 adapter 再调用 Controller。
- `CanonicalActionPolicy._formal_candidate_decision()` 重新编译并选择同一 canonical catalog；确认阶段的 `policy_recheck` 再执行一次相同判断。
- `PhysicalExecutionGate` 已绑定一次性 live credential 和执行瞬间真实帧像素，却用固定 `3.0s` 再次判断画面时效。现场正常链耗时约 `5.7s`，因此在 0 物理动作处稳定误拒绝。
- 根因归类为 **通用运行链重复权威**，不是 Qwen 模型随机、设备断线、机械不可达、App 状态或风险边界。

### 通用修复与样本

1. 删除 `CanonicalActionPolicy` 及所有运行时注入、初检和确认后复检；Orchestrator 只记录已经由单步决策验证通过的 canonical selection receipt。
2. 删除 Qwen 决策校验内的 Controller 实例化；Controller 只在 adapter 的真实执行入口运行一次。
3. 删除方向凭据的固定秒数 TTL；保留一次性领取、device/action/scene/frame-size 绑定和执行帧像素漂移拒绝。
4. 已有零动作异常 lineage 恢复修复保留；增加静态防恢复测试，禁止上述旧类、调用和 TTL 符号返回。

正向样本：canonical 动作在同一 observation/scope 下只执行一次 Controller；确认等待超过旧 3 秒且画面不变仍可消费一次。变化/反例：真实帧结构变化、错误设备/动作/scope、重复消费仍拒绝；真实物理动作失败不能恢复旧 App lineage。

### 影响、回滚与停止条件

- 影响：Qwen 单步决策校验、Orchestrator 动作授权记录、确认执行入口、方向物理门及对应测试。
- 不影响：DeepSeek 目标拆分、canonical candidate 类型、输入账本、机械坐标换算、登录/付款确认边界和卖家 `main.exe`。
- 回滚：撤销本批单一提交；不得以配置开关恢复旧 Policy 或 TTL。
- 停止条件：相关测试表明被删入口承担了 canonical/Controller 之外不可替代的通用事实，或完整回归出现无法归入本批的回归时停止扩大修改。

### 完成结果

- Qwen 校验层已删除 Controller 预执行；Qwen 只返回并校验当前观察下的 canonical candidate。
- Orchestrator 已删除 `CanonicalActionPolicy`、初检和确认后 `policy_recheck`；现在只保存同一个 canonical selection receipt，并在执行前一次性消费既有 scope。
- Controller 只在 adapter 的真实执行入口解析一次动作，并只做一次动作后验证；证据投影不再二次调用验证器。
- 物理执行门已删除固定 3 秒 TTL；仍保留一次性凭据、设备/动作/scene/frame-size 绑定和执行帧像素漂移拒绝。
- 已加入防恢复回归，禁止旧 Policy、Qwen 内部 Controller、固定 TTL 和重复动作后验证重新进入生产代码。
- 相关模块回归 `371/371` 通过；完整 Python 回归 `886/886`（`40.382s`）通过；浏览器合同回归 `13/13`（`44.739s`）通过；静态编译、旧符号扫描和 `git diff --check` 通过。

本轮代码与离线合同证据已完成。项目 API 已只重载为启动父 PID `22772`、实际监听 PID `10392`；官方类型化客户端确认 `service_version=0.2.0`、OpenAPI 已核对、controller/camera online、`busy=false`、0 活动任务/会话，零动作 doctor 为 `ready=true`、画面稳定且 `blockers=[]`。卖家 `main.exe` 保持 PID `2288` 和原启动时间，未被启动、停止、重启或操作。没有调用在线模型或执行真机动作，因此不能把本轮结果表述为新增真机验收完成。

## 2026-08-26 相机串行闭环

### 当前唯一缺口与根因证据

最新网页 session `0647ce7e8fe04bd18f0b022d86fa82d8` 的唯一 `home` 动作已经真实回到桌面，但动作后 10 秒窗口内只取得 2 帧，未满足连续 4 帧稳定证据，因此终态为 `failed/physical_actions=1`，后续动作未执行。现场同时有 Chrome/Edge 三个页面连接项目 API，前端每页每 700ms 请求一次 `/api/preview.jpg`；服务端共有 9 条已建立连接。相同现场的零动作 doctor 取得 4 帧耗时 `30.798s`。根因归类为 **网页实时预览与正式闭环争用同一物理相机**，不是 Qwen 合同、canonical 权威、Controller、手机状态或机械动作失败。

### 通用修复与变化样本

1. 每个设备建立独立 `DeviceCameraCoordinator`；Agent、能力验收和 doctor 在一次观察/执行/验证窗口内持有串行相机租约。
2. 正式 Agent 帧仍按原四帧稳定合同采集，并在本地更新最近 JPEG 缓存；每步仍只向 Qwen 提交一次观察。
3. 网页预览不得排队争抢租约；任务或 doctor 运行时只返回最近缓存帧，释放后自动恢复真实被动预览。
4. 正向样本：空闲时预览真实采集并更新缓存；变化样本：另一线程持有任务租约时，多次预览立即返回缓存且真实预览调用为 0；设备协调锁期间三个连续网页请求只触发 0 次新增相机采集。

本批不增加 App、截图、固定坐标或动作特例，不降低四帧稳定阈值，不延长动作后超时，也不允许自动重复物理动作。回滚方式为撤销本批相机 coordinator、adapter capture 包装及预览缓存分支。停止条件为相关测试显示租约跨设备串扰、缓存冒充正式 Agent 观察或正式采集未更新缓存。

### 验证清单

- 相机 coordinator 正反例与 Web 预览合同；
- Web 平台、runtime doctor、adapter/编排相关回归；
- 一次完整 Python 回归、浏览器合同、静态编译和 `git diff --check`；
- 离线全绿后只重载项目 API，官方客户端执行 0 动作 doctor，并在网页预览连接存在时确认 4 帧采集不再被拖慢。卖家 `main.exe` 不得被操作。

### 离线完成结果

- 相机 coordinator、三次缓存预览、跨线程租约、Web 预览和 doctor 最小正反例 `10/10` 通过。
- Web/doctor/adapter/编排相关回归 `370/370` 通过；完整 Python 回归 `889/889`（`39.919s`）通过；浏览器合同 `13/13`（`43.337s`）通过。
- `py_compile` 和 `git diff --check` 通过。

### 加载与运行结果

- 只重载项目 API：启动父 PID `27252`、实际监听 PID `2440`；卖家 `main.exe` 保持 PID `2288` 和原启动时间，未被启动、停止、重启或操作。
- 官方类型化客户端确认 `service_version=0.2.0`、OpenAPI 已核对、controller/camera online、`busy=false`、0 活动任务/会话。
- 保留现有 `13` 条网页连接不关闭时，零动作 doctor 的连续 4 帧采集耗时从修复前 `30.798s` 降至 `3.121s`，终态 `ready=true`、`camera_stable=true`、`blockers=[]`、`physical_actions=0`。

本批可声明 **通用相机串行租约已实现、相关与完整回归通过、运行服务已加载、带多网页连接的零动作运行验证通过**。没有调用在线模型或执行新的物理动作；原失败 session 仍为真实 `failed/physical_actions=1`，不得改写或续跑。

## 2026-08-26 当前截图单动作串行闭环

### 当前唯一缺口

每一个物理步骤必须只依据该步骤刚取得的当前截图和同一份 typed scene 产生一个 canonical 动作；动作后必须取得下一张新截图再决定后续。当前缺口是目标表面前提没有进入 canonical 候选过滤，且旧分页历史兜底可在错误页面自行选 swipe。

### 已观察事实与根因分类

- session `d7ff92ca46084f02803acc5667630e07` 的活动子目标为 `clear_card/在最近任务界面中清除微信卡片`，动作前四帧实际均为 Launcher 桌面。
- 单步 Qwen 已收到并结构化该桌面画面；错误发生在其后的本地 `_untried_paged_swipe_choice`，它把桌面 `paged_viewport` 当成可探索路径并选择 `swipe left`。
- 动作后新观察仍为桌面，并把桌面分页 `page_index` 从 `0` 报为 `1`；因此该 swipe 自身的 viewport 变化可以 matched，但这个回执不可能证明最近任务卡片已经移除。
- 根因归类为 **当前表面前提未进入 canonical 候选过滤，且历史分页兜底越过了当前子目标**。不是 App 特例、设备/相机离线、机械臂不可达或风险边界。

### 同类样本与通用修复

1. 正向：目标表面为 `recent_tasks`、当前为 Launcher 时，目录只保留 `open_recent_apps`。
2. 正向：新截图已为 `recent_tasks` 且存在唯一完整目标卡片时，目录只保留 element-bound swipe，后置条件为同一目标消失。
3. 变化：Launcher 同时存在 `paged_viewport` 和与目标同名 App 图标时，二者均不得抢占 `open_recent_apps`。
4. 反例：普通 viewport swipe 前后只有 fingerprint 噪声、typed scene 等价时继续不得判为 matched；该既有 Controller 合同保留并纳入相关回归。
5. 删除依赖历史导航记录选择尚未探索方向的本地分页兜底；保留当前截图中的 typed viewport 事实，但只有它和当前子目标共同唯一授权时才形成动作。

本批不增加微信、App 名称、截图、文字或固定坐标特例，不改变登录/付款确认边界，不新增在线模型调用。回滚方式为撤销本批提交；停止条件为当前表面过滤破坏 exact system action、跨 App 目标表面导航或 element-bound swipe 的既有合同。

### 验证清单

- 现场失败图/任务图离线重放与最近任务正反变化样本；
- TaskSemanticIR、canonical、Qwen 单步选择和 Controller 滑动验证相关回归；
- 一次完整 Python 回归、浏览器合同、静态编译和 `git diff --check`；
- 离线全绿后本地提交并只重载项目 API，使用官方类型化客户端做 0 动作状态/doctor 核对。卖家 `main.exe` 不得被操作。

### 离线完成结果

- TaskSemanticIR 会把最近任务卡片清除编译为 typed `surface.kind=recent_tasks` 和唯一 `required_action=swipe`；当前截图未到该表面时，canonical 只保留 `open_recent_apps`，到达后只保留绑定当前唯一卡片的左滑及 `element.exists absent` 后置条件。
- `_untried_paged_swipe_choice` 及其历史方向选择实现已从生产代码物理删除；防恢复回归禁止该符号返回。
- 现场形状、桌面分页/同名图标变化样本、最近任务卡片正向样本最小回归 `92/92` 通过；TaskSemanticIR、DeepSeek typed transport、canonical、Qwen、场景观察、adapter、Controller、Orchestrator 与 Web 相关回归合计 `599/599` 通过。
- 最终代码的最小回归 `92/92` 通过；完整 Python 回归 `891/891`（`41.725s`）通过；浏览器合同 `13/13`（`36.493s`）通过；静态编译、旧入口扫描和 `git diff --check` 通过。

### 提交与加载结果

- 代码、回归、边界决定和本台账已本地提交为 `e90a1b6 fix: bind each action to current visual surface`，未推送。
- 只重载项目 API：启动父 PID `28324`、实际监听 PID `17908`。官方类型化客户端确认 `service_version=0.2.0`、OpenAPI 已核对、controller/camera online、`busy=false`、0 活动任务/会话；零动作 doctor 为 `ready=true`、`camera_stable=true`、`physical_actions=0`、`blockers=[]`，正式动作能力包含 `open_recent_apps`。
- 卖家 `main.exe` 保持 PID `2288` 和原启动时间，未被启动、停止、重启或操作。

本批可声明严格当前截图单动作合同已实现、离线回归通过并由运行服务加载。没有执行新的真机物理任务；历史失败 session 仍保持原终态，新的网页任务是否最终 succeeded 仍须由用户下一次全新运行的真实证据确认。

### 用户二次纠正后的残留接口清理

- Git 追溯确认 `_untried_paged_swipe_choice` 不是远古遗留，而是提交 `b3d3439` 在删除在线二次选择时新引入的历史分页兜底；此前称其为“旧遗留”不准确。
- 提交 `e90a1b6` 已删除实际兜底函数，但审计发现 Orchestrator 仍把 `session.history` 作为 `navigation_history` 传入 Qwen 决策接口。该参数虽然已无读取者，仍是可恢复旧行为的无用生产管线。
- 本轮物理删除 Qwen `navigation_history` 形参、Orchestrator 传参和两份测试替身接口；`session.history` 本身仅作为动作回执、验证证据和报告记录保留，不再进入动作选择。
- 防恢复回归同时禁止生产代码出现 `_untried_paged_swipe_choice` 和 `navigation_history`。完成相关与完整回归、提交并重载项目前，不得再次声称“全部清除完成”。
- 当前生产扫描对 `_untried_paged_swipe_choice`、`_paged_viewport_key`、`navigation_history` 和旧分页选择理由均为 0 命中；相关 Qwen/编排/Mock 回归 `183/183` 通过，完整 Python 回归 `891/891`（`40.286s`）通过，静态编译通过。
- 残留接口清理已本地提交为 `20ba988 refactor: remove history driven action selection pipe`，未推送。项目 API 已只重载为启动父 PID `28660`、实际监听 PID `25684`；官方客户端确认 OpenAPI 正常、controller/camera online、`busy=false`、0 活动任务/会话，零动作 doctor 为 `ready=true/physical_actions=0/blockers=[]`。卖家 `main.exe` 保持 PID `2288` 和原启动时间，未被操作。

## 2026-08-26 无调用代码及专属测试清理

### 范围与判定

- 本批只删除当前正式生产源码中没有调用者、没有动态协议职责，或仅由其专属测试维持存在的定义；不把“静态搜索不到”直接等同于可删除。
- FastAPI 路由、设备执行器的动态动作接口、系统回调、上下文管理器、当前并发锁测试设施和通过 `getattr` 调用的视觉计数接口均保留。
- 仍被正式链读取的 `UIScene.app_id` 只读兼容字段及前端兼容展示未纳入本批；它们不是无调用代码。未跟踪/忽略的用户工具也未改动。

### 删除结果

1. 物理删除旧正则约束候选模块 `constraint_target_filter.py` 及其整份专属测试；该模块不在 canonical 正式调用图中。
2. 删除无调用的 canonical 目录二次选择包装器、旧 overview/ROI/JPEG 编码与坐标映射整簇、动作前后观察重试分类包装器。
3. 删除 typed input store 的无调用 `match_visual`、`match_surface` 和历史 session 恢复包装器；正式链仍使用当前 typed lineage 记录、加载和确定性匹配合同。
4. 删除 TrustedObservation 旧 prompt 投影、卖家 GUI 未调用诊断函数、UIScene 未调用推断/批校验函数、旧单次确认 `run_safe_loop` 兼容入口、重复模型身份投影和重复 Web adapter 工厂。
5. 删除上述入口的专属测试；保留仍覆盖当前正式能力的 canonical、lineage、autonomous loop、Controller、Web 和真机安全合同测试。
6. 增加防恢复回归：被删文件和 22 个旧定义签名不得重新出现在生产模块中。

### 验证结果

- 删除后生产符号反查仅在防恢复测试的禁止清单中命中；受影响生产文件全部通过 `py_compile`，`git diff --check` 通过。
- canonical、观察图像、typed lineage、Orchestrator、adapter、UIScene、模型配置、Web 与防恢复相关回归 `521/521` 通过。
- 最终完整 Python 回归 `863/863`（`24.110s`）通过。

本批属于离线代码减法，没有重载项目 API、调用在线模型或执行真机动作，也没有操作卖家 `main.exe`。因此可以声明无调用生产入口及其专属测试已删除、相关与完整回归通过；不能把本批表述为新增真机验收。

## 2026-08-26 轻量渐进式 DDD 模块化单体第一批

### 当前唯一缺口与根因证据

当前 Web/FastAPI 路由直接持有通用 Agent 会话字典和线程锁，并在路由函数中完成会话查找、设备绑定、任务边界、Orchestrator 调用、自动推进与取消/暂停。由此造成 HTTP 传输职责、应用用例和进程内存储混在 `web_app.py`；同一会话生命周期只能通过 Web 层复用和测试，尚未形成可独立调用的模块边界。

根因归类为 **代码组织与依赖方向缺陷**，不是 canonical 动作协议、Qwen/DeepSeek 判断、设备状态或真机执行缺陷。本批不改变任何动作语义、风险边界、模型调用次数、API schema、回执或机械臂行为。

### 同类样本

1. `/start` 同时检查设备占用、建立输出目录、清除旧停止状态、调用 Orchestrator、登记会话并决定是否自动推进。
2. `/confirm`、`/next`、`/auto` 分别重复会话查找、设备绑定、物理动作计数和 Orchestrator 调用。
3. `/cancel`、`/pause` 直接在 Web 层会话锁内修改生命周期；`/api/device` 也直接遍历同一内部字典。
4. 变化样本：同设备已有活动会话时必须在建立输出目录和调用模型前拒绝；请求设备与会话设备不一致时必须保持 0 物理动作。

### 通用修复

1. 建立 `poc/agent` 轻量模块化单体边界：`domain` 定义会话与仓储端口，`application` 承担通用 Agent 会话用例，`infrastructure` 提供线程安全的进程内仓储；现有 `web_app.py` 作为接口与组合根逐步变薄。
2. 第一批只迁移一个真实业务切片：通用 Agent 会话的开始、查询、确认、重观察、自动推进、暂停和取消；每个旧 Web 调用点迁移后直接删除，不保留第二套会话字典或兼容入口。
3. 现有 Orchestrator、canonical action、Controller、typed input lineage 和模型 provider 保持原位，后续每批只迁移一个可验证切片，不做一次性大搬家。
4. 增加静态依赖回归：domain/application 不得导入 FastAPI、Pydantic、Web 或 infrastructure；Web 不得重新持有通用会话字典和锁。

### 影响、回滚与停止条件

- 影响范围：通用 Agent 会话生命周期的代码位置、依赖注入和进程内存储入口。
- 不影响：公开 HTTP 路径/请求/响应、当前唯一动作协议、DeepSeek/Qwen 职责、登录/付款确认、设备锁、相机租约、真机动作和既有阶段证据。
- 回滚方式：撤销本批单一提交；不得通过同时保留旧字典和新仓储的方式回滚。
- 停止条件：任何测试显示 API payload、动作数、会话状态、模型调用次数或锁定语义变化时，停止扩大迁移并先恢复行为等价。

### 验证清单

- 应用服务正向、活动设备冲突和设备不匹配变化样本；
- Web 通用会话 API 与 Orchestrator 相关回归；
- 静态依赖方向和旧 Web 会话存储防恢复扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为行为保持的离线结构迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 完成结果

- 已建立真实运行中的 `poc/agent/domain`、`application`、`infrastructure` 三层；domain 只定义会话合同、设备绑定不变量与仓储端口，application 统一承担会话用例，infrastructure 提供唯一线程安全进程内仓储。
- `web_app.py` 已删除 `generic_supervised_sessions`、`generic_supervised_session_lock` 和 Web 私有设备绑定判断；`/start`、查询、效果确认、动作确认、`/next`、`/auto`、`/pause`、`/cancel` 现均调用同一个应用服务。不存在旧字典双写或兼容入口。
- 正向、活动设备冲突、跨设备变化样本与静态依赖测试 `5/5` 通过；Web API `74/74`、Orchestrator `156/156`，相关合计 `235/235` 通过。
- 本批唯一完整 Python 回归 `868/868`（`24.614s`）通过；前端纯协议 `11/11`、浏览器合同 `13/13`、阶段 2 控制台浏览器合同 `4/4` 通过。浏览器测试复用了本机已有 Playwright 缓存模块，没有安装依赖。
- 受影响模块 `py_compile`、依赖方向扫描、旧 Web 会话存储防恢复扫描与 `git diff --check` 通过。

本批已完成的是 **DDD 第一条真实垂直切片**，不是声称 7000 余行 Orchestrator 和全部平铺模块已经迁完。公开 API、模型调用次数、canonical 单一权威、输入账本、设备/相机锁和动作语义保持不变。本批没有重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-26 轻量渐进式 DDD 模块化单体第二批（实施前冻结）

### 当前唯一缺口与根因证据

当前设备执行切片分散在三个旧位置：根目录 `poc/device_executor.py` 同时定义领域请求/结果合同和 Robot/Replay 基础设施实现；根目录 `poc/device_exclusivity.py` 持有操作系统进程租约；`DeviceTaskRegistry` 则内嵌在 7000 余行的 `universal_agent_orchestrator.py`。因此应用层和接口层必须反向依赖平铺基础设施文件，Orchestrator 还承担了设备任务占用实现，无法形成独立、可替换的设备执行边界。

根因归类为 **代码组织与依赖方向缺陷**，不是机械臂、设备在线状态、canonical 动作语义、Qwen/DeepSeek 判断或输入链路缺陷。本批只迁移唯一正式实现并删除旧入口，不改变任何公开 API、动作参数、动作次数、异常时机、租约语义或硬件回执字段。

### 同类失败与变化样本集合

1. 正式 Robot 执行器必须继续将 canonical 动作映射到同一组 Robot transport，并为单击、双击、Back、Home、最近任务和长按生成同样的硬件事件回执。
2. 离线 Replay 执行器作为变化样本，必须继续逐项校验动作种类和请求参数，且不产生真实物理动作。
3. 同一进程内同设备的第二个活动 session 必须被拒绝；嵌套的同线程同 session 占用必须保持可重入。
4. 跨进程同设备租约必须互斥，而不同设备的任务锁必须互不影响。

### 通用修复

1. 在 `poc/agent/domain/device_execution.py` 定义设备动作请求、执行结果、执行器端口和设备占用错误；domain 不导入 Robot、操作系统租约、Web、FastAPI 或 Orchestrator。
2. 在 `poc/agent/infrastructure` 放置 Robot/Replay 执行器、OS 进程租约和设备任务注册表；接口与组合根直接注入这些实现。
3. 更新所有生产与测试调用方后，物理删除根目录 `device_executor.py`、`device_exclusivity.py` 以及 Orchestrator 内嵌的 `DeviceTaskRegistry`，不保留转发、重导出、双实现或兼容门面。
4. 本批所称“回执”仅指 `DeviceExecutionResult.hardware_receipt` 及其 transport 生成逻辑。`AgentEvidenceStore` 和 `CanonicalSelectionReceipt` 分别属于会话证据与 canonical 选择边界，本批不迁移，以免扩大为第二条切片。

### 影响、回滚与停止条件

- 影响范围：设备执行合同和实现的代码位置、同设备任务占用、跨进程租约的依赖入口。
- 不影响：公开 HTTP schema、canonical 候选与子目标归属、DeepSeek/Qwen、输入账本、相机租约、确认边界、设备标定、真机动作和既有阶段证据。
- 回滚方式：整体撤销本批单一提交；不得通过恢复旧文件并同时保留新模块来回滚。
- 停止条件：任何相关测试显示动作映射、硬件回执、物理动作计数、同设备互斥、跨设备独立性或异常的外部行为变化时，停止继续迁移并先恢复行为等价。

### 验证清单

- DeviceActionRequest 正向与非法参数变化样本、Robot 硬件回执和 Replay 行为回归；
- 同进程同设备冲突、可重入、跨进程竞争和不同设备独立性回归；
- adapter、能力验收、标定、Web、Orchestrator 相关回归；
- 静态依赖方向、旧根文件/旧 Orchestrator 类定义/旧导入防恢复扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 完成结果

- `poc/agent/domain/device_execution.py` 现为唯一设备动作请求、执行结果、执行器端口和设备占用错误合同；`poc/agent/infrastructure` 现为唯一 Robot/Replay 执行、OS 租约和设备任务注册表实现。
- 根目录 `poc/device_executor.py`、`poc/device_exclusivity.py` 以及 Orchestrator 内嵌 `DeviceTaskRegistry` 已物理删除；Orchestrator 只依赖 domain 的 `DeviceTaskRegistryPort`，注册表由 Web/测试组合根显式注入，不提供旧公开重导出或 infrastructure 默认实例。
- Robot 的 canonical transport 映射和硬件回执字段保持不变；Replay 继续为 0 真机动作。同设备活动任务冲突、跨进程互斥、不同设备独立和同线程锁可重入均由新边界直接验证。
- 新增的变化样本与架构防恢复最小回归 `17/17` 通过；adapter、最近任务、回放、能力验收、标定、Orchestrator、Mock 闭环和 Web 相关回归 `497/497` 通过。
- 最终代码的完整 Python 回归 `871/871`（`26.638s`）通过；前端纯协议 `11/11`、浏览器合同 `13/13`、阶段 2 控制台浏览器合同 `4/4` 通过。静态编译、Node 静态检查、旧文件/旧导入/旧类定义扫描与 `git diff --check` 通过。

本批完成的是 **设备执行与独占控制这一个真实 DDD 切片**。公开 API、canonical 单一权威、模型调用、输入事务、动作次数和真机语义没有改变；会话证据存储和 canonical 选择回执仍留在各自现有边界，未被错误并入设备层。本批没有重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-26 轻量渐进式 DDD 模块化单体第三批（实施前冻结）

### 当前唯一缺口与根因证据

当前会话证据持久化仍内嵌在 `universal_agent_orchestrator.py`：`AgentEvidenceStore` 同时负责证据对象投影、文件名校验、JSON 序列化、目录创建、同目录临时文件、`fsync` 和原子替换；Orchestrator 还在未注入工厂时默认自行实例化该文件系统实现。因此编排层直接依赖磁盘基础设施细节，测试和组合根也必须从 Orchestrator 模块取得存储实现，尚未形成可替换的会话证据端口。

根因归类为 **代码组织与依赖方向缺陷**，不是报告内容、任务图、canonical 选择、模型判断、设备状态或真机执行缺陷。本批只迁移唯一正式存储实现并删除旧入口，不改变证据文件名、JSON 内容、原子替换语义、异常消息、公开 API、模型调用次数、动作次数或终态判定。

### 同类失败与变化样本集合

1. Mapping、`snapshot()` 与 `to_dict()` 三类证据对象必须继续投影为同样的 JSON 对象；不能序列化的对象继续失败关闭。
2. `session.json`、任务图、effect policy、可信观察、Qwen 决策、Controller 决策、验证、动作后迁移、确认失败和报告的文件名及内容必须保持不变。
3. 同名目标已有旧内容时，替换失败必须保留完整旧文件并清理临时文件；路径穿越和非法文件名继续在写盘前拒绝。
4. 变化样本：Controller 或验证证据写入失败时，会话仍按原合同失败并停止后续推进；正式 Web 与能力验收必须由组合根显式注入同一文件存储实现。

### 通用修复

1. 在 `poc/agent/domain` 定义会话证据存储端口和存储错误合同；domain 不导入 JSON、OS 文件替换、Web、Orchestrator 或基础设施实现。
2. 在 `poc/agent/infrastructure` 放置唯一文件系统证据存储实现，完整迁移现有 JSON 投影、文件命名和原子替换行为。
3. Orchestrator 只依赖 domain 端口，并要求 Web、能力验收和测试组合根显式注入存储工厂；不允许在编排层恢复文件系统默认实现。
4. 更新所有调用方后，物理删除 Orchestrator 内嵌 `AgentEvidenceStore` 和旧存储错误定义，不保留重导出、转发门面、双实现或兼容默认值。`CanonicalSelectionReceipt` 仍属于 canonical 选择证据，本批不迁移。

### 影响、回滚与停止条件

- 影响范围：会话证据存储合同与实现的代码位置、Orchestrator 的证据工厂依赖、Web/能力验收/测试组合根装配。
- 不影响：证据文件 schema 与路径、任务图和报告语义、公开 HTTP schema、DeepSeek/Qwen、canonical、Controller、输入账本、设备锁、相机租约、确认边界、真机动作和既有阶段证据。
- 回滚方式：整体撤销本批单一提交；不得通过恢复 Orchestrator 内嵌实现并同时保留新模块来回滚。
- 停止条件：任何测试显示证据文件名/内容、原子失败行为、异常对外状态、物理动作计数或会话终态发生变化时，停止继续迁移并先恢复行为等价。

### 验证清单

- 证据对象投影、完整文件集合、路径穿越、不可序列化和原子替换失败正反变化样本；
- 动作后 Controller/验证证据写入失败、Web 与能力验收组合根回归；
- 静态依赖方向、Orchestrator 旧类/旧默认实现/旧重导出防恢复扫描；
- 一次相关链回归、一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 完成结果

- `poc/agent/domain/session_evidence.py` 现为唯一会话证据存储端口和存储错误合同；`poc/agent/infrastructure/file_system_evidence_store.py` 现为唯一文件系统 JSON 原子持久化实现。
- Orchestrator 内嵌 `AgentEvidenceStore`、旧 `EvidenceStoreError` 定义和文件系统默认工厂已物理删除。Orchestrator 只依赖 domain 端口；正式 Web、能力验收与测试组合根均显式注入 `FileSystemAgentEvidenceStore`，没有重导出、转发门面或双实现。
- Mapping/`snapshot()`/`to_dict()` 投影、完整文件名集合、路径穿越、不可序列化、替换失败保留旧目标及临时文件清理行为保持不变；动作前和动作后证据写入失败仍停止推进。新增 Web 变化样本证明对外仍为 HTTP `409/physical_actions=0`。
- 最小正反、失败停止与防恢复回归 `8/8` 通过；会话、编排、Mock、Web、能力验收相关链 `304/304` 通过；最终完整 Python 回归 `873/873`（`38.553s`）通过。
- 前端纯协议 `11/11`、浏览器合同 `13/13`、阶段 2 控制台浏览器合同 `4/4`，合计 `28/28` 通过。首次直接 Node 命令未取得 Playwright 模块路径，未安装依赖；改用工作区已有 bundle 后，终止本轮并行遗留的精确 Node 测试 PID `19488`，再串行取得上述完整结果。
- 受影响模块静态编译、依赖方向与旧入口扫描、Node 静态检查和 `git diff --check` 通过。

本批完成的是 **会话证据端口与文件系统持久化这一个真实 DDD 切片**。证据 schema、任务图、canonical 单一权威、模型调用、输入事务、设备执行、动作次数和真机语义没有改变；`CanonicalSelectionReceipt` 仍留在 canonical 选择边界。本批没有重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-26 轻量渐进式 DDD 模块化单体第四批（实施前冻结）

### 当前唯一缺口与根因证据

当前 canonical 选择回执 `CanonicalSelectionReceipt` 仍定义在
`universal_agent_orchestrator.py` 尾部，协议版本也由编排器私有常量持有；同一对象却被会话快照、
证据写盘、确认执行、自动循环和失败终态共同消费。这样会让“已经由唯一 canonical 选择器选中一个
动作”的领域事实依附于具体编排实现，并在快照和证据投影处重复拼装相同字段。

根因归类为 **代码组织与依赖方向缺陷**，不是新增动作判断，也不是 canonical、Qwen、Controller、
设备、风险边界或真机行为缺陷。本批只迁移已经存在的选择回执值对象和唯一序列化合同，不改变选择
算法、候选目录、字段名称、协议版本、允许/拒绝结果或任何执行时机。

### 同类失败与变化样本集合

1. `allowed=true` 的动作回执必须继续逐字保存 `reason`、`canonical_class` 和现有协议版本。
2. `allowed=false` 的无动作/能力缺失回执必须继续保留空 `canonical_class`，不能被新值对象升级为动作。
3. 会话快照、单步 Controller 证据和终态证据必须继续投影同一组字段，不能出现两套版本常量或字段拼装。
4. 变化样本：值对象必须可独立构造和序列化；生产代码不得再从 Orchestrator 模块导入或定义同名回执。

### 通用修复

1. 在 `poc/agent/domain/canonical_selection.py` 定义唯一 `CanonicalSelectionReceipt` 值对象和协议版本，
   由值对象自身提供稳定 `to_dict()` 投影。
2. Orchestrator、会话快照和证据写盘直接消费该领域对象；删除编排器尾部旧类、旧版本常量和重复字段拼装。
3. 通过 `agent.domain` 公开该合同；不建立旧模块重导出、兼容 facade、备用默认值或第二份 receipt。
4. 增加防恢复回归，验证 domain/application 依赖方向以及旧 Orchestrator 定义、旧常量和手工投影不能返回。

### 影响、回滚与停止条件

- 影响范围：canonical 选择回执的代码位置和 JSON 投影入口。
- 不影响：公开 HTTP schema、canonical 候选构建/选择、DeepSeek/Qwen、Controller、确认边界、输入账本、
  会话证据文件名、设备执行、模型调用次数、物理动作和既有真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时恢复 Orchestrator 内嵌类并保留新值对象。
- 停止条件：任何测试显示回执字段、协议版本、会话快照、终态、动作选择或物理动作计数发生变化时，
  停止继续迁移并先恢复行为等价。

### 验证清单

- 回执允许/拒绝正反例、稳定序列化和会话快照变化样本；
- Orchestrator、Mock 闭环、Web 与证据写盘相关回归；
- 静态依赖方向、旧 Orchestrator 类/常量/重复投影防恢复扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 完成结果

- `poc/agent/domain/canonical_selection.py` 现为唯一 canonical 选择回执值对象和协议版本合同；
  `CanonicalSelectionReceipt.to_dict()` 统一投影现有四个 JSON 字段。
- Orchestrator 尾部旧 `CanonicalSelectionReceipt` 类和私有版本常量已物理删除；会话快照、Controller
  证据与终态报告直接消费同一个领域对象，没有旧模块重导出、转发门面、备用默认值或第二份字段拼装。
- 允许/拒绝、稳定序列化、终态快照和确认前 scope 消费最小回归 `8/8` 通过；会话模块、
  Orchestrator、Mock 与 Web 相关链 `245/245` 通过。
- 本批唯一完整 Python 回归 `874/874`（`27.126s`）通过；前端纯协议 `11/11`、浏览器合同
  `13/13`、阶段 2 控制台浏览器合同 `4/4`，合计 `28/28` 通过。
- 受影响模块静态编译、Node 静态检查、旧类/旧版本定义扫描和 `git diff --check` 通过。

本批完成的是 **canonical 选择回执这一个真实 DDD 切片**。选择算法、候选目录、证据 schema、
公开 API、模型调用、Controller、确认边界、输入事务、设备执行、动作次数和真机语义没有改变。
本批没有重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-26 轻量渐进式 DDD 模块化单体第五批（实施前冻结）

### 当前唯一缺口与根因证据

动作确认与效果确认的一次性授权作用域仍以内嵌 `ConfirmationAuthority` 和
`EffectConfirmationAuthority` 数据类存在于 `universal_agent_orchestrator.py`。它们持有
task/device/revision/subgoal/effect/observation/decision 的精确绑定以及 `consumed`、
`invalid_reason` 生命周期，却只能通过具体编排模块构造和复用，导致确认领域事实与应用流程耦合。

根因归类为 **代码组织与依赖方向缺陷**。本批不新增确认类别、不改变风险策略；当前只有
`authentication` 和 `financial_transaction` 需要确认的产品边界保持原样。迁移不改变 scope 字段、
排序、一次性消费、失效原因、HTTP 请求体、确认时机或物理动作数量。

### 同类失败与变化样本集合

1. 动作确认 scope 必须继续精确绑定 session/task/device/revision/subgoal/effect_ids、当前
   observation/fingerprint、decision node 和 action digest。
2. 效果确认 scope 必须继续精确绑定同一任务维度、effect_ids 和 intent digest，同时保留只在服务端使用的
   intent preview。
3. 两类 scope 的 `effect_ids` 都必须稳定排序；`consumed` 或 authority 缺失时快照不得再暴露可执行确认。
4. 变化样本：旧 scope 漂移、重复消费、取消、暂停和失败后仍按原失效原因拒绝；生产代码不得再在
   Orchestrator 内定义这两个 authority。

### 通用修复

1. 在 `poc/agent/domain/confirmation_authority.py` 定义两类一次性确认 authority 及唯一 `scope()` 投影。
2. 会话状态、绑定、校验、自动循环、暂停和取消直接消费领域对象；删除 Orchestrator 内嵌类，不提供旧重导出。
3. 把会话字段从 `Any` 收窄为对应领域类型，防止应用流程悄悄写入第二种 scope 结构。
4. 增加领域正反例和防恢复扫描；不把策略判断或 HTTP/Pydantic 校验搬入 domain。

### 影响、回滚与停止条件

- 影响范围：两类确认授权对象的代码位置、类型注解与 scope 投影入口。
- 不影响：风险分类与确认政策、公开 API、DeepSeek/Qwen、canonical、Controller、输入账本、会话证据、
  设备执行、模型调用次数、物理动作和既有真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时恢复内嵌类并保留新领域对象。
- 停止条件：任何回归显示 scope 字段/排序、一次性消费、失效原因、会话状态、确认次数或动作数变化时，
  停止扩大迁移并先恢复行为等价。

### 验证清单

- 两类 scope 正向投影、排序、消费后快照隐藏和漂移/重放变化样本；
- Orchestrator 确认、失败、暂停/取消、Mock 与 Web 相关回归；
- domain 依赖方向和旧内嵌类/旧导入防恢复扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 完成结果

- `poc/agent/domain/confirmation_authority.py` 现为唯一动作确认与效果确认 authority 合同；两类
  `scope()` 均保留原字段和稳定排序，服务端 intent preview 没有泄漏到客户端 scope。
- Orchestrator 内嵌 `ConfirmationAuthority`、`EffectConfirmationAuthority` 已物理删除；会话字段已收窄为
  对应领域类型，绑定、漂移失效、一次性消费、暂停和取消仍直接消费同一对象，没有旧重导出或双实现。
- 两类 scope、排序、消费/漂移、重放、暂停/取消最小回归 `14/14` 通过；会话模块、Orchestrator、
  Mock 与 Web 相关链 `246/246` 通过。
- 本批唯一完整 Python 回归 `875/875`（`28.818s`）通过；前端纯协议 `11/11`、浏览器合同
  `13/13`、阶段 2 控制台浏览器合同 `4/4`，合计 `28/28` 通过。
- 受影响模块静态编译、Node 静态检查、旧内嵌类扫描和 `git diff --check` 通过。

本批完成的是 **动作与效果确认授权作用域这一个真实 DDD 切片**。确认类别、风险政策、scope schema、
公开 API、模型调用、canonical、Controller、输入事务、设备执行、动作次数和真机语义没有改变。
本批没有重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-26 轻量渐进式 DDD 模块化单体第六批（实施前冻结）

### 当前唯一缺口与根因证据

动作回执证明的 App 表面血缘 `VerifiedAppSurfaceLineage` 仍以内嵌值对象存在于
`universal_agent_orchestrator.py`。它精确绑定 session/task/device、产品 App 身份、当前 surface、来源回执/
子目标、真实前台包名和当时物理动作数，并被 Qwen 上下文重绑定、动作后携带、页面身份验证、会话快照和
终态报告共同消费。该领域证明不应由具体编排类拥有。

根因归类为 **代码组织与依赖方向缺陷**，不是 App 身份算法或视觉判断缺陷。本批只迁移现有不可变血缘
值对象及其 JSON 投影，不改变任何匹配、升级、失效、前台包桥接、canonical 或任务完成规则。

### 同类失败与变化样本集合

1. 血缘必须继续完整保存 10 个现有字段，`to_dict()` 字段名和值保持不变。
2. 同 session/task/device、来源回执/子目标和物理动作数约束继续由现有应用流程检查；迁移不得放宽。
3. 变化样本：产品 App ID 与真实运行包名不同仍可由同一已验证血缘桥接；Launcher、不同包或不同动作数
   仍不得复用。
4. 生产代码不得再在 Orchestrator 定义或从其导入 `VerifiedAppSurfaceLineage`。

### 通用修复

1. 在 `poc/agent/domain/app_surface_lineage.py` 定义唯一不可变血缘值对象与 `to_dict()`。
2. 会话状态和所有血缘消费点直接依赖领域对象；删除 Orchestrator 内嵌类，不提供重导出或兼容 facade。
3. 保留现有 lineage 匹配/携带/刷新应用逻辑原位，避免把编排判断错误包装成领域第二权威。
4. 增加字段投影、不可变性、运行包变化样本和防恢复扫描。

### 影响、回滚与停止条件

- 影响范围：已验证 App 表面血缘值对象的代码位置、类型入口与序列化。
- 不影响：App/surface 匹配算法、DeepSeek/Qwen、canonical、Controller、会话证据、确认边界、输入账本、
  设备执行、模型调用次数、物理动作和既有真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时恢复内嵌类并保留领域对象。
- 停止条件：字段/schema、血缘携带/失效、前台包匹配、会话终态或动作数任一变化时，停止迁移并恢复行为等价。

### 验证清单

- 10 字段投影、不可变性、产品 ID/运行包桥接和 Launcher/不同包反例；
- App 表面血缘、Orchestrator、Mock、Web 与会话快照相关回归；
- domain 依赖方向和旧内嵌类/旧导入防恢复扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 完成结果

- `poc/agent/domain/app_surface_lineage.py` 现为唯一不可变 `VerifiedAppSurfaceLineage` 值对象和
  10 字段 JSON 投影合同。
- Orchestrator 内嵌血缘类已物理删除；Qwen 上下文重绑定、动作后携带、页面身份验证、会话快照和终态
  报告继续消费同一个领域对象，没有旧重导出、转发 facade 或双实现。
- 字段投影、不可变性、产品 ID/运行包桥接、Launcher/不同包反例和跨动作携带最小回归 `15/15`
  通过；会话模块、Orchestrator、Mock 与 Web 相关链 `247/247` 通过。
- 本批唯一完整 Python 回归 `876/876`（`29.144s`）通过；前端纯协议 `11/11`、浏览器合同
  `13/13`、阶段 2 控制台浏览器合同 `4/4`，合计 `28/28` 通过。
- 受影响模块静态编译、Node 静态检查、旧内嵌类扫描和 `git diff --check` 通过。

本批完成的是 **已验证 App 表面血缘这一个真实 DDD 切片**。App/surface 匹配与升级规则、证据 schema、
公开 API、模型调用、canonical、Controller、确认边界、输入事务、设备执行、动作次数和真机语义没有改变。
本批没有重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-26 轻量渐进式 DDD 模块化单体第七批（实施前冻结）

### 当前唯一缺口与根因证据

真实运行中的 `UniversalAgentSessionState` 仍定义在 `universal_agent_orchestrator.py`，同时持有任务图、
当前观察/决策、canonical 回执、两类确认 authority、证据存储端口、App 血缘、输入 authority、动作计数、
自动循环状态、失败原因和对外快照。它是应用会话聚合，却与具体编排算法同文件，并私有持有动作后迁移和
纠正重试两个报告协议版本，使会话查询、持久化和 Web 投影都必须依赖整个 Orchestrator 模块。

根因归类为 **应用会话聚合与编排实现耦合**。本批只迁移会话状态、快照和两项报告版本，不改变字段默认值、
状态名、快照 schema、可确认判定、动作能力投影、证据路径去重、时间格式、模型调用或任何推进逻辑。

### 同类失败与变化样本集合

1. 新建会话的默认 status/step/action count、创建时间和空 scope 必须保持不变。
2. 允许的 canonical 回执与未消费 action authority 同时存在时才暴露 `confirmation_ready=true`；消费后隐藏。
3. effect authority、App 血缘、Qwen usage、capability、history/evidence 去重和动作后协议字段必须保持原快照。
4. 变化样本：adapter 报告动作能力时使用设备集合；没有该端口时仍使用 canonical `SUPPORTED_ACTIONS`。
5. 生产代码不得再在 Orchestrator 定义或从其导入 `UniversalAgentSessionState`；Web/测试组合根直接从
   application 边界取得类型。

### 通用修复

1. 在 `poc/agent/application/runtime_session.py` 定义唯一运行会话聚合、快照和两个既有报告版本。
2. Orchestrator、Web、能力验收和测试直接依赖 application 导出；物理删除 Orchestrator 内嵌类和私有版本定义，
   不保留重导出或 facade。
3. 聚合只依赖 domain 端口/值对象和现有纯合同模块，不导入 Web、FastAPI、Pydantic、infrastructure、Robot
   或模型 SDK；编排器仍负责推进和失败决策。
4. 增加默认状态、完整快照、scope 消费、动作能力变化和防恢复依赖测试。

### 影响、回滚与停止条件

- 影响范围：运行会话聚合、快照和报告版本的代码位置与导入入口。
- 不影响：公开快照字段、状态机推进、DeepSeek/Qwen、canonical、Controller、确认政策、输入账本、证据文件、
  设备执行、模型调用次数、物理动作和既有真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时恢复内嵌类和 application 聚合。
- 停止条件：任一快照字段/默认值、确认可用性、证据路径、状态终态、动作数或模型调用数变化时停止并恢复行为等价。

### 验证清单

- 新会话默认状态、完整快照、scope 消费前后、adapter 能力/默认能力和证据去重正反例；
- 应用服务、Orchestrator、Mock、Web、能力验收和持久化相关回归；
- application 依赖方向和旧内嵌类/旧版本/旧导入防恢复扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 完成结果

- `poc/agent/application/runtime_session.py` 现为唯一 `UniversalAgentSessionState` 运行会话聚合、快照和
  两项既有报告协议版本的定义位置。
- Orchestrator 内嵌会话类和私有版本定义已物理删除；Orchestrator 只编排 application 聚合，测试也从
  application 边界导入类型，没有旧类、旧版本默认值或第二份快照实现。
- 新会话默认状态、完整快照、scope 消费前后、动作能力、证据去重和协议字段最小回归 `11/11` 通过；
  应用服务、Orchestrator、Mock 与 Web 相关链 `248/248` 通过。
- 本批唯一完整 Python 回归 `877/877`（`45.151s`）通过；前端纯协议 `11/11`、浏览器合同
  `13/13`、阶段 2 控制台浏览器合同 `4/4`，合计 `28/28` 通过。
- 受影响模块静态编译、Node 静态检查、旧内嵌类/旧版本扫描和 `git diff --check` 通过。

本批完成的是 **通用 Agent 运行会话聚合这一个真实 DDD 切片**。快照 schema、状态推进、模型调用、
canonical、Controller、确认政策、输入事务、证据文件、设备执行、动作次数和真机语义没有改变。
本批没有重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-26 轻量渐进式 DDD 模块化单体第八批（实施前冻结）

### 当前唯一缺口与根因证据

`web_app.py` 仍直接实现 `DeviceCameraCoordinator` 和 `CameraPreviewUnavailable`：Web 接口层因此持有
线程锁、正式 Agent 相机租约、JPEG 预览缓存和非阻塞预览退化逻辑。相同协调器被正式 Agent、runtime
doctor 和多个浏览器预览共同消费，它是相机基础设施边界，不是 HTTP 路由职责。

根因归类为 **接口层内嵌基础设施实现**。本批只迁移唯一相机协调器实现和异常合同，不改变正式四帧采集、
锁粒度、可重入性、JPEG quality、缓存更新、cache-only 行为、预览是否等待或任何模型/机械动作语义。

### 同类失败与变化样本集合

1. 正式 Agent 持有 `serial_session` 时，同线程 Agent capture 可重入并更新最近 JPEG 缓存。
2. 另一线程持有租约时，网页 preview 不排队，已有缓存立即返回并标记 cached；没有缓存时抛出同一异常。
3. 空闲 preview 继续调用真实 capture、按请求 quality 返回并更新缓存。
4. 变化样本：不同设备仍由 Runtime 建立独立 coordinator，不能共享锁或缓存。
5. Web 与测试不得再从 `web_app.py` 定义/导入协调器实现，生产实现只能存在于 infrastructure。

### 通用修复

1. 在 `poc/agent/infrastructure/camera_coordinator.py` 放置唯一协调器与异常实现。
2. Web 组合根从 infrastructure 注入每设备实例；物理删除 Web 内嵌类和 `BytesIO` 基础设施依赖。
3. 更新测试直接依赖 infrastructure 边界，不保留 Web 转发类、兼容 facade 或第二份缓存。
4. 增加跨线程缓存、无缓存异常、设备隔离和旧 Web 定义防恢复回归。

### 影响、回滚与停止条件

- 影响范围：相机串行租约、预览缓存与异常合同的代码位置和组合根导入。
- 不影响：公开预览/doctor/API schema、四帧阈值、Qwen 调用次数、canonical、Controller、输入账本、设备
  执行、物理动作和既有真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时恢复 Web 内嵌实现并保留 infrastructure 实现。
- 停止条件：正式采集未更新缓存、网页等待租约、无缓存不再失败、跨设备串扰或 API 响应变化时停止并恢复。

### 验证清单

- 空闲/独占/无缓存/跨线程/设备隔离正反例；
- Web preview、runtime doctor、adapter、Orchestrator 与浏览器相关回归；
- infrastructure 依赖方向和旧 Web 类/`BytesIO` 定义防恢复扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 完成结果

- `poc/agent/infrastructure/camera_coordinator.py` 现为唯一 `DeviceCameraCoordinator` 和
  `CameraPreviewUnavailable` 实现；正式租约、JPEG 缓存和非阻塞 preview 逻辑完整迁移。
- `web_app.py` 已物理删除内嵌协调器、异常和 `BytesIO` 依赖；Web 组合根为每设备建立独立 infrastructure
  实例，测试直接依赖新边界，没有 Web 转发类、兼容 facade 或第二份缓存。
- 空闲/独占/无缓存/跨线程/跨设备最小回归 `5/5` 通过；Web preview、doctor、adapter 和 Orchestrator
  相关链 `381/381` 通过。
- 本批唯一完整 Python 回归 `880/880`（`29.671s`）通过；前端纯协议 `11/11`、浏览器合同
  `13/13`、阶段 2 控制台浏览器合同 `4/4`，合计 `28/28` 通过。
- 受影响模块静态编译、Node 静态检查、旧 Web 类/`BytesIO` 扫描和 `git diff --check` 通过。

本批完成的是 **每设备相机串行租约与预览缓存这一个真实 DDD 基础设施切片**。公开 API、四帧阈值、
模型调用、canonical、Controller、输入事务、设备执行、动作次数和真机语义没有改变。本批没有重载项目
API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-26 轻量渐进式 DDD 模块化单体第九批（实施前冻结）

### 当前唯一缺口与根因证据

`web_app.py` 仍内嵌 `DeviceControllerRegistry`：HTTP 接口层因此直接读取和校验设备 JSON、解析标定路径、
构造 Robot/Mock 控制器、维护设备描述，并为能力验收复制一台只增加候选动作的临时控制器。它同时被正式
Agent、预览、runtime doctor 和能力验收组合根消费，是设备控制器基础设施边界，不是 Web 路由职责。

根因归类为 **接口层内嵌设备基础设施实现**。本批只迁移唯一设备注册与控制器工厂，删除 Web 旧类；不改变
注册表 schema、默认设备、窗口唯一性、相对标定路径解析、verified actions、Mock 行为、候选动作约束、
公开 API、模型调用、canonical、Controller 回执或机械动作。

### 同类失败与变化样本集合

1. 正常注册表继续按 `default_device_id` 构造唯一控制器并返回稳定排序的设备描述。
2. 空/重复设备、重复控制窗口、无效 `verified_actions`、缺失默认设备仍在控制器或模型工作前失败。
3. 未知设备继续失败并保留原中文原因；Web 对外仍映射为相同状态和 detail。
4. 能力验收临时控制器只增加一个已批准候选动作，不修改正式注册表控制器；未知、不可晋级或已验证动作失败。
5. 变化样本：Mock 注册表与多个不同窗口设备保持隔离；测试直接依赖 infrastructure，不得从 Web 取得实现。

### 通用修复

1. 在 `poc/agent/infrastructure/device_controller_registry.py` 放置唯一注册表、控制器构造和内部错误合同。
2. 可晋级动作集合由 Web 组合根显式注入，infrastructure 不反向导入能力验收或 Orchestrator。
3. Web 只装配和消费注册表，并把基础设施错误纳入现有 HTTP 失败投影；物理删除内嵌类，不保留重导出、
   转发 facade 或第二份设备缓存。
4. 更新能力验收和 Web 测试直接依赖新边界，增加依赖方向、旧类与旧错误依赖防恢复扫描。

### 影响、回滚与停止条件

- 影响范围：设备 JSON 注册表、Robot/Mock 控制器构造、设备描述和能力验收临时控制器的代码位置与注入。
- 不影响：注册表文件内容、HTTP schema/status/detail、动作能力、相机、Qwen/DeepSeek、canonical、输入事务、
  设备租约、物理动作和既有真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时恢复 Web 内嵌类并保留 infrastructure 实现。
- 停止条件：默认设备、异常时机/对外投影、临时控制器隔离或控制器动作集合发生变化时停止并恢复。

### 验证清单

- 正常、多设备、Mock、无效注册表、未知设备和临时控制器正反例；
- Web、能力验收、runtime doctor、adapter 与 Orchestrator 相关回归；
- infrastructure 依赖方向和旧 Web 类/旧反向导入防恢复扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 完成结果

- `poc/agent/infrastructure/device_controller_registry.py` 现为唯一设备注册表、Robot/Mock 控制器构造、
  设备描述和临时能力验收控制器实现；可晋级动作集合由 Web 组合根显式注入。
- `web_app.py` 已物理删除内嵌 `DeviceControllerRegistry`，不再读取设备 JSON、解析标定路径或复制控制器；
  测试直接依赖 infrastructure，没有 Web 重导出、转发 facade、第二份控制器缓存或反向 Orchestrator 依赖。
- 正常、多设备、Mock、无效注册表、未知设备和临时控制器正反例最小回归 `8/8` 通过；Web、能力验收、
  doctor、adapter 与 Orchestrator 相关链 `409/409` 通过。
- 本批唯一完整 Python 回归 `883/883`（`29.269s`）通过；前端纯协议 `11/11`、浏览器合同
  `13/13`、阶段 2 控制台浏览器合同 `4/4`，合计 `28/28` 通过。

本批完成的是 **设备注册表与控制器构造这一个真实 DDD 基础设施切片**。公开 API、设备配置、动作能力、
模型调用、canonical、Controller 回执、输入事务、动作次数和真机语义没有改变。本批没有重载项目 API、
调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-26 轻量渐进式 DDD 模块化单体第十批（实施前冻结）

### 当前唯一缺口与根因证据

`web_app.Runtime` 仍直接持有两组 `RLock + dict`：一组按 `device_id` 延迟创建动作/观察协调锁，另一组按
设备创建 `DeviceCameraCoordinator`。这两组资源共同决定同设备串行、不同设备独立和预览是否只读缓存，
是每设备运行资源基础设施，不是 HTTP、Pydantic 或组合根状态。

根因归类为 **接口层内嵌并发资源注册表**。本批只迁移每设备协调锁与相机协调器实例表，删除 Web 的四个
字典/guard 字段及两个旧查询方法；不改变锁类型、延迟创建、默认设备预创建、相机缓存、占用判断、公开
API、模型调用、canonical、Controller 或机械动作。

### 同类失败与变化样本集合

1. 同一设备重复查询必须返回同一把非可重入动作协调锁和同一个相机协调器。
2. 不同设备必须取得不同锁和不同相机缓存，互不阻塞、互不泄漏 JPEG。
3. 默认设备在 Runtime 组合时预创建；新设备仍按需安全创建。
4. 空 `device_id` 继续在创建资源前失败，不能生成共享的空键资源。
5. 变化样本：测试用新注册表替换旧实例即可清空缓存，不再触碰 Web 内部 guard/dict。

### 通用修复

1. 在 `poc/agent/infrastructure/device_runtime_resources.py` 放置唯一每设备协调资源注册表和错误合同。
2. Web 组合根只创建一个注册表；正式采集、预览、doctor 和硬件租约直接消费该基础设施边界。
3. 物理删除 Runtime 的两组 guard/dict 和旧查询方法；测试直接替换 infrastructure 实例，不保留旧字段、
   兼容属性、转发 facade 或第二份资源表。
4. 增加同设备复用、跨设备隔离、并发创建、空设备和旧字段防恢复回归。

### 影响、回滚与停止条件

- 影响范围：每设备动作协调锁和相机协调器的创建、持有与查询位置。
- 不影响：锁/缓存行为、相机采集、HTTP schema/status/detail、Qwen/DeepSeek、canonical、输入事务、设备
  执行、物理动作和既有真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时恢复 Web 字典并保留 infrastructure 注册表。
- 停止条件：同设备资源不稳定、跨设备串扰、预览等待行为或错误投影变化时停止并恢复。

### 验证清单

- 同设备、跨设备、并发创建、空设备、缓存隔离和 Runtime 注入正反例；
- Web preview、doctor、硬件锁、adapter 与 Orchestrator 相关回归；
- infrastructure 依赖方向和旧 Runtime 字段/方法防恢复扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 完成结果

- `poc/agent/infrastructure/device_runtime_resources.py` 现为唯一每设备动作协调锁和相机协调器实例注册表；
  默认设备预创建、新设备延迟创建、同设备稳定复用和跨设备隔离保持不变。
- Web Runtime 已物理删除两组 guard/dict 和两个旧查询方法；采集、预览、doctor 和硬件租约直接消费同一
  infrastructure 实例，测试通过替换注册表复位，不存在兼容属性、转发 facade 或第二份资源表。
- 同设备、跨设备、并发创建、空设备、缓存隔离和 Runtime 注入最小回归 `10/10` 通过；Web、能力验收、
  doctor、adapter 与 Orchestrator 相关链 `413/413` 通过。
- 本批唯一完整 Python 回归 `887/887`（`28.633s`）通过；前端纯协议 `11/11`、浏览器合同
  `13/13`、阶段 2 控制台浏览器合同 `4/4`，合计 `28/28` 通过。

本批完成的是 **每设备运行协调资源这一个真实 DDD 基础设施切片**。公开 API、锁/缓存行为、模型调用、
canonical、Controller 回执、输入事务、动作次数和真机语义没有改变。本批没有重载项目 API、调用在线
模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-26 轻量渐进式 DDD 模块化单体第十一批（实施前冻结）

### 当前唯一缺口与根因证据

根目录 `semantic_action.py` 与 `ui_scene.py` 定义正式运行链共同使用的设备无关动作值对象、UI 元素、场景、
系统 UI 事实、相机对齐事实和场景不变量，却仍作为平铺模块被 canonical、Qwen、观察器、Controller、
Orchestrator、Web 和测试横向导入。两者只依赖标准库，属于同一视觉场景领域语言，不是接口或基础设施。

根因归类为 **核心领域合同仍位于平铺运行入口**。本批只移动原文件并统一导入，物理删除根目录旧入口；
不改协议版本、字段、校验阈值、序列化、动作名称、场景分类、模型调用、canonical 候选或机械语义。

### 同类失败与变化样本集合

1. `SemanticAction` 的冻结字段、默认 params 和 `to_dict()` 必须逐字节保持。
2. UIScene/UIElement/SystemUIFacts/CameraAlignmentFacts 的正反校验与 JSON 投影保持。
3. canonical、Qwen、观察器、Controller 与 Orchestrator 必须消费同一领域类型，不能因旧/新类并存导致
   `isinstance` 或序列化分裂。
4. 变化样本：系统最近任务、相机对齐、低置信局部元素和普通输入场景继续使用同一类型合同。
5. 根目录旧文件、旧导入和重导出 facade 必须全部消失。

### 通用修复

1. 将原文件原样迁到 `poc/agent/domain/semantic_action.py` 和 `poc/agent/domain/ui_scene.py`。
2. 所有生产、测试和 Web 入口直接导入 domain 子模块；不保留根目录转发文件或 `sys.modules` 别名。
3. 增加类型唯一性、domain 纯依赖、旧文件/旧导入防恢复扫描。

### 影响、回滚与停止条件

- 影响范围：视觉场景与语义动作领域类型的模块路径及全部导入点。
- 不影响：任何字段、协议、阈值、API、DeepSeek/Qwen、canonical、Controller、输入事务、设备执行、物理
  动作和既有真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时恢复根文件并保留 domain 文件。
- 停止条件：任何序列化、类型身份、场景校验、候选数量或动作结果变化时停止并恢复。

### 验证清单

- SemanticAction、UIElement、UIScene、系统 UI、相机对齐和最近任务正反例；
- canonical、Qwen、observer、Controller、adapter、Orchestrator 与 Web 相关回归；
- domain 标准库依赖和旧根文件/旧导入防恢复扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 完成结果

- 原 `semantic_action.py` 与 `ui_scene.py` 已原样迁入 `poc/agent/domain/`，根目录旧文件物理删除；canonical、
  Qwen、观察器、Controller、Orchestrator、Web 和测试全部直接依赖 domain 子模块，没有转发或类型双份。
- 类型唯一性、SemanticAction、UIElement、UIScene、系统 UI、相机对齐、canonical 和最近任务正反例
  `103/103` 通过；视觉决策—控制—编排相关链 `613/613` 通过。
- 首次完整回归只发现 `test_fixed_app_retirement.py` 仍读取已删除的旧路径；更新为新领域路径后，该防恢复
  测试通过，最终完整 Python 回归 `888/888`（`31.093s`）通过。
- 前端纯协议 `11/11`、浏览器合同 `13/13`、阶段 2 控制台浏览器合同 `4/4`，合计 `28/28` 通过。

本批完成的是 **视觉场景与语义动作领域语言这一个真实 DDD 切片**。协议版本、字段、阈值、序列化、模型
调用、canonical 候选、Controller、输入事务、动作次数和真机语义没有改变。本批没有重载项目 API、调用
在线模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-26 轻量渐进式 DDD 模块化单体第十二批（实施前冻结）

### 当前唯一缺口与根因证据

根目录 `text_input_utils.py` 与 `verified_text_transaction.py` 分别定义唯一文字规范化/字符计数和确定性键盘
事务规划，却仍由 canonical、观察器、Controller、Orchestrator、Robot 和能力验收横向导入。前者只依赖
标准库，后者只依赖纯确定性 `pypinyin` 与前者，属于同一文字事务领域切片。

根因归类为 **唯一输入规划权威仍位于平铺运行入口**。本批只原样移动两个模块并统一导入，物理删除根目录
旧文件；不改文字白名单、长度、NFC、字符计数、拼音、分段上限、键盘布局路径、步骤字段、候选数量、
输入动作次数或机械执行。

### 同类失败与变化样本集合

1. 普通英文、空格、LF、大写、数字、符号和中文的确定性步骤保持逐字段相同。
2. `normalize_user_text` 的 NFC、长度、控制符和不支持字符正反例保持。
3. `editable_character_count` 的英文空白/撇号与中文组合字符计数保持。
4. canonical、Controller、Robot 与 Orchestrator 必须消费同一 `VerifiedInputStep` 类型和异常合同。
5. 变化样本：混合中英文、换行、多字段续接和英文预编辑继续只使用这一 planner。

### 通用修复

1. 将原文件迁到 `poc/agent/domain/text_input_utils.py` 与 `poc/agent/domain/verified_text_transaction.py`，
   后者改为领域内相对导入。
2. 所有生产和测试直接导入 domain 子模块；删除根文件，不保留重导出、兼容别名或第二套 planner。
3. 更新当前协议/验收文档中的唯一权威路径，并增加类型身份、纯依赖和旧导入防恢复回归。
4. `input_value_lineage.py` 的领域/持久化混合留到下一独立批次拆分，本批不把磁盘实现带入 domain。

### 影响、回滚与停止条件

- 影响范围：文字规范化、字符计数和 verified input planner 的模块路径与导入点。
- 不影响：输入规则、payload、动作/步骤数量、API、Qwen/DeepSeek、canonical 选择、Controller、设备执行、
  物理动作和既有真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时恢复根文件并保留 domain 文件。
- 停止条件：任一输入步骤、异常、计数、布局选择、候选或物理动作数量变化时停止并恢复。

### 验证清单

- 文字规范化/计数、英文/空格/LF/大写/数字/符号/中文和混合输入正反例；
- canonical、lineage、observer、Controller、Robot、Orchestrator 与能力验收相关回归；
- domain 纯依赖和旧根文件/旧导入/旧文档路径防恢复扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 完成结果

- 原 `text_input_utils.py` 与 `verified_text_transaction.py` 已迁入 `poc/agent/domain/`，根目录旧文件物理
  删除；唯一 planner 使用领域内相对导入，所有生产和测试调用方直接依赖 domain 子模块。
- 类型唯一性、文字规范化/计数、英文/空格/LF/大写/数字/符号/中文和混合输入正反例 `15/15` 通过；
  输入—canonical—控制—编排相关链 `623/623` 通过。
- 首次完整回归只发现旧链退役测试仍读取已删除的根 `text_input_utils.py`；更新到新 domain 路径后该检查
  通过，最终完整 Python 回归 `889/889`（`33.578s`）通过。
- 当前协议、输入验收和交接文档中的唯一权威路径已统一；前端纯协议 `11/11`、浏览器合同 `13/13`、
  阶段 2 控制台浏览器合同 `4/4`，合计 `28/28` 通过。

本批完成的是 **确定性文字事务规划这一个真实 DDD 领域切片**。文字规则、步骤、payload、模型调用、
canonical、Controller、动作次数和真机语义没有改变。本批没有重载项目 API、调用在线模型、执行真机
动作或操作卖家 `main.exe`。

## 2026-08-26 轻量渐进式 DDD 模块化单体第十三批（实施前冻结）

### 当前唯一缺口与根因证据

根目录 `input_value_lineage.py` 同时定义 typed input lineage 的精确值/字段/表面身份规则、PIL 局部画面
描述与匹配，以及按设备写入 JSON 的原子文件存储。业务记录、应用匹配和基础设施持久化被一个平铺模块
共同拥有，`generic_action_adapter`、观察器、Controller 和 Web 组合根因此直接依赖同一个混合入口。

根因归类为 **输入连续性业务规则与技术实现未分层**。本批只拆分并迁移这一条正式运行切片，物理删除
根目录旧入口；不改 lineage 版本、TTL、摘要、字段、bounds 阈值、局部画面算法、输入值恢复条件、
pending/persisted 来源、动作回执、公开 API、模型调用或机械动作。

### 同类失败与变化样本集合

1. 英文、中文预编辑、候选提交、键盘状态、换行和逐字符输入必须生成与原来逐字段一致的 lineage。
2. 同一 typed `input_field_id`、App/screen 身份、bounds、TTL 与精确可见 cue 的正反匹配保持不变。
3. 持久记录仍必须绑定动作后四帧，损坏 JSON、过期记录和写入失败继续 fail closed。
4. 变化样本：占位文字消失、键盘遮挡、soft wrap、尾随换行和 App 标签由 unknown 恢复的既有样本继续
   只由同一 lineage 合同裁决。
5. 根目录旧模块、旧导入、转发 facade、第二个存储类或第二套 surface matcher 必须全部消失。

### 通用修复

1. `agent/domain/input_value_lineage.py` 只保存记录、序列化、身份/字段/TTL/bounds 业务规则和确定性构建；
   不导入 PIL、Path、文件系统、Web、模型 SDK、相机或机械臂。
2. `agent/application/input_value_lineage.py` 声明存储端口，并把当前图像帧转换为领域层只接收的
   `surface_matches` 事实；不访问 Web 或 infrastructure。
3. `agent/infrastructure/file_system_input_lineage_store.py` 实现按设备 JSON 原子存储和四帧描述生成；Web
   组合根只注入这一实现，adapter/observer 只依赖 application 端口。
4. 所有调用方直接使用新分层模块；删除根文件，不保留重导出、兼容别名、双写或第二权威。

### 影响、回滚与停止条件

- 影响范围：typed input lineage 的模块路径、图像匹配适配和文件存储装配位置。
- 不影响：协议字段/版本、匹配阈值、输入规划、canonical、Qwen/DeepSeek、Controller、公开 API、动作次数、
  设备执行和既有真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时恢复平铺模块并保留新分层实现。
- 停止条件：任一记录 JSON、匹配布尔值、异常类型、写入原子性、候选数量或动作结果变化时停止并恢复。

### 验证清单

- lineage 全部构建、序列化、TTL、identity、bounds、surface、cue、preedit、newline 与持久化正反例；
- observer、adapter、Controller、Web 和复杂输入历史失败相关回归；
- domain 无 PIL/文件系统、application 不依赖 infrastructure、旧根入口/导入与双实现防恢复扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 完成结果

- `poc/agent/domain/input_value_lineage.py` 现为唯一 lineage 记录、精确值/字段/身份/TTL/bounds 规则和
  确定性构建位置；它不导入 PIL、Path、文件系统、Web、模型 SDK、相机或机械臂。
- `poc/agent/application/input_value_lineage.py` 声明唯一存储端口并保留原局部画面描述/阈值算法，只把
  当前帧归约为 `surface_matches` 事实；`poc/agent/infrastructure/file_system_input_lineage_store.py`
  是唯一按设备 JSON 原子存储和四帧描述实现，Web 组合根显式注入该实现。
- 根目录 `poc/input_value_lineage.py` 已物理删除；adapter、observer、Controller、Web 和测试直接依赖
  对应新层，不存在旧导入、转发 facade、双写、第二存储类或第二套 surface matcher。
- lineage 构建/序列化/TTL/identity/bounds/surface/cue/preedit/newline/持久化与分层防恢复最小回归
  `54/54` 通过；observer、adapter、Controller、Web 和复杂输入相关链 `512/512` 通过。
- 首次完整回归只有既有 Mock 导航四帧稳定采集在限定时间内偶发超时；同一用例在相关链已通过，独立
  复验 `18/18` 通过，未修改生产代码。最终完整 Python 回归 `890/890`（`36.666s`）通过。
- 前端纯协议 `11/11`、浏览器合同 `13/13`、阶段 2 控制台浏览器合同 `4/4`，合计 `28/28` 通过；
  静态编译、依赖方向、旧入口/旧导入扫描和 `git diff --check` 通过。

本批完成的是 **typed input lineage 分层这一个真实 DDD 切片**。lineage 版本、TTL、字段、摘要、bounds、
局部画面算法、输入值恢复、公开 API、模型调用、canonical、Controller、动作次数和真机语义没有改变。
本批没有重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-26 轻量渐进式 DDD 模块化单体第十四批（实施前冻结）

### 当前唯一缺口与根因证据

根目录 `action_capabilities.py` 定义设备动作能力全集、不可变快照、稳定摘要和结构化能力缺口，却仍由
adapter、runtime doctor 和 Orchestrator 通过平铺模块导入，后两处还在运行方法内部动态导入。该模块只
依赖标准库，内容是设备无关的能力值对象和不变量，不是 Web、模型、相机或机械执行实现。

根因归类为 **动作能力领域合同仍位于平铺运行入口**。本批只原样迁移这一合同并统一导入，物理删除
根文件；不改协议版本、已知动作集合、默认 gap reason、设备 profile 合并、摘要、公开 API、模型调用、
canonical 候选、Controller 或机械动作。

### 同类失败与变化样本集合

1. 同一能力集合与不同输入顺序必须生成相同 `profile_digest`。
2. 支持动作继续返回无 gap；不支持动作继续返回绑定设备、参数和 profile 的 typed gap。
3. 未知动作、非法设备 ID、重复或未知 supported action、损坏协议继续 fail closed。
4. 变化样本：double tap、换行、pinch、hardware key 和普通 tap/swipe/input 的默认 gap 保持逐字段相同。
5. 根目录旧文件、旧绝对/动态导入、重导出 facade 和第二份动作能力全集必须全部消失。

### 通用修复

1. 将原文件原样迁入 `poc/agent/domain/action_capabilities.py`。
2. adapter、doctor、Orchestrator 和测试直接依赖 domain 子模块；把运行方法内动态导入改为模块级唯一导入。
3. 删除根文件，不保留转发、兼容别名或第二套常量，并增加类型身份、纯依赖和旧导入防恢复回归。

### 影响、回滚与停止条件

- 影响范围：动作能力值对象、构建函数和常量的模块路径与导入位置。
- 不影响：任何字段、协议、摘要、能力开关、API、Qwen/DeepSeek、canonical、Controller、设备 transport、
  物理动作和既有真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时恢复根文件并保留 domain 文件。
- 停止条件：任一快照/缺口 JSON、摘要、异常、supported actions 或动作结果变化时停止并恢复。

### 验证清单

- 动作能力快照、摘要、gap、非法输入和默认原因正反例；
- adapter、runtime doctor、Orchestrator、Web 和能力验收相关回归；
- domain 纯依赖与旧根文件/旧导入/局部动态导入防恢复扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 完成结果

- 原 `poc/action_capabilities.py` 已原样迁入 `poc/agent/domain/action_capabilities.py`，根文件物理删除；
  adapter、runtime doctor、Orchestrator 和测试直接依赖 domain 子模块。
- adapter 与 Orchestrator 运行方法内的三处动态导入已删除，模块级只保留一个正式构建函数身份；不存在
  转发文件、兼容别名、第二份 `KNOWN_ACTION_CAPABILITIES` 或第二套摘要/gap 实现。
- 动作能力快照、稳定摘要、typed gap、非法输入、默认 gap reason 与防恢复最小回归 `26/26` 通过；
  adapter、doctor、Orchestrator、能力验收与 Web 相关链 `475/475` 通过。
- 本批唯一完整 Python 回归 `891/891`（`40.466s`）通过；前端纯协议 `11/11`、浏览器合同 `13/13`、
  阶段 2 控制台浏览器合同 `4/4`，合计 `28/28` 通过；静态编译、domain 依赖、旧入口/导入扫描和
  `git diff --check` 通过。

本批完成的是 **设备动作能力这一个真实 DDD 领域切片**。协议、已知动作、默认缺口、profile 合并、摘要、
公开 API、模型调用、canonical、Controller、设备 transport、动作次数和真机语义没有改变。本批没有
重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-26 轻量渐进式 DDD 模块化单体第十五批（实施前冻结）

### 当前唯一缺口与根因证据

正式 canonical 可执行动作全集 `SUPPORTED_ACTIONS` 仍定义在平铺 `canonical_action_protocol.py`，而
`agent/domain/device_execution.py` 与 `agent/application/runtime_session.py` 反向导入该平铺模块取得集合。
这既违反 inward dependency，也使 canonical 导入 domain 子模块时经 package 初始化再次回到 canonical，
形成阻碍 task semantic IR 后续迁移的真实循环依赖。

根因归类为 **唯一动作种类词汇仍由外层目录拥有**。本批只迁移这一不可变集合并统一全部调用方，删除
canonical 内旧定义；不改集合成员、顺序投影、协议版本、候选编译、Qwen 允许动作、设备执行、公开 API、
模型调用或机械动作。

### 同类失败与变化样本集合

1. canonical catalog、Qwen、observer、Orchestrator、会话快照和设备执行必须引用同一个集合对象。
2. 14 个正式动作成员保持完全一致，pinch/hardware_key 仍只属于能力全集而非 canonical 可执行集合。
3. 未知动作继续在 proposal、affordance、candidate、catalog 和设备执行边界被拒绝。
4. 变化样本：文字、换行、最近任务、普通导航与只读等待动作的支持状态保持。
5. canonical 内旧集合定义、domain/application 对平铺 canonical 的反向导入和第二份集合必须全部消失。

### 通用修复

1. 在 `poc/agent/domain/canonical_action_kinds.py` 定义唯一 `CANONICAL_ACTION_KINDS`。
2. canonical 编译器、Qwen、observer、Orchestrator、runtime session 和 device execution 直接依赖该领域集合。
3. 删除 `canonical_action_protocol.SUPPORTED_ACTIONS`，不保留别名、重导出或兼容集合，并增加对象身份、
   成员、依赖方向和旧定义防恢复回归。

### 影响、回滚与停止条件

- 影响范围：canonical 可执行动作集合的定义位置、符号名和导入点。
- 不影响：集合内容、动作能力全集、候选/控制算法、API、Qwen/DeepSeek、输入事务、设备 transport、动作
  次数、风险边界和既有真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时恢复 canonical 内集合并保留 domain 集合。
- 停止条件：任一支持动作、候选数量、快照、异常、设备执行或动作结果变化时停止并恢复。

### 验证清单

- 14 项成员、唯一对象身份、未知动作和 capability/canonical 差集正反例；
- canonical、Qwen、observer、device execution、Orchestrator、Web 相关回归；
- domain/application 无平铺 canonical 反向导入与旧集合定义防恢复扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 完成结果

- `poc/agent/domain/canonical_action_kinds.py` 现为唯一 14 项 canonical 可执行动作集合；原
  `canonical_action_protocol.SUPPORTED_ACTIONS` 定义已物理删除，不保留别名或重导出。
- canonical 编译器、Qwen、observer、Orchestrator、runtime session 和 device execution 全部直接依赖
  同一 `CANONICAL_ACTION_KINDS` 对象；domain/application 不再反向导入平铺 canonical，原循环依赖消失。
- 14 项成员、唯一对象身份、未知动作、capability/canonical 差集与依赖防恢复最小回归 `30/30` 通过；
  canonical、Qwen、observer、device execution、Orchestrator 与 Web 相关链 `335/335` 通过。
- 本批唯一完整 Python 回归 `892/892`（`39.562s`）通过；前端纯协议 `11/11`、浏览器合同 `13/13`、
  阶段 2 控制台浏览器合同 `4/4`，合计 `28/28` 通过；静态编译、旧定义/反向依赖扫描和
  `git diff --check` 通过。

本批完成的是 **canonical 动作词汇这一个真实 DDD 领域切片**。集合成员、动作能力全集、候选/控制算法、
公开 API、模型调用、输入事务、设备 transport、动作次数、风险边界和真机语义没有改变。本批没有重载
项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-26 轻量渐进式 DDD 模块化单体第十六批（实施前冻结）

### 当前唯一缺口与根因证据

根目录 `task_semantic_ir.py` 定义正式 TaskSemanticIR、typed entity/surface/effect/constraint/desired state、
语义编译报告、正式风险权威和应用投影，却仍由 DeepSeek graph、canonical、Qwen、Orchestrator、Web 与测试
横向导入。该 2400 余行模块的唯一技术 I/O 是 `load_local_risk_policy()` 内的 `Path.read_text()`；其余均为
确定性领域值对象、校验、序列化和编译/裁决规则。

根因归类为 **核心任务语义与文件系统配置加载混在平铺入口**。本批迁移完整正式语义切片，并把唯一文件
读取实现分离到 infrastructure，物理删除根模块；不改 IR/风险协议、字段、默认确认种类、编译算法、
authority、effect preview、公开 API、模型调用、canonical 候选或机械动作。

### 同类失败与变化样本集合

1. 打开/导航、文字输入、换行、多字段、最近任务卡片、发送及登录/付款 effect 的 IR 必须逐字段一致。
2. 当前仅 authentication/financial_transaction 为 confirmation_required，其余 effect 保持 automatic。
3. source span、entity authority、constraint、dependency、desired state、evidence 和 graph digest 正反校验保持。
4. 风险 JSON 正常加载、字段漂移、损坏 JSON、缺失文件与重复 override 继续使用相同错误合同。
5. 根目录旧模块/导入、domain 内 Path/I/O、转发 facade 和第二套语义/风险权威必须全部消失。

### 通用修复

1. 将除文件读取外的原模块迁入 `poc/agent/domain/task_semantic_ir.py`，删除 `Path` 依赖。
2. 在 `poc/agent/infrastructure/file_system_risk_policy.py` 放置唯一 `load_local_risk_policy()` 实现，复用
   domain 的 JSON parser 与错误类型。
3. DeepSeek graph 从 infrastructure 取得 loader，其余生产与测试直接依赖 domain；删除根文件，不保留
   重导出、兼容别名或双实现。
4. 更新当前协议/交接路径，并增加类型身份、domain 纯依赖、loader 唯一性与旧导入防恢复回归。

### 影响、回滚与停止条件

- 影响范围：TaskSemanticIR/正式风险权威的模块路径及本地风险策略 loader 位置。
- 不影响：任何协议/字段/默认政策、DeepSeek/Qwen 请求、canonical、Controller、API、输入事务、设备执行、
  动作次数、风险边界和既有真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时恢复根模块并保留 domain/infra 实现。
- 停止条件：任一 IR/report JSON、风险决定、异常、候选数量、模型输入、动作结果或公开投影变化时停止。

### 验证清单

- TaskSemanticIR 全部值对象、编译、authority、风险投影与 loader 正反例；
- DeepSeek graph/transport、canonical、Qwen、Orchestrator、Web 和正式协议相关回归；
- domain 无 Path/I/O、loader/类型唯一性与旧根文件/导入防恢复扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 完成结果

- 原 `poc/task_semantic_ir.py` 的纯业务部分已迁入 `poc/agent/domain/task_semantic_ir.py`，根文件物理删除；
  TaskSemanticIR、编译报告、正式风险权威、effect preview 和运行投影只有一个类型/算法身份。
- 唯一 `Path.read_text()` 已迁到 `poc/agent/infrastructure/file_system_risk_policy.py`；domain 不再导入
  pathlib 或执行 I/O，DeepSeek graph 复用该 loader，其他调用方直接依赖 domain。
- TaskSemanticIR 全部值对象、编译、authority、风险投影、loader 和分层防恢复最小回归 `60/60` 通过；
  DeepSeek graph/transport、canonical、Qwen、最近任务、Orchestrator 与 Web 相关链 `404/404` 通过。
- 本批唯一完整 Python 回归 `893/893`（`41.878s`）通过；前端纯协议 `11/11`、浏览器合同 `13/13`、
  阶段 2 控制台浏览器合同 `4/4`，合计 `28/28` 通过；静态编译、domain I/O、旧入口/导入扫描和
  `git diff --check` 通过。

本批完成的是 **TaskSemanticIR 与正式风险权威这一个真实 DDD 领域切片**。IR/风险协议、字段、默认确认
种类、编译/authority/preview、公开 API、模型调用、canonical、Controller、动作次数、风险边界和真机
语义没有改变。本批没有重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-26 轻量渐进式 DDD 模块化单体第十七批（实施前冻结）

### 当前唯一缺口与根因证据

根目录 `canonical_action_protocol.py` 是当前唯一动作目录权威，定义 proposal、visual claim/relation、
affordance、state expectation、typed transition、candidate、catalog 与确定性 expected result。其依赖已经
全部收敛为标准库及 domain 内的 TaskSemanticIR、UIScene、SemanticAction、文字事务和动作词汇，但正式
模块仍位于平铺入口，被 Qwen、observer、adapter、Orchestrator、Web 与测试横向导入。

根因归类为 **纯 canonical 领域服务仍位于平铺运行入口**。本批只原样迁移整个唯一目录并统一导入，
物理删除根文件；不改协议版本、字段、阈值、候选编译、catalog digest、expected result、模型调用、
Controller、公开 API 或机械动作。

### 同类失败与变化样本集合

1. tap/swipe/back/home/recent apps/input/clear/newline/wait 等候选数量、字段和 digest 必须逐项一致。
2. UI claim/relation/affordance、App/surface identity、exact target 与多字段 next-field 规则保持。
3. 未知动作、原始坐标、低置信/歧义目标、错误 semantic IR 与不完整 catalog 继续 fail closed。
4. 变化样本：普通导航、换行、多字段、卡片清除、系统导航 reveal 和输入键盘状态继续共用同一目录。
5. 根目录旧文件/导入、重导出 facade、第二套候选编译或第二个类型身份必须全部消失。

### 通用修复

1. 将原文件迁入 `poc/agent/domain/canonical_action_protocol.py`，内部改为领域内相对导入。
2. Qwen、observer、adapter、Orchestrator、Web 和测试直接依赖 domain 子模块；局部动态导入也指向同一模块。
3. 删除根文件，不保留转发、兼容别名或第二目录，并增加类型身份、domain 纯依赖和旧导入防恢复回归。

### 影响、回滚与停止条件

- 影响范围：canonical 目录值对象/编译服务的模块路径与全部导入点。
- 不影响：任何协议/字段/阈值/候选、TaskSemanticIR、Qwen/DeepSeek、Controller、API、输入事务、设备执行、
  动作次数、风险边界和既有真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时恢复根文件并保留 domain 文件。
- 停止条件：任一类型身份、catalog JSON/digest、候选数量、错误、模型输入或动作结果变化时停止并恢复。

### 验证清单

- canonical 全值对象、catalog 编译、expected result、未知/歧义/坐标拒绝和变化样本；
- Qwen、observer、adapter、Orchestrator、Web、最近任务和复杂输入相关回归；
- domain 纯依赖、类型唯一性与旧根文件/导入防恢复扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 实施与验证结果

- 原平铺 `poc/canonical_action_protocol.py` 已物理迁入
  `poc/agent/domain/canonical_action_protocol.py`；内部只使用 domain 相对导入，根目录不保留转发门面、别名或
  第二套候选编译入口。
- Qwen、observer、adapter、Orchestrator、Web 与全部测试已直接消费同一 domain 模块；新增防恢复测试同时
  固定模块身份、根文件不存在、旧绝对导入为零和 domain 依赖方向。
- 最小与防恢复 `58/58`、canonical—Qwen—观察—适配—编排—Web 相关链 `466/466`、完整 Python
  `894/894`（`45.111s`）通过；前端合同合计 `28/28` 通过，静态编译、旧入口/导入扫描和
  `git diff --check` 通过。
- 本批完成的是 **canonical 动作目录值对象与确定性编译服务这一个真实 DDD 领域切片**。协议版本、字段、
  阈值、候选、catalog digest、expected result、公开 API、模型调用、Controller、动作次数、风险边界和
  真机语义均未改变。本批没有重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-26 轻量渐进式 DDD 模块化单体第十八批（实施前冻结）

### 当前唯一缺口与根因证据

平铺 `poc/generic_goal.py` 是通用目标只读投影、JSON 值校验和文本模型 JSON 对象解析的唯一实现；它只依赖
标准库，却被 domain/application 运行态、DeepSeek、adapter、Orchestrator 与测试横向导入。该平铺依赖会让
下一批 DeepSeek 图迁移继续反向依赖根目录模块。

根因归类为 **纯通用目标投影领域合同仍位于平铺运行入口**。本批只迁移这一完整、确定性切片并统一导入，
物理删除根文件；不修改投影字段、JSON 解析、校验、错误、DeepSeek 提示词、模型调用或动作语义。

### 同类失败与变化样本集合

1. understood 正反状态、App ID、目标、实体、成功条件、约束、账号影响与确认标记保持逐字段一致。
2. JSON 代码围栏、无效 JSON、非对象根值、空键和不支持值类型继续返回同一错误。
3. 变化样本：动作词、coordinates 等普通用户数据继续只作为数据，不形成第二动作否决或确认权威。
4. 根目录旧文件/导入、重导出 facade 和第二个 GenericIntentDraft 类型身份必须全部消失。

### 通用修复

1. 将 `GenericIntentDraft`、`GenericIntentError` 与纯 JSON 解析/校验原样迁入
   `poc/agent/domain/generic_goal.py`。
2. DeepSeek、application runtime session、adapter、Orchestrator 与测试直接依赖同一 domain 模块。
3. 删除根文件，不保留转发、兼容别名或双入口，并增加类型身份、旧导入和 domain 纯依赖防恢复回归。

### 影响、回滚与停止条件

- 影响范围：通用目标投影类型与纯 JSON 解析器的模块路径和导入点。
- 不影响：公开 API、DeepSeek/Qwen、canonical、Controller、输入事务、设备执行、动作次数、风险边界和真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时恢复根文件并保留 domain 文件。
- 停止条件：任一投影 JSON、异常类型/文本、模型请求、canonical 输入或运行态快照变化时停止并恢复。

### 验证清单

- 通用目标投影正反例、JSON 解析变化样本与架构防恢复测试；
- DeepSeek、adapter、runtime session、Orchestrator、Mock 闭环和 Web 相关回归；
- domain 纯依赖、类型唯一性与旧根文件/导入扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 实施与验证结果

- 原平铺 `poc/generic_goal.py` 已物理迁入 `poc/agent/domain/generic_goal.py`；
  `GenericIntentDraft`、`GenericIntentError`、纯 JSON 对象解析和 JSON 值校验只有一个类型/算法身份，根目录
  不保留 facade、别名或第二入口。
- DeepSeek、application runtime session、adapter、Orchestrator 与测试已直接消费同一 domain 模块；新增
  fenced JSON、无效/非对象 JSON、空键、不支持值和动作词作为普通数据的正反变化样本及防恢复扫描。
- 最小与防恢复 `27/27`、DeepSeek—adapter—runtime session—编排—Mock—Web 相关链 `485/485`、完整
  Python `898/898`（`49.365s`）通过；前端合同合计 `28/28` 通过，静态编译、旧入口/导入扫描和
  `git diff --check` 通过。
- 本批完成的是 **通用目标只读投影与纯 JSON 对象解析这一个真实 DDD 领域切片**。投影协议、字段、校验、
  异常、DeepSeek 提示词/模型调用、公开 API、canonical、Controller、动作次数、风险边界和真机语义均未
  改变。本批没有重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-26 轻量渐进式 DDD 模块化单体第十九批（实施前冻结）

### 当前唯一缺口与根因证据

平铺 `poc/deepseek_task_graph.py` 同时承载 DeepSeek JSON provider 端口、计划/重规划应用用例、提示词、
typed transport 解析和当前任务图模型，并直接导入 infrastructure 风险策略文件 loader。Web、Orchestrator、
能力规划、runtime session 与测试均横向依赖该根模块，application 也因此反向依赖平铺入口。

根因归类为 **DeepSeek 应用用例仍位于平铺入口且自行取得 infrastructure 配置**。本批先把这条完整正式运行
入口迁入 `agent/application`，由 Web 组合根加载并注入风险策略；根文件物理删除。任务图纯领域聚合的进一步
拆分留给下一独立批次，不在本批同时改协议或算法。

### 同类失败与变化样本集合

1. 初始计划、重规划、exact input/action 构造、typed transport 解析/规范化与全部错误保持一致。
2. 默认本地风险策略与当前 JSON 文件内容一致；自定义策略仍可显式注入且只由正式风险权威裁决。
3. 变化样本：导航、普通外部效果、认证、付款、换行、多字段、最近任务卡片和只读完成继续使用同一 planner。
4. 根目录旧文件/导入、application→infrastructure 导入、转发 facade 与第二个任务图类型身份必须消失。

### 通用修复

1. 将完整 DeepSeek planner/typed transport 运行入口迁入
   `poc/agent/application/deepseek_task_graph.py`，调用方直接更新为 application 模块路径。
2. planner 未显式传入策略时使用纯 domain `LocalRiskPolicyConfig()`；正式 Web 组合根继续从现有 JSON 文件
   读取并显式注入，保留运行配置能力且 application 不读取文件系统。
3. 删除根文件，不保留 facade、别名或旧导入，并增加应用层依赖、模块身份、组合根注入和旧入口防恢复测试。

### 影响、回滚与停止条件

- 影响范围：DeepSeek planner、typed transport 与任务图当前模块路径，以及风险策略装配位置。
- 不影响：正式图协议/字段、提示词、规范化、风险决定、模型调用次数、Qwen、canonical、Controller、API、
  输入事务、设备执行、动作次数和真机证据。
- 回滚方式：整体撤销本批单一提交；不得恢复根文件同时保留 application 模块。
- 停止条件：任一图 JSON、提示词、异常、策略决定、模型请求次数、replan 结果或公开状态变化时停止并恢复。

### 验证清单

- DeepSeek typed transport、计划/重规划、exact builder、风险策略默认/注入和变化样本；
- TaskSemanticIR、capability planner、runtime session、Orchestrator、Mock、canonical、Qwen 与 Web 相关回归；
- application 无 infrastructure/文件读取、类型唯一性与旧根文件/导入扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 实施与验证结果

- 原平铺 `poc/deepseek_task_graph.py` 已物理迁入
  `poc/agent/application/deepseek_task_graph.py`；Web、Orchestrator、capability planner、runtime session 与
  测试均直接依赖唯一 application 入口，根目录不保留 facade、别名或第二类型身份。
- application 不再导入 infrastructure、pathlib 或读取文件；默认离线构造使用纯 domain
  `LocalRiskPolicyConfig()`，正式 Web 组合根继续从既有 `config/local_risk_policy.v1.json` 加载并显式注入，
  配置能力和风险裁决结果保持。
- 最小与防恢复 `110/110`、DeepSeek—TaskSemanticIR—capability—canonical—Qwen—编排—Mock—Web 相关链
  `405/405`、完整 Python `899/899`（`51.736s`）通过；前端合同合计 `28/28` 通过，静态编译、application
  反向依赖/文件读取与旧入口/导入扫描、`git diff --check` 通过。
- 本批完成的是 **DeepSeek 计划/重规划应用入口与风险策略装配这一个真实 DDD 应用切片**。任务图协议、字段、
  提示词、规范化、风险决定、模型调用次数、Qwen、canonical、Controller、API、动作次数和真机语义均未
  改变。本批没有重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-26 轻量渐进式 DDD 模块化单体第二十批（实施前冻结）

### 当前唯一缺口与根因证据

`poc/agent/application/deepseek_task_graph.py` 已是唯一 DeepSeek 应用入口，但其中仍混有任务图协议常量、
TargetApp/GraphGoal/Subgoal/ObservedState/DynamicTaskGraph 等值对象、转换回执/视觉证据引用、全图不变量、
typed payload 解析、exact graph builder 和确定性重规划投影。application 因此同时拥有领域状态与应用用例。

根因归类为 **任务图纯领域聚合与 DeepSeek 模型应用用例尚未分层**。本批只把所有不发起模型调用、不读取
文件、不依赖 Web/设备的纯图合同和算法下沉到 domain；application 保留 provider、提示词、plan/replan 和
风险策略应用编排，直接消费唯一 domain 类型。

### 同类失败与变化样本集合

1. 全部图值对象、协议版本、to_dict/to_qwen_context、校验错误、依赖环和唯一 active frontier 保持。
2. exact input/action builder、typed transport payload、别名规范化、完成投影、视觉/Controller 证据绑定保持。
3. 变化样本：导航、发送、认证、付款、换行、多字段、最近任务卡片、纯观察和动作匹配/不匹配重规划保持。
4. application 内重复领域类/常量/构造器、从 application 导入 domain 类型的生产调用和第二类型身份必须消失。

### 通用修复

1. 新建 `poc/agent/domain/task_graph.py`，承接任务图值对象、不变量、序列化、typed payload 解析、builder 和
   确定性投影；只依赖标准库及同层 TaskSemanticIR。
2. `poc/agent/application/deepseek_task_graph.py` 只保留 provider 端口、提示词、模型请求和 plan/replan 用例，
   通过 domain 函数完成解析与裁决。
3. production 与测试分别从 domain 导入图合同、从 application 导入 planner，不保留重导出 facade 或双定义，
   并增加依赖方向、符号所有权与类型身份防恢复回归。

### 影响、回滚与停止条件

- 影响范围：DeepSeek 任务图领域符号的模块路径及 application 内部依赖。
- 不影响：图协议/字段、提示词、模型请求与次数、风险决定、Qwen、canonical、Controller、API、输入事务、
  设备执行、动作次数和真机证据。
- 回滚方式：整体撤销本批单一提交；不得在 application 与 domain 同时保留任务图定义或转发。
- 停止条件：任一图 JSON、异常、提示词、provider 请求、风险结果、replan 结果或公开投影变化时停止并恢复。

### 验证清单

- 全任务图值对象/校验/序列化、typed transport、exact builder、重规划投影与历史变化样本；
- DeepSeek planner、TaskSemanticIR、capability、runtime session、Orchestrator、canonical、Qwen、Mock 与 Web；
- domain/application 依赖、符号单一所有权、类型身份和旧 application 导入防恢复扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 实施与验证结果

- 新 `poc/agent/domain/task_graph.py` 现为任务图协议常量、全部图值对象/证据引用、全图不变量、序列化、
  typed payload 解析、exact builder 和确定性重规划投影的唯一领域实现；不含 provider、prompt、模型调用、
  application/infrastructure/Web 或设备依赖。
- `poc/agent/application/deepseek_task_graph.py` 已缩为 provider 端口、提示词、模型请求和 plan/replan 用例，
  公开 `__all__` 只包含 planner/provider；production 的任务图类型与 builder 均直接来自 domain，不存在转发
  facade、重复类/常量或第二类型身份。
- 分层后一个测试补丁观察不到提前复制的领域校验函数引用；application 已改为通过 domain 模块实时调用三项
  revision 校验，失败用例单独复验通过。最终最小与防恢复 `111/111`、DeepSeek—TaskSemanticIR—capability—
  canonical—Qwen—编排—Mock—Web 相关链 `406/406`、完整 Python `900/900`（`54.490s`）通过。
- 前端合同合计 `28/28` 通过；静态编译、domain/application 依赖、符号所有权、旧错误导入扫描和
  `git diff --check` 通过。本批没有改变图协议、字段、提示词、provider 请求/次数、风险决定、Qwen、
  canonical、Controller、API、动作次数或真机语义，也没有重载项目 API、调用在线模型、执行真机动作或
  操作卖家 `main.exe`。

## 2026-08-26 轻量渐进式 DDD 模块化单体第二十一批（实施前冻结）

### 当前唯一缺口与根因证据

共享 `VisionAgentError` 仍定义在平铺 `vision_agent.py` 的 HTTP/PIL provider 实现内；Qwen、scene observer、
adapter、Orchestrator、Web、离线评估与测试都必须反向导入该 infrastructure 模块才能表达同一失败合同。
同时 `vision_model_config.py` 把纯模型身份值对象/公开投影与 `os.environ` 读取混在一个平铺模块。

根因归类为 **视觉模型领域合同与 provider/环境配置实现尚未分层**。本批只迁移共享错误、不可变模型身份和
公开投影到 domain，把环境变量解析迁入 infrastructure；provider 直接消费两层合同，旧平铺配置文件删除。

### 同类失败与变化样本集合

1. JSON 缺失/重复/截断、provider 未配置、HTTP/连接、模型身份和视觉协议错误继续使用同一异常类型。
2. 默认模型、base URL、provider、thinking、coordinate scale 与 config version 保持逐字段一致。
3. 变化样本：显式配置、空环境、非法模型 ID、远程 HTTP 地址、公开无密钥模型身份投影保持。
4. 旧 `from vision_agent import VisionAgentError`、平铺 config 文件/导入、第二异常类型或第二配置 loader 必须消失。

### 通用修复

1. 新建 `poc/agent/domain/vision_model.py`，唯一拥有 `VisionAgentError`、`VisionModelConfig`、协议常量和
   `public_model_identity()`；只依赖标准库。
2. 新建 `poc/agent/infrastructure/environment_vision_model_config.py`，唯一读取环境变量并构造 domain 配置。
3. `vision_agent.py` 只实现 provider/JSON/image transport；Qwen、observer、adapter、Web 与测试直接依赖
   domain 错误/身份合同。删除 `poc/vision_model_config.py`，不保留 facade 或兼容重导出。

### 影响、回滚与停止条件

- 影响范围：视觉模型异常、配置值对象、环境 loader 与公开身份投影的模块路径和调用点。
- 不影响：模型名称/地址、请求参数/次数、JSON 解析、Qwen/scene/canonical/Controller、API、风险边界、
  设备执行、动作次数和真机证据。
- 回滚方式：整体撤销本批单一提交；不得恢复平铺配置或 provider 异常同时保留新合同。
- 停止条件：任一异常类型/文本、配置字段、provider 请求、status/identity 投影或模型调用次数变化时停止。

### 验证清单

- 视觉配置默认/显式/非法样本、provider JSON/HTTP/身份/usage 与异常身份；
- Qwen、scene observer、adapter、Orchestrator、Web、离线评估和输入 lineage 相关回归；
- domain 纯依赖、infrastructure 单 loader、旧文件/导入/异常定义防恢复扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 实施与验证结果

- 新 `poc/agent/domain/vision_model.py` 现为 `VisionAgentError`、`VisionModelConfig`、视觉模型协议常量和
  `public_model_identity()` 的唯一领域实现；只依赖标准库，不含环境读取、HTTP、PIL、模型 SDK 或设备实现。
- 新 `poc/agent/infrastructure/environment_vision_model_config.py` 是唯一环境配置 loader；
  `vision_agent.py`、Qwen、scene observer、adapter、Orchestrator、Web、离线评估和测试均直接依赖分层后的
  正式合同。原 `poc/vision_model_config.py` 已物理删除，不保留 facade、兼容重导出、第二异常或第二 loader。
- 初次完整回归只暴露固定 App 退役扫描仍硬编码已删除的旧配置路径；扫描目标改为新的 domain/infrastructure
  正式入口后，最小与防恢复 `48/48`、视觉配置—Qwen—scene—adapter—编排—Web 相关链 `481/481`、完整
  Python `901/901`（`55.982s`）通过；前端合同合计 `28/28` 通过，静态编译、旧入口/导入/异常定义扫描和
  `git diff --check` 通过。
- 本批没有改变模型名称/地址、请求参数/次数、JSON 解析、公开 API、Qwen/scene/canonical/Controller、风险
  边界、设备执行、动作次数或真机语义，也没有重载项目 API、调用在线模型、执行真机动作或操作卖家
  `main.exe`。

## 2026-08-26 轻量渐进式 DDD 模块化单体第二十二批（实施前冻结）

### 当前唯一缺口与根因证据

纯业务规则 `subgoal_binds_recipient()` / `subgoal_targets_recipient_control()` 仍位于平铺
`poc/message_intent.py`；通用目标上下文清洗规则 `_safe_goal_context()` 则埋在七千余行的 scene observer
实现内，导致 Qwen typed 上下文为了验证同一纯目标合同反向导入观察器基础设施。

根因归类为 **通用目标语义与无控制指令投影尚未由 domain 唯一持有**。本批只迁移收件人绑定语义和目标
上下文清洗；Qwen 决策、观察模型、provider、提示词、请求次数与候选算法均不改。

### 同类失败与变化样本集合

1. 收件人尚不可见的 App 导航、明确选择收件人和进入收件人页面后的正文编辑语义保持。
2. 合法嵌套目标、列表截断、字符串截断、最大深度和不支持值类型保持。
3. 变化样本：中英文选择/编辑措辞、嵌套控制字段和普通正文中的动作词保持原判定。
4. 根目录旧文件、observer 内嵌清洗函数、旧导入、兼容别名或第二规则实现必须消失。

### 通用修复

1. 将 `poc/message_intent.py` 物理迁入 `poc/agent/domain/message_intent.py`，调用方直接依赖 domain。
2. 将 `_safe_goal_context()` 迁入 `poc/agent/domain/generic_goal.py` 并命名为公开领域服务
   `safe_goal_context()`；scene observer、Qwen 与测试直接消费同一实现。
3. 删除根文件和 observer 内嵌实现，不保留 facade、别名或双重入口；增加依赖方向与防恢复测试。

### 影响、回滚与停止条件

- 影响范围：收件人绑定与目标上下文清洗函数的模块路径和唯一所有权。
- 不影响：函数输入输出/异常、DeepSeek/Qwen 协议、提示词、模型调用、候选、canonical、Controller、API、
  风险边界、设备执行、动作次数和真机证据。
- 回滚方式：整体撤销本批单一提交；不得在根模块/observer 与 domain 同时保留实现。
- 停止条件：任一清洗结果、收件人判定、异常文本、模型请求或公开投影变化时停止并恢复。

### 验证清单

- message intent、goal projection、scene observer 与 Qwen typed context 正反/变化样本；
- DeepSeek、Qwen、Orchestrator、adapter、Web 和最近任务相关回归；
- domain 依赖、函数单一所有权、旧文件/导入/observer 定义防恢复扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 实施与验证结果

- 原平铺 `poc/message_intent.py` 已物理迁入 `poc/agent/domain/message_intent.py`；收件人是否由当前子目标
  绑定、当前动作是否选择收件人两项语义只有一个领域实现，根目录不保留 facade 或兼容导入。
- 原 scene observer 内嵌 `_safe_goal_context()` 已迁入 `poc/agent/domain/generic_goal.py` 并成为唯一
  `safe_goal_context()`；observer 与 Qwen 通过 domain 模块调用，观察器不再拥有第二套目标控制字段规则。
- 最小与防恢复 `33/33`、目标语义—Qwen—scene—最近任务—编排—Web 相关链 `304/304`、完整 Python
  `902/902`（`59.254s`）通过；前端合同合计 `28/28` 通过，静态编译、旧文件/导入/函数定义扫描和
  `git diff --check` 通过。
- 本批没有改变清洗输入输出/异常、收件人判定、DeepSeek/Qwen 协议、提示词、模型调用、候选、canonical、
  Controller、API、风险边界、设备执行、动作次数或真机语义，也没有重载项目 API、调用在线模型、执行
  真机动作或操作卖家 `main.exe`。

## 2026-08-26 轻量渐进式 DDD 模块化单体第二十三批（实施前冻结）

### 当前唯一缺口与根因证据

平铺 `poc/qwen_visual_decision.py` 同时拥有 Qwen typed task context 的协议版本、scope/状态/效果门校验、
收件人和精确输入只读投影，以及 PIL 可信观察、provider 请求、模型输出解析和 canonical 选择应用流程。
纯 `QwenTaskContext` 因而无法独立于图像/provider 基础设施复用，Orchestrator 和离线评估也反向依赖整个模块。

根因归类为 **Qwen 输入上下文领域合同与视觉决策应用实现尚未分层**。本批只下沉 typed context 值对象、
协议常量和确定性校验/投影；可信观察与决策应用流程留在现有模块等待后续独立切片。

### 同类失败与变化样本集合

1. v4 协议字段、task/device/revision、active subgoal、effect intent/gate/scope 全部正反样本保持。
2. TaskSemanticIR scope、唯一 typed input field、收件人选择/页面身份和 observation context 投影保持。
3. 变化样本：observe/navigate/effect、自动效果、认证/付款确认、单/多收件人和单/多字段保持。
4. Qwen provider 模块内 context 类/常量/辅助解析器、从 provider 导入 context 和第二类型身份必须消失。

### 通用修复

1. 新建 `poc/agent/domain/qwen_task_context.py`，唯一拥有 `QwenTaskContext`、支持协议、scope/status 集合、
   task/device 格式和 JSON tuple/dict 校验辅助函数。
2. Qwen 决策、Orchestrator、离线评估与测试直接依赖 domain context；Qwen provider 通过模块限定名消费，
   不重导出旧类名。
3. 删除 provider 内重复定义与无用导入，增加模块身份、依赖纯度和旧导入防恢复测试。

### 影响、回滚与停止条件

- 影响范围：Qwen typed context 的模块路径和唯一所有权。
- 不影响：context 字段/校验/异常、DeepSeek 图、Qwen prompt/provider/请求次数、可信观察、决策/候选、
  canonical、Controller、API、风险边界、设备执行、动作次数和真机证据。
- 回滚方式：整体撤销本批单一提交；不得在 domain 与 provider 同时保留 context 类型或兼容转发。
- 停止条件：任一 context 序列化、校验、异常文本、模型请求、候选或公开状态变化时停止并恢复。

### 验证清单

- QwenTaskContext 全协议、scope、effect gate、TaskSemanticIR、input/recipient 与变化样本；
- Qwen、DeepSeek、Orchestrator、scene、adapter、最近任务、Mock 和 Web 相关回归；
- domain 纯依赖、类型/常量单一所有权、旧 provider 导入/定义防恢复扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 实施与验证结果

- 新 `poc/agent/domain/qwen_task_context.py` 现为 `QwenTaskContext`、v4 支持协议、task/device 格式、
  status/execution class 集合、scope/effect gate/TaskSemanticIR/input/recipient 校验与只读观察投影的唯一领域
  实现；只依赖标准库和同层 domain。
- `qwen_visual_decision.py` 已删除约 450 行 context 类型、常量和 JSON 辅助解析器，使用模块限定名消费
  domain；Orchestrator、离线评估与测试直接导入 domain 类型，provider 不重导出 `QwenTaskContext`，不存在
  facade 或第二类型身份。
- 最小与防恢复 `32/32`、Qwen—DeepSeek—TaskSemanticIR—scene—adapter—最近任务—编排—Web 相关链
  `532/532`、完整 Python `903/903`（`60.504s`）通过；前端合同合计 `28/28` 通过，静态编译、domain
  依赖、类型/常量所有权、旧 provider 导入/定义扫描和 `git diff --check` 通过。
- 首次相关回归命令误写了迁移后不存在的 `test_deepseek_task_graph.py`，其余 469 条未报功能失败；按仓库
  真实文件名重跑后得到上述 `532/532`，未以生产代码掩盖命令错误。
- 本批没有改变 context 字段/校验/异常、DeepSeek 图、Qwen prompt/provider/请求次数、可信观察、决策/候选、
  canonical、Controller、API、风险边界、设备执行、动作次数或真机语义，也没有重载项目 API、调用在线
  模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-26 轻量渐进式 DDD 模块化单体第二十四批（实施前冻结）

### 当前唯一缺口与根因证据

平铺 `poc/observation_images.py` 同时定义纯 `LocalFrameStability` / `VisualObstruction` 证据值对象并实现
依赖 PIL、环境阈值和像素算法的稳定性、清晰度、遮挡检测。Qwen 的可信观察因而必须从图像实现模块导入
领域证据类型，scene、adapter、doctor 与测试也共同依赖根目录基础设施入口。

根因归类为 **视觉证据值对象与本地图像测量实现尚未分层**。本批只迁移两个值对象到 domain、把完整像素
算法迁入 infrastructure；算法、阈值、环境变量、输入输出和调用次数均保持。

### 同类失败与变化样本集合

1. 稳定/不稳定、首帧离群、持续运动、静态边缘差异与清晰度测量保持。
2. 单帧/多帧顶部遮挡、共识 IoU、全宽状态栏和普通暗色区域排除保持。
3. 变化样本：不同分辨率、DPI 相对几何、环境阈值覆盖与四帧/更多帧保持。
4. 根目录旧文件、infrastructure 内值对象定义、旧导入、兼容 facade 或第二算法入口必须消失。

### 通用修复

1. 新建 `poc/agent/domain/visual_evidence.py`，唯一拥有稳定性与视觉遮挡不可变证据及序列化。
2. 将 `poc/observation_images.py` 物理迁入 `poc/agent/infrastructure/observation_images.py`，只实现 PIL/
   环境相关测量并返回 domain 值对象。
3. Qwen/scene 直接导入 domain 类型，所有算法调用方直接依赖 infrastructure；删除根入口并增加防恢复测试。

### 影响、回滚与停止条件

- 影响范围：视觉证据类型和图像测量算法的模块路径。
- 不影响：数据字段/序列化、像素算法、阈值、环境配置、Qwen/scene/adapter/doctor 行为、模型请求、
  canonical、Controller、API、风险边界、设备执行、动作次数和真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时保留根模块与新 infrastructure 或双定义值对象。
- 停止条件：任一测量值、遮挡结果、阈值读取、异常或调用方输出变化时停止并恢复。

### 验证清单

- observation images 全稳定性/清晰度/遮挡正反与变化样本；
- Qwen、scene、adapter、doctor、Orchestrator、Mock 与 Web 相关回归；
- domain 无 PIL/os、infrastructure 单算法入口、类型身份和旧文件/导入防恢复扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 实施与验证结果

- 新 `poc/agent/domain/visual_evidence.py` 现唯一拥有 `LocalFrameStability` 与 `VisualObstruction` 不可变证据
  及序列化；只依赖标准库，不含 PIL、环境变量、像素算法或设备实现。
- 原 `poc/observation_images.py` 已物理迁入 `poc/agent/infrastructure/observation_images.py`，稳定性、
  清晰度、静态边缘差异和顶部遮挡共识算法逐行保留并返回 domain 类型；Qwen/scene 直接依赖证据类型，
  adapter/doctor/scene/Qwen 与测试直接依赖唯一 infrastructure 算法入口，根目录无 facade。
- 最小与防恢复 `35/35`、图像证据—Qwen—scene—adapter—doctor—编排—Web 相关链 `433/433`、完整
  Python `904/904`（`62.528s`）通过；前端合同合计 `28/28` 通过，静态编译、domain 依赖、类型/算法
  所有权、旧文件/导入扫描和 `git diff --check` 通过。
- 本批没有改变数据字段/序列化、像素算法、阈值、环境配置、Qwen/scene/adapter/doctor 行为、模型请求、
  canonical、Controller、API、风险边界、设备执行、动作次数或真机语义，也没有重载项目 API、调用在线
  模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-27 轻量渐进式 DDD 模块化单体第二十五批（实施前冻结）

### 当前唯一缺口与根因证据

`TrustedObservation` 仍内嵌在平铺 Qwen 决策模块，并同时拥有纯观察快照/候选去重规则以及 PIL 四帧稳定性、
清晰度、环境阈值、UUID 和本地 fingerprint 构建/新鲜度校验。Qwen 决策应用因此直接拥有 infrastructure
职责，Orchestrator、离线评估与测试也必须从 provider 模块导入观察合同。

根因归类为 **可信观察领域快照与本地帧构建/校验实现尚未分层**。本批只分离观察值对象、候选规范化和
帧适配器；Qwen prompt、provider、选择算法、模型请求次数、canonical 与动作语义不改。

### 同类失败与变化样本集合

1. 四帧最小数、首帧离群、稳定性、最清晰尾帧、清晰度阈值和 fingerprint 新鲜度保持。
2. 重复视觉对象折叠、语义冲突、局部唯一目标、输入审计候选优先级和 system UI 投影保持。
3. 变化样本：显式 observation ID、非法 device/observation ID、环境阈值覆盖和动作前再次校验保持。
4. Qwen 模块内观察类/候选规范化/帧算法、从 Qwen 导入观察类型、observer 内 fingerprint 或第二实现必须消失。

### 通用修复

1. 新建 `poc/agent/domain/trusted_observation.py`，唯一拥有纯 `TrustedObservation`、候选去重/冲突和序列化。
2. 新建 `poc/agent/infrastructure/trusted_observation_frames.py`，唯一负责四帧构建与帧新鲜度校验；本地
   fingerprint 归入既有 image infrastructure。
3. Qwen observer 显式注入帧校验器；Orchestrator 注入/默认使用 infrastructure 构建器；所有合同调用方
   直接依赖 domain，不保留 Qwen 重导出、旧私有算法或兼容 facade。

### 影响、回滚与停止条件

- 影响范围：可信观察类型、构建器、帧校验器与 fingerprint 的模块路径和装配。
- 不影响：观察字段/序列化、稳定/清晰/fingerprint/候选算法、Qwen prompt/provider/请求次数、选择结果、
  canonical、Controller、API、风险边界、设备执行、动作次数和真机证据。
- 回滚方式：整体撤销本批单一提交；不得在 Qwen 与 domain/infrastructure 同时保留观察或帧算法。
- 停止条件：任一观察 JSON、候选/冲突、帧校验、异常文本、模型请求或选择结果变化时停止并恢复。

### 验证清单

- TrustedObservation 四帧构建/校验、候选去重/冲突、system UI、ID/阈值正反与变化样本；
- Qwen、scene、adapter、Orchestrator、离线评估、Mock 和 Web 相关回归；
- domain 无 PIL/os/uuid/infrastructure、Qwen 无观察实现、类型/算法单一所有权与旧导入扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 实施与验证结果

- 新 `poc/agent/domain/trusted_observation.py` 现唯一拥有 `TrustedObservation`、system UI 投影、候选去重/
  冲突、局部目标裁决和序列化；只依赖标准库与同层 UI/视觉证据，不含 PIL、环境、UUID 或 infrastructure。
- 新 `poc/agent/infrastructure/trusted_observation_frames.py` 唯一负责四帧稳定性/清晰度/fingerprint 构建和
  新鲜度校验；`local_frame_fingerprint()` 归入既有 image infrastructure。Qwen observer 显式接收帧校验器，
  Web 组合根显式注入校验器和观察构建器，Orchestrator 不导入 infrastructure。
- Qwen 已删除内嵌观察类、frame 构建/校验和候选规范化算法；Orchestrator、离线评估与测试直接依赖 domain
  类型，Qwen 不重导出 `TrustedObservation`，scene observer 不保留旧 fingerprint 实现。
- 最小与防恢复 `41/41`、可信观察—Qwen—scene—adapter—编排—离线评估—Mock—Web 相关链 `443/443`、
  完整 Python `905/905`（`66.934s`）通过；前端合同合计 `28/28` 通过，静态编译、依赖、类型/算法所有权、
  旧导入/定义扫描和 `git diff --check` 通过。
- 初次最小回归暴露上一批架构测试仍要求 Qwen 暴露已下沉的稳定性类型，以及 Orchestrator 默认构建器造成
  application→infrastructure 反向依赖；前者改为确认 Qwen 不再导出，后者改为组合根强制注入，未改变业务。
- 本批没有改变观察字段/序列化、稳定/清晰/fingerprint/候选算法、Qwen prompt/provider/请求次数、选择结果、
  canonical、Controller、API、风险边界、设备执行、动作次数或真机语义，也没有重载项目 API、调用在线
  模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-27 轻量渐进式 DDD 模块化单体第二十六批（实施前冻结）

### 当前唯一缺口与根因证据

经过 typed context、视觉证据和可信观察三批拆分后，平铺 `poc/qwen_visual_decision.py` 已只剩 Qwen 单步
选择应用流程：provider 调用、确定性 canonical choices、模型 payload 解析/补全、blocked 决定和诊断指标。
它不再读取文件/环境或导入 infrastructure，但仍位于根目录，被 Web、离线评估和测试当作横向入口。

根因归类为 **Qwen 单步视觉决策应用用例尚未进入 application**。本批只物理迁移现有模块并更新直接导入；
不拆改选择算法、提示词、provider 端口、模型请求次数或输出协议。

### 同类失败与变化样本集合

1. 确定性精确候选、identity/recipient/input gates、launcher App、最近任务和完成判断保持。
2. provider JSON、page state、canonical hydration、目标区域、blocked/低置信度与诊断指标保持。
3. 变化样本：observe/navigate/effect、tap/swipe/drag/system navigation、输入/发送/清卡片保持。
4. 根目录旧文件/导入、application→infrastructure、转发 facade、第二 observer/协议常量必须消失。

### 通用修复

1. 将 `poc/qwen_visual_decision.py` 物理迁入 `poc/agent/application/qwen_visual_decision.py`，代码原样运行。
2. Web、离线评估与测试直接依赖 application；Qwen context/可信观察/canonical/scene 继续直接依赖 domain。
3. 删除根入口，不在 `agent.application.__init__` 建兼容重导出；增加应用依赖与旧导入防恢复测试。

### 影响、回滚与停止条件

- 影响范围：Qwen visual decision observer 与内部应用服务的模块路径。
- 不影响：协议/字段、提示词、provider、请求参数/次数、候选/选择/blocked、诊断、canonical、Controller、
  API、风险边界、设备执行、动作次数和真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时保留根模块与 application 模块或兼容 facade。
- 停止条件：任一模型请求、选择结果、异常、status/diagnostics 或公开 API 变化时停止并恢复。

### 验证清单

- Qwen 全协议/选择/候选/blocked/provider/diagnostics 正反与变化样本；
- DeepSeek、scene、adapter、Orchestrator、离线评估、最近任务、Mock 与 Web 相关回归；
- application 无 infrastructure/文件读取、唯一 observer/常量、旧根文件/导入防恢复扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 实施与验证结果

- 原 `poc/qwen_visual_decision.py` 已物理迁入 `poc/agent/application/qwen_visual_decision.py`；provider 调用、
  确定性 canonical choices、模型 payload 解析/补全、目标区域、blocked 决定和诊断指标保持同一实现。
- Web、离线评估与全部 Qwen/最近任务测试直接依赖 application；根目录不保留文件、alias、`__init__`
  重导出或第二 observer/协议常量。application 只依赖 domain、标准库与 PIL 帧载体，不导入 infrastructure、
  文件系统、环境变量、scene observer、Web 或设备实现。
- 最小与防恢复 `69/69`、Qwen—scene—adapter—编排—离线评估—最近任务—Mock—Web 相关链 `458/458`、
  完整 Python `906/906`（`69.468s`）通过；前端合同合计 `28/28` 通过，静态编译、application 依赖、
  observer/常量所有权、旧根文件/导入扫描和 `git diff --check` 通过。
- 本批没有改变协议/字段、提示词、provider、请求参数/次数、候选/选择/blocked、诊断、canonical、
  Controller、API、风险边界、设备执行、动作次数或真机语义，也没有重载项目 API、调用在线模型、执行
  真机动作或操作卖家 `main.exe`。

## 2026-08-27 轻量渐进式 DDD 模块化单体第二十七批（实施前冻结）

### 当前唯一缺口与根因证据

平铺 `poc/universal_action_controller.py` 已是纯确定性动作解析与验证服务：只消费 semantic action、UI scene、
typed input transaction 和 input lineage 领域合同，生成 resolved action 并校验动作后证据；它不读取文件、
不访问设备、不调用模型，但仍位于根目录并被 Web、adapter、能力验收和测试当作横向入口。

根因归类为 **唯一 canonical 动作控制领域服务尚未进入 domain**。本批只物理迁移现有模块并更新直接导入；
不拆改动作规则、几何算法、文字事务、异常、协议版本或公开状态中的逻辑权威名称。

### 同类失败与变化样本集合

1. tap、swipe、drag、long press、Back/Home/最近任务和 system navigation 的解析/后置验证保持。
2. 文字输入、清空、换行、键盘模式、中文候选、字段绑定和精确前缀事务保持。
3. 变化样本：局部 OCR 落点、页面容器拒绝、跨 App/页面身份、过期 fingerprint 和 typed transition 保持。
4. 根目录旧文件/导入、domain 反向依赖、转发 facade、第二 Controller/ResolvedAction 定义必须消失。

### 通用修复

1. 将 `poc/universal_action_controller.py` 物理迁入
   `poc/agent/domain/universal_action_controller.py`，保持同一实现与协议。
2. Web、adapter、能力验收和测试直接依赖 domain；公开架构状态中的 logical authority 名称保持不变。
3. 删除根入口，不在 `agent.domain.__init__` 建兼容重导出；增加依赖边界、唯一类型身份与旧导入防恢复测试。

### 影响、回滚与停止条件

- 影响范围：UniversalActionController、ResolvedSemanticAction、LocalPointGrounding 和错误类型的模块路径。
- 不影响：动作协议/规则、scene/canonical、几何/文字事务、异常文本、Qwen/DeepSeek、API、风险边界、设备
  执行、动作次数和真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时保留根模块与 domain 模块或兼容 facade。
- 停止条件：任一 resolved action、验证结论、异常、公开状态或执行调用发生变化时停止并恢复。

### 验证清单

- Controller 全动作解析/验证、UI scene、adapter、能力验收、最近任务和 Orchestrator 正反与变化样本；
- domain 无 application/infrastructure/Web/文件/PIL/模型/设备依赖，唯一 Controller/ResolvedAction 所有权；
- 根文件/旧导入防恢复扫描、一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 实施与验证结果

- 原 `poc/universal_action_controller.py` 已物理迁入
  `poc/agent/domain/universal_action_controller.py`；动作解析、局部落点、几何约束、typed input transaction、
  resolved action 和动作后证据验证保持同一实现与协议版本。
- Web、adapter、能力验收和测试已直接依赖 domain；根目录无文件、facade 或重导出，公开
  `execution_architecture.controller=universal_action_controller` 作为逻辑权威名称保持不变。
- 最小与防恢复 `96/96`、Controller—UI scene—adapter—能力验收—编排—Mock—Web 相关链 `531/531`、
  完整 Python `907/907`（`70.559s`）通过；前端合同合计 `28/28` 通过，静态编译、domain 依赖、
  Controller/ResolvedAction 唯一所有权、旧根文件/导入扫描和 `git diff --check` 通过。
- 本批没有改变动作协议/规则、scene/canonical、几何/文字事务、异常文本、Qwen/DeepSeek、公开 API、风险
  边界、设备执行、动作次数或真机语义，也没有重载项目 API、调用在线模型、执行真机动作或操作卖家
  `main.exe`。

## 2026-08-27 轻量渐进式 DDD 模块化单体第二十八批（实施前冻结）

### 当前唯一缺口与根因证据

平铺 `poc/generic_scene_observer.py` 是正式单步视觉观察 adapter：直接使用 PIL、本地图像测量、视觉 provider、
OCR、缓存/时间和硬件键盘配置，把一组真实帧转换为 `UIScene` 与输入结构审计。它已消费 domain/application
合同，却仍位于根目录，被 Web、执行 adapter、离线评估和测试当作横向入口。

根因归类为 **本地视觉观察实现尚未进入 infrastructure**。本批只物理迁移同一 observer 并更新直接导入；
不拆改 prompt、解析/规范化、输入审计、缓存、模型调用次数、scene 字段或异常。

### 同类失败与变化样本集合

1. 单步 fused observation、post-action context、system UI、页面身份和 canonical candidates 保持。
2. 专用输入审计、QWERTY/IME/中文候选、清空/换行/多字段和本地 OCR 几何恢复保持。
3. 变化样本：invalid coordinate grid、遮挡输入证据、reload、最近任务、keyboard mode 和 cache key 保持。
4. 根目录旧文件/导入、Web/Orchestrator/adapter 反向依赖、转发 facade 和第二 observer 定义必须消失。

### 通用修复

1. 将 `poc/generic_scene_observer.py` 物理迁入
   `poc/agent/infrastructure/generic_scene_observer.py`，保持同一实现与协议。
2. Web 组合根、adapter、离线评估和测试直接依赖 infrastructure；domain/application 不重导出 observer。
3. 删除根入口，增加基础设施边界、唯一类型身份与旧导入防恢复测试。

### 影响、回滚与停止条件

- 影响范围：SingleStepGenericSceneObserver、PostActionVisualContext 及观察辅助函数的模块路径。
- 不影响：观察协议/字段、prompt/provider/模型请求次数、解析/审计/缓存、Qwen/DeepSeek、Controller、API、
  风险边界、设备执行、动作次数和真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时保留根模块与 infrastructure 模块或兼容 facade。
- 停止条件：任一 scene、审计、诊断、模型请求、缓存键、异常或公开状态发生变化时停止并恢复。

### 验证清单

- scene observer 全协议/解析/审计/缓存正反与变化样本，输入 lineage、adapter、Qwen、编排和 Web 相关链；
- infrastructure 无 Web/Orchestrator/adapter 依赖，唯一 observer/context 所有权与根文件/旧导入防恢复扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 实施与验证结果

- 原 `poc/generic_scene_observer.py` 已物理迁入
  `poc/agent/infrastructure/generic_scene_observer.py`；PIL/本地图像测量、视觉 provider、OCR、缓存、prompt、
  scene 解析/规范化和专用输入结构审计保持同一实现与协议。
- Web 组合根、adapter、离线评估和测试已直接依赖 infrastructure；根目录无文件、facade、重导出或第二
  `SingleStepGenericSceneObserver`/`PostActionVisualContext`，infrastructure 不反向依赖 Web、Orchestrator
  或执行 adapter。
- 最小与防恢复 `88/88`、observer—输入 lineage—adapter—Qwen—编排—Mock—Web 相关链 `497/497`、
  完整 Python `908/908`（`85.669s`）通过；前端合同合计 `28/28` 通过，静态编译、基础设施边界、唯一
  observer/context 所有权、旧根文件/导入扫描和 `git diff --check` 通过。
- 本批没有改变观察协议/字段、prompt/provider/模型请求次数、解析/审计/缓存、Qwen/DeepSeek、Controller、
  API、风险边界、设备执行、动作次数或真机语义，也没有重载项目 API、调用在线模型、执行真机动作或
  操作卖家 `main.exe`。

## 2026-08-27 轻量渐进式 DDD 模块化单体第二十九批（实施前冻结）

### 当前唯一缺口与根因证据

平铺 `poc/generic_action_adapter.py` 是正式单动作执行 adapter：负责动作前后帧采集、本地 OCR/清晰度、方向
凭证、typed input lineage、Controller 解析/验证、设备执行和失败证据持久化。它直接使用 PIL、文件系统、
环境、时钟、OCR 与设备执行器，却仍位于根目录并被 Web、Orchestrator、能力验收和测试当作横向入口。

根因归类为 **单动作执行与证据适配实现尚未进入 infrastructure**。本批只物理迁移同一 adapter 并更新直接
导入和 mock patch 路径；不拆改动作、观察、OCR、方向、证据、设备调用或异常。

### 同类失败与变化样本集合

1. tap/swipe/drag/long press/system navigation、动作前解析与动作后验证保持。
2. 输入/清空/换行/IME/中文候选、local grounding、typed lineage 和失败证据保持。
3. 变化样本：Replay/Robot、帧不稳定、方向漂移、Qwen 失败脱敏、最近任务和 post-action context 保持。
4. 根目录旧文件/导入、Web/Orchestrator 反向依赖、旧 mock 路径、转发 facade 和第二 adapter 定义必须消失。

### 通用修复

1. 将 `poc/generic_action_adapter.py` 物理迁入
   `poc/agent/infrastructure/generic_action_adapter.py`，保持同一实现与协议。
2. Web 组合根、Orchestrator、能力验收和测试直接依赖 infrastructure；domain/application 不重导出 adapter。
3. 删除根入口并更新 patch 目标；增加基础设施边界、唯一类型身份与旧导入防恢复测试。

### 影响、回滚与停止条件

- 影响范围：GenericSingleActionAdapter、GenericActionExecutionResult、错误/诊断与 OCR 辅助函数的模块路径。
- 不影响：动作/观察协议、Controller/canonical、OCR/方向/lineage/证据算法、设备请求、Qwen/DeepSeek、API、
  风险边界、动作次数和真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时保留根模块与 infrastructure 模块或兼容 facade。
- 停止条件：任一执行请求/回执、scene、lineage、证据、异常、物理动作计数或公开状态变化时停止并恢复。

### 验证清单

- adapter 全动作/输入/证据/方向/Replay 正反与变化样本，Controller、observer、能力验收、编排和 Web 相关链；
- infrastructure 无 Web/Orchestrator 依赖，唯一 adapter/result 所有权与根文件/旧导入/旧 patch 防恢复扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 实施与验证结果

- 原 `poc/generic_action_adapter.py` 已物理迁入
  `poc/agent/infrastructure/generic_action_adapter.py`；帧采集、本地 OCR/清晰度、方向凭证、typed lineage、
  Controller 解析/验证、设备执行和失败证据持久化保持同一实现。
- 相关回归最初证明 Orchestrator 直接依赖具体 infrastructure 会破坏既定依赖方向；因此纯
  `PostActionVisualContext`/阶段常量唯一归入 `agent/domain/post_action_observation.py`，应用端
  `GenericActionAdapterError`/`GenericSingleActionAdapterPort` 唯一归入 `agent/application/action_adapter.py`，
  Orchestrator 只消费 application/domain，具体 adapter 继续由 Web 组合根注入。
- 根目录无 adapter 文件/facade/重导出，旧 import 与 mock patch 路径均已删除；infrastructure 不反向依赖
  Web/Orchestrator，domain/application 不依赖 infrastructure，observer 不再持有第二 post-action context。
- 最小与防恢复 `211/211`、adapter—Controller—observer—能力验收—编排—Mock—Web 相关链 `509/509`、
  完整 Python `909/909`（`78.705s`）通过；前端合同合计 `28/28` 通过，静态编译、依赖方向、唯一类型
  所有权、旧根文件/导入/patch 扫描和 `git diff --check` 通过。
- 本批没有改变动作/观察协议、Controller/canonical、OCR/方向/lineage/证据算法、设备请求、Qwen/DeepSeek、
  API、风险边界、动作次数或真机语义，也没有重载项目 API、调用在线模型、执行真机动作或操作卖家
  `main.exe`。

## 2026-08-27 轻量渐进式 DDD 模块化单体第三十批（实施前冻结）

### 当前唯一缺口与根因证据

平铺 `poc/universal_agent_orchestrator.py` 是正式通用 Agent 应用编排用例：推进运行会话、任务图、单步观察、
canonical 选择、一次一动作执行、动作后验证与终态。前批拆分后它已只消费 application/domain 合同，但仍位于
根目录，并直接依赖根目录内存用量账本与文件系统 DeepSeek 失败诊断实现。

根因归类为 **通用 Agent 编排应用用例及其剩余依赖尚未完成分层**。本批只物理迁移同一编排器和内存用量账本，
把文件系统诊断写入移至 infrastructure 并由 Web 组合根注入；不拆改编排顺序、任务图、canonical、模型调用、
动作执行、终态或公开 API。

### 同类失败与变化样本集合

1. start/continue/cancel、任务图推进、一次一动作、纠正重规划和 succeeded/failed/blocked 终态保持。
2. Qwen/DeepSeek 用量、模型身份、请求计数、Token/耗时/费用账本和错误分类保持。
3. 变化样本：DeepSeek 失败诊断持久化、无诊断 writer 的纯应用用例、Mock/Replay 与 Web 组合根保持。
4. 根目录旧文件/导入、application→infrastructure、转发 facade、第二 Orchestrator/usage ledger 定义必须消失。

### 通用修复

1. 将 `poc/universal_agent_orchestrator.py` 与 `poc/vision_usage.py` 物理迁入 `poc/agent/application/`。
2. 将 `poc/deepseek_failure_diagnostics.py` 物理迁入 `poc/agent/infrastructure/`，以注入 callable 实现端口边界。
3. Web 组合根注入真实诊断 writer；离线调用未注入时使用无副作用空实现。全部调用方直接依赖新路径。
4. 删除三个根入口，不建立 `__init__` 重导出或兼容 facade；增加依赖、唯一类型身份与旧导入防恢复测试。

### 影响、回滚与停止条件

- 影响范围：UniversalAgentOrchestrator、VisionSessionUsageLedger 和 DeepSeek 失败诊断 writer 的模块路径与装配。
- 不影响：任务/动作协议、编排顺序、模型 prompt/provider/请求次数、用量计算、canonical、Controller、API、
  风险边界、设备执行、动作次数和真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时保留根模块与分层模块或兼容 facade。
- 停止条件：任一会话状态、任务图推进、模型请求/用量、诊断内容、执行回执、异常或公开 API 变化时停止并恢复。

### 验证清单

- Orchestrator start/continue/终态/纠正/输入/发送/诊断正反与变化样本，usage、DeepSeek、Qwen、Mock、Web 相关链；
- application 无 infrastructure/Web/文件写入依赖，Web 显式注入诊断 writer，唯一 Orchestrator/ledger/诊断所有权；
- 根文件/旧导入/旧 patch 防恢复扫描、一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 实施与验证结果

- 原 `poc/universal_agent_orchestrator.py` 与 `poc/vision_usage.py` 已物理迁入 `poc/agent/application/`；原
  `poc/deepseek_failure_diagnostics.py` 已物理迁入 `poc/agent/infrastructure/`。根目录不保留文件、facade、
  `__init__` 重导出、旧 import/patch 或第二类型/函数身份。
- Orchestrator 只消费 application/domain 与注入端口；已有报告读取由证据存储端口承担，Web 组合根显式
  注入文件系统诊断 writer，离线调用默认无副作用。application 不导入 infrastructure、Web、模型 SDK、
  Robot 或文件读写实现。
- 最小与防恢复 `202/202`、DeepSeek—Qwen—scene—adapter—编排—Mock—doctor—能力验收—Web 相关链
  `578/578`、完整 Python `910/910`（`82.203s`）通过；前端合同合计 `28/28` 通过，静态编译、依赖方向、
  唯一 Orchestrator/usage ledger/诊断 writer 所有权、旧根文件/导入/patch 扫描和 `git diff --check` 通过。
- 本批没有改变任务/动作协议、编排顺序、模型 prompt/provider/请求次数、用量计算、canonical、Controller、
  API、风险边界、设备执行、动作次数或真机语义，也没有重载项目 API、调用在线模型、执行真机动作或操作
  卖家 `main.exe`。

## 2026-08-27 轻量渐进式 DDD 模块化单体第三十一批（实施前冻结）

### 当前唯一缺口与根因证据

平铺 `poc/intent_provider.py` 负责读取 DeepSeek 环境配置、发起 HTTP 请求、解析响应并处理超时/传输错误与
有界重试，是 DeepSeek task-graph application port 的具体外部服务实现，却仍位于根目录并由 Web 直接导入。

根因归类为 **DeepSeek 文本模型 provider 尚未进入 infrastructure**。本批只物理迁移同一 provider 并更新
Web/测试导入和 mock patch 路径；不改变模型、URL、请求体、重试、错误文本、状态或调用次数。

### 同类失败与变化样本集合

1. 已配置/未配置、环境变量与 Windows 用户环境回退保持。
2. 成功 JSON、空内容、无效响应、HTTP/transient/timeout/transport 重试保持。
3. 变化样本：显式 key/model/base URL、零重试延迟和错误状态投影保持。
4. 根目录旧文件/导入、转发 facade、第二 provider/error 定义和旧 mock patch 必须消失。

### 通用修复

1. 将 `poc/intent_provider.py` 物理迁入 `poc/agent/infrastructure/deepseek_intent_provider.py`。
2. Web 组合根与测试直接依赖 infrastructure；application 继续只消费既有 provider port。
3. 删除根入口，不建立 `__init__` 重导出或兼容 facade；增加唯一类型身份与旧导入防恢复测试。

### 影响、回滚与停止条件

- 影响范围：DeepSeekIntentProvider、IntentProviderError 及测试 patch 的模块路径。
- 不影响：模型/URL、环境变量、请求体、thinking/temperature、超时/重试、响应解析、任务图、API、Qwen、
  canonical、Controller、风险边界、设备执行、动作次数和真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时保留根模块与 infrastructure 模块或兼容 facade。
- 停止条件：任一请求参数、尝试次数、状态、异常文本或 Web 公开结果变化时停止并恢复。

### 验证清单

- provider 成功/未配置/环境回退/HTTP/超时/传输/响应错误正反与变化样本；
- DeepSeek planner、Orchestrator、runtime doctor、Web 相关链和 provider 单一所有权/旧导入/patch 扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 实施与验证结果

- 原 `poc/intent_provider.py` 已物理迁入 `poc/agent/infrastructure/deepseek_intent_provider.py`；环境读取、
  HTTP 请求/响应、状态、重试和异常保持同一实现。Web 与测试直接依赖新路径，根目录不保留文件、facade、
  重导出、第二 provider/error 或旧 mock patch。
- 最小与防恢复 `117/117`、DeepSeek planner—编排—doctor—Web 相关链 `361/361`、完整 Python
  `911/911`（`83.591s`）通过；前端合同合计 `28/28` 通过，静态编译、唯一类型所有权、旧根文件/导入/
  patch 扫描和 `git diff --check` 通过。
- 本批没有改变模型/URL、环境变量、请求体、thinking/temperature、超时/重试、响应解析、任务图、API、
  Qwen、canonical、Controller、风险边界、设备执行、动作次数或真机语义，也没有重载项目 API、调用在线
  模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-27 轻量渐进式 DDD 模块化单体第三十二批（实施前冻结）

### 当前唯一缺口与根因证据

平铺 `poc/vision_agent.py` 负责 DashScope 环境装配、JPEG 编码、HTTP 请求、响应解析、重试与会话用量回执；
平铺 `poc/qwen_runtime_errors.py` 分类该运行链的传输、格式、协议与本地停止错误。二者均被正式 scene/adapter
基础设施消费，却仍位于根目录。

根因归类为 **Qwen 视觉模型外部服务实现尚未完整进入 infrastructure**。本批只物理迁移同一 provider 与其
运行诊断，更新直接导入和 mock patch；不改变模型、图像、请求、重试、用量、错误分类或模型调用次数。

### 同类失败与变化样本集合

1. JPEG 缩放/编码、重复 JSON key、fenced JSON、无效/截断响应保持。
2. 配置/模型身份、request scope、usage ledger、cache hit 和并发元数据隔离保持。
3. HTTP/transient/inline URL rejection/timeout/transport 重试及错误分类保持。
4. 根目录旧文件/导入、旧 mock patch、转发 facade、第二 provider/分类器必须消失。

### 通用修复

1. 将 `poc/vision_agent.py` 物理迁入 `poc/agent/infrastructure/dashscope_vision_provider.py`。
2. 将 `poc/qwen_runtime_errors.py` 物理迁入 `poc/agent/infrastructure/qwen_runtime_errors.py`。
3. Web、scene/adapter、离线评估和测试直接依赖 infrastructure；domain/application 不重导出实现。
4. 删除根入口，不建立兼容 facade；增加唯一类型/函数身份、旧导入和旧 patch 防恢复测试。

### 影响、回滚与停止条件

- 影响范围：DashScopeVisionProvider、JPEG/JSON helpers、Qwen runtime diagnostics 的模块路径。
- 不影响：视觉协议、模型配置/身份、图片尺寸/质量、请求体、重试、用量、错误分类、prompt、scene/Qwen 决策、
  canonical、Controller、API、风险边界、设备执行、动作次数和真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时保留根模块与 infrastructure 模块或兼容 facade。
- 停止条件：任一图片字节、请求参数/次数、usage、错误分类、响应解析、scene 或公开状态变化时停止并恢复。

### 验证清单

- provider 图片/JSON/配置/网络/用量/并发与 runtime error 正反和变化样本；
- Qwen、scene、adapter、离线评估、Orchestrator、doctor、Web 相关链与旧导入/patch 扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 实施与验证结果

- 原 `poc/vision_agent.py` 已物理迁入 `poc/agent/infrastructure/dashscope_vision_provider.py`；原
  `poc/qwen_runtime_errors.py` 已物理迁入 `poc/agent/infrastructure/qwen_runtime_errors.py`。Web、scene/
  adapter、离线评估和测试直接依赖新路径，根目录无文件/facade/重导出/第二实现或旧 mock patch。
- 最小与防恢复 `85/85`、Qwen provider—scene—adapter—编排—doctor—Web 相关链 `465/465`、完整 Python
  `911/911`（`84.785s`）通过；前端合同合计 `28/28` 通过，静态编译、唯一类型/函数所有权、旧根文件/
  导入/patch 扫描和 `git diff --check` 通过。
- 本批没有改变视觉协议、模型配置/身份、图片尺寸/质量、请求体、重试、用量、错误分类、prompt、scene/Qwen
  决策、canonical、Controller、API、风险边界、设备执行、动作次数或真机语义，也没有重载项目 API、
  调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-27 轻量渐进式 DDD 模块化单体第三十三批（实施前冻结）

### 当前唯一缺口与根因证据

平铺 `poc/ocr_runtime.py` 负责 Windows OCR PowerShell 资源定位、临时图片、子进程调用、JSON/错误解析、缩放
恢复与文字框匹配，是单动作 adapter 的具体本地 OCR 实现，却仍位于根目录。

根因归类为 **Windows OCR adapter 尚未进入 infrastructure**。本批只物理迁移同一实现并保留其 `poc/`
资源根；不改变 OCR 脚本、语言、缩放、子进程参数、超时、解析、匹配或坐标。

### 同类失败与变化样本集合

1. Windows/PowerShell/脚本可用性与不可用错误保持。
2. 多词合并、整行退化、空目标、去重和 center 几何保持。
3. 缩放恢复、子进程非零、无效/非对象 JSON 与临时文件清理保持。
4. 根目录旧文件/导入、错误脚本根、转发 facade 和第二 OCR 类型/函数必须消失。

### 通用修复

1. 将 `poc/ocr_runtime.py` 物理迁入 `poc/agent/infrastructure/windows_ocr_runtime.py`。
2. 资源根显式保持为 `poc/`，adapter 直接依赖新 infrastructure 路径。
3. 删除根入口，不建立兼容 facade；增加资源路径、匹配变化样本、唯一身份与旧导入防恢复测试。

### 影响、回滚与停止条件

- 影响范围：OCR runtime/OcrMatch/错误类型及 adapter import 的模块路径。
- 不影响：PowerShell/脚本、语言、图片缩放、子进程命令/超时、JSON、匹配/坐标、动作落点、API、模型、
  canonical、Controller、风险边界、设备执行、动作次数和真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时保留根模块与 infrastructure 模块或兼容 facade。
- 停止条件：任一资源路径、子进程参数、OCR payload、匹配框、异常或动作落点变化时停止并恢复。

### 验证清单

- OCR 可用性、匹配/空目标/多词变化样本、缩放/子进程/解析/清理合同；
- adapter、输入/QWERTY、本地文字落点、Orchestrator、Web 相关链与旧导入扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不启动 OCR 子进程、不重载项目 API、不调用在线模型、不执行真机动作、不操作
  卖家 `main.exe`。

### 实施与验证结果

- 原 `poc/ocr_runtime.py` 已物理迁入 `poc/agent/infrastructure/windows_ocr_runtime.py`；移动后资源根仍为
  `poc/`，`OCR_SCRIPT` 保持指向原 `poc/windows_ocr.ps1`。adapter 直接依赖新路径，根目录无文件/facade、
  重导出、第二 OCR 实现或旧 import。
- 最小与防恢复 `167/167`、OCR—adapter—输入/QWERTY—编排—Web 相关链 `479/479`、完整 Python
  `912/912`（`86.150s`）通过；前端合同合计 `28/28` 通过，静态编译、资源路径、唯一类型/函数所有权、
  旧根文件/导入扫描和 `git diff --check` 通过。
- 本批没有改变 PowerShell/脚本、语言、图片缩放、子进程命令/超时、JSON、匹配/坐标、动作落点、API、
  模型、canonical、Controller、风险边界、设备执行、动作次数或真机语义，也没有启动 OCR 子进程、重载
  项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-27 轻量渐进式 DDD 模块化单体第三十四批（实施前冻结）

### 当前唯一缺口与根因证据

平铺 `poc/orientation_safety.py` 负责 PIL 帧绑定、方向凭据签发/校验、进程内一次性 live seal 与物理执行门，
被 Robot、设备执行器、通用 adapter 和能力验收共同消费，却仍位于根目录。

根因归类为 **机械执行方向安全 adapter 尚未进入 infrastructure**。本批只物理迁移同一实现并更新导入；
不改变凭据协议、阈值、帧算法、一次性 claim/consume、动作范围或错误。

### 同类失败与变化样本集合

1. audited/QWERTY/single-step 三种签发源、设备/场景/帧/置信绑定保持。
2. 正向、90/180/270/unknown、固定系统导航与普通几何动作差异保持。
3. 亮度/结构漂移、复制/伪造、重复 claim/consume、错误动作与并发保持。
4. 根目录旧文件/导入、转发 facade、第二 credential/gate/seal 表必须消失。

### 通用修复

1. 将 `poc/orientation_safety.py` 物理迁入 `poc/agent/infrastructure/orientation_safety.py`。
2. Robot、device executor、adapter、能力验收和测试直接依赖 infrastructure。
3. 删除根入口，不建立兼容 facade；增加唯一类型身份与旧导入防恢复测试。

### 影响、回滚与停止条件

- 影响范围：OrientationCredential、PhysicalExecutionGate、错误/签发 helpers 的模块路径。
- 不影响：凭据协议/字段、阈值、帧 fingerprint/视觉绑定、一次性授权、支持动作、Controller、API、风险边界、
  设备 transport、动作次数和真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时保留根模块与 infrastructure 模块或兼容 facade。
- 停止条件：任一凭据字段、授权结论、像素漂移、claim/consume、异常或物理动作门变化时停止并恢复。

### 验证清单

- orientation 全协议/帧/签发/claim/consume/并发正反与变化样本；
- Robot、device executor、adapter、能力验收、编排和 Web 相关链与旧导入扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 实施与验证结果

- 原 `poc/orientation_safety.py` 已物理迁入 `poc/agent/infrastructure/orientation_safety.py`；Robot、device
  executor、adapter、能力验收和测试直接依赖新路径，根目录无文件/facade/重导出、第二 credential/gate/
  seal 表或旧 import。
- 最小与防恢复 `58/58`、方向—Robot—device executor—adapter—能力验收—编排—Web 相关链 `493/493`、
  完整 Python `913/913`（`89.228s`）通过；前端合同合计 `28/28` 通过，静态编译、唯一类型/状态所有权、
  旧根文件/导入扫描和 `git diff --check` 通过。
- 本批没有改变凭据协议/字段、阈值、帧 fingerprint/视觉绑定、一次性授权、支持动作、Controller、API、
  风险边界、设备 transport、动作次数或真机语义，也没有重载项目 API、调用在线模型、执行真机动作或
  操作卖家 `main.exe`。

## 2026-08-27 轻量渐进式 DDD 模块化单体第三十五批（实施前冻结）

### 当前唯一缺口与根因证据

平铺 `poc/tap_calibration.py` 负责标定文件读写、仿射拟合、覆盖凸包、点击纠偏和系统边缘轨迹推导，
被 Robot、能力验收与独立标定工具共同消费，却仍位于根目录。

根因归类为 **触控标定基础设施尚未进入 infrastructure**。本批只物理迁移同一实现并显式保持原
`poc/tap_calibration.json` 资源位置；不改变拟合算法、阈值、覆盖判定、纠偏、轨迹、异常或设备动作。

### 同类失败与变化样本集合

1. 仿射拟合/逆变换、点数不足、共线、奇异与噪声拒绝保持。
2. 启用/禁用、v1/v2、等比/非等比尺寸、覆盖凸包与边缘轨迹正反样本保持。
3. 默认路径、显式临时路径、保存/加载和 Robot/验收/标定工具消费保持。
4. 根目录旧文件/导入、错误资源根、转发 facade 和第二标定实现必须消失。

### 通用修复

1. 将 `poc/tap_calibration.py` 物理迁入 `poc/agent/infrastructure/tap_calibration.py`。
2. 默认标定资源路径显式保持为原 `poc/tap_calibration.json`；所有正式消费者直接依赖 infrastructure。
3. 删除根入口，不建立兼容 facade；增加默认资源路径、唯一类型/函数身份与旧导入防恢复测试。

### 影响、回滚与停止条件

- 影响范围：Affine2D、TapCalibrationError、标定构建/保存/加载、点纠偏和系统边缘轨迹 helper 的模块路径。
- 不影响：标定 JSON 路径/schema、版本/阈值、拟合/覆盖/纠偏算法、轨迹、Robot、API、模型、canonical、
  Controller、风险边界、设备 transport、动作次数和真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时保留根模块与 infrastructure 模块或兼容 facade。
- 停止条件：任一资源路径、标定 payload、矩阵、覆盖结论、纠偏点、轨迹、异常或设备调用变化时停止并恢复。

### 验证清单

- 标定拟合/覆盖/加载/保存/纠偏/系统边缘轨迹正反与变化样本；
- Robot、能力验收、标定工具、adapter、编排和 Web 相关链与旧导入扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不运行标定工具、不重载项目 API、不调用在线模型、不执行真机动作、不操作
  卖家 `main.exe`。

### 实施与验证结果

- 原 `poc/tap_calibration.py` 已物理迁入 `poc/agent/infrastructure/tap_calibration.py`；默认资源路径显式
  保持为原 `poc/tap_calibration.json`。Robot、能力验收、独立标定工具和测试直接依赖新路径，根目录无
  文件/facade/重导出、第二标定实现或旧 import/patch。
- 最小与防恢复 `76/76`、标定—方向—Robot—device executor—能力验收—adapter—编排—Web 相关链
  `539/539`、完整 Python `914/914`（`93.617s`）通过；前端合同合计 `28/28` 通过，静态编译、默认资源
  路径、唯一类型/函数所有权、旧根文件/导入/patch 扫描和 `git diff --check` 通过。
- 本批没有改变标定 JSON 路径/schema、版本/阈值、拟合/覆盖/纠偏算法、轨迹、Robot、API、模型、canonical、
  Controller、风险边界、设备 transport、动作次数或真机语义，也没有运行标定工具、重载项目 API、调用
  在线模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-27 轻量渐进式 DDD 模块化单体第三十六批（实施前冻结）

### 当前唯一缺口与根因证据

平铺 `poc/robot_gui_poc.py` 负责卖家 Windows 窗口发现、DPI/客户区几何、相机截图、鼠标/键盘原语、事件
栅栏和光标临时租约，是 Robot 与独立标定/采集工具共同消费的具体 Win32 adapter，却仍位于根目录。

根因归类为 **卖家控制端 Win32 adapter 尚未进入 infrastructure**。本批只物理迁移同一实现并显式保持
原 `poc/output` 资源根；不改变窗口标题、几何、截图、输入事件、光标、等待或物理动作。

### 同类失败与变化样本集合

1. 100%/125%/150% DPI、横竖屏、裁剪/小对话框与客户区几何保持。
2. 被动截图、遮挡检查、最小化拒绝、事件栅栏、点击/拖动/长按与异常清理保持。
3. 光标已在/不在相机区、用户中途移动、异常恢复和虚拟桌面覆盖变化样本保持。
4. 根目录旧文件/导入/patch、错误资源根、转发 facade 和第二 Win32 adapter 必须消失。

### 通用修复

1. 将 `poc/robot_gui_poc.py` 物理迁入 `poc/agent/infrastructure/seller_window_adapter.py`。
2. 原资源根显式保持为 `poc/`；Robot、标定/采集工具和测试直接依赖 infrastructure。
3. 删除根入口，不建立兼容 facade；增加默认资源路径、唯一结构/函数身份与旧导入/patch 防恢复测试。

### 影响、回滚与停止条件

- 影响范围：卖家窗口 adapter 的模块路径及测试 patch 路径。
- 不影响：窗口标题、DPI、几何、相机截图/裁剪、鼠标/键盘事件、光标租约、事件栅栏、Robot、API、模型、
  canonical、Controller、风险边界、动作次数和真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时保留根模块与 infrastructure 模块或兼容 facade。
- 停止条件：任一资源路径、几何、截图、事件序列、光标恢复、回执、异常或 Robot 调用变化时停止并恢复。

### 验证清单

- DPI/布局/相机/事件栅栏/光标租约/输入原语正反与变化样本；
- Robot、标定/采集工具、device executor、adapter、编排和 Web 相关链与旧导入/patch 扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不捕获卖家窗口、不重载项目 API、不调用在线模型、不执行真机动作、不操作
  卖家 `main.exe`。

### 实施与验证结果

- 原 `poc/robot_gui_poc.py` 已物理迁入 `poc/agent/infrastructure/seller_window_adapter.py`；资源根仍为
  `poc/`、输出根仍为 `poc/output`。Robot、标定/采集工具和测试直接依赖新路径，根目录无文件/facade、
  重导出、第二 Win32 adapter 或旧 import/patch。
- 最小与防恢复 `66/66`、Win32/DPI—标定—方向—Robot—device executor—能力验收—adapter—编排—Web
  相关链 `560/560`、完整 Python `915/915`（`96.127s`）通过；前端合同合计 `28/28` 通过，静态编译、
  资源路径、唯一结构/函数所有权、旧根文件/导入/patch 扫描和 `git diff --check` 通过。
- 本批没有改变窗口标题、DPI、几何、相机截图/裁剪、鼠标/键盘事件、光标租约、事件栅栏、Robot、API、
  模型、canonical、Controller、风险边界、动作次数或真机语义，也没有捕获卖家窗口、重载项目 API、调用
  在线模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-27 轻量渐进式 DDD 模块化单体第三十七批（实施前冻结）

### 当前唯一缺口与根因证据

平铺 `poc/robot_core.py` 负责 Robot/Mock controller、设备状态、截图、动作原语、手机可见键位输入、配置读取、
停止边界和本地回执，是设备注册、执行 adapter、观察和 Web 组合根共同消费的具体硬件 controller，却仍
位于根目录。

根因归类为 **Robot controller 基础设施尚未进入 infrastructure**。本批只物理迁移同一实现并显式保持原
`poc/controller_config.json` 与 `poc/tap_calibration.json` 路径；不改变动作、输入、配置、回执或停止语义。

### 同类失败与变化样本集合

1. controller/camera/busy/stop、新任务清除旧 stop 与 Mock 独立状态保持。
2. tap/dismiss/back/home/recent/swipe/double/long/drag/reveal 与一次性方向门保持。
3. QWERTY anchor、大小写/拼音/可见键/退格、配置默认/覆盖/损坏和多设备标定变化样本保持。
4. 根目录旧文件/导入/patch、错误配置/标定根、转发 facade 和第二 Robot/Mock controller 必须消失。

### 通用修复

1. 将 `poc/robot_core.py` 物理迁入 `poc/agent/infrastructure/robot_controller.py`。
2. 两个默认资源路径显式保持在原 `poc/`；设备注册、scene/adapter、Web、工具和测试直接依赖 infrastructure。
3. 删除根入口，不建立兼容 facade；增加默认路径、唯一 controller/helper 身份与旧导入/patch 防恢复测试。

### 影响、回滚与停止条件

- 影响范围：RobotController、MockRobotController、WorkflowNotReady、配置/QWERTY helpers 的模块路径。
- 不影响：配置/标定 JSON 路径与 schema、窗口/截图、动作/输入序列、方向门、停止状态、回执、API、模型、
  canonical、风险边界、动作次数和真机证据。
- 回滚方式：整体撤销本批单一提交；不得同时保留根模块与 infrastructure 模块或兼容 facade。
- 停止条件：任一默认路径、配置值、QWERTY 点、动作参数、stop、回执、异常或公开设备状态变化时停止并恢复。

### 验证清单

- controller 配置/状态/停止、QWERTY、全动作/输入/Mock、资源路径正反与变化样本；
- registry、scene、adapter、能力验收、编排和 Web 相关链与旧导入/patch 扫描；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线行为保持迁移，不实例化真实 controller、不重载项目 API、不调用在线模型、不执行真机动作、不
  操作卖家 `main.exe`。

### 实施与验证结果

- 原 `poc/robot_core.py` 已物理迁入 `poc/agent/infrastructure/robot_controller.py`；默认配置仍指向
  `poc/controller_config.json`，默认标定仍指向 `poc/tap_calibration.json`，Web 输出仍位于
  `poc/output/web`。
- 设备 registry、scene observer、单动作 adapter、Web、标定/采集工具和测试已直接依赖 infrastructure；
  根目录无旧文件、转发 facade、旧 import/patch 或第二 Robot/Mock controller/helper 身份。
- 最小与防恢复 `62/62`、controller—registry—scene—adapter—能力验收—编排—Web 相关链 `589/589`、
  完整 Python `916/916`（`98.330s`）通过；前端合同合计 `28/28` 通过，静态编译、默认资源路径、唯一
  controller/helper 身份、旧根文件/导入/patch 扫描和 `git diff --check` 通过。
- 本批没有改变配置/标定 JSON 路径或 schema、窗口/截图、动作/输入序列、方向门、stop、新任务清理、
  回执、API、模型、canonical、Controller、风险边界、动作次数或真机语义，也没有实例化真实 controller、
  重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-27 轻量渐进式 DDD 模块化单体第三十八批（实施前冻结）

### 当前唯一缺口与根因证据

平铺 `poc/capability_acceptance_planner.py` 是确定性能力验收任务图 planner，正式 Web 组合根直接使用；它只
编排领域任务图，却反向从混合文件 `poc/capability_acceptance.py` 读取可晋级动作集合，因而既未进入
application，也让同一动作资格合同被基础设施模块持有。

根因归类为 **能力验收 planning 用例及其动作资格合同尚未进入 application/domain**。本批只迁移 planner，
并把现有动作集合移动到 `agent/domain/action_capabilities.py` 成为同一对象的唯一权威。

### 同类失败与变化样本集合

1. drag 确定性 plan、matched 完成、mismatched 阻断和未知动作拒绝保持。
2. PROMOTABLE/CALIBRATION_BOUND 动作集合的成员、对象身份和所有正式消费者保持。
3. planner 继续只依赖 domain，不导入 infrastructure、Web、文件系统、模型或设备实现。
4. 根目录旧文件/导入、旧集合定义、转发 facade 和第二 planner/动作资格权威必须消失。

### 通用修复

1. 将 planner 物理迁入 `poc/agent/application/capability_acceptance_planner.py`。
2. 将两个既有动作集合移入 `poc/agent/domain/action_capabilities.py`，所有消费者直接引用 domain。
3. 删除根 planner 入口，不建立兼容 facade；增加唯一 planner/集合身份与旧导入防恢复测试。

### 影响、回滚与停止条件

- 影响范围：能力验收 planner 和两个动作资格集合的模块路径。
- 不影响：动作集合成员、任务图文字/结构、验收执行/报告/晋级、公开 API、模型、canonical、Controller、
  风险边界、动作次数和真机语义。
- 回滚方式：整体撤销本批单一提交；不得同时保留根 planner、第二集合或兼容 facade。
- 停止条件：任一集合成员、任务图字段/文字、matched/blocked 终态、异常或公开 API 变化时停止并恢复。

### 验证清单

- planner 正反与变化样本、动作集合身份/成员和旧根文件/导入扫描；
- 能力验收、任务图、编排和 Web 相关链；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线职责迁移，不重载项目 API、不调用在线模型、不执行真机动作、不操作卖家 `main.exe`。

### 实施与验证结果

- 原 `poc/capability_acceptance_planner.py` 已物理迁入
  `poc/agent/application/capability_acceptance_planner.py`；`PROMOTABLE_ACTIONS` 与
  `CALIBRATION_BOUND_ACTIONS` 现由 `poc/agent/domain/action_capabilities.py` 唯一定义。
- Web、验收 runtime/core 和测试直接依赖 application/domain；根目录无旧 planner、转发 facade、旧 import、
  第二 planner 或第二动作资格集合，现有 runtime/core 暂仍作为后续独立迁移切片。
- 最小与防恢复 `48/48`、能力资格—planner—任务图—验收—编排—Web 相关链 `472/472`、完整 Python
  `917/917`（`101.318s`）通过；前端合同 `11/11 + 13/13 + 4/4 = 28/28` 通过，静态编译、唯一对象身份、
  旧根文件/导入/集合定义扫描和 `git diff --check` 通过。
- 完整回归首次运行时，既有 Mock 滑动用例在稳定四帧采集时限内偶发超时；该用例在此前相关链、随后单项
  `1/1` 和最终完整回归中均通过，归类为测试时序波动，未修改生产代码或测试阈值。
- 本批没有改变动作集合成员、任务图文字/结构、验收执行/报告/晋级、公开 API、模型、canonical、Controller、
  风险边界、动作次数或真机语义，也没有重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 2026-08-27 轻量渐进式 DDD 模块化单体第三十九批（实施前冻结）

### 当前唯一缺口与根因证据

平铺 `poc/capability_acceptance.py` 负责触控标定证据读取、动作执行证据校验、四帧/JPEG 报告文件校验、
live promotion source 绑定、一次性晋级 authority 和设备注册表原子更新；这些都是使用 PIL、文件系统和进程锁
的具体能力验收基础设施，却仍由 Web、验收 runtime 和测试从根目录直接导入。

根因归类为 **能力验收报告与晋级基础设施尚未进入 infrastructure**。第三十八批已移除 planner 对该混合模块
的反向依赖，因此本批可以物理迁移同一实现而不改变领域/application 边界。

### 同类失败与变化样本集合

1. 标定证据、exact input、各动作执行证据和报告正反校验保持。
2. PromotionScope/Authority 的 live 对象绑定、一次性消费、漂移/重放拒绝保持。
3. registry lock、摘要绑定、原子替换、晋级证据和并发变化样本保持。
4. 根目录旧文件/导入/patch、转发 facade 和第二 validator/promoter/authority 必须消失。

### 通用修复

1. 将 `poc/capability_acceptance.py` 物理迁入 `poc/agent/infrastructure/capability_acceptance.py`。
2. Web、验收 runtime、adapter 测试和验收测试直接依赖 infrastructure。
3. 删除根入口，不建立兼容 facade；增加唯一类型/函数所有权与旧导入/patch 防恢复测试。

### 影响、回滚与停止条件

- 影响范围：能力验收报告 validator、live promotion authority 和 registry promoter 的模块路径。
- 不影响：报告版本/schema、动作资格集合、证据规则、授权/锁/摘要/原子写语义、公开 API、模型、canonical、
  Controller、风险边界、动作次数和真机语义。
- 回滚方式：整体撤销本批单一提交；不得同时保留根模块与 infrastructure 模块或兼容 facade。
- 停止条件：任一报告字段、证据规则、authority 生命周期、registry 写入、异常或公开 API 变化时停止并恢复。

### 验证清单

- validator/promoter/authority 正反、并发和变化样本，唯一身份及旧根文件/导入/patch 扫描；
- 能力验收 runtime、adapter、编排和 Web 相关链；
- 一次完整 Python 回归、前端合同、静态编译和 `git diff --check`；
- 本批为离线职责迁移，不读取真实验收证据、不写设备注册表、不重载项目 API、不调用在线模型、不执行
  真机动作、不操作卖家 `main.exe`。

### 实施与验证结果

- 原 `poc/capability_acceptance.py` 已物理迁入
  `poc/agent/infrastructure/capability_acceptance.py`；报告 validator、live promotion authority 和 registry
  promoter 保持同一实现与类型身份。
- Web、验收 runtime 和测试直接依赖 infrastructure；根目录无旧文件、转发 facade、旧 import/patch 或第二
  validator/promoter/authority，第三十八批的动作资格集合仍由 domain 唯一持有。
- 最小与防恢复 `208/208`、标定—方向—执行证据—验收 runtime—adapter—编排—Web 相关链 `540/540`、
  完整 Python `918/918`（`102.679s`）通过；前端合同 `11/11 + 13/13 + 4/4 = 28/28` 通过，静态编译、
  唯一类型/函数所有权、旧根文件/导入/patch 扫描和 `git diff --check` 通过。
- 本批没有改变报告版本/schema、动作资格集合、证据规则、authority 生命周期、锁/摘要/原子写、公开 API、
  模型、canonical、Controller、风险边界、动作次数或真机语义，也没有读取真实验收证据、写设备注册表、
  重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`。
