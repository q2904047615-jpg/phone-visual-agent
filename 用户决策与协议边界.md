# 用户决策与协议边界

更新时间：2026-08-22（Asia/Shanghai）
状态：当前唯一产品边界清单

本文记录用户已经批准的自动化范围和正式协议边界。`AGENTS.md`、交接文档、历史验收台账、旧协议、测试和代码若与本文冲突，必须删除或迁移冲突内容，不能保留旧否决权。

## 1. 当前用户决定

- 产品首先实现跨 App、跨措辞的通用全自动手机视觉操作 Agent。
- 用户交给 Agent 的普通任务视为已授权执行，不再逐次请求确认。
- 当前只有两类效果需要明确确认：
  - `authentication`：登录、身份认证；
  - `financial_transaction`：付款、资金交易。
- 用户接受当前阶段边界暂时较宽；后续是否收紧由用户决定，Agent 不得预防性增加限制。

提出者：用户
确认时间：2026-08-20
批准方式：当前对话明确要求“全部改”。

## 2. 默认自动执行

以下行为不是风险确认项，应在目标和可见控件可执行时自动进入通用闭环：

- 打开或切换 App、Home、返回；
- 点击、双击（设备支持时）、滑动、长按、拖动；
- 聚焦、输入、清空、换行和键盘布局切换；
- 发送、提交、评论、关注、发布、收藏、点赞、邀请、加入；
- 删除、普通数据修改、普通权限变更；
- 保存设置及其他普通账号或外部状态变化。

这些任务仍遵守统一执行循环：根据新鲜画面选择一个动作，执行后重新观察并验证。该循环用于确保动作确实完成，不是额外风险确认。

## 3. 需要确认

仅以下两类效果在最终动作前展示具体对象和内容并等待确认：

1. 登录或身份认证；
2. 付款或资金交易。

不得根据关键词、App 名称、账号影响、数据是否可逆或“可能敏感”等推测扩展确认集合。确认权威只读取 typed `EffectIntent.kind` 和本地版本化政策。

## 4. 运行故障不是风险

以下情况可以停止当前动作，但必须报告具体技术原因，不能表述为“任务有风险”或擅自新增产品边界：

- 控制器、相机或设备离线；
- 同一设备被其他任务占用；
- 当前画面无法读取；
- 目标完全无法唯一识别；
- 当前标定无法到达目标；
- 动作后的真实结果无法确认；
- 正式模型响应无法形成最低限度的可执行结构。

停止后不得自动重复物理动作；应保存证据并修复通用故障。与动作无关的缺字段、自然语言差异、旧枚举或重复检查不得阻碍任务。

## 5. 当前唯一正式权威

- 高层任务协议：`2026-08-20-deepseek-typed-task-graph-v4`；
- 语义 IR：`2026-08-20-task-semantic-ir-v3`；
- 效果权威：`2026-08-20-typed-effect-authority-v1`；
- 唯一动作协议：`2026-08-20-canonical-action-v1`；
- 风险政策入口：`poc/config/local_risk_policy.v1.json`；
- 默认确认集合代码入口：`poc/task_semantic_ir.py` 的 `DEFAULT_CONFIRMATION_EFFECT_KINDS`。

DeepSeek task graph、TaskSemanticIR、UIScene、Qwen choice、Policy、Controller 和 Receipt 分别是任务顺序、语义数据、观察、选择、同目录复核、物理执行和事后证据，不得再各自产生或否决动作语义。旧视觉 action authority/shadow/selection 协议、旧 v2/v3 任务协议、自由文本风险正则、远程风险审计、`risk_actions/risk_ids`、`/approve-risk` 和固定 App 工作流均不得进入正式运行链。

## 6. 允许阻止动作的最小技术门

以下技术门只保证动作能正确落到当前真实设备与目标，不拥有产品风险分类权：

- 当前 `device_id` 与设备独占；
- 新鲜 observation/fingerprint 和一次性 action scope；
- 当前画面上唯一可执行目标；
- 机械标定可达；
- 一次一个物理动作；
- 动作后重新观察并验证结果。

不得重复实现这些门，也不得把无关模型字段、历史文本协议或 App 专用判断加入其中。

## 7. 边界变更流程

任何会使原本可执行任务变成 `blocked`、`confirmation_required`、`unsupported` 或人工介入的修改，实施前必须向用户说明：

1. 新增或修改的具体边界；
2. 会被阻止的真实任务；
3. 现有机制为何不足；
4. 不新增边界的替代方案；
5. 回滚方式。

用户未明确批准时不得实施。实施后必须在本文记录规则、提出者、批准时间、代码入口、删除的旧入口和回归测试。

## 8. 本次删除和替换的旧边界

- 删除“所有外部状态动作都需确认”的产品口径；
- 删除“发送、提交、删除、关注、评论等只能准备到确认前”的产品口径；
- 删除“敏感权限、不可逆账号或数据删除默认需要确认”的当前政策；
- 删除“任何缺字段或自然语言歧义一律失败关闭”的泛化口径，改为只在无法唯一执行时停止；
- 将历史严格协议与旧验收记录降为只读证据，不再作为当前执行权威；
- 普通修复不再强制先制作四份重型文档，只有产品边界、正式协议权威或架构级变化才需要完整产物。

## 9. 汇报要求

每轮必须分别报告：

- 内部机制修改了什么；
- 真机实际完成了什么；
- 用户目标还剩什么。

代码、测试、协议或文档完成不得替代真实功能完成。

## 10. 2026-08-20 本次实施记录

- 全局规则：`D:\AIData\Codex\Home\AGENTS.md` 已加入“用户决策优先与产品边界变更”，写入后 SHA-256 为 `833582DF592D6B25BFF61CC695BDA89B3CC20CF566C5EF9880056B3F8FBDB983`；
- 项目规则和最高目标：`AGENTS.md`、`项目最终目标.md` 已删除普通外部效果确认要求和泛化失败关闭表述；
- 正式说明：DeepSeek v4、通用架构、网页说明、正式协议、阶段 3 交接和项目交接已统一为仅登录/付款确认；
- 历史材料：保留原始历史事实，但在可能误导当前执行的台账顶部标为“历史/不可作为当前边界权威”；
- 网页：确认弹窗只显示“登录或付款”，不再泛称所有外部状态动作；
- 生产权威核对：配置文件和 Python 默认确认集合均精确等于 `authentication`、`financial_transaction`；
- 验证：语义政策测试 `25/25`、前端完整浏览器合同 `7/7`、前端语法检查、文档差异检查均通过。

## 11. API 调用真实性规则

- 触发原因：Agent 曾按常见 REST 命名习惯猜测不存在的 `/api/agent/generic-supervised/sessions`；FastAPI 将 `sessions` 匹配为 `{session_id}`，返回“通用单步会话不存在”。该错误属于 Agent 命令路径写错，不是项目、协议、模型、服务或手机故障。
- 正式处理：任何本地 API 调用前，必须从当前源码路由声明或当前 `/openapi.json` 核对 HTTP 方法与完整路径；不得猜测列表、复数或状态端点。
- 当前事实：正式通用会话入口是 `POST /api/agent/generic-supervised/start`，会话读取是 `GET /api/agent/generic-supervised/{session_id}`；当前没有通用会话列表端点。
- 防重复：没有公开列表端点时，只使用公开状态字段或已知 `session_id`；服务版本或路由变化后重新核对。调用参数不符合公开合同造成的错误，必须明确标记为 Agent 命令错误。
- 全局规则已同步；加入唯一类型化网关要求后的 `D:\AIData\Codex\Home\AGENTS.md` SHA-256 为 `BCC034233F386474C9739357DBC6999338AD38BCD349B5B9F48F4FAA2250E95E`。
- 正式调用入口已实现为 `poc/local_agent_api_client.py` 和 `poc/agent_api_cli.py`。它们自动读取当前服务的 `/api/session` 与 `/openapi.json`，在本地校验路由、方法、请求体、服务版本和最新 confirmation scope，并自动携带当前令牌；令牌不落盘、不输出。
- 命令行和 Agent 不再允许直接拼接通用 Agent HTTP 请求。写请求发生超时时只发送一次，不自动重试；错误统一区分为客户端合同、服务、模型、设备、验证、会话状态和网络类别。

## 12. 单一动作协议决定的实施记录

- 当前唯一动作协议为 `2026-08-20-canonical-action-v1`；旧 visual action shadow/authority/selection 文件、fixture、测试和生产引用已删除。
- Qwen 不再独立组装动作或动作后结果，只接收 canonical candidate 的机械投影并返回一个 choice ID；Policy 不再按输入、App 或 EffectIntent 二次解释动作，只重建相同目录复核。
- Web 状态中的 `action_protocol` 只表示 canonical action；机械执行接口另列 `controller_protocol`。
- 生产旧协议符号扫描为 0；Python 全量 `1301/1301`、前端协议与离线浏览器 `22/22` 通过。真机结果必须在服务重载后的全新 session 另行记录，不能由这些离线证据替代。

## 13. 语义目标与逐字文字边界

用户再次确认：普通视觉任务应以“当前画面中能够唯一执行的语义目标”为准，不能要求用户描述中的每个控件名、页面名或图标名都逐字出现在画面上。

- `target_ui_label` 只向观察和 Qwen 提供目标语义上下文，不再拥有 Qwen 前置否决权；“刷新图标”“发送键”“长文本输入框”等描述不因控件没有同名字面文字而直接 `blocked`。
- 收件人、输入正文、多字段值等真正要求逐字一致的数据仍由 typed entity、`InputFieldIntent`、`EffectIntent` 和 canonical candidate 绑定，不因本次放宽而改写或猜测。
- DeepSeek 只负责高层任务图，不再因为页面名称缺少第二份“结构化页面身份锚点”而否决已经引用当前正式视觉证据的完成结果；证据真实性由观察、canonical candidate、控制器回执和动作后新观察各自唯一负责。
- 目标完全没有可执行几何、存在无法消解的多个同等目标、观察不可用或机械不可达时，仍按具体运行故障停止；不得把它表述为风险或逐字协议失败。

提出者：用户

确认时间：2026-08-21

批准方式：当前对话明确要求先解决过严协议门。

## 14. 2026-08-21 语义门放宽验收记录

- 本地提交：`ea1810f fix: remove duplicate literal protocol vetoes`。
- 相关合同测试 `190/190`、观察/编排/动作适配回归 `497/497`、完整 Python 回归 `1344/1344`；另有 54 个已隔离的旧固定 App 历史测试跳过。
- 全新在线 session `c3a945154a7447449dfedd44250d7442` 使用此前会被 `exact_text_missing: 刷新图标` 阻断的同类目标，0 物理动作生成唯一刷新动作并达到 `awaiting_confirmation/confirmation_ready=true`；随后主动取消，未点击手机。
- 全新在线 session `e88fe230ee374039b7c428fdebd46d9a` 使用此前会被“命名页面缺少结构化画面身份锚点”拒绝的只读目标，0 物理动作最终 `succeeded/revision=2/active_subgoal=null`。
- 验收后 controller/camera online、`busy=false`、无活动任务和会话。

以上证明两项旧重复否决权已退出当前正式服务；它不替代后续真实刷新、输入、换行、多字段和发送的物理闭环验收。

## 15. 2026-08-21 输入字段身份唯一权威

- 当正式 typed 输入交易已经提供非空且非 `unknown` 的 `input_field_id` 时，该 ID 是动作前后输入框绑定的唯一字段身份权威；动作后元素临时 ID、占位文字、当前可见文字和几何重叠不得拥有第二份否决权。
- 动作后必须且只能找到一个相同 `input_field_id` 的 `application_text_input`。错误字段 ID、重复字段 ID、明确冲突的字段标签、App/页面族不兼容、键盘状态漂移或精确值不匹配仍按具体运行故障拒绝。
- 没有 typed 字段 ID 的旧观察才允许按元素 ID、语义标签和几何重叠依次退化绑定；退化证据不能覆盖已经存在的 typed 身份。
- 横向裁剪仍只负责证明并恢复完整输入值，持续动作等待仍只负责相机归位；两者不再额外决定字段身份。
- 该修改没有新增风险类别、确认项、App 限制、文字黑名单或人工介入，只删除输入字段身份上的重复权威。

提出者：用户

确认时间：2026-08-21

代码入口：`poc/universal_action_controller.py`

验证：与后续通用审计裁剪修复合并后，相关回归 `577/577`，完整 Python 回归 `1348/1348`，另有 54 个已隔离旧固定 App 历史测试跳过。

## 16. 2026-08-21 输入 transport 单一权威

- 本决定不新增产品风险或确认项，只收敛已授权普通输入的 transport：小写 ASCII 字母与空格在最多 20 字符内作为一个确定性批次；真实 LF 始终作为独立 `press_enter`；大写、数字、标点和中文继续使用各自已审计路径。
- `verified_text_transaction.py` 是唯一文字分段权威。`text_input_utils.py` 的旧分段/重输状态机与 Qwen 的旧完整动作输出提示词已物理删除，不允许通过兼容层恢复第二套分段、后置条件或动作候选权威。
- 卖家 Qt 文字对话框的确认只允许向已验证的精确对话框 HWND 投递 Return；不得通过全局键盘事件发送 Enter。目标窗口投递或关闭失败时停止本批次，归类为运行故障。
- 该内部实现不替代真机验收。换行、多字段和普通发送仍须按当前顺序取得新的同会话 `succeeded` 证据。

提出者：用户

确认时间：2026-08-21

代码入口：`poc/verified_text_transaction.py`、`poc/robot_core.py`、`poc/robot_gui_poc.py`

验证：初批相关 `399/399`；删除 `robot_core.validate_verified_text()` 最后一条旧字符集否决后相关 `166/166`、完整 Python 回归 `972/972`；旧权威物理缺失由防恢复测试覆盖。

真机纠正：session `f0caeec256084889a22340af7287ff0d` 已证明卖家批量字符串中的空格被错误执行为换行/标点，`first line` 实际成为 `first\n,line\n`。因此“空格可直接进入卖家批量字符串”不是已批准能力，当前仅是已证伪实现；在通用 transport 重做并通过真机前不得对外声明可用。

## 17. 2026-08-21 永久废除卖家文字对话框

- 厂商 `main.exe` 的电脑端文字输入对话框永久退出正式产品能力。任何普通文字、长文本、空格、换行、中文、数字或符号输入都不得调用该对话框。
- 必须物理删除对话框打开、填字、确认、关闭、Qt/IDOK 兼容、能力声明和专属测试；不得保留环境变量、备用入口、失败回退或未来自动启用开关。
- 正式文字输入只能使用当前真实手机画面上经过独立审计的键盘按键与机械臂动作。后续提速也必须在该可见键位合同内完成，不能重新引入电脑端批量文字窗口。
- 本决定不新增确认类别或风险边界；它删除一个已被真机证伪且会产生错误文字的 transport。

提出者：用户

确认时间：2026-08-21

批准方式：当前对话明确要求“以后都不要用这个对话框，彻底废除”。

## 18. 2026-08-22 删除 QWERTY 外框重复否决权

- 完整 QWERTY 键盘的 `q/p/a/l/z/m/backspace` 七个锚点已经由本地连续多帧证据独立稳定后，模型未能同时给出可信的整体 `keyboard.bounds` 不得再单独否决同一键盘身份；本地只据七点行距与键距重建一次执行包络。
- 没有本地稳定七点、七点水平结构错误、非 QWERTY 布局或键盘不完整时，缺失/无效整体外框仍拒绝，不得猜测。
- Enter、退格、字面键、模式/布局切换键仍分别要求自己的标签、边界、置信度、完整可见性和语义；重建整体包络不得替代任何单键审计，也不得生成动作坐标。
- 本决定删除同一视觉事实上的第二份否决权，不新增风险类别、确认项、App/页面/截图/固定坐标特例。

提出者：用户

确认时间：2026-08-22

批准方式：当前对话明确要求“删除”。

代码入口：`poc/generic_scene_observer.py`

验证：最小正反 `5/5`、相关输入/Qwen/canonical/编排回归 `424/424`、完整 Python 回归 `967/967`（`35.818s`）；真机换行尚未重新执行。

## 19. 2026-08-22 换行键语义与分段推进

- 用户所说的手机键盘右下角“换行键”与“回车键”、`Enter key`、`new line key` 是同一正式动作语义 `press_enter`；不得因为自然语言使用“换行”而只公开普通点击或输入模式切换候选。
- 当正式输入事务的当前片段已 matched，且任务图把下一确定性字符 `LF` 明确拆成唯一直接后继 `press_enter` 子目标时，当前输入子目标必须通过一次性 typed receipt 推进；不得继续把它当作同一高层输入微步并让 Qwen重复处理已完成的旧子目标。
- 一个高层输入子目标内部仍可包含多个确定性片段；没有唯一、动作类型匹配的正式后继时，不得提前完成该子目标。
- 本决定不依赖 App、页面、截图或坐标，不新增风险、确认或人工介入边界。

提出者：用户

确认时间：2026-08-22

批准方式：当前对话明确指定“要换行就点之前说的那个键”。

代码入口：`poc/task_semantic_ir.py`、`poc/universal_agent_orchestrator.py`

验证：最小 `4/4`、相关语义/编排/Qwen/canonical/输入事务回归 `182/182`、完整 Python 回归 `969/969`（`35.552s`）；修复后的全新真机会话尚未执行。

## 20. 2026-08-22 当前页面刷新不得升级为伪 effect

- 点击当前页面可见刷新/重新加载控件是可逆页面导航动作；模型不得仅因刷新会清空未发送草稿，就把该动作升级为 `data_mutation` effect 或保留第二套 effect 否决权。
- 当目标、当前表面、刷新控件、绑定子目标和无 payload 的 `data_mutation` effect 唯一一致，且没有正向发送、提交、登录、付款等外部效果时，本地确定性删除该伪 effect、恢复 navigation，并按唯一依赖前沿激活子目标。
- “不要发送或提交”等明确否定约束不属于正向外部效果；真正要求刷新后提交、登录或付款的目标不得被本规则降级。
- 本决定不增加 App、截图、坐标特例，也不改变 authentication 和 financial_transaction 的确认边界。

提出者：用户

确认时间：2026-08-22

代码入口：`poc/deepseek_task_graph.py`

验证：两份现场失败形状及正向外部效果反例定向 `1/1`，相关 `212/212`、完整 Python `969/969`（`35.807s`）；修复后真机尚未重新执行。

## 21. 2026-08-22 已验证标定允许覆盖整个可见手机画面

- 用户确认机械臂能够点击手机可见画面内的目标，旧九点标定凸包不得继续作为可见边缘控件的第二份否决权。
- 当触控标定已启用、独立验证通过、画面尺寸兼容且覆盖质量合格时，纠偏仿射允许在整个归一化可见画面 `0..1` 内使用；不再仅因目标中心位于历史样本凸包外而拒绝或把点击中心移入凸包。
- 目标中心或目标边界越出可见画面、标定无效/未验证、画面尺寸漂移、纠偏矩阵异常或纠偏结果越出既有安全边界时仍按具体运行故障拒绝。
- 本决定不增加 App、页面、截图、键盘或固定坐标特例；同一规则适用于所有经过独立视觉几何审计的可见目标。

提出者：用户

确认时间：2026-08-22

批准方式：当前对话明确说明“在能显示的地方都可以点到，范围弄大一点又没有关系”。

代码入口：`poc/tap_calibration.py`

验证：标定与动作适配相关回归 `150/150`、完整 Python 回归 `980/980`（`35.443s`）；服务重载后的真机换行复验仍待执行。

## 22. 2026-08-22 exact tap 允许唯一文字链接进入动作目录

- 用户决定让正式动作目录可以点击文字链接。仅当当前子目标是正式 `exact_tap_semantic`、唯一 required action 为 `tap_semantic`，且 typed `target_ui_label` 与当前画面一个完整可见、`goal_relevant=true` 的 `role=text` 元素逐字唯一绑定时，该文字链接才生成 canonical `tap_semantic` candidate。
- 动作仍须经过 fresh observation/fingerprint、后续新鲜几何复核和 Controller 校验；本规则只补齐 typed exact target 到 canonical catalog 的通用桥接，不直接产生坐标或执行权。
- 普通说明文字、重复或模糊文字、没有 exact target 的普通导航文字均不进入动作目录；`role=text` 的长按和拖动能力不放宽。
- 本决定不依赖 App、页面、截图、链接原文或固定坐标，也不新增风险类别或确认项。

提出者：用户

确认时间：2026-08-22

批准方式：当前对话明确决定“让动作目录也可以点击链接”。

代码入口：`poc/canonical_action_protocol.py`

验证：最小正向、变化与反例 `6/6`，相关 canonical/Qwen exact/Qwen visual/orchestrator 回归 `161/161`，完整 Python 回归 `1006/1006`；真机尚未复验。

## 23. 2026-08-22 否定动作与部分可见滚动视口的单一语义

- 明确否定的动作词只作为禁止该动作的上下文，不得生成 typed `required_action`；同一句中后续明确要求的正向动作仍正常编译。例如“向上滑动，不点击列表项”只能生成 `swipe`，不能同时生成 `tap_semantic`。
- 已有完整可见 scrollable viewport 的跨 App swipe 合同保持不变。对于 `fully_visible=false` 的滚动视口，该结构化状态视为内容裁切事实；仅当它是当前唯一满足条件的 `role=container`、`goal_relevant=true`、高置信、可见、`scrollable=true`、滚动轴为 vertical/horizontal 且携带非空裁切观察证据时，才允许生成 canonical swipe affordance。
- 部分可见但非目标、低置信、无滚动轴、非 scrollable、无证据或存在多个同等滚动视口时，仍不得生成 swipe。普通列表项 tap、App、页面、截图、文字和固定坐标均不因本规则获得特例。
- 本决定不新增风险类别、确认项或第二套动作协议；它删除否定词造成的伪动作权威，并让现有 swipe 能力使用一致的 typed 滚动事实。

提出者：用户

确认时间：2026-08-22

批准方式：用户原任务已明确授权分别完成多字段、普通发送和中文输入三项真机闭环；当前对话在多字段准备过程中进一步明确要求“只执行一次页面向上滑动，不点击任何列表项”，并授权修复已证明的通用缺口。

代码入口：`poc/task_semantic_ir.py`、`poc/canonical_action_protocol.py`

验证：现场失败报告离线重放已由 `required_action={swipe,tap_semantic}` 且无 swipe candidate，收敛为唯一 `required_action=swipe`、一个 swipe affordance 和四个方向候选；最小正向、变化与反例 `4/4`，相关语义/canonical/Qwen/编排回归 `187/187`，完整 Python 回归 `1010/1010`。修复后真机尚未复验。
