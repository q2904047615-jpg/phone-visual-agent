# 用户决策与协议边界

更新时间：2026-09-02（Asia/Shanghai）
状态：当前唯一产品边界与运行权威清单

本文只记录当前有效决定。旧协议、旧测试、旧文档和历史提交只可追溯，不得进入正式运行或恢复否决权。

## 1. 产品方向

- 产品是跨 App、跨措辞、可扩展到多手机的通用视觉操作 Agent，不是任何固定 App 或固定命令脚本。
- 新任务能由现有通用动作组合完成时，必须无需改代码即可执行。
- 用户接受当前阶段较宽的自动化边界；不得以历史协议、工程便利或“将来可能出错”为理由重新收紧。
- 采用轻量 DDD 模块化单体，只划分职责，不增加运行层数。

提出者：用户
批准时间：2026-08-20；2026-09-02 再次确认最短循环与彻底删除旧权威。

## 2. 唯一正式运行循环

普通自然语言任务只有以下一条正式链：

```text
DeepSeek 在会话开始时生成一次轻量有序目标，然后退出运行循环
→ 取得当前新鲜截图
→ Qwen 在同一次响应中发布 scene/input_structure，并只返回一个 canonical action 或同帧 finish
→ 本地只校验当前 device、lease、scope/fingerprint、被引用元素、合法 bounds、机械/可信 transport、一次执行和必要确认
→ 执行一次
→ 取得新截图并回到 Qwen
```

- 当前动作语义协议：`2026-08-20-canonical-action-v1`。
- 当前高层目标协议：`2026-08-20-deepseek-typed-task-graph-v4`。
- `matched` 只证明刚才动作及必要硬校验成立；只有下一张当前截图中的 Qwen `finish` 能推进高层目标或结束任务。
- 当前截图和随它同值返回的 decision 是本轮唯一视觉事实；不得缓存上一轮 decision、复用旧坐标或通过可变旁路取 decision。
- Qwen 普通输出只允许 `action` 或 `finish`，没有普通语义 `blocked` 否决权。
- Controller、Receipt、可见进度、App 身份和动作结果不得选择下一动作或裁决高层完成。
- 公开的 `exact_input_text`、`exact_action_kind` 是用户已明确结构化的直推入口，可以跳过 DeepSeek；进入设备循环后仍使用同一 Qwen 单动作链和硬校验。

正式运行不存在 TaskSemanticIR、Policy 二次复核、candidate selector、动作后 DeepSeek replan、corrective selector、App lineage 完成裁决、持久视觉/输入 lineage、decision cache、scene-only decision 回退或第二套动作权威。

## 3. 默认自动执行

在当前目标与设备可执行时，下列普通动作不要求人工确认：

- 打开或切换 App、Home、返回；
- 点击、双击、滑动、长按、拖动；
- 聚焦、输入、清空、换行、字段切换和键盘布局切换；
- 发送、提交、评论、关注、发布、收藏、点赞、邀请、加入；
- 删除、普通数据修改、普通权限或设置变更。

## 4. 唯一需要确认的产品风险

只有以下两类效果在最终动作前展示具体对象并确认：

1. `authentication`：登录或身份认证；
2. `financial_transaction`：付款或资金交易。

不得按关键词、App 名称、账号影响、是否可逆、发送/删除等动作词扩展确认集合。

## 5. 允许停止的必要技术门

以下是运行故障，不是产品风险：

- 设备、控制器或相机离线；
- 当前设备租约/独占失效；
- 新观察不可用、过期或 scope/fingerprint 不匹配；
- Qwen 引用的元素不唯一，或元素 ID、语义、bounds 不合法；
- 机械标定不可达，或可信 transport 未配置/回执不匹配；
- 登录/付款尚未取得对应确认；
- 一次动作没有取得必要的真实执行回执或下一组新截图。

无关冗余字段、可选 scene 状态、confidence、标签、占位符、光标、可见文字、可选证据、空白输入框没有正文，以及 DeepSeek 的可选澄清问题，不得让普通合法任务停止。

## 6. 当前输入合同

- 当前应用正文的唯一视觉权威是本轮 `input_structure.application_inputs[*].text`。
- scene 输入元素只提供同帧身份和页面上下文；不要求重复正文、标签、状态或几何重合。
- typed 输入事务已确定时，`input_field_id` 是字段身份；动作后以同一字段 ID 和精确 `states.value` 验证。
- 聚焦前，唯一合法 bounds 的完整空白编辑面可以签发一次聚焦点击，即使 scene 的可选 `goal_relevant` 为 false。
- malformed 的可选 element states 只被丢弃，不得否决具有合法 ID、角色、语义和 bounds 的已选元素。
- Companion IME 只操作当前已聚焦 Android 编辑连接并返回 transport ACK；它不观察页面、不选字段、不裁决完成。
- 正式文字链不得使用卖家文字对话框、剪贴板、Accessibility、任意 ADB/Shell、ADB Keyboard 或候选词点击。

提出者：用户
批准时间：2026-08-21 至 2026-09-02 多次确认；2026-09-02 删除持久 input lineage 与可选字段否决。

## 7. 可信 App 直启

- `launch_app` 只读取本地批准的包名注册表，并执行一个固定 launch reference。
- 用户、网页和模型不能传入 package、Shell、Intent、组件或任意命令。
- transport 回执只证明直启动作完成；随后必须取得新截图，由 Qwen 决定页面、下一动作或 finish。
- 系统包名可作为当前 scene 身份事实，但不得在 Qwen finish 后建立第二完成否决。

## 8. API 与真机调用

- 完整 HTTP 方法、路径和 schema 只以当前源码路由或运行中 `/openapi.json` 为准。
- 命令行和 Agent 只使用 `poc/local_agent_api_client.py` 或 `poc/agent_api_cli.py`。
- 不得猜测路由或手写通用 Agent HTTP 请求。
- 项目 API 可以按当前任务需要重载；卖家 `main.exe` 始终由用户管理，Codex 不得启动、停止、重启或操作其界面。
- 同一真实设备同时只允许一个任务拥有机械臂或 Companion IME。

## 9. 已永久退役的旧权威

以下内容必须保持物理删除或运行时不可达，禁止以兼容层、默认值、测试 fixture 或文档重新引入：

- TaskSemanticIR 和 canonical catalog 编译链；
- DeepSeek 动作后 replan 与普通语义前置否决；
- 本地 candidate 排名、目标重选、动作重绑或 corrective selector；
- App/页面血缘与持久输入值 lineage；
- 动作后本地高层完成判断；
- observation/decision 缓存和跨帧 decision source；
- scene 与 input_structure 两套正文/字段权威；
- confidence、visible、enabled、fully_visible、goal_relevant 等可选模型字段对普通已选动作的第二否决；
- 固定 App 流程、固定截图坐标、动作词黑名单及未获批准的确认类别。

## 10. 边界变更纪律

新增任何会让合法任务变成 `blocked`、`confirmation_required`、`unsupported` 或人工介入的规则，必须先说明受影响任务、现有机制为何不足、替代方案和回滚方式，并取得用户明确批准。

汇报必须分开说明：

- 内部机制与代码；
- 自动测试；
- 项目 API/设备当前状态；
- 真机实际完成结果；
- 尚未完成的用户目标。

代码或测试通过不得包装成真机任务成功。
