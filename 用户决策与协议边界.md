# 用户决策与协议边界

更新时间：2026-08-30（Asia/Shanghai）
状态：当前唯一产品边界清单

本文记录用户已经批准的自动化范围和正式协议边界。`AGENTS.md`、交接文档、历史验收台账、旧协议、测试和代码若与本文冲突，必须删除或迁移冲突内容，不能保留旧否决权。

## 1. 当前用户决定

- 产品首先实现跨 App、跨措辞的通用全自动手机视觉操作 Agent。
- 用户交给 Agent 的普通任务视为已授权执行，不再逐次请求确认。
- 当前只有两类效果需要明确确认：
  - `authentication`：登录、身份认证；
  - `financial_transaction`：付款、资金交易。
- 用户接受当前阶段边界暂时较宽；后续是否收紧由用户决定，Agent 不得预防性增加限制。

提出者：用户
确认时间：2026-08-20
批准方式：当前对话明确要求“全部改”。

## 2. 默认自动执行

以下行为不是风险确认项，应在目标和可见控件可执行时自动进入通用闭环：

- 打开或切换 App、Home、返回；
- 点击、双击（设备支持时）、滑动、长按、拖动；
- 聚焦、输入、清空、换行和键盘布局切换；
- 发送、提交、评论、关注、发布、收藏、点赞、邀请、加入；
- 删除、普通数据修改、普通权限变更；
- 保存设置及其他普通账号或外部状态变化。

这些任务仍遵守统一执行循环：根据新鲜画面选择一个动作，执行后重新观察并验证。该循环用于确保动作确实完成，不是额外风险确认。

## 3. 需要确认

仅以下两类效果在最终动作前展示具体对象和内容并等待确认：

1. 登录或身份认证；
2. 付款或资金交易。

不得根据关键词、App 名称、账号影响、数据是否可逆或“可能敏感”等推测扩展确认集合。确认权威只读取 typed `EffectIntent.kind` 和本地版本化政策。

## 4. 运行故障不是风险

以下情况可以停止当前动作，但必须报告具体技术原因，不能表述为“任务有风险”或擅自新增产品边界：

- 控制器、相机或设备离线；
- 同一设备被其他任务占用；
- 当前画面无法读取；
- 目标完全无法唯一识别；
- 当前标定无法到达目标；
- 动作后的真实结果无法确认；
- 正式模型响应无法形成最低限度的可执行结构。

停止后不得自动重复物理动作；应保存证据并修复通用故障。与动作无关的缺字段、自然语言差异、旧枚举或重复检查不得阻碍任务。

## 5. 当前唯一正式权威

- 高层任务协议：`2026-08-20-deepseek-typed-task-graph-v4`；
- 语义 IR：`2026-08-20-task-semantic-ir-v3`；
- 效果权威：`2026-08-20-typed-effect-authority-v1`；
- 唯一动作协议：`2026-08-20-canonical-action-v1`；
- 风险政策入口：`poc/config/local_risk_policy.v1.json`；
- 默认确认集合代码入口：`poc/agent/domain/task_semantic_ir.py` 的 `DEFAULT_CONFIRMATION_EFFECT_KINDS`。

DeepSeek task graph、TaskSemanticIR、UIScene、Qwen choice、Policy、Controller 和 Receipt 分别是任务顺序、语义数据、观察、选择、同目录复核、物理执行和事后证据，不得再各自产生或否决动作语义。旧视觉 action authority/shadow/selection 协议、旧 v2/v3 任务协议、自由文本风险正则、远程风险审计、`risk_actions/risk_ids`、`/approve-risk` 和固定 App 工作流均不得进入正式运行链。

## 6. 允许阻止动作的最小技术门

以下技术门只保证动作能正确落到当前真实设备与目标，不拥有产品风险分类权：

- 当前 `device_id` 与设备独占；
- 新鲜 observation/fingerprint 和一次性 action scope；
- 当前画面上唯一可执行目标；
- 机械标定可达；
- 一次一个物理动作；
- 动作后重新观察并验证结果。

不得重复实现这些门，也不得把无关模型字段、历史文本协议或 App 专用判断加入其中。

## 7. 边界变更流程

任何会使原本可执行任务变成 `blocked`、`confirmation_required`、`unsupported` 或人工介入的修改，实施前必须向用户说明：

1. 新增或修改的具体边界；
2. 会被阻止的真实任务；
3. 现有机制为何不足；
4. 不新增边界的替代方案；
5. 回滚方式。

用户未明确批准时不得实施。实施后必须在本文记录规则、提出者、批准时间、代码入口、删除的旧入口和回归测试。

## 8. 本次删除和替换的旧边界

- 删除“所有外部状态动作都需确认”的产品口径；
- 删除“发送、提交、删除、关注、评论等只能准备到确认前”的产品口径；
- 删除“敏感权限、不可逆账号或数据删除默认需要确认”的当前政策；
- 删除“任何缺字段或自然语言歧义一律失败关闭”的泛化口径，改为只在无法唯一执行时停止；
- 将历史严格协议与旧验收记录降为只读证据，不再作为当前执行权威；
- 普通修复不再强制先制作四份重型文档，只有产品边界、正式协议权威或架构级变化才需要完整产物。

## 9. 汇报要求

每轮必须分别报告：

- 内部机制修改了什么；
- 真机实际完成了什么；
- 用户目标还剩什么。

代码、测试、协议或文档完成不得替代真实功能完成。

## 10. 2026-08-20 本次实施记录

- 全局规则：`D:\AIData\Codex\Home\AGENTS.md` 已加入“用户决策优先与产品边界变更”，写入后 SHA-256 为 `833582DF592D6B25BFF61CC695BDA89B3CC20CF566C5EF9880056B3F8FBDB983`；
- 项目规则和最高目标：`AGENTS.md`、`项目最终目标.md` 已删除普通外部效果确认要求和泛化失败关闭表述；
- 正式说明：DeepSeek v4、通用架构、网页说明、正式协议、阶段 3 交接和项目交接已统一为仅登录/付款确认；
- 历史材料：保留原始历史事实，但在可能误导当前执行的台账顶部标为“历史/不可作为当前边界权威”；
- 网页：确认弹窗只显示“登录或付款”，不再泛称所有外部状态动作；
- 生产权威核对：配置文件和 Python 默认确认集合均精确等于 `authentication`、`financial_transaction`；
- 验证：语义政策测试 `25/25`、前端完整浏览器合同 `7/7`、前端语法检查、文档差异检查均通过。

## 11. API 调用真实性规则

- 触发原因：Agent 曾按常见 REST 命名习惯猜测不存在的 `/api/agent/generic-supervised/sessions`；FastAPI 将 `sessions` 匹配为 `{session_id}`，返回“通用单步会话不存在”。该错误属于 Agent 命令路径写错，不是项目、协议、模型、服务或手机故障。
- 正式处理：任何本地 API 调用前，必须从当前源码路由声明或当前 `/openapi.json` 核对 HTTP 方法与完整路径；不得猜测列表、复数或状态端点。
- 当前事实：正式通用会话入口是 `POST /api/agent/generic-supervised/start`，会话读取是 `GET /api/agent/generic-supervised/{session_id}`；当前没有通用会话列表端点。
- 防重复：没有公开列表端点时，只使用公开状态字段或已知 `session_id`；服务版本或路由变化后重新核对。调用参数不符合公开合同造成的错误，必须明确标记为 Agent 命令错误。
- 全局规则已同步；加入唯一类型化网关要求后的 `D:\AIData\Codex\Home\AGENTS.md` SHA-256 为 `BCC034233F386474C9739357DBC6999338AD38BCD349B5B9F48F4FAA2250E95E`。
- 正式调用入口已实现为 `poc/local_agent_api_client.py` 和 `poc/agent_api_cli.py`。它们自动读取当前服务的 `/api/session` 与 `/openapi.json`，在本地校验路由、方法、请求体、服务版本和最新 confirmation scope，并自动携带当前令牌；令牌不落盘、不输出。
- 命令行和 Agent 不再允许直接拼接通用 Agent HTTP 请求。写请求发生超时时只发送一次，不自动重试；错误统一区分为客户端合同、服务、模型、设备、验证、会话状态和网络类别。

## 12. 单一动作协议决定的实施记录

- 当前唯一动作协议为 `2026-08-20-canonical-action-v1`；旧 visual action shadow/authority/selection 文件、fixture、测试和生产引用已删除。
- Qwen 不再独立组装动作或动作后结果，只接收 canonical candidate 的机械投影并返回一个 choice ID；Policy 不再按输入、App 或 EffectIntent 二次解释动作，只重建相同目录复核。
- Web 状态中的 `action_protocol` 只表示 canonical action；机械执行接口另列 `controller_protocol`。
- 生产旧协议符号扫描为 0；Python 全量 `1301/1301`、前端协议与离线浏览器 `22/22` 通过。真机结果必须在服务重载后的全新 session 另行记录，不能由这些离线证据替代。

## 13. 语义目标与逐字文字边界

用户再次确认：普通视觉任务应以“当前画面中能够唯一执行的语义目标”为准，不能要求用户描述中的每个控件名、页面名或图标名都逐字出现在画面上。

- `target_ui_label` 只向观察和 Qwen 提供目标语义上下文，不再拥有 Qwen 前置否决权；“刷新图标”“发送键”“长文本输入框”等描述不因控件没有同名字面文字而直接 `blocked`。
- 收件人、输入正文、多字段值等真正要求逐字一致的数据仍由 typed entity、`InputFieldIntent`、`EffectIntent` 和 canonical candidate 绑定，不因本次放宽而改写或猜测。
- DeepSeek 只负责高层任务图，不再因为页面名称缺少第二份“结构化页面身份锚点”而否决已经引用当前正式视觉证据的完成结果；证据真实性由观察、canonical candidate、控制器回执和动作后新观察各自唯一负责。
- 目标完全没有可执行几何、存在无法消解的多个同等目标、观察不可用或机械不可达时，仍按具体运行故障停止；不得把它表述为风险或逐字协议失败。

提出者：用户

确认时间：2026-08-21

批准方式：当前对话明确要求先解决过严协议门。

## 14. 2026-08-21 语义门放宽验收记录

- 本地提交：`ea1810f fix: remove duplicate literal protocol vetoes`。
- 相关合同测试 `190/190`、观察/编排/动作适配回归 `497/497`、完整 Python 回归 `1344/1344`；另有 54 个已隔离的旧固定 App 历史测试跳过。
- 全新在线 session `c3a945154a7447449dfedd44250d7442` 使用此前会被 `exact_text_missing: 刷新图标` 阻断的同类目标，0 物理动作生成唯一刷新动作并达到 `awaiting_confirmation/confirmation_ready=true`；随后主动取消，未点击手机。
- 全新在线 session `e88fe230ee374039b7c428fdebd46d9a` 使用此前会被“命名页面缺少结构化画面身份锚点”拒绝的只读目标，0 物理动作最终 `succeeded/revision=2/active_subgoal=null`。
- 验收后 controller/camera online、`busy=false`、无活动任务和会话。

以上证明两项旧重复否决权已退出当前正式服务；它不替代后续真实刷新、输入、换行、多字段和发送的物理闭环验收。

## 15. 2026-08-21 输入字段身份唯一权威

- 当正式 typed 输入交易已经提供非空且非 `unknown` 的 `input_field_id` 时，该 ID 是动作前后输入框绑定的唯一字段身份权威；动作后元素临时 ID、占位文字、当前可见文字和几何重叠不得拥有第二份否决权。
- 动作后必须且只能找到一个相同 `input_field_id` 的 `application_text_input`。错误字段 ID、重复字段 ID、明确冲突的字段标签、App/页面族不兼容、键盘状态漂移或精确值不匹配仍按具体运行故障拒绝。
- 没有 typed 字段 ID 的旧观察才允许按元素 ID、语义标签和几何重叠依次退化绑定；退化证据不能覆盖已经存在的 typed 身份。
- 横向裁剪仍只负责证明并恢复完整输入值，持续动作等待仍只负责相机归位；两者不再额外决定字段身份。
- 该修改没有新增风险类别、确认项、App 限制、文字黑名单或人工介入，只删除输入字段身份上的重复权威。

提出者：用户

确认时间：2026-08-21

代码入口：`poc/universal_action_controller.py`

验证：与后续通用审计裁剪修复合并后，相关回归 `577/577`，完整 Python 回归 `1348/1348`，另有 54 个已隔离旧固定 App 历史测试跳过。

## 16. 2026-08-21 输入 transport 单一权威（已由第 54 节取代）

- 本决定不新增产品风险或确认项，只收敛已授权普通输入的 transport：小写 ASCII 字母与空格在最多 20 字符内作为一个确定性批次；真实 LF 始终作为独立 `press_enter`；大写、数字、标点和中文继续使用各自已审计路径。
- 本节仅保留机械逐键时期的历史证据。自 2026-08-30 起，正式文字 transport 选择、Unicode 能力与执行边界只按第 54 节裁决；本节不得恢复为运行时默认值或第二否决权。
- `agent/domain/verified_text_transaction.py` 是唯一文字分段权威。`agent/domain/text_input_utils.py` 的旧分段/重输状态机与 Qwen 的旧完整动作输出提示词已物理删除，不允许通过兼容层恢复第二套分段、后置条件或动作候选权威。
- 卖家 Qt 文字对话框的确认只允许向已验证的精确对话框 HWND 投递 Return；不得通过全局键盘事件发送 Enter。目标窗口投递或关闭失败时停止本批次，归类为运行故障。
- 该内部实现不替代真机验收。换行、多字段和普通发送仍须按当前顺序取得新的同会话 `succeeded` 证据。

提出者：用户

确认时间：2026-08-21

代码入口：`poc/agent/domain/verified_text_transaction.py`、`poc/robot_core.py`、`poc/robot_gui_poc.py`

验证：初批相关 `399/399`；删除 `robot_core.validate_verified_text()` 最后一条旧字符集否决后相关 `166/166`、完整 Python 回归 `972/972`；旧权威物理缺失由防恢复测试覆盖。

真机纠正：session `f0caeec256084889a22340af7287ff0d` 已证明卖家批量字符串中的空格被错误执行为换行/标点，`first line` 实际成为 `first\n,line\n`。因此“空格可直接进入卖家批量字符串”不是已批准能力，当前仅是已证伪实现；在通用 transport 重做并通过真机前不得对外声明可用。

## 17. 2026-08-21 永久废除卖家文字对话框

- 厂商 `main.exe` 的电脑端文字输入对话框永久退出正式产品能力。任何普通文字、长文本、空格、换行、中文、数字或符号输入都不得调用该对话框。
- 必须物理删除对话框打开、填字、确认、关闭、Qt/IDOK 兼容、能力声明和专属测试；不得保留环境变量、备用入口、失败回退或未来自动启用开关。
- 卖家电脑端批量文字窗口仍永久禁止恢复。正式文字输入可使用当前真实手机画面上的机械键位，或按第 54 节使用用户安装、启用并配对的项目自有 Companion IME；两者都不得回退到卖家文字对话框。
- 本决定不新增确认类别或风险边界；它删除一个已被真机证伪且会产生错误文字的 transport。

提出者：用户

确认时间：2026-08-21

批准方式：当前对话明确要求“以后都不要用这个对话框，彻底废除”。

## 18. 2026-08-22 删除 QWERTY 外框重复否决权

- 完整 QWERTY 键盘的 `q/p/a/l/z/m/backspace` 七个锚点已经由本地连续多帧证据独立稳定后，模型未能同时给出可信的整体 `keyboard.bounds` 不得再单独否决同一键盘身份；本地只据七点行距与键距重建一次执行包络。
- 没有本地稳定七点、七点水平结构错误、非 QWERTY 布局或键盘不完整时，缺失/无效整体外框仍拒绝，不得猜测。
- Enter、退格、字面键、模式/布局切换键仍分别要求自己的标签、边界、置信度、完整可见性和语义；重建整体包络不得替代任何单键审计，也不得生成动作坐标。
- 本决定删除同一视觉事实上的第二份否决权，不新增风险类别、确认项、App/页面/截图/固定坐标特例。

提出者：用户

确认时间：2026-08-22

批准方式：当前对话明确要求“删除”。

代码入口：`poc/generic_scene_observer.py`

验证：最小正反 `5/5`、相关输入/Qwen/canonical/编排回归 `424/424`、完整 Python 回归 `967/967`（`35.818s`）；真机换行尚未重新执行。

## 19. 2026-08-22 换行键语义与分段推进

- 用户所说的手机键盘右下角“换行键”与“回车键”、`Enter key`、`new line key` 是同一正式动作语义 `press_enter`；不得因为自然语言使用“换行”而只公开普通点击或输入模式切换候选。
- 当正式输入事务的当前片段已 matched，且任务图把下一确定性字符 `LF` 明确拆成唯一直接后继 `press_enter` 子目标时，当前输入子目标必须通过一次性 typed receipt 推进；不得继续把它当作同一高层输入微步并让 Qwen重复处理已完成的旧子目标。
- 一个高层输入子目标内部仍可包含多个确定性片段；没有唯一、动作类型匹配的正式后继时，不得提前完成该子目标。
- 本决定不依赖 App、页面、截图或坐标，不新增风险、确认或人工介入边界。

提出者：用户

确认时间：2026-08-22

批准方式：当前对话明确指定“要换行就点之前说的那个键”。

代码入口：`poc/agent/domain/task_semantic_ir.py`、`poc/universal_agent_orchestrator.py`

验证：最小 `4/4`、相关语义/编排/Qwen/canonical/输入事务回归 `182/182`、完整 Python 回归 `969/969`（`35.552s`）；修复后的全新真机会话尚未执行。

## 20. 2026-08-22 当前页面刷新不得升级为伪 effect

- 点击当前页面可见刷新/重新加载控件是可逆页面导航动作；模型不得仅因刷新会清空未发送草稿，就把该动作升级为 `data_mutation` effect 或保留第二套 effect 否决权。
- 当目标、当前表面、刷新控件、绑定子目标和无 payload 的 `data_mutation` effect 唯一一致，且没有正向发送、提交、登录、付款等外部效果时，本地确定性删除该伪 effect、恢复 navigation，并按唯一依赖前沿激活子目标。
- “不要发送或提交”等明确否定约束不属于正向外部效果；真正要求刷新后提交、登录或付款的目标不得被本规则降级。
- 本决定不增加 App、截图、坐标特例，也不改变 authentication 和 financial_transaction 的确认边界。

提出者：用户

确认时间：2026-08-22

代码入口：`poc/agent/domain/task_graph.py`、`poc/agent/application/deepseek_task_graph.py`

验证：两份现场失败形状及正向外部效果反例定向 `1/1`，相关 `212/212`、完整 Python `969/969`（`35.807s`）；修复后真机尚未重新执行。

## 21. 2026-08-22 已验证标定允许覆盖整个可见手机画面

- 用户确认机械臂能够点击手机可见画面内的目标，旧九点标定凸包不得继续作为可见边缘控件的第二份否决权。
- 当触控标定已启用、独立验证通过、画面尺寸兼容且覆盖质量合格时，纠偏仿射允许在整个归一化可见画面 `0..1` 内使用；不再仅因目标中心位于历史样本凸包外而拒绝或把点击中心移入凸包。
- 目标中心或目标边界越出可见画面、标定无效/未验证、画面尺寸漂移、纠偏矩阵异常或纠偏结果越出既有安全边界时仍按具体运行故障拒绝。
- 本决定不增加 App、页面、截图、键盘或固定坐标特例；同一规则适用于所有经过独立视觉几何审计的可见目标。

提出者：用户

确认时间：2026-08-22

批准方式：当前对话明确说明“在能显示的地方都可以点到，范围弄大一点又没有关系”。

代码入口：`poc/tap_calibration.py`

验证：标定与动作适配相关回归 `150/150`、完整 Python 回归 `980/980`（`35.443s`）；服务重载后的真机换行复验仍待执行。

## 22. 2026-08-22 exact tap 允许唯一文字链接进入动作目录

- 用户决定让正式动作目录可以点击文字链接。仅当当前子目标是正式 `exact_tap_semantic`、唯一 required action 为 `tap_semantic`，且 typed `target_ui_label` 与当前画面一个完整可见、`goal_relevant=true` 的 `role=text` 元素逐字唯一绑定时，该文字链接才生成 canonical `tap_semantic` candidate。
- 动作仍须经过 fresh observation/fingerprint、后续新鲜几何复核和 Controller 校验；本规则只补齐 typed exact target 到 canonical catalog 的通用桥接，不直接产生坐标或执行权。
- 普通说明文字、重复或模糊文字、没有 exact target 的普通导航文字均不进入动作目录；`role=text` 的长按和拖动能力不放宽。
- 本决定不依赖 App、页面、截图、链接原文或固定坐标，也不新增风险类别或确认项。

提出者：用户

确认时间：2026-08-22

批准方式：当前对话明确决定“让动作目录也可以点击链接”。

代码入口：`poc/agent/domain/canonical_action_protocol.py`

验证：最小正向、变化与反例 `6/6`，相关 canonical/Qwen exact/Qwen visual/orchestrator 回归 `161/161`，完整 Python 回归 `1006/1006`；真机尚未复验。

## 23. 2026-08-22 否定动作与部分可见滚动视口的单一语义

- 明确否定的动作词只作为禁止该动作的上下文，不得生成 typed `required_action`；同一句中后续明确要求的正向动作仍正常编译。例如“向上滑动，不点击列表项”只能生成 `swipe`，不能同时生成 `tap_semantic`。
- 已有完整可见 scrollable viewport 的跨 App swipe 合同保持不变。对于 `fully_visible=false` 的滚动视口，该结构化状态视为内容裁切事实；仅当它是当前唯一满足条件的 `role=container`、`goal_relevant=true`、高置信、可见、`scrollable=true`、滚动轴为 vertical/horizontal 且携带非空裁切观察证据时，才允许生成 canonical swipe affordance。
- 部分可见但非目标、低置信、无滚动轴、非 scrollable、无证据或存在多个同等滚动视口时，仍不得生成 swipe。普通列表项 tap、App、页面、截图、文字和固定坐标均不因本规则获得特例。
- 本决定不新增风险类别、确认项或第二套动作协议；它删除否定词造成的伪动作权威，并让现有 swipe 能力使用一致的 typed 滚动事实。

提出者：用户

确认时间：2026-08-22

批准方式：用户原任务已明确授权分别完成多字段、普通发送和中文输入三项真机闭环；当前对话在多字段准备过程中进一步明确要求“只执行一次页面向上滑动，不点击任何列表项”，并授权修复已证明的通用缺口。

代码入口：`poc/agent/domain/task_semantic_ir.py`、`poc/agent/domain/canonical_action_protocol.py`

验证：现场失败报告离线重放已由 `required_action={swipe,tap_semantic}` 且无 swipe candidate，收敛为唯一 `required_action=swipe`、一个 swipe affordance 和四个方向候选；最小正向、变化与反例 `4/4`，相关语义/canonical/Qwen/编排回归 `187/187`，完整 Python 回归 `1010/1010`。修复后真机尚未复验。

## 24. 2026-08-22 Back 与 swipe 动作后只观察结果画面

- `verified_navigation_result_v1` 是 Action Adapter 在一次物理动作已执行后才能铸造的本地观察标记。除既有完成型导航外，只有已解析的 `back` 或 `swipe`、typed execution class 为 `navigate`、expected effect 字段全部属于安全白名单，且逐字包含 `scene_changed=true` 或 `content_changed=true` 时才能使用。
- 标记只让动作后的 fresh compact observation 观察当前稳定结果画面，不再搜索已经消耗的源目标。它不改变动作前 canonical candidate、target identity、geometry、Controller 或 confirmation scope，也不产生第二次物理动作。
- 未执行动作、无画面/内容变化、错误 effect、未知 effect 字段仍不能铸造标记。目标精查返回未声明画布的越界 `x/y/w/h` 仍不得缩放、截断或转换；合法 0..1000 shorthand 的原有有界规范化保持不变。
- 本决定不新增风险类别、确认项、App、页面、截图、文字或坐标特例；它只修复 Back 和 swipe 已执行后被无关目标精查 schema 失败覆盖真实结果证据的通用可用性缺口。

提出者：用户

确认时间：2026-08-22

批准方式：用户原任务已授权分别完成多字段、普通发送和中文输入三项真机闭环，并在多字段准备中依次授权 exact Back 与单次 swipe；当前批次按两次真实 post-action 失败合并修复同一通用根因。

代码入口：`poc/generic_action_adapter.py`

验证：Back 与 swipe 两份现场上下文、scene/content 变化正例、未执行/无变化/错误 effect/未知 effect 反例及两份越界 geometry 反例最小测试 `7/7`；相关 Action Adapter 与 Scene Observer 回归 `372/372`；完整 Python 回归 `1014/1014`。修复后真机尚未复验。

## 25. 2026-08-22 exact tap 的 typed label 是唯一目标身份

- 正式 `exact_tap_semantic` 的逐字 typed `target_ui_label` 已在 canonical 目录中唯一绑定目标后，Qwen 对同一可见控件生成的自由文本 `meaning` 只是一项观察描述，不得在确认阶段重新成为第二套目标身份并否决同一目标。
- 该优先级只在 planned/fresh 两次观察中 typed label 各自逐字唯一、role 相同、两侧 `goal_relevant=true`、fresh `fully_visible=true` 时成立；确认时仍必须通过 App/screen 兼容、稳定 states 与后续 fresh geometry/Controller 校验。
- 不同 label、不同 role、重复目标、状态冲突、App 或 screen 切换、目标区域明显移动均继续拒绝。普通 `tap_semantic`、没有 typed exact target 的任务及其他动作不获得 meaning 漂移豁免。
- 本决定不增加 meaning 同义词、App、页面、截图、文字内容或固定坐标特例，也不改变动作前 canonical candidate、confirmation scope 或一次一动作合同。

提出者：用户

确认时间：2026-08-22

批准方式：用户授权以逐字目标“两个字段分别输入”执行全新 exact tap，并要求多字段正式闭环；本次真实确认失败证明 typed label 已唯一成立后仍被自由 meaning 二次否决，当前批次按该通用根因修复。

代码入口：`poc/generic_action_adapter.py`

验证：现场 `two_fields_input_test -> two_fields_input` 重放、英文 meaning 变化正例及不同 label/role、重复、状态、goal flag、可见性、App/screen 与 geometry 反例最小测试 `5/5`；Action Adapter 相关回归 `122/122`；完整 Python 回归 `1016/1016`。修复前 session `e0989e1e9fe5486aa05edbc17e9474e8` 为 `needs_reobservation/0 physical actions`，修复后真机尚未复验。

## 26. 2026-08-22 多字段观察只投影当前 typed 字段

- 多字段任务的完整 `input_fields` 继续保留在根级正式 typed graph 中；执行某一个写入子目标时，`active_subgoal_visual_context.goal_entities` 不再重复嵌入全部字段，只保留当前字段的 `active_input_transaction_text`、`active_input_field_id`、`active_input_field_label`、`active_input_multiline` 和必要的扁平非输入实体。
- 最终只读核对节点仍从正式 TaskSemanticIR 保留所有 `input.value_equals` desired states，并以 field_id 绑定的浅层期望值/标签映射提供给观察器；不得因收窄当前写入观察而丢失双字段同时逐字验证。
- 根级 typed 身份、payload、字段依赖和证据要求均不删除；观察器既有最大嵌套深度不提高，真正过深的未知上下文仍拒绝。
- 本修复不依赖 App、页面、截图、字段顺序、具体标签、文字内容或坐标，不新增风险类别、确认项或动作权限。

提出者：用户

确认时间：2026-08-22

批准方式：用户明确授权完成“多字段依次定位、输入和验证”的正式真机闭环；首次正式 session 的 typed graph 已正确，但动作前观察因完整 `input_fields` 在 active context 中重复嵌入而失败，当前批次只修复该通用观察投影缺口。

代码入口：`poc/universal_agent_orchestrator.py`

验证：现场旧投影错误重放、字段标签/顺序变化、最终双 desired states 与真正过深反例最小测试 `4/4`；编排/观察/语义 IR/Qwen 相关回归 `419/419`；完整 Python 回归 `1020/1020`（`36.539s`）。修复前 session `7f3466b9f52242ea953e69f1e2225ffc` 为 `failed/0 physical actions`，修复后真机尚未复验。

## 27. 2026-08-24 输入状态账本单一权威

- 正式输入闭环只保留一份 typed 输入状态账本。账本以 `input_field_id` 绑定字段，只发布已提交正文、未提交预编辑、候选及键盘状态；compact 场景中的自由摘要、输入框自由转写和专用输入审计不得分别维护或相互推翻第二份正文真相。
- compact 场景只保留页面身份和输入审计选区提示，不得据其输入值生成、恢复、清空或否决账本中的 committed value。正式正文值只能由专用输入结构审计的应用字段事实，或同一 typed 字段上的确定性动作 lineage 与动作后专用审计共同归约。
- 精确 IME 候选提交后，候选动作前的 typed 字段、prior/expected、拼音和唯一候选，机械臂单击事件回执，以及动作后同一字段的唯一完整结构和精确候选片段共同更新账本；残留预测栏只作为键盘事实，不得重新把已经提交的正文改写为预编辑。
- 错误或重复字段 ID、不同设备、明确不兼容的输入表面、字段几何不连续、动作前候选不唯一、动作后片段不等于 expected 后缀、应用字段明确报告其他非空正文，仍按具体运行故障拒绝。自然语言摘要与账本不一致本身不再拥有否决权。
- 必须物理删除依赖 compact 输入值恢复专用审计正文的旧分支，并增加防恢复回归；不得以兼容层保留旧双重权威，也不得增加 App、页面、截图、文字内容或固定坐标特例。

提出者：用户

确认时间：2026-08-24

批准方式：当前对话明确要求“按你说的进行修改，不要再出现两套视觉判断”。

代码入口：`poc/generic_scene_observer.py`、`poc/agent/domain/input_value_lineage.py`、
`poc/agent/application/input_value_lineage.py`、
`poc/agent/infrastructure/file_system_input_lineage_store.py`

验证：最小与变化样本 `38/38`、观察器模块 `273/273`、输入/几何/Qwen/canonical/编排相关回归 `660/660`、正式完整 Python 回归 `1083/1083`（`43.873s`）通过；静态编译与 `git diff --check` 通过。防恢复测试同时要求旧 compact 取值函数、旧摘要/线索回填标记、旧 `coarse_input_value` 接口名及无审计时返回 compact 输入元素的分支在生产代码中物理缺失。本批未加载服务、未执行真机动作；中文输入完整正式真机验收仍未完成。

## 28. 2026-08-24 Qwen Plus 单模型与会话调用预算（历史过渡合同，会话硬门已由第 44 节废除）

- 当前正式视觉模型保持 `qwen3.7-plus`，不切换 Flash，不设置自动降级、失败回退或多模型抽样。模型服务不可用时应报告运行故障，不能暗中改用较弱模型继续执行。
- 同一设备、同一画面 fingerprint、同一 typed 目标上下文的成功观察只允许复用，不得重复计费调用。复用只减少相同只读观察，不改变 canonical candidate、一次性 scope、Controller 或动作后验证。
- 输入闭环中，确认前四帧已经通过本地稳定性、尺寸和全画面像素身份门时，继续使用同一帧上原 strict input-structure audit 签发的 trusted scene；不再重复做整页 Qwen 观察和 planned/fresh 两份 Qwen 几何审计。完整 QWERTY 行仍须由新鲜本地多帧 OCR 复核；画面身份或行结构不满足条件时走既有正式审计或停止，不能降级为旧粗坐标。
- 同一 typed 输入事务的动作后观察可以在有效 pending lineage、同一设备和字段身份下直接运行一份专用 input-structure audit，并继承此前已验证的 App/screen 身份；专用审计若不能唯一重建当前字段则停止。它不能把旧字段值、旧坐标或旧候选带入新场景。
- 完整 QWERTY 三行已被本地连续多帧 OCR 唯一确认时，该本地事实可以签发一次性的输入表面方向凭据；没有完整 QWERTY 的首个聚焦动作及非输入动作仍使用独立 Qwen 方向审计。凭据仍绑定当前设备、场景、帧尺寸和动作前真实帧，并在一个物理原语前消费。
- 本节曾设置每个 session `16` 次请求和 `50000` Token 的本地硬门；该资源止损已被用户于 2026-08-25 明确废除。正式运行不得再根据累计请求数或 Token 数拒绝下一闭环步骤，旧错误码和旧阈值不得拥有运行时入口。普通导航的单次新观察纠正仍只按第 30 节执行。
- 每次请求必须在 session 目录持久化阶段、模型、provider request ID、网络尝试数、prompt/completion/total Token、画面 fingerprint 和费用估算；同 fingerprint 复用次数也要进入账本。费用只按明确版本化的 `qwen3.7-plus` 公开单价估算，并同时标注原价与当日限时价，不能包装成阿里云最终账单。
- 本节只保留为历史证据，不能恢复为兼容层、配置开关或默认值。当前正式决定见第 44 节：用量与费用只记录，不阻止合法任务；模型仍不自动降级。

提出者：用户

确认时间：2026-08-24

批准方式：用户批准调用链收敛，并明确要求“模型不需要降级”。

代码入口：`poc/vision_usage.py`、`poc/vision_agent.py`、`poc/generic_scene_observer.py`、`poc/generic_action_adapter.py`、`poc/orientation_safety.py`、`poc/universal_agent_orchestrator.py`

历史验证：历史 65 调用中文失败批次已完成离线计数；相关回归分别为 `432/432`、`209/209`、`51/51`，当时完整 Python 回归为 `1097/1097`（`43.363s`）。当时的六动作 `10` 次/`28000` Token 只是已废止多审计路径的历史基线，不能再作为当前验收目标或运行入口。

## 29. 2026-08-24 每个闭环步骤最多一次 Qwen 调用

- 一个闭环步骤只允许发起最多一次在线 Qwen 请求。“一步”指一份按当前实际活动子目标建立的新鲜稳定观察：该请求在同一份结构化结果中完成当前画面理解、上一物理动作结果验证（首步除外），并且仅在活动子目标没有变化时为该同一子目标选择唯一下一 canonical candidate；若目标已经完成，则返回终态而不再选择动作。
- 页面摘要、前台 App 身份、目标几何、屏幕方向、输入结构和动作选择不得再分别拥有联网入口。它们只能成为同一步结构化结果中的字段，或由本地稳定帧、OCR、标定、typed 状态账本、canonical catalog、scope 和 Controller 确定性校验。
- 第一步用一次调用选择首个动作；此后每次动作后的新观察先验证上一动作。若 DeepSeek 仍保持同一活动子目标，该观察可继续选择同一子目标的下一微动作；若活动子目标发生变化，本步必须在验证和任务图推进后结束，下一循环按新子目标重新截图并调用一次 Qwen。不得为了维持历史 `N+1` 计数上限而让模型提前观察尚未成为当前目标的后继子目标；调用数按真实活动子目标切换和一次新观察纠正如实记录，不设置会话硬门。
- 同一步中模型输出不合法、结构缺失或无法唯一执行时，保存该步证据并停止；不得通过第二次 Qwen 请求重试、改写提示词或抽样。仅允许既有不改变语义的本地有界语法规范化。
- 每步一次合同只限制“同一个闭环步骤不能重复联网”，不限制一个 session 可以包含多少闭环步骤，也不限制累计 Token。旧 `16` 次/`50000` Token 会话硬门已由第 44 节废除；不得用资源统计重新建立同类停止条件。
- 本决定不改变模型档次，正式模型仍为 `qwen3.7-plus`；不增加备用模型、自动降级、App/页面/截图/坐标特例、风险类别或确认项。

提出者：用户

确认时间：2026-08-24

批准方式：当前对话明确要求“每一个步骤就调用一次”并随后要求“修改”。

代码入口：`poc/vision_agent.py`、`poc/generic_scene_observer.py`、`poc/qwen_visual_decision.py`、`poc/generic_action_adapter.py`、`poc/orientation_safety.py`、`poc/universal_agent_orchestrator.py`、`poc/vision_usage.py`、`poc/web_app.py`

历史验证：正式运行入口只实例化 `SingleStepGenericSceneObserver` 与 `SingleStepQwenVisualDecisionObserver`；账本只允许 `single_step_observation`，旧 `compact/foreground/geometry/orientation/input/visual_action_selection` stage 在计数和联网前以 `vision_step_contract_violation` 拒绝。历史“唯一直接后继复用动作后观察”结论已被第 50 节明确废止，不再属于正式运行合同。离线通过不能替代新的真机 session 成功证据。

## 30. 2026-08-24 普通导航动作的一次新观察纠正

- 普通 `read_only` / `navigation_only` 子目标的物理动作已真实执行、但动作后没有出现预期语义变化时，不再永久停在 `needs_reobservation`。自动循环必须取得一份新鲜稳定画面，以新的单步 Qwen 调用重新理解当前状态，由 DeepSeek 修订当前任务图，并重新生成 canonical candidate、几何、Controller 决策和一次性动作 scope。
- 同一原始失败只允许一个纠正物理动作。纠正动作不得直接复用旧 observation、旧 candidate、旧 bounds、旧坐标或已消费 scope；新观察独立解析后得到相同落点并不构成盲重试，但必须有新的 observation/fingerprint、重新绑定的 candidate 和新的一次性 scope。
- 纠正动作 matched 后恢复正常自动闭环；仍然 mismatched 时必须终结为明确运行失败，不得继续显示“进行中”，不得第三次点击。新观察无法唯一生成动作、设备离线、画面不可用、目标不唯一或机械不可达时，也按具体运行故障停止。
- 登录/身份认证和付款/资金交易不适用自动纠正；普通打开 App、点击、滑动、返回、Home 等导航动作适用。普通发送、评论、关注等效果动作继续服从正式 EffectIntent 流程，不能借导航纠正绕过效果语义和结果验证。
- 该机制把“无反应后重新看、重新规划、自动纠正一次”作为新的正式权威，取代自动循环中“任意 mismatched 均立即停止且不进行一次纠正”的旧否决。每个闭环步骤最多一次 Qwen 的第 29 节保持有效：纠正重观察是一个新的闭环步骤，不允许在同一步增加第二次模型调用。

提出者：用户

确认时间：2026-08-24

批准方式：当前对话明确要求“改成更合理的通用方案”。

当前失败证据：session `6c4c1012f44c4f038561d4e83cf1604f` 的首个 `tap_semantic` 已执行一次，但动作后仍为 `launcher/home_screen`，回执为 `mismatched`；旧自动循环停在 `needs_reobservation`、无 proposal，网页长期显示进行中。该样本只证明通用导航纠正缺口，不授权微信、截图或固定坐标特例。

代码入口：`poc/universal_agent_orchestrator.py`

验证要求：正向样本必须证明首个 mismatched 后发生 0 动作新观察、使用新 observation/scope 执行最多一个纠正动作并成功继续；变化样本覆盖目标仍未变化时第二次 mismatched 明确失败；反例覆盖效果、认证/付款或无法形成新唯一动作时 0 纠正物理动作。离线回归通过不能替代后续全新真机会话证据。

## 31. 2026-08-24 结构化 App 场景身份统一归约

- 真实 Android 包名、用户目标中的语义 App ID 与视觉模型生成的结构化 `screen_id` 可能使用不同命名。正式 App 前台判断必须联合使用当前场景顶层 `foreground_app_id`、`app_id` 和 `screen_id` 的结构化身份；任一顶层身份与任务图目标 App 的正式身份词唯一绑定时，可以证明目标 App 当前前台。
- 不得读取自由摘要或子元素中的 App 名称来完成上述绑定。Launcher 中的 App 图标、标签、按钮和搜索结果仍只能证明“可以进入”，不能证明目标 App 已在前台；`app_id=launcher/screen_id=home_screen` 等结构化 Launcher 场景必须继续拒绝。
- 不增加包名到产品名的固定映射，不增加 App、页面、截图或坐标特例。该规则适用于任意“运行包名不含产品语义名、但结构化 screen_id 包含任务图正式 App 身份”的场景；三个顶层身份均无法绑定时继续使用已验证的动作转换/表面 lineage，证据仍不足则停止。

提出者：用户

确认时间：2026-08-24

批准方式：用户要求修复网页任务长期停滞并采用通用方案；加载后的新 session 证明首击已经 matched 打开目标 App，但旧单字段包名比较错误阻止任务推进，因此按已观察到的通用根因修复。

当前失败证据：session `b735906573aa47cbbe21ae83c181f771` 的动作后场景为 `app_id/foreground_app_id=com.tencent.mm`、`screen_id=wechat_main_chat_list`，首个 `tap_semantic` 为 matched，手机已经进入聊天列表；旧校验仅比较 `com.tencent.mm` 与语义 ID `wechat`，错误产生“Launcher 入口不能证明目标 App 前台”。

代码入口：`poc/universal_agent_orchestrator.py`

验证要求：包名与语义名不同、结构化 screen_id 唯一绑定的正例必须通过；相同包名但通用 screen_id、Launcher 中仅子元素带目标 App 名、其他无关 App 三类反例必须拒绝。离线测试通过不能替代重新加载后的全新会话推进证据。

## 32. 2026-08-24 单步观察内层协议版本的唯一有界规范化

- `2026-08-24-single-step-qwen-observation-v1` 的顶层封装已经唯一确定内层输入审计协议。若模型返回的 `input_structure` 恰好包含完整且仅有 `application_inputs`、`ime_preedit_regions`、`keyboard` 三个事实字段，唯一遗漏固定的 `protocol_version`，本地可以补入当前正式 `INPUT_STRUCTURE_AUDIT_VERSION` 后继续严格解析。
- 该规范化只恢复一个由外层合同唯一决定的常量，不补视觉事实、不删字段、不改坐标、不推断动作。存在任意其他额外字段、缺少任一事实字段、显式给出错误协议版本或值结构不合法时仍必须拒绝；不得发起第二次远程采样来碰运气。
- 该规则适用于任意 App 和任意输入目标。它解决的是组合封装的确定性格式兼容，不建立新的动作协议、风险边界或 App 特例。

提出者：用户

确认时间：2026-08-24

批准方式：用户要求采用更合理的通用方案；全新 session `7add250dce84430084ac3e7906d7698a` 在 0 次物理动作时证明模型返回了完整输入事实结构、但仅遗漏唯一固定的内层协议版本，旧解析器因此错误终止。

验证要求：遗漏且仅遗漏内层固定版本的正例必须在一次模型请求内通过；含额外字段、缺事实字段和错误显式版本的反例继续拒绝。离线测试通过不能替代重新加载后的全新真机会话证据。

## 33. 2026-08-24 精确标签归约不得晋升无效几何

- `target_ui_label` 的逐字匹配只能消除多个合法可见候选之间的目标归属歧义。候选自身必须先满足当前正式 0–1000 几何合同；本地归约器不得把越界、倒置、缺失或其他无效 bounds 的元素从 `goal_relevant=false` 晋升为动作目标。
- 模型原本已经标为目标的无效几何仍严格拒绝，不能被丢弃或修坐标；明确非目标的无效外围元素可继续按既有规则删除。该规则不会把无效坐标转换为可执行坐标，也不会增加 App、页面或标签特例。
- 场景顶层身份或其他合法事实已经证明当前子目标完成时，任务图可直接重规划；不能因为一个无效的同名外围标签阻止这些非几何完成证据。

提出者：用户

确认时间：2026-08-24

批准方式：用户要求采用更合理的通用方案；全新 session `c3ee0d4c` 的模型把底部已选标签明确标为非目标，但旧本地精确标签归约器因文字匹配把它改成目标，造成 0 动作终止。

验证要求：合法唯一标签仍能纠正模型的错误目标归属；无效几何且原本非目标的同名外围元素不得被晋升，并可被安全丢弃；无效几何且原本为目标的元素继续失败关闭。

## 34. 2026-08-24 具名子页面完成必须同时绑定类别与限定身份

- 通用页面类别只能证明类别本身。例如结构化 `chat/conversation` 只能证明当前是聊天类表面，不能单独证明“指定联系人/会话的聊天页面”；`detail/settings/list` 等类别同理。
- 完成条件包含联系人、订单、文件、设置项或其他具名限定身份时，正式完成证据必须同时绑定该限定身份与目标页面容器。合法来源是当前结构化 `screen_id`、页面标题或页面级 container/text 身份；列表项、按钮、图标、搜索结果等入口只能生成导航候选，不能证明其目标页面已经进入。
- 目标 App 身份与具名子页面身份分开判断。完整 App 名称/ID可以约束当前 App 或 verified surface lineage；只有去掉该完整 App 身份后仅剩“打开、应用、主界面、前台、可见”等通用状态词时，App 已在前台才足以直接完成节点。若还剩联系人、订单、设置项等限定词，必须继续取得子页面证据。
- 不再用 App 名称和完成条件之间任意短词/n-gram 相交来证明目标 App 表面；必须出现完整 App 名称或完整 App ID。该规则不依赖微信、聊天、联系人或具体页面，适用于任意 App 的入口→具名目标页导航。

提出者：用户

确认时间：2026-08-24

批准方式：用户要求修复 session `026c17edd2034033875fdf0ba5c0d917` 暴露的通用缺陷。该会话在微信聊天列表仅看见“文件传输助手”入口时，旧语义别名把 `chat_main_list` 的通用聊天类别当成完整具名聊天页身份，并错误完成导航节点。

验证要求：通用聊天类别可证明无具名限定的聊天页面；同一类别不能证明另一联系人/会话的具名页面；结构化页面标题包含具名身份时可以完成。App 主界面幂等、浏览器当前页面 lineage、Launcher 入口防冒充和具名列表入口反例必须同时通过。

## 35. 2026-08-24 App 前台身份由 canonical 表面合同统一裁决

- Android 运行包名、用户目标中的产品 ID/名称与结构化 `screen_id` 可能使用不同命名。当前前台 App 的正式身份必须由 canonical action protocol 统一裁决；任务图零动作推进、Qwen canonical 目录和 Policy 不得各自保留不同的 App 匹配规则。
- 合法证据只包括场景顶层 `foreground_app_id/app_id/screen_id`、精确 App 页面标题，以及已有 receipt 证明的 runtime package lineage。结构化 `screen_id` 可用有界身份词把产品 ID 绑定到当前运行包；摘要、正文、按钮、列表项、桌面图标和搜索结果不得参与 App 前台身份绑定。
- receipt lineage 强于结构化别名：已经由真实入口动作证明运行包名时，typed surface 必须继续升级为该精确包名，不能因 `screen_id` 已能模糊绑定而丢失强证据。
- 该规则不包含 App 名称映射、微信分支、页面分支、固定坐标或自动重试。Launcher `home_screen`、无关通用 `screen_id` 和仅正文提及 App 名的画面继续拒绝。

提出者：用户

确认时间：2026-08-24

批准方式：用户要求继续修复全新 session 的通用阻塞；本节只统一既有 App 表面身份权威，不增加风险、确认或产品边界。

当前失败证据：session `1847fd05a76d42d1a32bf02bb8cff099` 已正确保留 `find_recipient`，画面只有唯一强目标 `e1/文件传输助手`，但任务推进认可 `screen_id=wechat_main_chat_list` 属于微信，canonical 目录仍因 `foreground_app_id=com.tencent.mm` 与产品 ID `wechat` 不同而移除点击候选，只剩 `home/back/wait_for_change`，最终 0 动作 blocked。

验证要求：该现场离线重放必须恢复唯一 `tap_semantic(e1)` 选择；运行包名不透明但结构化 `screen_id` 含产品身份的变化样本通过；Launcher、无关 screen ID、正文提及三类反例拒绝；receipt lineage 仍升级为精确运行包。离线测试不能替代加载后的全新真机会话证据。

## 36. 2026-08-24 单步空输入框的单值权威（2026-08-27 修订）

- 单步响应内 `input_structure.application_inputs[*].text` 是应用输入正文空/非空事实的唯一视觉权威。场景分支只可证明目标相关输入表面存在、完整可见及其同帧几何，不再被要求填写 `states.value`，也不得因该字段缺失、为空或含过时转写而生成、覆盖或否决正式输入值。
- 当 `input_structure` 只报告一个完整可见、置信度不低于 `0.9` 且 `text=""` 的应用输入框，同时场景只报告一个 `goal_relevant=true`、完整可见、置信度不低于 `0.9`、具有非空外观证据的输入表面，且两者几何双向重合不低于 `0.85` 时，可以建立当前空输入框的唯一本地目标。最终值与可执行几何均只取自 `input_structure`；场景框仅作同帧表面证明并立即退出正式投影。
- 任一分支存在多个输入目标、场景表面证据为空、几何不重合或当前没有活动输入事务时继续拒绝。不得根据 App、页面、截图、固定位置或任务正文猜测输入框。
- 旧的“scene 与 input_structure 两处都必须逐字写空字符串”规则、提示词双写要求、场景值提取、相等比较及其旧运行时命名必须物理删除；正式执行只使用重新生成的 `local_audited_input_*`。

提出者：用户

确认时间：2026-08-24；单值权威修订确认时间：2026-08-27

批准方式：用户先要求继续修复全新真机会话中唯一空输入框没有占位文字的问题；本轮再次明确要求完成正确设计并删除过时代码。本节只把输入内容收敛到一套权威，不新增风险、确认或 App 边界。

当前失败证据：session `5b4b928f7486444c8a6adca340d39620` 的同帧 `input_structure` 已报告唯一空应用输入框 `text=""/bounds=[120,1170,680,1230]`，场景也报告同一可见输入表面，但场景省略了非权威 `states.value`；旧相等门因此丢弃本地目标并终止为“没有建立当前输入事务的唯一本地目标”。终态 `failed/physical_actions=2`，不是网络、设备、坐标或机械臂故障。

验证：场景省略 `states.value` 的现场结构样本与跨 App 场景带过时转写的变化样本均生成唯一 `local_audited_input_1/value=""`，值只来自 `input_structure.text`；旧函数名、旧参数名、旧证据字段名和旧相等比较均有物理缺失防恢复扫描。决定性样本 `3/3`、观察器与输入账本 `58/58`、scene—输入—Qwen—canonical—adapter—orchestrator—DDD 相关链 `503/503`、完整 Python 回归 `926/926`（`108.689s`）通过；静态编译通过。离线结果不能替代加载后全新 session 的输入和发送真机证据。

## 37. 2026-08-24 空输入框不再强制依赖占位符或光标

- `input_structure` 对空输入框的正式视觉证据是完整、可分辨的输入表面。完整边界、与周围 App chrome 可区分的完整填充/轮廓、占位符、光标或聚焦高亮均可作为文字证据；占位符、光标和聚焦高亮不再是强制条件。
- 仅凭任务要求、图标之间没有内容的空隙或相邻图标本身，仍不得推断输入框。模型必须报告完整输入表面、原始可见线索和几何；本地解析仍要求唯一字段、完整可见、有效坐标和足够置信度。
- 本节删除的是输入视觉合同中“完整边框还必须再带占位符/光标/高亮”的旧重复否决，不增加 App、页面、工具栏、固定位置或坐标特例，也不改变 `input_structure` 对正式输入值和几何的唯一权威。

提出者：用户

确认时间：2026-08-24

批准方式：用户反复明确要求删除过严旧协议，并在本轮要求直接修复当前基本输入闭环。

当前失败证据：加载提交 `0065c55` 后的全新 start `generic_supervised_20260824_184326_fde89cb1` 已在 0 个物理动作前把任务正确推进到 `input_exact_text`，画面为具名会话页且底部存在完整浅色输入表面；旧提示却要求空字段必须另有占位符、光标或高亮，Qwen 因此返回 `application_inputs=[]` 并导致观察失败。

验证要求：提示合同必须明确完整可分辨的空输入表面无需占位符、光标或高亮，并明确拒绝仅凭任务或图标空隙猜测；相关观察回归 `284/284`、完整 Python 回归 `1137/1137`（`54.940s`）通过。是否能在该真实画面识别输入框仍须加载后的全新 session 证明。

## 38. 2026-08-24 键盘辅助状态正式回执与 canonical 合同对齐

- canonical action protocol 已正式允许并生成 `element.state.keyboard_layout`、`element.state.keyboard_input_mode`、`element.state.keyboard_case_mode` 三类输入辅助状态后置谓词；Controller 必须逐一消费同一正式谓词，不能以“尚未实现”否决 canonical 已发布的合法动作。
- 三类谓词统一绑定执行前辅助键携带的非空 `input_element_id`。动作后必须仍有且只有一个同 ID 的可见输入字段，字段值逐字等于授权事务的预期值，同时对应键盘状态逐字等于正式谓词值；字段丢失、值变化、状态不符或多目标均拒绝。
- `input_element_id`、授权前后值、正式谓词和 `expected_effect` 必须四者一致。该修复不根据 App、键盘品牌、键面文字、坐标或当前样本放宽状态，也不把动作后重新规划生成下一动作当作前一步成功证据。

提出者：用户

确认时间：2026-08-24

批准方式：用户要求继续修复基本输入闭环；现场证明物理切换实际成功，但正式回执因 Controller 漏实现 canonical 已允许的谓词而错误失败。

当前失败证据：session `edf738e5f6834dbf9be67fc278fcaa6d` 的聚焦动作 matched；第二个键盘模式切换动作后，同一字段从 `chinese_pinyin` 变为 `direct_latin` 且值保持空，下一动作已生成 `input_verified_text`，但 `verification_step_2.json` 仍以“尚未实现的 typed transition expectation：element.state.keyboard_input_mode/equals”写成 mismatched。会话已取消，未输入正文。

验证要求：现场 `qwen_decision_step_2`、`trusted_observation_step_2/3` 必须离线重放为 matched；三个键盘状态谓词的正向变化样本均通过，字段 ID、字段值或状态不一致反例拒绝。相关回归 `423/423`、完整 Python 回归 `1138/1138`（`45.678s`）通过；测试子进程 PID `15916` 已自然退出。真机必须使用全新 session，不能改写已取消会话的历史回执。

## 39. 2026-08-25 唯一键盘输入路由（取代旧数字中转路径）

- 当前键盘布局仍只有 `qwerty / numeric / symbol` 三态，但正式拓扑改为 `numeric <-> qwerty <-> symbol`，QWERTY 是中心。旧 `qwerty -> numeric -> symbol` 路径及“把 `123` 当作符号下一跳”的运行规则、默认值和测试期望全部废除；`123` 只能表示进入数字层，不能推进符号输入。
- 拉丁字母：先由当前新观察证明 QWERTY 且 `direct_latin`；若当前为中文拼音，在 QWERTY 上点击方向明确的中英文切换键，动作后重新观察，再按确定性字母键序输入。
- 中文：先由当前新观察证明 QWERTY 且 `chinese_pinyin`；若当前为英文，在 QWERTY 上切到中文，重新观察后输入本地确定性拼音，并只点击与目标中文逐字相同的唯一候选。
- 十进制数字：当前不在 numeric 时，只点击当前画面明确指向 numeric 的 `123` 键；重新观察数字层后，只点击逐字相同的数字键。数字输入不要求先切换中英文模式。
- 其他可打印复杂字符：必须先证明输入模式为 `direct_latin`。若当前不是 QWERTY 且模式不符，先返回 QWERTY；若为中文则切到英文并重新观察；随后只点击当前画面明确指向 symbol 的 `！？#`、`!?#` 或等价逐字可见符号层键，再重新观察并点击逐字相同的字符。看不到符号层键时停止建立动作，不得改点 `123`。
- 空格和真实换行继续使用各自唯一可见键，不被误归为复杂符号，也不强制切换中英文。每次模式/布局切换仍绑定同一 `input_element_id` 与不变输入值，并由第 38 节 typed 状态回执验证；所有几何来自当前画面，不保存固定坐标、App 或键盘品牌分支。

提出者：用户

确认时间：2026-08-24

批准方式：用户明确指定字母、复杂字符、中文和数字四类输入顺序，并要求直接按该规则实现；本节取代 2026-08-24 的旧数字中转设计。

当前失败证据：session `893115ea26af411ebec124b56055222e` 的唯一 `input_verified_text` 物理动作已使输入框显示 `aaazjie`。动作后 Qwen 逐字报告该值、光标、direct Latin QWERTY 和唯一 `qwerty -> numeric` 的 `123` 键；下一字符为全角 `？`，旧规划器只寻找直达 symbol，因而以“没有建立唯一本地目标”终止。会话为 `failed/physical_actions=1`，未输入中文、未发送。

验证结果：新正向与变化样本证明中文模式下的符号先切英文、英文 QWERTY 的 `？` 只选择 `！？# -> symbol`、数字只选择 `123 -> numeric`、只见 `123` 时符号任务产生 0 个布局动作、symbol 层只发布逐字 `？` 键；空格和换行不触发模式切换。输入观察/事务/编排相关 `224/224`、canonical/执行相关 `59/59`、完整 Python `854/854`（`26.126s`）通过。项目 Uvicorn 已加载本批代码，0 动作 doctor 为 `ready=true`；尚未执行新的真机输入，完整混合输入仍须全新 session 逐步 matched 并最终 succeeded。

## 40. 2026-08-24 可选字符键语义冲突只撤销该候选

- `keyboard.literal_keys` 中结构完整但“可见 label 与声称插入值不逐字一致”的字符键，不能成为动作候选；本地只撤销该可选键，不再用它否决同一响应中独立成立的输入字段值、IME 提交结果、布局键或其他键盘事实。
- 空格键的声称值/标签与空格语义不一致时同样只撤销该键。字段缺失、对象字段集合错误、坐标非法、值不是单字符或键类型非法仍属于输入结构协议错误；被撤销的键永远不会进入 canonical 目录或 Controller。
- 该规则处理的是同一输入结构内非权威可选集合的隔离顺序，不把 ASCII `?` 等价为全角 `？`，不新增字符映射、键盘/App 分支或固定坐标。当前字符仍通过第 39 节的合法布局路径解决。

提出者：用户

确认时间：2026-08-24

批准方式：用户要求清除不合理重复限制并继续修复真实混合输入闭环。

当前失败证据：session `b30e9fbfbfba47dc87652af281a02515` 点击唯一 IME 候选后，动作后场景与 `application_inputs` 已逐字证明应用值为 `aaazjie`。同一响应额外把可见 ASCII `?` 键申报为会输入全角 `？`；旧解析器因此在验证已成功的候选提交前终止。会话为 `failed/physical_actions=1`，未继续布局、中文或发送。

验证要求：现场响应离线重放必须保留字段值 `aaazjie`、撤销错误字符键；下一字符为符号时只允许第 39 节的英文模式与可见 symbol 路由，不能恢复 `qwerty -> numeric`。正确逐字字符键仍可用，字段结构错误与非法几何仍拒绝；真机须新会话证明候选提交和后续符号动作回执 matched。

## 41. 2026-08-24 正式运行链执行减法并删除备用权威

- 每个步骤只允许一次 Qwen 远程视觉调用，该调用负责把当前真实画面结构化并建立 canonical candidate。之后的 selector 必须是纯本地、确定性的目录选择，不得再次调用 Qwen、重试采样或通过开关恢复旧在线选择链。
- Qwen 不再拥有子目标或任务完成判定权，不得输出 `finished`、完成标志或完成证据。DeepSeek 任务图是完成条件的唯一作者；本地动作后验证只评估同一正式条件和 typed receipt，不能建立第二套完成语义。
- 正式 Web、评测和 Agent 入口不再保留旧多轮 targeted/App/system/input 视觉审计作为备用路径。正式单步观察不需要的生产代码、默认值、提示词、包装类和专属测试在确认调用图不可达后直接删除，不保留兼容开关或静默回退。
- canonical action protocol 是动作候选的唯一构建权威。Policy 只能复核同一个 candidate；Controller 只处理设备、scope、观察时效、机械可达性与动作后证据，不得重新解释任务或重建候选。
- App 前台身份继续由第 35 节 canonical 表面合同裁决；输入值继续由 typed input ledger 裁决。分段输入规划、页面 lineage 和回执比较只能消费这些事实，不得成为第二身份权威。
- 几何只在候选建立时完成一次语义目标绑定与基本合法性审计；执行前 Controller 只检查该候选与当前 observation/scope 绑定、坐标可达和证据新鲜，不得用另一套目标框规则改选目标。
- Controller 与动作重绑定层不得维护“发送、输入、关注、评论、删除、长按、拖动”等动作词黑名单，也不得从目标说明文字重新推断产品风险。fresh-frame 重绑定只消费 canonical candidate 的唯一可见标签/角色/稳定状态、scope 和几何；产品风险仍只由本地类型化风险引擎按 `authentication`、`financial_transaction` 两类裁决。

提出者：用户

确认时间：2026-08-24

批准方式：用户同意按“保留任务图、唯一 canonical 动作、动作后验证和机械控制，删除重复几何门、输入身份判断、完成判断及无用旧链”的方案实施，并明确要求把无用内容全部删除。

验证要求：`架构减法重构验收台账.md` 中列出的旧符号扫描、相关历史失败重放、最小测试和完整回归全部成立；汇报必须分别说明已删除代码、自动测试结果和仍需真机证明的能力，不能把本轮离线重构写成新的真机验收完成。

## 42. 2026-08-25 通用执行接口、零动作诊断与双击接入

- `2026-08-20-canonical-action-v1` 继续是唯一动作候选权威。原运行时 `PhaseOneNavigationPolicy` 名称已退役，正式 `CanonicalActionPolicy` 直接引用 canonical 的 `SUPPORTED_ACTIONS` 并只重建同一目录复核，不再维护第二份动作白名单。
- Controller 已解析的唯一动作统一交给 `DeviceExecutor` 动作表。该接口只做 typed action 到 Robot transport 的映射，不理解任务、不选择目标、不判断完成或产品风险；正式 Robot、Mock 和离线 Replay 共享同一请求/结果合同。
- 新增正式零动作 `doctor`，通过类型化本地 API 客户端核对当前 OpenAPI 后读取模型、设备、会话、Controller、camera、四帧稳定性和能力快照，固定 `physical_actions=0`。旧 `run_universal_agent_live_preflight.py` 及其专属测试已删除，不再拥有运行入口。
- `double_tap` 已接入 typed required action、canonical 目录、Qwen 单步选择、Controller、DeviceExecutor 和 Robot。它必须绑定当前唯一可见目标和结构化动作后预期；卖家“连点次数”在一次 canonical 请求内设为 2，并在返回前强制恢复为 1，回执同时记录 `click_count=2` 与 `click_count_restored_to=1`。
- 双击与点击、滑动、长按、拖动一样属于普通自动执行动作，不新增确认或风险类别。真实设备 registry 在取得一次全新真机 matched/succeeded 证据前仍保持 `double_tap` disabled，缺口码为 `requires_double_tap_live_acceptance`；代码、离线回放或 Mock 不能自动晋级真机能力。
- 任务级 benchmark 使用 `ReplayDeviceExecutor` 批量复核跨 App 录制动作序列、参数和终态，固定 `physical_actions=0`、`remote_model_calls=0`，并明确标记为离线只读证据，不能冒充视觉模型或真机完成。
- Qwen 会话用量账本在原 token/费用字段之外增加请求耗时总计、平均值和最大值；不增加模型调用次数，也不改变 `qwen3.7-plus` 正式模型。

提出者：用户

确认时间：2026-08-25

批准方式：用户在对比 Open-AutoGLM 后明确同意实施零动作 doctor、薄设备执行接口、卖家原子双击和任务级只读回放，并要求继续删除无用旧入口。

验证要求：相关正反例、完整 Python/前端回归、旧入口/旧策略名扫描和差异检查通过；双击仍须加载当前提交后用全新真实会话单独验收，未取得证据前不得写成真机能力完成。

## 43. 2026-08-25 单步视觉坐标只保留一份 canonical 权威

- 正式场景、输入结构、canonical candidate、Controller 和机械执行之间唯一有效的视觉坐标仍为两轴分别归一化的 `0..1000`。Qwen 原始响应中的其他图像网格不是第二套动作坐标，也不得直接进入候选、scope、Controller 或机械臂。
- 单步 Qwen 外层响应必须逐字声明整份响应使用的一个 `coordinate_space`。正常输出声明 `normalized_1000 / 1000×1000` 并直接进入现有严格解析；若模型实际使用了等比例缩放后的图像网格，只能声明 `image_grid` 及其精确宽高，由本地入口在任何场景/输入解析之前把整份响应的全部边界和锚点原子换算为 `0..1000`，随后立即删除原始网格权威。
- `image_grid` 只在以下事实全部成立时允许一次确定性语法归一化：宽高为有界正整数；宽高比与本轮实际发送给 Qwen 的 JPEG 完全相容；整份 scene 与 input_structure 的每一个坐标都属于同一个声明网格；至少一个坐标确实超出 `1000`；全部坐标换算后仍形成合法边界。任一条件不成立、坐标空间未声明、不同分支混用坐标尺或换算结果退化时，必须在 `0` 新物理动作处报告具体坐标合同错误。
- 旧的“只在 input_structure 内按相机原图尺寸猜测像素坐标”入口退出正式运行链。外层原子换算同时覆盖 scene 和 input_structure，避免同一控件产生两份不同几何。既有多帧本地 OCR 对 QWERTY 行的独立复核只负责验证或重建 canonical 几何，不构成另一种坐标协议。
- 本节仅取代第 33 节中“任何越界坐标一律不得转换”的绝对表述：有精确坐标空间声明且满足上述唯一换算条件的整份响应可以规范化；未声明、混合、越界于声明网格或无法唯一证明的坐标继续严格拒绝。不得按 `1920/2000`、App、页面、键盘品牌、截图或固定控件位置猜测网格高度。

提出者：用户

确认时间：2026-08-25

批准方式：用户明确批准“Qwen 使用统一坐标，本地控制器根据当前真实画面换算图像像素与机械臂坐标；模型明显使用图像网格时先确定性归一化，系统内部只保留一份 `0..1000` 权威”的方案。

当前失败证据：session `075ca70dbd2e43e58b44362fd0c44f58` 已真实打开微信并进入文件传输助手会话页；动作后单步响应同时在 scene 和 input_structure 中把同一空输入框写为 `[120,1750,780,1830]`，随后旧输入内层解析以“bounds 不符合0..1000协议”终止。终态 `failed/physical_actions=2`，没有输入或发送；该失败不是网络、机械臂、风险确认或字符键问题。

验证要求：现场坐标样本在声明与实际请求图片等比例的 `image_grid` 后必须原子归一化并建立唯一空输入框；分辨率变化样本仍产生等价 canonical bounds；错误宽高比、分支混用、坐标越出声明网格和无声明越界四类反例必须在 0 动作处拒绝。相关与完整自动测试通过后，仍须加载项目 API 并用原任务的全新 session 取得真实输入和发送终态，离线重放不得冒充真机完成。

## 44. 2026-08-25 废除 Qwen 单会话请求数与 Token 硬门

- 正式 universal-agent session 不再设置本地 Qwen 累计请求数上限或累计 Token 上限。合法任务不得因为已经调用多少次 Qwen、已经累计多少 Token 或超过历史参考值而停止。
- `VisionSessionUsageLedger` 只负责记录逐请求 stage、fingerprint、provider request ID、网络尝试、Token、耗时和费用估算。统计字段没有否决权，不能产生运行故障、产品风险、确认要求或自动取消。
- 第 29 节“每个闭环步骤最多一次 Qwen 调用”继续有效：它防止同一步重复采样，但每一个新的动作后观察、纠正重观察或重规划步骤都可正常发起自己的一次调用，不受整个 session 的累计量限制。
- 正式模型仍固定为 `qwen3.7-plus`，不增加 Flash、备用模型或自动降级。模型身份错误、旧联网 stage、服务网络错误、账户余额不足和模型服务自身的上下文限制仍按各自真实技术原因报告；它们不得被重新包装成本地会话预算。
- 旧 `16/50000` 默认值、请求前预算检查、预算异常类、预算错误分类和输出中的剩余预算/预算耗尽字段必须从生产代码物理删除，不保留环境变量、配置开关、兼容默认值或回退入口。历史报告中的旧错误仅为只读证据，不能影响新 session。

提出者：用户

确认时间：2026-08-25

批准方式：用户明确要求“将 Qwen 单会话 Token 预算耗尽的方案废除”。

当前失败证据：session `917203ed3cb8496fb9668ae73acb1f45` 的前五步均 matched，第六个键盘布局动作已执行；六次成功 Qwen 请求累计 `50163` Token 后，动作后复核在第七次请求前被旧本地硬门以 `model_budget_exhausted` 拒绝，终态 `failed/physical_actions=6`。该失败不是网络、坐标、Controller、camera 或模型输出错误。

验证要求：构造会话超过历史 `16` 次请求且累计超过 `50000` Token 后，下一份 `single_step_observation` 仍必须成功取得请求 authority；生产源不得再包含旧预算异常类或错误码；用量、耗时和费用统计继续准确；模型身份与单步 stage 合同继续生效。自动测试通过只能证明硬门已删除，原发送任务仍须加载后用全新 session 验收。

验证结果：旧阈值变化样本在 `17` 次成功请求、累计 `51000` Token 后仍成功预留第 `18` 次请求；用量账本最小测试 `8/8`、账本/模型配置/编排相关回归 `173/173`、完整 Python 回归 `839/839`、前端回归 `30/30` 全部通过。生产 Python 源扫描确认旧预算异常类、错误码、阈值字段和请求前硬门均不存在。

## 45. 2026-08-25 英文字母预编辑必须点击逐字候选后才算提交

- `direct_latin` 只表示当前 QWERTY 键盘按确定性拉丁字母键序输入，不再表示字母已经直接写入 App 输入框。任何代码、提示词、测试或文档都不得用键盘模式本身证明提交完成。
- 字母键序执行后必须取得新观察。若同一 typed 输入框的应用值已经逐字等于授权前缀且没有预编辑，可按既有 exact value verifier 继续；若应用值仍为动作前前缀、同时出现与本段授权字母逐字相同的预编辑和唯一完整可见候选，则本段仍未提交，下一 canonical 动作必须点击该候选。
- 观察器必须区分输入框内带下划线或组合态的预编辑，以及键盘字母行上方或预编辑附近独立显示的候选。相同字母在输入框组合态和候选栏各出现一次时，第二个完整可点击文字必须作为独立候选报告自己的当前画面几何，不能因当前模式名为 `direct_latin` 而遗漏。
- 候选点击必须继承原 typed `input_field_id`、授权全文、本段 prior/fragment/expected、同一 App/页面和新鲜 fingerprint。点击后重新观察；只有同一 App 输入框的 committed `states.value` 逐字等于 expected，且本段预编辑已经消失或被 receipt-bound 提交结果确定性消费，字段才完成。
- 本规则同时适用于空前缀、已有前缀、换行后的前缀和多字段活动字段；错误字段、错误候选、重复候选、缺失几何、错误授权前缀或点击后仍未提交一律停止，不得清除、盲点、重复键入或把预编辑冒充正文。不得增加 App、正文、截图或固定坐标特例。

提出者：用户

确认时间：2026-08-25

批准方式：用户明确要求“英文模式也有候选，字母输完后在预编辑里点对应的字母，使它输入到输入框里”。

当前失败证据：session `ea9a0e1ab3de447681fb6d5993a65210` 在同一 typed 消息字段键入 `aaazjie` 后，真实帧显示输入框内带下划线的 `aaazjie` 预编辑，以及键盘 QWERTY 字母行上方独立的 `aaazjie` 候选。单步 Qwen 正确把应用 `text` 写为空、预编辑写为 `aaazjie`，却返回 `candidates=[]`；解析器因此以“有用输入法预编辑必须提供唯一逐字候选几何”停止。终态为 `failed/physical_actions=4`，没有点击候选、没有继续 `？你好`、没有发送。

验证要求：现场输出形状必须在一次单步 Qwen 响应中枚举英文预编辑和逐字候选；缺失、错误或重复候选继续 0 新动作拒绝。正向样本覆盖 `aaazjie`，变化样本覆盖不同英文片段、已有前缀/换行前缀和多字段活动字段；候选点击后的 `direct_latin` 残留预测栏必须由同一 pending typed lineage 归约为 committed App 值。相关回归和一次完整 Python 回归通过后，仍须加载服务并用全新 session 取得候选点击 matched、后续输入及最终 succeeded 的真机证据；离线测试不能改写本次失败终态。

离线实现与验证结果：输入结构合同已升级为 `2026-08-25-input-structure-audit-v11`；单步提示明确 `direct_latin` 只表示按键模式，并要求把输入框内带下划线的字母组合与 QWERTY 上方第二处同文字候选分别框出。候选点击后的 receipt-bound 状态归约同时接受 `direct_latin` 和 `chinese_pinyin`，但字段、授权前缀、唯一候选、几何与新鲜观察门不变。定向 `6/6`、输入/观察/canonical/Controller/编排相关 `420/420`、本批唯一完整 Python 回归 `856/856`（`22.957s`）通过；没有调用在线模型、重载项目 API 或执行真机动作。

## 46. 2026-08-25 系统最近任务作为通用类型化动作

- 新增 `open_recent_apps` 作为与 `home`、`back` 同层的 Android 系统导航动作。它只表示打开系统最近任务页面，不包含选择、关闭或清理任何应用，也不得退化为微信、某张截图或固定任务流程的专用动作。
- 动作由 canonical action protocol 建立唯一候选，Qwen 只选择该候选；Controller 与 `DeviceExecutor` 只消费同一 typed action、当前设备能力和一次单击事件栅栏。落点来自当前设备独立标定配置，不得由模型输出坐标，也不得复用普通 App 控件点击权威。
- 动作后必须取得新观察，并以 `foreground_app_id=system`、`screen_id=system_recent_tasks` 归类出的 `surface.kind=recent_tasks` 作为正式后置条件。普通 App 内标题或页面名含“最近任务/多任务”不能冒充系统最近任务页面。
- 在最近任务页关闭某个应用卡片仍是后续独立动作：必须由新观察唯一识别当前可见卡片并使用通用手势候选，动作后再观察验证卡片已经消失。不得把应用名称、卡片坐标或截图补丁写进系统动作。

提出者：用户

确认时间：2026-08-25

批准方式：用户明确指定“Home → 调出最近任务 → 划掉微信卡片”，并同意先补通用最近任务动作后完成真机闭环。

当前实现与验证状态：typed action 已接入任务语义、精确任务图、canonical 目录、Qwen 本地确定性选择、Controller、设备执行器、Robot/Mock、设备能力与单步观察合同。中文/英文/误匹配、候选选择、typed 后置条件和单击凭据定向样本 `5/5`，受影响模块回归 `476/476`、完整 Python 回归 `864/864` 通过；服务 doctor 已加载并列出 `open_recent_apps`。全新 session `ca59034b643142c99a1346fc5eb4d5df` 的唯一系统点击回执为 `mismatched/physical_actions=1`，动作后仍是 `launcher/home_screen`；会话已取消且没有自动重复。根据用户此前允许重新校正并扩大可点击范围，设备 `android_recents_x_ratio` 从 `0.315` 向按键内侧调整为 `0.330`，相关 `95/95`、完整 Python `865/865` 通过。纠正 session `e185750fabad4cb381c80480592cdf5f` 的唯一动作后真实四帧已经显示系统最近任务页、居中的微信卡片和底部全局关闭按钮，证明系统动作物理成功；但同一次 Qwen 把卡片内部误报为前台 `com.tencent.mm/chat_window`，所以正式 typed receipt 仍为 `mismatched/physical_actions=1`、session 最终 `blocked`。当前唯一缺口已经从系统键标定收敛为“单次视觉响应必须把系统最近任务容器与其中的 App 卡片分层”，微信卡片尚未被划掉。

## 47. 2026-08-26 元素消失验证只允许观察内身份连续

- `UIElement.element_id` 是每次观察局部生成的标识，不是跨页面、跨 App 或跨系统表面的永久身份。新观察复用同一个 ID，或在其他页面出现同名文字，都不能单独证明它是动作前的原元素。
- `element_absent` 的正式验证先判断 typed 表面连续性：`surface.kind`、前台 App 和具体 screen 均相容时，才允许把观察局部身份延续到动作后。表面连续时，动作前后边界对较小元素面积的重叠比例至少为 `0.60`，并且 ID 或逐字标签一致，可继续判为同一目标；因此同一张卡片即使 role/meaning 措辞漂移，也不能被误报为已消失。
- 表面不连续时，重复 ID 或逐字同名必须失效。只有 role 与非空 meaning 严格连续，并且标签相同或一侧没有标签时，才保留保守的同一语义目标判断。这样既不会把桌面同名 App 图标当成已划掉的最近任务卡片，也不会因模型轻微措辞漂移而放过同页面仍存在的原目标。
- 本规则只定义通用视觉元素的连续性，不新增 App、目标文字、截图、坐标或动作方向特例；canonical 仍只产生一个 element-bound 动作，Controller 仍只验证同一 typed 后置条件。

提出者：用户

确认时间：2026-08-26

批准方式：用户明确要求修复本次同名误判、把清除卡片结果写进项目，并回复“可以”批准上述通用方案。

当前真实证据：session `56801f9ce21641d685e566746a87b1d1` 只执行一次向左 element-bound swipe，动作后新场景从系统最近任务变为桌面；旧验证器把桌面同名微信图标误认成原卡片，所以历史终态保留 `cancelled/mismatched/physical_actions=1`。独立 session `228f8c04f5b74469bff744fe2ca790ce` 随后一次 typed `open_recent_apps` 为 `matched` 并最终 `succeeded/physical_actions=1`，新观察逐字显示“近期没有任何内容”，证明卡片已经被清除。两份证据不得拼接成前一会话 succeeded。

验证结果：正向、反向和跨表面同名/重复 ID 变化样本通过；最小 `2/2`、相关回归 `326/326`、本批唯一完整 Python 回归 `878/878`（`25.952s`）通过。项目 API 已加载 `2026-08-26-universal-action-v17`，官方 doctor 为 `ready=true/physical_actions=0/blockers=[]`；加载后没有再执行手机动作，卖家 `main.exe` 未被操作。

## 48. 2026-08-26 正式运行只保留一条动作权威链

- 正式闭环固定为：DeepSeek 只维护用户目标、子目标顺序和完成条件；一次 Qwen 单步观察只发布当前画面事实并从 canonical 目录选择一个 candidate；`2026-08-20-canonical-action-v1` 是该动作的唯一语义权威；Controller 只在真正执行前消费设备、一次性 scope、当前帧绑定、坐标可达和设备能力，并在动作后按 canonical transition 生成 typed receipt；DeepSeek 只能消费该 receipt 和下一份新观察推进任务图。
- `CanonicalActionPolicy` 及确认前后的重复 `policy.evaluate()` 退出正式运行链。Qwen 决策已经绑定 task/device/revision/observation/fingerprint、candidate 和 transition 后，任何后续层不得重新编译目录、改选 candidate、重新解释动作所属子目标或用业务文字二次否决。确认阶段只消费已经绑定的同一一次性 scope，不再重新裁决同一动作。
- Qwen 决策校验不得实例化 Controller。Controller 只由物理执行适配器调用一次；Qwen/selector 只验证单步响应与 canonical candidate 的结构和绑定，不能提前运行第二个 Controller 判断。
- 方向执行门继续要求同设备、同 scene fingerprint、同动作、一次性 live credential 和执行瞬间真实帧像素绑定；删除独立的固定秒数 TTL。真实帧尺寸或像素结构漂移仍立即拒绝，凭据也仍只可消费一次。固定时间窗口不能在像素绑定已经成立时成为第二份“画面是否新鲜”权威。
- 动作后 canonical receipt 已经证明的事实不得再被 DeepSeek 自由文本页面名、Qwen `meaning` 或历史 App lineage 推翻。零物理动作的观察/执行准备故障不得清空此前已验证的 App 表面 lineage；发生真实物理动作后则必须以新观察和 receipt 更新。
- Open-AutoGLM 只作为单循环形状参考：当前截图进入模型、模型只给一个动作、ActionHandler 执行、下一张截图继续。项目不复制其 ADB 文字注入、任意 Shell 或直接坐标；App 包名直启仅按第 53 节后来批准的窄范围可信映射执行。本项目仍保留 canonical typed candidate、一次性 scope 和动作后 receipt，且这些组件只能串行消费同一动作，不能形成平行裁决层。

提出者：用户

确认时间：2026-08-26

批准方式：用户明确要求“只保留一条权威层”，并同意参考 Open-AutoGLM 的单循环链路。

当前失败证据：session `f9b18e89553f4dcf93a9936090627ce2` 的 canonical `home` 已建立，但两次均在 `physical_actions=0` 时被 3 秒方向授权 TTL 拒绝；同机实际从最后方向证据到失败记录约 `5.7s`。此前 session `060dd58faa4d456fa8450cfd0a8b3e41` 已真实进入文件传输助手，但零动作重规划故障清空 verified App surface lineage，导致 DeepSeek 后置复核推翻已经成立的导航事实。两者共同证明重复层会否决同一权威事实。

本轮验收要求：生产代码中物理缺失 `CanonicalActionPolicy`、`policy.evaluate`、`policy_recheck`、Qwen 内 `UniversalActionController().resolve_one` 和固定方向 TTL；防恢复测试覆盖这些旧入口。正向样本证明一个 canonical 动作只经过一次选择、一次 scope 消费和一次 Controller 执行；变化样本覆盖确认等待超过历史 3 秒但真实帧未变仍可执行、真实帧变化仍拒绝、零动作故障保留 lineage、真实动作失败不保留旧 lineage。相关回归和一次完整 Python 回归通过后，才允许重载项目 API；离线通过不能替代新的真机结果。

## 49. 2026-08-26 每一步只由当前截图决定一个动作

- 正式执行循环固定为严格串行：取得当前新鲜稳定截图 → 单次 Qwen 把该截图结构化为当前页面事实和唯一目标 → canonical 目录只依据该份当前观察生成并选择一个动作 → Controller 执行一次 → 重新截图。上一张截图、上一页候选、历史坐标或尚未到达页面上的后续动作不得直接进入本步执行。
- 子目标要求特定系统表面时，当前截图中的 typed `surface.kind` 是候选前提。例如目标要求最近任务中的应用卡片，而当前截图仍是桌面或 App 页面时，本步只允许生成通往 `recent_tasks` 的 `open_recent_apps`；当前截图已经证明处于 `recent_tasks` 后，下一步才可依据该新截图为唯一目标卡片生成 element-bound swipe。
- 删除按历史分页轨迹自行挑选“尚未探索方向”的本地动作兜底。分页、滚动或卡片滑动必须由当前截图中的 typed viewport/目标元素和当前子目标共同形成唯一 candidate；不能因为上一动作历史或桌面存在分页视口就替用户目标选择滑动方向。
- `session.history` 只保存已发生动作的回执、证据和报告，不得作为 Qwen/selector 的输入参数。正式 Qwen 决策接口不得保留 `navigation_history` 或任何等价的历史动作选择管线；即使当前实现没有读取该参数，也必须物理删除，避免旧兜底通过接口残留恢复。
- 普通容器滑动继续不得仅凭前后 fingerprint 不同判定成功。目标绑定滑动要求动作前目标存在、动作后同一目标消失；其他滑动至少需要当前新观察的结构化语义变化。该既有验证只能证明“所选动作产生了对应页面变化”，不能反向证明一个脱离目标表面的动作原本就该被选择。
- 本决定不增加 App 名称、目标文字、截图样本或固定坐标特例，不改变登录/付款确认边界，也不引入第二套视觉判断。Qwen 负责当前画面事实；canonical 负责同一观察下的唯一候选；Controller 只验证并执行该候选。
- “当前”同时指当前画面和当前实际活动子目标。观察请求不得携带尚未成为活动节点的后继子目标；动作后任务图一旦推进到新子目标，旧观察即使来自刚拍摄的新画面，也不能作为新子目标的候选来源。

提出者：用户

确认时间：2026-08-26

批准方式：用户明确要求“每一步都把当前截图交给模型，再基于当前页面选择下一动作，不会脱离当前页面直接执行后续动作”，并同意按严格串行循环修改。

当前失败证据：session `d7ff92ca46084f02803acc5667630e07` 的活动子目标是“在最近任务界面中清除微信卡片”，但当前截图实际为 Launcher 桌面。旧本地分页兜底在该桌面截图上选择 `swipe left`；动作后新观察仍属于 Launcher，只把桌面分页 `page_index` 从 `0` 报为 `1`，所以该 swipe 的 viewport 变化回执可以 matched，却完全没有证明最近任务卡片被移除，随后 DeepSeek 正确 blocked。该失败属于当前表面前提未进入动作选择，不是设备离线、机械臂不可达、风险确认或模型没有收到截图。

验证要求：现场失败图重放时，桌面只能产生 `open_recent_apps`；新的最近任务截图中存在唯一目标卡片时只能产生绑定该卡片的 swipe；桌面分页 viewport 不得抢占目标；相机噪声导致 fingerprint 变化但结构化 viewport 未变时不得 matched。正向与变化样本、相关回归和一次完整 Python 回归通过后，才允许只重载项目 API；离线通过不能替代新的真机任务成功。

## 50. 2026-08-27 动作后观察不得预测或复用后继子目标

- 正式闭环唯一顺序为：按当前实际活动子目标取得新鲜稳定截图 → 一次 Qwen 发布该截图的当前事实 → 本地 canonical 目录为同一子目标选择一个动作 → Controller 执行一次 → 动作后新截图只验证刚执行的动作 → DeepSeek 消费 typed receipt 和该验证结果推进或保持任务图。
- 动作后观察可以在活动子目标没有变化时继续为同一子目标选择下一微动作，例如同一 typed 输入事务中的连续可见键位；也可以验证刚执行的发送/删除等效果结果。它不得携带 `next_subgoal_visual_context` 或任何等价后继上下文，不得预取后继目标的输入字段、文字、目标元素或候选。
- DeepSeek 一旦推进、替换或重写活动子目标，本轮必须以 `needs_reobservation` 结束并清空候选、Controller 决策和一次性 scope。下一循环必须按新活动子目标重新截图、重新调用一次 Qwen，并从该新观察重新建立 canonical candidate；即使像素 fingerprint 未变化也不得复用旧目标条件下的结构化结果。
- 页面指纹变化触发 DeepSeek 重规划时同样适用：若重规划改变活动子目标，当前按旧子目标取得的观察只能用于重规划，不能在同一轮继续为新子目标选动作。普通导航动作一次纠正仍按第 30 节取得独立新观察，不受本决定削弱。
- 旧后继融合阶段、结果字段、运行分支、回退路径、测试夹具和文档许可必须物理删除，不得保留兼容模式。本决定不增加 App、截图、目标文字或坐标特例，不改变 canonical、Controller、风险确认或机械臂语义。

提出者：用户

确认时间：2026-08-27

批准方式：用户在比较现有后继融合与严格“新截图 → Qwen → canonical 动作 → 执行一次 → 再拍新截图”循环后明确回复“可以，改正”。

本轮验收合同：生产代码不再生成、传递或消费后继子目标观察上下文；唯一后继、歧义后继、页面变化重规划和同一子目标连续微动作均有变化样本；防恢复测试扫描全部旧入口；普通导航一次新观察纠正相关回归保持通过；相关回归和一次完整 Python 回归通过后重载项目 API并只读核验版本。离线通过和 API 在线均不能替代新的真机任务成功证据。

离线验证结果：严格串行正向/变化/防恢复最小集 `7/7`，scene—adapter—orchestrator—Qwen—canonical—用量相关链 `361/361`，Web/官方客户端相关链 `94/94`，本批唯一完整 Python 回归 `926/926`（`122.289s`）通过；静态编译、旧运行符号扫描和 `git diff --check` 通过。上述结果只证明机制与离线回归，不是新的真机任务成功证据。

加载结果：本地提交 `cce6517` 已由项目 API 加载；官方 bootstrap 为 `openapi_verified=true/control_token_loaded=true`，状态为 controller/camera 在线、busy=false、0 活动任务/会话，公开 observer role 已变为 `single_step_current_scene_observation` 且 `max_online_calls_per_observation=1`，doctor 为 `ready=true/physical_actions=0/blockers=[]`。重载只替换项目 API PID `8832/18316` 为 `4452/24396`；卖家 `main.exe` PID `4860` 保持原启动时间且 Responding，未被操作。

## 51. 2026-08-27 可选视觉区域不得否决独立输入目标

- 每一轮新鲜截图及其唯一单步 Qwen 响应，是该轮当前画面事实的唯一来源。`input_structure.application_inputs`、`ime_preedit_regions`、候选和键盘是同一响应中的并列结构事实，不得再用其中一个可选区域的粗糙几何建立第二份整场否决权。
- 唯一应用输入框已经由完整可见表面、`application_inputs[*].text`、typed `input_field_id` 和当前活动输入事务独立建立时，预编辑区域与输入框重叠过大、边界过宽或不够紧，只能撤销该预编辑区域自身的几何用途；不得删除应用输入框，也不得删除具有自己完整有效 bounds 的独立逐字候选。
- 逐字候选仍必须在当前同一响应中唯一、文字与本段授权输入逐字一致、候选自身完整可见且 bounds 合法，并绑定当前 typed 字段、授权 payload、设备和新鲜 fingerprint。错误、缺失或重复候选继续不能执行；本决定没有把预编辑大框当成候选点击坐标，也没有放宽设备、scope、机械可达或动作后 committed value 验证。
- 旧的“预编辑与输入框重叠达到阈值，且预编辑面积超过输入框 60%，就删除整个输入框”的跨对象冲突门必须从生产代码物理删除，不保留开关、兼容路径或第二默认值。输入框正文仍只由 `application_inputs[*].text` 发布。
- 本决定不依赖 App、页面、正文、截图样本或固定坐标；相同规则适用于消息框、表单、备注、多字段和其他可编辑表面。

提出者：用户

确认时间：2026-08-27

批准方式：用户明确要求“删除‘一个可选区域不准确就否决整个输入目标’的旧冲突规则，让每张新截图成为这一轮唯一视觉事实”。

当前失败证据：session `246fff8b57364176b3eecd5f1c5a3c83` 已进入真实会话页并键入 `aaazjie`。同一新截图中，Qwen 正确报告唯一应用输入框 `text=""`、预编辑 `aaazjie` 和独立逐字候选 `aaazjie`，但把预编辑 bounds 粗画为整个输入框。旧 60% 面积门因此删除输入框，终态为 `failed/physical_actions=5`；没有点击候选、没有继续 `？你好`、没有发送。

离线验证结果：旧实现下，最新失败形态和跨页面变化样本均复现“没有建立当前输入事务的唯一本地目标”；删除跨对象冲突门后，两者均保留唯一 `local_audited_input_1/value=""`，并只发布候选自身 bounds。观察器 `22/22`、输入账本与 Qwen 决策 `42/42`、scene—输入—Qwen—canonical—adapter—orchestrator 相关链 `391/391`、完整 Python 回归 `927/927` 通过。

加载结果：本地提交 `a44905a` 已由项目 API 加载；只替换已核验项目 API PID `6448/7256`，新父/监听 PID 为 `26812/16756`，两者均位于交互 session 1。官方类型化客户端确认 `openapi_verified=true/control_token_loaded=true`、controller/camera 在线、camera stable、`busy=false`、0 活动任务/会话、Qwen 仍为 `qwen3.7-plus`，doctor 为 `ready=true/physical_actions=0/blockers=[]`。没有调用在线模型或执行新真机动作；session `246fff8b...` 的失败历史不变，卖家 `main.exe` PID `24024` 未被操作。

## 52. 2026-08-28 单步视觉线协议只保留当前图片纵轴网格

- 第 43 节中的 `normalized_1000`、`image_grid` 兼容规则由本决定明确取代，不再是正式运行合同。每次单步请求已经知道实际发送给 Qwen 的 JPEG 尺寸；响应必须只声明 `{"kind":"axis_grid","width":1000,"height":当前请求图片高度}`，横轴使用 `0..1000`，纵轴使用 `0..当前请求图片高度`。
- scene、input_structure、IME 候选和键盘锚点必须在同一外层 `axis_grid` 中报告；本地只在解析前做一次确定性换算到内部 `0..1000`。线协议不再接受 `normalized_1000`、`image_grid`、错误高度、缺失坐标声明、分支混尺或坐标越界，也不再根据坐标分布猜测模型实际使用的纵轴。
- 单步响应只接受完整外层 envelope；旧平铺 UIScene 不再自动包装。`input_structure` 缺失协议版本或出现协议外字段时不再自动补齐。上述旧修补入口和对应成功测试必须物理删除，并由“旧格式明确拒绝”的防恢复测试替代。
- 本决定不改变内部 canonical `0..1000` 坐标、Qwen 每张新截图一次调用、输入正文权威、canonical 动作、Controller、机械标定、公开 API 或风险边界；它只删除同一步视觉事实进入正式解析前的多套线协议权威。

提出者：用户

确认时间：2026-08-28

批准方式：用户明确要求纵坐标按实际图片 `0..1280` 声明并按该方案修改，随后要求彻底删除旧代码并继续收敛到少于 20000 行。

离线验证要求：实际请求高度变化时产生相同 canonical bounds；`normalized_1000`、`image_grid`、错误 axis 高度、越界、缺失 coordinate_space、平铺 scene 和缺失嵌套协议版本全部在 0 动作处拒绝；观察、Qwen、输入账本和 Adapter 相关回归及一次完整 Python 回归通过。离线通过不代表新的真机任务成功。

## 53. 2026-08-30 可选可信包名直启 App

- 用户批准借鉴 Open-AutoGLM 的 `Launch` 思路，但只新增一个通用 typed 动作 `launch_app`。DeepSeek 仍只声明目标 App，Qwen 仍只选择本轮 canonical candidate；模型、用户目标和 Web 请求都不得提供 Android 包名、组件名、ADB 参数或 Shell 命令。
- App 语义 ID/名称到 Android 包名的解析只来自本机、当前设备绑定的可信注册表。canonical 目录只接收该注册表签发的 opaque `launch_ref` 和固定目标包；没有映射、ADB 可执行文件、明确设备 serial 或目标安装能力时，`launch_app` candidate 必须完全缺席，已有当前画面图标 `tap_semantic` 路径继续正常工作，不得仅因直启不可用而阻止打开 App。
- transport 只允许固定参数列表、`shell=False`、严格包名格式和固定超时的一次包启动；禁止任意 ADB/Shell、ADB Keyboard、文字注入、force-stop、清数据、清缓存、后台脚本、Intent/组件透传和用户可控命令。
- `launch_app` 仍服从唯一串行闭环：当前新截图和当前活动子目标 → 同一 canonical 目录唯一候选 → 当前设备独占与一次性 scope → 执行一次 → 新截图。进程退出码不能证明成功；动作后只以这张新截图的结构化 App 身份为视觉事实。观察若真实给出运行包，可与注册表包精确匹配；若画面只能证明语义 App ID、App 名称或结构化页面身份，则按现有 typed App surface 身份权威匹配。不得把目标包投影进观察，也不得因视觉模型无法从像素读取 Android 包名而误判失败。
- 当直启候选可用时，它替代同一目标 App 的桌面图标点击；当直启候选不可用时，视觉点击是正常路径而非兼容旧权威。App 内点击、输入、发送、返回、滑动等后续动作仍全部使用相机、当前截图、canonical、Controller 和机械臂，不得转移到 ADB。
- 该动作沿用现有公开会话/API schema 和一次动作计数：一次成功或已尝试的包启动记为 `physical_actions=1`，同时能力元数据必须明确 `transport=adb_package_launch`、`mechanical_contact_ack=false`，避免把它误写成机械接触。
- ADB transport 超时、非零返回或启动器异常发生在调用已经尝试之后时，不得在同一轮重发；仍必须取得动作后新截图，再由上述唯一视觉身份权威判定 matched 或 mismatched。`/api/device` 的协议动作清单必须从唯一 canonical 集合派生，不能维护第二份硬编码词表。

提出者：用户

确认时间：2026-08-30

批准方式：用户在了解“仅包名启动、后续仍走视觉机械闭环、不可用时保留视觉路径”的方案后明确回复“可以”。

当前实施与验收合同：见根目录 `ADB包名直启验收台账.md`。本机只读检查当前未找到 `adb.exe`，因此本批可以完成通用代码、回放和能力禁用证据，但在 ADB 工具、明确 serial 和真机绑定就绪前不得声称当前设备已经完成直启真机验收。

## 54. 2026-08-30 项目自有 Companion IME 作为正式可信文字通道

- 用户批准为 Android 设备增加项目自有 Companion IME。它只承担已由当前 typed 任务和 canonical 目录授权的文字传输，不识别页面、不寻找控件、不规划任务、不选择动作，也不拥有输入成功的第二判断权威。
- `input_verified_text` 与 `clear_verified_text` 仍是唯一正式文字动作，不新增 `adb_input_text`、`ime_set_text` 或其他平行动作。前者只能追加本步授权片段，后者只能清空当前已绑定字段；不得在 `input_verified_text` 内偷偷先清空，也不得把两步合成不可审计的 replace。
- 每台设备必须显式配置一个当前文字 transport。一次动作只能消费一个 transport，不得在结果未知或失败后自动切换到机械键盘、ADB、剪贴板、无障碍或其他通道重输。机械逐键可以作为另一台设备或明确配置下的正常 transport，但不是 Companion IME 的失败回退。
- Companion IME 由用户手动安装、在 Android 设置中启用并选择，再通过一次性配对建立设备密钥；项目不得静默安装、静默启用输入法或要求日常 USB 调试。正式 Companion IME 通道不使用 ADB、Shell、卖家文字对话框、系统剪贴板或 Accessibility 节点树。
- 每个一次性文字授权必须绑定 `device_id`、`session_id`、`task_id`、`revision`、`action_id`、typed `input_field_id`、当前 observation fingerprint、授权 prior/fragment/expected 摘要、短时有效期和 nonce。IME 只在自身当前启用且 Android `InputConnection` 存在时接受一次；重复 nonce、过期、设备不符、签名不符或正文摘要不符必须在提交文字前拒绝。
- Companion IME 只允许最小命令集合：追加授权 UTF-8/Unicode 文字、清空当前编辑连接、读取无正文的在线/能力状态。禁止任意 Shell、包名启动、点击、滑动、读取其他 App 内容、读取剪贴板、枚举无障碍节点或透传用户可控命令。
- transport 返回值只证明 `accepted/rejected/attempted`，不能证明输入成功。执行后仍必须取得新的真实四帧观察，并由现有 typed 输入账本按同一 `input_field_id` 和精确 expected value 裁决 matched/mismatched；未知结果和 mismatched 均不得自动重发。
- 日志、公开状态和证据只记录 transport 类型、能力、nonce/action 摘要、正文长度、正文 SHA-256 摘要和结果，不记录正文、配对密钥或完整签名。登录/身份认证与付款/资金交易继续按现有两类确认边界执行，Companion IME 不得绕过确认。
- 当前能力只面向允许用户安装并启用第三方输入法的 Android 设备。禁止第三方输入法、没有有效配对、连接离线或当前编辑连接不可用时，应报告具体运行故障；不得假装已经输入，也不得用视觉模型猜测成功。
- 回滚方式是禁用该设备的 Companion IME transport、撤销设备配对密钥并由用户切回原输入法；不影响既有机械导航和显式机械文字 transport。不得以回滚为由恢复卖家文字对话框或 ADB Keyboard。

提出者：用户

确认时间：2026-08-30

批准方式：用户在了解自有 Companion IME 的安装、启用、无日常 ADB、Unicode 能力、截图复核和无助手设备限制后明确回复“可以，那做这个方案”。

当前实施与验收合同：见根目录 `Companion IME验收台账.md`。在 APK 构建、手机安装/启用/配对以及至少三个真实 App 的中文、Emoji/复杂符号、清空、换行、长文本和多字段新会话证据完成前，只能声明内部实现或相关测试状态，不得声明 Companion IME 真机阶段验收完成。

## 55. 2026-08-31 单一计划权威与最小运行校验

- 用户再次明确：本项目不采用“比 Open-AutoGLM 更严格”的产品方向。正式闭环只保留 `当前截图 → 一个 canonical 动作 → 执行一次 → 新截图`；不得以更严格、更多字段或更多判断层作为卖点，也不得在登录/身份认证和付款/资金交易之外恢复确认或阻止边界。
- DeepSeek 只输出计划定义：用户目标、约束、正向完成条件、效果意图、子目标依赖和确实缺失的澄清信息。它不再填写任务 `status`、`active_subgoal_id`、子目标运行状态、完成证据、反向 `effect_ids` 或重复的 `expected_results`。
- 任务运行状态只有一个本地派生权威：按子目标顺序和依赖选择唯一当前子目标；由当前新观察及其严格绑定的一次动作结果推进已完成状态；由 effect 的 `source_subgoal_ids` 派生子目标反向关联；由绑定子目标的正向完成条件派生效果结果。页面可继续展示这些派生字段，但模型不再提交第二份副本，本地也不得用副本不一致否决合法计划。
- 回执只记录“本轮哪个作用域的一次动作在新观察中得到什么结果”，用于推进运行状态和防止同一动作被重复消费；它不建立第二套目标语义、页面语义或风险分类。可选字段、重复摘要、自然语言措辞或非当前目标的视觉区域不得成为整张计划的独立否决权。
- 仍保留执行一次动作必需的最小技术条件：当前设备与控制权可用、当前活动子目标有新鲜观察、目标和动作在本轮能够唯一执行、几何/机械能力可达，以及动作后重新观察。它们属于运行可执行性，不是新增产品风险。只有登录/身份认证和付款/资金交易继续要求确认。
- 旧模型可写运行态 schema、运行态别名修补、双向效果关系比对、重复结果逐字比对、模型完成证据比对及其运行入口和成功测试必须物理删除。历史输出中的这些旧字段不再读取、修补、校验或影响结果；只投影正式计划定义字段后继续解析，避免无权字段反过来阻止合法任务。

提出者：用户

确认时间：2026-08-31

批准方式：用户在看到“保留更严格的回执、验证和风险边界”后再次明确纠正“我的项目不必这么严格，我记得我说过很多次了”。

当前实施与验收合同：见根目录 `单一权威最小校验验收台账.md`。自动测试通过只能证明内部合同，不代表产生了新的真机任务成功证据。

## 56. 2026-08-31 非动作视觉上下文不得否决唯一可操作目标

- 当前截图中“与目标有关”不等于“可以执行动作”。按钮、图标、输入框、标签、列表项和开关等可操作元素参与本轮动作目标唯一性；分页容器、页面说明、图片和其他只承担场景理解的非动作元素，即使同样标记为 `goal_relevant`，也不得否决已经唯一、完整可见且高置信的可操作目标。
- 同一截图中出现两个以上高置信可操作目标，或候选本身与另一可操作元素存在未解决的重叠冲突时，仍然停止；本决定只删除“辅助上下文拥有整场否决权”的旧规则，不放宽设备、当前 observation/fingerprint、canonical 动作种类、元素存在、坐标范围、机械能力和动作后新观察。
- 正式选择仍只有一条权威链：Qwen 发布当前截图事实 → canonical 目录把当前可操作元素编译为候选 → 本地选择当前唯一候选 → Controller 校验并执行一次。容器可用于证明分页、滚动轴和页面结构，但不能成为第二动作目标权威或反向取消独立按钮。
- Open-AutoGLM 的当前主循环同样以每步新截图直接取得一个动作并交给 ActionHandler 执行；本项目继续保留 typed canonical 与机械执行校验，但不得额外添加 Open-AutoGLM 主循环中不存在、且会让明确目标在 0 动作处失败的整场视觉一致性门。
- 旧的“任意另一个高置信 `goal_relevant` 元素存在就返回无目标”生产分支及其成功路径必须物理删除；防恢复测试必须同时覆盖“唯一按钮 + 目标相关分页容器可执行”和“两个可操作目标仍不唯一”。

提出者：用户

确认时间：2026-08-31

批准方式：用户明确要求先提供接近 Open-AutoGLM 的简单可运行版本、删除不必要旧判断，并要求任务不要在开始时直接报错；本节是该批准方向下对最新真实 0 动作失败的通用修复。

当前失败证据：session `2b680b64aa744f099d56cb4ef8582489` 的单次 Qwen 调用已在 Launcher 当前截图中明确给出唯一按钮 `e1/微信/launch_wechat`，同时给出不可点击的 `e2/container/paged_viewport`。旧 `unique_trusted_goal_element()` 因 `e2` 同样为 `goal_relevant` 返回空目标，canonical 最终 `blocked/physical_actions=0`。这不是模型没看见微信、设备离线、风险确认或机械不可达。

离线验证结果：最新画面形态与变化样本 `4/4`、视觉—canonical—Adapter—Orchestrator 相关回归 `443/443`、完整 Python 回归 `954/954`（`skipped=1`，`90.732s`）通过。

加载与真机结果：提交 `e9c29b4` 已由新项目 API PID `23760` 加载；官方 bootstrap、status 和 doctor 确认 OpenAPI/令牌、controller、camera、camera stable、模型配置和设备独占均正常。新 session `e4e120cf7651435a924fccb66c4fcb57` 只执行一次 `tap_semantic(e1/微信)`，动作后新 observation/fingerprint 证明前台进入 `com.tencent.mm`；receipt `receipt_bf9f7906ac9b48a3b1f432ce4ba8de05` 为 `matched/physical_actions=1`，最终 `succeeded/revision=2/active_subgoal=null`。卖家 `main.exe` PID `14500` 的启动时间保持不变且 Responding，未被操作。

## 57. 2026-08-31 正式运行改为模型直接决定 action 或 finish 的单循环

- 正式运行入口固定为：当前新鲜截图进入一次 Qwen 响应；该响应同时发布当前 `scene`、必要的
  `input_structure`，并且只选择一个 `action`、报告当前子目标 `finish`，或说明当前画面确实无法唯一继续；
  本地把模型动作精确映射到同一响应生成的 canonical candidate，执行一次后取得下一张新截图。禁止再由
  本地“唯一候选”兜底替 Qwen 选择动作，也禁止动作后调用 DeepSeek 决定本轮是否完成。
- `finish` 是 Qwen 对当前新截图的单步视觉结论，只能引用同一响应中的 scene 或 element 证据。本地只验证
  引用存在并推进一份本地运行进度；若还有下一高层目标，必须按下一目标重新截图。DeepSeek 只在会话开始
  生成轻量有序计划，不再参加正常 matched、finish 或下一动作循环；本批简单版本不保留运行时重规划入口。
- canonical 仍是动作类型和参数的唯一执行语义：模型只引用 action kind、当前 element ID、方向或拖动两端；
  精确输入正文、可信包名、typed transition、机械坐标和 Companion IME scope 继续由当前本地合同绑定。
  本地允许拒绝不存在、歧义、设备不支持或机械不可达的动作，但不得改选另一个动作。
- 普通点击、导航、输入、发送、提交、删除及其它非登录/付款动作自动串行执行；只有
  `authentication` 与 `financial_transaction` 在相应高层目标开始前等待用户确认。一次性技术 scope
  只防止旧截图动作重放，不得成为新的用户确认或业务否决层。
- 页面/API 可以继续投影计划、当前步骤、历史回执和状态，但这些字段只用于展示和审计，不得反向参与动作
  选择。旧本地 deterministic selector、可见子目标推进器、动作后 DeepSeek replan、App lineage 二次完成
  裁决及只验证这些旧入口的测试必须从正式运行代码物理删除，不保留兼容开关或隐藏回退。
- Open-AutoGLM 只提供循环形状参考：模型每轮返回 `do(...)` 或 `finish(...)`，ActionHandler 执行后重新截图。
  本项目不复制其裸坐标、ADB Keyboard、任意 Shell 或更宽确认策略；继续使用当前相机、canonical、机械臂、
  可信包名注册表和 Companion IME。

提出者：用户

确认时间：2026-08-31

批准方式：用户在确认此前“简单版本”仍由完整项目运行后，明确要求继续完成，并确认目标就是
“截图 → 模型选择一个动作 → 本地执行 → 新截图 → 继续或结束”的标准。

当前验收合同：见根目录 `单一权威最小校验验收台账.md`。先完成同响应 action/finish、正式编排入口替换、
旧权威删除和正反变化回放；相关回归与一次完整回归通过后才可重载项目 API。离线代码与测试结果不能替代
新的真机端到端证据。

## 58. 2026-09-01 Qwen 只输出推进动作或当前截图完成结论

- 正式 Qwen 单步决策状态只允许 `action` 或 `finish`。当前高层目标尚未由本轮截图证明完成时，Qwen
  必须从本轮实际可用 canonical 动作中选择一个最能推进目标的动作；该动作只需推进路径，不要求一步
  直接完成整个高层目标。当前不在目标 App、目标入口尚未出现、需要先返回主屏幕、返回上一层、滚动、
  等待加载或打开中间页面，都属于正常多步路径，不得成为模型停止理由。
- `blocked` 从 Qwen 响应 schema、提示词、解析器、决策对象、指标和正式编排入口物理删除，不保留旧响应
  兼容、隐藏开关或本地兜底改选。历史 Qwen `status=blocked` 输出按过时模型合同处理，不能再建立合法
  决策，也不能继续拥有普通任务否决权。
- Qwen 仍是当前截图的唯一动作选择权威。本地 canonical 只验证模型明确选择的动作能否精确映射到同一
  响应中的唯一候选；映射不存在、映射不唯一、响应格式无效或设备不支持时，必须报告具体模型合同或
  运行故障，本地不得替 Qwen 选择 `home`、点击某元素或任何其它动作。
- `finish` 仍只能引用本轮同一响应中的 scene/element 可见证据。没有当前完成证据时不得 `finish`，动作
  `matched` 也不自动代表高层目标完成；执行一个动作后必须取得新截图，再由下一次 Qwen 响应选择动作
  或 `finish`。
- canonical 目录内部“当前没有可执行候选”、设备/相机离线、观察过期、目标或动作无法唯一映射、机械
  不可达等继续作为本地技术故障处理；它们不是 Qwen 的语义 `blocked`，也不得伪装为产品风险。只有
  `authentication` 和 `financial_transaction` 保留用户确认，其他已授权动作继续自动串行执行。
- Open-AutoGLM 的参考边界是每轮模型只返回 `do(...)` 或 `finish(...)`，随后由 ActionHandler 执行并取得
  下一张截图。本项目采用同样的动作/完成二选一循环，但继续保留 typed canonical、机械可达校验、可信
  Companion IME、可信包名注册表和当前两类确认边界，不复制裸坐标、任意 ADB/Shell 或 App 专用脚本。

提出者：用户

确认时间：2026-09-01

批准方式：用户在得知 Qwen 的普通语义 `blocked` 是连续 0 动作失败的直接原因，并了解改为
“每张新截图只选一个推进动作或在有当前证据时 finish”的方案后明确回复“可以，改，我不希望一直报错”。

当前验收合同：见根目录 `单一权威最小校验验收台账.md`。必须包含两次历史 `blocked` 响应的防恢复
回放、至少一个跨 App 中间导航正向样本、一个不同目标/画面变化样本、相关回归、一次完整回归，以及
重载后新的跨 App 多步真机 `succeeded` 证据；在真机证据成立前只能声明内部合同已实现。
