> 历史/参考材料，不是当前运行权威。现行结论见[项目交接文档](项目交接文档.md)，产品方向见[项目最终目标](项目最终目标.md)。正文保留用于追溯，不据此恢复旧代码或执行旧步骤。

# 视觉动作影子核心验收台账（批次 2）

## 本轮必须证明的能力

在不改变正式 Qwen、控制器、机械臂和现行确认协议的前提下，把同一份 `UIScene + TaskSemanticIR` 确定性投影为：

1. 具有稳定 ID 的类型化视觉事实，而不是复制自然语言 evidence；
2. 元素、场景、任务实体之间的关系图；
3. 不依赖 `states.goal_relevant` 的可操作性；
4. 2–8 个由本地代码生成的合法单步动作候选；
5. 每个候选绑定明确的类型化前置条件和后置状态，普通动作不得只写 `scene_changed`；
6. 影子选择只能选择候选，不能改写动作或后置状态。

本轮所有结果必须固定为 `authoritative=false`、`execution_allowed=false`，不得进入正式执行链。

## 已观察事实与根因分类

- 事实：正式候选生成仍以视觉模型写入的 `states.goal_relevant=true` 作为主要筛选条件。
- 事实：现有候选的 `expected_result` 仍包含 `scene_changed/content_changed` 等弱类型字典。
- 事实：视觉 evidence 是自然语言字符串，缺少可稳定引用的事实 ID。
- 事实：当前已有“本地候选、模型只选一个”的雏形，但候选的资格和验证合同仍受上述旧字段影响。
- 根因分类：通用协议数据模型缺口，不是单个 App、截图、坐标或模型措辞问题。

## 同类失败与变化样本

样本固化在 `poc/fixtures/visual_action_shadow/cases.json`，至少覆盖：

- launcher 中目标 App 图标与目标 surface 的逐字关系；
- 任意 App 的输入框与 `input_text` 实体；
- 任意列表中的接收者/对象与 effect target；
- 相同语义换 App、换措辞后仍走同一编译器；
- 重复同名目标、低置信度、不完整控件、伪造 `goal_relevant`；
- 普通动作的类型化状态变化与探索动作的弱变化边界。

## 通用修复说明

- 新增独立 shadow 模块；只读取 `UIScene` 和 `TaskSemanticIR`。
- 本地生成 `VisualClaim`、`VisualRelation`、`Affordance`、`TypedStateTransition`、`ShadowActionCandidate`。
- 精确文本关系来自 scene label/value 与类型化 entity value 的本地逐字比较。
- affordance 来自 role、可见/启用/完整状态和已支持动作，不读取 `goal_relevant`。
- 候选只引用 claim/relation/affordance ID，不携带坐标，不生成 App 名称分支。
- 普通语义动作必须有具体 typed expectation；只有标记 `exploratory=true` 的候选可以使用 viewport/observation 变化。
- Qwen 的影子接口只能提交 `candidate_id`；本地返回原候选及 report digest。

影响范围仅限新增 shadow 文件、离线测试和只读诊断。回滚方式为删除该 shadow 模块及调用；正式生产行为不变。

## 验证清单

- [x] 同一输入重复编译字节级确定，claim/relation/candidate ID 稳定。
- [x] 修改任意元素的 `goal_relevant` 不改变 shadow 输出。
- [x] evidence 自然语言不进入 claim 内容，仅保留本地摘要。
- [x] 三类不同 surface/App 样本无需新增分支即可生成候选。
- [x] 精确目标唯一时生成元素候选；重复、低置信、不完整时不生成。
- [x] ready 报告候选数为 2–8，候选无坐标。
- [x] 普通动作不以 `scene_changed` 作为唯一后置条件。
- [x] 影子选择不能改写 action、subject 或 transition。
- [x] shadow 失败不会改变正式 Qwen 结果、controller authority 或物理动作计数。
- [x] 最小相关测试和历史相关回归通过；批次稳定后仅运行一次完整离线回归。

## 验证结果

- shadow 专项及正式相关模块：`301/301` 通过。
- Python 完整 discover：共 `1423` 项，`1393` 通过、`30` 失败、`0` error。30 项均位于既有 `test_web_platform.ApiEndToEndTests`：一组仍期待已退役固定 App/兼容路由返回 200/201/409/422 而当前正式合同返回 410，另一组受既有队列残留影响返回 409/queued。与本批新增 shadow 文件无调用关系，数量与本批开始前记录的 30 项一致；因此本批不宣称“完整回归通过”，也不为清理旧测试扩大生产改动。
- `py_compile` 和 `git diff --check` 通过（仅现有 CRLF 提示）。
- 未加载服务、未调用在线模型、相机、机械臂或卖家 `main.exe`。

## 本轮停止条件

以上功能证据通过并形成一个本地提交即停止。完整回归的 30 项既有失败单独记录，不在本批修复。不得在本批切换正式协议、重启服务、调用在线模型、相机或机械臂；真实跨 App 验收留给后续正式切换批次。
