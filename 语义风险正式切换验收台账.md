# 语义与风险正式切换验收台账

> **历史验收台账。** 本文记录切换当时的旧确认集合，不再定义当前政策。当前只有登录和付款需要确认，详见 `用户决策与协议边界.md`。

## 本轮唯一验收缺口

把已经通过影子测试的 `TaskSemanticIR` 与本地类型化风险策略切换为正式语义/风险权威，同时保持现有视觉动作决策仍为影子模式；完整离线回归必须恢复全绿，且不能恢复旧固定 App 工作流、不能触碰服务或真机。

## 必须出现的证据

1. Web 全量测试基线中的 30 个失败被确定性归因并关闭，生产默认仍返回 410 拒绝旧固定工作流。
2. 同一批旧任务样本同时经过旧风险解释与新类型化解释，差异被结构化记录；未经允许的差异会失败关闭。
3. 正式任务图的确认要求只由 `TaskSemanticIR.effect_intents` 和本地风险策略产生；约束、完成条件、收件人或正文等字段不能再被扫描成风险。
4. 发送、关注、评论等普通外部效果为自动执行；登录、付款、敏感权限变更和不可逆删除仍要求确认。
5. 换 App、换措辞、约束文本移动、未知效果等正反样本通过；完整离线回归全绿。

## 已观察事实与根因分类

- 生产默认 `LEGACY_WORKFLOWS_ENABLED=false`，旧固定工作流端点按设计返回 410。
- `ApiEndToEndTests` 的旧兼容测试未显式开启兼容模式，却仍期待 201/409/422；这是测试基线口径错误，不是恢复旧生产路径的理由。
- 同一测试类复用全局 `runtime.jobs` 和单一 `TaskStore`，用例间会残留队列/任务，造成只读预览收到 409、后台任务停在 queued；这是测试隔离缺陷。
- 当前正式风险路径仍可能从自由文本约束或完成条件推导未知风险；影子 IR 已证明可把实体、界面引用、效果和关键绑定分离，但尚未拥有正式权威。
- 根因分类：测试隔离错误 + 语义/风险权威重复；不是视觉模型、真机环境或特定 App 页面问题。

## 同类失败样本集合

- 普通消息发送：指定收件人与正文不得产生未知风险，效果为 `send_message`。
- 关注、评论：动作词或目标实体变化仍属于普通外部效果，不要求确认。
- 登录、付款、敏感权限、不可逆删除：必须要求确认。
- 同一约束分别放在 goal、subgoal、completion/constraint 字段：不得改变风险分类。
- 换成第二个 App 或不提供 App 名：效果分类保持一致，App 只作为 surface/entity 绑定。
- 未知效果：不得由自由文本猜成高风险，也不得自动获得可执行权限；应以明确的 unsupported/blocked 语义失败关闭。
- 旧兼容端点：默认 410；只有测试显式兼容上下文才验证旧端点自身的锁、确认和 worker 行为。

## 通用修复说明

1. 测试基线：为旧兼容用例显式开启兼容模式，并为每个用例隔离 `TaskStore`、清空任务队列；不改生产默认值。
2. 差异门：新增严格的新旧风险比较结果，按 effect、确认要求和来源分类；仅允许预先声明的“去除自由文本伪风险”等差异，其他差异直接使测试失败。
3. 正式切换：`TaskSemanticIR` 负责字段角色；本地策略只读取 `EffectIntent.kind` 决定是否确认。旧自由文本审计降为诊断，不能新增、删除或升级正式风险。
4. 视觉边界：Qwen、视觉候选和物理动作控制本轮不切换；没有 App 分支、固定命令、固定步骤或坐标补丁。
5. 回滚：语义/风险切换保持在独立提交；出现未解释差异时回退该提交即可，测试隔离修复可独立保留。

## 验证清单与停止条件

1. 先运行 30 个历史 Web 失败及相关兼容测试。
2. 运行语义 IR、风险策略和新旧差异门最小测试，包含正向与变化样本。
3. 运行 DeepSeek 任务图、通用编排器、Web 协议相关回归。
4. 最后只运行一次完整离线回归并记录总数。
5. 任一历史失败仍红、差异无法解释、生产默认旧路径被重新开启、或正式路径仍从自由文本产生风险时立即停止；不启动服务、不调用模型、不操作真机。

## 最终验证结果

- 旧 Web API 端到端兼容基线：`49/49` 通过；兼容开关仅存在于隔离测试夹具，生产默认旧端点仍为 `410`。
- 语义 IR、DeepSeek、Qwen 与编排器联合相关回归：`483/483` 通过；Web、失败诊断与视觉影子相关回归：`196/196` 通过。
- Python 完整离线回归：`1425/1425` 通过。首次运行唯一失败是旧测试仍期待 `unknown` 进入 Qwen；断言按新合同改为“Qwen 前拒绝”后，完整复跑全绿。
- 前端纯 Node 协议：`27/27` 通过；触控页面合同：`4/4` 通过；使用项目捆绑 Playwright 运行时的浏览器合同：`11/11` 通过；`app.js` 与 `protocol_adapter.js` 语法检查通过。
- `git diff --check` 通过。全程未启动、停止或重载服务，未调用在线模型、摄像头、机械臂或真机，未操作卖家 `main.exe`。

结论：本轮“正式语义/风险权威切换”的离线验收合同已完成。阶段 3 真实跨 App 验收仍未完成，不能由本轮离线结果替代。
