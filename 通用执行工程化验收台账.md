# 通用执行工程化验收台账

更新时间：2026-08-25

## 本轮必须证明的能力

在不增加第二套动作、完成或风险权威的前提下，补齐四项通用工程能力：一个零动作正式自检入口；一个只消费 canonical 已解析动作的设备执行接口；当前卖家控制端已经具备的双击原子动作；一个可批量运行、可只读回放的任务级评测入口。

## 必须出现的证据

1. `agent_api_cli doctor` 通过当前服务 OpenAPI 核对后的正式 API，统一报告服务合同、模型配置、设备锁、Controller、camera、稳定帧和动作能力，全程 `physical_actions=0`。
2. 旧 `run_universal_agent_live_preflight.py` 及其阶段一策略绑定被删除，不能与 doctor 并存为第二预检入口。
3. `DeviceExecutor` 只接收 Controller 已解析的一个动作并通过唯一分发表调用一个硬件原语；它不生成候选、不解释目标、不判断完成或风险。
4. `double_tap` 由 typed required action 进入现有 `2026-08-20-canonical-action-v1`，绑定唯一可见目标、一次 fresh scope 和动作后视觉后置条件；卖家连点次数在执行后恢复为 1。
5. 任务级评测可以批量读取陌生任务夹具，使用只读 replay executor 验证动作序列、终态和统计；报告明确标记为离线回放，不能冒充真机完成。
6. Qwen 用量账本除 token/成本外记录每次请求总耗时，并在会话报告中可见；不增加第二次模型调用。
7. 相关正向样本、变化样本、失败样本、完整回归、旧入口扫描和差异检查通过。

## 已有证据

- 正式链已在提交 `712d717` 收敛为任务图、canonical action、typed receipt 和机械 Controller。
- `poc/local_agent_api_client.py` 已在每次调用前核对当前 `/openapi.json`，但目前没有 doctor 操作。
- `poc/run_universal_agent_live_preflight.py` 可以零动作采四帧，但仍要求 `PhaseOneNavigationPolicy`，属于过期入口。
- `poc/robot_gui_poc.py` 已能安全设置卖家“连点次数”为 1；设备 profile 记录卖家存在 multi-click transport，但 `double_tap` 仍为 disabled。
- `poc/vision_usage.py` 已记录 Qwen token 和估算成本，尚未记录端到端请求耗时。
- 当前离线基线：Python `820/820`、前端 `30/30`。

## 唯一缺口与根因分类

- 自检：架构/代码缺陷。旧预检与当前正式协议脱节，导致诊断入口不能代表当前运行链。
- 设备执行：架构/代码缺陷。动作分派仍内嵌在大型 adapter 中，设备 transport 不能独立替换或回放。
- 双击：已知设备能力未接入。卖家 transport 已观察，但缺少原子恢复、typed receipt 和 canonical 全链绑定。
- 任务评测：验收设计缺口。已有单模块评测，缺少面向完整任务序列的统一离线报告。

## 通用修复

1. 新建当前协议感知的 doctor，接入 Web、类型化客户端和 CLI；物理动作计数固定为零。
2. 新建设备执行请求/结果和 handler registry；正式 Robot、Mock 与 Replay 共享同一接口。
3. 把双击作为现有 canonical 协议中的一个元素动作接通目录、Controller、Executor 和 Robot，不新增协议版本或独立 planner。
4. 新建任务级 manifest runner 和 replay executor；案例按动作种类与任务变化组织，不按 App 写生产分支。
5. 在现有 Qwen usage event 上增加 elapsed 字段和汇总，不改变预算或模型调用拓扑。

## 影响、回滚和停止条件

- 影响：只读运行诊断、Controller 到 Robot 的执行 transport、双击能力、离线评测和用量报告。
- 不影响：DeepSeek 任务图、Qwen 单步观察、登录/付款确认边界、既有动作语义和阶段 1–3 历史证据。
- 回滚点：本批开始前提交 `712d717`。
- 若双击不能从卖家事件返回有效 barrier、不能保证执行后恢复单击，或没有可验证动作后结果，则保持 typed capability gap，不伪造支持。
- 若 DeviceExecutor 需要重新解释任务、选择目标或重建 canonical candidate，立即停止该抽取。
- 离线报告始终标记 `hardware_actions_enabled=false`；没有全新真机证据时只写“已实现/测试通过”。

## 2026-08-25 最终结果

- 零动作诊断已实现为 `GET /api/doctor/{device_id}`，并接入 OpenAPI 校验客户端与 `agent_api_cli doctor`。报告固定声明 `physical_actions=0`，聚合当前模型、设备锁、Controller、camera、稳定帧、fingerprint 与 typed capability；旧 `run_universal_agent_live_preflight.py` 及其测试已删除。
- `RobotDeviceExecutor` 已成为 canonical 动作到 Robot transport 的唯一分发表；adapter 只负责准备 typed request 和消费 typed result。`ReplayDeviceExecutor` 使用相同请求合同且固定零硬件动作。Executor 与旧 `PhaseOneNavigationPolicy` 更名后的 `CanonicalActionPolicy` 均直接引用 `SUPPORTED_ACTIONS`，不再维护第二份白名单。
- `double_tap` 已贯通 canonical 目录、Qwen 场景动作、Controller、DeviceExecutor、Robot、能力验收和 Web 展示。一次 canonical 请求通过卖家 `click_count=2` transport 发出，`finally` 恢复为 `click_count=1`；正式能力晋级仍要求新鲜真机报告同时证明事件 barrier、`click_count=2` 和恢复值为 1。
- 离线任务级 benchmark 覆盖 Browser、Settings、Gallery 三个不同 App scope、三条任务、九个动作，结果 `3/3` 任务通过、`9/9` 动作匹配，`physical_actions=0`、`remote_model_calls=0`。这是只读回放，不是真机验收。
- Qwen usage 账本已记录单次请求耗时及总计、平均、最大耗时，没有增加调用次数或第二模型链。
- 最终离线验证：Python `836/836`、前端 `30/30`；旧运行入口扫描、静态编译和差异检查通过。全量回归最初的五个失败均来自同一个旧测试 Robot 缺少点击 barrier 回执，补齐测试替身的正式 transport 合同后清零，生产代码未恢复兼容分支。
- 只读在线状态核对通过官方客户端完成：`service_version=0.2.0`、OpenAPI/token 已核对、controller/camera online、busy=false、默认设备为 `device-local-01`。但旧服务仍有 session `fcce25192ce34c24b6dbfb62f6a10993` 处于 `awaiting_confirmation`，因此本批没有重载项目 API、没有取消或续跑该会话、没有调用 Qwen、没有操作卖家 `main.exe`、没有产生物理动作。
- 结论：四项通用工程能力均已实现并通过自动回归；doctor 的当前服务加载证据和 `double_tap` 新鲜真机晋级证据尚未取得。待活动会话自然释放后，先加载本提交，再运行一次 `doctor`；只有存在明确、安全、可验证的双击目标时才做一次真机验收，否则保持 capability gap。

## 2026-08-25 服务加载补充证据

- 提交 `b965ab5` 已加载到交互桌面 Uvicorn，监听 PID `25444`。首次普通隐藏启动虽能加载 OpenAPI，但因位于非交互桌面导致 controller/camera offline；该实例仅为本任务创建的 PID `2064/26452`，经精确核验后已终止。随后通过一次性 `Interactive` 任务启动当前服务，临时任务已删除，卖家 `main.exe` 始终保持 PID `2288` 与原启动时间且未被操作。
- 官方客户端重新核对结果：`service_version=0.2.0`、`openapi_verified=true`、control token 已加载，controller/camera online、busy=false、stop=false、0 活动任务/会话，默认设备 `device-local-01`。DeepSeek 与 Qwen 均已配置，正式视觉模型为 `qwen3.7-plus`、思考关闭。
- 新 `doctor` 已在当前服务真实运行：`schema_version=2026-08-25-runtime-doctor-v1`、`ready=true`、`physical_actions=0`、`blockers=[]`。连续四帧均为 `810x1440`、稳定性 `stable=true`，设备独占可用；本次没有调用在线模型、没有创建 Agent session、没有执行机械臂动作。
- 当前双击仍按合同保持 `enabled=false / gap_reason=requires_double_tap_live_acceptance`。本次只证明新服务与 doctor 已加载，不构成 double_tap 真机晋级证据。
