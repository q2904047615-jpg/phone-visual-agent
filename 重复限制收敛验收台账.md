# 重复限制收敛验收台账

更新时间：2026-08-20（Asia/Shanghai）

## 1. 当前唯一验收缺口

typed v4 正式链已经由 DeepSeek 任务图、TaskSemanticIR、本地效果策略、Qwen 视觉选择、
确认前新观察、控制器与动作后验证组成。当前缺口不是放宽这些安全边界，而是消除同一时刻、
同一字段被多个裁决器重复判定且结论互相矛盾的限制。

现场 session `605afc5a3665414bbf4c909a8bad38e4` 在 0 物理动作阶段失败。DeepSeek 的
合法 revision 只把已有全局完成条件的 `satisfied/evidence` 从未满足推进为当前视觉证据已满足；
`deepseek_task_graph._validate_revision()` 已验证条件 ID、描述、证据要求、证据来源和不可回退。
编排器随后又用 dataclass 整体相等比较，错误把合法状态更新视为“修改全局完成条件”。

## 2. 必须出现的证据

1. 现有完成条件可以基于当前 typed visual claim 更新 `satisfied/evidence`，而条件定义不能增删、
   重排或改写。
2. 伪造证据、旧证据、条件 ID/描述/证据要求变化、已满足条件回退继续失败关闭。
3. DeepSeek replan 的 execution-class 和 EffectIntent 不变量只由一个 revision validator 裁决一次，
   不再在同一 candidate 上重复调用。
4. 退役 Web 路由只保留一个确定的 410 门，不重复执行同一门。
5. 设备独占、fresh observation、scope/digest、目标唯一性、几何审计、标定凸包、一次一动作、
   动作后稳定与清晰度、typed receipt 和效果结果证据均保持不变。

## 3. 同类失败样本集合

### 正样本

- 现场同形：全局条件 `input_visible` 保持同一 ID/描述/evidence_required，只从
  `satisfied=false/evidence=[]` 更新为当前 observation 的唯一 visual claim。
- 变化样本：另一个 App 的页面可见条件以当前 visual claim 完成；旧已满足条件保留原证据并追加
  当前证据。

### 反样本

- 删除、新增或重排完成条件。
- 改写 condition ID、description 或 evidence_required。
- `false→true` 使用未知、旧 observation 或自然语言伪证据。
- `true→false`，或删除旧 completion evidence。
- 修改 goal、constraints、EffectIntent 定义或子目标 objective/dependency/execution class。

## 4. 全链路重复权威审计

| 层 | 检查 | 结论 | 处理 |
|---|---|---|---|
| DeepSeek transport | 严格 typed v4 schema、禁止旧 v2/v3 字段 | 单一输入边界 | 保留 |
| TaskSemanticIR | 将 typed entities/effects/subgoals 编译为本地语义 IR | 单一语义权威 | 保留 |
| 本地效果策略 | automatic / confirmation / block | 单一风险权威 | 保留 |
| DeepSeek revision | revision、图不变量、typed evidence 来源 | 单一任务图修订权威 | 保留 |
| visible-presence 外层 | 整体比较 CompletionCondition dataclass | 与 revision validator 冲突 | 改为定义不变量 + 本地证据状态检查 |
| DeepSeek replan | execution class 与 EffectIntent 在同一 candidate 前后各检查一次 | 同时刻完全重复 | 删除前置重复调用，保留正式 revision validator |
| Qwen selection | 只能选择本地生成 candidate | 视觉选择边界 | 保留 |
| visual policy | candidate 必须属于当前 typed subgoal/effect | 与 Qwen 职责不同 | 保留 |
| confirmation scope | session/task/device/revision/observation/action digest | 防过期与替换 | 保留 |
| fresh rebind | 确认前重新采四帧并重绑语义/几何 | 时间不同，非重复 | 保留 |
| controller | 动作种类、输入状态、效果后置条件 | 执行边界 | 保留 |
| calibration | 凸包与局部共识点 | 物理可达性边界 | 保留 |
| post-action | 稳定、相对清晰、新 scene、typed receipt | 动作后证据边界 | 保留 |
| delayed effect verify | 已执行效果只允许 0 动作结果复核 | 防重复外部效果 | 保留 |
| 退役 Web 路由 | 相邻两次调用同一个 410 guard | 完全重复且不可达第二次 | 删除一处 |

内部属性名 `risk_actions/risk_action_ids/external_impact` 只是 typed v4 的确定性运行时投影；
生产 transport 不接收同名旧字段，也不让它们重新分类或签发确认，因此不按名称删除。
`robot_core` 中的 `legacy` 是卖家 GUI 传输实现，不是旧高层协议，也不在本批删除。

## 5. 通用修复说明

1. 在编排器的 visible-presence 路径中比较完成条件的结构定义：顺序、ID、description、
   evidence_required 必须完全一致。
2. 状态变化只允许 `false→true` 或保持；`true` 不得回退，旧 evidence 不得删除。新增满足证据必须
   精确属于本轮 `ObservedState.visual_claim_evidence_refs`；没有 typed refs 时才使用同轮 controller-owned
   visible/grounded facts。外层不自行生成或改写证据。
3. 删除 replan 入口对 execution-class/EffectIntent 的第一次重复调用；正式
   `_validate_replan_candidate()` 仍在本地风险投影后统一验证全部 revision 不变量。
4. 删除退役 `/api/tasks/{task_id}/confirm` 中连续重复的第二个 410 guard。

不修改 App 名称、页面路径、目标正文、固定步骤、截图坐标、动作映射、风险分类或确认政策。
回滚范围限于上述三个局部修改及其测试。

## 6. 验证清单与停止条件

1. 增加现场同形全局 completion state 推进正例。
2. 增加定义改写、未知证据、回退和删旧证据反例。
3. 运行 DeepSeek typed transport/revision、编排器 visible-presence、Web 退役路由最小测试。
4. 运行任务图、语义 IR、Qwen、控制器、adapter、Web 相关回归。
5. 批次稳定后运行一次 Python 完整回归、前端合同、静态编译和 `git diff --check`。
6. 离线全绿后才本地提交并只重载项目 Uvicorn；不操作卖家 `main.exe`。
7. 重载后先核对 controller/camera/busy/device/活动会话，再从新鲜四帧建立一个全新 session 继续
   当前长文本任务。任一新失败立即停止，不自动重试、不发送。

## 7. 离线实施与验证结果

- 已移除三处同一时刻、同一输入上的重复裁决：visible-presence 对整个完成条件对象的二次等值否决、
  replan 对 execution-class/EffectIntent 不变量的前置重复调用、退役 Web confirm 路由相邻重复的 410 guard。
- 完成条件现在只允许定义完全不变后的证据状态推进；新增、删除、重排、改写、已满足回退、删除旧证据、
  引用非当前观察证据均继续失败关闭。
- 定向与相关回归分别为 `5/5`、DeepSeek+编排器 `213/213`、语义/风险/Qwen/Web 相关
  `585/585`；前端 Node 合同 `15/15`，离线浏览器合同 `11/11`；完整 Python 回归
  `1369/1369`。生产文件 `py_compile`、JS 语法和相邻重复调用扫描通过。
- 本地提交 `5f0bb4d` 已加载到项目 Uvicorn PID `28036`；卖家 `main.exe` 保持 PID `2904` 和原启动时间。
  重载后 controller/camera online、busy=false、0 活动任务/会话。独立四帧只读观察为 0 动作，确认文件传输助手、
  唯一输入值 `longinputvalidation`、已聚焦和 numeric/direct_latin 键盘。
- 随后的全新只读 session `990a2e79...` 在第 1 轮动作前观察以 `physical_actions=0` 失败，尚未到达本批修复的
  completion-state 推进路径。Qwen targeted delta 把纵坐标写成 `1090/1095`，超出协议 `0..1000` 坐标系，
  且保留为未规范化的 bounds 对象；本地严格拒绝。证据位于
  `poc/output/web/generic_supervised_20260820_174849_990a2e79/`。这是单次模型坐标合同输出失败，
  不是旧重复限制；按止损规则未重试、未放宽坐标合同、未执行任何物理动作。
