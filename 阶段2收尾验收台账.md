# 阶段 2 严格单会话收尾验收台账

更新时间：2026-08-16（Asia/Shanghai）  
状态：实施前证据门已冻结；尚未修改生产代码，尚未开始新真机验收。

> 本文只定义当前历史交接语境中的“阶段 2 严格收尾门”。项目产品方向和最终阶段定义仍以 `项目最终目标.md` 为准。若本文与最终目标冲突，以最终目标为准。

## 1. 当前唯一验收缺口

使用修复后的当前代码，从新鲜状态创建一个全新自然语言多动作目标，并在同一个 `universal-agent session` 中：

- 连续完成至少 2 个真实物理动作；
- 每个动作都有动作前后各 4 帧、新 `observation_id`、新 `fingerprint`、typed receipt 和 revision `+1`；
- 每个动作均为 `matched`，没有自动重试、旧 scope 或旧 receipt 复用；
- 最终同一 session 为 `status=succeeded`、`active_subgoal=null`。

独立 0 动作只读成功只能作为附加复核，不能替代上述证据。

## 2. 已证实事实与根因分类

### 已证实能力

- 机械臂已在同一会话成功执行 `tap_semantic` 与 `back`，两步均有前后 4 帧、不同 observation/fingerprint、typed receipt 和 revision 推进。
- 通用控制器能拒绝旧 scope、协议外动作、伪造完成证据和不安全输入，并在失败时保持 0 动作或不自动重试。
- 2026-08-16 当前相关离线基线为 DeepSeek、场景观察、Qwen 决策和通用编排 `511/511` 通过。
- 当前 `/api/device` 为 controller/camera online、`busy=false`、无活动任务/会话；卖家 `main.exe` PID 为 `19956`，启动时间未变化。

### 主要根因

分类：**模型结构化输出合同过宽，导致截断与语义漂移；本地安全控制器正确失败关闭。**

- 当前 Qwen 输出需要重复大量本地已有身份、候选、bounds 和协议字段，出错面与输出长度过大。
- 当前实时状态记录 Qwen 最近调用 `finish_reason=length`、`completion_tokens=1200`，证明截断已经实际发生。
- 当前远程格式修复会重新生成整份输出，增加第二次随机性；历史两次输出仍可连续非法。
- 这不是机械臂坐标、特定键盘控件或某个 App 路径缺陷，不得据此添加页面或坐标补丁。

## 3. 同类失败样本集合

1. `generic_supervised_20260816_210819_39b0a993/report.json`
   - 真实动作链已显示目标文字 `codex`；动作后观察连续返回非法 `mode_switch`，最终 `failed`。
2. `generic_supervised_20260816_213759_e0121503/report.json`
   - 同会话两个动作均 `matched`、revision `1→2→3`；键盘已隐藏后模型仍提出第三动作，为避免离开页面而安全取消。
3. `generic_supervised_20260816_215243_689e9177/report.json`
   - 模型为系统消失动作返回协议外 `expected_result.system_ui.soft_keyboard_visible=false`，物理动作前阻塞。
4. `generic_supervised_20260816_220337_5243db64/report.json`
   - 本地合法动作已经收窄，模型仍返回集合外 `tap_semantic`，0 动作阻塞。
5. `generic_supervised_20260816_220511_b4a129fb/report.json`
   - 同类集合外动作再次发生，0 动作阻塞；按止损规则不再继续在线采样。
6. `generic_supervised_20260816_220643_128c247f/report.json`
   - 第一轮把 input-only 状态写入 container，第二轮 JSON 不可解析，0 动作失败。
7. `generic_supervised_20260816_201218_051425f2/report.json`
   - 动作成功后 DeepSeek 使用当前观察之外的完成证据；当前代码已加入严格边界，保留为回归反例。

## 4. 通用修复说明

本批次只缩小模型与本地控制器之间的通用输出表面：

1. Qwen 结构化调用使用供应商已有 `json_object` 输出模式，减少不可解析 JSON。
2. 场景观察继续使用现有通用 schema，不重写观察架构；只处理已证实的输出截断和无意义重复成本。
3. Qwen 下一动作输出改为最小选择合同：模型从本地可信观察生成的通用合法候选中返回 `status`、`choice_id`、预期变化、完成证据、置信度和依据。
4. task/device/revision/observation/fingerprint、候选 meaning/role/label/bounds 由本地可信数据确定性绑定，模型不能重写或编造。
5. Qwen 仍负责从当前画面选择动作、判断 `finished/blocked`、给出预期变化；本地不得因“只有一个合法动作”而替模型自动选择。
6. 删除整份输出的远程格式重生成循环；最多允许一次唯一、只修语法且不改变语义的本地修复。无唯一修复时失败关闭。

该设计不包含 App 名称、固定命令、固定步骤、验收页面状态机或坐标补丁。

### 影响范围

- `poc/qwen_visual_decision.py`：最小选择合同、解析与本地确定性绑定。
- `poc/generic_scene_observer.py`：结构化输出模式和受测量约束的输出预算。
- `poc/vision_agent.py`：若需要，只扩展通用 provider 参数类型，不改变硬件权限。
- 对应专项测试和历史失败重放夹具。

### 风险与回滚

- 风险：前端或编排层仍读取旧完整 Qwen 输出形状。
- 控制：最小输出仅作为模型输入边界，解析后继续产生现有 `QwenVisualDecision` 正式对象，外部协议不变。
- 回滚：本批次保持单独提交；若完整回归出现大范围不相关失败，撤销该提交，不叠加兼容分支。

## 5. 验证清单

### 专项离线验证

- 合法 `tap_semantic`、`input_verified_text`、`back` 与 `finished` 均能生成原正式对象。
- 非法 choice、旧身份、伪造候选、伪完成证据、字段错层和截断 JSON 均失败关闭。
- 历史六类 Qwen失败样本重放，不产生任何硬件动作。
- 至少包含键盘、系统弹层或普通页面导航中的变化样本，证明不是验收页特例。
- 相关 DeepSeek、场景观察、Qwen 决策与通用编排测试全部通过。

### 完整离线回归

- Python 全量测试一次。
- 前端协议与浏览器合同测试一次。
- JavaScript 语法检查与 `git diff --check`。

### 0 动作在线门

- 只重载项目 Uvicorn，不触碰卖家 `main.exe`。
- 新观察与模型调用必须得到完整合法输出，Qwen `finish_reason=stop`，能形成合法 action/finished/blocked 和新 scope。
- 若同类合同错误再次出现，停止，不进行第二次采样或物理动作。

### 正式真机验收

- 开始前重新核对 `/api/device`、controller、camera、busy、设备、活动会话和 `main.exe` PID。
- 从新鲜状态创建全新自然语言目标和全新 session。
- 一次确认一个动作，动作后重新观察；失败不自动重试。
- 最终必须满足第 1 节全部条件。

## 6. 时间与止损

- 基线门：30 分钟；超时先报告服务或证据问题。
- 单批合同实现：累计 2 小时；超时停止扩大范围并报告。
- 专项加完整回归：累计 4 小时。
- 0 动作在线门：累计 4.5 小时；失败即报告，不进入真机。
- 真机、文档和提交：最迟累计 6 小时结束。

同一根因修改两轮仍未缩小缺口，或在线最小合同仍无法稳定建立合法 action/finished 时，阶段 2 保持未完成并报告唯一外部模型阻塞，不再进入第三轮补丁。

## 7. 2026-08-16 实际执行结果与停止点

### v1 最小选择合同

- 本地提交 `61d55c4` 将 Qwen 生产输出缩为选择包络，移除远程格式重生成，并将普通场景输出预算从 1200 调整为 1800；Python 全量 `1194/1194`、前端/浏览器 `41/41`、版本定向补测 `136/136` 通过。
- 只重载项目 Uvicorn 后，服务实际加载 observer v41 / 1800 token；controller/camera online、`busy=false`、无活动任务或会话，校准路径仍指向本工作树。卖家 `main.exe` 保持 PID `19956` 和原启动时间。
- 唯一一次 0 动作在线门会话为 `690af2f3d4e74d97a92d43dba1f2237c`，报告 `poc/output/web/generic_supervised_20260816_235050_690af2f3/report.json`。Qwen `finish_reason=stop`、输出 211 token，证明 1200 token 截断已消失；但模型仍把 `local_audited_input_1` 写成 `expected_result.element_state` 的键，本地以协议外字段拒绝。该会话 `physical_actions=0`、无 confirmation scope、无自动重试。
- 按第 5 节预设停止条件，本轮没有第二次在线采样，也没有进入物理动作。

### v2 完整选择合同

- 根因复核确认 v1 仍让 Qwen 自由生成嵌套 `expected_result`，留下最后一处控制器结构歧义。v2 将每个通用 choice 的 `expected_result` 一并由本地可信动作合同生成；Qwen 现在只返回 `status`、`choice_id`、`confidence`、`reason` 和完成证据 ID。
- 本地结果合同按通用动作语义产生：输入框聚焦使用候选 meaning + `focused=true`，认证文字输入使用候选 meaning + DeepSeek 已绑定逐字 value，swipe 使用 `content_changed=true`，系统导航及普通动作使用相应的严格可验证变化。没有 App、页面、命令、步骤或坐标分支。
- 模型若再输出 `expected_result`、action、element_id、bounds 或其他控制器字段，会在第一次输出后失败关闭；不发起远程修复，不执行硬件。
- v2 离线验证为 Qwen 专项 `91/91`、DeepSeek/观察/Qwen/编排关联 `517/517`、Python 全量 `1195/1195`；`git diff --check` 通过。前端未改变，沿用同一代码基线此前已通过的前端/浏览器 `41/41`。
- v2 尚未进行新的在线模型采样或真机动作。阶段 2 因缺少全新同一 session 至少两个动作并最终 `succeeded` 的证据，继续保持“核心完成、严格单会话真机闭环待补”；真正阶段 3 仍不得开始。
- 提交 `d2fae2d` 后已只重载项目 Uvicorn：实际监听 PID `41488`，observer v41 / 1800 token，controller/camera online、`busy=false`、无活动任务/会话；卖家 `main.exe` 仍为 PID `19956` 和原启动时间。该健康检查没有创建会话，也没有调用 DeepSeek/Qwen 或执行手机动作。

## 8. 2026-08-17 键盘模式切换单一权威修复批次

### 8.1 验收台账与根因证据

- 当前阶段合同缺口保持不变：必须在修复后的全新 `universal-agent session` 中完成至少两个真实物理动作，并最终 `status=succeeded`、`active_subgoal=null`。中英文模式切换只是阻止取得该证据的当前失败现象，不是新的阶段完成定义。
- 已观察事实：三个独立 0 动作启动目录中的普通场景观察，连续六轮把可见模式切换键写成超出完整画面 `0..1000` 合同的纵向坐标；本地解析器均在建 session 和机械动作前失败关闭。
- 已观察事实：历史合法场景中，普通场景曾把同一“英”键写成 `current_mode=chinese_pinyin`，而同轮专用输入结构审计把输入框写成 `keyboard_input_mode=direct_latin`，证明两个观察入口会对同一键盘模式作出冲突裁决。
- 已观察事实：显式模式切换目标必定运行专用全帧输入结构审计；该审计已经独立校验键盘区域、当前模式、模式键标签、方向、几何、置信度和与键盘的包含关系，并且只有本地代码能把结果重建为 `local_audited_keyboard_mode_switch_1`。
- 主要根因分类：**代码合同缺陷**。普通 compact scene 在专用输入审计之前仍被允许生成和阻断模式切换候选，造成两个权威来源；模型坐标为何漂移属于未证实推测，不作为生产修复依据。
- 必须改代码的原因：同一语义事实存在两个裁决入口，且较弱的前置入口会在较强审计运行前终止流程。继续改提示词、采样模型或放宽越界坐标都不能消除该接口冲突。

### 8.2 同类失败样本集合

1. `poc/output/web/generic_supervised_20260816_165905_78f1e14f/report.json`
   - 两轮相同越界 `[730,1130,830,1210]` 形状，`physical_actions=0`。
2. `poc/output/web/generic_supervised_20260817_031218_792edfd3/report.json`
   - 两轮分别为 `[680,1130,790,1210]`、`[730,1130,810,1210]` 形状，`physical_actions=0`。
3. `poc/output/web/generic_supervised_20260817_032047_3a760220/report.json`
   - 扩展 `keyboard_key` 假设后仍出现两轮越界，证明角色枚举不是完整根因；该假设已回滚。
4. `poc/output/web/generic_supervised_20260817_005612_52c98b81/trusted_observation_step_1.json`
   - 正向变化样本：普通场景模式键几何合法，但普通场景和输入结构审计对当前模式结论冲突。
5. `poc/output/web/generic_supervised_20260817_002641_9f024b99/trusted_observation_step_1.json`
   - 正向变化样本：合法模式键几何及另一种当前模式结论。
6. 构造负向样本
   - 模式键对象含动作字段、非模式键目标越界、输入元素越界、专用输入审计模式键越界或方向冲突时必须继续失败关闭。

### 8.3 通用修复说明

- 对显式键盘模式切换目标，compact scene 中任何“模式切换候选”都只作为非权威初步描述，在不含动作/计划/命令类字段时统一撤销；不读取、不裁剪、不换算也不复用其几何、角色、标签、模式方向或相关性。
- 随后强制运行既有专用全帧输入结构审计。只有该审计通过完整 schema、键盘包含关系、明确标签、当前/目标模式一致性和 `0..1000` 几何校验后，本地才重建唯一可执行候选。
- compact 候选含动作字段，或其他目标/输入元素自身越界时仍严格失败关闭；专用审计非法时同样失败关闭。该修复只消除重复权威，不扩大执行权限。
- 设计不依赖 App、用户措辞、键盘坐标、固定步骤或当前截图；适用于任何 App 中由当前软键盘显式提供中英文模式键的输入目标。
- 影响范围仅为 `poc/generic_scene_observer.py`、对应测试和观察器协议版本。回滚方式为撤销本批次单一提交；若历史反例被放行、专用审计不能唯一签发候选或完整回归失败，立即停止，不进入在线门。

### 8.4 验证清单

- 历史越界 compact 形状分别以 `button`、`icon`、`keyboard_key`、缺少可选状态和 `goal_relevant=false` 重放，最终只能使用专用审计的新几何。
- 合法 compact 模式键也不得越过专用审计成为最终候选，消除模式语义双权威。
- compact 模式键含动作字段、普通目标越界、input 越界继续失败关闭。
- 专用输入审计的越界模式键、标签/当前模式冲突、方向相同、普通字母键继续拒绝。
- 运行观察器最小测试、Qwen/编排关联回归、全部历史失败重放和一次 Python 完整回归。
- 前端未变时做 JavaScript 语法、纯 Node 合同和 `git diff --check`；浏览器合同仅在依赖可用时运行并如实记录。
- 离线全部通过后只进行一次 0 动作在线门；同类失败即停止，不自动重试。在线门成功后才重新核对真机前置状态并决定单次物理动作。

### 8.5 实施与离线结果

- 观察器升级为 `2026-08-17-generic-scene-observer-v43`。显式键盘模式切换目标的 compact prompt 不再要求模式键候选；即使模型仍返回无动作字段的初步候选，本地也完全撤销其角色、相关性、模式方向和几何。
- 该目标不再执行一次无权威价值的普通 targeted refinement，调用链缩为“compact 场景事实 → 专用全帧输入结构审计”。专用审计仍是唯一能够由本地重建 `local_audited_keyboard_mode_switch_1` 的入口。
- 历史越界形状已用 `button`、`icon`、`keyboard_key`、合法但语义冲突、缺少方向和 `goal_relevant=false` 变化样本重放；最终候选全部只采用专用审计的新几何。含动作字段的 compact 对象、普通目标/input 越界和专用审计越界继续失败关闭。
- 最小正反测试 `7/7`；`test_generic_scene_observer` 全模块 `157/157`；观察器、Qwen 决策、通用编排和网页状态关联回归 `559/559`。
- Python 完整回归 `1209/1209`，只有既知测试子进程 `ResourceWarning`，所有断言通过。
- JavaScript 语法检查通过；纯前端协议 `26/26`、主浏览器合同 `7/7`、阶段 2 控制台浏览器合同 `4/4`，合计 `37/37`。三文件并行启动浏览器时曾出现一次多设备用例时序失败并导致测试进程无输出；该用例隔离重跑 `1/1`，三个文件顺序重跑 `37/37`，没有为环境并发增加生产代码。
- `git diff --check` 通过。本节结果全部为离线代码与测试证据，尚未调用在线 Qwen、创建新 session 或执行机械臂动作，不能据此宣称阶段 2 完成。

### 8.6 v43 在线重放否定与接口级再收口

- 提交 `eaf8c1e` 后，项目 Uvicorn 已加载 observer v43；controller/camera online、`busy=false`、活动 task/session 为空。卖家 `main.exe` 由外部状态变为 PID `4968`、启动时间 `2026-08-17 09:52:18`，本任务没有启动、停止或重启它。
- 当前手机已离开键盘页，因此新的实时模式切换会话 `f9d4f447d64c42089345e7777a98a72c` 只读确认“验收模式选择”且以 `physical_actions=0` blocked；它没有验证键盘合同。
- 随后使用原失败目录 `generic_supervised_20260817_031218_792edfd3` 的四张真实键盘帧，绕过 DeepSeek 和机械臂做一次在线 Qwen 重放。v43 仍在第 1 次 compact 解析返回 `(0.73,1.13,0.81,1.21)`，`finish_reason=stop`、`model_calls=1`、`input_structure_audit_used=false`。
- 新事实否定了“只要按 meaning/state 识别并撤销模式键候选即可”的实现假设。实际 compact 元素没有使用该语义标记；继续扩展角色、标签或枚举属于补丁式猜测，禁止继续。
- 根因进一步收敛为：对显式键盘模式切换目标，compact elements 整体都不是动作权威，但 v43 仍需先理解模型自报语义才能撤销其中一项，导致非权威入口仍可阻断权威审计。
- 接口级修复改为：该目标的 compact 只保留 scene、system UI、相机方向和摘要事实；`elements` 中每一项先通过“标准字段集合且无任何动作/计划/命令字段”的安全筛查，全部通过后统一清空。任何协议外或动作字段使整份 scene 继续失败关闭。专用全帧输入结构审计仍是唯一元素和模式键来源。
- 变化样本新增：compact 中混入合法按钮、普通 `keyboard_key`、input、模式键别名或任意越界几何时，都不能成为候选或阻断审计；任一元素含动作字段、顶层字段异常、专用审计越界/冲突时仍拒绝。
- v43 在线失败后不再调用 Qwen。必须完成上述接口级实现、全部历史重放、相关与完整回归后，才允许最后一次相同四帧在线重放；再次失败即报告外部模型/审计阻塞，不进入真机。

### 8.7 v44 接口级实现与离线验收

- 观察器升级为 `2026-08-17-generic-scene-observer-v44`。对于显式键盘模式切换目标，compact `elements` 只接受完整标准被动场景字段集合；集合中全部元素通过无动作/计划/命令字段筛查后，整组元素统一清空，不读取、不解释也不复用其中的角色、语义、标签、状态或几何。
- 任一 compact 元素含协议外字段、动作类字段或不是标准对象时，整组元素不会被隐藏，随后仍由严格场景解析失败关闭。scene 身份、system UI、相机方向和摘要事实不受影响；专用全帧输入结构审计仍是唯一能够签发 `local_audited_keyboard_mode_switch_1` 的元素权威。
- 变化样本新增普通 input、普通按钮、普通键盘键、模式键别名和不同越界几何；正向样本证明它们不会抢先阻断专用审计，负向样本证明动作字段、协议外字段、普通非切换目标、专用审计越界/冲突和普通字母键仍失败关闭。
- 最小有效正反测试 `10/10`；`test_generic_scene_observer` 全模块 `158/158`；观察器、Qwen 视觉决策、通用编排和 Web 平台关联回归 `560/560`。
- Python 完整回归 `1210/1210`，只有既知测试子进程 `ResourceWarning`，全部断言通过。JavaScript 语法检查和纯协议 `26/26` 通过；使用 Codex 捆绑 Playwright 依赖后，主浏览器合同 `7/7`、阶段 2 控制台浏览器合同 `4/4` 通过。
- 本节仍仅证明离线合同。下一步只允许本地提交、仅重载本工作树 Uvicorn，再对同一组已保存真实键盘四帧做最后一次 0 动作在线 Qwen 重放；不得调用 DeepSeek、创建真机 session 或执行机械臂动作。

### 8.8 v44 唯一在线重放结果与停止点

- v44 已以本地提交 `ec90e37` 落盘并只重载本工作树 Uvicorn；新监听 PID `21864`，observer 确认为 v44。重载前后卖家 `main.exe` 均保持 PID `4968`、启动时间 `2026-08-17 09:52:18`，本任务没有操作它。
- 重放前 `/api/device` 显示 controller/camera online、`busy=false`、设备 `device-local-01`、活动 task/session 均为 0，校准文件仍指向本工作树。复验只使用 `generic_supervised_20260817_031218_792edfd3` 已保存的 attempt 2 四张真实键盘帧，没有获取新手机画面。
- 最后一次在线 Qwen 重放已经越过 compact scene 并进入 `_apply_input_structure_audit`，证明 v43 的“compact 越界候选先于专用审计阻断”已由 v44 消除。随后专用审计返回的 `mode_switch` 对象未精确满足 `label/bounds/confidence/current_mode/target_mode` 五字段 schema，本地在 `_validated_keyboard_mode_switch` 失败关闭。
- 本次没有调用 DeepSeek、没有创建 universal-agent session、没有 confirmation scope、没有机械臂动作或自动重试。失败后没有第二次 Qwen 采样，符合预设停止条件。
- 一次性重放进程没有持久化 Qwen 原始响应，因此当前不能证明是缺字段、额外字段还是对象形状错误；不能据猜测放宽 schema 或增加字段特例。当前唯一模型合同阻塞已收敛为：**专用输入结构审计的 `mode_switch` 输出不符合精确协议**。
- 阶段 2 仍未完成：尚无修复后全新同一 session 至少两个物理动作并最终 `status=succeeded`、`active_subgoal=null` 的证据。真正阶段 3 继续禁止开始。

## 9. 2026-08-17 输入审计非空 mode_switch schema 修复批次

### 9.1 验收台账与根因证据

- 阶段合同唯一缺口不变：修复后全新同一 universal-agent session 至少两个物理动作并最终 `status=succeeded`、`active_subgoal=null`。本批只处理阻止创建安全模式切换候选的输入审计合同缺陷。
- 第二次、带异常诊断持久化的同四帧在线重放确认：observer v44、`model_calls=2`、`targeted_refinement_used=false`、`input_structure_audit_used=true`、`finish_reason=stop`，失败阶段为 `parsing_input_structure_audit`。
- Qwen 返回可解析的 v3 顶层与 keyboard 对象，但非空 `mode_switch` 只有 `bounds/current_mode/label`，缺少严格解析器要求的 `confidence/target_mode`；这是连续第二次同类合同失败，已停止继续采样。
- 代码审查发现正式提示中的 JSON 示例只展示 `mode_switch:null`，没有展示非空对象的五字段 schema；文字虽描述方向语义，却没有定义精确对象结构。主要根因归类为 **代码接口合同缺陷**，不是截图、App、键位或坐标问题。

### 9.2 同类失败样本集合

1. v44 首次同四帧在线重放：进入专用输入审计后以“mode_switch 字段不符合协议”失败，原始形状未持久化。
2. v44 第二次诊断重放：精确记录 `mode_switch_keys=[bounds,current_mode,label]`，缺 `confidence/target_mode`；网络只尝试一次，`finish_reason=stop`。
3. 构造负向样本：同样三字段对象、额外字段对象、无效 bounds、模式相同、label 与 current_mode 冲突，均必须继续失败关闭。
4. 正向变化样本：完整五字段的 `chinese_pinyin→direct_latin` 与 `direct_latin→chinese_pinyin` 对象均由现有严格解析器验证，证明合同不依赖单一方向或 App。

### 9.3 通用修复说明

- 将输入结构审计协议升级为 v4，在正式提示中明确：`mode_switch` 只能是 `null`，或精确包含 `label/bounds/confidence/current_mode/target_mode` 的对象；给出非空对象完整 JSON 形状、置信度范围和相反模式约束。
- 不修改 `_validated_keyboard_mode_switch` 的严格字段、几何、置信度、label、方向和键盘一致性校验；不在本地补缺失字段，不把目标文字当视觉证据，也不自动猜测当前模式。
- 该修复只让模型看到解析器原本已经要求的完整通用接口，适用于任意 App 的可见软键盘；不包含 App 名称、固定命令、固定步骤、坐标或截图特例。
- 影响范围仅为输入审计协议常量、提示合同和对应测试。回滚方式为撤销单一提交；若完整回归失败或在线仍返回不合法 schema，不扩展兼容分支，直接报告模型合同阻塞。

### 9.4 验证清单

- 合同测试必须逐字证明提示同时包含 `mode_switch:null` 和完整非空五字段对象，且协议版本为 v4。
- 原三字段失败形状与额外字段继续被拒绝；两个方向的完整对象继续通过；专用审计的 bounds、置信度、label 和模式冲突测试继续通过。
- 先运行最小提示/解析正反测试，再运行观察器模块、关联回归和一次 Python 完整回归；前端未改变，只做既定语法和合同回归。
- 全部离线通过并本地提交、仅重载本工作树 Uvicorn 后，才允许一次最后的同四帧 0 动作在线重放。若仍为同类合同失败，停止；不调用 DeepSeek、不创建 session、不执行机械臂动作。

### 9.5 实施与离线结果

- 输入结构审计协议已升级为 `2026-08-17-input-structure-audit-v4`。正式提示现在同时定义 `mode_switch:null` 和中→英、英→中两种完整五字段非空对象，明确禁止省略 `confidence/target_mode`，并要求所有值来自当前画面而不是复制示例或目标。
- 严格解析器、几何门禁、置信度阈值、label/current_mode 一致性、方向相反约束和 keyboard.input_mode 交叉校验均未修改；没有新增本地补字段或模型输出容错。
- 最小正反合同 `7/7`；观察器模块 `159/159`；观察器、Qwen 决策、通用编排和 Web 平台关联回归 `561/561`。
- Python 完整回归 `1211/1211`，只有既知测试子进程 `ResourceWarning`。JavaScript 语法、前端协议 `26/26`、主浏览器 `7/7`、阶段 2 浏览器 `4/4` 全部通过。
- 本节仍为离线合同证据。下一步仅允许本地提交、只重载本工作树 Uvicorn，再做一次同四帧 0 动作在线重放；阶段 2 严格单会话证据仍未取得。

### 9.6 v4 唯一在线重放成功

- 提交 `18bc3f9` 后只重载本工作树 Uvicorn；新监听 PID `20576`。重放前 controller/camera online、`busy=false`、设备 `device-local-01`、活动 task/session 均为 0；卖家 `main.exe` 仍为 PID `4968`、启动时间 `2026-08-17 09:52:18`。
- 同一组已保存真实键盘四帧的唯一 v4 重放成功：`model_calls=2`、`targeted_refinement_used=false`、`input_structure_audit_used=true`、`finish_reason=stop`、网络尝试一次、无失败阶段。
- 专用审计生成唯一空白聚焦输入框，`keyboard_layout=qwerty`、`keyboard_input_mode=direct_latin`，并生成本地模式键 `local_audited_keyboard_mode_switch_1`：label=`英`、bounds=`[0.72,0.90,0.79,0.95]`、confidence=`1.0`、方向 `direct_latin→chinese_pinyin`。
- 这证明当前保存画面已经处于英文直输模式；目标“切换到英文直输”应以当前可见状态 0 动作完成，不能反向点击。通用编排器还会独立拒绝任何不是 `chinese_pinyin→direct_latin` 的切换动作，因此该反向候选不能取得物理权限。
- 本次没有调用 DeepSeek、没有创建 session、confirmation scope 或机械臂动作。中英文模式的 compact 抢权和非空 schema 两项代码缺陷已闭环，但阶段 2 严格同会话多动作成功证据仍未取得。

## 10. 2026-08-17 DeepSeek 动作词字面 UI 标签正规化批次

### 10.1 验收台账与根因证据

- 阶段唯一完成缺口仍是全新同一 session 至少两个物理动作并最终 `succeeded`。本批只处理阻止新 session 建立的 DeepSeek 高层图接口缺陷。
- 新目标在 0 动作 start 阶段失败：`DeepSeek 高层任务图包含低层动作表达：goal.objective`，`physical_actions=0`、session/report 均为空。规划器内部已经按现有机制做过一次完整修复请求，仍返回同类非法图，故停止继续换措辞采样。
- 本目标正向文本中唯一会被 `LOW_LEVEL_INSTRUCTION_PATTERN` 命中的内容是带引号的字面 UI 标签“语义点击”；“回到/查看”不在禁词表，其他动作词均处于明确否定约束。
- 现有 `_normalize_explicit_ui_label_payload` 只识别“目标可见文字是‘…’”形式，不能识别同义且常见的“‘…’入口/按钮/选项/控件/元素”形式。主要根因分类为 **通用字面 UI 实体接口缺陷**，不是 App、页面路径或固定命令问题。

### 10.2 同类失败样本集合

1. 当前真实目标中的“‘语义点击’入口”与 `goal.objective` 低层动作错误。
2. 既有正向样本“目标入口的可见文字是‘长按目标’”，证明带动作词的标签必须只进入 `goal.entities.target_ui_label`。
3. 构造变化样本“‘拖动项目’按钮”“‘输入文字’选项”，应执行同一实体隔离，不让标签进入高层状态文本。
4. 构造负向样本：无引号的正向动作指令继续拒绝；一次目标含多个不同的动作词字面 UI 标签时因单实体歧义失败关闭；模型返回与用户标签冲突的 `target_ui_label` 继续拒绝。

### 10.3 通用修复说明

- 在现有窄正规化入口中增加第二种明确语法：被中文/英文引号包围的 1–80 字符文字，且其后紧邻 `入口/按钮/选项/标签/控件/元素`，仅当标签自身命中低层动作词时才作为字面 UI 标签。
- 将唯一标签确定性绑定到 `goal.entities.target_ui_label`，并继续把它从 goal/subgoal objective 与 completion 状态文本替换为通用“目标入口”；原始用户目标保持不变供独立风险审计使用。
- 不放宽 `_reject_low_level_instruction`，不推导动作、不生成步骤或坐标。发现多个不同动作词标签、已有实体冲突或无引号正向动作时继续失败关闭。
- 影响范围仅为 DeepSeek 字面实体正规化与专项测试。回滚为撤销单一提交；若相关/完整回归或一次 0 动作 start 仍失败，不继续追加措辞分支。

### 10.4 验证清单

- 新句式三个变化样本正确隔离为实体；既有“可见文字是”样本继续通过。
- 多标签歧义、实体冲突、无引号动作、低层步骤和坐标负向样本继续拒绝。
- 运行 DeepSeek 最小专项、DeepSeek模块、通用编排关联和一次完整 Python 回归；前端未变但提交前核对既定合同。
- 离线全绿后本地提交并仅重载本工作树 Uvicorn；只允许同一新目标做一次 0 动作 start。得到新鲜合法 scope 后才评估一个物理动作，失败则停止。

### 10.5 实施与离线结果

- 字面 UI 标签正规化现支持两类明确语法：“可见文字/标签/名称是‘…’”以及“‘…’入口/按钮/选项/标签/控件/元素”。只有标签自身命中低层动作词且全目标唯一时才进入 `goal.entities.target_ui_label`。
- 多个不同动作词标签会以单实体歧义失败关闭；无引号动作、模型实体冲突、低层步骤和坐标门禁均未放宽。原始用户目标不变，继续供独立语义风险审计使用。
- 最小正反 `6/6`、DeepSeek 模块 `139/139`、DeepSeek/Qwen v3 合同/通用编排/Web 关联 `459/459`、Python 全量 `1213/1213` 通过；仅有既知测试子进程 `ResourceWarning`。
- JavaScript 语法、前端协议 `26/26`、主浏览器 `7/7`、阶段 2 浏览器 `4/4` 通过。本节尚未加载到运行服务，也未创建新 session 或执行动作。
