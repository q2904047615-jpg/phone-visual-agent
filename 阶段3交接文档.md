# 阶段 3 交接文档：异常重规划与陌生命令组合验收

更新时间：2026-08-16  
上游分支：`codex/complete-universal-agent`  
第二阶段交接提交：`7991d26`  
状态：第二阶段已完成，阶段 3 可直接开始。

## 1. 开始前必须读取

按顺序完整读取：

1. `项目最终目标.md`
2. `项目交接文档.md`，重点是第 17.14 与第 18 节
3. 本文档

最终方向始终是通用手机视觉操作 Agent。不得增加 App 名称分支、固定命令、固定步骤、现场截图坐标补丁或测试页专用状态机。

## 2. 阶段 3 的唯一目标

完成并验收“跨起始状态的异常重规划与陌生命令组合”：

- 页面起始状态与计划预期不同时，DeepSeek 根据新的可信观察跳过已经满足的步骤、替换失效路径或安全阻塞。
- 已生成确认范围后页面发生变化时，旧范围必须以 0 物理动作失效；重新观察后先修订任务图，再生成新 revision 下的唯一动作。
- 使用项目没有预设过的自然语言措辞，在不修改正式代码的前提下，组合现有通用动作完成一条本地离线真机任务。
- 每次确认最多一个物理动作；每次动作后重新采集 4 帧、重新观察、验证并写 typed transition receipt；失败不自动重试。

阶段 3 不包含跨三个真实 App 的最终验收，也不包含多手机调度；这些属于后续阶段。

## 3. 已完成的上游能力

- DeepSeek v3 动态任务图、Qwen3.7 Plus 唯一下一视觉动作、本地策略、机械臂单动作和动作后重观察已经接入默认 `universal_agent`。
- `controller_transition` 回执严格绑定 session/task/device/revision/subgoal/decision、confirmed/requested/rebound/resolved 动作摘要以及前后 observation/fingerprint，并且只能消费一次。
- `/next` 检测画面漂移后会先重规划；旧 confirmation scope 和旧 Qwen decision 会失效。
- `wait_for_change` 的 0 动作转换、连续合法 swipe、终态失败分类、前端一次性确认与历史不推断均已实现。
- 已晋级通用动作：`input_verified_text`、`long_press`、`drag`；`tap_semantic`、`swipe`、`back`、`home`、`dismiss_overlay`、`wait_for_change` 可用。
- `reveal_system_navigation=false`，不得把它当成已认证能力，也不得点击卖家全屏、横屏、竖屏或底栏来绕过。

## 4. 第二阶段最终证据

- 真机连续链路会话：`a4d479dcf3ae4f5a9d02a3392e8e1208`，目录 `poc/output/web/generic_supervised_20260816_054856_a4d479dc/`。它执行两次物理动作（语义点击、系统返回），最终可信画面逐字显示 `连续闭环验收通过`。旧会话因当时的命名页面身份锚点误判而标记 blocked；缺口已由 `1aaae5a` 修复，不得改写旧报告。
- 修复后的最终只读会话：`0f257a282e124c199285be4e5a6fd755`，目录 `poc/output/web/generic_supervised_20260816_055812_0f257a28/`。状态 `succeeded`、revision `2`、`physical_actions=0`。
- 最终 Python 完整回归：`1108/1108`。
- 前端协议与纯离线浏览器回归：`37/37`。

## 5. 当前运行状态

交接时 `/api/device` 已确认：

- `controller_online=true`
- `camera_online=true`
- `busy=false`
- `active_tasks=[]`
- 两类监督会话均为空
- Qwen：`qwen3.7-plus`、`thinking_enabled=false`、`error=null`
- DeepSeek：`deepseek-v4-flash`、`error=null`
- 项目 Uvicorn 父进程 PID `34604`、实际进程 PID `5648`
- 卖家 `main.exe` PID `19956`，启动时间 `2026-08-13 15:37:53`
- 手机当前停留在显示 `连续闭环验收通过` 的本地离线结果页

PID 和页面状态是交接瞬间快照，开始执行前必须重新只读核对，不得直接假定仍然有效。

## 6. 阶段 3 执行顺序

### 6.1 离线合同验收

先补齐或复核以下正反测试，只改通用合同：

1. 初始画面已经满足第一个或全部子目标时，任务图应跳过对应步骤；全部满足时以 0 动作 succeeded。
2. 旧 confirmation scope 的 observation/fingerprint/revision 任一变化时，确认请求 0 动作拒绝并消费旧范围。
3. `/next` 在画面漂移后必须先产生精确 `revision+1` 的 DeepSeek 修订，再调用 Qwen；DeepSeek 不可用或证据不足时 blocked。
4. typed receipt 不得跨 session、task、device、revision、subgoal 或 observation 重放；视觉证据与控制器动作证据继续分源。
5. 已满足步骤允许跳过；未满足但路径失效时可替换剩余子目标；不得重复已完成的一次性动作。
6. 陌生措辞只要可由现有动作组合完成，不得为这句话增加正则、关键词路由或专用任务图。

### 6.2 真机异常重规划验收

只使用本地离线验收页，不涉及账号数据：

1. 从非标准起始页创建一条新的自然语言目标，验证 DeepSeek 根据当前画面跳过已满足步骤或选择新的安全入口。
2. 生成一个 action confirmation scope 后，利用页面自身安全导航制造可见状态漂移；提交旧 scope，必须 `physical_actions=0` 且旧 scope 失效。
3. 调用 `/next` 重新观察，确认任务图先修订、旧动作不重复，并在新 revision 下给出唯一安全动作或 blocked。
4. 每次真实确认前重新检查设备在线、空闲、无其他会话；每次只确认一个动作，随后检查新 observation、fingerprint、verification 和 transition receipt。

### 6.3 陌生命令组合验收

在不修改正式代码的前提下，现场使用一条仓库测试中未出现的自然语言表达，组合至少两种已经认证的通用动作完成本地离线目标。建议包含一个页面导航动作和一个非坐标语义动作，最终用独立只读目标确认结果。若模型不能形成唯一安全路径，应记录 blocked 并修复通用合同，不得改成固定命令或固定坐标。

## 7. 阶段 3 完成标准

必须同时满足：

- 离线相关回归全绿，完整 Python 和前端/浏览器回归重新执行并记录准确计数。
- 至少一个“初始步骤已满足”的会话正确跳过，且无多余物理动作。
- 至少一个旧 scope 在真实或严格回放的状态漂移下以 0 动作失效，随后新观察触发高层修订。
- 至少一个未预设措辞的两种以上动作组合在真机上完成；每个动作都有新的前后证据和 typed receipt。
- 最终完成条件由独立只读会话确认，不能靠旧报告或动作返回值代替可见证据。
- 卖家 `main.exe` 未被启动、停止或重启；没有点击全屏、横屏、竖屏或卖家底栏。
- 更新 `项目交接文档.md`，明确区分代码测试、模型在线测试、真机动作证据和仍未覆盖的边界；提交仅保存在本地，不推送 GitHub。

## 8. 安全与协作边界

- 新任务是阶段 3 唯一允许控制真实机械臂的任务；其他任务只能做只读审计和离线测试。
- 用户当前只能控制对话，不能代为操作手机或浏览器；任务内可安全完成的页面导航、刷新和验收动作由 Agent 自己完成，不向用户反复询问。
- 不得自行启动、停止或重启卖家 `main.exe`。如果控制器窗口/摄像头离线，先穷尽只读诊断；需要用户恢复外部窗口时才报告阻塞。
- 账号影响动作继续需要单独确认；阶段 3 应只使用本地离线验收页，避免账号影响。
- 不得自动重试物理动作，不得复用旧观察、旧 confirmation scope、旧 live authority 或有损 JPEG 重建晋级凭证。
