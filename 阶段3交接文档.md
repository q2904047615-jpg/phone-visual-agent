# 阶段 3 交接文档：跨 App 陌生命令正式验收

更新时间：2026-08-16  
上游分支：`codex/stage3-replan-unseen-acceptance`
第二阶段核心能力基线：`7991d26`
阶段 3 前置加固提交：`84ba6b8`
状态：阶段 3 未开始，硬件验收暂停。第二阶段严格复跑仍未取得同一会话多动作并最终 `succeeded`；当前唯一阻塞是中文拼音软键盘的“英”模式切换键无法形成合法归一化几何证据。

## 1. 编号纠正

旧版本文把“异常重规划与本地页陌生措辞组合”写成阶段 3，这是错误编号。`项目最终目标.md` 已明确：

- 连续闭环与重规划属于第二阶段已经完成的主路径能力；
- 真正的阶段 3 是“跨 App 陌生命令验收”；
- 多手机调度属于后续阶段，不在本阶段完成范围内。

提交 `84ba6b8` 及其真机证据全部保留，统一定性为阶段 3 前置加固。它证明旧 scope 失效、异常重规划、否定约束、严格几何和本地页陌生措辞组合，但不能证明跨 App 阶段完成。

第二阶段的现有会话 `a4d479dcf3ae4f5a9d02a3392e8e1208` 虽然在同一会话完成两个动作并到达可见结果，但最终 `status=blocked`；修复后的独立 0 动作会话不能替代修复后同一会话端到端成功。阶段 3 开始前必须先用当前修复代码补齐一个全新同会话多动作 `succeeded` 证据。

最新严格复跑同样没有降低标准：`1a8b2a71ba3b4c8b842513fa3c955244` 在同一会话完成聚焦与输入两个动作但最终 `blocked`；通用回执和证据字段修复后，`80f5c585c94f4f40a13f8ac4562f5611` 的聚焦动作及 revision 推进为 `matched`，随后因真实键盘仍是 `chinese_pinyin` 而停止。独立模式切换会话 `78f1e14fa7b6458991dfa7dd5a6c3c69` 在 0 动作观察阶段因切换键 bounds 超出归一化画面而失败关闭。详细证据见 `项目交接文档.md` 第 21 节。

## 2. 开始前必须读取

按顺序完整读取：

1. `项目最终目标.md`
2. `项目交接文档.md`，重点是第 18、19、20 节
3. 本文档

最终方向始终是通用手机视觉操作 Agent。不得增加 App 名称分支、固定命令、固定步骤、截图坐标补丁、单 App 状态机或针对验收现场的关键词路由。

## 3. 阶段 3 的唯一目标

在至少三个真实且不同的手机 App 中，现场输入仓库未预编排过的自然语言目标，通过同一条默认通用链路完成真实操作与验证：

```text
自然语言目标
→ DeepSeek 动态任务图
→ Qwen 基于真实画面的唯一下一动作
→ 本地门禁和确认前新观察
→ 机械臂一次一个动作
→ 动作后 4 帧重观察与 typed receipt
→ 推进、动态调整、风险确认或完成
```

同一浏览器里的多个页面、离线截图回放或单元测试不能冒充多个 App。每个 App 都必须有自己的真实前后画面和至少一个真实物理动作证据。

## 4. 必须覆盖的能力

1. 至少三个不同 App，各执行一条此前未预编排的自然语言目标。
2. 至少一条目标组合两种以上已认证通用动作。
3. 至少一条目标换一种说法后，仍形成等价高层目标并安全完成或在已经可见完成时以 0 动作结束。
4. 至少覆盖不同起始页、弹层或路径变化之一；系统必须依据新画面调整，不能坚持固定序列。
5. 文字输入、导航和选择进入同一通用闭环，并保留每步确认范围、前后 observation/fingerprint、动作后 4 帧和 typed receipt。
6. 发送、提交、删除、关注、评论、下单或其他账号/外部影响动作只能准备草稿并停在风险确认前；不得自行确认或执行。
7. 任一 App 因登录、权限、联网、弹层或外部状态无法安全继续时，记录 0 动作 blocked 并更换安全 App，但三个 App 的成功标准不得降低。

## 5. 真机执行纪律

- 本任务是阶段 3 唯一允许控制真实机械臂的任务。
- 每次真机动作前重新核对 `/api/device`、controller、camera、busy、活动任务、活动会话、选中 `device_id`、当前 observation/fingerprint 和卖家 `main.exe` PID/启动时间。
- 一次确认只执行一个物理动作；动作后必须重新采集 4 帧、观察、验证并写 typed receipt。
- 物理动作失败不自动重试，不复用旧 scope、旧观察、旧 live authority 或旧动作摘要。
- 不得启动、停止或重启卖家 `main.exe`；不得点击卖家全屏、横屏、竖屏或底栏。
- 用户无法代为操作手机或浏览器；任务范围内的安全本地操作由本任务自主完成。

## 6. 推荐验收选择原则

优先选择不涉及账号或远端状态、结果可直接从当前画面确认的系统或本地 App，例如计算、系统设置只读查询、本地文件浏览、时钟或其他本地工具。这里的 App 仅是现场样本，不得写入正式业务编排。

每条验收目标都应：

- 从当前真实状态出发，不预置固定起始页；
- 使用可见语义而非坐标描述目标；
- 具有明确、可独立观察的完成条件；
- 避免发送、保存、删除、授权、交易或账号关系变化；
- 若需要文字输入，只输入本地查询或草稿内容，不执行外部提交。

## 7. 执行顺序

1. 修正文档编号与结论，记录阶段 1 完成、阶段 2 核心完成但严格单会话真机闭环待补、`84ba6b8` 只是阶段 3 前置加固。
2. 运行完整离线回归并确认默认通用合同无回退。
3. 先在本地安全页面补齐第二阶段：全新自然语言目标、同一 universal-agent session 至少两个物理动作、每步完整回执与 revision 推进、最终同会话 `succeeded`。
4. 只有第二阶段严格证据通过后，才只读发现手机当前状态与可安全使用的真实 App，并开始真正阶段 3。
5. 逐 App 执行：每次先 0 动作规划，再逐个确认物理动作并在每步后验证。
6. 对其中一条目标使用不同措辞做等价目标验证。
7. 完成三个 App 后，新建独立只读会话分别核对最终可见结果或读取各自最终报告证据。
8. 更新 `项目交接文档.md`，再次运行完整回归，创建仅本地提交，不推送 GitHub。

## 8. 阶段 3 完成标准

必须同时满足：

- 完整 Python、前端协议和浏览器合同回归全绿并记录准确计数。
- 三个不同真实 App 各至少有一条陌生命令成功证据；每个 App 至少一个真实物理动作。
- 每个物理动作都有确认前新观察、动作后 4 帧、前后 observation/fingerprint、验证结果和 typed receipt，且无自动重试。
- 至少一条目标组合两种以上已认证动作。
- 同一目标换一种说法仍形成等价高层目标并完成，或以当前可见证据 0 动作确认已经完成。
- 不同起始页、弹层或路径变化得到动态调整证据。
- 文字输入、导航和选择至少各有一项进入默认通用闭环；外部影响动作没有被擅自执行。
- 卖家 `main.exe` 未被启动、停止或重启；全屏、横屏、竖屏和卖家底栏未被点击。
- `项目交接文档.md` 明确区分离线测试、在线模型、真机动作、0 动作阻塞和仍未覆盖的多手机边界。
- 变更仅本地提交，不推送。

只有以上标准全部满足，才能声明真正阶段 3 完成。否则必须明确写出“阶段 3 未完成”和唯一阻塞，不得用前置加固或同一浏览器内页面替代。
