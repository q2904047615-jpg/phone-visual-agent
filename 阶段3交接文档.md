# 阶段 3 交接文档：跨 App 陌生命令正式验收

更新时间：2026-08-18
当前集成分支：`codex/integrate-phase2-stage3`
证据来源分支：`codex/stage3-replan-unseen-acceptance`
第二阶段严格真机证据：session `18c29c4db30246d1a3ffaa8f5116f047`
本地输入准备合同提交：`4b1af7e`
状态：阶段 2 严格单会话真机闭环已经完成并进入独立集成分支。阶段 3 已取得相机、系统设置、Browser 三个真实 App 的成功会话，但仍缺 matched `input_verified_text` typed receipt 和其后的最终独立 0 动作复核，因此阶段 3 未完成。最后一个 DeepSeek 阻塞已经完成通用离线修复和历史脱敏图重放，尚未加载服务或在线复验。

## 1. 编号纠正

旧版本文把“异常重规划与本地页陌生措辞组合”写成阶段 3，这是错误编号。`项目最终目标.md` 已明确：

- 连续闭环与重规划属于第二阶段已经完成的主路径能力；
- 真正的阶段 3 是“跨 App 陌生命令验收”；
- 多手机调度属于后续阶段，不在本阶段完成范围内。

提交 `84ba6b8` 及其真机证据全部保留，统一定性为阶段 3 前置加固。它证明旧 scope 失效、异常重规划、否定约束、严格几何和本地页陌生措辞组合，但不能证明跨 App 阶段完成。

第二阶段的现有会话 `a4d479dcf3ae4f5a9d02a3392e8e1208` 虽然在同一会话完成两个动作并到达可见结果，但最终 `status=blocked`；修复后的独立 0 动作会话不能替代修复后同一会话端到端成功。阶段 3 开始前必须先用当前修复代码补齐一个全新同会话多动作 `succeeded` 证据。

最新严格复跑同样没有降低标准：`1a8b2a71ba3b4c8b842513fa3c955244` 在同一会话完成聚焦与输入两个动作但最终 `blocked`；通用回执和证据字段修复后，`80f5c585c94f4f40a13f8ac4562f5611` 的聚焦动作及 revision 推进为 `matched`，随后因真实键盘仍是 `chinese_pinyin` 而停止。独立模式切换会话 `78f1e14fa7b6458991dfa7dd5a6c3c69` 在 0 动作观察阶段因切换键 bounds 超出归一化画面而失败关闭。详细证据见 `项目交接文档.md` 第 21 节。

后续通用输入边框、语义别名、键盘隐藏动作选择和子目标审计隔离已通过完整回归；同会话 `07322badf6c9488683c17a858ca63dbd` 已取得聚焦与输入两个 `matched` 动作，但隐藏键盘动作在旧合同下被拒绝，最终仍为 `blocked`。最新全新会话 `d2445b84a60e4191bdc0d2259c7e78cf` 的唯一刷新动作真实 `matched`，typed receipt 为 `receipt_a939b6a67a84467880ce25aecea98647`，动作后输入为空且键盘隐藏；DeepSeek 随后因上述命名页面误判导致 `blocked_replan_failure`，没有重试。完整证据和当前停止点见 `项目交接文档.md` 第 22 节。

具名页面误判随后按“无名称视觉容器状态不构成页面身份”的通用边界修复，Python `1180/1180`、前端与浏览器合同 `41/41` 通过。全新会话 `051425f229c2421abfeaf65699986bb9` 的唯一聚焦动作真实 `matched`：前后各 4 帧，observation 从 `obs_2286a6cf732d4932b81538b651fec26e` 变为 `obs_26b93ee180154f0ea22f92b52afaa0eb`，typed receipt 为 `receipt_cf7191e7910d4bbba9a8d0c05edda45f`；动作后画面证明空输入框已聚焦、QWERTY 且为 `direct_latin`。但 DeepSeek 随后为 `ensure_input_visible` 使用当前 observation/typed controller ref 之外的完成证据，会话以 `blocked_replan_failure` 关闭，只有 1 个物理动作且没有重试。详细证据见 `项目交接文档.md` 第 23 节。

## 2. 开始前必须读取

按顺序完整读取：

1. `项目最终目标.md`
2. `项目交接文档.md`，重点是第 18、19、20 节
3. 本文档

最终方向始终是通用手机视觉操作 Agent。不得增加 App 名称分支、固定命令、固定步骤、截图坐标补丁、单 App 状态机或针对验收现场的关键词路由。

## 3. 阶段 3 的唯一目标

在至少三个真实且不同的手机 App 中，现场输入仓库未预编排过的自然语言目标，通过同一条默认通用链路完成真实操作与验证：

```text
自然语言目标
→ DeepSeek 动态任务图
→ Qwen 基于真实画面的唯一下一动作
→ 本地门禁和确认前新观察
→ 机械臂一次一个动作
→ 动作后 4 帧重观察与 typed receipt
→ 推进、动态调整、风险确认或完成
```

同一浏览器里的多个页面、离线截图回放或单元测试不能冒充多个 App。每个 App 都必须有自己的真实前后画面和至少一个真实物理动作证据。

## 4. 必须覆盖的能力

1. 至少三个不同 App，各执行一条此前未预编排的自然语言目标。
2. 至少一条目标组合两种以上已认证通用动作。
3. 至少一条目标换一种说法后，仍形成等价高层目标并安全完成或在已经可见完成时以 0 动作结束。
4. 至少覆盖不同起始页、弹层或路径变化之一；系统必须依据新画面调整，不能坚持固定序列。
5. 文字输入、导航和选择进入同一通用闭环，并保留每步确认范围、前后 observation/fingerprint、动作后 4 帧和 typed receipt。
6. 发送、提交、删除、关注、评论、下单或其他账号/外部影响动作只能准备草稿并停在风险确认前；不得自行确认或执行。
7. 任一 App 因登录、权限、联网、弹层或外部状态无法安全继续时，记录 0 动作 blocked 并更换安全 App，但三个 App 的成功标准不得降低。

## 5. 真机执行纪律

- 本任务是阶段 3 唯一允许控制真实机械臂的任务。
- 每次真机动作前重新核对 `/api/device`、controller、camera、busy、活动任务、活动会话、选中 `device_id`、当前 observation/fingerprint 和卖家 `main.exe` PID/启动时间。
- 一次确认只执行一个物理动作；动作后必须重新采集 4 帧、观察、验证并写 typed receipt。
- 物理动作失败不自动重试，不复用旧 scope、旧观察、旧 live authority 或旧动作摘要。
- 不得启动、停止或重启卖家 `main.exe`；不得点击卖家全屏、横屏、竖屏或底栏。
- 用户无法代为操作手机或浏览器；任务范围内的安全本地操作由本任务自主完成。

## 6. 推荐验收选择原则

优先选择不涉及账号或远端状态、结果可直接从当前画面确认的系统或本地 App，例如计算、系统设置只读查询、本地文件浏览、时钟或其他本地工具。这里的 App 仅是现场样本，不得写入正式业务编排。

每条验收目标都应：

- 从当前真实状态出发，不预置固定起始页；
- 使用可见语义而非坐标描述目标；
- 具有明确、可独立观察的完成条件；
- 避免发送、保存、删除、授权、交易或账号关系变化；
- 若需要文字输入，只输入本地查询或草稿内容，不执行外部提交。

## 7. 执行顺序

1. 阶段 2 严格证据已经由 session `18c29c4db30246d1a3ffaa8f5116f047` 完成，不复用该 session 作为阶段 3 证据。
2. 从当前 `launcher/home_screen` 做只读发现，选择至少三个安全、真实且不同的 App；每个 App 使用仓库未预编排的自然语言目标。
3. 逐 App 执行：每次先 0 动作规划，再逐个确认物理动作并在每步后验证。
4. 三个 App 都使用多步目标并在同一会话中完成至少两个已认证动作；对其中一条目标从独立安全起始状态使用不同措辞做等价目标验证。
5. 完成三个 App 后，新建独立只读会话分别核对最终可见结果或读取各自最终报告证据。
6. 更新 `项目交接文档.md`，再次运行完整回归，创建仅本地提交，不推送 GitHub。

## 8. 阶段 3 完成标准

必须同时满足：

- 完整 Python、前端协议和浏览器合同回归全绿并记录准确计数。
- 三个不同真实 App 各至少有一条陌生命令成功证据；每个 App 的同一成功会话至少组合两个已认证真实物理动作。
- 每个物理动作都有确认前新观察、动作后 4 帧、前后 observation/fingerprint、验证结果和 typed receipt，且无自动重试。
- 同一目标从独立安全起始状态换一种说法，仍形成等价高层目标并完成；单纯复读旧报告不能替代。
- 不同起始页、弹层或路径变化得到动态调整证据。
- 文字输入、导航和选择至少各有一项进入默认通用闭环；外部影响动作没有被擅自执行。
- 卖家 `main.exe` 未被启动、停止或重启；全屏、横屏、竖屏和卖家底栏未被点击。
- `项目交接文档.md` 明确区分离线测试、在线模型、真机动作、0 动作阻塞和仍未覆盖的多手机边界。
- 变更仅本地提交，不推送。

只有以上标准全部满足，才能声明真正阶段 3 完成。否则必须明确写出“阶段 3 未完成”和唯一阻塞，不得用前置加固或同一浏览器内页面替代。

## 9. 2026-08-16 最新第二阶段停止点

- 观察器已升级到 v40：只有输入结构审计明确 `keyboard.visible=false` 才会生成“软键盘不可见”的控制器证据；空 overlays 本身不能证明缺席。Qwen 为系统消失动作返回的精确 `system_ui.soft_keyboard_visible=false` 只在 `back/home/dismiss_overlay` 中降级为可独立验证的 `scene_changed=true`，普通点击、true 值和额外字段继续失败关闭。
- 完整离线回归为 Python `1189/1189`、关联 `375/375`、前端/浏览器 `41/41`，JS 语法和 `git diff --check` 通过；没有 App 分支、固定命令、固定步骤、验收页状态机或坐标补丁。
- 会话 `e0121503813d4a8caa4d5af0c363e28d` 在同一会话完成聚焦空输入框和认证 `back` 两个真实动作，两个 typed receipt 均为 matched，revision `1→2→3`，每步前后各 4 帧且 observation/fingerprint 更新；但 Qwen 在键盘已真实隐藏后仍误把浏览器底栏当成疑似键盘，继续提出第三动作。为避免离开页面，会话安全取消，最终不是 `succeeded`。
- 后续新会话在 0 动作阶段重复暴露同一在线模型阻塞：隐藏键盘子目标的本地动作集合已经收窄为唯一 `back`，Qwen 两轮仍提出 `tap_semantic`；独立准备请求又先把 `keyboard_layout` 写入 container，随后返回不可解析 JSON。均无物理动作。
- 因此阶段 2 仍为“核心完成、严格单会话真机闭环待补”，阶段 3 不得开始。下一次只允许从全新 0 动作观察验证 Qwen 同时遵守 scene schema 和唯一 `back` 动作集合；在此之前禁止继续采样、放宽白名单、固定坐标、复用旧 scope 或用上述两动作 cancelled 会话冒充成功。

## 10. 2026-08-16 阶段 2 最新停止点：visual selection v2 待在线门

- Qwen 输出截断已经通过最小 choice 选择合同、JSON object 模式和 observer v41 / 1800 token 消除。唯一新 0 动作会话 `690af2f3d4e74d97a92d43dba1f2237c` 的 Qwen `finish_reason=stop`、输出 211 token，但 v1 仍允许模型生成嵌套 `expected_result`，模型把输入 element ID 放错层，本地在动作前拒绝；`physical_actions=0`、无 scope、无重试。
- visual selection v2 已在离线代码中彻底移除模型输出的 `expected_result`：Qwen 只选择本地生成的通用 choice，动作语义、候选、几何和可验证结果均由本地可信合同水合。没有 App 名称、固定命令、固定步骤、验收页状态机或坐标补丁。
- v2 已通过 Qwen 专项 `91/91`、关联 `517/517`、Python 全量 `1195/1195`；前端未变且本轮已通过 `41/41`，`git diff --check` 通过。按既定止损规则，本轮不进行第二次在线模型采样或真机动作。
- 阶段 2 仍未完成，阶段 3 仍不得开始。下一次必须先让 v2 通过一次全新 0 动作在线门；只有得到合法且新鲜的 confirmation scope，才可从新 session 执行同会话至少两个物理动作并要求最终 `succeeded`。

## 11. 2026-08-17 阶段 2 最新停止点：在线门已通过，真机仍未成功终态

- visual selection v2 已能生成合法新 scope，旧的“待在线门”结论已过时；但这不等于阶段 2 完成。会话 `9ea2c9fb373f46ea814cb74c1ad7edcc` 的页面内语义点击使用新 observation/fingerprint、三帧 OCR 几何、确认前 4 帧和动作后 4 帧，只执行一次且未重试；页面未导航，最终 `blocked`、`physical_actions=1`。
- 更早的 `547282de17f84976892c7b2d6cf170c5` 虽在同一 session 记录两个真实动作与 typed receipt，第二步未产生目标转移且最终仍为 `blocked`。因此仍没有任何修复后全新同一 session 同时满足“至少两个物理动作”和“最终 succeeded”。
- 两条 0 动作替代路径没有被强行执行：键盘收起目标连续只给被约束排除的 `back`；刷新目标未形成独立审计签发的刷新候选。最终键盘模式切换观察连续把目标 bounds 写为 `y=1.13..1.21`，两轮都在建 session 前失败关闭。
- 一个仅将越界 `keyboard_key` 撤销并转交专用输入审计的假设修复 `25ef65c` 虽通过 `392/392` 正反回归，但新 0 动作在线门 `poc/output/web/generic_supervised_20260817_032047_3a760220/` 仍出现同类越界；该假设已由 `3f6c479` 完整 revert，没有扩大容错或触发真机动作。
- 当前唯一阻塞：真实画面为中文拼音 QWERTY 键盘，文字输入安全合同要求先取得 `direct_latin`，但模式切换键缺少合法归一化几何。不得将越界数值当源截图像素换算，不得增加固定坐标或专用键盘流程，也不得用页面点击失败、刷新缺证据或旧 session 拼接成功。
- 最新 Python 全量 `1207/1207`、关联 `462/462` 通过；前端本轮无修改，两个浏览器合同因本机缺少 `playwright` 模块未加载，不能虚报通过。最终 controller/camera online、`busy=false`、无活动 task/session；项目 Uvicorn PID `25596`，卖家 `main.exe` 仍为 PID `19956` 且原启动时间未变。阶段 2 仍未完成，阶段 3 仍不得开始。

## 12. 2026-08-17 阶段 2 模式切换合同离线收敛

- 连续越界已按“compact scene 与专用输入结构审计重复拥有模式键裁决权”的通用代码合同缺陷处理。observer v43 撤销 compact 候选的全部模式键权限，跳过冗余普通目标精查，只有专用全帧输入结构审计能够签发本地模式切换候选。
- 生产权限没有放宽：动作字段、普通目标/input 越界、专用审计越界、方向/标签冲突仍失败关闭；没有 App、命令、固定步骤或坐标补丁。
- 离线验证为最小 `7/7`、观察器 `157/157`、关联 `559/559`、Python 全量 `1209/1209`、前端与浏览器合同 `37/37`，JavaScript 语法和 `git diff --check` 通过。
- 当前修复尚未经过新 0 动作在线门或任何真机动作，不能宣称当前阻塞已经在在线 Qwen 上解除。阶段 2 与阶段 3 状态均不变；下一步只能在本地提交并只重载项目 Uvicorn后，执行一次 0 动作在线门。

## 13. 2026-08-17 阶段 2 模式切换接口 v44 待在线复验

- v43 的真实键盘四帧在线重放证明：按 compact 元素自报语义筛出模式键仍会漏判，不能继续堆叠角色、标签或坐标兼容。v44 已改为显式模式切换目标下 compact 元素整体无权威，只有标准被动对象才可整组撤销；协议外/动作字段仍严格失败关闭。
- 专用全帧输入结构审计仍是唯一模式键签发入口，没有降低几何、方向、模式和标签门禁，也没有加入 App、命令、步骤、坐标或截图分支。
- v44 离线结果为最小 `10/10`、观察器 `158/158`、关联 `560/560`、Python 全量 `1210/1210`、前端协议 `26/26`、主浏览器 `7/7`、阶段 2 浏览器 `4/4`，JavaScript 语法通过。
- 阶段状态不变：阶段 2 尚缺修复后同一新 session 至少两个物理动作并最终 `succeeded` 的严格证据；阶段 3 不能开始。下一步仅允许最后一次同四帧 0 动作在线 Qwen 重放，成功后才重新评估真机入口。

## 14. 2026-08-17 阶段 2 v44 在线停止点

- `ec90e37` 的 v44 已加载到本工作树 Uvicorn。唯一一次已保存真实键盘四帧在线重放成功进入专用输入结构审计，说明 compact 越界元素不再抢先阻断权威审计。
- Qwen 专用审计输出的 `mode_switch` 未精确符合五字段 schema，本地在动作前失败关闭；没有 DeepSeek、session、confirmation scope、机械臂动作或自动重试。由于一次性进程未保留原始响应，不能根据未证实字段差异继续修改协议。
- 当前唯一阻塞为专用输入结构审计 `mode_switch` 合同输出不合法。阶段 2 仍未取得严格单会话成功证据，阶段 3 不得开始。

## 15. 2026-08-17 阶段 2 输入审计 v4 待在线复验

- 连续两次同类失败已定位为提示接口遗漏：解析器要求非空 `mode_switch` 五字段，但 v3 JSON 示例只有 `null`。v4 已补全 nullable 与双向五字段正式形状，严格解析器和安全门禁未放宽。
- v4 离线结果为最小 `7/7`、观察器 `159/159`、关联 `561/561`、Python `1211/1211`、前端协议 `26/26`、浏览器 `7/7+4/4`，JavaScript 语法通过。
- 尚未在线复验，阶段 2 仍未完成且阶段 3 不得开始。下一步只能提交并加载 v4，然后做一次同四帧 0 动作 Qwen 重放。

## 16. 2026-08-17 阶段 2 输入审计 v4 在线通过

- `18bc3f9` 加载后的唯一同四帧在线重放成功进入专用审计并产生完整本地模式键，Qwen `finish_reason=stop`，无硬件动作。当前保存画面被严格识别为 `direct_latin`，模式键方向为 `direct_latin→chinese_pinyin`。
- “切到英文”在该画面上已经满足，不能反向点击；本地策略也只允许 `chinese_pinyin→direct_latin` 的安全文字输入前置切换。本次没有 DeepSeek、session 或 scope。
- 模式观察问题已闭环，但阶段 2 严格同会话多动作成功证据仍缺失，阶段 3 不得开始。

## 17. 2026-08-17 阶段 2 DeepSeek 字面标签待在线门

- 当前错误页上的新多动作目标在 0 动作 start 阶段因 DeepSeek 把字面 UI 标签“语义点击”留在 `goal.objective` 而失败；无 session、Qwen 或硬件动作。
- 通用正规化已扩展为唯一的“‘动作词标签’入口/按钮/选项/控件/元素”语法；多标签歧义和全部低层动作门禁保持失败关闭。
- 离线结果为最小 `6/6`、DeepSeek `139/139`、关联 `459/459`、Python `1213/1213`、前端 `26/26+7/7+4/4`。尚未在线复验，阶段 2/3 状态不变。

## 18. 2026-08-17 阶段 2 活动子目标标签上下文待在线门

- 新 session `d6c3d10c056548a192db25816d88dcdc` 的首个真实 `back` 已 matched，并有前后 4 帧、新 observation/fingerprint、typed receipt 和 revision `1→2`；手机回到“验收模式选择”。
- 第二子目标中 Qwen 逐字选择唯一“语义点击”按钮，但活动子目标标签没有进入场景相关性正规化，元素保留 `goal_relevant=false`，本地策略拒绝。会话以 `blocked`、`physical_actions=1` 停止，无第二动作或自动重试。
- 通用修复已统一活动子目标与根层 `target_ui_label` 的只读解析；仅一致且唯一时绑定，冲突/重复/缺失继续失败关闭。离线结果为最小 `5/5`、观察器 `161/161`、关联 `563/563`、Python `1215/1215`、前端 `26/26+7/7+4/4`。
- 修复尚未加载运行服务。阶段 2 仍未完成，阶段 3 不得开始；下一步仅允许提交、重载本工作树 Uvicorn，然后从当前列表页建立全新两动作 session 做一次严格验收。

## 19. 2026-08-17 阶段 2 严格证据完成，阶段 3 解锁

- 通用修复 `e35ec90` 加载后，最终 session `18c29c4db30246d1a3ffaa8f5116f047` 使用全新自然语言目标，从真实手机桌面短暂查看系统设置首页标题并回到桌面。
- 同一 session 动作 1 为唯一“设置”图标的 `tap_semantic`，receipt `receipt_616ff09bb3b54fa3959ac3fa116a1741` matched、revision `1→2`；动作 2 为无坐标 Android `home`，receipt `receipt_28acaea0723f4d57b103cbbd2a526ece` matched、revision `2→3`。两步均有前后各 4 帧及不同 observation/fingerprint。
- 最终 `status=succeeded`、`physical_actions=2`、任务图 `completed`、`active_subgoal=null`、`current_subgoal=null`，无旧 scope 复用或自动重试。报告为 `poc/output/web/generic_supervised_20260817_111820_18c29c4d/report.json`。
- 独立 Qwen 四帧只读复核确认最终 `launcher/home_screen` 稳定，`executed=false`、`physical_action_requested=false`、fingerprint `a569401a52909695aae4`。阶段 2 因此完成；阶段 3 现在解锁，但必须另取三个不同真实 App 的全新证据，不能把本 session 直接计入阶段 3。

## 20. 2026-08-17 阶段 3 跨 App 可见状态合同修复台账

- **当前唯一验收缺口**：阶段 3 仍缺另外两个真实 App 的成功会话，以及文字输入、导航、选择同闭环证据；相机会话 `95844a8cd0a8466ebd166793c6cfb29d` 已成功，不能用 0 动作失败或阶段 2 设置会话替代剩余 App。
- **已观察事实与根因分类**：浏览器会话 `49a7f58f7c3a423a8ec5daa75f403e21` 和手机管家会话 `968ae59f863843748e9214aad441a930` 均在 `launcher/home_screen`、`physical_actions=0` 时，仅因桌面存在具名 App 入口图标就把“App 界面/首页可见”推进为 completed；设置样本 `4ff8fcc3f6634d79a6791a492ff2933e` 也在 launcher 上触发同一路径后被更下游的页面身份门禁拒绝。主要类别为通用代码合同缺陷，不是 App 路径、坐标或单次模型措辞。
- **同类失败样本集合**：浏览器入口图标不能证明浏览器应用界面；手机管家入口图标不能证明手机管家首页；设置入口图标不能证明设置主界面。变化样本为目标 App 已真实处于前台时，结构化 `foreground_app_id` 与目标 App 一致，应仍允许页面可见状态推进；相机目标中的“手机主桌面可见”不指向目标 App 页面，也必须保持可推进。
- **通用修复说明**：只为“子目标文字明确要求任务图目标 App 的页面、界面、首页或已打开状态”增加结构化前台 App 一致性门禁；从任务图动态读取目标 App，不写浏览器、手机管家、设置、命令、步骤或坐标分支。影响面仅为 0 动作 visible-presence 自动推进；失败关闭为不推进并交回 Qwen 选择下一动作。回滚方式为撤销该单一门禁及对应测试。
- **验证清单**：先新增浏览器/手机管家/设置三个 launcher 反例，再增加目标 App 已在前台和“主桌面可见”正例；只运行可见状态推进最小测试，随后重放相关 orchestrator/DeepSeek/observer 测试，批次稳定后运行一次完整回归。离线全绿且重载本工作树 Uvicorn 后，才恢复一个新的手机管家真机会话；不得把真机当调试器。
- **停止条件**：若两个不同 launcher 反例仍能推进，或正例被误阻断，撤销本批修改并重新分类；若修复后的首个新真机会话仍出现同一根因，不继续采样或扩散补丁，明确记录阶段 3 未完成。
- **离线结果**：最小正反样本 `4/4`、orchestrator `145/145`、DeepSeek/observer/Qwen `395/395`、最终 Python 全量 `1219/1219` 通过；前端协议 `26/26`、主浏览器 `7/7`、阶段 2 浏览器 `4/4`、触控合同 `4/4` 与 JavaScript 语法通过。实现只读取动态任务图目标 App 和结构化 `foreground_app_id`，没有新增 App 名称分支、固定命令、固定步骤或坐标。
- **加载状态**：修复已加载到本工作树 Uvicorn；实际监听 PID `19552`，controller/camera online、`busy=false`，卖家 `main.exe` 保持 PID `4968` 与原启动时间，没有被启动、停止或重启。
- **相机成功证据**：session `95844a8cd0a8466ebd166793c6cfb29d` 在真实相机 App 中依次执行 `tap_semantic` 打开取景页与 Android `home` 返回桌面。两个动作均 matched、前后各 4 帧并生成 typed receipt `receipt_9022d5a02eb4464e91e6fdf6e4f391b4`、`receipt_d461b6949ebb48e5be1a7f56627ed916`；最终 revision `6`、`status=succeeded`、`physical_actions=2`、任务图 completed、`active_subgoal=null`。报告为 `poc/output/web/generic_supervised_20260817_112801_95844a8c/report.json`，可计为阶段 3 的第一个 App，并已覆盖两种通用动作。
- **修复后手机管家证据**：新 session `14df6f5b83f643ca8084af03b1fc6c6d` 的 0 动作在线门正确保持 `open_phone_manager` active，并由 Qwen 提出唯一手机管家入口点击，证明 launcher 误完成根因已在线关闭。该点击一次 matched、前后各 4 帧，新画面真实进入 `phone_manager/permission_dialog`；因权限与隐私确认弹层，按目标停止且不触碰同意/不同意，最终 blocked，不能计入三个成功 App。报告为 `poc/output/web/generic_supervised_20260817_114849_14df6f5b/report.json`。
- **设置组合阻塞**：从手机管家弹层开始的两种等价设置/本地草稿目标均为 `physical_actions=0`。第一种在 DeepSeek 高层门禁因低层动作表达拒绝；第二种通过建图后，Qwen 对同一真实弹层连续两轮返回越界元素 bounds `(0.09,1.75,0.49,1.89)` 与 `(0.12,1.75,0.48,1.88)`，本地严格失败关闭。证据目录为 `poc/output/web/generic_supervised_20260817_115158_e0e6d953/`。
- **当前唯一阻塞与停止点**：手机仍停在手机管家权限/隐私弹层。该画面已连续两次触发同类 Qwen bounds 合同失败，按止损规则不得继续调用模型；项目没有不调用模型且仍生成完整 typed receipt 的恢复入口，直接调用机械臂 Home 会绕过阶段合同，因此未执行。阶段 3 当前只能标记为未完成；恢复需要用户/外部条件先让手机主桌面可见，或另行批准并实现一个不绕过证据门的通用模型失败恢复合同，之后仍需两个不同 App 成功、文字输入/导航/选择同闭环及最终独立 0 动作复核。

## 21. 2026-08-17 阶段 3 否定约束候选过滤验收台账

- **当前唯一验收缺口**：阶段 3 仍缺两个真实 App 成功，其中一个必须把导航、选择和文字输入接入同一默认通用闭环。新设置组合会话 `1caac2463ee44c39a6b3dee175227ba2` 在 Launcher 上 0 物理动作阻塞，尚未缩小该阶段缺口。
- **已观察事实与根因分类**：可信四帧观察唯一识别 `label=设置`、`role=icon`、`goal_relevant=true` 的桌面入口，设备能力也包含 `tap_semantic`；但本地 `_selection_choices` 离线重建只产生 `back/home/swipe/wait_for_change`，没有任何元素点击 choice。根因是共享否定约束过滤把安全边界“不得改变任何设置值”中的“设置”二字与 App 入口标签“设置”做二元词交集，错误地把状态变化禁令当成了 UI 目标禁令。主要类别为跨 App 通用代码合同缺陷，不是 Qwen 随机输出、坐标、设置 App 路径或当前截图。
- **同类失败样本集合**：真实样本为“设置入口”遇到“不得改变任何设置值”；构造变化样本为“计算器入口”遇到“不得保存任何计算记录”，两者都应保留安全导航候选。反向样本为“不得打开任何搜索结果”必须排除搜索结果候选、“不要再次使用某入口”必须排除该入口、“禁止通过任何页面正文元素跳转”必须继续排除页面元素。
- **通用修复说明**：候选级否定过滤只处理明确指向 UI 目标的禁止动作（例如点击、触碰、打开、进入、选择、切换、操作、使用、访问、启动、按下、长按、滑动、拖动）或既有的全称页面元素范围；保存、提交、发送、安装、发布、删除、改变状态等结果/外部影响约束继续由任务风险和动作控制器裁决，不再仅凭与候选共享一个二元词就删除安全导航入口。实现不读取 App 名称、命令、步骤或坐标。影响面仅为 Qwen 候选投影和本地策略共用的否定目标过滤；回滚方式为撤销该谓词边界与对应测试。
- **验证清单**：先增加上述真实样本、跨 App 变化样本和三个反向样本；再重放 `1caac246` 的落盘任务图与观察，要求重新出现绑定 `settings_icon` 的 `tap_semantic` choice；随后只运行约束过滤、Qwen 选择和通用策略最小回归，批次稳定后运行一次完整回归。离线全绿后才允许提交、只重载本工作树 Uvicorn，并从全新 observation/session 做一次在线 0 动作门。
- **停止条件**：若状态变化约束仍删除安全入口，或明确禁止的搜索结果/具名入口/页面元素重新进入候选，则撤销本批修改；若首个新在线门仍因同一候选过滤根因阻塞，不进行第二次模型采样或真机动作，阶段 3 继续标记未完成。
- **离线结果**：真实设置样本、计算器变化样本、搜索结果/具名入口/页面元素三个反例 `5/5` 通过；Qwen 选择与通用策略相关回归 `240/240` 通过。对 `1caac246` 落盘任务图与可信观察的离线重放已重新生成 `action=tap_semantic, element_id=settings_icon` 的本地 choice。最终 Python 全量 `1224/1224` 通过，仅有既知测试子进程 `ResourceWarning`；前端未修改。本批尚未提交或加载运行服务，不能把离线结果写成在线门或真机成功。

## 22. 2026-08-17 阶段 3 设置输入后验观察验收台账

- **当前唯一验收缺口**：阶段 3 仍缺两个成功 App 和文字输入闭环。设置打开动作在 session `47e761a971cc4d818561cbd0f9c80d2c` 中 matched，但该 session 因 DeepSeek 重规划证据不合法而 blocked；新鲜设置页 session `c0a65e1978a24d28b31a7a9569c8cda5` 的聚焦动作只执行一次后，正式后验观察失败，不能继续输入或把设置计为成功 App。
- **已观察事实与根因分类**：聚焦前可信输入为唯一空白 `application_text_input`；动作后真实画面显示设置搜索页和软键盘。第 1 轮后验观察未重建同一输入语义，第 2 轮明确返回 `role=keyboard_key` 且把全局 `states.keyboard_layout` 挂到该普通键，严格 scene 协议按预期拒绝。会话最终 `status=failed`、`physical_actions=1`、无 typed receipt，且没有自动重试。主要类别为通用观察合同缺陷；物理点击本身不允许重试。
- **同类失败样本集合**：真实样本为输入目标下 `goal_relevant=true` 的普通 `keyboard_key` 携带全局 `keyboard_layout`；变化样本覆盖同类按键同时携带 `keyboard_input_mode`。正向权威样本是唯一 `role=input` 继续保留合法 QWERTY 和输入模式事实；反向样本是可成为动作候选的非输入 `button` 若伪造同类字段仍必须失败关闭，未知字段和动作字段也不能被撤销。
- **通用修复说明**：普通 `keyboard_key` 从协议上不能承载全局键盘布局/输入模式，也不能成为通用语义动作目标；因此无论模型自报相关性如何，只撤销这两个不可能授权的外围字段。唯一 input 仍是全局键盘事实权威；button、模式切换控件和其他可动作元素的畸形字段继续由严格 scene 协议拒绝。该修复不创建元素、不改角色/几何/相关性，不读取 App、命令、步骤或坐标，权限只缩小。回滚方式为撤销该两字段撤权及测试。
- **验证清单**：新增真实 keyboard_key 正例、输入权威保留正例和可动作 button 反例；重放保存的动作后四帧必须能越过 compact scene 且由既有输入结构审计重建唯一输入，随后运行观察器和输入/Qwen/编排相关回归，批次稳定后只运行一次完整回归。离线全部通过才允许提交并只重载 Uvicorn；重载后先做 0 动作只读观察，确认输入值、焦点、QWERTY 和 `direct_latin`，不能重复聚焦动作。
- **停止条件**：若输入权威字段被删除、可动作非输入畸形字段被放行，或保存四帧仍在同一 `keyboard_key.keyboard_layout` 根因失败，则撤销修复并停止；新的 0 动作观察若无法同时证明安全输入前置条件，也不得输入、切换模式或猜测当前文字。
- **离线结果**：新增普通键双字段撤权、input 权威保留和可动作 button 反例后，观察器 `163/163`、观察/Qwen/通用编排相关回归 `403/403`、Python 全量 `1226/1226` 通过，仅有既知测试子进程 `ResourceWarning`；前端未修改。当前只能声明离线修复完成，尚未提交、加载服务或对真实当前画面执行 0 动作复核。

## 23. 2026-08-17 阶段 3 键盘复杂画面 compact 预算台账

- **当前唯一验收缺口**：加载 `87cd87c` 后的唯一 0 动作复核仍未取得安全输入前置证据；接口返回 HTTP 422，设备和手机均未执行动作，阶段 3 文字输入闭环继续阻塞。
- **已观察事实与根因分类**：观察器状态明确记录 `last_finish_reason=length`；compact JSON 在约 2928 字符、145 行处以缺少逗号结束，`last_input_structure_shape={}`，说明请求尚未进入独立输入结构审计。当前画面是设置搜索页加完整 QWERTY 键盘，而 compact 正式提示仍要求字母、数字、退格和回车普通键逐个作为 `keyboard_key` 报告，与“最多 4 个目标相关元素”和后续独立输入审计职责冲突。主要类别为通用模型接口/预算设计错误，不是坐标、App 路径或输入法模式。
- **同类失败样本集合**：真实样本为设置搜索页的字母 QWERTY；变化样本覆盖 numeric 与 symbol 键盘。三者的普通键都不能成为通用语义动作目标，也不应占 compact 元素预算。正向样本是唯一应用 input、可见的独立中英模式按钮和必要页面导航仍可报告；反向样本是 compact 中任何动作字段、未知角色或目标元素非法结构继续失败关闭。
- **通用修复说明**：从所有 compact/targeted 页面观察提示中撤销“逐个报告普通键”的职责，明确普通字母、数字、退格、回车不得进入 elements；键盘可见性、布局、输入模式和按键几何由既有独立全帧输入结构审计统一负责。解析器和 Token 上限不放宽，模型若仍返回普通键则沿用现有权限缩小和严格校验；不创建控件、不改几何、不增加 App/命令/步骤/坐标分支。回滚方式为恢复原提示句并撤销提示测试。
- **验证清单**：增加 compact 与 targeted 提示的普通键禁报断言，并确认 input/mode-switch 权威说明仍存在；运行观察器最小回归、输入/Qwen/编排相关回归，再运行一次完整回归。离线全绿后提交并只重载 Uvicorn；只允许一次新的 0 动作观察，必须 `finish_reason=stop` 且输入结构审计成功，才能评估输入动作。
- **停止条件**：若提示仍要求普通键、输入/模式按钮也被错误删除，或新 0 动作仍以 `length`/JSON 截断结束，则不增加 Token、不再次采样、不输入，阶段 3 保持未完成并报告该唯一阻塞。
- **离线结果**：compact/targeted 普通键禁报与独立输入审计权威断言通过；观察器 `164/164`、Qwen/通用编排 `240/240`、Python 全量 `1227/1227` 通过，仅有既知测试子进程 `ResourceWarning`；前端未修改。本批尚未提交、加载或在线复验。

## 24. 2026-08-17 阶段 3 DeepSeek 结果状态与回执消费合同台账

- **当前唯一验收缺口**：阶段 3 已有相机成功、设置成功和真实浏览器页面中的成功物理动作证据，但仍缺一个非预设页面的文字输入成功闭环及最终三 App 口径复核；继续换 App、换词抽样不能直接关闭该缺口。
- **已观察事实与根因分类**：计算器目标 `从桌面的系统应用文件夹中找到并打开计算器，输入12+34并让结果清晰可见后停下` 与小米视频弹层目标 `关闭当前遮挡小米视频主界面的广告弹层...` 均在 `physical_actions=0` 时连续触发 `DeepSeek 高层任务图包含低层动作表达：goal.objective`，证明同类失败跨 App、跨动作类别成立。另有浏览器 session `afa938cb7f31427789150228958f543d`、音乐 session `4aae54b2023e466ea9230f5d35e117dd` 和小米视频 session `4529263de9724ed8b558334c7e1af1e9`：真实动作均 matched 且生成 typed receipt，但 DeepSeek 修订未完成该 receipt 严格绑定的 `navigation_only` 子目标，随后由编排器以 `blocked` 终止。主要类别为通用 DeepSeek 接口合同缺口，不是坐标、App 路径、单张截图或机械臂失败。
- **同类失败样本集合**：初始规划正向样本覆盖“关闭遮挡层后页面无遮挡”“在输入框留下未提交文字后字段显示指定内容”“打开入口后目标页面前台可见”，变化样本覆盖本机临时计算结果可见；动作后样本覆盖 `tap_semantic` 打开 App、`home` 返回 Launcher 和 `close` 关闭弹层。反向样本要求无 matched typed receipt、receipt 不绑定当前活动子目标、外部状态子目标或第二次仍不消费 receipt 时继续失败关闭。
- **通用修复说明**：第一，只在 DeepSeek 初始与唯一修复提示中加入通用“原始动作措辞→可见结果状态”例式，不读取 App 名称、命令、固定步骤或坐标；严格低层动作校验不放宽。第二，在重规划校验层检测“当前 matched `verified_action_transition` 已提供严格绑定 controller refs，但候选仍未完成上一 `navigation_only` 活动子目标”，把它作为可修复协议错误交给现有唯一一次 DeepSeek 修复；不由本地自动完成、不生成动作、不绕过视觉身份锚点，第二次仍不合规则失败关闭。影响面仅为初始高层文本合同和动作后 receipt 消费合同；回滚方式为撤销新增提示例式、校验分支及测试。
- **验证清单**：新增初始/修复提示结果状态例式断言；新增 matched receipt 首轮未消费、第二轮合法消费的正向测试，以及第二轮仍未消费的失败关闭测试；重放已有低层动作、外部状态、controller ref、命名页面身份和未知证据测试；随后运行 DeepSeek/编排最小回归、关联回归和一次完整回归。离线全绿后才允许提交、只重载本工作树 Uvicorn，并仅从当前真实小米视频广告弹层做一次新会话在线门。
- **停止条件**：若低层动作字段被放行、外部状态能借 receipt 完成、错误 receipt 能跨子目标消费，立即撤销；若修复后第一次在线门仍出现同一低层动作或 receipt 未消费根因，不继续采样或再加提示补丁，阶段 3 保持未完成并报告唯一阻塞。
- **离线结果**：结果状态例式、matched receipt 首轮未消费后唯一修复、第二轮仍未消费失败关闭的最小正反测试 `4/4` 通过；DeepSeek 与通用编排相关回归 `286/286` 通过；使用项目依赖环境的 Python 全量 `1229/1229` 通过，仅有既知测试子进程 `ResourceWarning`。前端协议 `26/26`、主浏览器 `7/7`、阶段 2 浏览器 `4/4`、触控合同 `4/4`，合计 `41/41`；两个 JavaScript 文件语法和 `git diff --check` 通过。当前只证明离线合同与回归完成，尚未提交、加载服务或在线复验。

## 25. 2026-08-17 阶段 3 可逆遮挡结果状态风险合同台账

- **当前唯一验收缺口**：阶段 3 仍缺第三个非预设真实 App 的成功终态及文字输入闭环。小米视频新 session `d72c5ccb2fe34710987c0ef5d9c0adb9` 首次 0 动作观察已合法提出弹层右上角唯一关闭候选；确认前刷新观察时在 `physical_actions=0` 失败关闭，尚未产生真机成功证据。
- **已观察事实与根因分类**：任务图依照正式结果状态例式把活动子目标写为“小米视频主界面无遮挡，主要内容可见”并标为 `navigation_only`；独立语义风险审计却把同一状态判为 `external_state`，确定性校验报 `close_ad_popup audit=external_state graph=navigation_only`。本地可逆导航复核只识别打开、返回、切换、弹层等动作式/容器式词形，没有识别提示合同已经要求 DeepSeek 使用的“页面不再被遮挡、主要内容可见”结果式词形。主要类别为跨 App 通用接口合同不一致，不是 App 路径、坐标、Qwen 候选或机械臂故障。
- **同类失败样本集合**：真实样本为视频主界面“无遮挡且主要内容可见”；构造变化样本为任意当前页面“遮挡层不再可见且页面内容清晰可见”。正向边界要求任务图本身为无风险 `navigation_only`、无关联 risk、实体及同 scope 全部文本均无外部状态效果；反向样本为同一句同时要求登录、保存、发送或其他未否定外部效果时必须继续判为外部状态并失败关闭。
- **通用修复说明**：把“页面/界面不再被遮挡或无遮挡，主要内容可见”纳入结构化可逆导航的结果状态语义；仍须同时通过现有任务图影响分类、无风险关联、实体无外部效果、逐字段无外部效果四重门禁，且只用于纠正模型把安全本机临时界面状态误判为 `external_state/unknown_external_effect|data_mutation` 的假阳性。实现不读取 App 名称、命令、固定步骤、控件或坐标，不自动生成/执行动作。回滚方式为撤销该结果状态模式及对应正反测试。
- **验证清单**：新增无遮挡真实表达和换措辞变化表达的正向风险复核测试；新增同句包含未否定登录/保存效果的反向测试；重放既有临时标签页、输入草稿、键盘模式、登录/退出及风险冲突测试。仅运行 DeepSeek 最小测试后再跑 DeepSeek/编排相关回归；批次稳定后运行一次完整回归。全部离线通过并加载本工作树 Uvicorn 后，只允许从当前小米视频弹层建立一个全新会话做最终真机验收。
- **停止条件**：若外部状态反例被降级、无风险结果状态仍冲突，立即撤销并报告；若加载后的唯一新会话仍出现同一分类冲突，不再抽样或追加措辞补丁，阶段 3 保持未完成。
- **离线结果**：无遮挡真实表达、换措辞变化表达、外部状态反例及既有风险实体反例最小测试 `4/4` 通过；DeepSeek 与通用编排相关回归 `288/288` 通过；项目依赖环境 Python 全量 `1231/1231` 通过，仅有既知测试子进程 `ResourceWarning`。前端本批未修改，沿用同一提交前已经通过的 `41/41` 合同结果。当前只证明离线修复完成，尚未提交、加载或取得新的真机动作证据。

## 26. 2026-08-17 阶段 3 状态效果约束与目标禁令语法边界台账

- **当前唯一验收缺口**：阶段 3 仍缺设置文字草稿成功闭环。桌面新 session `5481157756304b0d8faa08f83b0699c4` 已在 0 动作观察中唯一识别 `settings_icon`，但本地 choices 只剩 `back/home/swipe/wait_for_change`，无法生成进入设置的确认 scope。
- **已观察事实与根因分类**：任务图全局约束包含“不得执行任何可能修改设置的操作”。共享候选过滤器把句末作为名词的“操作”命中元素目标动词正则，再以“设置”二元词交集删除设置图标；这违反第21节已经冻结的“状态/结果禁令不能仅凭共享词删除安全入口”合同。主要类别为共享通用语法边界缺陷，不是 App、坐标、Qwen 随机输出或手机状态。
- **同类失败样本集合**：真实样本为“不得执行任何可能修改设置的操作”与设置入口；变化样本为“不得进行任何可能删除相册内容的操作”与相册入口，两者都应保留安全入口。反向样本为“不得操作设置入口”仍应删除该入口；混合句“不得执行可能修改设置的操作，也不得打开设置入口”必须由后半句直接目标禁令删除入口。
- **通用修复说明**：在共享过滤器中区分“执行/进行可能产生状态效果的操作”语法和直接“点击/打开/进入/选择/使用/操作某元素”语法。仅当约束是前者且没有同句强直接目标动词时，交回任务风险与动作控制层处理；同句任何明确直接目标禁令仍优先过滤。不读取 App 名称、命令、步骤或坐标，也不改变动作权限。回滚方式为撤销这一语法边界和对应正反测试。
- **验证清单**：新增真实样本、跨 App 变化样本、直接操作反例和混合句反例；重放第21节五个既有正反样本；运行共享过滤器、Qwen 选择与通用编排相关回归，再运行一次完整回归。全绿并加载本工作树 Uvicorn 后，只允许重新从桌面建立一个设置目标在线门。
- **停止条件**：若直接目标禁令被放行，立即撤销；若修复后的唯一设置在线门仍因候选过滤缺失阻塞，则同根因两轮未收敛，停止修改并明确阶段 3 未完成。
- **离线结果**：共享过滤器真实样本、跨 App 变化样本、直接操作反例、混合句反例及第21节既有样本合计 `9/9` 通过；Qwen 选择与通用编排使用方回归 `240/240` 通过；项目依赖环境 Python 全量 `1235/1235` 通过，仅有既知测试子进程 `ResourceWarning`。前端未修改。当前只证明离线收敛，尚未提交、加载或取得设置在线 scope。

## 27. 2026-08-18 阶段 2/3 分批集成与输入准备合同收敛

- 新建本地分支 `codex/integrate-phase2-stage3`。先从主分支连续引入截至严格阶段 2 完成点 `019a19f` 的 32 个提交；该基线独立通过 Python `1215/1215`、前端协议与触控合同 `30/30`、JavaScript 语法和 `git diff --check`。
- 阶段 3 后续生产代码没有整体合并历史调试文档，而是按三批引入并分别验证：跨 App 页面/约束/回执合同 `461/461`；任务图正规化、诊断和输入作用域 `639/639`；输入载体、canonical 文本和纯禁止风险合同 `754/754`。两份交接文档在冲突时保留当前版本，本节重新给出最终准确状态。
- 新提交 `4b1af7e` 增加本地派生的 `local input preparation` 结构证明。只有正式输入载体处于可见、可编辑或已聚焦状态，节点与已经独立证明的 canonical 未提交输入节点位于同一依赖链，且正向结果不包含发送、保存、提交、账号或其他外部效果时，才把该节点视为可逆准备状态。该证明不授权文字输入；正式写入仍只能使用 `input_verified_text`。
- 正向测试覆盖最后真实失败图的中文结构和 Notes 英文跨 App 变化；反向测试覆盖链外节点、真实保存结果和缺 canonical `input_text`。最后失败目录 `generic_supervised_20260817_222950_1008db4e` 的完整脱敏响应已直接离线重放，结果为三个节点均 `navigation_only`、`risk_count=0`、风险链接全空、活动节点仍为首个导航节点，没有生成或执行动作。
- 新提交 `9fb694c` 停止把随机控制令牌明文打印到终端；令牌仍按原合同通过本机受保护的页面初始化接口使用。
- 最终离线结果：DeepSeek/诊断/观察/Qwen/编排/Web 相关 `761/761`，Python 全量 `1289/1289`，前端协议与触控 `30/30`，从本机已有 npm 缓存加载 Playwright 后浏览器合同 `11/11`，前端合计 `41/41`；Python 仍只有既知测试子进程 `ResourceWarning`，所有断言通过；`git diff --check` 通过。
- 本轮没有重载 Uvicorn、没有调用 DeepSeek/Qwen 在线服务、没有操作相机、控制器、机械臂或卖家 `main.exe`。代码完成不能替代真机验收。

## 28. 下一次唯一在线门与阶段 3 完成边界

1. 在当前集成分支代码被加载后，先只读核对 `/api/device`、controller、camera、busy、活动任务/会话、`device_id` 和卖家 `main.exe` 原进程；不得启动、停止或重启卖家程序。
2. 只建立一个全新的 0 动作草稿任务。若 DeepSeek 形成合法的本地输入准备链且能够进入新鲜观察/Qwen 决策，才允许继续；同类风险合同失败时停止，不换词抽样。
3. 合法 scope 成立后仍严格一次一动作、失败不重试，取得 matched `input_verified_text` typed receipt，确认输入框逐字显示 canonical 文本且未提交、未发送、未搜索、未保存或发布。
4. 输入成功后建立独立四帧 0 动作会话核对最终可见状态。该证据成立前，阶段 3 不得标记完成。
5. 最终验收还必须补强为三个 App 的安全多步任务，并完成一组同目标不同措辞的真实等价执行；当前相机已有两动作证据，系统设置和 Browser 的现有单动作会话不能单独满足最终目标中“至少三个不同 App 中组合执行多步任务”的保守标准。

## 29. 2026-08-18 前台 App 身份占位符合同台账

- **当前唯一验收缺口**：浏览器陌生多步目标必须在页面状态变化后使用新观察安全重规划，生成全新的 `home` scope，并在同一会话以真实桌面证据结束；当前 session `8622a674436149aea9d58dcd83a6c62a` 尚未执行任何物理动作，不能作为成功证据。
- **已观察事实与根因分类**：旧确认因画面差异 `18.83 > 6.00` 在机械臂前失败关闭，`physical_actions=0`。随后 `/next` 的四帧新观察稳定识别 `screen_id="通用动作真机验收页"`、唯一 `page_title` 和严格审计的 reload 控件，但 scene 把前台 App 写成目标上下文占位符 `current_foreground`。DeepSeek 因而无法用结构化事实证明“浏览器页面”，以“命名页面完成声明缺少结构化画面身份锚点”阻塞。主要类别为通用观察器与任务图之间的 App 身份接口合同缺陷，不是 Home、坐标、机械臂、相机或页面标题识别失败。
- **同类失败样本集合**：真实样本为浏览器画面返回 `current_foreground`；构造变化样本为设置或聊天画面返回同一占位符。三者都不能把目标上下文引用词当成视觉身份。正向样本为独立只读 App 身份审计返回高置信、非占位、稳定的语义 ID；反向样本为 `current_foreground/current_app/target_app`、低置信、`unknown`、重复键、额外字段或动作/坐标证据，均不得铸造身份权限。
- **通用修复说明**：当且仅当完整场景的 `foreground_app_id` 是引用占位符时，增加一次只读、单图、无目标上下文的前台 App 身份审计。审计只允许返回严格版本、lower-snake-case 语义 ID、置信度和短视觉依据；本地拒绝重复/额外字段、占位符、动作和坐标内容。高置信唯一结果只替换 scene 的 App 身份，不改页面、元素、几何、目标相关性或动作权限；低置信或 `unknown` 统一降为 `unknown` 并继续失败关闭。正常已有结构化 App 身份的场景不增加调用。该设计不含 App 名称分支、固定命令、步骤、截图或坐标；回滚为撤销独立审计、版本和对应测试。
- **验证清单**：加入浏览器真实形状与设置变化样本正测，加入占位符/低置信/重复键/额外字段/动作证据反测，并证明已有 `browser/settings/chat_app/unknown` 场景不被改写；运行观察器最小测试、DeepSeek 身份和编排相关回归，批次稳定后仅运行一次完整回归。全绿后本地提交并只重载项目 Uvicorn；随后仅做一次全新 0 动作观察，若得到合法 browser 身份和新 scope，才确认一次 `home`。失败不再次采样、不放宽身份门禁。
- **停止条件**：独立审计若仍返回占位符、无法高置信识别 App、改写任何非身份字段，或 DeepSeek 仍因同一身份根因阻塞，则阶段 3 保持未完成；不得绕过通用链路直接调用 Home，也不得继续追加浏览器专用别名或截图规则。
- **离线结果**：新增真实浏览器形状、设置/聊天变化样本和占位符、低置信、`unknown`、重复键、额外字段、动作/坐标证据反例，最小测试 `5/5`；观察器、既有中英身份和编排相关回归 `346/346`；Python 完整回归 `1325/1325`。静态编译与 `git diff --check` 通过，仍只有既知测试子进程 `ResourceWarning`。当前仅能声明代码和离线合同完成，尚未提交、加载服务或取得新的在线 App 身份与真机 Home 证据。

## 30. 2026-08-18 动作后只读标题证据合同台账

- **当前唯一验收缺口**：浏览器新会话必须在同一 session 中完成“桌面打开浏览器 → 用动作后可信画面确认主标题或错误提示 → Home 返回桌面”，每个真实动作各一份 matched typed receipt，最终 `status=succeeded`。session `a5f374a0c0d2485e93b678397a3e6b15` 已完成第一个真实动作，但最终 blocked，不能计为成功。
- **已观察事实与根因分类**：该 session 从 `launcher/home_screen` 以唯一 `浏览器` 元素执行一次 `tap_semantic`，动作前后各 4 帧、指纹更新、receipt `receipt_b01790de59d3408084f17b28d850aead` matched；动作后场景为 `browser/通用动作真机验收页`，且结构化元素 `e1` 明确为 `role=text`、`meaning=page_title`、`label=通用动作真机验收页`、`fully_visible=true`。DeepSeek 在结算全局 `title_read` 条件时被本地报错“命名页面完成声明缺少结构化画面身份锚点”，原因是 `_named_visual_identity_anchor` 把“已读取打开后页面的主标题或错误提示”中的时序引用“打开后页面”误当成稳定页面名称。主要类别为通用 DeepSeek 证据语法合同缺陷，不是浏览器、Home、坐标、模型视觉、机械臂或相机失败。
- **同类失败样本集合**：真实样本为“已读取打开后页面的主标题或错误提示”；变化样本覆盖“进入后页面的标题已确认”“操作后页面的错误提示可见”和英文 `the page shown after opening`，这些都是对当前动作结果页面的引用，不能被铸造成具名页面。反向样本为“跨境订单结果页面”“系统设置页面”“浏览器页面”等稳定名称，仍必须由结构化 App/screen/标题事实锚定；引号中的逐字标题仍必须完整匹配，不能因本修复放宽。
- **通用修复说明**：只在具名视觉身份抽取器中把明确的动作后/进入后/打开后/加载后/切换后等时序页面引用归为无名当前容器；随后完成证据仍必须逐字来自 `visible_evidence` 或 `grounded_visual_facts`，read_only 节点仍由 DeepSeek 新 revision 结算，外部状态、未知证据、引号逐字标题和真正具名页面门禁完全不变。实现不读取 App 名称、命令、步骤、截图或坐标；回滚为撤销这一时序引用语法和对应正反测试。
- **验证清单**：先把真实失败短语做 `_named_visual_identity_anchor` 回归，再加入中英文变化正例和稳定具名页面/逐字标题反例；重放 DeepSeek 完成条件验证，确认真实 `page_title` grounded fact 可完成 `title_read`；运行 DeepSeek 与通用编排相关回归，再运行一次完整回归。全绿后本地提交并只重载项目 Uvicorn；最后从当前浏览器页先通过全新 0 动作证据结算标题，再只执行一次 Home，或从桌面建立全新两动作 session，失败不重试。
- **停止条件**：若具名页面反例被放行、逐字标题不再完整匹配、外部/未知证据可完成 read_only，立即撤销；若加载后的唯一新会话仍出现同一“打开后页面”身份误判，不换词采样、不追加 App 特例，阶段 3 保持未完成。
- **离线结果**：真实失败短语、中英文时序引用变化样本、稳定具名页面反例、逐字标题反例和 exact grounded `page_title` 完成条件测试 `4/4` 通过；DeepSeek 与通用编排相关回归 `337/337`；项目 `.venv` Python 完整回归 `1327/1327`，仅有既知测试子进程 `ResourceWarning`。静态编译通过。当前仍只证明离线合同完成，尚未提交、加载服务或取得最终 Home 与 `succeeded` 真机证据。
- **在线动作与当前停止点**：修复前新 session `a5f374a0c0d2485e93b678397a3e6b15` 从真实 Launcher 对唯一“浏览器”图标执行一次 `tap_semantic`，动作后 4 帧确认 `browser/通用动作真机验收页`、标题元素完整可见，receipt `receipt_b01790de59d3408084f17b28d850aead` matched；随后因本节时序身份误判 blocked，未执行 Home。修复已本地提交为 `9516e09` 并加载到新 Uvicorn；卖家 `main.exe` 始终保持 PID `4968`、启动时间 `2026-08-17 09:52:18`，未被启动、停止或重启。项目服务重载后连续只读发现 controller/camera offline；Win32 进程与线程窗口枚举均确认该 seller 进程响应但当前没有任何顶层窗口，无法重新取得控制器或相机句柄。按真机门禁停止于 0 新动作，阶段 3 仍未完成；恢复条件是卖家控制端重新出现可枚举窗口，之后必须重新核对设备并建立全新 session，不能复用 blocked scope。

## 31. 2026-08-18 手机桌面结构化身份合同台账

- **当前唯一验收缺口**：先让当前浏览器页面通过一个独立受监督 Home 会话安全回到 Launcher，再从桌面建立全新的浏览器多步 session；恢复会话 `07b1a3679f2841fd95370a15b3742a67` 在 0 动作阶段失败，尚未产生 Home scope。
- **已观察事实与根因分类**：用户确认卖家控制窗口一直可见。此前窗口“找不到”已证实是项目 Uvicorn 从非交互后台桌面启动；改用当前登录用户的交互任务启动后，`/api/device` 立即重新得到准确标题、`810x1515`、controller/camera online，卖家 PID/启动时间未变。随后恢复会话的可信画面明确为 `browser/通用动作真机验收页`，唯一 goal-relevant 元素是 reload；任务目标要求 `手机桌面`。`_named_visual_identity_anchor` 未把“手机桌面”视为稳定系统画面身份，后续候选绑定仅因两段文本共享“显示”二元词，就让 DeepSeek 用 reload 元素声称桌面子目标 completed，最终被“全局条件未满足”拦截。主要类别为通用系统画面身份/可见状态绑定缺陷，不是窗口、Home、坐标、Qwen、机械臂或相机失败。
- **同类失败样本集合**：真实反例为 browser 页面+reload 元素不能证明手机桌面；变化反例为 settings/chat 页面中的任意可见元素也不能证明桌面。正例为结构化 `app_id=launcher` 或 `screen_id=home_screen` 的稳定画面应能证明“手机桌面/主屏幕/Android home screen”；反向边界为网站 homepage、App 首页和“桌面版”文字不能自动等价为系统 Launcher。
- **通用修复说明**：把手机系统桌面/主屏幕作为稳定的结构化视觉身份，使用本地语义别名只与 `launcher/home_screen` 对齐；页面元素、摘要中的通用“显示/可见”词不能替代该前台身份。该门禁只影响 0 动作可见状态推进，不生成动作、不修改坐标、不读取 App 名称或固定步骤；既有具名 App 页面、逐字标题和普通元素定位门禁保持不变。回滚为撤销 launcher 视觉身份别名和对应测试。
- **验证清单**：加入 browser/settings/chat+任意目标元素均不得完成桌面的反例；加入 launcher/home_screen 的中英文正例；加入 homepage/桌面版反例；重放 `07b1a367` 的任务图与可信 scene，要求不调用 DeepSeek 可见状态完成而进入 Qwen Home 候选；运行 DeepSeek/编排相关回归和一次完整回归。全绿、提交并用交互桌面方式加载后，才建立一个新恢复会话；只执行一次 Home 并重新观察。
- **停止条件**：若非 Launcher 场景仍能 0 动作完成桌面、App 首页被误当系统桌面，或 Launcher 正例被阻塞，立即撤销；若新恢复会话仍出现同一错误推进，不再换词或真机采样，阶段 3 保持未完成。
- **离线结果**：真实 browser+reload 失败形状、settings/chat 变化反例、launcher/home_screen 中英文正例及 App 首页/homepage/桌面版反例通过；最小 `2/2`、DeepSeek 与编排相关 `339/339`、项目 `.venv` Python 完整回归 `1329/1329`，仅有既知测试子进程 `ResourceWarning`。交互桌面启动方式已验证 `/api/device` 能重新识别卖家窗口，临时计划任务、探针和日志已删除；卖家程序没有被重启或操作。当前仍待提交、加载和一次新恢复会话真机验证。

## 32. 2026-08-18 目标 App 前台状态措辞闭环台账

- **当前唯一验收缺口**：浏览器多步 session `06ebbdcf5ce44771a9782e7363bb2b82` 必须在 Launcher 上保留 `open_browser` 导航节点并生成浏览器入口 scope；当前以 0 物理动作 blocked，不能继续或复用。
- **已观察事实与根因分类**：revision 1 正确包含 `open_browser → read_page_title → return_to_desktop`。Launcher 可信画面只有“浏览器”入口图标，但 visible-presence 自动推进把 `浏览器应用在前台可见` 误当成已满足，revision 2 将 `open_browser` 0 动作 completed；随后 read_only 节点禁止 Qwen 点击入口而 blocked。既有前台一致性门禁只识别“页面/界面/首页”，漏掉同义的“应用在前台可见”。主要类别是第20节同一通用前台身份合同遗漏，不是 DeepSeek 漏节点、Qwen、坐标或机械臂失败。
- **同类失败样本集合**：真实浏览器 Launcher 图标、设置与聊天 App Launcher 图标都不能证明对应应用已在前台；变化正例为真实 foreground_app_id 与目标 App 一致时，“应用在前台可见/in the foreground”可以推进。系统桌面仍由第31节 launcher 身份处理；“入口图标可见”不能被本修复升级为 App 前台。
- **通用修复说明**：把明确的“应用在前台/foreground app/in the foreground”加入现有目标 App 页面身份门禁的结构化表面类别；凡子目标同时指向任务图中的具名目标 App，必须由 scene.foreground_app_id 与该 App 匹配。只扩展同义状态，不新增 App 名称、固定命令、步骤或坐标，不改变 Qwen/控制器权限。回滚为撤销该表面类别与测试。
- **验证清单**：扩展现有 browser/phone_manager/settings Launcher 反例为“应用在前台”措辞；增加真实目标 App 前台正例和英文变化；重放 `06ebbdcf` 初始图与 Launcher scene，要求 planner 不发生 0 动作推进而 Qwen 产生入口动作；运行编排相关回归和一次完整回归，提交、交互桌面加载后只建立一个全新 session。
- **停止条件**：若 Launcher 图标仍能完成前台状态，或 matching foreground 正例被阻塞，立即撤销；若新会话再次出现同一前台误完成，不再采样或扩展措辞。
- **离线结果**：browser/settings/chat Launcher 图标反例、matching foreground 正例和英文表面类别最小 `3/3`；DeepSeek 与通用编排相关 `341/341`；项目 `.venv` Python 完整回归 `1331/1331`，仅有既知测试子进程 `ResourceWarning`。实现只扩展既有结构化前台 App 门禁，没有新增 App 分支或动作权限；当前待提交、交互桌面加载与唯一新 session 在线复验。
- **交互桌面与恢复结果**：用户指出卖家窗口一直可见后，已纠正第30节基于后台进程句柄作出的错误环境判断。真正原因是项目 Uvicorn 从非交互后台桌面启动，无法枚举当前用户桌面。改用一次性 `InteractiveToken` 任务启动项目服务后，`/api/device` 稳定识别标题 `智联新途AI机械臂控制端v1.0.1018`、client `810x1515`，controller/camera online；每次临时任务均已立即删除，卖家 `main.exe` 保持 PID `4968` 和原启动时间，未被启动、停止、重启或点击。修复 `7cae986` 加载后，恢复 session `633b729f4aff41e799d2f159da24cb43` 只执行一次无坐标 `home`，receipt `receipt_1cd643881dcb4b868608643b2a05c981` matched，revision `1→2`，最终 `launcher/home_screen`、`status=succeeded`。
- **浏览器多步成功证据**：全新 session `ee63d7a897034eafa0bd80c18f5725b6` 从 Launcher 出发，动作 1 对唯一“浏览器”入口执行 `tap_semantic`，receipt `receipt_f843571b2b71464080513d13265f4e20` matched；动作后可信场景为 `browser/通用动作真机验收页`，`read_page_title` 在同一新画面中 completed 且记录 2 条 completion evidence，随后生成绑定 revision 2 和新 observation/fingerprint 的 Home scope。动作 2 为无坐标 `home`，receipt `receipt_5a49fc0dbb3b4d42a276c1a8df386632` matched；最终 revision `3`、`physical_actions=2`、task graph `completed`、`active_subgoal=null`、`status=succeeded`，最终场景 `launcher/home_screen`。报告为 `poc/output/web/generic_supervised_20260818_113353_ee63d7a8/report.json`。该会话正式补齐 Browser 的陌生多步真实闭环，但阶段 3 总体仍须按第8节复核另外两个 App 的保守证据与同目标不同措辞会话，不能仅凭本会话宣称整个阶段完成。

## 33. 2026-08-18 初始任务图终态与唯一活动前沿一致性台账

- **当前阶段合同缺口**：阶段 3 仍缺第三个真实 App 的同会话多动作成功、默认通用闭环中的文字输入以及同目标改写证据。设置草稿 session `298baa06c55b483bae916e1358505a6f` 已有一次打开设置的 matched 动作；第二次聚焦动作真实进入设置搜索页并显示英文 QWERTY，但顶部输入框被卖家预览 `PX/MM` 黑块遮挡，正式验证为 mismatched，按规则停止且没有输入。随后用于恢复桌面的全新任务在 0 动作建图阶段被严格拒绝，不能继续真机。
- **已观察事实与根因分类**：失败目录 `generic_supervised_20260818_122546_4561cad4` 的完整脱敏响应声明顶层 `status=completed`，同时唯一子目标 `navigate_home` 为 `pending`、`active_subgoal_id=navigate_home`，全局完成条件 `satisfied=false`、证据为空、无风险和澄清问题。本地报错“完成或阻塞任务图不能保留活动子目标”，`physical_actions=0`。主要类别为通用初始任务图状态一致性合同缺口；不是 Home、坐标、相机、控制器或卖家窗口失败。
- **同类失败样本集合**：真实样本为系统主屏幕目标的 `completed + 唯一 pending navigation_only 前沿 + 未满足完成条件`；变化样本为任意本地 App 前台目标返回同一结构。反向样本包括：已有任一已满足完成条件或 completed 子目标、多个独立可运行前沿、外部状态/风险节点、blocked 状态、存在澄清问题；这些情况不得本地改写成 running。
- **通用修复说明**：只在初始规划、第一次严格验证之前，且顶层唯一错误是过早写成 `completed` 时，依据其余不可变字段确定性归一为 `running`。前提必须同时满足：无全局或子目标完成声明/证据、无澄清问题、无风险、恰好一个依赖已满足的 `pending/active` 安全前沿、`active_subgoal_id` 精确指向该节点，且节点为 `read_only/navigation_only`。随后仍走现有唯一活动前沿归一、完整 graph validate、风险审计和“初始规划不得宣称完成”门禁。不改目标、App、步骤、约束、风险、坐标或动作权限；回滚为撤销该单一状态归一函数及测试。
- **验证清单**：新增真实 Home 形状与跨 App 变化正例；新增已满足条件、completed 子目标、多前沿、external/risk、blocked 和 clarification 反例；证明正例不触发第二次远程图修复，反例继续严格失败。先运行 DeepSeek 最小测试，再运行 DeepSeek/编排相关回归，批次稳定后运行一次完整 Python 回归与现有前端合同；全绿后才提交、用交互桌面方式只重载项目 Uvicorn并建立一次新的 0 动作恢复门。
- **停止条件**：任一反例被改写、目标/子目标/风险字段发生变化、或新在线门仍出现同一终态冲突，则撤销修复并保持 0 新动作；不得换词抽样或绕过通用确认链直接调用 Home。
- **离线结果**：真实 Home 形状与跨 App 变化正例、完成声明、多前沿、外部风险及澄清反例最小 `3/3`；DeepSeek 与通用编排相关回归 `344/344`；项目 `.venv` Python 完整回归 `1334/1334`，仅有既知测试子进程 `ResourceWarning`；前端协议与浏览器合同 `37/37`、触控合同 `4/4`，合计 `41/41`，JavaScript 语法与 `git diff --check` 通过。当前只证明实现与离线回归完成，尚未提交、加载服务或取得新的 Home scope。

## 34. 2026-08-18 文字选择器几何裁剪消歧台账

- **当前阶段合同缺口**：阶段 3 已有 Camera、Browser、Settings 三个不同真实 App 的同会话双动作成功证据，但仍缺默认通用闭环中的认证文字输入和同一目标改写证据。浏览器输入 session `12486e7d99bb4eb78574d8ca456a11fc` 已从 Launcher 执行一次打开浏览器的 matched 动作；下一步“返回验收模式选择”连续两次在机械臂前因确认几何漂移失败，`physical_actions` 始终为 `1`，不得进行第三次在线采样或点击。
- **已观察事实与根因分类**：两组动作前四帧与计划画面稳定一致，计划框 `(0.13,0.225,0.56,0.265)` 覆盖真实白色下划线入口；确认几何审计却返回 `(0.0916,0.2779,0.4765,0.3139)`，该框在保存原图中实际落到下方绿色 `long_press 验收通过` 卡片。控制器以 `iou=0.250`、`smaller_coverage=0.448` 在 0 新动作时拒绝，证明后段安全门禁正确。Windows OCR 在原始物理相机画面上能够识别邻近卡片文字，却连续无法识别带下划线的目标入口，因此不能靠 OCR 伪造正向授权。主要类别为通用元素几何审计裁剪合同缺陷：当前所有目标都使用至少 60% 宽且 60% 高的方形大 ROI，短文字选择器会同时暴露多个上下相邻控件，使模型可能把给定标签复制到错误控件。
- **同类失败样本集合**：真实样本为验收页中短文字入口与下方结果卡片；第二次独立确认帧复现同一错位类别。构造变化样本为设置列表中的短文字入口与上下相邻开关/列表项。反向样本为粗文字框已经严重偏离真实目标、目标触及内部裁剪边缘、返回零个或多个匹配、标签/角色不一致；这些情况必须继续 0 动作失败，不能回退粗框。图标、输入框、拖动端点和无标签控件不属于本批修改范围。
- **通用修复说明**：对 `text/button/tab/list_item` 且存在非空逐字标签的文字型选择器，使用候选相对的“宽而矮”单图裁剪：横向仍保留至少 60% 视野，纵向仅保留至少 20% 视野并按粗框高度扩展 3 倍；其他角色继续使用原 60%×60% 广域定位。每个模型调用仍只有一个 crop-local 坐标系，严格唯一匹配、置信度、完整可见、内部边缘和本地映射门禁均不变；粗框若错误到使目标不在窄裁剪中，结果必须是 0 匹配并失败关闭，不进行广域回退或自动重试。实现不读取 App、命令、步骤、标签内容、截图身份或固定像素；回滚为撤销文字型 ROI 参数选择及对应测试。
- **验证清单**：先增加真实长宽比裁剪测试，证明目标入口保留而下方卡片被排除；增加不同分辨率、不同 App 列表文字变化样本；证明图标/input/drag 仍使用原广域 ROI；保留粗框严重偏移、内部边缘、零/多匹配等反例。随后运行元素几何与观察器最小测试、通用动作/编排相关回归，再只运行一次 Python 完整回归和现有前端/浏览器合同。全部离线通过后才本地提交、只重载项目 Uvicorn，并用完全相同的原始目标从新鲜状态做一次最终真机验收。
- **停止条件**：若文字裁剪仍包含相邻控制卡片、任一反向样本获得授权、非文字角色坐标合同改变，或修复后的唯一新在线验收仍出现同类错位，立即停止并保持阶段 3 未完成；不得降低 IoU、改用计划旧坐标、固定标签坐标或继续抽样。
- **离线结果**：真实长宽比、不同分辨率和 observer profile 正反测试随元素几何套件 `33/33` 通过；观察器、几何和通用动作关联 `303/303`，加入 DeepSeek、Qwen 与编排后的关联回归 `744/744`；项目 `.venv` Python 完整回归 `1337/1337`，仅有既知子进程 `ResourceWarning`；前端协议、主浏览器、阶段 2 控制台和触控合同 `41/41`。两文件静态编译及 `git diff --check` 通过。当前只证明代码与离线合同完成，尚未加载服务或取得修复后真机证据。

## 35. 2026-08-18 初始总目标低层措辞的结果状态归一台账

- **当前阶段合同缺口**：v51 已加载并由独立 Home session `be692eacd1df43528acbe9b65382f264` 以 1 个 matched 动作、前后各 4 帧、revision `1→2`、最终 `launcher/home_screen` 和 `status=succeeded` 恢复到新鲜桌面。完全相同的原始浏览器输入目标随后在新目录 `generic_supervised_20260818_130818_103f5ba0` 的初始任务图阶段 0 动作失败，尚未验证文字 ROI 修复或产生任何新真机动作。
- **已观察事实与根因分类**：DeepSeek 初次输出和协议内修复输出连续把总目标写成“在唯一空白输入框中输入小写agent”，触发 `goal.objective` 低层动作门禁；同一 JSON 的 `goal.entities.input_text=agent`、全局完成条件和最终子目标却已经写成合法状态“唯一空白输入框中的内容为小写agent”，风险、约束和子目标链也均独立存在。本地按规则拒绝，未建立活动 session、未调用 Qwen、`physical_actions=0`。历史目录 `generic_supervised_20260818_021011_ab4f8ccb` 的清空输入任务也出现同一字段冗余现象，但它没有非空 canonical `input_text`，因此只能作为失败关闭边界，不能被本修复授权。主要类别为通用初始任务图字段冗余一致性缺口，不是 App、坐标、手机状态或文字 ROI 失败。
- **同类失败样本集合**：真实正例为当前 `input_text=agent` 任务；构造变化正例为另一 App 的 `input_text=draft42` 临时文字任务。反向样本包括历史无 canonical input_text 的清空任务、任一全局完成条件自身仍含低层指令、没有完成条件、完成条件已声称 satisfied 或带 evidence、完成状态不含精确 input_text、合成文本超过上限、目标没有低层词；这些情况不得归一或吞掉原错误。
- **通用修复说明**：仅在 initial graph 的第一次严格验证前，当且仅当 `goal.objective` 含低层词、`goal.entities.input_text` 是非空 canonical 文本、所有全局完成条件均为未满足且无证据、每条 description 都通过现有低层动作门禁、合成状态包含精确 canonical input_text 且长度合法时，用这些已结构化完成条件的 description 按原顺序重建 `goal.objective`。target_apps、entities、constraints、risk_actions、subgoals、active frontier 和 raw user goal 全部保持不变；重建后仍执行完整 graph validate、独立风险审计和初始不得完成门禁。清空输入、缺少完成条件或任何歧义时继续失败关闭，不发第二次模型调用，不为 App/标签/命令写分支。回滚为撤销该单一 initial normalizer 及测试。
- **验证清单**：加入当前落盘形状与跨 App 非空 input_text 变化正例，以及历史清空边界、低层 completion、缺失 completion、已满足/evidence、错误/缺失 input_text、超长和无需归一反例；证明正例不触发修复模型调用，反例仍抛原错误。运行 DeepSeek 最小测试、DeepSeek/编排相关回归，批次稳定后一次完整 Python 回归；前端未改，只重跑现有 41 项合同。全部通过后本地提交并只重载项目 Uvicorn，再从仍为 Launcher 的新鲜状态提交完全相同原始目标一次。
- **停止条件**：若任一反例被改写、风险/约束/子目标发生变化、低层词进入 completion/subgoal 后仍被放行，或加载后的唯一新请求再次出现同类 `goal.objective` 错误，立即停止；不得换词、删除风险、放宽低层词门禁或继续模型采样。
- **离线结果**：当前真实形状、跨 App 非空 input_text 变化样本及缺失/低层/已完成/超长反例最小 `5/5`；DeepSeek 与编排关联回归 `347/347`；项目 `.venv` Python 完整回归 `1340/1340`，仅有既知测试子进程 `ResourceWarning`；前端协议/触控 `30/30`、主浏览器 `7/7`、阶段 2 控制台 `4/4`，合计 `41/41`。静态编译及 `git diff --check` 通过。当前只证明离线合同完成，尚未提交、加载或取得相同目标的新在线证据。

## 36. 2026-08-18 阶段 3 当前最终停止点

- 文字 ROI 修复已提交为 `61853b4`，初始非空 input_text 结果状态归一已提交为 `4b045c8`；两者均为本地提交、未推送。v51 与 `4b045c8` 已加载到项目 Uvicorn，controller/camera online、`busy=false`。卖家 `main.exe` 始终保持 PID `4968`、启动时间 `2026-08-17 09:52:18`，未被启动、停止、重启、全屏、旋转或点击底栏。
- 独立恢复 session `be692eacd1df43528acbe9b65382f264` 从浏览器页只执行一次无坐标 Home，动作 matched、前后各 4 帧、revision `1→2`，最终 `launcher/home_screen`、`status=succeeded`。这只是安全恢复，不计为阶段 3 的第三 App 证据。
- 完全相同的原始浏览器输入目标在加载 `4b045c8` 后成功通过初始建图与 0 动作在线门。新 session `6f9303b4386f40069f60b26728431375` 从 Launcher 对唯一“浏览器”入口执行动作 1；前后各 4 帧、receipt `receipt_faf5294b0f2943e494201b2bbe849a7d` matched、revision `1→2`，新画面为 `browser/通用动作真机验收页`。
- 动作 2 的新 scope 精确绑定逐字标签“返回验收模式选择”。确认前 v51 不再产生旧版落到绿色相邻卡片的错误几何，而是在窄裁剪中因“没有建立唯一 label+role 匹配”失败关闭；`request_physical_actions=0`，session 总 `physical_actions=1`、status `needs_reobservation`、confirmation 已消费且不可复用。没有 `/next`、第三次模型采样、机械臂重试或坐标回退。
- 阶段 3 的三 App 双动作基础证据已经具备：Camera session `95844a8cd0a8466ebd166793c6cfb29d`、Browser session `ee63d7a897034eafa0bd80c18f5725b6`、Settings session `51ea62098d2548cab8a568b697ddcd35` 均为同会话至少两个 matched 真实动作并最终 `succeeded`。但严格阶段 3 仍未完成，因为默认通用闭环中的认证文字输入和同目标改写成功证据仍缺失；本次失败 session 不能替代。
- **停止决定**：按第34节既定停止条件和用户要求的收口原则，本轮不再修改生产代码、不换词、不继续 Qwen/DeepSeek 采样、不执行恢复动作。当前手机保持浏览器验收页，设备空闲且无可用 confirmation scope。下一轮若继续，必须先重新评估文字选择器几何权威的总体方案；不能沿用“再缩一次裁剪/再改一次提示词”的补丁路径。阶段状态为“核心跨 App 证据具备，但阶段 3 未验收完成”。

## 37. 2026-08-18 通用消息能力首批离线实现

- 已按 `阶段3通用消息能力验收台账.md` 实现 canonical 目标应用、收件人、消息正文和本地风险摘要。导航、收件人选择、草稿输入及最终发送仍由通用任务图编排，不包含微信、文件传输助手、固定联系人、固定正文或坐标分支。
- 默认主链路已支持精确前缀事务：小写英文直接分段，中文使用本地拼音并要求动作后出现精确预编辑和唯一逐字候选，再以独立动作选择候选。输入正文始终来自 canonical 原文，Qwen 只在本地合法选择中选动作。
- 离线结果：Python 完整回归 `1359/1359`，前端协议和浏览器合同 `38/38`，核心静态编译与差异检查通过。
- 当前仍缺数字、空格、标点和大写字母的逐键可见审计，且代码尚未加载项目服务或取得真机草稿证据。阶段 3 保持未完成；下一批只能补通用键位/布局合同，不能回到 App 或截图补丁。

## 38. 2026-08-18 通用键位与大小写离线闭环

- 已完成第二批通用键位合同：可见精确字符/空格键、qwerty/numeric/symbol 布局切换、英文 upper 状态与大小写切换均由输入结构审计签发，本地事务只选下一字符或下一英文分段所需的一步。
- 机械臂英文路径可在 upper 状态使用相同审计 QWERTY 几何输入大写；数字和标点不复用卖家不可靠的批量输入。动作后仍必须以原输入框精确值完成验证。
- 离线结果为 Python `1369/1369`、前端 `38/38`；相关 `589/589` 和 Web/适配器 `233/233` 同时通过。没有新增 App、联系人、正文、步骤或固定坐标。
- 尚未加载项目服务或完成真机草稿验收，阶段 3 仍未完成。字符范围仍限 100 字以内的项目正式白名单；换行、表情和生僻 Unicode 保持失败关闭。

## 39. 2026-08-18 消息草稿在线门的唯一新缺口

- v53/v14 已加载，设备与相机在线且空闲；独立四帧只读观察为 Browser 通用动作验收页，0 动作。卖家控制软件未被操作。
- 完全相同的 `Agent 8，你好。` 未提交草稿目标在 DeepSeek 初始图阶段 0 动作失败；证据目录 `poc/output/web/generic_supervised_20260818_145201_ed1bf243`。根因不是手机、Qwen 或坐标，而是模型把“禁止发送”错误登记为风险动作，且 canonical 正文内标点被旧子句检查切断。
- 通用修复保持失败关闭：仅删除“风险声明和正式约束均逐项否定、关联节点均为安全可逆链”的自相矛盾风险；缺约束或真实发送仍必须保留风险确认。现场形状和反例通过，DeepSeek `195/195`、Python `1371/1371`。
- 当前唯一下一步：提交本批、只重载项目 Uvicorn，重新核对设备/会话/观察后，用原目标执行一次新的在线门；若合法 scope 成立，再严格一动作一观察推进草稿，禁止提交或发送。阶段 3 仍未完成。
- `d387324` 已加载到交互桌面 Uvicorn PID `19576`。原目标 session `75af74036b554ccea723479bc985be2c` 成功生成无风险的 revision 1 和新 swipe scope，证明否定风险修复在线有效。
- swipe 在机械臂前因共享物理门抓帧漂移 `22.66/73.09` 失败关闭，`physical_actions=0`。已有两组持久化帧仅差 `0.10/0.31`，事后同抓图路径的只读帧仅差 `1.12/0.90`，但异常最后帧未保存，不能判断瞬时窗口遮挡、抓图异常或其他环境状态。未降低门限、未重试、未执行手机动作。
- 会话已取消并释放设备。下一轮的唯一允许批次是让门禁拒绝证据包含实际比对帧和摘要，增加环境变化正反测试并完整回归；只有再次出现可复现的通用缺陷才修改行为。阶段 3 仍未完成。
- 该证据批次已离线完成：拒绝异常在进程内携带实际帧和数值摘要，适配器仅在异常时追加 `physical_gate_actual` 会话证据；没有改变门限、授权或动作行为。最小 `4/4`、方向/适配器 `92/92`、Python 全量 `1372/1372` 通过。尚未提交、加载或做真机重试。
