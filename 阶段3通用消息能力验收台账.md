# 阶段 3 通用消息能力验收台账

更新时间：2026-08-18（Asia/Shanghai）

## 1. 当前唯一验收缺口

默认 universal-agent 主链路尚不能在不修改代码的情况下，针对用户指定的现有收件人，逐字准备任意消息草稿，并在发送前展示绑定收件人和消息原文的风险确认；确认后也尚未取得单次发送与动作后可见验证证据。

本轮能力合同是通用的“目标应用 + 唯一收件人 + 消息原文 + 草稿核验 + 发送确认 + 发送后验证”，不是微信、文件传输助手、某个联系人或“你好”的固定流程。

## 2. 已观察事实与主要根因

- `goal.entities.input_text` 已能保存 1～100 个原文字符，Qwen 也被要求逐字复用，发送属于 `external_state` 并必须等待风险确认。
- 当前正式 `input_verified_text` 从控制器、适配器到机械臂都只接受 `[a-z]{1,30}`，且要求空输入框、QWERTY 和 `direct_latin`。中文、数字、标点和混合文本在机械动作前被拒绝。
- 机械臂已经具备按本轮视觉 QWERTY 几何点击小写字母的能力；项目依赖中已有本地 `pypinyin`。旧 App 专用状态图曾实现“本地拼音 → 重新观察 → 逐字候选 → 点击候选”，但默认 universal-agent 主链路没有这项通用合同，不能直接复用旧 App 编排。
- 当前任务图的 `entities` 未正式校验收件人字段；全局 exact-text 约束也不能区分“打开 App”与“选择收件人”子目标。若直接把收件人设为全局唯一文字，会错误阻塞 App 启动等前置导航。
- 风险 scope 目前只绑定风险 ID 和任务版本，没有绑定收件人及消息原文摘要。页面虽然显示任务和风险说明，但无法证明用户确认的正是当前收件人和当前消息内容。

主要类别：通用输入事务、目标身份和风险确认合同缺失。不是 App 路径、固定坐标、单张截图或模型偶发措辞。

## 3. 同类样本集合

正向样本：

1. 聊天应用 A，收件人“文件传输助手”，消息“你好”。
2. 聊天应用 A，收件人“张三”，消息“今晚八点见。”。
3. 聊天应用 B，收件人“李四”，消息“A-1024”。
4. 聊天应用 B，收件人“Alice”，消息“Meeting at 8”。
5. 同一目标更换措辞、从 App 首页/搜索页/已有聊天页等不同安全起始状态进入。

必须失败关闭的反向样本：

- 两个同名收件人、收件人文字不完整或当前聊天标题无法逐字确认；
- DeepSeek 改写消息正文、候选文字与 canonical 原文不一致；
- 当前输入框值不是 canonical 原文的精确前缀；
- 拼音组合后没有唯一逐字一致候选，或候选触及裁剪边缘/存在冲突；
- 风险确认后的收件人、消息原文、任务 revision 或观察发生变化；
- 未经风险确认的发送、重复发送、自动重试，以及发送后看不到逐字消息证据。

## 4. 通用修复说明

1. 正式化 canonical `recipient` 与 `input_text`：两者逐字保存用户原文，禁止模型改写；收件人只在当前子目标确实要求该身份时成为 exact-text 约束。
2. 把文字输入改成可恢复的本地事务：只从已观察的精确前缀继续；英文/数字/符号和中文按确定性分段；中文先本地生成拼音并执行一次键盘输入，随后必须重新观察，再点击唯一逐字候选，不能在一次动作中盲选候选。
3. 输入结构审计只为当前所需分段公开必要的组合态、候选或键位，不枚举整张键盘；模型不能决定输入内容。
4. 发送风险确认绑定 canonical 目标应用、收件人、消息原文、revision 和风险 ID 的本地摘要；任一字段变化都使旧确认失效。
5. 最终发送仍是当前画面中唯一发送控件的一次 `tap_semantic`；发送后必须重新观察当前聊天身份、输入框状态和逐字消息结果。

不增加 App 名称分支、联系人列表步骤、固定命令、固定坐标或单 App 状态机。回滚方式为撤销新的消息实体、输入事务和风险摘要合同，不影响阶段 1 已认证的英文 `input_verified_text` 兼容路径。

## 5. 验证清单

1. 任务图：收件人/消息逐字保存、不同措辞等价、导航与发送风险分离；错误/缺失/改写字段失败关闭。
2. 输入事务：ASCII、中文、数字、标点、混合文本正向；前缀不一致、多候选、错误模式、未知键位反向。
3. 风险确认：摘要绑定、字段漂移、scope 重放和未确认发送反向。
4. 历史回归：阶段 1 输入/长按/拖动，阶段 2 两动作闭环，现有 1340 项 Python 与 41 项前端合同。
5. 真机：先在无外部影响页面完成至少两种不同文本的草稿输入；再在一个真实聊天 App 完成指定收件人草稿并停在发送确认前。只有用户明确确认当前收件人和消息原文后，才执行一次发送并重新观察。

## 6. 本轮停止条件

- 若需要 App 名称、联系人固定路径、截图坐标或模型生成输入文字，停止并撤销该方向。
- 同类模型合同失败连续两次，停止在线采样，回到保存样本做离线修复。
- 离线正反样本未全部通过，不加载服务或使用真机。
- 真机任一步失败立即保存证据并停止，不自动重试，不把草稿成功写成发送成功。

## 7. 2026-08-18 第二批实现结果

- 输入事务现把大写英文绑定到本轮可见大小写状态；若状态不符，只允许先点击方向明确的大小写切换键。实际字母仍使用同一组本轮 QWERTY 几何，按键序列由本地把字母映射为对应物理键，动作后必须出现保留大小写的精确文本。
- 数字、空格和标点不再交给卖家不可靠的批量输入，也不从 QWERTY 猜坐标。输入结构审计只接受完整可见、逐字值一致的字符键/空格键，或标签与目标布局一致的 qwerty/numeric/symbol 切换键；低置信、标签和值冲突、方向冲突、重复候选均不给动作权限。
- Qwen 只能从本地生成的“精确下一字符”“切换到下一字符所需布局”“切换到下一英文分段所需大小写”候选中选择一步；控制器再次核对原输入框、精确前缀和后置值。每一步仍只有一个物理动作并重新观察，不自动重试。
- observer 升级为 v53、input audit 为 v6、controller 为 v14。相关回归 `589/589`、Web/适配器 `233/233`、Python 全量 `1369/1369`、前端协议与浏览器 `38/38` 全部通过；仅有既知测试子进程 `ResourceWarning`，没有断言失败。
- 当前状态是“代码与完整离线回归通过”，尚未加载项目服务或取得真机草稿证据。普通消息字符范围仍以 `operation_specs.py` 的 100 字符中文、英文字母、数字和常用标点白名单为准；换行、表情和生僻组合字符继续失败关闭，不能宣称任意 Unicode 已完成。

## 8. 2026-08-18 首次在线门与否定风险合同修复

- v53/v14 已加载到项目 Uvicorn；只读设备检查为 controller/camera online、`busy=false`，卖家 `main.exe` 未被启动、停止、重启或点击。独立四帧观察确认当前为 Browser 的通用动作验收页，且调用为 0 物理动作。
- 完全相同的草稿目标首次在线建图在 DeepSeek 阶段以 0 动作停止。落盘响应把“不得提交或发送”错误登记成 low 风险动作，并把“保持在输入框页面，未提交或发送”中的否定状态误判为正向发送结果；没有 session、Qwen scope 或机械臂动作。
- 通用修复仅删除同时满足以下条件的自相矛盾风险标记：正式约束逐项否定风险动作、风险描述/外部效果本身也逐项否定、全部关联节点均能被本地证明为可逆导航或 canonical 未提交输入链。缺少禁止约束或真正要求发送的目标继续保留风险并等待确认。
- canonical 输入正文现在可包含自身标点：本地以“完整 literal 匹配范围之外的标点”确定状态子句，避免把 `Agent 8，你好。` 在逗号或句号处分裂后丢失精确正文绑定。
- 真实脱敏响应形状与缺少禁止约束的反例均通过；DeepSeek `195/195`、Python 完整回归 `1371/1371`。当前尚未重新加载此批修复，也尚未重试同一在线目标，因此只能声明通用合同修复与完整离线回归通过。
- 修复已提交为 `d387324` 并通过一次性交互任务加载到项目 Uvicorn PID `19576`；临时任务和辅助脚本均已删除，卖家 `main.exe` 保持 PID `4968` 和原启动时间。原目标新 session `75af74036b554ccea723479bc985be2c` 成功建立，DeepSeek 风险为空、revision 1、0 动作并产生新 swipe scope，证明本节否定风险合同在线生效。
- swipe 确认在控制端原语前被共享物理门拒绝：方向审计绑定帧与最后实际抓帧报告亮度差 `22.66`、结构差 `73.09`，`request_physical_actions=0`、总动作 `0`，scope 已消费。计划四帧与方向审计四帧离线仅差 `0.10/0.31`；随后同一交互抓图路径的只读帧与审计帧仅差 `1.12/0.90`，没有复现异常。因触发异常的最后一帧未落盘，当前只能归类为瞬时环境/诊断证据缺口，不能放宽门限或修改动作逻辑。
- 会话已取消释放设备，最终 `cancelled`、`physical_actions=0`、无可用 scope。阶段 3 仍未完成；下一次继续前必须先让共享物理门在拒绝时保存实际比对帧，离线证明根因后再决定是否需要通用修复，不能直接重试滑动。

## 9. 共享物理门拒绝证据批次

- **当前唯一验收缺口与根因事实**：共享门能在控制原语前安全拒绝漂移，但只持久化了审计前四帧，没有保存 `_assert_visually_bound` 实际比较的最后一帧；因此本次 `22.66/73.09` 无法复盘。当前未证明动作逻辑有缺陷，只证明诊断证据不完整。
- **同类样本集合**：真实样本为 session `75af74036b554ccea723479bc985be2c` 的 0 动作 pre-action failure；匹配变化样本为同路径事后只读帧 `1.12/0.90`；构造反例包括明显结构漂移、尺寸变化和普通曝光/微小噪声。拒绝行为、阈值和物理动作计数必须保持不变。
- **通用修复说明**：仅让视觉绑定异常对象在进程内携带发生拒绝时的 RGB 帧和数值摘要；适配器只在该异常上把帧写入当前 session 证据目录并加入异常 evidence。不得改变方向审计、门限、动作授权、机械臂调用顺序或 App/页面逻辑。回滚为撤销异常证据类型和单帧持久化。
- **验证清单与停止条件**：明显漂移必须仍为 0 动作且新增一张实际帧；匹配/曝光噪声继续通过且不产生拒绝帧；无 evidence_dir 时不写文件；现有方向凭据一次性消费和全部通用动作测试不得退化。只做离线实现、相关回归和一次完整回归；本批不加载服务、不重试真机动作。
- **离线结果**：异常现以专用 `OrientationFrameMismatchError` 在进程内携带被拒绝 RGB 帧及亮度/结构摘要，适配器只在当前 evidence directory 写入一张 `physical_gate_actual` JPEG；异常文本、0 动作计数、门限和一次性授权消费均不变。最小正反 `4/4`、方向与通用适配器 `92/92`、Python 全量 `1372/1372` 通过，仅有既知测试子进程 `ResourceWarning`。当前尚未提交或加载，本批按停止条件不进行真机重试。

## 10. 正向发送任务的输入状态约束误判

- **当前合同缺口与根因证据**：加载 `571d394` 并恢复交互桌面后，用全新目标“打开微信，找到唯一的文件传输助手对话，输入消息‘你好’并发送；发送后确认当前对话中可见刚发送的‘你好’”启动通用会话。DeepSeek 在 0 物理动作阶段给出合法 canonical `recipient=文件传输助手`、`input_text=你好` 和独立 `send_message` 外部风险，但本地低层指令检测把子目标约束“输入内容必须为‘你好’”中的名词短语“输入内容”误判成执行输入指令，失败类型为 `low_level_instruction`。原始脱敏响应与摘要已落盘在 `poc/output/web/generic_supervised_20260818_153737_8bfc*/`；未进入 Qwen、未生成 scope、未操作机械臂。离线重放在排除该误判后还暴露同一响应的两个后续合同边界：`verify_message` 只读节点依赖已确认的 `send_message` 外部节点，并只要求观察“刚发送的消息可见”，却仍被当成新的未声明外部动作；通信风险推断识别“发送消息”，但没有识别等价结果态语序“消息已发送”，使合法 `message_or_communication` 被误降为 `unknown_external_effect`。
- **同类样本集合**：本次正例为“输入内容必须为‘你好’”；变化正例包括“当前输入内容应为‘A-1024’”“输入内容保持为‘Meeting at 8’”以及“消息‘你好’已发送到指定对话”。反例必须继续拒绝“在输入框输入文字你好”“输入消息你好”“点击发送按钮”“输入内容必须为你好并点击发送”和任何坐标、按键或裸动作字段；“发送按钮可见”只能是界面状态，不能当成消息已发送。
- **通用修复说明**：只在 `subgoals.constraints` 中，把完整句式严格符合“输入内容 + 必须/应当/需要 + 为/是/等于/保持为 + 非空引号结果值”的当前状态约束排除出单个低层 token；句中其余 token 继续逐项扫描，因此夹带点击、坐标、按键或动作步骤仍失败关闭。通信风险增加“消息/私信/留言 + 发送/已发送/已回复/已通知”的等价语序，并明确排除按钮、图标、控件、入口和按键。只读后验节点仅在它没有风险、明确依赖一个带风险的 `external_state/unknown` 前驱、全部外部效果文字都同时是“可见/显示/确认/核对”等观察语句，且效果类型与前驱风险一致（未知类型只能绑定该前驱已有风险）时成立；当全图恰好只有一种已声明风险时，同样的纯观察型全局完成证据可以借用该唯一风险类型核对历史效果。本地验证与独立语义风险审计都不把风险或动作权限赋给只读证据，因此它不能自行授权或重复外部动作。不读取 App、收件人、固定文本、页面或坐标；回滚为删除这三个窄合同。
- **验证清单与停止条件**：重放本次完整 DeepSeek 脱敏形状；加入中英文/数字标点变化正例和上述低层反例；加入“已确认发送后只读观察”正例，以及无外部前驱、风险类型不符、夹带新动作的反例；只运行 DeepSeek 最小及完整模块回归，稳定后运行一次 Python 全量。全部通过后才重载项目 Uvicorn，并且最多用完全相同目标重新建立一次新 0 动作会话；若仍出现同类合同失败，不换词、不继续采样、不扩大修复。
- **离线结果**：本次完整脱敏响应已原样重放为 `ready/open_wechat`，canonical `recipient/input_text`、唯一通信风险和末尾 `read_only` 后验节点均通过；新增状态约束、后验绑定、风险类型不匹配和否定草稿回归 `9/9`，DeepSeek 模块 `200/200`，Python 全量 `1377/1377` 通过，仅有既知测试子进程 `ResourceWarning`。当前尚未重载本批代码或再次调用在线模型；下一步仍只允许一次相同目标的新 0 动作会话。
