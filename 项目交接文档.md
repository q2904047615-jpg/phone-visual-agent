# 手机自动点击器项目交接文档

更新时间：2026-08-11

> 最高优先级产品目标见根目录 `项目最终目标.md`。本文记录阶段状态和历史证据；若本文、旧需求或现有代码与最终目标冲突，以 `项目最终目标.md` 为准。

## 1. 项目最终目标

本项目要做的不是“只会操作微信或抖音的脚本”，而是一个能够理解不同自然语言表达、动态拆分任务、依据真实画面逐步操作不同 App，并可扩展到多台手机的通用视觉操作 Agent：

1. 用户在本机网页输入自然语言目标。
2. DeepSeek理解目标、约束和完成条件，动态拆分为高层子目标，并在页面变化时调整剩余计划。
3. 摄像头观察真实手机。
4. Qwen结合当前子目标判断页面、可见控件、状态和位置，并提出当前唯一下一视觉动作及预期结果。
5. 唯一的本地控制器检查目标唯一性、置信度、风险和坐标边界，不编排App专用流程。
6. `main.exe` 控制物理机械臂执行一次点击、滑动、返回、长按、拖动或文字输入动作。
7. 每次动作后重新观察并验证；符合预期则继续，不符合则重新规划，完成时保存可见证据。
8. 同一协议携带 `device_id`，为以后操作多台手机保留统一调度接口。

核心循环：

```text
用户目标
  ↓
DeepSeek：理解目标并动态拆分高层子目标
  ↓
Qwen3-VL-Plus：结合当前子目标观察画面并提出一个下一视觉动作
  ↓
单一控制器：校验动作、安全、置信度和坐标边界
  ↓
机械臂动作适配器 → 卖家 main.exe → 真实手机
  ↓
重新采集画面并验证结果 → 继续、重规划或结束
```

以后接入更多 App 时，应优先复用视觉理解和通用动作。只要新目标能由已有动作组合完成，就不应修改代码；不能为每条用户指令重新堆一条独立线性脚本。

## 2. 当前设备与运行环境

- 操作系统：Windows。
- 真实执行设备：物理机械臂、摄像头、真实手机。
- 卖家控制端窗口：`智联新途AI机械臂控制端 v1.0.1018`。
- 本机网页地址：`http://127.0.0.1:8765/`。
- 网页启动文件：`C:\Users\Administrator\Documents\Claude\Projects\手机自动点击器\启动机械臂网页控制台.cmd`。
- 文本理解模型：DeepSeek，密钥通过用户环境变量保存，禁止写进项目文件。
- 视觉模型：Qwen3-VL-Plus，密钥通过用户环境变量保存，禁止写进项目文件。
- 网页服务、`main.exe`、摄像头是三个不同部分。只重启网页服务时，不要关闭 `main.exe`、摄像头或移动手机。

## 3. 当前架构约束

以下规则是当前方向，不得退回旧的“自由 Agent + 线性脚本 + 不断打补丁”方式：

1. DeepSeek负责理解用户目标、维护高层动态任务图和根据新状态重规划，但不输出机械坐标。
2. Qwen负责目标驱动的页面观察和单步视觉动作建议，但不直接调用机械臂。
3. 所有物理动作只能经过唯一控制器和唯一机械臂动作适配器。
4. 每次只允许执行一个最小动作；动作完成后必须重新观察。
5. 控制器负责动作许可、安全门禁、坐标转换和验证，不承担微信或抖音业务流程编排。
6. 页面未知、画面模糊、观察不稳定、目标不唯一或置信度不足时停止，不能猜。
7. 点赞、评论、发送消息、关注等会改变账号或外部状态的动作，必须单独显示确认草稿，得到用户明确确认后才能执行。
8. 同一时间只能有一个任务控制真实机械臂。其他并行任务只能做只读检查、文档、界面或离线测试。
9. 模型不能绕过白名单执行任意坐标、任意命令或系统 Shell。
10. 不得为每一种用户说法增加固定命令、正则匹配或专用接口。
11. 不得以点赞、上划或某一条业务流程成功作为通用 Agent 完成的证明。

当前协议版本：

- 目标协议：`2026-08-10-generic-intent-v1`
- 页面协议：`2026-08-10-ui-scene-v2`
- 动作协议：`2026-08-11-universal-action-v3`
- 观察器：`2026-08-10-generic-scene-observer-v5`

## 4. 已真实验证的能力

以下内容已经过真实手机、真实摄像头和机械臂闭环验证，不只是模拟测试：

### 4.1 基础闭环

- 网页能够读取摄像头画面。
- 网页能够检测卖家控制端和摄像头状态。
- DeepSeek能够把用户指令转换为结构化目标。
- Qwen能够对当前手机画面输出结构化页面观察。
- 控制器能够生成一个受限语义动作并等待确认。
- 动作后能够重新采集画面，并由控制器判断动作是否完成。

### 4.2 单步上划验证成功

2026-08-11 已完成一次受监督的真实上划：

- 会话 ID：`82b65ecd1e464285b9aff26f4a7983f3`
- 只执行了一次物理上划。
- 执行前后均识别为抖音普通视频页。
- 执行前后画面指纹不同，人工也确认已经切换到另一条视频。
- 任务状态：`succeeded`。
- 报告：`poc\output\web\generic_supervised_20260811_123259_82b65ecd\report.json`

### 4.3 点赞状态观察和安全确认

- 页面结构现在允许用 `container` 表示可滑动内容区，但 `container` 本身禁止被当作点击目标。
- 控制器能识别 `点赞按钮`、`like_button` 以及 `is_liked=true/false` 等状态。
- 已修复“名称为点赞按钮却没有被识别为账号影响动作”的精确匹配问题。
- 点赞、评论等动作现在会标记 `account_effect_possible=true`，必须等待单独确认。

### 4.4 自动测试

- 最近一次完整自动测试：`352/352` 通过。
- 自动测试证明代码协议、状态限制和安全门禁没有回归，但不能代替真实设备验证。

## 5. 当前精确停点

目前存在一个“单次点赞”受监督草稿：

- 会话 ID：`a5d4c6175eaf48efaca5732b79434fb6`
- 状态：`awaiting_confirmation`
- 报告：`poc\output\web\generic_supervised_20260811_124947_a5d4c617\report.json`
- 当前观察截图：`poc\output\web\generic_supervised_20260811_124947_a5d4c617\initial_scene_2.jpg`
- 当前页面：抖音普通视频页。
- 观察结果：存在 `like_button`，`is_liked=false`，观察置信度约 `0.95`。
- 草稿只允许执行一次点赞点击，最大物理动作数为 1。
- 到更新本文档时，还没有执行这个点赞动作。

除非用户明确说“确认执行一次点赞”或表达同等明确的确认，否则不得执行这个草稿。新任务接手时还要先检查它是否仍存在，不能仅凭本文档假定现场没有变化。

## 6. 已实现但尚未完成充分实机验收的能力

这些能力在代码、旧流程或自动测试中存在，但不能描述为已经稳定可用：

- 通用网页控制台、任务草稿、确认、停止、SQLite记录和异常截图。
- 微信指定聊天发送文字、文件传输助手流程、相册图片选择等旧工作流。
- 抖音搜索、点赞、评论和批量交互等旧工作流。
- 中文拼音输入、候选词选择、英文/数字/标点输入、错误清空后重输。
- 弹层关闭、未知页面返回、输入框 ROI 验证和发送后验证。

以上部分过去曾采用较多 App 专用规则和线性脚本，出现过定位偏差、键盘变化、错字、直播/广告误判、评论区未关闭就上划等问题。它们需要逐步迁移到当前“结构化观察 → 单一控制器 → 一个动作 → 重新观察”的通用协议后，再做真实设备连续验收。

## 7. 当前仍未解决的主要问题

1. 通用控件定位还需要更多不同页面、弹层、亮度和布局下的真实数据验证。
2. 通用文字输入还不能保证在所有键盘布局下可靠工作。
3. 微信指定聊天发送文字和图片，尚未在新通用控制器下完成连续 9/10 以上实机验收。
4. 抖音搜索、连续点赞和评论，尚未在新通用控制器下完成连续实机验收。
5. 未知页面的“有叉先关叉，无叉再返回”已经确定为恢复原则，但还需迁移并验证所有相关状态。
6. 摄像头反光、运动模糊、页面动画和模型延迟仍会影响观察，应使用自适应稳定等待，不能依赖固定短延时。
7. 机械臂多位置 XY 精度、落笔深度和手机位置变化仍需持续校准；模型识别正确不等于物理落点一定正确。
8. 需要更多连续运行数据证明长期可靠性，而不是以一次成功代表整条工作流完成。

## 8. 下一步建议顺序

严格按一个可验证的通用能力节点推进。此前单次点赞草稿可以保留为历史证据，但除非用户重新明确要求，不再把它作为项目主线：

1. 将通用自然语言入口和DeepSeek动态任务图接为默认主路径，旧固定命令降为兼容路径。
2. 让Qwen结合当前子目标输出结构化场景和一个下一视觉动作建议。
3. 扩展并统一机械臂底层动作：点击、滑动、返回、长按、拖动和可靠文字输入。
4. 接通连续闭环：每步重新观察、验证、推进或重规划，普通安全导航不需要逐步人工确认。
5. 网页展示动态计划、当前画面、执行轨迹、停止和外部状态动作确认。
6. 使用未预设措辞、未预编排命令和至少三个不同 App 做组合任务验收。
7. 全链路携带 `device_id`，单手机稳定后再启用多手机并行调度。

如果发现小问题，只有在能明确证明修复不会改变其他模块协议时才局部修复；如果暴露出架构方向问题，立即向用户说明并先决定方向，不能自行继续叠补丁。

## 9. 新任务接手操作

新开 Codex 任务后：

1. 先完整读取根目录 `项目最终目标.md`，再读取本文件，不要重新从零设计项目。
2. 检查 `http://127.0.0.1:8765/api/device`。
3. 必须同时核对真实摄像头画面，不能只相信 API 的在线状态。
4. 确认控制端、摄像头、模型配置、忙碌状态和活动任务。
5. 查看最近一次会话的 `report.json`、动作前后截图和实际机械臂状态。
6. 未经明确确认，不执行任何会改变账号或外部状态的动作。
7. 不要同时让两个 Codex 任务控制 `main.exe` 或真实机械臂。
8. 修改后要完成：检查失败报告 → 修改 → 自动回归测试 → 必要时重启网页服务 → 再验证真实状态。

## 10. 主要代码和资料

通用执行核心：

- `poc\universal_action_controller.py`：通用动作决策和阶段限制。
- `poc\generic_supervised_runtime.py`：受监督单步会话和确认执行。
- `poc\generic_step_planner.py`：根据目标与观察生成下一语义步骤。
- `poc\generic_scene_observer.py`：调用视觉模型并形成结构化观察。
- `poc\ui_scene.py`：页面、元素、角色和状态数据结构。
- `poc\generic_action_adapter.py`：语义动作到机械臂能力的唯一适配入口。
- `poc\generic_intent.py`：自然语言目标的结构化意图。

网页与设备：

- `poc\web_app.py`：FastAPI、任务队列、状态接口和网页服务。
- `poc\robot_core.py`：摄像头、坐标、机械臂和旧工作流基础能力。
- `poc\static\app.js`：网页前端逻辑。
- `poc\README_WEB.md`：网页平台使用说明；其中旧工作流描述需结合本文档判断，不能全部视为已实机验收。
- `poc\test_web_platform.py` 及其他 `test_*.py`：自动测试。

运行证据：

- `poc\output\web\`：任务报告、动作截图和异常证据。
- `poc\data\tasks.db`：SQLite任务记录。
- `poc\data\web_tasks.jsonl`：任务日志。

## 11. 历史硬件说明

项目早期曾自行组装铝型材框架、V轮、龙门板、电机和皮带。该部分不是当前软件验证的主要阻塞点。当前通用 Agent 的实机验证主要使用已经能运行的成品机械臂和卖家 `main.exe`。

除非用户重新要求继续自制机械结构，否则不要从 M5 螺丝、电机座或龙门架安装重新开始分析。

## 12. 交接结论

当前项目已经具备通用观察、单步动作、安全确认和动作后验证的基础，但默认入口、任务规划、文字输入和连续执行仍受旧固定工作流限制。下一阶段的唯一主线是实现“自然语言目标 → DeepSeek动态任务图 → Qwen目标驱动的单步视觉动作 → 机械臂通用动作 → 重新观察验证 → 动态推进或重规划”，并用跨措辞、跨App、无需新增命令代码的真实任务证明其通用性。
