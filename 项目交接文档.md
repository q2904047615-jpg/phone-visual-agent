# 手机自动点击器项目交接文档

更新时间：2026-08-14（Asia/Shanghai）

> 新对话必须先完整读取根目录 `项目最终目标.md`，再读取本文。`项目最终目标.md` 是唯一最高优先级依据；本文只记录当前代码、运行状态、证据和下一步。若两者冲突，以 `项目最终目标.md` 为准。

## 1. 一句话交接

项目已经形成“DeepSeek 动态任务图 → Qwen 真实画面观察与唯一下一动作 → 本地通用安全控制器 → 机械臂一次动作 → 重新观察验证”的通用主路径，并完成 Home、严格文字证据和本地输入清空控件的协议化。最新正式输入验收只执行了 1 次动作，真实结果为 `a'gen't` 而不是 `agent`，证明 QWERTY 布局不能代表英文直输模式；验收已正确失败且没有自动重试。当前代码已把 `keyboard_layout` 与 `keyboard_input_mode` 分离，下一主线是先清理错误文本、显式切换到 `direct_latin` 并重新观察，再创建全新正式验收。

不得把下一步改成微信、抖音或设置 App 的专用脚本，也不得用固定截图坐标完成清空或输入。

## 2. 最终产品方向

本项目不是单功能自动点击器，而是可扩展到不同自然语言、不同 App 和多台手机的通用视觉操作 Agent：

1. 用户在网页输入自然语言目标。
2. DeepSeek 理解目标、约束、完成条件和风险，动态拆分高层子目标。
3. Qwen 结合当前子目标和摄像头真实画面，输出一个结构化页面观察和唯一下一视觉动作。
4. 本地通用控制器校验目标唯一性、画面身份、置信度、风险、能力和动作边界。
5. `main.exe` 驱动机械臂只执行一个底层动作。
6. 动作后重新拍摄、观察并验证；成功则推进，失败则停止或重规划。
7. 新命令若能由现有通用动作组合完成，不得为它新增专用代码。
8. 所有任务携带 `device_id`；同一设备同一时间只允许一个物理控制任务。

## 3. 当前仓库状态

- 仓库：`C:\Users\Administrator\Documents\Claude\Projects\手机自动点击器`
- 当前分支：`codex/complete-universal-agent`
- 当前相对远程同名分支本地领先 8 个提交。
- 用户当前要求：允许本地提交，不推送 GitHub。
- 2026-08-14 最后检查时工作区干净。

最近关键提交与本轮变更：

- `6b7b1bc fix: distinguish keyboard input mode`
- `d704da9 fix: reject imprecise clear controls`

1. `7575082 feat: formalize home and safe input flow`
   - 将 Home 纳入正式通用动作协议。
   - 收紧安全文字输入的设备能力边界。
2. `b7b9832 fix: require exact verified text evidence`
   - 修复“实际输入错误但仅凭画面变化被判成功”的问题。
   - 输入验收必须取得并逐字比对 `states.value`。
   - 增加 `states.keyboard_layout`：`qwerty / numeric / symbol / unknown`。
   - 当前真机输入仅允许空白、已聚焦、标准 QWERTY 输入框中的 1–30 个小写英文字母；其他内容在机械臂动作前拒绝。
3. `af1a149 fix: recognize safe local input clear controls`
   - 将输入框内部或紧邻右侧的独立圆形 `×` 建模为 `clear_local_text`。
   - 只允许它绑定唯一、非空、已聚焦、目标相关的输入框。
   - 必须通过几何绑定检查；键盘退格键不能冒充清空控件。
   - 没有增加设置 App 名称分支或固定坐标。
4. 本轮通用观察与控制器加固：
   - 观察器升级为 `2026-08-14-generic-scene-observer-v8`。
   - 只有唯一非空输入框与紧邻、紧凑的圆形清空控件形成可信几何绑定时，才补齐清空结构状态。
   - 文字“取消”/`cancel`、键盘输入法区域和远离输入框的控件不能冒充 `clear_local_text`。
   - 控制器独立复查取消语义、控件尺寸、间距和几何绑定，不依赖模型自报标签放行。
5. 本轮卖家预览取景修复第一阶段：
   - 卖家 v1.0.1018 会在最近鼠标位置绘制不透明 `PX/MM` 坐标提示；旧“停到标题栏”方案仍被映射为相机顶部坐标，遮住页面顶部输入框。
   - 第一阶段把鼠标停放在同一卖家窗口底部、相机裁剪区域之外的控制条；它能避免初始取景误触发顶部提示，但动作后的旧提示仍会残留。
   - 100%/125%/150% DPI 与横屏布局继续使用统一的卖家窗口缩放函数计算相机高度。
6. 本轮精确英文输入前置条件修复：
   - 观察器升级为 `2026-08-14-generic-scene-observer-v9`。
   - 输入框状态新增 `keyboard_input_mode=direct_latin / chinese_pinyin / unknown`；QWERTY 只表示按键排列。
   - Qwen、通用策略、通用动作控制器和设备控制器均在 `chinese_pinyin` 或缺少模式证据时，于硬件动作前拒绝 `input_verified_text`。
   - 通用模式切换只允许绑定画面中底部紧凑、独立、明确从 `chinese_pinyin` 指向 `direct_latin` 的按钮；一次切换后必须重新观察，普通字母键、退格键仍禁止成为 Qwen 动作目标。
7. 本轮卖家预览取景修复第二阶段：
   - 动作后证据证明窗口内控制条停放不能清除已绘制的 `PX/MM` 黑色提示。
   - 鼠标现在优先停放到卖家窗口之外的虚拟桌面角落，以触发真实 mouse-leave；只有卖家窗口覆盖整个虚拟桌面时才回退到底部控制条。
   - 该变化只移动电脑鼠标，不点击或操作手机；需要重载网页服务后用只读观察验证真实效果。
8. 本轮输入法机器人误报防护：
   - v9 清空专用精查曾把输入法预编辑栏右侧的蓝色机器人图标误报为 `clear_local_text`；该观察缺少 `focused=true` 和键盘状态，现有执行门已拒绝，`physical_actions=0`。
   - `clear_local_text` 现在必须在 `label` 逐字保存真实可见的 `×/✕/✖/x` 图形，空 label 或自由描述“圆形叉号”不能授权。
   - 被绑定输入框必须同时具备唯一、非空、目标相关、`focused=true` 和当前 `keyboard_layout` 证据；输入法预编辑区不能仅凭邻近图标冒充页面输入框。
   - `ui_scene`、通用动作控制器和通用策略三层独立复查；观察器升级为 v10，正式动作协议升级为 v7，通用策略升级为 v6。

以上提交均只在本地，未推送 GitHub。

## 4. 最近验证结果

代码与协议回归：

- Python 全量测试：`793/793` 通过。
- 前端与浏览器协议测试：`25/25` 通过。
- `git diff --check` 通过。
- 自动测试只能证明代码规则未回归，不能代替真机验证。

最近一次真机文字输入暴露的问题：

- 目标文字曾是 `Agent123`。
- 手机输入框实际出现 `agent.com`。
- Qwen 的动作后截图正确看到了 `agent.com`；错误来自旧验收逻辑只检查“画面变化”，不是 Qwen 看错。
- 该错误促成提交 `b7b9832`，现在缺少精确文字证据或文字不匹配都会失败，不能提升能力。
- 错误动作后的截图证据：
  `poc\output\web\capability_acceptance_86499e6ed51341b0b062a41e57c1c063\qwen_visual_revision_1_3c22e5ffe5ff4507b3fde7902d63148c_after_attempt_1_4.jpg`

本轮清空真机结果：

- 通用会话：`9cf5325e6bb74d9d9d9d04e5268546ac`。
- 精确作用域确认后只执行 1 次物理点击，没有自动重试。
- 原会话的动作后自动观察因一次协议非法字段和一次低置信而标记失败，不能直接作为成功证据。
- 随后独立只读观察取得新指纹 `d7ed81310d297e8e213a`，结构化证据为 `states.value==""`、`focused=true`、`keyboard_layout="qwerty"`，场景置信度 `0.95`。
- 动作后截图：
  `poc\output\web\generic_supervised_20260814_125917_9cf5325e\qwen_visual_revision_1_996fe0eaa5c94a8a803225734c123601_after_attempt_2_4.jpg`

本轮第一次正式输入验收草稿：

- trial：`ae2f7044d5ff465cb8653f736dfa1c00`。
- `physical_actions=0`，没有执行输入。
- 安全停止原因：卖家预览的 `PX/MM` 黑色提示遮住顶部输入框边界，Qwen 没有建立可信 input 候选。
- 该失败促成通用鼠标停靠点修复；必须在新提交基线重启网页服务并确认遮罩消失后，重新创建全新 trial，不能复用旧 trial。

本轮正式输入验收结果：

- trial：`bd66605343b5478caa8068d01be9f3c0`。
- 代码基线：`042464f8d5ef1ae908b672869a581aa6e405b6bd`。
- 精确确认前是唯一空白、已聚焦、`keyboard_layout="qwerty"` 的目标输入框；当时协议尚未区分输入语言模式。
- 精确作用域确认后只执行 1 次 `input_verified_text`，`physical_actions=1`，没有自动重试，没有搜索、提交、发送、保存或发布。
- 动作后两轮结构化观察都得到 `states.value=="a'gen't"`，与 `agent` 不匹配；报告状态为 `failed`，动作结果为 `mismatched`，没有产生 promotion scope。
- 新的独立只读观察再次确认当前真实值为 `a'gen't`、`focused=true`、`keyboard_layout="qwerty"`，指纹 `f91b192e7eb0ae42d331`。
- 根因不是模型误读，而是中文拼音 QWERTY 会产生组合分词文本；这次失败促成 `keyboard_input_mode` 的通用协议与三层前置阻断。
- 动作后证据截图：
  `poc\output\web\capability_acceptance_bd66605343b5478caa8068d01be9f3c0\qwen_visual_revision_1_13b6c6c18b3a4beea8d1d9722d671d01_after_attempt_2_4.jpg`

本轮失败后的只读恢复观察：

- v9 只读观察指纹 `0f11eb6017c224a395ad`，正确取得 `states.value=="a'gen't"`、`keyboard_layout="qwerty"`、`keyboard_input_mode="chinese_pinyin"`，并识别底部“中”模式切换键从 `chinese_pinyin` 指向 `direct_latin`；`physical_action_requested=false`。
- 清空专用精查指纹 `5258c5a299342e13e3de` 误把输入法蓝色机器人图标当作清空叉号；输入元素位于键盘预编辑栏，且缺少 `focused=true` 和 `keyboard_layout`，所以本地门未放行，实际 0 物理动作。
- 该误报促成 v10/v7/v6 的逐字 `×` 图形、聚焦、键盘事实和三层复查；重新加载服务后必须先用 0 动作观察证明机器人图标不再成为清空候选。
- 卖家黑色提示诊断：窗口外停放和 `WM_MOUSELEAVE` 都不能清除主画布内黑块；窗口本体抓图可去掉 `PX/MM` 白字但仍保留黑色背景。当前不能把这项取景修复宣称真机完成。
- 服务加载 v10/v7 后，新的清空专用只读精查被场景协议拒绝：模型仍把 `states.local_text_clear` 放到了不允许的元素角色上，HTTP 422；`physical_action_requested=false`，没有任何物理动作。这证明误报不能再进入可执行候选，但当前自动恢复路径也无法取得真实 `×` 证据。

## 5. 当前实时运行状态

2026-08-14 最后一次只读检查结果：

- 网页服务：`http://127.0.0.1:8765/`
- `/api/device`：可访问。
- `controller_online=true`
- `camera_online=true`
- `busy=false`
- 没有可继续执行的活动任务；失败 trial 已暂停并失效，旧确认和旧观察不可复用。
- 当前设备：`device-local-01`
- 卖家窗口：`智联新途AI机械臂控制端 v1.0.1018`
- 已验证真机能力：`back`、`dismiss_overlay`、`home`、`swipe`、`tap_semantic`、`wait_for_change`
- 尚未提升的能力：`input_verified_text=false`、`long_press=false`、`drag=false`
- 网页服务当前已加载正式动作协议 `2026-08-14-universal-action-v7`、观察器 `2026-08-14-generic-scene-observer-v10`、通用策略 v6。

不要重启、关闭或代替用户启动 `main.exe`。

`main.exe` 路径：

`D:\main软件发客户-20260705\main实时点击控制软件\main.exe`

用户明确要求以后由用户手动打开 `main.exe`。项目代码或新对话不得自行启动、停止或重启它。

## 6. 当前手机画面停点

最近一次真实画面显示：

- 手机处于系统设置搜索页面。
- 页面顶部搜索输入框保持焦点，但实际值为 `a'gen't`。
- 独立只读观察给出 `states.value=="a'gen't"`、`focused=true`、`keyboard_layout="qwerty"`。
- 画面中的中文候选与拼音分词证明当前不是可验收的英文直输状态。
- 卖家预览顶部仍残留 `PX/MM` 黑色提示；新“窗口外停放”代码尚未由网页服务加载和真机验证。

以上是最近证据，不是永久事实。新对话必须先重新拍摄并由 Qwen 观察当前画面，不能直接假定手机没有变化。

此前尝试通过通用目标清空文字时：

- Qwen 一次选择了输入框本身，只会聚焦，不会清空，未执行物理动作。
- Qwen 一次选择了键盘删除键，本地安全策略正确拒绝，未执行物理动作。
- 所有失败尝试的 `physical_actions=0`。
- 提交 `af1a149` 后，正确方案应是识别并点击独立 `clear_local_text` 控件，而不是键盘删除键。

## 7. 下一步唯一主线

接下来严格按以下顺序执行，不要并行控制机械臂：

### 第一步：加载新基线并只读验证

1. 提交当前通用修复，确保工作区干净。
2. 只重载 Uvicorn 网页服务，不得触碰 `main.exe`。
3. 检查 `/api/device` 确认 v9/v6、设备在线且不忙。
4. 通过 0 物理动作的通用观察验证输入法机器人不再成为 `clear_local_text`；黑色提示仍遮挡时不得靠猜测点击。

### 第二步：恢复可验收输入状态

1. 当前已进入人工恢复边界：请用户在手机上手动只使用顶部搜索框自身的可见清空控件，把 `a'gen't` 清空；不要搜索、不要点“取消”、不要切换输入模式。用户回复“已清空”后，先做只读观察确认 `states.value==""`，不得直接相信口头状态继续动作。
2. 新观察必须同时报告 `keyboard_layout="qwerty"` 和 `keyboard_input_mode="chinese_pinyin"`，并唯一识别底部本地模式切换按钮。
3. 通过一次独立 `tap_semantic` 把模式从 `chinese_pinyin` 切到 `direct_latin`；动作后重新观察，禁止同一确认继续输入。
4. 只有新证据确认 `states.value==""`、`focused=true`、`keyboard_layout="qwerty"`、`keyboard_input_mode="direct_latin"` 时，才进入正式验收。

### 第三步：完成安全文字输入真机验收

1. 使用低风险测试文字：`agent`。
2. 前置条件必须同时成立：输入框为空、已聚焦、目标唯一、键盘为 QWERTY、输入模式为 `direct_latin`、画面稳定。
3. 通过正式 capability acceptance 路径执行一次 `input_verified_text`。
4. 不允许自动重试，不允许把测试内容改成大写、数字、中文、符号或 `.com`。
5. 动作后重新观察；必须取得结构化证据 `states.value=="agent"`。
6. 若缺少 `states.value`、文字不匹配、键盘布局错误或画面身份变化，验收失败且不得提升能力。

### 第四步：单独决定能力提升

1. 只有真机报告通过且证据完整，页面才能出现能力提升条件。
2. 提升是独立的零物理动作操作，不能和输入动作共用一次确认。
3. 在执行提升前向用户展示结果，并取得对提升 `input_verified_text` 的单独明确确认。

## 8. 物理动作安全规则

每次真实机械臂动作前必须：

1. 完整读取 `项目最终目标.md` 和本文。
2. 检查 `/api/device`。
3. 确认 `device_id=device-local-01`。
4. 确认 `controller_online=true`、`camera_online=true`、`busy=false`。
5. 确认没有其他活动任务或会话。
6. 获取新画面、新 `observation_id` 和新 `fingerprint`。
7. 确认 Qwen 只提出一个动作，且目标、角色、状态和 bounds 全部绑定当前可信观察。
8. 使用包含 task、device、revision、subgoal、observation 和 fingerprint 的精确确认作用域。
9. 一次确认最多执行一个物理动作，不自动重试。
10. 动作后重新观察并保存证据。

账号或外部状态动作，例如发送、评论、关注、点赞、加好友、拉群、保存设置或删除外部数据，必须先展示草稿并取得用户明确确认。普通安全导航和已授权的本地纠错仍必须遵守一次一动作和重新观察。

## 9. 禁止事项

- 不得为设置、微信、抖音或某张截图增加固定坐标补丁。
- 不得让 DeepSeek 输出机械坐标、Shell 命令或连续动作序列。
- 不得让 Qwen 直接调用机械臂。
- 不得绕过确认作用域调用底层 `robot_core` 兼容入口。
- 不得使用键盘退格键作为当前清空方案。
- 不得在符号键盘上尝试输入 `agent`。
- 不得把 `keyboard_layout="qwerty"` 当成 `keyboard_input_mode="direct_latin"`。
- 不得在 `chinese_pinyin` 或输入模式未知时尝试输入 `agent`。
- 不得因自动测试通过就宣称真机输入能力完成。
- 不得同时让两个对话、Worktree 或服务控制同一机械臂。
- 不得自行打开、关闭或重启 `main.exe`。
- 不得在用户当前“仅本地提交”的要求下推送 GitHub。
- 不得把 Token、API Key 或控制令牌写进仓库、日志或交接文档。

## 10. 关键代码位置

通用任务和协议：

- `poc\deepseek_task_graph.py`：DeepSeek 动态任务图与风险协议。
- `poc\qwen_visual_decision.py`：Qwen 唯一下一动作协议。
- `poc\ui_scene.py`：结构化页面、元素和状态。
- `poc\generic_scene_observer.py`：真实画面观察和 `states.value/keyboard_layout/keyboard_input_mode/local_text_clear`。
- `poc\universal_agent_orchestrator.py`：通用任务编排、安全策略和确认作用域。
- `poc\universal_action_controller.py`：动作后精确验证。

真机执行与能力验收：

- `poc\generic_action_adapter.py`：唯一通用机械臂动作适配入口。
- `poc\robot_core.py`：设备、摄像头、标定和受能力门禁保护的底层动作。
- `poc\capability_acceptance.py`：真机能力验收规则和报告。
- `poc\capability_acceptance_runtime.py`：验收会话、一次动作和独立提升流程。
- `poc\generic_supervised_runtime.py`：受监督通用单步执行。

网页与证据：

- `poc\web_app.py`：FastAPI 接口、设备锁和网页服务。
- `poc\static\app.js`：网页界面。
- `poc\output\web\`：动作前后截图与报告。
- `poc\data\tasks.db`：任务记录。

## 11. 回归测试命令

Python 全量测试：

```powershell
Set-Location 'C:\Users\Administrator\Documents\Claude\Projects\手机自动点击器\poc'
.\.venv\Scripts\python.exe -m unittest discover -p "test_*.py"
```

前端和浏览器协议测试使用 Codex bundled Node；不要因为系统 Node 缺少 Playwright 就擅自安装依赖：

```powershell
Set-Location 'C:\Users\Administrator\Documents\Claude\Projects\手机自动点击器'
$env:NODE_PATH='C:\Users\Administrator\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\node_modules'
$node='C:\Users\Administrator\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe'
& $node --check poc\static\protocol_adapter.js
& $node --check poc\static\app.js
& $node --test poc\test_frontend_protocol.js poc\test_frontend_browser_contract.js
```

提交前：

```powershell
git diff --check
git status --short
```

## 12. 新对话建议首条指令

把下面这段话发送给新对话即可：

> 请先完整读取根目录 `项目最终目标.md` 和 `项目交接文档.md`，然后只读检查当前 Git 状态和 `http://127.0.0.1:8765/api/device`。不要启动、停止或重启 `main.exe`，不要推送 GitHub。当前主分支已集成通用任务闭环、通用动作验收和视觉稳定性三个工作树成果，正式版本为观察器 v11、动作协议 v8；`input_verified_text`、`long_press`、`drag` 仍未真机晋级。最新正式输入 trial `bd66605343b5478caa8068d01be9f3c0` 已失败且不可重试。用户截图已经证明机械臂控制窗口始终可见；先前 controller/camera offline 是 Web 端重载后的短暂窗口发现失败，随后已自行恢复为在线、空闲、无活动任务。当前真实阻塞是顶部输入框视觉证据不稳定：独立 0 动作观察曾确认 `value=""`、已聚焦和真实“中”模式键，但正式受监督新画面只稳定识别到键盘及模式键，没有同时证明唯一空白、已聚焦的中文拼音 QWERTY 输入框，因此本地策略拒绝生成 confirmation scope。不得绕过该门禁。下一步应先解决预览顶部黑色遮挡/裁切或输入框审计一致性，再重新创建全新模式切换会话；只有同一新 fingerprint 的完整证据齐全时，才申请一个动作切到 `direct_latin`。不得增加 App 专用流程或固定坐标。

## 13. 交接结论

三个并行工作树已经全部完成并由主任务审查、合并：`5612c3a` 强化通用任务闭环，`4ee8660` 强化通用动作验收，`61f073a` 将观察器升级至 v11；主任务随后提交 `542043d`、`ebdbb9a`、`e52597c`、`efb005c`，只对已知键盘枚举做安全归一化、增加脱敏诊断，并忽略非目标外围键盘容器上的无效元数据。未知枚举和目标错误结构仍失败关闭。主仓库在前三项集成及首次枚举修复后 Python 全量 `823/823`、前端与浏览器协议 `25/25` 通过；最终外围容器修复后相关视觉/协议测试 `169/169` 通过，`git diff --check` 通过。网页服务已实际加载观察器 v11、动作协议 v8，三项未验收硬件能力仍为 false。

所有本轮真机请求均为只读观察，未执行机械臂动作。v11 首次真机诊断确认 Qwen 会额外生成非目标 `container=kb_layout_qwerty` 并错误附带 `keyboard_layout`；该通用外围元数据问题已经修复。用户随后提供截图证明控制窗口一直可见，`/api/device` 重新检查也已恢复 `controller_online=true`、`camera_online=true`、`busy=false`，所以不得再把 `MainWindowTitle` 为空解释成控制窗口已关闭。修复后的独立 0 动作观察成功返回顶部应用输入框 `value=""`、`focused=true`，并识别真实“中”键方向 `chinese_pinyin -> direct_latin`；输入法机器人没有成为目标。

正式受监督会话 `b8ba6f9182f2429fb925f5ed28d3f5b8` 生成了正确的单一 `tap_semantic` 模式切换候选，但同一次可信观察没有稳定识别顶部输入框，也没有把空值、焦点、QWERTY 和中文拼音事实绑定到唯一 input；本地策略以“键盘模式切换要求唯一空白、已聚焦的中文拼音 QWERTY 输入框”阻断，`confirmation_scope=null`、`physical_actions=0`，随后会话释放设备并拒绝重复观察。当前真实阻塞是预览顶部黑色区域导致输入框结构在不同观察间不稳定，不是控制窗口关闭。下一步先解决这一通用视觉证据问题，再创建全新会话；禁止复用旧 fingerprint、旧候选或绕过控制器。能力提升仍需正式报告通过并取得用户单独明确确认。本地分支当前未推送。

## 14. 2026-08-14 触控落点偏差新诊断与安全修复

用户观察到输入法模式切换实际落到相邻句号键。失败会话 `fd9cdaffe41a41d8b71051e549afbd84` 的正式候选中心为 `(0.735, 0.91)`，视觉上位于目标模式键；动作后输入框出现 `。` 且输入模式没有改变，证明不能把这次失败解释为用户看错。该动作只执行 1 次，没有自动重试。

旧九点标定的最下排相机坐标约为 `y=1062/1440=0.738`，而模式键中心约为 `y=0.91`。旧验证 RMS `1.7499 CSS px`、最大误差 `3.8046 CSS px` 只证明中上区域准确，不能证明底部小按键；底部属于明显的仿射外推区。这也是不同位置会出现偏左、偏右或偏上的主要原因之一：相机透视、手机与机械臂平面不完全平行、连杆回差和触控笔斜压会产生位置相关误差，而不是一个固定偏移量。

当前通用修复：

- `tap_calibration.py` 升级为标定 v2，保存实际采样点的凸包覆盖范围；已启用标定若缺少覆盖信息或目标位于凸包外，直接拒绝外推点击。
- 中部小范围九点即使残差很低也不再被接受；覆盖至少需要横向跨度 `0.68`、纵向跨度 `0.82`。
- 校准页改为边缘九点并尝试标准/Android WebKit 全屏接口；校准脚本先用一个独立中央触点进入全屏，再逐点重新观察采集九点，并保守记录失败准备动作。
- 通用导航语义新增 `refresh/reload/刷新/重新加载`，策略版本为 `2026-08-14-universal-action-policy-v8`，不是浏览器或 App 专用流程。
- 已将当前旧标定设为 `enabled=false`、`validated=false`、`accepted_fit=false`，禁止继续使用只覆盖到约 74% 高度的矩阵。

真机证据：通用会话 `321a964974a0458ca918938d4c2d48f1` 在新观察 `obs_78645ce256714dfc91f92c94a5608d0b`、fingerprint `4f6addf8e5b5f4bf6241` 上唯一识别浏览器刷新控件，策略 v8 放行后执行 1 次，卖家返回落点 `[676,36]`。动作后 fingerprint 变为 `83a0767f527f9ae470cb`，画面明确出现中央品红十字靶标；报告把它标成 `mismatched` 是语义验证器没有认可目标元素变化，不代表刷新失败。

随后中央全屏准备靶点执行 1 次，页面回传仍为 `phase=fullscreen_setup`、`fullscreen=false`，九个采样点均未执行。标准全屏 API 被当前手机浏览器拒绝；WebKit 兼容代码已加入并通过自动测试，但尚未通过再次页面加载完成真机验证。禁止在这一步失败后自动重复中央点击。

回归结果：Python 全量 `840/840`，前端与浏览器协议 `25/25`，`git diff --check` 通过。当前控制器和相机在线、设备空闲、无活动会话，`main.exe` 未启动、停止或重启。继续主线时先让当前校准页加载最新 WebKit 兼容代码，再做一次中央全屏准备验证；只有进入全屏后才允许采集边缘九点并独立验证，之后才能重新验收底部输入法模式键。

## 15. 2026-08-14 系统导航唤出正式验收与当前恢复边界

主线已在提交 `00d7e26` 加入独立系统 UI 只读审计，观察器升级为 `2026-08-14-generic-scene-observer-v13`。只有目标明确涉及系统导航栏、全屏或沉浸状态时才额外审计同一稳定帧的原始、顺时针和逆时针方向；审计只允许两个布尔事实、置信度和一至两条无坐标/无动作证据。普通观察返回非法布尔字符串时只降级为 `unknown`，不能直接授权动作；独立审计允许一次严格格式修复，仍有 `unknown`、低置信、对象证据、坐标或动作字段就失败关闭。Python 全量 `921/921`、前端协议 `25/25` 和 `git diff --check` 均通过，8765 已实际加载 v13，`main.exe` 未启动、停止或重启。

正式 trial `3580ec4ca24f4797b067bc32ba6022a9` 的零动作阶段成功：

- 新观察 `obs_f92c115a6f594b13875ac3f6221455a2`、fingerprint `17b045a1e904938d0c69`，结构化事实为 `immersive_or_fullscreen=true`、`navigation_bar_visible=false`。
- Qwen 只提出无坐标、无方向和无距离的 `reveal_system_navigation`，目标区域为 `system_navigation`，预期效果严格为 `system_ui.navigation_bar_visible=true`，本地策略 v11 放行，确认作用域完整绑定 trial、task、device、revision、subgoal、observation 和 fingerprint。
- 动作前设备为 `device-local-01`，控制器/摄像头在线、空闲、无队列或其他会话；确认只消费一次，没有自动重试。

真机动作执行 1 次后正式失败，不得提升或复用该 trial：

- 本地 v2 标定生成的 DOM 轨迹为约 `[(0.5001,0.9526),(0.4996,0.7025)]`，控制端 client 轨迹为 `[(76,699),(254,698)]`。
- 校准页把起点当成 App 内触摸，页面 `sequence` 从 `3` 变为 `4`，记录实际 pointerdown 约 `(441,373)`；说明系统没有接管边缘手势。
- 动作后新观察 `obs_b84fc6f2a2394a01ab801b1952dc41ba`、fingerprint `81a38f257bb566fa2638`，仍为 `immersive_or_fullscreen=true`、`navigation_bar_visible=false`；报告 `action_outcome=mismatched`、`physical_actions=1`、status `failed/blocked`。
- 证据目录为 `poc\output\web\capability_acceptance_3580ec4ca24f4797b067bc32ba6022a9\`。`reveal_system_navigation` 仍未晋级，不能再次自动尝试。

对“把起点向真实底边外推最多 0.03”的候选修复已经主任务和独立工作树共同否决并撤销：名义纠偏起点约在 client `x=55`，而当前显示边缘约 `x=60`，只有约 5 px 余量；采集最大误差 `13.8115 px`、独立验证最大误差 `7.1686 CSS px`，还没有计入触控笔半径、机械回差、边框高度差和安全缓冲。两个凸包只能证明包内插值，不能证明包外玻璃边界或外推精度。当前 Git 工作区因此保持无该外推改动。

另一个已确认的通用根因是卖家控制端布局方向和手机内容方向不一致：失败动作时控制端 client 为竖向 `810x1515`，手机内容却是横屏；历史成功的系统返回使用横向相机布局。主任务通过完整窗口截图和底栏 OCR 唯一识别“横屏”切换控件，只点击卖家控制软件一次，没有触摸手机；控制端随后变为 `1440x810`，而校准页 `sequence` 保持 `4`，控制器/摄像头继续在线、空闲。当前页面仍是 `phase=calibration`、`fullscreen=true`、viewport `873x393`。卖家底栏 `H/B` 没有可访问性控件树，公开资料也未证明其语义，因此尚未点击，不能假定 `B` 是不落笔的原生 Back。

当前有三个离线工作树继续推进且均禁止控制真机、服务或 `main.exe`：

1. 通用检测“控制端相机布局方向 vs 手机内容方向”不一致，并在 back/home/tap/drag 前失败关闭。
2. 设计带真实可触控玻璃/显示边界、残差、触控笔半径、回差和安全缓冲的 v3 边缘证据；现有缺证据的 v2 不能据此外推。
3. 只读确认卖家控制端 H/B/圆点/横屏/输入的真实语义，证据不足就否决。

继续主线时先验收这三个离线结果。没有可靠的原生 Back 证据或 v3 真实边界证据前，不得再次执行系统边缘手势、隐藏导航栏 Back/Home 或直接点击 H/B。`input_verified_text`、`long_press`、`drag` 仍未真机晋级；只有安全退出当前全屏校准页并重新准备输入/动作验收页后才能继续。所有提交仍只在本地，未推送。

## 16. 2026-08-14 离线安全审查、卖家控件语义与当日冻结状态

### 16.1 两项上游安全整改的审查结论

- `8adad686`（相机布局方向与手机内容方向不一致时失败关闭）和 `d6605fa`（系统边缘手势要求实测边界）在首轮交叉审查中均为 **REJECT**。它们尚未达到可合入主线的安全标准，不能因各自测试通过或组合后没有文本冲突而改变该结论。
- 从 `00d7e26` 依次整合这两个提交形成的实验组合，最终实验提交为 `1cd9b3c`。该组合只用于暴露协议、验收和入口之间的交互缺口，明确 **不可合并到主线**；不得把它视为对两个被拒上游实现的间接批准。
- 实验组合发现并加固的通用内容，在 A/B 两项上游安全整改重新审查通过后可以择取复用：按动作读取和传递标定证据；v3 双批次独立边界证据校验；动作回执与测量 ID、误差包络及标定版本绑定；验收报告对相机方向、置信度、真实帧方向和非等比漂移的独立复核；专用唤栏动作恰好调用一次且失败后不回退 `H/B`、`back`、`home` 或普通 `swipe` 的负向测试；scene 协议版本入口测试和独立 lease 测试方式。
- 不可直接复用的部分包括两个被拒提交本身、任何未经证明的卖家 `H/B` 语义映射，以及尚未完成正式真机验收的系统边缘物理执行实现。

### 16.2 卖家 v1.0.1018 底栏静态逆向结论

- 对“智联新途AI机械臂控制端 v1.0.1018”的安装内容、模块和二进制/资源进行纯静态只读检查后，可以确认底栏 `H`、`B` 和“输入”都进入机械动作路径，不是无接触的 Android 原生系统键协议。
- `B` 不能视为不落笔的原生 Android Back；禁止把它作为系统返回、退出全屏或系统导航唤出的无接触回退入口。
- “横屏/竖屏”是单选布局控件，其切换可能同时带来机位回中副作用。没有新的现场证据和用户逐次明确授权前，不得把它当作无物理副作用的普通显示旋转操作。

### 16.3 后续 UI Automation 精确证据

- 后续主控通过 Windows UI Automation 精确定位到卖家窗口中唯一的“竖屏”控件；这是可访问性控件定位结果，不是 OCR 文本猜测或截图坐标补丁。本节的后续精确状态取代第 15 节中较早的 OCR/方向状态描述。
- 在用户明确要求后，该控件只触发 1 次。卖家控制端 `client_size` 从 `1440x810` 变为 `810x1515`，同一位置的按钮文字变为“横屏”。
- 操作后 `helper samples` 仍为 `4`；控制器和摄像头保持在线，`busy=false`，没有活动任务。
- 这些事实只证明卖家控制端布局状态发生了上述变化，不证明手机内容已经转正，也不证明机械坐标、相机取景或网页方向已经一致。

### 16.4 当前冻结状态与未验收边界

- 当前校准网页内容仍呈横向/侧转状态。用户决定于明天到现场查看，因此今天冻结所有页面、真机和机械臂动作；不得刷新、切页、退出全屏、再次切换横竖屏、执行系统边缘手势或尝试任何恢复动作。
- A（方向一致性失败关闭）和 B（v3 实测边界及误差包络）两项整改继续保持纯离线开发与审查，未通过新的独立安全审查前不得进入真机。
- `input_verified_text`、`long_press`、`drag` 仍未完成正式真机验收；退出当前全屏状态同样尚未验收。以上能力和恢复步骤都不能标记为已完成。
- 当日冻结期间不得启动、停止或重启网页服务及 `main.exe`，不得控制机械臂，不得复用旧 trial、旧确认、旧观察或旧 fingerprint。

## 17. 2026-08-15 方向安全集成与用户恢复后的新边界

### 17.1 离线集成结论

- 用户已在现场自行恢复手机页面，确认此前异常是手机进入全屏状态；第 16.4 节的“当日冻结”因此结束，但不构成对任意恢复动作的通用授权。
- 用户明确要求：后续不得再点击全屏、横屏、竖屏或其他卖家窗口布局控件。以上控件进入禁触范围；通用动作真机验收只能操作手机页面内、由当前可信观察唯一识别的目标。
- 用户无法在手机浏览器中手工输入网址。后续验收页应由 Agent 通过已验收的通用动作自行准备；不得要求用户手工输入 URL，也不得为浏览器增加固定坐标、固定网址步骤或专用流程。
- 相机布局方向门禁、动作时独立方向审计、live-trial promotion 来源绑定和单次 compact JSON 严格本地修复已经过独立集成审查，本轮现场修复前的集成 HEAD 为 `232b462a9c76bab12df87e1a851c88fdfbd9001e`。
- 新门禁要求真实 `device_id`、当前帧尺寸和稳健视觉绑定一致；每次物理动作前独立采集三张方向审计帧，普通观察仍只使用一张画面。方向未知、低置信、内容旋转、设备或帧尺寸漂移、旧授权重放均在机械臂调用前失败关闭。
- 能力提升必须绑定同一进程内真实动作结果和一次性的 `OrientationCredential`。仅凭序列化报告、复制对象、旧进程报告、修改后的报告或并发重复请求不能写入能力注册表；确认快照写入失败会立即使授权失效。
- 旧 App 专用多步入口已禁用，不能绕过通用单动作路径。先前被拒绝的 v3 边缘手势提交 `bc372f0` 没有合入；`reveal_system_navigation` 继续保持未验收和禁用状态。

### 17.2 主工作区离线验证

- 方向、能力验收、通用观察、通用规划/动作、通用编排和网页 API 组合回归在主工作区通过 `470/470`。
- 前端协议通过 `18/18`，浏览器协议通过 `7/7`，合计 `25/25`；两个 JavaScript 文件语法检查通过。
- 独立集成树完整 Python 回归共 `936` 项，`932` 通过；仅剩三张仓库未携带的历史图片造成的既知 `2 failures + 2 errors`，没有新增代码失败。
- `git diff --check` 通过。所有测试没有占用默认机械臂 lease；测试期间没有执行真机动作，也没有操作 `main.exe`。既知的测试子进程 `ResourceWarning` 仍然存在，但不影响本轮断言结果。

### 17.3 继续真机验收前的当前状态

- 截至本次 v17 提交前，运行中的 8765 服务已加载观察器 v16、scene v3、动作协议 v10；提交后仍必须只重载 Uvicorn 并确认 v17，不得操作 `main.exe`。
- 控制器和摄像头在线、设备空闲、没有活动任务或会话。`input_verified_text`、`long_press`、`drag` 和 `reveal_system_navigation` 仍为 false，不能把离线集成通过写成真机能力已完成。
- 下一步只允许安全刷新 8765 网页服务，不得触碰 `main.exe`；加载 v17 后重新检查设备、会话、观察和 fingerprint，再按“一次动作、重新观察、验证结果、失败不重试”的顺序逐项验收。
- 页面准备也必须遵守同一单动作闭环。若进入目标页必须依赖尚未晋级的输入能力，应先在当前安全页面完成 `input_verified_text` 正式验收；不能通过底层输入、ADB、卖家“输入”按钮或固定坐标绕过门禁。

### 17.4 浏览器实机导航与 compact 观察修复

- 用户恢复竖屏后，新会话 `70e23d0907ff49afb62268da4a9c00f2` 在新观察 `obs_9bf3d47b921341f79bddb6be0f9770f0` 上唯一识别启动器中的浏览器图标。方向审计为 upright，确认后只执行 1 次 `tap_semantic`，没有自动重试。
- 动作后证据图已确认浏览器成功打开且仍为竖屏。正式会话因动作后 compact JSON 没有唯一严格的单结构标点修复而报 failed；这只是后验观察失败，不是浏览器点击失败。证据位于 `poc\output\web\generic_supervised_20260815_161143_70e23d09\`。
- 随后两次高层页面准备请求被 DeepSeek 低层动作表达门禁拒绝，物理动作均为 0；改为合法高层目标后，浏览器复杂首页的动作前 compact JSON 再次失败，仍为 0 物理动作。失败目录为 `poc\output\web\generic_supervised_20260815_161836_d5df3725\`。
- 现场服务该次调用的 completion tokens 恰好为当时配置上限 `800/800`，这强烈提示输出长度不足，但因证据目录没有保存 `finish_reason` 和原始响应，不得把它写成已确证的截断。
- 观察器 v18 将单次 compact 输出预算从 800 提高到 1200，并把首轮元素严格限制为最多 6 个，必须优先目标相关控件和输入框，单元素只允许一条短证据；同时允许在 JSON 解析错误位于 EOF 时严格枚举一个结构标点。只有恰好一个候选通过去重复键、compact schema 和完整 `_parse_scene` 时才接受；仍然只调用一次 compact 模型，不会远程重写响应，多字符缺失、重复键、错误 schema 或多候选继续失败关闭。
- 当前浏览器顶部搜索框内有旧文字，键盘未出现，右侧四角图标是全屏而不是清空按钮。下一步只能通过新观察唯一绑定左侧输入区域；全屏图标、横屏/竖屏、卖家底栏、输入按钮和固定坐标全部禁止。
