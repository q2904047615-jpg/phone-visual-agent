# 手机自动点击器项目交接文档

更新时间：2026-09-03（Asia/Shanghai）

本文件只保留当前权威状态。历史过程查看 Git 和 `历史文档索引.md`，不得把旧交接、旧协议或旧输出恢复为运行权威。

## 0. 新对话快速接手

- 当前待验收生产版本是本文件所在的本地提交，分支为 `codex/integrate-phase2-stage3`；未推送。退役 Android Companion 源码的修改另存于只读归档分支 `codex/companion-ime-retired-archive` 的 `bc1d879`，不进入生产集成。
- 当前唯一协议为 action `2026-09-03-canonical-recents-home-clear-v5`、DeepSeek task graph `2026-09-03-deepseek-required-action-v6`、Qwen `2026-09-03-qwen-required-action-v14`、single-step observation `2026-09-03-single-step-required-action-v10`。
- 用户于 2026-09-02 明确决定以 ADB Keyboard 替换 Visual Agent Companion IME。生产组合根、文字合同、执行器、文档和测试已切换；Companion bridge/配对/setup Python 入口及测试已删除，项目 API 不再监听 `18766`。ADB Keyboard 现在只允许固定设备/serial/IME 的只读 ready 预检和 `ADB_INPUT_B64`、`ADB_CLEAR_TEXT` 两种广播；`dumpsys`、foreground identity domain/provider 及覆盖 Qwen 当前截图结论的入口已全部从 Python 正式运行链删除。
- 当前待验收源码的完整回归为 Python 727/727、前端 33/33；受影响模块 296/296，语法检查和 `git diff --check` 通过。上述均是离线/本地证据，不替代真机最终验收。
- 无线 ADB serial `192.168.0.162:5555` 已在拔除 USB 后独立在线；设备已有 `com.android.adbkeyboard`，`com.android.adbkeyboard/.AdbIME` 已启用并选为默认输入法，Visual Agent Companion IME 已禁用。下载的 v2.4-dev APK 因与设备现有包签名不一致而未覆盖安装，现有包仍可被 Android 识别为有效输入法。手机重启或局域网 IP 变化后需重新建立/更新无线绑定。
- 项目 Uvicorn PID `22460` 当前仍加载旧提交 `a66b9e6`，尚未加载本文件所在的 Controller v23；卖家 `main.exe` 仍为用户原进程 PID `20736`，本批未启动、停止、重启或点击。此前固定无线 ADB serial `192.168.0.162:5555` 已用可信配置恢复且官方 `doctor.ready=true`，但该运行状态易变，重载后必须重新核对。
- ADB Keyboard 真机会话 `12ad565455184bafb4c3848f0257d87b` 只执行一次 `clear_verified_text`：固定无线 serial 广播 accepted，动作后 `input_structure` 的 `primary_input` 值为 `""`，未发送。原任务会话 `1796b7a954d849caa349348b88247bbf` 先完成一次聚焦和一次 ADB Keyboard 精确输入，再只点击一次发送；发送前后原图中输入框仍保留正文且没有新增气泡，会话按真实结果 `failed`，没有重发。
- 失败会话 `79256e54a60940f48caddc457f42a66c` 是验收设计错误：`exact_input_text` 合同只操作“当前唯一输入框”，但启动时手机处于 Launcher，任务没有可导航目标；Qwen 连续选择 `home`，本地错误地执行 9 次相同 Home。会话没有输入、清空或发送，ADB Keyboard 尚未被真实调用。当前生产守卫会在已处于 Launcher 时于下一次物理动作前停止 `home`，并保留从 App 首次返回 Launcher 的合法动作。
- 会话 `c30c02c4dd0c4efcbc0aa8f94d5a0234` 的 DeepSeek 目标图和 Qwen 选择均正确：Launcher 新截图中 Qwen 唯一绑定微信元素 `e1`，bounds 为 `[0.36,0.586,0.56,0.742]` 并选择 `tap_semantic`；旧 `stable_visual_point_grounding` 却把同一图标的多层颜色掩码误判为多个候选，在设备动作前停止，`physical_actions=0`。该失败证明旧本地表面分割是第二落点否决权，不是 Qwen 未识别目标。
- 官方 ADB Keyboard APK 保留于忽略输出目录 `poc/output/setup/adb-keyboard-v2.4-dev.apk`，大小 17,374 字节、ZIP magic `504B0304`、SHA-256 `E698ADEA5633135A067B038F9A0CF41BAA4DE09888713A81593FB2B9682CDC59`。
- 新对话的第一步只需按下方顺序读取四份文档，然后核对 `git status`、当前源码/OpenAPI、官方客户端 `status`/`doctor`；不得凭本文件中的旧 PID 或截图直接执行。

## 1. 默认必读顺序

1. `项目最终目标.md`；
2. `AGENTS.md`；
3. 本文件；
4. `单一权威最小校验验收台账.md`。

## 2. 产品方向与唯一权威

- 产品是通用多 App 手机视觉操作 Agent，不是固定 App 或固定命令脚本。
- 普通自然语言主链只有：
  `DeepSeek 首次计划一次 → 新截图 → Qwen 同响应 scene/input + 一个 action 或 finish → 必要硬校验 → 执行一次 → 新截图`。
- DeepSeek 不参加动作后的循环；Qwen 普通输出没有 `blocked`；本地不另选动作、不重绑目标、不裁决高层完成。
- `matched` 只证明刚才动作及必要硬校验；只有下一张当前截图上的 Qwen `finish` 推进目标。
- 公开 `exact_input_text`、`exact_action_kind` 是已结构化直推入口，可以跳过 DeepSeek；进入设备循环后仍使用同一 Qwen 单动作链。
- 唯一动作协议：`2026-09-03-canonical-recents-home-clear-v5`。
- 当前 Qwen 响应协议：`2026-09-03-qwen-required-action-v14`；单步观察协议为 `2026-09-03-single-step-required-action-v10`。
- 当前待加载 Controller 协议：`2026-09-03-universal-action-v23`；点按类动作的唯一落点是 Qwen 同帧唯一元素合法 bounds 的中心，本地只做 scope/fingerprint、屏幕范围和机械标定校验，不再运行 OCR/颜色表面分割或第二候选裁决。
- 只有 `authentication` 和 `financial_transaction` 需要用户确认；其他普通合法动作自动执行。

完整产品边界见 `用户决策与协议边界.md`。

## 3. 当前仓库与运行状态

- 分支：`codex/integrate-phase2-stage3`。
- 当前 Git 跟踪的 `poc/agent` 生产 Python 仍低于 20,000 行合同；精确文件数与行数需在提交前从当前工作树重新统计，不沿用旧快照。
- 已物理删除的正式旧实现：
  - `domain/task_semantic_ir.py`；
  - `domain/post_action_observation.py`；
  - `domain/input_value_lineage.py`；
  - `application/input_value_lineage.py`；
  - `infrastructure/file_system_input_lineage_store.py`；
  - `application/prompts/deepseek_replan.txt`；
  - 对应旧测试。
- 生产运行已无 TaskSemanticIR、candidate selector、动作后 DeepSeek replan、动作重绑、App lineage 完成裁决、持久输入 lineage、decision cache 或 scene-only decision 回退。
- 生产运行已无 Visual Agent Companion IME、TLS bridge、配对密钥、editor session、前台上报、setup CLI 或旧注册表入口；ADB Keyboard 是唯一非机械文字 transport。
- 前端只把当前 Qwen v14 `action/finish` 视为可执行视觉合同；旧 Qwen 协议只用于防恢复负样本，不能生成动作授权。
- 2026-09-03 真实任务“打开后台，把微信卡片划掉”的失败会话 `66144bb7c8614de3a5d39f68d4050c92` 共执行 8 个物理动作：一次 `open_recent_apps` 后，旧 canonical `swipe(up)` 被系统最近任务网格解释为列表滚动并重复 7 次，微信卡片始终存在。根因不是 Qwen 未识别微信，而是旧协议没有区分页面滚动与直接操纵卡片，也没有通用元素轨迹重复边界。
- 用户批准后，生产协议已拆为 `scroll` 与 `swipe_element`：后者由 Qwen 同帧给出元素和起终点，本地校验元素内部安全起点、屏幕终点、距离与唯一主方向；同目标近似轨迹先只读纠正一次，仍相同则停止，同目标最多两次且禁止第三次。设备注册表的 `swipe` 仅保留为物理 transport，由本地映射，模型/API 不再暴露旧动作。
- 用户于 2026-09-03 明确决定：`screen_id=system_recent_tasks` 时，移除选中任务卡片只能使用向左 `swipe_element`，上下只能作为 `scroll` 寻找目标。该系统页面语义已成为 v3 唯一权威；不是微信、MIUI、截图或坐标特例。
- typed 输入聚焦本批三个本地代码提交为：`a92af6a`（caret/cursor 聚焦归一化与防重复基础）、`9a62f44`（唯一 Qwen 单步合同先聚焦后输入）、`da3f4a2`（删除可绕过的视觉状态摘要并加入 typed effect 一次物理尝试预算）；`b6d68d9` 只记录验收证据。均未推送。
- 2026-09-03 01:40 后项目 API 已重载当前工作树并执行一次稳定视觉落点真机动作；动作结束后官方客户端确认 `doctor.ready=true`、controller/camera 在线、无线 ADB Keyboard `ready=true`、`physical_actions=0`、无活动任务且设备独占可用。该状态仍是易变事实，后续必须重新读取。
- 2026-09-03 02:23 后项目 API 已在交互桌面权限上下文加载 v3，当前 PID `11812`；官方 `doctor` 再次确认 ready、controller/camera 在线、`physical_actions=0`、设备独占可用且 blockers 为空。卖家 `main.exe` 仍为原 PID `20172`，未启动、停止、重启或人工点击。
- 项目 API 和设备在线状态属于易变运行事实，必须在提交后用官方类型化客户端重新读取；不得沿用历史 PID、会话或截图。
- 2026-09-03 04:12 官方客户端重新只读核对：OpenAPI 已验证、action v3 与 Controller v21 已加载，controller/camera 在线、设备空闲、`doctor.ready=true`、`physical_actions=0`、blockers 为空；卖家 `main.exe` 未被操作。

## 4. 阶段结论与证据边界

内部实现：

- DeepSeek 只调用一次；空 `subgoals` 与可选澄清问题不再把普通合法目标投影成 `blocked`，必要目标 scope 仍必须存在。
- 一个 Qwen 请求直接返回 `(scene, model_decision)`；初始观察与动作后观察都以值传递，生产代码不接受 scene-only 回退。
- Qwen 所选元素必须有唯一 ID、合法角色、语义和 bounds；malformed 可选 states 被丢弃，不再否决合法元素。
- 空白输入框的可选 `goal_relevant=false` 不再覆盖本地同帧聚焦资格。
- 当前唯一 audited typed 输入框内的肯定式 caret/cursor/光标/插入符证据会规范化为 `focused=true`；`soft_keyboard_visible=false` 只说明键盘未显示，不能推翻该焦点。否定式或普通边框/outline 证据不会建立焦点。
- 同一子目标、同一 typed `input_field_id` 的聚焦点击最多执行一次：若 Qwen 仍选同一字段，只允许额外一次只读截图/Qwen 纠正；仍重复则以明确运行故障停止。元素 ID、bounds、光标闪烁、正文、键盘显示/布局/模式等诊断变化均不能重新授权第二次聚焦点击。
- 输入框已经 focused 时，Qwen 再次建议聚焦点击也不会重复执行；Qwen 改为 `input_verified_text` 或选择其他合法动作时正常继续。
- 当前 typed 输入框没有 `keyboard.visible=true`、肯定式 caret/cursor cue 或合法 `caret_line_index` 时，Qwen 必须先选择一次 `tap_semantic`；执行并取得新截图后，才可选择文字、清空或换行动作。输入/清空引用该 typed 输入框，换行引用以 `states.input_element_id` 绑定该输入框的 Enter 键。
- 本地动作可用集合现与该顺序一致：同子目标没有已匹配聚焦回执时不暴露 typed transport 动作；聚焦动作后的观察使用独立动作后集合。Qwen仍选择具体输入元素和动作，本地不把无效清空自动改写成点击。
- 当前 typed effect scope 已执行 1 个物理动作后，下一张新截图若 Qwen `finish` 则正常完成；若仍为任意 `action`，保留该诊断决策但不签发第二次执行 scope，任务以运行失败停止。该预算直接复用既有 `ConfirmationAuthority.effect_ids`，不识别 App、按钮文字、元素 ID、坐标或动作类型。
- 带 `effect_ids` 的动作后若 Qwen 报告 `finish`，本地还要求动作后四帧相对动作前四帧存在可归因的原始画面变化；该门禁排除卖家浅层顶部/底部叠加，并以稳定分块中位差识别局部 UI 变化。它只证明真实帧发生过变化，不裁决变化语义，也不取代 Qwen 的完成权威。
- 终态失败即使发生在 `refresh_decision` 内也会在 `finally` 释放设备租约，避免失败会话继续占用设备。
- typed 文字仍保留当前事务的 field ID、prior/expected、摘要 scope、ADB Keyboard transport 回执和动作后精确正文；只删除跨会话持久视觉 lineage。
- Controller 只执行 Qwen 引用的当前元素或方向，不再按 label/role/states 重选目标。
- `scroll` 只移动页面/可滚动容器；`swipe_element` 直接使用 Qwen 同帧轨迹操纵目标元素。元素滑动起点必须位于目标内部安全区，终点必须在屏幕可执行区，距离与主方向由本地确定性校验；同目标近似重复先进行一次 0 动作纠正观察，仍重复则停止，同一目标最多两个物理元素滑动。
- Qwen 同帧唯一选择目标、动作和元素 bounds；点按类动作由 Controller 直接取该元素中心。旧 `stable_visual_point_grounding`、OCR 精定位、颜色/表面分割、局部候选排序和相应否决入口已从生产域、适配器和组合根删除；动作后仍必须取得新截图，由 Qwen 判断结果或 `finish`。
- 前端已删除 Qwen `blocked`、旧 Qwen v5 执行兼容和动作后 replan 展示链。

自动验证：

- 本轮 typed 聚焦顺序、ADB Keyboard、控制器、观察、网页相关回归：374/374。
- 最终 Python 完整回归：729/729（123.560 秒）。
- Qwen 离线评估模块：13/13。
- 前端完整回归：33/33（unit 17、浏览器合同 12、离线控制台 4）。

真机与服务：

- 失败会话 `9110e19bd9584d0893f77371e7181280` 证明机械点击已落在输入框且画面出现光标，但旧 observer 没有发布 `focused=true`，随后同一字段被重复点击 8 次；它是本批根因样本，不是成功证据。
- 失败会话 `0987cbb1122e483c91904cce4d71d0d3` 的 `physical_actions=0`：Qwen 在未报告聚焦时直接选择文字动作，本地硬校验在机械执行前停止。它是历史聚焦样本，不是当前 ADB Keyboard 证据。
- 真机会话 `85f1c3b573c64f368ffa7375e7e91507` 是已退役 Companion transport 的历史成功证据，不能证明当前 ADB Keyboard 已跑通。
- 同一会话也证明旧守卫仍有缺陷：第 1/2 步对同一 `primary_input` 各执行一次聚焦点击，第 4/5 步对同一 `send_message_effect` 各执行一次发送点击，共 5 个物理动作。因此该会话不是“无重复动作”的干净验收证据，不能把终态成功包装成阶段验收完成。
- 上述真实回放已固化为光标/键盘/元素/bounds 漂移与 `send_message`、`publish_content → swipe` 变化样本；`da3f4a2` 及当时的 710 项完整回归证明第二次物理动作会在执行前停止。本轮自然新任务已进一步证明同字段只聚焦一次、同 effect 只执行一次。
- 失败会话 `79256e54a60940f48caddc457f42a66c` 共执行 9 次 `home`，每次机械回执点为 `[405, 1405]`；无输入、清空或发送。根因是把只适用于当前输入框的 `exact_input_text` 验收从 Launcher 启动，同时幂等系统动作没有执行前守卫；该守卫已经离线修复并回归，本轮自然任务未再出现重复 Home。
- 真机清空会话 `12ad565455184bafb4c3848f0257d87b` 为 `succeeded`、`physical_actions=1`：`ADB_CLEAR_TEXT` 回执 accepted，新截图中输入框为空且发送按钮消失，证明当前无线 ADB Keyboard 清空闭环成立。
- 原任务会话 `1796b7a954d849caa349348b88247bbf` 在新任务自己的逐目标截图中完成微信与文件传输助手识别；一次聚焦后，`ADB_INPUT_B64` 回执 accepted，动作后 `input_structure` 精确正文为 `aaazjie？你好`，证明输入闭环成立。随后唯一一次发送点击未产生新增气泡，输入框也未清空，最终 `failed`、累计 `physical_actions=3`，设备租约已释放。
- 本轮新增真实帧变化门禁重放了两个“无变化”失败样本和一个历史真实变化样本：当前失败发送 `global_median_delta=0.723`、`max_tile_median_delta=0.988`，低于阈值 `2.0`，因此没有再被旧气泡误判成功；历史真实变化样本高于阈值。
- 会话 `a7fa03bb77bc4f59b5d66d7ca564732f` 中，Qwen 正确选择同一 `send_message` 元素；当时 v2 表面框约为像素 `[650,1226,757,1284]`，却把视觉内点放在约 `[716,1280]`，距下边界仅约 4 像素，实际命令回执为 `[719,1274]`。动作后新截图仍保留 `aaazjie？你好` 和发送按钮、没有新增气泡，Qwen 因当前 JPEG 未证明预期结果而继续给出动作；系统据此以 `failed`、`physical_actions=1` 停止且未重发。v3 对同一四帧离线重放得到表面框约 `[650,1226,757,1284]`、安全点 `[704,1256]`，四向内缩约 28 至 54 像素。该结果证明 v2 的“最深内点”实现存在通用边界缺陷并已离线修复，但尚不能证明它就是此前真机失败的唯一原因。卖家 GUI 回执与 `mechanical_contact_ack=false` 仍只作诊断，不参与任务成败裁决。
- v3 无外部效果验收预检保持 0 动作：聚焦输入框自然任务在 DeepSeek 任务图阶段因当前观察没有 typed `input_field_id` 以 HTTP 409 停止；随后会话 `6abcb05a18494ac2b67abfe48731c560` 中 Qwen 正确选择 `open_emoji_panel`，但本地四帧预检发现该非填充图标附近存在多个同等可信表面，按合同在设备动作前拒绝猜点。会话已取消、`total_physical_actions=0`，设备租约已释放。当前页唯一可稳定检测的填充控件是具有外部效果的发送按钮，不能用于无外部效果验收。
- 用户于 2026-09-03 批准按 Open-AutoGLM 的跨 App 点按方式收敛本项目：Qwen 选中唯一元素后直接使用其 bounds 中心，不再保留 v3 本地表面分割运行入口。以后只有新任务实际出现点击问题时，才使用该次全新动作前后截图重新分类，不得恢复第二套本地视觉裁决或用独立落点验收阻塞合法任务。
- recents gesture v3 真机会话 `8bc4754b3c604e72ada0168e4bb7eb72` 从网页执行原始任务“打开后台，把微信卡片划掉”，证据目录为 `poc/output/web/generic_supervised_20260903_040441_8bc4754b`，共执行 2 个物理动作：一次打开后台，一次对 Qwen 同帧绑定的微信卡片 `e1` 向左 `swipe_element`。模型轨迹为 `[0.265,0.559] -> [0.065,0.559]`，本地合法化为 `[0.265,0.559] -> [0.05,0.559]`，机械客户端路径为 `[198,797] -> [17,797]`；证明新方向权威已生效。
- 同一会话动作后原始截图人工复核仍可见微信卡片，物理 swipe 返回路径但 `hardware_receipt=null`；随后动作后 Qwen 请求发生一次 `SSL: UNEXPECTED_EOF_WHILE_READING`，会话按真实结果 `failed`、`physical_actions=2`，没有重复滑动。SSL 中断解释终态为何没有模型 `finish`，但不能解释截图中卡片没有消失。
- 用户批准后，机械 `swipe` 已从 `drag_client_path` 分离为独立 `swipe_client_path`：0.35 秒触点建立窗口后，在 0.30 秒内完成 6 段连续轨迹，释放后验证卖家坐标状态栅栏；`drag` 仍保持原 0.8 秒/16 段。回执记录按下、插值、抬起、参数、起终点和栅栏，并明确 `mechanical_contact_ack=false`。异常路径必释放右键；相关回归与最终完整回归 726/726 通过。
- 项目 API 重载后官方 profile 已显示 `relative_path_receipt=seller_position_barrier`、`touch_down_ms=350`、`movement_ms=300`、`steps=6`，OpenAPI 验证通过，`doctor.ready=true`。最终网页会话 `3412a7008d8f4c8aa8add2aa6b68dddd` 从已经打开的后台页只执行一次向左 `swipe_element`，客户端路径 `[198,797] -> [30,797]`，完整本地回执成立；动作后四张新截图仍显示微信卡片，Qwen 继续给出同一轨迹，系统经一次 0 动作纠正后在第二次物理动作前停止。会话 `failed`、`physical_actions=1`，没有网络错误或重复滑动。证据目录：`poc/output/web/generic_supervised_20260903_043236_3412a700`。
- 用户随后明确要求“将微信卡片移到最左边”。网页会话 `42029a32637246eb8f7000a243cd907f` 从当前后台页只执行一次向左 `swipe_element`，Qwen 终点为归一化 `x=0.05`，机械客户端路径 `[198,741] -> [17,741]`，即向左 181 像素且比上一会话的 `x=30` 更靠边。完整本地回执成立但仍为 `mechanical_contact_ack=false`；动作前后原图显示微信卡片没有可见位移，Qwen 新截图仍识别同一微信卡片，系统在第二次动作前停止。会话 `failed`、`physical_actions=1`、三次 Qwen 请求均成功；证据目录：`poc/output/web/generic_supervised_20260903_044445_42029a32`。
- 用户要求同条件再试一次后，网页会话 `56ce624b70aa414d9a29260ea0dbad7b` 从全新后台截图绑定微信卡片 `e1`，执行 `[0.265,0.559] -> [0.05,0.559]`，机械客户端路径 `[198,797] -> [17,797]`。只执行一次，完整回执成立；动作后 Qwen 和人工原图复核均确认微信卡片仍在且无可见位移。会话 `failed`、`physical_actions=1`、两次 Qwen 请求均成功；证据目录：`poc/output/web/generic_supervised_20260903_045922_56ce624b`。已知单击链能够触达手机，因此当前准确口径是连续按住—移动—释放未被手机识别，而不是机械触点完全没有接触。
- 用户决定明天现场核对物理滑动后，网页执行新自然任务“打开微信，清空当前聊天输入框中的全部内容，然后回到主屏幕”。会话 `a28f3f30a4f84bd89df4b82f5698ac00` 步骤 1 从后台成功点击微信卡片并进入文件传输助手；步骤 2 观察到正文 `aaazjie？你好`、`soft_keyboard_visible=false` 且无真实焦点证据，却直接执行 `clear_verified_text`。动作后硬校验仍为实际 `aaazjie？你好`、预期 `""`，会话 `failed`、`physical_actions=2`，没有重清，也没有继续 Home。证据目录：`poc/output/web/generic_supervised_20260903_050352_a28f3f30`。该时点失败主类是 typed 清空前置门禁缺失，不是 Qwen 未识别输入框。
- 第一轮通用焦点硬门禁加载后，会话 `7ff74e465ce140628898fe991e20ae20` 在同一无焦点截图上再次收到 Qwen `clear_verified_text`，canonical 绑定层于 ADB 广播前拒绝，`physical_actions=0`；证明盲清空已关闭。该会话同时暴露请求侧仍把清空列为可用动作，与执行绑定层冲突。证据目录：`poc/output/web/generic_supervised_20260903_052321_7ff74e46`。
- 第二轮已离线实现唯一顺序：typed 子目标尚无同 revision、同 `input_field_id` 的已匹配聚焦动作回执时，不向 Qwen 开放输入/清空；聚焦动作后的新截图请求才开放对应文字动作，而 canonical 与 Controller 仍要求新图 `focused=true`。相关回归 374/374、完整 Python 回归 729/729（123.560 秒）通过。第二轮尚未加载到项目 API，也未再次操作手机；当前运行 API 仍在线、空闲、`doctor.ready=true`、blockers 为空，卖家 `main.exe` PID 20172 未被操作。

## 5. 当前输入与执行语义

- 当前应用正文唯一视觉权威：同轮 `input_structure.application_inputs[*].text`。
- typed 字段身份：当前事务 `input_field_id`。
- ADB Keyboard 只提供当前已聚焦编辑连接的可信文字 transport；不观察页面、不选字段、不判断完成。
- 本项目只借鉴 Open-AutoGLM 的 UTF-8 Base64 `ADB_INPUT_B64` 与 `ADB_CLEAR_TEXT` 固定广播机制，不照搬输入前自动清空、每次自动切回输入法、未绑定 serial 或只看进程返回码的部分。输入和清空仍是独立 canonical 动作，广播后必须新截图验证。
- “一次性效果执行边界”只防止同一输入/外部效果 scope 被意外重复执行：一次物理执行后先看新截图；未得到 Qwen `finish` 时停止该 scope，不自动重输或重发。它不表示整个任务只能执行一个动作，不影响此前的普通导航，也不是新增确认项或风险类别。
- 禁止卖家文字对话框、剪贴板、Accessibility、任意 ADB/Shell 和候选词点击进入正式输入链；唯一例外是本地固定 ADB、固定 serial、固定 ADB Keyboard IME 与固定输入/清空广播命令族。
- `launch_app` 只使用本地可信包名注册表；transport 回执不是高层完成，随后仍需新截图交给 Qwen。

## 6. 当前唯一主线

当前唯一主线是提交并加载 Controller v23，然后对自然任务“打开微信，清空当前聊天输入框中的全部内容，然后回到主屏幕”只做一次最终证明；物理 swipe 问题继续按用户决定延后到现场核对：

1. ADB Keyboard 的输入和显式清空已经取得既有真机证据，不再重复改 transport，也不恢复 Companion IME；ADB Keyboard 不观察页面、不选择字段、不判断完成，Qwen 当前截图是唯一页面视觉权威；
2. Controller v23 已删除本地 OCR/颜色表面分割第二落点权威，直接使用 Qwen 同帧唯一元素中心；Python 727/727、前端 33/33 和受影响模块 296/296 已通过，但项目 API 尚未重载该提交；
3. 只重载项目 API，不操作卖家 `main.exe`；重载后用官方客户端重新确认 OpenAPI、controller、camera、Qwen/DeepSeek、设备独占及固定无线 ADB Keyboard `doctor.ready=true`；任一不成立即停止；
4. 运行条件成立后只跑一次最终自然任务，必须得到“打开微信 → 聚焦一次 → 清空一次 → 新截图为空 → Home 一次 → 主屏幕”的同会话证据；任一步失败即停止，不重复点击、不自动重清或重跑；
5. 最近任务清理只使用已批准的“主屏幕 → 后台 → 唯一一键清理按钮”固定状态顺序，不再执行卡片滑动；该规则不扩展到普通查看后台或切换 App。

当前验收台账：`单一权威最小校验验收台账.md`。

## 7. 本轮停止条件

出现以下任一情况时停止真机动作并报告具体运行故障：

- controller、camera 或设备离线；
- ADB serial 不在线，或 ADB Keyboard 未安装、未启用、未选中；
- 当前设备已有其他任务占用；
- 新观察不可用、过期或 scope/fingerprint 不匹配；
- Qwen 引用目标不唯一或 bounds 非法；
- 机械标定不可达；
- 登录/付款缺少确认；
- 项目 API 未加载当前提交。

离线测试失败时不得把真机当调试器，也不得操作卖家 `main.exe`。

## 8. 关键入口

- 组合根与公开路由：`poc/web_app.py`
- 官方类型化客户端：`poc/local_agent_api_client.py`
- 官方 CLI：`poc/agent_api_cli.py`
- 会话编排：`poc/agent/application/universal_agent_orchestrator.py`
- Qwen 同响应绑定：`poc/agent/application/qwen_visual_decision.py`
- 当前截图观察：`poc/agent/infrastructure/generic_scene_observer.py`
- 单动作执行：`poc/agent/infrastructure/generic_action_adapter.py`
- ADB Keyboard transport：`poc/agent/infrastructure/adb_keyboard_transport.py`
- 必要 Controller 校验：`poc/agent/domain/universal_action_controller.py`
- 当前产品边界：`用户决策与协议边界.md`

## 9. 交接纪律

- 先读取当前源码和 OpenAPI，不猜路由。
- 保留用户未提交修改；不得 reset、checkout 或覆盖。
- 不新增 App、截图、固定坐标或单任务特例。
- 同一设备只有一个任务拥有机械臂或 ADB Keyboard 文字 transport。
- 本地提交可以创建；未经用户明确要求不推送。
- 汇报分开写内部实现、自动测试、服务/设备状态和真机结果。
- 旧协议只可留在明确的负测试或 Git 历史中，不得保留运行时兼容入口。
