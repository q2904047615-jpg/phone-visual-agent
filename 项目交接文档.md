# 手机自动点击器项目交接文档

更新时间：2026-08-15（Asia/Shanghai）

> 新对话必须先完整读取根目录 `项目最终目标.md`，再读取本文。`项目最终目标.md` 是唯一最高优先级依据；本文只记录当前代码、运行状态、证据和下一步。若两者冲突，以 `项目最终目标.md` 为准。

## 1. 一句话交接

项目已经形成“DeepSeek 动态任务图 → Qwen 真实画面观察与唯一下一动作 → 本地通用安全控制器 → 机械臂一次动作 → 重新观察验证”的通用主路径，并完成 Home、严格文字证据和本地输入清空控件的协议化。最新正式输入验收只执行了 1 次动作，真实结果为 `a'gen't` 而不是 `agent`，证明 QWERTY 布局不能代表英文直输模式；验收已正确失败且没有自动重试。当前代码已把 `keyboard_layout` 与 `keyboard_input_mode` 分离，下一主线是先清理错误文本、显式切换到 `direct_latin` 并重新观察，再创建全新正式验收。

不得把下一步改成微信、抖音或设置 App 的专用脚本，也不得用固定截图坐标完成清空或输入。

## 2. 最终产品方向

本项目不是单功能自动点击器，而是可扩展到不同自然语言、不同 App 和多台手机的通用视觉操作 Agent：

1. 用户在网页输入自然语言目标。
2. DeepSeek 理解目标、约束、完成条件和风险，动态拆分高层子目标。
3. Qwen 结合当前子目标和摄像头真实画面，输出一个结构化页面观察和唯一下一视觉动作。
4. 本地通用控制器校验目标唯一性、画面身份、置信度、风险、能力和动作边界。
5. `main.exe` 驱动机械臂只执行一个底层动作。
6. 动作后重新拍摄、观察并验证；成功则推进，失败则停止或重规划。
7. 新命令若能由现有通用动作组合完成，不得为它新增专用代码。
8. 所有任务携带 `device_id`；同一设备同一时间只允许一个物理控制任务。

## 3. 当前仓库状态

- 仓库：`C:\Users\Administrator\Documents\Claude\Projects\手机自动点击器`
- 当前分支：`codex/complete-universal-agent`
- 当前相对远程同名分支本地领先 8 个提交。
- 用户当前要求：允许本地提交，不推送 GitHub。
- 2026-08-14 最后检查时工作区干净。

最近关键提交与本轮变更：

- `6b7b1bc fix: distinguish keyboard input mode`
- `d704da9 fix: reject imprecise clear controls`

1. `7575082 feat: formalize home and safe input flow`
   - 将 Home 纳入正式通用动作协议。
   - 收紧安全文字输入的设备能力边界。
2. `b7b9832 fix: require exact verified text evidence`
   - 修复“实际输入错误但仅凭画面变化被判成功”的问题。
   - 输入验收必须取得并逐字比对 `states.value`。
   - 增加 `states.keyboard_layout`：`qwerty / numeric / symbol / unknown`。
   - 当前真机输入仅允许空白、已聚焦、标准 QWERTY 输入框中的 1–30 个小写英文字母；其他内容在机械臂动作前拒绝。
3. `af1a149 fix: recognize safe local input clear controls`
   - 将输入框内部或紧邻右侧的独立圆形 `×` 建模为 `clear_local_text`。
   - 只允许它绑定唯一、非空、已聚焦、目标相关的输入框。
   - 必须通过几何绑定检查；键盘退格键不能冒充清空控件。
   - 没有增加设置 App 名称分支或固定坐标。
4. 本轮通用观察与控制器加固：
   - 观察器升级为 `2026-08-14-generic-scene-observer-v8`。
   - 只有唯一非空输入框与紧邻、紧凑的圆形清空控件形成可信几何绑定时，才补齐清空结构状态。
   - 文字“取消”/`cancel`、键盘输入法区域和远离输入框的控件不能冒充 `clear_local_text`。
   - 控制器独立复查取消语义、控件尺寸、间距和几何绑定，不依赖模型自报标签放行。
5. 本轮卖家预览取景修复第一阶段：
   - 卖家 v1.0.1018 会在最近鼠标位置绘制不透明 `PX/MM` 坐标提示；旧“停到标题栏”方案仍被映射为相机顶部坐标，遮住页面顶部输入框。
   - 第一阶段把鼠标停放在同一卖家窗口底部、相机裁剪区域之外的控制条；它能避免初始取景误触发顶部提示，但动作后的旧提示仍会残留。
   - 100%/125%/150% DPI 与横屏布局继续使用统一的卖家窗口缩放函数计算相机高度。
6. 本轮精确英文输入前置条件修复：
   - 观察器升级为 `2026-08-14-generic-scene-observer-v9`。
   - 输入框状态新增 `keyboard_input_mode=direct_latin / chinese_pinyin / unknown`；QWERTY 只表示按键排列。
   - Qwen、通用策略、通用动作控制器和设备控制器均在 `chinese_pinyin` 或缺少模式证据时，于硬件动作前拒绝 `input_verified_text`。
   - 通用模式切换只允许绑定画面中底部紧凑、独立、明确从 `chinese_pinyin` 指向 `direct_latin` 的按钮；一次切换后必须重新观察，普通字母键、退格键仍禁止成为 Qwen 动作目标。
7. 本轮卖家预览取景修复第二阶段：
   - 动作后证据证明窗口内控制条停放不能清除已绘制的 `PX/MM` 黑色提示。
   - 鼠标现在优先停放到卖家窗口之外的虚拟桌面角落，以触发真实 mouse-leave；只有卖家窗口覆盖整个虚拟桌面时才回退到底部控制条。
   - 该变化只移动电脑鼠标，不点击或操作手机；需要重载网页服务后用只读观察验证真实效果。
8. 本轮输入法机器人误报防护：
   - v9 清空专用精查曾把输入法预编辑栏右侧的蓝色机器人图标误报为 `clear_local_text`；该观察缺少 `focused=true` 和键盘状态，现有执行门已拒绝，`physical_actions=0`。
   - `clear_local_text` 现在必须在 `label` 逐字保存真实可见的 `×/✕/✖/x` 图形，空 label 或自由描述“圆形叉号”不能授权。
   - 被绑定输入框必须同时具备唯一、非空、目标相关、`focused=true` 和当前 `keyboard_layout` 证据；输入法预编辑区不能仅凭邻近图标冒充页面输入框。
   - `ui_scene`、通用动作控制器和通用策略三层独立复查；观察器升级为 v10，正式动作协议升级为 v7，通用策略升级为 v6。

以上提交均只在本地，未推送 GitHub。

## 4. 最近验证结果

代码与协议回归：

- Python 全量测试：`793/793` 通过。
- 前端与浏览器协议测试：`25/25` 通过。
- `git diff --check` 通过。
- 自动测试只能证明代码规则未回归，不能代替真机验证。

最近一次真机文字输入暴露的问题：

- 目标文字曾是 `Agent123`。
- 手机输入框实际出现 `agent.com`。
- Qwen 的动作后截图正确看到了 `agent.com`；错误来自旧验收逻辑只检查“画面变化”，不是 Qwen 看错。
- 该错误促成提交 `b7b9832`，现在缺少精确文字证据或文字不匹配都会失败，不能提升能力。
- 错误动作后的截图证据：
  `poc\output\web\capability_acceptance_86499e6ed51341b0b062a41e57c1c063\qwen_visual_revision_1_3c22e5ffe5ff4507b3fde7902d63148c_after_attempt_1_4.jpg`

本轮清空真机结果：

- 通用会话：`9cf5325e6bb74d9d9d9d04e5268546ac`。
- 精确作用域确认后只执行 1 次物理点击，没有自动重试。
- 原会话的动作后自动观察因一次协议非法字段和一次低置信而标记失败，不能直接作为成功证据。
- 随后独立只读观察取得新指纹 `d7ed81310d297e8e213a`，结构化证据为 `states.value==""`、`focused=true`、`keyboard_layout="qwerty"`，场景置信度 `0.95`。
- 动作后截图：
  `poc\output\web\generic_supervised_20260814_125917_9cf5325e\qwen_visual_revision_1_996fe0eaa5c94a8a803225734c123601_after_attempt_2_4.jpg`

本轮第一次正式输入验收草稿：

- trial：`ae2f7044d5ff465cb8653f736dfa1c00`。
- `physical_actions=0`，没有执行输入。
- 安全停止原因：卖家预览的 `PX/MM` 黑色提示遮住顶部输入框边界，Qwen 没有建立可信 input 候选。
- 该失败促成通用鼠标停靠点修复；必须在新提交基线重启网页服务并确认遮罩消失后，重新创建全新 trial，不能复用旧 trial。

本轮正式输入验收结果：

- trial：`bd66605343b5478caa8068d01be9f3c0`。
- 代码基线：`042464f8d5ef1ae908b672869a581aa6e405b6bd`。
- 精确确认前是唯一空白、已聚焦、`keyboard_layout="qwerty"` 的目标输入框；当时协议尚未区分输入语言模式。
- 精确作用域确认后只执行 1 次 `input_verified_text`，`physical_actions=1`，没有自动重试，没有搜索、提交、发送、保存或发布。
- 动作后两轮结构化观察都得到 `states.value=="a'gen't"`，与 `agent` 不匹配；报告状态为 `failed`，动作结果为 `mismatched`，没有产生 promotion scope。
- 新的独立只读观察再次确认当前真实值为 `a'gen't`、`focused=true`、`keyboard_layout="qwerty"`，指纹 `f91b192e7eb0ae42d331`。
- 根因不是模型误读，而是中文拼音 QWERTY 会产生组合分词文本；这次失败促成 `keyboard_input_mode` 的通用协议与三层前置阻断。
- 动作后证据截图：
  `poc\output\web\capability_acceptance_bd66605343b5478caa8068d01be9f3c0\qwen_visual_revision_1_13b6c6c18b3a4beea8d1d9722d671d01_after_attempt_2_4.jpg`

本轮失败后的只读恢复观察：

- v9 只读观察指纹 `0f11eb6017c224a395ad`，正确取得 `states.value=="a'gen't"`、`keyboard_layout="qwerty"`、`keyboard_input_mode="chinese_pinyin"`，并识别底部“中”模式切换键从 `chinese_pinyin` 指向 `direct_latin`；`physical_action_requested=false`。
- 清空专用精查指纹 `5258c5a299342e13e3de` 误把输入法蓝色机器人图标当作清空叉号；输入元素位于键盘预编辑栏，且缺少 `focused=true` 和 `keyboard_layout`，所以本地门未放行，实际 0 物理动作。
- 该误报促成 v10/v7/v6 的逐字 `×` 图形、聚焦、键盘事实和三层复查；重新加载服务后必须先用 0 动作观察证明机器人图标不再成为清空候选。
- 卖家黑色提示诊断：窗口外停放和 `WM_MOUSELEAVE` 都不能清除主画布内黑块；窗口本体抓图可去掉 `PX/MM` 白字但仍保留黑色背景。当前不能把这项取景修复宣称真机完成。
- 服务加载 v10/v7 后，新的清空专用只读精查被场景协议拒绝：模型仍把 `states.local_text_clear` 放到了不允许的元素角色上，HTTP 422；`physical_action_requested=false`，没有任何物理动作。这证明误报不能再进入可执行候选，但当前自动恢复路径也无法取得真实 `×` 证据。

## 5. 当前实时运行状态

2026-08-14 最后一次只读检查结果：

- 网页服务：`http://127.0.0.1:8765/`
- `/api/device`：可访问。
- `controller_online=true`
- `camera_online=true`
- `busy=false`
- 没有可继续执行的活动任务；失败 trial 已暂停并失效，旧确认和旧观察不可复用。
- 当前设备：`device-local-01`
- 卖家窗口：`智联新途AI机械臂控制端 v1.0.1018`
- 已验证真机能力：`back`、`dismiss_overlay`、`home`、`swipe`、`tap_semantic`、`wait_for_change`
- 尚未提升的能力：`input_verified_text=false`、`long_press=false`、`drag=false`
- 网页服务当前已加载正式动作协议 `2026-08-14-universal-action-v7`、观察器 `2026-08-14-generic-scene-observer-v10`、通用策略 v6。

不要重启、关闭或代替用户启动 `main.exe`。

`main.exe` 路径：

`D:\main软件发客户-20260705\main实时点击控制软件\main.exe`

用户明确要求以后由用户手动打开 `main.exe`。项目代码或新对话不得自行启动、停止或重启它。

## 6. 当前手机画面停点

最近一次真实画面显示：

- 手机处于系统设置搜索页面。
- 页面顶部搜索输入框保持焦点，但实际值为 `a'gen't`。
- 独立只读观察给出 `states.value=="a'gen't"`、`focused=true`、`keyboard_layout="qwerty"`。
- 画面中的中文候选与拼音分词证明当前不是可验收的英文直输状态。
- 卖家预览顶部仍残留 `PX/MM` 黑色提示；新“窗口外停放”代码尚未由网页服务加载和真机验证。

以上是最近证据，不是永久事实。新对话必须先重新拍摄并由 Qwen 观察当前画面，不能直接假定手机没有变化。

此前尝试通过通用目标清空文字时：

- Qwen 一次选择了输入框本身，只会聚焦，不会清空，未执行物理动作。
- Qwen 一次选择了键盘删除键，本地安全策略正确拒绝，未执行物理动作。
- 所有失败尝试的 `physical_actions=0`。
- 提交 `af1a149` 后，正确方案应是识别并点击独立 `clear_local_text` 控件，而不是键盘删除键。

## 7. 下一步唯一主线

接下来严格按以下顺序执行，不要并行控制机械臂：

### 第一步：加载新基线并只读验证

1. 提交当前通用修复，确保工作区干净。
2. 只重载 Uvicorn 网页服务，不得触碰 `main.exe`。
3. 检查 `/api/device` 确认 v9/v6、设备在线且不忙。
4. 通过 0 物理动作的通用观察验证输入法机器人不再成为 `clear_local_text`；黑色提示仍遮挡时不得靠猜测点击。

### 第二步：恢复可验收输入状态

1. 当前已进入人工恢复边界：请用户在手机上手动只使用顶部搜索框自身的可见清空控件，把 `a'gen't` 清空；不要搜索、不要点“取消”、不要切换输入模式。用户回复“已清空”后，先做只读观察确认 `states.value==""`，不得直接相信口头状态继续动作。
2. 新观察必须同时报告 `keyboard_layout="qwerty"` 和 `keyboard_input_mode="chinese_pinyin"`，并唯一识别底部本地模式切换按钮。
3. 通过一次独立 `tap_semantic` 把模式从 `chinese_pinyin` 切到 `direct_latin`；动作后重新观察，禁止同一确认继续输入。
4. 只有新证据确认 `states.value==""`、`focused=true`、`keyboard_layout="qwerty"`、`keyboard_input_mode="direct_latin"` 时，才进入正式验收。

### 第三步：完成安全文字输入真机验收

1. 使用低风险测试文字：`agent`。
2. 前置条件必须同时成立：输入框为空、已聚焦、目标唯一、键盘为 QWERTY、输入模式为 `direct_latin`、画面稳定。
3. 通过正式 capability acceptance 路径执行一次 `input_verified_text`。
4. 不允许自动重试，不允许把测试内容改成大写、数字、中文、符号或 `.com`。
5. 动作后重新观察；必须取得结构化证据 `states.value=="agent"`。
6. 若缺少 `states.value`、文字不匹配、键盘布局错误或画面身份变化，验收失败且不得提升能力。

### 第四步：单独决定能力提升

1. 只有真机报告通过且证据完整，页面才能出现能力提升条件。
2. 提升是独立的零物理动作操作，不能和输入动作共用一次确认。
3. 在执行提升前向用户展示结果，并取得对提升 `input_verified_text` 的单独明确确认。

## 8. 物理动作安全规则

每次真实机械臂动作前必须：

1. 完整读取 `项目最终目标.md` 和本文。
2. 检查 `/api/device`。
3. 确认 `device_id=device-local-01`。
4. 确认 `controller_online=true`、`camera_online=true`、`busy=false`。
5. 确认没有其他活动任务或会话。
6. 获取新画面、新 `observation_id` 和新 `fingerprint`。
7. 确认 Qwen 只提出一个动作，且目标、角色、状态和 bounds 全部绑定当前可信观察。
8. 使用包含 task、device、revision、subgoal、observation 和 fingerprint 的精确确认作用域。
9. 一次确认最多执行一个物理动作，不自动重试。
10. 动作后重新观察并保存证据。

账号或外部状态动作，例如发送、评论、关注、点赞、加好友、拉群、保存设置或删除外部数据，必须先展示草稿并取得用户明确确认。普通安全导航和已授权的本地纠错仍必须遵守一次一动作和重新观察。

## 9. 禁止事项

- 不得为设置、微信、抖音或某张截图增加固定坐标补丁。
- 不得让 DeepSeek 输出机械坐标、Shell 命令或连续动作序列。
- 不得让 Qwen 直接调用机械臂。
- 不得绕过确认作用域调用底层 `robot_core` 兼容入口。
- 不得使用键盘退格键作为当前清空方案。
- 不得在符号键盘上尝试输入 `agent`。
- 不得把 `keyboard_layout="qwerty"` 当成 `keyboard_input_mode="direct_latin"`。
- 不得在 `chinese_pinyin` 或输入模式未知时尝试输入 `agent`。
- 不得因自动测试通过就宣称真机输入能力完成。
- 不得同时让两个对话、Worktree 或服务控制同一机械臂。
- 不得自行打开、关闭或重启 `main.exe`。
- 不得在用户当前“仅本地提交”的要求下推送 GitHub。
- 不得把 Token、API Key 或控制令牌写进仓库、日志或交接文档。

## 10. 关键代码位置

通用任务和协议：

- `poc\deepseek_task_graph.py`：DeepSeek 动态任务图与风险协议。
- `poc\qwen_visual_decision.py`：Qwen 唯一下一动作协议。
- `poc\ui_scene.py`：结构化页面、元素和状态。
- `poc\generic_scene_observer.py`：真实画面观察和 `states.value/keyboard_layout/keyboard_input_mode/local_text_clear`。
- `poc\universal_agent_orchestrator.py`：通用任务编排、安全策略和确认作用域。
- `poc\universal_action_controller.py`：动作后精确验证。

真机执行与能力验收：

- `poc\generic_action_adapter.py`：唯一通用机械臂动作适配入口。
- `poc\robot_core.py`：设备、摄像头、标定和受能力门禁保护的底层动作。
- `poc\capability_acceptance.py`：真机能力验收规则和报告。
- `poc\capability_acceptance_runtime.py`：验收会话、一次动作和独立提升流程。
- `poc\generic_supervised_runtime.py`：受监督通用单步执行。

网页与证据：

- `poc\web_app.py`：FastAPI 接口、设备锁和网页服务。
- `poc\static\app.js`：网页界面。
- `poc\output\web\`：动作前后截图与报告。
- `poc\data\tasks.db`：任务记录。

## 11. 回归测试命令

Python 全量测试：

```powershell
Set-Location 'C:\Users\Administrator\Documents\Claude\Projects\手机自动点击器\poc'
.\.venv\Scripts\python.exe -m unittest discover -p "test_*.py"
```

前端和浏览器协议测试使用 Codex bundled Node；不要因为系统 Node 缺少 Playwright 就擅自安装依赖：

```powershell
Set-Location 'C:\Users\Administrator\Documents\Claude\Projects\手机自动点击器'
$env:NODE_PATH='C:\Users\Administrator\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\node_modules'
$node='C:\Users\Administrator\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe'
& $node --check poc\static\protocol_adapter.js
& $node --check poc\static\app.js
& $node --test poc\test_frontend_protocol.js poc\test_frontend_browser_contract.js
```

提交前：

```powershell
git diff --check
git status --short
```

## 12. 新对话建议首条指令

把下面这段话发送给新对话即可：

> 请先完整读取根目录 `项目最终目标.md` 和 `项目交接文档.md`，然后只读检查当前 Git 状态和 `http://127.0.0.1:8765/api/device`。不要启动、停止或重启 `main.exe`，不要推送 GitHub。当前主分支已集成通用任务闭环、通用动作验收和视觉稳定性三个工作树成果，正式版本为观察器 v11、动作协议 v8；`input_verified_text`、`long_press`、`drag` 仍未真机晋级。最新正式输入 trial `bd66605343b5478caa8068d01be9f3c0` 已失败且不可重试。用户截图已经证明机械臂控制窗口始终可见；先前 controller/camera offline 是 Web 端重载后的短暂窗口发现失败，随后已自行恢复为在线、空闲、无活动任务。当前真实阻塞是顶部输入框视觉证据不稳定：独立 0 动作观察曾确认 `value=""`、已聚焦和真实“中”模式键，但正式受监督新画面只稳定识别到键盘及模式键，没有同时证明唯一空白、已聚焦的中文拼音 QWERTY 输入框，因此本地策略拒绝生成 confirmation scope。不得绕过该门禁。下一步应先解决预览顶部黑色遮挡/裁切或输入框审计一致性，再重新创建全新模式切换会话；只有同一新 fingerprint 的完整证据齐全时，才申请一个动作切到 `direct_latin`。不得增加 App 专用流程或固定坐标。

## 13. 交接结论

三个并行工作树已经全部完成并由主任务审查、合并：`5612c3a` 强化通用任务闭环，`4ee8660` 强化通用动作验收，`61f073a` 将观察器升级至 v11；主任务随后提交 `542043d`、`ebdbb9a`、`e52597c`、`efb005c`，只对已知键盘枚举做安全归一化、增加脱敏诊断，并忽略非目标外围键盘容器上的无效元数据。未知枚举和目标错误结构仍失败关闭。主仓库在前三项集成及首次枚举修复后 Python 全量 `823/823`、前端与浏览器协议 `25/25` 通过；最终外围容器修复后相关视觉/协议测试 `169/169` 通过，`git diff --check` 通过。网页服务已实际加载观察器 v11、动作协议 v8，三项未验收硬件能力仍为 false。

所有本轮真机请求均为只读观察，未执行机械臂动作。v11 首次真机诊断确认 Qwen 会额外生成非目标 `container=kb_layout_qwerty` 并错误附带 `keyboard_layout`；该通用外围元数据问题已经修复。用户随后提供截图证明控制窗口一直可见，`/api/device` 重新检查也已恢复 `controller_online=true`、`camera_online=true`、`busy=false`，所以不得再把 `MainWindowTitle` 为空解释成控制窗口已关闭。修复后的独立 0 动作观察成功返回顶部应用输入框 `value=""`、`focused=true`，并识别真实“中”键方向 `chinese_pinyin -> direct_latin`；输入法机器人没有成为目标。

正式受监督会话 `b8ba6f9182f2429fb925f5ed28d3f5b8` 生成了正确的单一 `tap_semantic` 模式切换候选，但同一次可信观察没有稳定识别顶部输入框，也没有把空值、焦点、QWERTY 和中文拼音事实绑定到唯一 input；本地策略以“键盘模式切换要求唯一空白、已聚焦的中文拼音 QWERTY 输入框”阻断，`confirmation_scope=null`、`physical_actions=0`，随后会话释放设备并拒绝重复观察。当前真实阻塞是预览顶部黑色区域导致输入框结构在不同观察间不稳定，不是控制窗口关闭。下一步先解决这一通用视觉证据问题，再创建全新会话；禁止复用旧 fingerprint、旧候选或绕过控制器。能力提升仍需正式报告通过并取得用户单独明确确认。本地分支当前未推送。

## 14. 2026-08-14 触控落点偏差新诊断与安全修复

用户观察到输入法模式切换实际落到相邻句号键。失败会话 `fd9cdaffe41a41d8b71051e549afbd84` 的正式候选中心为 `(0.735, 0.91)`，视觉上位于目标模式键；动作后输入框出现 `。` 且输入模式没有改变，证明不能把这次失败解释为用户看错。该动作只执行 1 次，没有自动重试。

旧九点标定的最下排相机坐标约为 `y=1062/1440=0.738`，而模式键中心约为 `y=0.91`。旧验证 RMS `1.7499 CSS px`、最大误差 `3.8046 CSS px` 只证明中上区域准确，不能证明底部小按键；底部属于明显的仿射外推区。这也是不同位置会出现偏左、偏右或偏上的主要原因之一：相机透视、手机与机械臂平面不完全平行、连杆回差和触控笔斜压会产生位置相关误差，而不是一个固定偏移量。

当前通用修复：

- `tap_calibration.py` 升级为标定 v2，保存实际采样点的凸包覆盖范围；已启用标定若缺少覆盖信息或目标位于凸包外，直接拒绝外推点击。
- 中部小范围九点即使残差很低也不再被接受；覆盖至少需要横向跨度 `0.68`、纵向跨度 `0.82`。
- 校准页改为边缘九点并尝试标准/Android WebKit 全屏接口；校准脚本先用一个独立中央触点进入全屏，再逐点重新观察采集九点，并保守记录失败准备动作。
- 通用导航语义新增 `refresh/reload/刷新/重新加载`，策略版本为 `2026-08-14-universal-action-policy-v8`，不是浏览器或 App 专用流程。
- 已将当前旧标定设为 `enabled=false`、`validated=false`、`accepted_fit=false`，禁止继续使用只覆盖到约 74% 高度的矩阵。

真机证据：通用会话 `321a964974a0458ca918938d4c2d48f1` 在新观察 `obs_78645ce256714dfc91f92c94a5608d0b`、fingerprint `4f6addf8e5b5f4bf6241` 上唯一识别浏览器刷新控件，策略 v8 放行后执行 1 次，卖家返回落点 `[676,36]`。动作后 fingerprint 变为 `83a0767f527f9ae470cb`，画面明确出现中央品红十字靶标；报告把它标成 `mismatched` 是语义验证器没有认可目标元素变化，不代表刷新失败。

随后中央全屏准备靶点执行 1 次，页面回传仍为 `phase=fullscreen_setup`、`fullscreen=false`，九个采样点均未执行。标准全屏 API 被当前手机浏览器拒绝；WebKit 兼容代码已加入并通过自动测试，但尚未通过再次页面加载完成真机验证。禁止在这一步失败后自动重复中央点击。

回归结果：Python 全量 `840/840`，前端与浏览器协议 `25/25`，`git diff --check` 通过。当前控制器和相机在线、设备空闲、无活动会话，`main.exe` 未启动、停止或重启。继续主线时先让当前校准页加载最新 WebKit 兼容代码，再做一次中央全屏准备验证；只有进入全屏后才允许采集边缘九点并独立验证，之后才能重新验收底部输入法模式键。

## 15. 2026-08-14 系统导航唤出正式验收与当前恢复边界

主线已在提交 `00d7e26` 加入独立系统 UI 只读审计，观察器升级为 `2026-08-14-generic-scene-observer-v13`。只有目标明确涉及系统导航栏、全屏或沉浸状态时才额外审计同一稳定帧的原始、顺时针和逆时针方向；审计只允许两个布尔事实、置信度和一至两条无坐标/无动作证据。普通观察返回非法布尔字符串时只降级为 `unknown`，不能直接授权动作；独立审计允许一次严格格式修复，仍有 `unknown`、低置信、对象证据、坐标或动作字段就失败关闭。Python 全量 `921/921`、前端协议 `25/25` 和 `git diff --check` 均通过，8765 已实际加载 v13，`main.exe` 未启动、停止或重启。

正式 trial `3580ec4ca24f4797b067bc32ba6022a9` 的零动作阶段成功：

- 新观察 `obs_f92c115a6f594b13875ac3f6221455a2`、fingerprint `17b045a1e904938d0c69`，结构化事实为 `immersive_or_fullscreen=true`、`navigation_bar_visible=false`。
- Qwen 只提出无坐标、无方向和无距离的 `reveal_system_navigation`，目标区域为 `system_navigation`，预期效果严格为 `system_ui.navigation_bar_visible=true`，本地策略 v11 放行，确认作用域完整绑定 trial、task、device、revision、subgoal、observation 和 fingerprint。
- 动作前设备为 `device-local-01`，控制器/摄像头在线、空闲、无队列或其他会话；确认只消费一次，没有自动重试。

真机动作执行 1 次后正式失败，不得提升或复用该 trial：

- 本地 v2 标定生成的 DOM 轨迹为约 `[(0.5001,0.9526),(0.4996,0.7025)]`，控制端 client 轨迹为 `[(76,699),(254,698)]`。
- 校准页把起点当成 App 内触摸，页面 `sequence` 从 `3` 变为 `4`，记录实际 pointerdown 约 `(441,373)`；说明系统没有接管边缘手势。
- 动作后新观察 `obs_b84fc6f2a2394a01ab801b1952dc41ba`、fingerprint `81a38f257bb566fa2638`，仍为 `immersive_or_fullscreen=true`、`navigation_bar_visible=false`；报告 `action_outcome=mismatched`、`physical_actions=1`、status `failed/blocked`。
- 证据目录为 `poc\output\web\capability_acceptance_3580ec4ca24f4797b067bc32ba6022a9\`。`reveal_system_navigation` 仍未晋级，不能再次自动尝试。

对“把起点向真实底边外推最多 0.03”的候选修复已经主任务和独立工作树共同否决并撤销：名义纠偏起点约在 client `x=55`，而当前显示边缘约 `x=60`，只有约 5 px 余量；采集最大误差 `13.8115 px`、独立验证最大误差 `7.1686 CSS px`，还没有计入触控笔半径、机械回差、边框高度差和安全缓冲。两个凸包只能证明包内插值，不能证明包外玻璃边界或外推精度。当前 Git 工作区因此保持无该外推改动。

另一个已确认的通用根因是卖家控制端布局方向和手机内容方向不一致：失败动作时控制端 client 为竖向 `810x1515`，手机内容却是横屏；历史成功的系统返回使用横向相机布局。主任务通过完整窗口截图和底栏 OCR 唯一识别“横屏”切换控件，只点击卖家控制软件一次，没有触摸手机；控制端随后变为 `1440x810`，而校准页 `sequence` 保持 `4`，控制器/摄像头继续在线、空闲。当前页面仍是 `phase=calibration`、`fullscreen=true`、viewport `873x393`。卖家底栏 `H/B` 没有可访问性控件树，公开资料也未证明其语义，因此尚未点击，不能假定 `B` 是不落笔的原生 Back。

当前有三个离线工作树继续推进且均禁止控制真机、服务或 `main.exe`：

1. 通用检测“控制端相机布局方向 vs 手机内容方向”不一致，并在 back/home/tap/drag 前失败关闭。
2. 设计带真实可触控玻璃/显示边界、残差、触控笔半径、回差和安全缓冲的 v3 边缘证据；现有缺证据的 v2 不能据此外推。
3. 只读确认卖家控制端 H/B/圆点/横屏/输入的真实语义，证据不足就否决。

继续主线时先验收这三个离线结果。没有可靠的原生 Back 证据或 v3 真实边界证据前，不得再次执行系统边缘手势、隐藏导航栏 Back/Home 或直接点击 H/B。`input_verified_text`、`long_press`、`drag` 仍未真机晋级；只有安全退出当前全屏校准页并重新准备输入/动作验收页后才能继续。所有提交仍只在本地，未推送。

## 16. 2026-08-14 离线安全审查、卖家控件语义与当日冻结状态

### 16.1 两项上游安全整改的审查结论

- `8adad686`（相机布局方向与手机内容方向不一致时失败关闭）和 `d6605fa`（系统边缘手势要求实测边界）在首轮交叉审查中均为 **REJECT**。它们尚未达到可合入主线的安全标准，不能因各自测试通过或组合后没有文本冲突而改变该结论。
- 从 `00d7e26` 依次整合这两个提交形成的实验组合，最终实验提交为 `1cd9b3c`。该组合只用于暴露协议、验收和入口之间的交互缺口，明确 **不可合并到主线**；不得把它视为对两个被拒上游实现的间接批准。
- 实验组合发现并加固的通用内容，在 A/B 两项上游安全整改重新审查通过后可以择取复用：按动作读取和传递标定证据；v3 双批次独立边界证据校验；动作回执与测量 ID、误差包络及标定版本绑定；验收报告对相机方向、置信度、真实帧方向和非等比漂移的独立复核；专用唤栏动作恰好调用一次且失败后不回退 `H/B`、`back`、`home` 或普通 `swipe` 的负向测试；scene 协议版本入口测试和独立 lease 测试方式。
- 不可直接复用的部分包括两个被拒提交本身、任何未经证明的卖家 `H/B` 语义映射，以及尚未完成正式真机验收的系统边缘物理执行实现。

### 16.2 卖家 v1.0.1018 底栏静态逆向结论

- 对“智联新途AI机械臂控制端 v1.0.1018”的安装内容、模块和二进制/资源进行纯静态只读检查后，可以确认底栏 `H`、`B` 和“输入”都进入机械动作路径，不是无接触的 Android 原生系统键协议。
- `B` 不能视为不落笔的原生 Android Back；禁止把它作为系统返回、退出全屏或系统导航唤出的无接触回退入口。
- “横屏/竖屏”是单选布局控件，其切换可能同时带来机位回中副作用。没有新的现场证据和用户逐次明确授权前，不得把它当作无物理副作用的普通显示旋转操作。

### 16.3 后续 UI Automation 精确证据

- 后续主控通过 Windows UI Automation 精确定位到卖家窗口中唯一的“竖屏”控件；这是可访问性控件定位结果，不是 OCR 文本猜测或截图坐标补丁。本节的后续精确状态取代第 15 节中较早的 OCR/方向状态描述。
- 在用户明确要求后，该控件只触发 1 次。卖家控制端 `client_size` 从 `1440x810` 变为 `810x1515`，同一位置的按钮文字变为“横屏”。
- 操作后 `helper samples` 仍为 `4`；控制器和摄像头保持在线，`busy=false`，没有活动任务。
- 这些事实只证明卖家控制端布局状态发生了上述变化，不证明手机内容已经转正，也不证明机械坐标、相机取景或网页方向已经一致。

### 16.4 当前冻结状态与未验收边界

- 当前校准网页内容仍呈横向/侧转状态。用户决定于明天到现场查看，因此今天冻结所有页面、真机和机械臂动作；不得刷新、切页、退出全屏、再次切换横竖屏、执行系统边缘手势或尝试任何恢复动作。
- A（方向一致性失败关闭）和 B（v3 实测边界及误差包络）两项整改继续保持纯离线开发与审查，未通过新的独立安全审查前不得进入真机。
- `input_verified_text`、`long_press`、`drag` 仍未完成正式真机验收；退出当前全屏状态同样尚未验收。以上能力和恢复步骤都不能标记为已完成。
- 当日冻结期间不得启动、停止或重启网页服务及 `main.exe`，不得控制机械臂，不得复用旧 trial、旧确认、旧观察或旧 fingerprint。

## 17. 2026-08-15 方向安全集成与用户恢复后的新边界

### 17.1 离线集成结论

- 用户已在现场自行恢复手机页面，确认此前异常是手机进入全屏状态；第 16.4 节的“当日冻结”因此结束，但不构成对任意恢复动作的通用授权。
- 用户明确要求：后续不得再点击全屏、横屏、竖屏或其他卖家窗口布局控件。以上控件进入禁触范围；通用动作真机验收只能操作手机页面内、由当前可信观察唯一识别的目标。
- 用户无法在手机浏览器中手工输入网址。后续验收页应由 Agent 通过已验收的通用动作自行准备；不得要求用户手工输入 URL，也不得为浏览器增加固定坐标、固定网址步骤或专用流程。
- 相机布局方向门禁、动作时独立方向审计、live-trial promotion 来源绑定和单次 compact JSON 严格本地修复已经过独立集成审查，本轮现场修复前的集成 HEAD 为 `232b462a9c76bab12df87e1a851c88fdfbd9001e`。
- 新门禁要求真实 `device_id`、当前帧尺寸和稳健视觉绑定一致；每次物理动作前独立采集三张方向审计帧，普通观察仍只使用一张画面。方向未知、低置信、内容旋转、设备或帧尺寸漂移、旧授权重放均在机械臂调用前失败关闭。
- 能力提升必须绑定同一进程内真实动作结果和一次性的 `OrientationCredential`。仅凭序列化报告、复制对象、旧进程报告、修改后的报告或并发重复请求不能写入能力注册表；确认快照写入失败会立即使授权失效。
- 旧 App 专用多步入口已禁用，不能绕过通用单动作路径。先前被拒绝的 v3 边缘手势提交 `bc372f0` 没有合入；`reveal_system_navigation` 继续保持未验收和禁用状态。

### 17.2 主工作区离线验证

- 方向、能力验收、通用观察、通用规划/动作、通用编排和网页 API 组合回归在主工作区通过 `470/470`。
- 前端协议通过 `18/18`，浏览器协议通过 `7/7`，合计 `25/25`；两个 JavaScript 文件语法检查通过。
- 独立集成树完整 Python 回归共 `936` 项，`932` 通过；仅剩三张仓库未携带的历史图片造成的既知 `2 failures + 2 errors`，没有新增代码失败。
- `git diff --check` 通过。所有测试没有占用默认机械臂 lease；测试期间没有执行真机动作，也没有操作 `main.exe`。既知的测试子进程 `ResourceWarning` 仍然存在，但不影响本轮断言结果。

### 17.3 继续真机验收前的当前状态

- 截至本次 v17 提交前，运行中的 8765 服务已加载观察器 v16、scene v3、动作协议 v10；提交后仍必须只重载 Uvicorn 并确认 v17，不得操作 `main.exe`。
- 控制器和摄像头在线、设备空闲、没有活动任务或会话。`input_verified_text`、`long_press`、`drag` 和 `reveal_system_navigation` 仍为 false，不能把离线集成通过写成真机能力已完成。
- 下一步只允许安全刷新 8765 网页服务，不得触碰 `main.exe`；加载 v17 后重新检查设备、会话、观察和 fingerprint，再按“一次动作、重新观察、验证结果、失败不重试”的顺序逐项验收。
- 页面准备也必须遵守同一单动作闭环。若进入目标页必须依赖尚未晋级的输入能力，应先在当前安全页面完成 `input_verified_text` 正式验收；不能通过底层输入、ADB、卖家“输入”按钮或固定坐标绕过门禁。

### 17.4 浏览器实机导航与 compact 观察修复

- 用户恢复竖屏后，新会话 `70e23d0907ff49afb62268da4a9c00f2` 在新观察 `obs_9bf3d47b921341f79bddb6be0f9770f0` 上唯一识别启动器中的浏览器图标。方向审计为 upright，确认后只执行 1 次 `tap_semantic`，没有自动重试。
- 动作后证据图已确认浏览器成功打开且仍为竖屏。正式会话因动作后 compact JSON 没有唯一严格的单结构标点修复而报 failed；这只是后验观察失败，不是浏览器点击失败。证据位于 `poc\output\web\generic_supervised_20260815_161143_70e23d09\`。
- 随后两次高层页面准备请求被 DeepSeek 低层动作表达门禁拒绝，物理动作均为 0；改为合法高层目标后，浏览器复杂首页的动作前 compact JSON 再次失败，仍为 0 物理动作。失败目录为 `poc\output\web\generic_supervised_20260815_161836_d5df3725\`。
- 现场服务该次调用的 completion tokens 恰好为当时配置上限 `800/800`，这强烈提示输出长度不足，但因证据目录没有保存 `finish_reason` 和原始响应，不得把它写成已确证的截断。
- 观察器 v20 将单次 compact 输出预算从 800 提高到 1200，并把首轮元素严格限制为最多 6 个，必须优先目标相关控件和输入框，单元素只允许一条短证据；同时允许在 JSON 解析错误位于 EOF 时严格枚举一个结构标点。只有恰好一个候选通过去重复键、compact schema 和完整 `_parse_scene` 时才接受；普通场景中非法的系统 UI 事实值只降级为 `unknown`，不会被转成布尔值或授权系统动作，缺失/多余字段仍然硬失败。安全打开/切换标签页的目标中，`close_tab` 和标签组容器会被强制降为非目标，只减少危险竞争候选，不新建目标或改动边界。整个 compact 阶段仍只调用一次模型，不会远程重写响应。
- 当前浏览器顶部搜索框内有旧文字，键盘未出现，右侧四角图标是全屏而不是清空按钮。下一步只能通过新观察唯一绑定左侧输入区域；全屏图标、横屏/竖屏、卖家底栏、输入按钮和固定坐标全部禁止。

### 17.5 通用标签页准备与软键盘风险边界

- 观察器 v20 加载后，主任务通过通用视觉单步路径分别完成了：打开浏览器窗口管理、新建一个标签页、切回“通用动作真机验收页”、聚焦页面内唯一空白输入框。四次都是一个物理动作、动作后重新观察，没有自动重试。
- 关键会话为 `49729a90668844d68e9e7ba7a20384bf`、`f2125adc5a8f4999ac78bfc56038cd9b`、`924184b64fcb4fba8daed631db423026`、`37d80edc0a8d46dc94bcec1db4a287bd` 和 `9c2138f840fa43278de2518df1b83d9e`。其中不符合单步目标的后续建议均在未执行第二个动作时取消。
- 当前真机页面为通用动作验收页；新观察已结构化证明唯一输入框 `value=""`、`focused=true`、`keyboard_layout="qwerty"`、`keyboard_input_mode="chinese_pinyin"`，并唯一识别当前到目标为 `chinese_pinyin -> direct_latin` 的底部模式控件。尚未执行模式切换物理动作。
- 模式切换在动作前被 DeepSeek 语义风险审计误判为持久外部状态修改，相关失败会话的 `physical_actions=0`。本地通用边界现只将“当前前台、可见软键盘、未提交的临时输入模式/语言/布局”视为 `navigation_only`；任何系统默认、全局设置、账号同步、云端词库、安装/启用/停用或其他外部风险仍失败关闭。
- 该边界修复的任务图、风险审计、通用编排、模拟闭环和网页 API 组合回归为 `370/370`。下一步只允许提交并重载确切的 8765 Uvicorn 服务，然后创建全新会话；仍禁止操作 `main.exe`、全屏、横屏/竖屏、卖家底栏或固定坐标。

### 17.6 英文直输切换真机结果与动作后审计修复

- 提交 `e176c72` 并只重载 8765 Uvicorn 后，会话 `90899cee062c4b938c45d21d7aa272f1` 的零动作阶段完整证明：唯一空白聚焦输入框、QWERTY、`chinese_pinyin`、唯一“中”模式键及 `chinese_pinyin -> direct_latin`；普通方向与独立方向审计均为 upright。
- 确认前重新检查设备为 `device-local-01`，控制器/摄像头在线、空闲、无 active task，只有该 active session。精确作用域只消费一次，机械臂落点回执为 `[594,1302]`，对应可见“中”键中心，没有落到句号键。
- 动作后 4 帧与新 fingerprint `b3ed57a2a2bbec22e5c5` 明确显示：键位变为小写 Latin 字母、中文候选/“分词”消失、Shift 键出现、模式键标签变为“英”，输入框仍为空且聚焦，动作验证为 `matched`。该切换物理动作已成功，禁止重复点击。
- 首次动作后输入结构审计虽逐字读出“英”，却因固定 JSON 示例偏置仍返回 `keyboard_input_mode="chinese_pinyin"`，随后 DeepSeek 又返回超长 evidence，因此会话阻塞但没有第二个物理动作。
- 新通用修复要求 `keyboard.input_mode` 必须由当前整张键盘的可见事实判定，不得复制目标或 schema 示例；“英/EN/English/ABC/Latin”与 `current_mode="chinese_pinyin"` 的组合现会失败关闭。重规划 evidence 也限为最多 3 条 visible_evidence 中逐字一致的短字符串，禁止拼接或复制整个观察 JSON。相关观察、scene、任务图、编排、模拟闭环和网页 API 组合回归为 `501/501`。
- 正式 `input_verified_text` 验收尚未开始。必须先提交本次修复、只重载 8765，并用全新零动作观察同时确认 `value=""`、`focused=true`、`keyboard_layout="qwerty"`、`keyboard_input_mode="direct_latin"`，才能执行一次 `agent` 输入。
- 提交 `599b8a1` 并重载后，首个正式零动作启动目录为 `capability_acceptance_9b487301d1e94b8f8e040e6ee33ec6b1`。两轮新观察都因“英”与 `current_mode="chinese_pinyin"` 冲突而失败关闭，`physical_actions=0`，没有生成 trial，也没有再次点击模式键。
- 为消除最后的 schema 示例偏置，输入结构审计的返回模板现使用中性 `input_mode="unknown", mode_switch=null`，而不再预填中文模式和切换方向。对文字验收目标，审计必须优先报告当前整张键盘证明的 `input_mode`，方向不独立明确时 `mode_switch` 必须为 null。观察、scene、能力验收 runtime、编排和网页 API 组合回归为 `441/441`。
- 提交 `6c8d459` 并重载后，第二个正式零动作启动目录为 `capability_acceptance_509f7010cde746cb8100982cd6c6176c`。新诊断已正确得到 `keyboard_input_mode="direct_latin"`，但普通 scene 中的模式键使用了明确同义值 `current_mode="english"`、`target_mode="chinese"`，严格 scene 协议因枚举值不匹配而拒绝。两轮均为 `physical_actions=0`，无 trial、无手机操作。
- 本地现只对输入模式字段做两个确定的一一对应：`english -> direct_latin`、`chinese -> chinese_pinyin`；任何其他未知别名（例如 `English_US`）仍由严格 scene 协议失败关闭。相关观察、scene、能力验收 runtime、编排和网页 API 组合回归为 `443/443`。
- 提交 `64b305b` 并重载后，最后一个正式零动作启动目录为 `capability_acceptance_ec8e46982a6549678a0b048e4cc931fb`。新诊断已同时得到 `keyboard_layout="qwerty"`、`keyboard_input_mode="direct_latin"`、`current_mode="direct_latin"`，但输入结构审计仍返回了协议外形状的可选 `mode_switch` 对象；两轮均在动作前严格拒绝，`physical_actions=0`、无 trial、无重复输入。
- 现场已停止继续调整视觉提示或重复请求。当前设备为 controller/camera online、`busy=false`、无 active task/session，`main.exe` 仍为原有 1 个进程。`input_verified_text`、`long_press`、`drag` 和 `reveal_system_navigation` 继续为 false。下次只应做一个离线通用修复：在文字输入目标中，已严格证明唯一空白聚焦 input 及 `direct_latin` 时，将非目标、协议外形状的可选 `mode_switch` 降级为 null；任何模式切换目标中的同类异常必须继续失败关闭。必须先补离线正反测试，不得直接再试真机。
- 上述离线修复已实现：只有文字输入目标、唯一空白可编辑输入框、可见 QWERTY 键盘且 `keyboard_input_mode="direct_latin"` 同时成立时，才将“只缺必需字段”的非目标 `mode_switch` 降级为 null。键盘模式切换目标、未知/中文模式、非空或不唯一输入、以及任何额外字段（包括 `action`）继续严格失败关闭。观察、scene、能力验收 runtime、通用编排、模拟闭环和网页 API 组合回归为 `453/453`；尚未重载服务或再次进行真机验收。

### 17.7 `input_verified_text` 首次正式动作与动作后观察边界

- 提交 `d5035e4` 后只重载了确切的 8765 Uvicorn 进程，没有操作 `main.exe`。全新正式 trial `2f0db66b965a481abc59ad69e15840c8` 的零动作阶段成功绑定 `device-local-01`、观察 `obs_5539080fa4aa47e3921ddbf83b5bf56c`、fingerprint `62147556a6605b6f74a1` 和唯一 `local_audited_input_1`；动作前事实为 `value=""`、已聚焦、QWERTY、`direct_latin`，Qwen 只提出一次 `input_verified_text(text="agent")`。
- 确认前再次证明控制器/摄像头在线、设备空闲、无其他任务或会话。确认作用域只消费一次，`physical_actions=1`，没有自动重试。动作后证据帧中输入框逐字显示 `agent`，页面“当前”值也为 `agent`，页面自身显示 `input_verified_text 验收通过`；未提交、未搜索、未离开页面。
- 正式报告仍为 `failed`，不能晋级：两次动作后观察都在输入结构审计处被与当前目标无关、只缺字段的可选 `mode_switch` 拒绝，因此没有产生结构化 after scene。证据目录为 `poc\output\web\capability_acceptance_2f0db66b965a481abc59ad69e15840c8\`。该 trial 已消费且不可重试；页面可见成功不能替代正式报告，`input_verified_text` 仍为 false。
- 新的通用离线修复把观察器升级为 v21：对于非键盘模式切换目标，只要同一输入审计已经证明唯一可信输入、可见 QWERTY 和 `direct_latin`，就允许把“严格 schema 的缺字段子集”型可选 `mode_switch` 降级为 null，不再要求输入值必须为空。这只让动作后 `value="agent"` 能形成可验证场景；动作授权仍由 `UniversalActionController` 独立要求动作前 `value=""`，非空输入框继续失败关闭。模式切换目标、未知/中文模式、额外字段、畸形几何和不唯一输入仍严格拒绝。
- 新增动作后非空正向测试和动作前非空拒绝测试后，相关观察、scene、能力验收、通用编排、模拟闭环和网页 API 组合回归为 `455/455`，`git diff --check` 通过。此修复尚未加载到 8765；当前运行服务仍报告 v20，设备在线空闲、无活动会话，`main.exe` 保持原有 1 个进程。
- 下一步不得重复输入。必须先以一个新的通用单动作闭环把当前输入框恢复为空，并在动作后重新观察证明空值；只有空框、QWERTY、`direct_latin` 和方向 upright 再次同时成立，才能创建全新的 `input_verified_text` trial。清空动作本身若没有唯一可信 `clear_local_text` 控件就必须停止，禁止用键盘退格、固定坐标或底层接口绕过。

### 17.8 `input_verified_text` 正式通过、晋级帧不变量修复与刷新准备边界

- 观察器 v22 只公开输入审计字段名/类型的脱敏形状，v23 对与输入无关的目标移除严格可判定、非目标且无动作字段的键盘模式切换外围候选，v24 对重新加载目标只保留逐字 `refresh/reload/刷新/重新加载` 控件为目标相关；这些修复均不创建元素、不改坐标、不加入浏览器或 App 分支。对应本地提交为 `4055ad8`、`b4eaca5`、`fea3aa7`。
- 通过通用单动作闭环，刷新会话 `6c91ef6bb42d4d10a92d119a84b2bbc` 只点击一次可信圆形重新加载控件，页面恢复为初始空白状态；聚焦会话 `d59cb2cea61743039b9168446464c245` 只点击一次唯一空白输入框。两次动作后均重新观察；聚焦动作的正式后验观察仍受当时模式键可选结构影响而阻塞，但画面已证明空框、光标、小写 Latin QWERTY 和 upright，未触碰全屏、横屏或竖屏控件。
- 全新正式 trial `83c61faedfbf46689d3d68bb8b4e3e32` 在零动作阶段绑定观察 `obs_939a6546ffa748768127731a57e6d91e`、fingerprint `a27806c456bf634309c9` 和唯一 `local_audited_input_1`；动作前事实严格为 `value=""`、`focused=true`、`keyboard_layout="qwerty"`、`keyboard_input_mode="direct_latin"`。精确确认只执行 1 次 `input_verified_text(text="agent")`，没有自动重试。
- 动作后新观察 `obs_3c63fadc74974bfdae3d8607ee185298`、fingerprint `8c63520bc73e94c583e1` 逐字证明输入框 `states.value="agent"`，页面自身显示 `input_verified_text 验收通过`；正式报告为 `status="passed"`、`physical_actions=1`、`action_outcome="matched"`，前后各 4 张证据且无观察/验证错误。证据目录为 `poc\output\web\capability_acceptance_83c61faedfbf46689d3d68bb8b4e3e32\`。
- 本次 trial 没有生成 `promotion_scope`，能力仍不得晋级。根因不是帧丢失：`GenericActionExecutionResult` 虽声明 `before_frames/after_frames` 为 tuple，采集器实际传入 list；报告可正确记录 4 帧，但 live promotion 来源门禁因对象容器不是不可变 tuple 而失败关闭。提交 `e9dc7be` 在执行结果边界保留同一 PIL 对象并冻结为 tuple，同时新增真实 adapter 结果直达 live promotion 来源校验的回归。完整 Python 回归为 `983/983`。
- 旧 passed trial 不能补发晋级凭证：原始内存帧、方向凭据对象、seal 与 nonce 都只存在于原进程；落盘 JPEG 为有损证据，只能验证报告摘要，不能重建原始对象身份。该 trial 保持只读，不得从磁盘恢复 authority，也不得把 passed 报告直接写入能力注册表。要晋级必须在修复加载后的新 trial 中重新取得 live authority，然后向用户展示结果并取得对 `input_verified_text` 晋级的单独明确确认；晋级步骤必须为 0 物理动作。
- 修复加载后，当前页面仍保留已通过的 `agent`。三个新的页面恢复请求 `9a615ca381054927961947c526db6f51`、`2dfe9fd9b0364c69bfd165dddf0284fc`、`eb5d265f7460465bba62c788379316e7` 均在动作前阻断，`physical_actions=0`：第一个目标精查把相邻书签与刷新图标簇合为低置信、非完整候选；第二个把键盘机器人误报成清空符号但被输入框相对几何门禁拒绝；第三个目标精查漏掉刷新控件。没有点击书签、全屏、机器人、退格、开始按钮或任何固定坐标。
- 提交 `b81a2d1` 将观察器升级为 v25：compact 阶段已有低置信或明确裁切的非输入目标时仍进入目标精查，目标图标必须与相邻图标分开紧框；输入目标继续使用更严格的独立结构审计。Python 完整回归为 `984/984`。该提交尚未加载到 8765；当前服务仍为 v24、`main.exe` 仍为原进程。
- 独立审计确认提示词精查不足以直接授权刷新。观察器 v26 已增加按需、只读、严格 schema 的相邻图标簇审计：模型自报的 `reload_visual_audit` 一律先剥除；只有同一稳定原图中唯一、完整可见、置信度至少 0.90、同时具有圆弧与箭头头部、无书签/四角展开冲突且与相邻图标边界不重叠的候选，才由本地重建为 `meaning="reload"` 并写入 `reload_visual_audit=true`。审计使用完整原图及其目标方位高清裁剪，不使用浏览器名称、固定坐标或现场截图模板。
- 通用动作策略升级为 v12。刷新只允许 `navigation_only + tap_semantic + button/icon + meaning="reload"`，并强制 `fully_visible=true`、本地审计凭据、置信度至少 0.90，以及 Qwen 对元素 states 的逐项复用；书签、四角全屏、合并图标簇、两个 reload、重复 JSON key、未知 shape cue、低置信、裁切、边界重叠、模型伪造凭据和动作 states 篡改均在动作前失败关闭。观察器、策略和网页组合回归 `371/371`，Python 完整回归 `996/996`，`git diff --check` 通过。
- v26/v12 的加载与新观察必须继续遵守：只重载 Uvicorn、保持 `main.exe` 不变，并先确认设备在线空闲。只有新审计形成唯一可信 refresh 候选时才允许一次确认，失败仍不得固定坐标或自动重试。
- 提交 `21ac14c` 后已只重载 8765 Uvicorn：服务 PID 从 `19992` 变为 `16932`，`main.exe` 保持 PID `19956`；`/api/device` 确认观察器 v26、控制器/摄像头在线、`busy=false`、无活动任务或会话。随后唯一一次新刷新准备请求目录为 `poc\output\web\generic_supervised_20260815_190347_c78277bc\`，在第一轮动作前观察调用千问时收到 `HTTP 400 Arrearage`（账号欠费/状态异常），`physical_actions=0`、没有生成会话或候选、没有点击手机。该错误来自外部模型账户状态；恢复账户前禁止重复请求、复用旧观察或绕过 Qwen。
- 当前注册表中的 `input_verified_text`、`long_press`、`drag`、`reveal_system_navigation` 仍全部为 false；本地提交未推送 GitHub。

### 17.9 Qwen3.7 Plus 迁移与可替换模型配置层

- 用户确认将正式视觉模型迁移为主线别名 `qwen3.7-plus`，并要求以后更换视觉模型时不再牵动 DeepSeek 任务图、本地安全控制器、机械臂标定和动作闭环。未选择带日期的快照作为长期运行模型。
- 新增不可变配置层 `vision_model_config.py`，版本为 `2026-08-15-vision-model-config-v1`。默认模型为 `qwen3.7-plus`、统一坐标尺度为 `0..1000`，新配置名为 `VISION_MODEL` 与 `VISION_MODEL_BASE_URL`；旧 `QWEN_VL_MODEL` 与 `DASHSCOPE_BASE_URL` 仅作为兼容输入保留。模型可通过配置替换，无需修改观察、决策或机械臂代码。
- Qwen3.7 Plus 默认会启用思考，正式视觉请求现显式发送 `enable_thinking=false`。这是视觉模型传输层的固定默认，不改变 DeepSeek 的独立任务规划职责。当前仍保留各观察阶段的本地输出预算和严格 JSON/schema 校验，模型多写、少写、别名漂移或格式错误继续在硬件动作前失败关闭。
- 视觉服务状态与只读诊断现记录 `model_config_version`、请求模型、思考开关、统一坐标尺度、服务实际返回模型和 `finish_reason`。网页 `/api/apps` 不再写死旧模型名称；离线回放缓存指纹同时包含模型配置层，避免不同模型配置错误复用旧缓存。
- 默认模型与旧环境变量当前在进程和用户环境中均未被外部值覆盖，因此新服务加载后会使用代码默认的 `qwen3.7-plus`。启动脚本也会显示实际选择的模型，并明确显示 thinking disabled。
- 离线验证：新增配置/请求测试 `7/7`；配置、观察器、视觉决策和网页聚焦回归 `358/358`；Python 完整回归 `1003/1003`；离线视觉题库结构校验 `35/35`，没有调用模型或产生 Token；前端协议 `18/18`、浏览器契约 `7/7`、触控标定页 `4/4`，合计 `29/29`；`py_compile` 与 `git diff --check` 通过。完整 Python 测试仍出现既知的测试子进程 `ResourceWarning`，断言全部通过。
- 本地提交 `2bff5c0` 保存上述迁移，未推送。随后只重载确切的 8765 Uvicorn：旧监听 PID 为 `16932`，启动器返回 PID `32120` 后实际监听 PID 稳定为 `31988`；`main.exe` 保持 PID `19956` 不变。新 `/api/device` 与 `/api/apps` 已证明配置版本 `2026-08-15-vision-model-config-v1`、请求模型 `qwen3.7-plus`、`thinking_enabled=false`、坐标尺度 `1000`、观察器 v26；控制器和摄像头在线、`busy=false`、无活动任务或会话。
- 本轮没有调用 Qwen3.7、没有执行真机动作，也没有操作 `main.exe`。`response_model`、`finish_reason` 和用量因此仍为空。上次在线调用曾受 `HTTP 400 Arrearage` 阻塞；本轮为避免无效付费请求没有重新调用，账户是否已恢复尚未验证，因此不能宣称 Qwen3.7 已完成在线视觉或真机验收。

### 17.10 Qwen3.7 Plus 在线零动作验收与用量计量

- 用户充值后，`qwen3.7-plus` 已通过真实阿里云接口恢复调用。验收只使用仓库中的历史真机截图，`hardware_actions_enabled=false`，没有启用摄像头、网页控制台或机械臂，也没有操作 `main.exe`。
- 首次代表性全链路用例 `launcher_text_icon_single_action` 通过：GenericSceneObserver 建立可信场景，QwenVisualDecision 提出唯一 `tap_semantic`，本地评分通过；观察和决策各调用一次，均为首轮成功，实际响应模型为 `qwen3.7-plus`、`thinking_enabled=false`。
- 随后的 7 用例回归首轮为 `6/7`。唯一失败的 `input_value_already_complete` 画面确实是符号键盘，新模型返回严格等价但协议外的 `keyboard.layout="symbols"`，本地协议按预期在动作前失败关闭。修复只把大小写/空白归一后精确的 `symbols` 映射为既有枚举 `symbol`；任何其他未知值（例如 `symbols_custom`）仍严格拒绝，不放宽 schema，也不创建控件或坐标。
- 修复后的合并报告为 `poc\output\offline_qwen_visual_decision\20260815_210214_095623\report.json`：`7/7` 通过、`failed=0`、`hardware_actions_enabled=false`。其中三个可执行场景形成唯一视觉动作，一个输入已完成场景返回 finished，三个风险或不可信场景保持本地 blocked。
- `DashScopeVisionProvider` 现累计记录成功调用数、输入 Token、输出 Token 和总 Token；评估报告只汇总本轮结果，不把旧报告引用计入本轮用量。三份计量报告合计 19 次成功调用、输入 41,032 Token、输出 8,888 Token、总计 49,920 Token；最早一份迁移冒烟报告发生在计量补丁前，不在该合计中。
- 典型“观察并提出一个下一动作”实测需要 2–3 次 Qwen 调用、约 5,500–7,800 Token；完整物理动作还需要动作后重新观察，估算为 3–4 次调用。费用估算必须把它称为决策循环或物理动作，不能把一次多步用户任务误算成一次调用；DeepSeek 任务图费用另计。
- 新增计量、枚举正反测试后，观察器、配置和评估聚焦回归 `133/133`，Python 完整回归 `1007/1007`，`git diff --check` 和语法检查通过。完整测试仍出现既知测试子进程 `ResourceWarning`，断言全部通过。

### 17.11 Qwen3.7 实时坐标纪律加固与零动作停止点

- 用户进入下一阶段后，先重新检查 `/api/device`：`device-local-01` 的控制器、摄像头在线，`busy=false`、无活动任务或会话；`main.exe` PID 仍为 `19956`，新模型为 `qwen3.7-plus` 且思考关闭。能力注册表仍为 `input_verified_text=false`、`long_press=false`、`drag=false`、`reveal_system_navigation=false`。
- 两个不同的只读实时目标（确认输入状态、重新加载恢复空白）都在 compact scene 解析阶段得到完全相同的越界元素 bounds：原始 0..1000 协议值被解析为 `(0.73, 1.13, 0.81, 1.19)`。本地协议正确失败关闭；两次均未建立候选、未创建确认作用域、`physical_actions=0`。
- 本地只读预览证明当前仍为“通用动作真机验收页”，输入框可见值为 `agent`，页面显示上次 `input_verified_text 验收通过`，英文 QWERTY 键盘仍可见；没有触碰刷新、全屏、横屏、竖屏、卖家底栏或手机页面。
- 新通用修复明确告诉模型：0和1000代表原图四边，禁止复制例如 810x1515 画面的像素坐标。解析端只允许丢弃 `states.goal_relevant=false` 且 `role!=input` 的越界外围元素；目标元素、输入框、未明确非目标的元素仍保留并由严格 scene 协议拒绝。该处理只减少非目标信息，不转换坐标、不创建目标，也不能授权动作。
- 新增提示词、非目标外围正向测试，以及目标元素/输入框继续失败关闭的反向测试。观察器聚焦回归 `120/120`，Python 完整回归 `1010/1010`，断言全部通过；仍有既知测试子进程 `ResourceWarning`。
- 此修复尚未通过新的实时 Qwen 画面复核，更没有执行页面刷新。下一步必须在明确允许当前手机实时预览发送给阿里云 Qwen 后，只做一次零动作观察；只有新场景和严格 reload 图标簇审计都形成合法、唯一、归一化候选时，才进入一次物理刷新确认。

### 17.12 实时图标坐标闭环与安全刷新成功

- 用户已明确允许将当前手机实时预览发送给阿里云 Qwen 做视觉观察。该授权不改变本地单动作、动作后重观察、失败不自动重试和禁触全屏/横竖屏控件的边界。
- 提交 `d0613e0` 将 `camera_alignment.evidence` 的坐标/控制器污染规则与 scene 协议统一：存在至少一条安全手机内容证据时只移除被污染证据；全部证据均不安全时仍失败关闭。此前 `1ca7180` 已处理非目标外围像素坐标污染。两项均未转换模型坐标或创建动作目标。
- 首个刷新会话 `5aa64ac03d2e4aa5878e1b22c89fed6c` 的旧 v26 审计把刷新框错误写为 `[0.88,0.015,0.95,0.05]`，确认请求目标点 `(915,32)` 超出机械臂实测标定范围，机器人在落笔前拒绝外推。该请求记为 `physical_actions=1`，但页面未变化、没有 after 帧和实际接触证据，确认范围已消费且没有重试。证据目录为 `poc\output\web\generic_supervised_20260815_213014_5aa64ac0\`。
- 观察器 v27 将刷新语义与落点几何分离。第一次 Qwen 审计只枚举紧凑图标簇；第二次使用带受限上下文边距的簇裁剪重新核对书签/刷新形状。Qwen 给出的冗余 `cluster_bounds` 若不包含其全部已严格校验控件，只允许唯一地重建为这些控件框的最小并集；未知字段、重复键、非法形状 cue、多目标或非紧凑簇仍拒绝。
- 动作坐标不再直接采用 Qwen 框中心。本地只在已审计簇内提取孤立高对比主轮廓，去除被大轮廓包含的内部分量和显著更小的文字/压缩噪声；优先按框重叠绑定。当两轮模型坐标整体漂移时，只有“模型控件数与本地主轮廓数相等、至少两个控件、主排列轴一致且顺序一一对应”才允许同时吸附簇内全部控件。少一个、多一个、轴/顺序不清、一个框碰到两个轮廓或最终边界重叠均失败关闭。
- 现场回放把 Qwen 漂移刷新框 `[871,18,953,54]` 绑定为真实轮廓 `[821,12,857,33]`。新增错误框纠正、局部复核失败不回退、全控件同步吸附、噪声过滤和冗余外包框唯一归一化测试；观察器聚焦回归为 `126/126`，项目虚拟环境完整 Python 回归为 `1016/1016`，`git diff --check` 通过。完整测试仍只有既知子进程 `ResourceWarning`。
- 新会话 `2bd3f60e65e645c8a9e296e43268c67b` 的零动作阶段绑定观察 `obs_111f444627464c8aab85d25e3d5f29cf`、fingerprint `e9a784dde30c9ea1036a` 和唯一刷新框 `[0.821,0.012,0.857,0.033]`，中心 `(0.839,0.0225)`；本地策略 v12 允许且确认前设备在线空闲、`main.exe` PID 为 `19956`。
- 精确确认只执行一次，机器人回执像素点 `[682,27]`。动作后观察 `obs_60c3da39218b4348a07a2332abc73c19`、fingerprint `ec13e47898ee6e4d9b78` 明确证明输入框由 `value="agent"` 变为 `value=""`、键盘收起、页面恢复空白，`action_outcome="matched"`、前后各 4 帧且无验证错误。报告目录为 `poc\output\web\generic_supervised_20260815_221400_2bd3f60e\`。
- 动作后 DeepSeek v3 仍未把 `trigger_reload` 标记完成并再次生成确认范围；主任务没有消费第二个范围，而是取消会话，最终 `physical_actions=1`。这是完成复核/重规划的后续离线问题，不能靠重复刷新掩盖。
- 当前 8765 实际监听 PID 为 `31280`，`main.exe` 仍为 PID `19956`；页面已为空白初始状态，无活动通用会话。`input_verified_text` 的旧 passed trial 仍不能跨进程补发晋级 authority；下一步必须聚焦空白框并创建全新正式 trial，在新 live authority 下重新输入、验证、预览晋级并单独执行 0 动作晋级。随后再分别验收 `long_press` 和 `drag`。

### 17.13 拖拽验收确定性编排、几何审计与当前停止点

- 本地提交 `03cfff3` 将能力验收任务图与随机的通用目标拆解分离：正式通用 Agent 仍由 DeepSeek 动态拆解目标，但 `drag/long_press` 的能力认证只使用固定语义、固定风险边界和单动作的确定性验收任务图。该改动只影响认证流程，不加入 App、页面或截图坐标分支。
- 同一提交增加通用元素几何审计：模型每次只接收一张候选裁剪图，返回裁剪局部 `0..1000` 坐标；本地按真实像素裁剪变换映射回全图。重复键、额外字段、低置信、目标不唯一、标签/角色不一致、目标不完整或触及裁剪内部边缘均失败关闭，不回退到初次粗框。`drag` 的起点与终点分别审计，确认前新画面再次独立审计，并要求前后审计框 IoU 至少 `0.60` 才能进入机械臂。
- 正式零动作 trial `c6f62c4d3be34886be5e3b62f868f5ec` 证明确定性任务图和实时观察正常：Qwen3.7 Plus 唯一识别 `e1`（`role=image`、`meaning=drag_source_block`、`label=起点`、bounds `[0.19,0.67,0.38,0.81]`）与 `e2`（`role=container`、`meaning=drop_target_zone`、`label=绿色终点`、bounds `[0.53,0.63,0.83,0.86]`），场景稳定度和两端置信度均为 `1.0`。该 trial 在决策协议解析处安全阻塞，`physical_actions=0`。
- 阻塞根因是正式提示内部自相矛盾：主协议要求拖拽端点为扁平字段，重试骨架却诱导模型补 `source/destination` 嵌套对象；同时旧提示错误地禁止 `container` 作为拖拽终点。提交 `bcc581a` 明确只允许 `source_element_id/...` 与 `destination_element_id/...` 扁平字段，禁止嵌套对象，并只允许可信 `container` 作为拖拽终点、不能作为单元素动作或起点。
- 修复后相关回归 `279/279`、完整 Python 回归 `1068/1068`、`git diff --check` 通过；完整回归仍只有既知测试子进程 `ResourceWarning`，所有断言通过。两个提交均只在本地，未推送。
- 使用该 trial 已落盘的 4 张真实动作前画面和可信场景进行一次 Qwen3.7 Plus 零动作回放。模型首轮即返回合法 `drag`，准确绑定 `e1 -> e2`，`target_region.kind=element_path`，两端 bounds 逐项复用可信候选；`protocol_retry_used=false`、`first_pass_success=true`、`physical_actions=0`。本次调用实际用量为输入 `3611`、输出 `600`、合计 `4211` Token。随后本地策略 v12 对同一结构化决策返回 `allowed=true`、`canonical_class=drag`。
- 提交后只停止并重载了监听 8765 的项目 Uvicorn，没有停止、启动或点击卖家 `main.exe`。当时从 Codex 沙箱桌面枚举不到卖家窗口，后来已确认交互桌面中的卖家窗口实际存在（HWND `328280`），`main.exe` 保持 PID `19956`；根因是先前的 Uvicorn 运行在无法访问交互窗口的桌面会话，不是卖家窗口消失。改用交互桌面隐藏启动项目 Uvicorn 后，controller/camera 恢复在线。
- 当前停止点：`drag` 已完成代码、全量离线测试、真实历史画面的在线模型决策和本地策略放行，但尚未完成确认前实时几何复核、一次机械拖拽、动作后重观察、正式报告和 0 动作晋级，所以注册表中的 `drag=false` 必须保持。`long_press` 仍受卖家控制协议缺少可证明 down/up ACK 的边界阻塞，也不得标记完成或靠扩大鼠标按住时间猜测。
- 下一步只能在卖家控制端窗口由用户环境恢复且 `/api/device` 重新证明 controller/camera online、设备空闲、无其他会话后，创建全新 `drag` trial；旧 trial、旧观察和旧确认范围不得复用。只允许一次物理拖拽，失败不自动重试。不得自行重启 `main.exe`，不得点击全屏、横屏、竖屏或卖家底栏。

### 17.14 第一阶段完成：拖拽与长按正式通过并晋级

- 第一阶段的三项未晋级通用动作现均完成：`input_verified_text=true`、`drag=true`、`long_press=true`。它们都经过独立正式 trial、恰好一次动作、动作后重新观察、报告校验和单独的 0 物理动作晋级；没有把测试页面、App 名称或固定截图坐标写入正式编排。
- 拖拽正式 trial 为 `cf3d9af3f71a4ef79a515a954f18f7f8`：唯一源块被拖入唯一绿色终点，`status=passed`、`physical_actions=1`、`action_outcome=matched`，随后 0 动作晋级。晋级后注册表 SHA256 为 `756ccb7c6b74bed896faac9557ba697f8c76c4ed019c48f922319ecc6057e8d2`。
- 长按链路增加三层通用硬化：确定性能力验收上下文可安全绑定逐字长按标签；长按和拖拽一样在计划帧与确认帧分别做单裁剪局部几何审计；逐字标签、角色和 states 相同的安全长按目标允许在去除动作词后做同义语义重绑定，但删除等危险语义仍失败关闭。
- 卖家 GUI 没有公开机械接触 ACK。项目没有直连不明返回码的 WCF，也没有把右键注入返回当作接触成功；正式通道使用右键按下后两次同 GUI 线程坐标状态转换作为事件顺序栅栏，再等待 `0.45s` 机械下降稳定窗口，之后才完整计时 Qwen 请求的 `0.8s`，最终总是抬起。回执版本为 `2026-08-16-seller-gui-contact-barrier-v3`。
- 长按正式 trial 为 `dda7a8e0fc33425a9dd96131fee88644`：实际像素落点 `[392,813]`，栅栏变化像素 `5237`、返回变化像素 `174`、栅栏耗时 `625ms`、下降稳定窗口 `0.45s`、请求保压 `0.8s`；动作后唯一结构化结果为绿色 `long_press 验收通过`。报告 `status=passed`、`physical_actions=1`、`action_outcome=matched`，前后各 4 帧，观察与验证错误均为空。
- 长按晋级预览和正式晋级均为 `physical_actions=0`。报告 SHA256 为 `8474b9283f1b76bc9ad29cacc0f95b567b3ccf456043ee63267a92c9d3152831`；注册表从 `756ccb7c6b74bed896faac9557ba697f8c76c4ed019c48f922319ecc6057e8d2` 原子更新为 `fd4e01e3a43d14bce602c301ea5b92730bc6d47faa20bfde6a38e4f14399c736`。
- 本阶段新增关键本地提交：`620102b`（卖家长按事件栅栏与拖拽注册表）、`95e811e`（本地手势模式入口重绑定）、`ec3c101`（确定性长按验收策略）、`cae952d`（长按独立几何审计）、`e82285d`（双向事件转换回执）、`b22c013`（长按安全同义语义重绑定）、`292039d`（结构化长按结果）、`f80cd26`（机械下降稳定窗口）。均未推送 GitHub。
- 正式验收期间只重载项目 Uvicorn，卖家 `main.exe` 始终保持 PID `19956`，没有启动、停止或重启它；没有点击全屏、横屏、竖屏或卖家底栏。下一阶段应转入通用连续闭环与重规划，不再继续为已晋级的三个动作重复真机验收。

## 18. 2026-08-16 第二阶段核心完成、严格单会话真机闭环待补

### 18.1 通用闭环合同

- 第二阶段已把“一个动作后重新观察、验证、推进或重规划”接入默认 `universal_agent` 主路径，没有增加 App 名称分支、固定步骤、现场坐标或专用状态机。
- 动作后证据拆分为当前画面的 `visible` 证据和本地构造的 `controller_transition` 回执。控制器回执严格绑定 session、task、device、旧 revision/subgoal、Qwen decision、确认动作摘要、执行时 rebound/resolved 动作摘要、前后 observation 与 fingerprint，并且只能消费一次；它只可证明所绑定的 `navigation_only` 动作已经执行，不能代替画面证据证明全局状态、账号结果或外部状态。
- 任务图 revision 必须精确递增 `+1`；旧确认范围、旧 Qwen 决策和已消费回执不能跨 revision 重放。`wait_for_change` 的 0 动作观察转换与物理动作回执分离；连续滚动等合法多步导航不会被粗粒度“重复动作”误杀。
- `/next` 发现画面漂移时先让 DeepSeek 根据新观察修订剩余任务，再让 Qwen提出新 revision 下的唯一下一动作。一次确认仍最多执行一个物理动作、没有自动重试；执行前、执行中和动作后的失败分别落盘为不同类型，0 动作失败不会伪装成 `post_action_failure`。
- 通用控制台同步支持完整 action scope（含 `decision_node_id` 与 `action_digest`）、一次性确认、防双击并发、过期范围失效、独立终态节点和精确的 revision/after-observation 转场。旧记录缺少权威回执时显示 unknown，不根据最终状态猜测历史；DOM 隐藏视觉坐标、绝对证据路径和控制令牌，窄屏降为单列。

### 18.2 关键通用修复

- `6852535` 分开“确认范围绑定的计划场景 fingerprint”和“确认帧重新观察得到的执行场景 fingerprint”，允许独立几何复核合法纠正粗定位，同时继续严格核对两层身份。
- `a63ca35` 只对本地已完整验证的 inline JPEG、且阿里云明确返回已知 URL 内部错误时做一次受限网络重试；远程 URL、畸形数据和其他 400 仍失败关闭。
- `6a3aa87` 使用页面自身的结构化延续线索识别仍可继续浏览的长页面；`1aaae5a` 不再把“流程/结果/通过”等工作流泛词误当成命名页面；`8cd0688` 保留较长的否定安全约束，同时拒绝把“不要点广告，但点击确定”错误解析成全局禁止动作。
- 第二阶段后端工作树最终提交链为 `d2fa5cb`、`b1d1f75`、`9fd1e52`，控制台工作树为 `3d0f3c5`、`da1120f`；主工作区集成提交为 `0879517`、`507c796`、`3f5b955`、`34d4436`、`f42637b`、`8281d6b`。以上均为本地提交，未推送 GitHub。

### 18.3 离线验证

- 最终 Python 完整回归：`1108/1108` 通过，耗时约 `47.9s`。
- 前端协议与纯离线浏览器回归：`37/37` 通过，其中包含真实后端历史形状、完整/过期 scope、重复确认、转场不推断、终态、脱敏、XSS 和移动端单列测试。
- 后端最终专项复审确认：错误确认范围为 0 动作 `confirmation_failure`；策略复核拒绝保持 `blocked` 且不被外层改写；动作后结果绑定错误保持恰好一次 adapter 调用并落盘 `post_action_failure`。未发现新的证据覆盖或伪造问题。

### 18.4 真机连续闭环验收

- 会话 `a4d479dcf3ae4f5a9d02a3392e8e1208` 从当前可见的“第2步 · 打开只读详情”开始，先由 Qwen 提出并执行唯一语义点击；动作后新观察进入“连续闭环第3步”，产生绑定前后 observation/fingerprint 的 typed receipt 和新 revision。随后只确认一次系统 `back`，最终真实画面逐字显示 `连续闭环验收通过`，并显示“已按顺序完成向上滑动、语义点击和系统返回。”；总物理动作为 `2`，每次动作后都有新观察和回执，没有自动重试。
- 该会话最终状态为 `blocked`，原因是当时 DeepSeek 把“结果页/流程”泛词错误当作命名页面身份锚点；物理动作与最终画面已经成功，不能把该旧会话状态改写为 succeeded。该通用解析缺口随后由 `1aaae5a` 修复。
- 修复加载后的独立只读会话 `0f257a282e124c199285be4e5a6fd755` 使用目标“确认当前页面已经显示‘连续闭环验收通过’；不要执行任何改变状态的操作”，从当前真实画面建立新可信观察，DeepSeek 将任务图推进到 revision `2` 并以 `status=succeeded` 结束；`physical_actions=0`，没有为证明结果再点击手机。
- 因此第二阶段的诚实结论是：连续物理链路已在真实手机上完成“语义点击 → 动作后重观察/推进 → 系统返回 → 最终可见结果”，修复后的任务图又以独立 0 动作任务确认最终完成条件；它不是靠修改旧报告、复用旧确认范围或增加测试页专用流程得到的。

### 18.5 当前运行状态与下一阶段边界

- 最终 `/api/device`：`controller_online=true`、`camera_online=true`、`busy=false`、`active_tasks=[]`，通用监督会话和旧监督会话均为空；Qwen 为 `qwen3.7-plus`、`thinking_enabled=false`、最近调用 `finish_reason=stop` 且 `error=null`，DeepSeek 最近调用同样 `error=null`。
- 项目 Uvicorn 父进程 PID `34604`、实际监听进程 PID `5648`；卖家 `main.exe` 仍为原 PID `19956`（启动时间 `2026-08-13 15:37:53`），本阶段没有启动、停止或重启它，也没有点击全屏、横屏、竖屏或卖家底栏。
- 第二阶段的通用闭环、动作回执与异常重规划核心已经实现，但严格完成证据仍缺一项：必须使用修复后的当前代码，在一个全新 universal-agent session 中依次完成至少两个物理动作，并让同一 session 最终 `status=succeeded`、`active_subgoal=null`。`a4d479dcf3ae4f5a9d02a3392e8e1208` 最终为 `blocked`，修复后的独立 0 动作会话不能替代这一证据。因此第二阶段当前只能标为“核心完成、严格单会话真机闭环待补”。

## 19. 2026-08-16 阶段 3 前置加固：异常重规划与未预设措辞本地页验收

### 19.1 通用合同与离线验证

- 本节原先被错误编号为“第三阶段完成”。按 `项目最终目标.md` 重新复核后，连续闭环与异常重规划属于第二阶段能力；本节全部实现和证据保留，但只作为真正阶段 3（跨 App 陌生命令正式验收）的前置加固。它没有证明三个真实 App，不能再作为阶段 3 完成结论。
- 前置加固继续使用 DeepSeek 动态任务图、Qwen 当前画面唯一下一动作、本地策略/几何复核、机械臂单动作和动作后新观察的通用主路径；没有新增 App 名称分支、固定命令、固定步骤、测试页坐标或单功能状态机。
- 旧确认范围在执行前发现 observation、fingerprint 或 revision 漂移时会一次性消费并以 0 物理动作转为 `needs_reobservation`；新观察先触发 DeepSeek 修订，旧 Qwen 决策和旧 scope 不能跨 revision 复用。只读子目标在同一可信画面推进一个可见节点后也转为 `needs_reobservation`，但继续保留设备独占，下一次 `/next` 只能重新观察，不能执行动作。
- 通用几何复核不再接受与原可信候选完全不相交的“中心重定位”。同一静态画面只有目标语义、标签、角色一致且新旧几何实际重叠时才可使用更精确审计框；不相交框一律在机械臂前失败关闭。现场失败动作不会因代码修复而自动重试。
- 新增共享否定约束候选过滤层，Qwen 可见候选和本地策略使用同一通用语义；明确点名的禁止对象会被排除，“任何/所有页面正文元素”可排除全部页面元素并引导无坐标系统返回，但“不要触碰页面练习按钮”不会误伤无关列表项。该层不改变权威观察、fingerprint 或证据，只缩小可提议候选；模型若重新编造被排除对象，本地策略仍独立拒绝。
- 最终离线验收为 Python `1149/1149`、前端协议与浏览器合同 `33/33`，`node --check poc/static/app.js` 与 `git diff --check` 通过。完整 Python 回归仍出现既知的测试子进程 `ResourceWarning`，所有断言通过。

### 19.2 跨起始状态的异常重规划

- 初始全部满足的独立会话 `3c5839bf779b4c42a5220dd2afe29646` 直接以 `succeeded`、`physical_actions=0` 结束，报告为 `poc/output/web/generic_supervised_20260816_090055_3c5839bf/report.json`。
- 真机异常会话 `57ab94605752408a957e7e078d4a74e3` 在旧 scope 与实时画面漂移后以 0 物理动作拒绝确认并消费旧范围；随后 `/next` 使用新 observation/fingerprint 把任务图 revision 从 `1` 修订为 `2`，再让 Qwen 基于新 revision 提出唯一菜单语义点击。该点击只执行一次，动作后菜单真实打开；后续 Qwen 协议解析失败后会话停止，没有自动重试。证据目录为 `poc/output/web/generic_supervised_20260816_092632_57ab9460/`。
- 这条验收证明了“旧 scope 失效 → 新观察 → 高层修订 → 新动作”的默认主链路；旧确认凭据、旧动作摘要和旧 fingerprint 均没有被复用。

### 19.3 未预设措辞与两种通用动作

- 现场首次使用的自然语言目标要求从“验收模式选择”列表进入从上往下第二项、看清下一页标题后回到原列表，并包含本地只读与禁触区域约束；该完整措辞在正式代码和测试中均未预设，也没有为它增加命令路由或专用任务图。
- 会话 `173648d86d1340eb97e9b6844cf58ec6` 以 `tap_semantic` 进入第二项“语义点击”：动作前 observation `obs_55f3d45f2e114bc8a3ea301982471644`、fingerprint `29d2d3574e2cefaadb9b`；动作后 observation `obs_9e7d99b1f1e94a2e979edc4cb17528af`、fingerprint `aae04f2b296416fbe743`，可信画面标题为“通用动作真机验收页”。动作只执行一次并产生 typed transition；动作后的页面身份重规划失败后没有重试。
- 后续页面正文“返回验收模式选择”点击会话 `c38769535b164d7fa9ea6450f350befc` 诚实记录为 `blocked`：一次点击落点 `[139,390]` 后画面未变化，`action_outcome=mismatched`，未自动重试。该证据暴露了不相交几何框被错误接受的问题；通用重定位合同随后收紧，旧失败点击没有再次执行。报告为 `poc/output/web/generic_supervised_20260816_131727_c3876953/report.json`。
- 系统返回会话 `f494fcb446b0424fb13e6d1059c988c2` 明确排除所有页面正文元素，Qwen 因此提出无 element_id、无坐标的 `back`。动作前 observation `obs_f728576386b7464692aaf7d364ab84b1`、fingerprint `a6c745076670b42a302b`；动作后 observation `obs_1b8436868c8f48d7a30ae9f03f4f90db`、fingerprint `d8a2955550e404ed2f11`，画面回到“验收模式选择”，`action_outcome=matched`、无验证错误。报告为 `poc/output/web/generic_supervised_20260816_144935_f494fcb4/report.json`。
- 最终严格复跑又发现“不要触碰页面练习按钮”曾被误扩成排除全部页面候选；会话 `67620556e593404aa6ecc5c61d59fc41` 在 0 动作阶段 blocked，通用全称/点名约束区分修复后完整回归通过。新会话 `4933ae6c72c9437f921b8df4e62df45c` 的第一步 `tap_semantic` 成功进入详情页，前后 observation 为 `obs_1522c93bffd04b199cba95a5e7b1ec50` 与 `obs_0a35bddf9f974eae9e9e6808facfa7aa`，结果 `matched`；第二步模型两次提议页面链接而不是命令要求的访问历史返回，两个 scope 均未确认，会话以 `cancelled` 保留，不能把它写成单会话成功。
- 恢复会话 `adc280cb28c948f9bf618d3b61b53430` 只确认一次系统 `back`：动作前 observation `obs_042e08ea5189481b9383226ce7d1777d`、fingerprint `24436b9eb9a37764a213`；动作后 observation `obs_f2ffac72bff84936b28ddf0e9b4a0aa2`、fingerprint `4acf08723efe199428d4`，重新显示“验收模式选择”，`status=succeeded`、`action_outcome=matched`、无验证错误。报告为 `poc/output/web/generic_supervised_20260816_152557_adc280cb/report.json`。
- 因此阶段 3 的两种已认证通用动作真机证据为 `tap_semantic` 与 `back`，每个动作均有独立的前后 observation/fingerprint、动作后新画面和 typed receipt；失败动作与语义不一致提案都被保留且没有重试。严格单会话自动闭环尚未作为成功证据，完成链路使用显式受监督的会话边界，不能把它描述成无人监督连续执行。

### 19.4 最终独立 0 动作确认与运行状态

- 最终新建的独立只读会话 `1cb42a6f1a8e459689175e156125fff5` 只要求确认当前页面逐字显示“选择单一验收模式”。首次观察以 `needs_reobservation` 保持设备独占；一次 `/next` 取得全新 observation `obs_e52b8a62fcd7493ab18b594e07f10510` 与 fingerprint `deac0bb29c0bc23688ee` 后，任务图 revision `4`、`status=succeeded`、`physical_actions=0`，最终摘要明确包含“选择单一验收模式”。报告为 `poc/output/web/generic_supervised_20260816_152807_1cb42a6f/report.json`。
- 最终 `/api/device` 复核应保持 controller/camera online、`busy=false`、无活动任务或通用会话；项目 Uvicorn 当前由项目虚拟环境运行，实际服务 PID 为 `32936`。卖家 `main.exe` 全阶段始终为 PID `19956`、启动时间 `2026-08-13 15:37:53.0769547`，没有启动、停止或重启，也没有点击全屏、横屏、竖屏或卖家底栏。
- 提交 `84ba6b8` 到此完成的是阶段 3 前置加固，不是真正阶段 3。三个真实 App 的陌生命令、等价改写、跨起始状态和文字输入/导航/选择统一闭环尚需继续验收；多手机调度仍属于更后续阶段。

## 20. 2026-08-16 阶段编号纠正与真正阶段 3 当前状态

- 权威阶段编号已纠正：真正阶段 3 是“跨 App 陌生命令正式验收”，不是重复第二阶段的连续闭环或异常重规划。
- `阶段3交接文档.md` 已按三个真实 App、未预设自然语言、等价改写、动态起始状态、文字输入/导航/选择、单动作回执和账号影响停在确认前的标准重写。
- 阶段 1 已完成。阶段 2 当前为“核心完成、严格单会话真机闭环待补”：阶段 3 硬件验收暂停，必须先用当前修复代码取得全新同会话至少两动作、每步完整前后 4 帧/observation/fingerprint/typed receipt/revision 推进、最终同会话 `succeeded` 的证据。
- 当前状态：真正阶段 3 尚未完成。在第二阶段严格证据补齐前，不得开始三个 App 的硬件验收；之后仍须在至少三个不同真实 App 上取得各自的真实画面和物理动作证据。不得用 `84ba6b8`、本地浏览器测试页或离线测试代替。

## 21. 2026-08-16 第二阶段严格单会话复跑与当前唯一阻塞

- 本轮先保持阶段 1 为完成、阶段 2 为“核心完成、严格单会话真机闭环待补”，并暂停真正阶段 3 的跨 App 硬件验收。卖家 `main.exe` 始终保持 PID `19956`、启动时间 `2026-08-13T15:37:53.0769547+08:00`；只重载过项目 Uvicorn，没有点击卖家全屏、横屏、竖屏或底栏。
- 通用前置加固包含：输入目标必须先聚焦唯一输入框再输入；唯一完整输入控件允许空标签几何审计；同一输入控件在动作前后允许严格几何重叠下的通用语义别名漂移；输入回执以唯一输入控件及精确 `states.value` 复核；DeepSeek 对具名页面身份和非法完成证据各有一次高层协议修复机会。以上均未增加 App 名称、固定命令、固定步骤、坐标或验收页状态机。
- 会话 `1a8b2a71ba3b4c8b842513fa3c955244` 在同一会话真实完成 `tap_semantic` 聚焦和 `input_verified_text` 两个动作，输入框与页面真实显示 `agent`/验收通过；两步各有前后 4 帧、新 observation/fingerprint 和 typed receipt。但第 2 步因输入控件 meaning 别名漂移被旧控制器记为 `mismatched`，随后重规划失败，最终仍为 `blocked`，不能作为严格成功证据。报告目录 `poc/output/web/generic_supervised_20260816_162638_1a8b2a71/`。
- 会话 `e683139eb2094fd8b4c209da7a4d115d` 从验收模式清单执行一次语义点击进入输入页，动作结果 `matched`，前后各 4 帧，observation 从 `obs_8dc69b762a0d4e19b608288342a31bee` 变为 `obs_1d69216032b845eb9d83b0319274a3d3`，typed receipt 为 `receipt_be6c99054bac4f59824528521a96fbe2`。DeepSeek 随后把 subgoal ID 错填为完成证据，会话按合同终止；通用一次性证据字段修复已加入并通过专项回归，旧会话未续跑或重试。报告目录 `poc/output/web/generic_supervised_20260816_164713_e683139e/`。
- 修复加载后，`2d8e040dab3a458dbcbf44d3c6c22c62` 因模型额外生成不可唯一证明的 read-only 前置观察而在 0 动作阶段终止。新会话 `80f5c585c94f4f40a13f8ac4562f5611` 随后成功执行一次输入框聚焦：revision `1→2`，前后 observation 为 `obs_3648b361594847c5a4f76ffed1b331b3` 与 `obs_a71cd75a51414d7c9cc3f5f3da475784`，typed receipt `receipt_ea5012a7017c40cba6841ed1528bfd7c` 为 `matched`。动作后画面证明输入框已聚焦，但软键盘为 `chinese_pinyin`；Qwen 没有可用的模式切换候选而再次提议点击已聚焦输入框，本地冗余动作门禁拒绝，未执行第 2 动作。报告目录 `poc/output/web/generic_supervised_20260816_165535_80f5c585/`。
- 为恢复安全输入前置状态，独立会话 `78f1e14fa7b6458991dfa7dd5a6c3c69` 只要求把当前可见软键盘切换为英文直接输入模式。观察器识别到切换目标，但模型返回的元素归一化 bounds 为 `(0.73, 1.13, 0.83, 1.21)`，超出画面协议范围；两轮只读观察均失败关闭，`physical_actions=0`。报告目录 `poc/output/web/generic_supervised_20260816_165905_78f1e14f/`。
- 当前唯一阻塞：`input_verified_text` 的安全合同要求当前唯一输入框已聚焦且键盘 `keyboard_input_mode=direct_latin`；真实手机当前是中文拼音模式，而通用观察器尚不能为可见“英”模式切换键提供合法的归一化几何。直接输入被安全策略正确禁止，重复点击输入框被冗余动作门禁正确禁止，固定坐标或专用键盘补丁也被项目方向禁止。因此第二阶段仍未完成，真正阶段 3 仍未开始；在通用键盘控件几何证据问题解决前不得降低标准或继续真机动作。
- 停止真机动作后的完整回归为 Python `1156/1156`、前端协议与浏览器合同 `41/41`；`node --check poc/static/app.js`、`node --check poc/static/protocol_adapter.js` 和 `git diff --check` 通过。Python 仍只出现既知测试子进程 `ResourceWarning`，所有断言通过。

## 22. 2026-08-16 第二阶段再次严格复跑：刷新成功但同会话仍未成功终态

- 阶段状态不变：阶段 1 已完成；阶段 2 仍为“核心完成、严格单会话真机闭环待补”；真正阶段 3 的三个 App 硬件验收仍未开始。不得用本节的单动作或旧会话替代最终同会话 `succeeded` 证据。
- 本轮通用加固只处理真实失败暴露的合同边界：输入元素在聚焦前后允许唯一可信 input 语义别名漂移；模型审计后的输入框可由无 App/标签/命令/坐标依赖的本地单一边框收紧；“当前页面”不再误作具体命名页面；当前子目标视觉上下文与后续子目标审计隔离；`keyboard_key` 不能成为元素动作目标，可信画面证明软键盘可见且当前子目标明确要求隐藏时，Qwen 的可选动作收窄为已认证系统 `back`；刷新/重新载入等转移目标不能走 0 动作对象存在捷径。所有变化都缩小或保持执行权限，没有新增 App 分支、固定命令、固定步骤、验收页状态机或坐标补丁。
- 会话 `07322badf6c9488683c17a858ca63dbd` 在同一会话完成两个真实且 `matched` 的物理动作：聚焦输入框与输入 `agent`；两步均有前后 4 帧、新 observation/fingerprint、typed receipt 和 revision 推进。随后 Qwen 为隐藏键盘提出 `keyboard_key` 点击，本地策略正确拒绝，最终 `status=blocked`，不能作为第二阶段完成证据。
- 修复后的多轮 0 动作启动检查分别暴露并关闭了：高层目标含底层动作措辞、转移型子目标被误作可见对象完成、全局 `input_text`/继承的软键盘约束错误触发后续输入审计并覆盖刷新候选。所有这些失败均为 `physical_actions=0`，旧 observation、fingerprint 和 scope 均未复用。
- 最终全新会话 `d2445b84a60e4191bdc0d2259c7e78cf` 使用自然语言目标“在这个本地页面完成一次重新载入；重新载入之后，让唯一输入框中的内容为 agent，并让软键盘保持不可见。”。动作 1 的 Qwen 决策只绑定本地严格审计的唯一刷新图标，确认前重新采集 4 帧并验证 scope；机械臂只点击一次实际像素 `[682,27]`。动作后 4 帧证明输入框由 `agent` 变为空、软键盘隐藏，`action_outcome=matched`，前后 observation 为 `obs_271d854363794cae83d7e738534b288e` 与 `obs_fd3821572ad14a8f8a4e3790356d74b0`，fingerprint 为 `2b3a939fd2438ef08b3b` 与 `5414d677e449532c897c`，typed receipt 为 `receipt_a939b6a67a84467880ce25aecea98647`。
- 同一会话随后在动作后 DeepSeek 重规划处失败关闭：`当前本地页面完成重新载入` 被命名页面身份门禁误判为缺少结构化页面身份锚点，disposition 为 `blocked_replan_failure`；最终 `status=blocked`、`physical_actions=1`、revision 仍为 `1`、无新 scope。该动作没有重试，真机动作已停止。报告为 `poc/output/web/generic_supervised_20260816_193755_d2445b84/report.json`。
- 当前唯一阻塞已经更新为：动作后重规划仍会把带“当前本地页面”的重新载入完成声明误判为具体命名页面。必须先离线修正并证明该通用命名页面边界，再用全新会话从新 observation 开始；旧会话不得续跑或补写成功。第二阶段在取得同一全新会话至少两个动作并最终 `succeeded` 前仍未完成，真正阶段 3 仍不得开始。
- 当前离线回归：Python `1178/1178` 通过；前端协议 `26/26`、浏览器合同 `7/7 + 4/4`，合计 `37/37` 通过；`git diff --check` 通过。完整 Python 仍只有既知的子进程 `ResourceWarning`。最终 0 动作运行状态为 controller/camera online、`busy=false`、无活动任务或通用会话；项目 Uvicorn PID `40908`，卖家 `main.exe` 仍为 PID `19956`、启动时间 `2026-08-13T15:37:53.0769547+08:00`，未被启动、停止或重启，也未点击卖家全屏、横屏、竖屏或底栏。

## 23. 2026-08-16 第二阶段收尾复跑：具名页面边界已修复，动作后完成证据仍阻塞

- 第 22 节的具名页面误判已按通用语法边界离线修复：去除“当前页面”等指代后，只有带名称修饰语的页面、界面、视图等容器才要求结构化身份锚点；以无名称“页面/界面/视图”开头的状态谓词不再反向冒充页面名称。`系统设置页面`、`跨境订单结果页面` 等真实具名页面仍保持原身份门禁。没有增加 App 名称、固定命令、固定步骤、坐标或验收页状态机。
- 精确复现测试覆盖 `当前本地页面完成重新载入`、`页面重新载入后的可见状态`、matched controller transition 和动作后空输入框/键盘隐藏事实；同时保留真实具名页面正反测试。完整回归为 Python `1180/1180`、前端与浏览器合同 `41/41`，两个 JavaScript 文件语法检查与 `git diff --check` 通过。Python 仅有既知测试子进程 `ResourceWarning`。
- 只重载了项目 Uvicorn。普通 PTY 和沙箱桌面启动均无法发现交互桌面的卖家窗口，均在 0 动作状态停止；随后按既有交互桌面隐藏启动方式恢复服务，最终项目 Uvicorn PID 为 `23680`。卖家 `main.exe` 始终保持 PID `19956`、启动时间 `2026-08-13T15:37:53.0769547+08:00`，没有启动、停止或重启，也没有点击全屏、横屏、竖屏或底栏。
- 0 动作会话 `27fb80b59033430aa79d01b29b521d7a` 使用“唯一文本区域最终显示 agent”的高层目标；真实观察已证明唯一空输入框和键盘隐藏，但 DeepSeek 额外生成“文本区域可见”前置子目标，Qwen 因当前候选没有逐字 `agent` 而在动作前 blocked。该会话 `physical_actions=0`，没有生成确认范围或触摸手机。
- 全新会话 `051425f229c2421abfeaf65699986bb9` 使用目标“让当前本地页面唯一输入框中的内容最终为 agent；不得进行搜索、提交、发送、保存或发布。”。初始 0 动作观察 `obs_2286a6cf732d4932b81538b651fec26e`、fingerprint `0ad4b4376c6b644e98a9` 唯一绑定空输入框；Qwen 只提出 `tap_semantic` 聚焦该输入框，精确 scope 绑定当前 task、device、revision、subgoal、observation、fingerprint 和 action digest。
- 该确认只执行 1 次真实物理动作，落点回执为 `[398,776]`，没有自动重试。前后各 4 帧，动作后 observation `obs_26b93ee180154f0ea22f92b52afaa0eb`、fingerprint `8caf7775cdd272b1bbb0`；typed receipt `receipt_cf7191e7910d4bbba9a8d0c05edda45f`，`action_outcome=matched`、无 verification error。动作后可信画面严格证明唯一输入框 `value=""`、`focused=true`、`keyboard_layout="qwerty"`、`keyboard_input_mode="direct_latin"`，软键盘可见。
- 同一会话随后在动作后 DeepSeek 重规划处失败关闭：模型为 `ensure_input_visible` 填入当前 observation/typed controller ref 之外的完成证据，本地合同以 `子目标使用了当前观察之外的完成证据：ensure_input_visible` 阻断。最终 `status=blocked`、`physical_actions=1`、revision 仍为 `1`、无第二动作和新 scope；报告为 `poc/output/web/generic_supervised_20260816_201218_051425f2/report.json`。
- 阶段状态仍不变：阶段 1 已完成；阶段 2 仍为“核心完成、严格单会话真机闭环待补”；真正阶段 3 仍未开始。当前唯一阻塞更新为动作后 DeepSeek 对当前可见子目标使用了未绑定当前观察或 typed controller transition 的完成证据。必须先离线修正并保留伪证据拒绝反例，再从当前新观察创建全新会话；旧会话、旧 scope 和本次成功聚焦动作不得续跑、拼接或补写为成功。
- 停止真机后的最终状态为 controller/camera online、`busy=false`、无活动任务或通用会话；手机停在唯一空输入框已聚焦、英文直输 QWERTY 软键盘可见的本地验收页。第二阶段只有在修复后的全新同一会话至少两个物理动作并最终 `status=succeeded`、`active_subgoal=null` 后才能改为完成。

## 24. 2026-08-16 第二阶段最终收尾尝试：两动作均 matched，但 Qwen 协议漂移仍阻止成功终态

- 阶段状态仍为：阶段 1 完成；阶段 2 核心完成，但严格的“修复后全新同一会话至少两个物理动作并最终 `status=succeeded`、`active_subgoal=null`”尚未取得；真正阶段 3 的三个真实 App 验收未开始。本节任何单动作、两动作但非成功终态或 0 动作观察都不能替代该标准。
- 本轮只增加通用合同边界，没有 App 名称、固定命令、固定步骤、验收页状态机或坐标补丁：场景级容器身份与元素自身角色分离；DeepSeek 逐字复制单一可见证据子句时只规范回控制器已有完整证据；系统 UI 消失结果只对 `back/home/dismiss_overlay` 的精确缺席对象降级为 `scene_changed=true`；非模式切换目标中的缺字段可选 `mode_switch` 可在唯一输入与已知 QWERTY 模式独立成立时丢弃，但输入授权仍单独要求空值和 `direct_latin`；隐藏键盘目标允许只保留无 bounds 的存在/聚焦事实来授权认证 `back`，仍不能授权文字输入；输入结构审计明确返回 `keyboard.visible=false` 时，观察器 v40 才生成控制器所有的“输入结构只读审计确认软键盘不可见”证据，普通空 overlays 不得伪造该缺席事实。
- 最终离线回归：Python `1189/1189`；观察、Qwen 决策与通用编排关联回归 `375/375`；前端协议与浏览器合同 `41/41`；JavaScript 语法检查和 `git diff --check` 通过。完整 Python 仍只有既知测试子进程 `ResourceWarning`，所有断言通过。
- 会话 `39b0a99325ae4131a5e31dd4e28a26b3` 在同一会话完成刷新、聚焦和输入 `codex` 三个真实物理动作；前两个 receipt 为 matched，第三个动作后真实画面也逐字显示 `codex`，但当时 Qwen 连续两轮返回缺字段 `mode_switch`，结构化动作后观察失败，最终 `status=failed`。该动作没有重试，报告目录为 `poc/output/web/generic_supervised_20260816_210819_39b0a993/`。
- 会话 `112d6c8e8ffe4e80837bc23f6471a016` 的刷新动作 matched，receipt 为 `receipt_f4e0e951d71940ecabd42cbdae2fc2d2`；后续 Qwen 选择了逐字正确的“等待动作”按钮，却同时把候选标为 `goal_relevant=false`，本地策略正确拒绝，没有执行第二动作。会话 `f9b75b86c7c84f20be7208162bc44402` 的刷新前后语义相同，receipt 正确为 mismatched，未把像素变化冒充完成。
- 会话 `e0121503813d4a8caa4d5af0c363e28d` 是本轮最接近严格标准的证据：动作 1 聚焦空输入框，receipt `receipt_a9f912a4218e495ea4cf14d43aebdee4` 为 matched，revision `1→2`；动作 2 使用认证 `back` 收起键盘，receipt `receipt_84e05c7f5e104952b4030c47611ec100` 为 matched，revision `2→3`。两步均有前后 4 帧、新 observation/fingerprint，`physical_actions=2`。真实动作后画面和 scene 均证明键盘已隐藏、输入为空，但 Qwen 把浏览器系统导航栏误作疑似键盘并继续提出第三动作；一次 0 动作重观察仍未完成任务。为避免错误第三次 `back` 离开页面，该会话以 2 个 matched 动作安全取消，最终不是 `succeeded`，不能作为阶段 2 完成证据。
- 修复后的会话 `689e9177865f4ca698098e5bc4393b36` 的聚焦动作 receipt `receipt_52f01aa2b7224f51b745b1e0e7918751` 为 matched；进入隐藏键盘子目标后，Qwen 两轮返回协议外 `expected_result.system_ui.soft_keyboard_visible=false`，动作 2 在物理执行前 blocked。该精确缺席形状现已纳入上述仅限系统消失动作的严格归一化，并通过正反回归。
- 最新三个 0 动作启动再次证明当前阻塞来自在线 Qwen 协议遵循而非硬件：`5243db64e846479ab0a3209f3a83b95f` 和 `b4a129fb806d4c9881b1d47441ed01f7` 在隐藏键盘子目标中，明知本地可用动作已收窄为唯一 `back`，两轮格式修复仍提出被排除的 `tap_semantic`；最后的独立隐藏键盘准备请求目录 `poc/output/web/generic_supervised_20260816_220643_128c247f/`，第一轮把仅 input 可用的 `keyboard_layout` 写到 container，第二轮返回不可解析 JSON。三次均为 `physical_actions=0`，没有触摸手机。
- 当前唯一阻塞：已配置的 Qwen 在同一稳定键盘画面上连续违反通用观察或动作白名单合同，严格控制器无法安全生成唯一 `back` 确认范围。不得通过放宽 `tap_semantic`、复用旧 scope、固定坐标、底层返回接口或自动重试绕过；需要模型在新 0 动作观察中同时给出合法 scene，并遵守控制器下发的唯一认证动作，之后才能创建全新同会话两动作验收。
- 最终运行状态：observer `2026-08-16-generic-scene-observer-v40`；controller/camera online、`busy=false`、无 active task/session；手机仍在本地验收页，唯一输入框为空、聚焦且软键盘可见。卖家 `main.exe` 始终为 PID `19956`、启动时间 `2026-08-13T15:37:53.0769547+08:00`，从未启动、停止或重启，也未点击卖家全屏、横屏、竖屏或底栏。项目变更只允许本地提交，不推送。

## 25. 2026-08-16 第二阶段收尾合同收敛：截断已消除，v2 待一次新在线门

- 本轮先以 `阶段2收尾验收台账.md` 冻结验收条件和停止规则，再处理同一根因，没有继续为单个页面、键盘或命令叠加补丁。提交 `61d55c4` 将 Qwen 生产边界缩为本地合法 choice 选择，task/device/revision/observation/fingerprint、候选语义和 bounds 全由本地可信数据水合；远程格式重生成已删除，非法首轮输出直接 0 动作失败关闭。
- 场景观察器升级到 `2026-08-16-generic-scene-observer-v41`，普通结构化输出预算为 1800 并启用供应商 JSON object 模式。第一批完整离线回归为 Python `1194/1194`、前端/浏览器 `41/41`，版本补测 `136/136`。
- 只重载项目 Uvicorn 后，controller/camera online、`busy=false`、活动任务/会话均为 0，工作树校准路径未变；卖家 `main.exe` 保持 PID `19956`、启动时间 `2026-08-13T15:37:53.0769547+08:00`，没有被启动、停止或重启。
- 唯一一次新 0 动作会话 `690af2f3d4e74d97a92d43dba1f2237c` 使用目标“把当前可见的软键盘收起，保持输入框内容不变并留在当前页面；不得输入、搜索、提交、发送、保存或发布。”。真实 4 帧观察 `obs_1d6caa4ae02e4a49a27f5c7ddd989051`、fingerprint `3708a9d7d88c6ce7ef3f` 证明唯一空输入框已聚焦、direct_latin QWERTY 键盘可见；Qwen 已变为 `finish_reason=stop`、输出 211 token，因此原 1200 token 截断根因已消失。
- 该会话仍在动作前安全阻塞：Qwen 把 `local_audited_input_1` 放到 `expected_result.element_state` 的键位，本地拒绝协议外字段；`physical_actions=0`、无 confirmation scope、无自动重试。按预设停止规则，本轮没有第二次在线采样或任何机械臂动作。报告为 `poc/output/web/generic_supervised_20260816_235050_690af2f3/report.json`。
- 随后离线完成 visual selection v2：每个 choice 的可验证 `expected_result` 也由本地通用动作合同生成，Qwen 只负责 `status`、`choice_id`、完成判断、置信度和理由，不能再生成控制器嵌套对象。输入值仍逐字来自 DeepSeek 结构化目标，模型不能改写；Qwen 即使面对唯一 choice 也必须明确选择，本地不会自动代选。
- v2 专项 `91/91`、DeepSeek/观察/Qwen/通用编排关联 `517/517`、Python 全量 `1195/1195` 均通过；完整回归只有既知测试子进程 ResourceWarning，所有断言通过。前端未变，沿用本轮已通过的 `41/41`；`git diff --check` 通过。
- 当前阶段结论不变：阶段 1 完成；阶段 2 核心完成，但 visual selection v2 尚未经过一次新的 0 动作在线门，更没有取得修复后全新同一 session 至少两个物理动作并最终 `status=succeeded`、`active_subgoal=null` 的严格证据；真正阶段 3 仍未开始。下一次只能先做一次全新 0 动作在线门，成功形成合法新 scope 后才可进入真机；若同类模型合同错误再次出现，继续停止而不是放宽白名单或重试。
- visual selection v2 已提交为 `d2fae2d` 并只重载项目 Uvicorn；当前实际监听 PID `41488`，observer v41 / 1800 token，controller/camera online、`busy=false`、无活动任务或会话。卖家 `main.exe` 仍是 PID `19956`、原启动时间。重载后的检查只读取 `/api/device`，没有调用模型、创建 session 或触发手机动作。

## 26. 2026-08-17 第二阶段严格收尾复核：单会话成功证据仍未取得

- 阶段状态保持不变：阶段 1 已完成；阶段 2 仍为“核心完成、严格单会话真机闭环待补”；真正阶段 3 的三个真实 App 硬件验收没有开始。本节的两动作非成功会话、单动作失败会话和 0 动作观察均不得替代“全新同一 session 至少两个物理动作并最终 `status=succeeded`、`active_subgoal=null`”的严格证据。
- 本轮先完成通用合同加固，提交链为 `59d6648`、`339da11`、`0fdf75a`、`3fa07ec`、`3ba4d5a`、`24bbd8b`、`4d81aa8`、`050f966`、`5cb7b27`。变化只涉及动作后完成证据、否定状态转移、输入审计隔离、稳定语义重绑定、逐字 UI 身份锚定，以及对唯一逐字可见文字控件的三帧本地 OCR 几何收紧；没有增加 App 名称分支、固定命令、固定步骤、验收页状态机或 App 坐标补丁。
- 会话 `547282de17f84976892c7b2d6cf170c5` 在同一 session 执行了两个真实 `back`，每步都有前后 4 帧、observation/fingerprint、typed receipt 和 revision 记录；第二个动作没有产生预期转移，最终 `blocked`，仅证明两动作链路可运行，不能证明阶段 2 完成。报告为 `poc/output/web/generic_supervised_20260817_022459_547282de/report.json`。
- 会话 `9ea2c9fb373f46ea814cb74c1ad7edcc` 使用全新自然语言多动作目标。动作前 scope 为 revision `1`、observation `obs_145169cd3f6c4b6eb4f2c4ed6942efeb`、fingerprint `1381cf6b29e88cca8658`；三帧本地 OCR 将“返回验收模式选择”收紧到唯一 bounds，机械臂只执行一次 `tap_semantic`，实际回执 `[189,141]`，动作后 4 帧和新 observation `obs_e375060bb8e141f3ae03b0c39f9d1605` 已保存。页面没有进入选择页，DeepSeek 按逐字完成合同阻塞，最终 `status=blocked`、`physical_actions=1`，没有重试。报告为 `poc/output/web/generic_supervised_20260817_025316_9ea2c9fb/report.json`。
- 0 动作替代路径均安全停止：`ffa5e3fd3d65463989e43eb2eac95e13` 连续两次只读观察仍只提出被目标排除的系统 `back`，随后以 `physical_actions=0` 取消；`b8eb178293e2492aa47e58cfb53dd643` 的刷新目标没有形成经独立图标审计签发的刷新候选，Qwen 越级提出点击当前输入框，未确认即取消；最终模式切换启动目录 `poc/output/web/generic_supervised_20260817_031218_792edfd3/` 连续两轮把可见键盘模式键写成越界 bounds `(0.68,1.13,0.79,1.21)` 与 `(0.73,1.13,0.81,1.21)`，建 session 前即失败关闭，`physical_actions=0`。
- 为避免盲目扩大容错，曾以提交 `25ef65c` 只允许一个越界 `keyboard_key` 候选被撤销并交给既有专用输入审计；正反回归 `392/392` 通过。但唯一新 0 动作在线门目录 `poc/output/web/generic_supervised_20260817_032047_3a760220/` 仍连续返回 `(0.73,1.13,0.81,1.20)` 与 `(0.68,1.13,0.78,1.21)`，证明实际畸形结构不满足该安全撤销条件。该无效假设已立即以 `3f6c479` 完整 revert，没有继续扩大字段白名单或执行物理动作。
- 当前唯一阻塞收敛为：真实手机停在空输入框已聚焦且中文拼音 QWERTY 软键盘可见的本地页；后续 `input_verified_text` 必须先取得 `direct_latin`，但在线观察连续无法为可见模式切换键给出合法 0..1 几何。不得把 1130 类数值按截图像素强行换算，不得写固定键盘坐标、放宽越界门禁、复用旧 scope 或自动重试。页面链接点击和刷新候选路径已经分别以真实失败或 0 动作缺证据否定，不能拼接成成功。
- 最终离线回归为 Python `1207/1207`，关联回归 `462/462`；`node --check poc/static/app.js`、`node --check poc/static/protocol_adapter.js` 与 `git diff --check` 通过。纯 Node 合同本轮 `30/30` 通过；两个 Playwright 浏览器合同因当前工作树及原项目均无 `playwright` 模块而未加载，前端代码本轮未修改，不能把该依赖缺失写成浏览器合同通过。
- 最终运行状态以本节提交后重新加载的项目服务为准，observer `2026-08-17-generic-scene-observer-v42`；controller/camera online、`busy=false`、活动 task/session 均为 0。卖家 `main.exe` 始终为 PID `19956`、启动时间 `2026-08-13T15:37:53.0769547+08:00`，没有被启动、停止或重启，也没有点击卖家全屏、横屏、竖屏或底栏。全部提交仅保留在本地，未推送 GitHub。

## 27. 2026-08-17 键盘模式切换单一权威离线修复

- 第 26 节的连续越界现象已重新分类为通用观察合同缺陷，而不是某个键位或坐标补丁需求：显式模式切换目标在专用输入结构审计之前，普通 compact scene 仍可重复生成并阻断模式键候选，形成两个模式与几何权威来源。
- observer v43 将模式、方向和模式键几何统一交给既有专用全帧输入结构审计。compact prompt 不再要求该键；模型仍返回的无动作字段初步候选会被完全撤销且不复用任何值。该目标同时跳过一次冗余 targeted refinement，随后只有专用审计通过严格 schema、键盘包含、标签、方向、置信度和 `0..1000` 几何时，本地才重建唯一候选。
- compact 对象含动作字段、普通目标/input 越界、专用审计越界或模式冲突仍失败关闭。没有增加 App、命令、固定步骤、键盘坐标、截图模板或专用状态机。
- 离线结果：最小正反 `7/7`、观察器 `157/157`、关联 `559/559`、Python 全量 `1209/1209`；JavaScript 语法通过，前端与浏览器合同顺序执行 `37/37`，`git diff --check` 通过。
- 当前只能写“离线修复与完整回归通过”。尚未重载项目 Uvicorn、尚未进行新 0 动作在线门或真机动作；阶段 2 继续为“核心完成、严格单会话真机闭环待补”，阶段 3 仍不得开始。
