# 手机自动点击器项目交接文档

更新时间：2026-08-28（Asia/Shanghai）

本文件只保留当前权威状态。历史过程不再追加到这里；需要追溯时查看 Git 历史和 `历史文档索引.md`。

## 1. 默认必读顺序

新对话只需依次读取：

1. `项目最终目标.md`；
2. `AGENTS.md`；
3. 本文件；
4. 本文件“当前唯一主线”指向的一个验收台账。

除非正在追查历史证据，不得把旧阶段台账、旧交接或整个 `poc/output` 当作默认上下文。

## 2. 产品方向与唯一权威

- 产品是通用多 App 手机视觉操作 Agent，不是微信、抖音或任一固定 App 脚本。
- 主循环：按当前实际活动子目标取得新截图 → Qwen 结构化当前画面并由本地 canonical 选择一个动作 → Controller/机械臂执行一次 → 动作后新截图验证 → DeepSeek 推进或重规划；活动子目标变化后必须重新截图，禁止预测或复用后继子目标观察。
- 唯一动作协议：`2026-08-20-canonical-action-v1`。
- 当前正式高层任务图协议：`2026-08-20-deepseek-typed-task-graph-v4`。
- 只有 `authentication` 和 `financial_transaction` 需要确认。输入、发送、提交、关注、评论、发布、收藏、删除、权限变更、打开 App、点击、滑动、长按、拖动、返回和 Home 默认自动执行。
- 设备、相机、控制器、观察、目标唯一性或机械标定问题属于运行故障，不得伪装成产品风险。
- 旧协议只能作为 Git 历史或隔离测试证据，不得注册路由、导入运行时或拥有否决权。

完整边界见 `用户决策与协议边界.md`。

## 3. 当前仓库与运行状态

- 分支：`codex/integrate-phase2-stage3`。
- DDD 迁移前代码基线：`d9ff0f3 refactor: remove uncalled compatibility code`；已完成会话生命周期、设备执行与独占、会话证据持久化、canonical 选择回执、一次性确认作用域、已验证 App 表面血缘、运行会话聚合和每设备相机协调八个真实切片，均删除旧入口且保持行为等价。
- 交接精简提交：`147b518 docs: slim active project handoff`。
- 旧固定 App 运行链退役提交：`571bf1c refactor: retire fixed app runtime paths`。
- 旧固定 App/旧协议二次物理清理已完成；跟踪源码、测试与 259 个未跟踪旧回放历史
  文件均已物理删除并通过完整回归，本文件与代码一并纳入本地提交。
- 项目 Uvicorn：监听 `127.0.0.1:8765`；本轮重载返回父 PID `6448`，当前监听 PID `7256`，运行 PID 会漂移，继续前必须通过官方客户端重新核对。
- 卖家 `main.exe`：当前 PID `4860`。不得由 Codex 启动、停止、重启或操作其全屏、横屏、竖屏和底栏。
- 严格长文本真机闭环已完成。卖家文字对话框 transport 已被真机证伪并按用户决定从生产代码、配置与测试中永久物理删除；正式输入只使用手机可见键位。
- 本地提交允许；未经单独授权不得推送远端。

运行状态会漂移。后续需要真机时，必须用 `poc/agent_api_cli.py` 或 `poc/local_agent_api_client.py` 重新核对 OpenAPI、设备、controller、camera、busy、活动会话和新 observation；不得沿用本节 PID 或历史会话作执行依据。

## 4. 阶段结论与证据边界

- 阶段 1：已完成。
- 阶段 2：文档记录的权威 session 为 `18c29c4db30246d1a3ffaa8f5116f047`，终态 `succeeded`，同会话完成打开设置和 Home 两个动作。原报告路径 `poc/output/web/generic_supervised_20260817_111820_18c29c4d/report.json` 当前已不存在；结论可由 Git/历史文档追溯，但在找回原始报告前不得声称磁盘上仍有完整原始证据。
- 阶段 3：三个不同真实 App 的正式验收已完成，报告仍在磁盘：
  - Browser：session `61f26496f1a046118468e922eacb5741`，`succeeded/3 actions`；
  - Settings：session `3756155cb1a04e9bad66ed216f0d97bf`，`succeeded/2 actions`；
  - WeChat：session `778e5031798048f5abe0f02676d337e2`，`succeeded/2 actions`。
- 上述阶段结论只覆盖对应合同，不代表复杂输入、换行、多字段和普通发送的新闭环已经完成。

## 5. 当前复杂输入真实状态

- 提交 `7c1d039` 已实现占位文字消失后以 typed `input_field_id` 和授权 payload 精确前缀维持输入目标身份。
- 提交 `85ae1ab` 已实现单行长文本横向裁剪时，以 typed 字段身份和可见后缀恢复完整输入值的通用验证。
- 新提交 `d6142df` 保留输入结构审计的可见字段标签，`e819e96` 允许明确清空已有草稿时在无新输入 payload 的情况下生成唯一 `clear_verified_text`，`35be67c` 确定性修复 DeepSeek 的唯一 transport 字段/枚举投影错误。
- 提交 `094b4db` 把非空 typed `input_field_id` 提升为动作前后输入框绑定的唯一身份权威；元素临时 ID、占位文字、可见值和几何只在没有 typed 身份时退化使用。提交 `fe82737` 修复聚焦后页面上移时，空审计重试裁剪仍按旧粗框截掉当前输入框的通用观察缺口。
- 清空 session `c850fdcee747422494d16083547176e5` 只执行一次后仍剩 `abcdefghijklmno`，回执 mismatched；没有重试。刷新 session `4286a4ad0d8c4a39983c1dc7b8677bc1` 的唯一 audited reload tap matched，但动作后页面变成 `browser/web_error_page`，网页无法访问且输入框不存在。
- 最新失败报告分别为 `poc/output/web/generic_supervised_20260821_163223_c850fdce/report.json` 与 `poc/output/web/generic_supervised_20260821_164034_4286a4ad/report.json`。
- 恢复 session `4f68370ee30f4786af1f2d19bcdfe196` 执行一次 audited reload，`matched/succeeded`，重新显示空白长文本验收页。随后 session `d066e2bd13154c34ba971a2cca399df7` 的聚焦点击真实执行一次，`physical_actions=1`；动作后专用审计返回 `application_inputs=[]`，终态为 `post_action_failure`，没有输入字符。新 session `07f293e3ff2043c4b96512a4aee6859b` 从已聚焦空框开始，但在 `physical_actions=0` 再次出现同类空审计并失败；达到连续两次门后已停止在线采样。
- 两次失败离线证明：键盘出现后输入框上移，但第二次审计裁剪仍由旧粗框从画面约 `0.47` 处开始，完整输入框位于裁剪上方；这不是输入 transport、字段身份、风险边界或页面不可访问。`fe82737` 只用第一次审计已证明的键盘大框扩展裁剪选择，重试仍须独立重建字段和键盘。
- 严格长文本 session `e2134caef5d7425c8b71a07a5eb5bddc` 已在同一新会话完成 20+17 两个 `input_verified_text`，最终逐字值为 `abcdefghijklmnopqrstuvwxyzabcdefghijk`，`physical_actions=2`、两步 matched、终态 `succeeded/active_subgoal=null`。报告：`poc/output/web/generic_supervised_20260821_175502_e2134cae/report.json`。
- 换行第一次正式 session `07dbde6449e74965b68530e2a8d3eabd` 实际仍在旧长文本页，输入 `first` 后观察审计失败；这是验收起始页面错误，不能算换行证据。随后用临时 303 路由把浏览器明确加载到空白多行页，临时服务已停止并删除。
- 两次相同正式换行 start 在 0 动作处把普通输入错标为无绑定 `effect`；提交 `b8f6e1f` 依据 typed `input_text/input_fields`、局部输入语义和否定外部效果短语做确定性归一化。定向 `3/3`、DeepSeek 相关 `33/33`、最终完整 Python 回归 `1354/1354`，`skipped=54`。
- 修复加载后的正式换行 session `2513efc8e1fb4f1c90c752e12f4941d4` 成功建立并以 0 动作完成 locate；第一个聚焦动作执行后，画面显示字段已聚焦、QWERTY 和真实换行键可见，但动作后采集清晰度比 `0.794` 低于门槛 `0.800`，终态 `failed/physical_actions=1`。按规则未重试。
- 本轮进一步修复了会话/DeepSeek 对内部 LF 的折叠、Qwen 未公开的键盘最小几何合同、键盘顶起时过窄 retry crop，以及模型把真实 LF 二次转义成 `\\n` 的唯一可逆 transport 错误。相关回归 `529/529`、完整 Python `951/951`（`35.518s`）。
- 最新 session `bdeb930dd5554438883068a2bc5195f3` 已保留真实 LF，但两轮 Qwen 输入审计依次因键盘 bounds 高度不足和 QWERTY anchors 排列异常失败，`physical_actions=0/status=failed`。报告：`poc/output/web/generic_supervised_20260821_205315_bdeb930d/report.json`。达到同类模型合同两次停止门后未再采样。
- 后续通用修复将稳定三帧本地 OCR 行中心前移到输入审计签发阶段；typed 多行字段或 payload 含 LF 时允许高输入区域；只读输入定位可用 compact 唯一输入框粗框做一次独立 crop 复核；当前分段不消费且仅坐标无效的可选 mode/case 控件被撤权。相关回归 `425/425`、完整 Python 回归 `960/960`。
- `v66` 全新 session `ba9ec9a281274fb68b0e6abf28e66a71` 已建立唯一 `local_audited_input_1`、本地 OCR anchors `q/p=708`、`a/l=780`、`z/m/backspace=853`，并生成第一段 `first` 的唯一 `input_verified_text`。确认前独立几何审计把目标 crop 顶边判为截断，`geometry match 未证明完整可见的整个控件`；authority 已消费但 `physical_actions=0`，会话随后取消。报告：`poc/output/web/generic_supervised_20260821_215149_ba9ec9a2/report.json`。
- 本轮已用按输入框自身高度扩展的通用 crop 修复上述截顶，并允许三帧 OCR 中一帧丢行但仍要求两帧一致；聚焦动作不再使用键盘弹出前后不可比的相对清晰度。session `978ca475c37747d48a4cd6b361306c51` 的聚焦真实执行一次后仍被旧相对清晰度 `0.794 < 0.800` 拒绝，报告：`poc/output/web/generic_supervised_20260821_225619_978ca475/report.json`。
- session `626f2ed204b64e5ab65c576f72b555e8` 已生成第一段 `first` 的唯一输入动作并执行一次卖家批量输入，但旧实现只查找原生 `IDOK`，而卖家对话框是单 HWND 的 Qt 窗口，终态 `failed/physical_actions=1`；没有把全局 Enter 发给手机。报告：`poc/output/web/generic_supervised_20260821_230506_626f2ed2/report.json`。
- 提交 `a0f4d5f` 加载后的全新 session `f0caeec256084889a22340af7287ff0d` 只确认一次 `first line` 批次。动作后真实页面和正式状态均证明值变成 `first\n,line\n`，不是 `first line`；回执 `mismatched`、`physical_actions=1`、终态 blocked。已按规则停止，未执行正式换行或第二行。当前手机保留该错误草稿，下一轮不得当作空白起点。
- 用户已明确决定永久废除卖家文字对话框。`robot_core.py`、`robot_gui_poc.py`、`controller_config.json` 中的对话框打开、填字、确认、关闭、Qt/IDOK 兼容、批量能力声明和配置已物理删除；没有环境变量、备用入口或失败回退。
- 正式输入规划仍只保留 `agent/domain/verified_text_transaction.py` 一套权威。连续小写词使用真实手机可见键位序列，空格与真实 LF 分别作为独立可见键动作；`first line\nsecond line` 当前确定性规划为 7 步。该路径优先恢复正确性，尚未形成新的快速复合动作。
- 防恢复扫描只允许退役名称出现在测试黑名单中；相关回归 `569/569`、完整 Python 回归 `965/965`（`35.284s`）通过。最后删除两个无引用的对话框专属常量后，退役/硬件适配定向回归 `89/89` 再次通过。用例较此前减少 7，来自删除 8 个旧对话框专属测试并增加 1 个物理缺失测试。
- 最新严格换行 session `6e8883c3b6474c34a530462e155d6e24` 已依次真实完成聚焦、`first`、右下角 `↵` 和 `second` 四个动作；最终 observation 逐字显示同一 `input_field_1` 的值为 `first\nsecond`。但动作后模型把 `screen_id` 从“通用动作具机验收页”改写为“通用动作真机验收页”，旧 Controller 因页面描述漂移把最后一步判为 mismatched，正式 session 终态 blocked，不能算换行验收成功。报告：`poc/output/web/generic_supervised_20260822_164614_6e8883c3/report.json`。
- 当前批次把非空 typed 字段身份提前为动作后同一输入框权威；任意页面措辞漂移可由同字段、同输入类型/多行属性、同聚焦、同键盘模式和同相机方向桥接，不同字段 ID、真实包名切换和键盘模式变化仍拒绝。最小 `4/4`、相关 `560/560`、完整 Python `999/999`（`36.944s`）通过。
- 换行、多字段和新的普通发送同会话成功证据仍未完成；多字段与发送尚未开始。

复杂输入历史细节保留在 `复杂输入闭环交接文档.md` 与 `复杂输入完整批次验收台账.md`，但它们不是当前仓库收敛任务的默认必读文件。

## 6. 当前唯一主线

当前唯一主线已切换为 `20k核心重构验收台账.md`：在保持通用多 App、DeepSeek 高层任务图、每步一次
Qwen 单步观察、唯一 canonical 动作、本地 Controller、机械执行与证据、公开 API 和现有风险边界不变的
前提下，将 Git 跟踪的 `poc/agent/**/*.py` 生产代码从 `45158` 个物理行真实收敛到少于 `20000` 行。
禁止通过移出统计目录、压缩排版、删除仍有效测试或隐藏兼容实现凑数；内部目标为 `<=19500` 行。
本阶段只做离线重构与自动验证，不重载项目 API、不调用在线模型、不执行真机动作，也不操作卖家
`main.exe`。

20k 重构第一批已完成：typed 任务图继续作为唯一高层权威，Semantic IR 收敛为确定性只读视图；
`task_semantic_ir.py` 由 `2424` 行降至 `903` 行，`poc/agent` 生产总量由 `45158` 行降至 `43637` 行。
DeepSeek/语义/canonical/Qwen/最近任务/Mock/Orchestrator 相关回归 `312/312`，本批唯一完整 Python
回归 `922/922`（`112.714s`）通过。未重载项目 API、未调用在线模型、未执行真机动作或操作
`main.exe`。下一切片是收敛 `task_graph.py` 的模型 wire、领域状态和历史规范化分支。

以下运行链简化第一至第五批是本次 20k 重构的干净基线与历史证据，不再是当前最终停止条件。

轻量、渐进式 DDD 模块化单体迁移已在第四十二批完成；严格串行截图合同基线为本地提交 `cce6517`，输入正文单一权威修复为 `6b5ef08`。可选预编辑粗框不再跨对象否决独立应用输入框，相关修复提交 `a44905a`、文档提交 `c9fd401` 已完成；历史 session `246fff8b...` 仍保持失败终态，未被改写为成功。

用户批准的“运行链简化第一批”已完成，只做行为等价的编排收敛：

1. **已实现能力**：开始、重新观察、效果确认后观察和动作后同子目标续接现共用 `_stage_current_observation_decision()`；三个动作前观察入口共用 `_build_and_record_current_observation()`。开始路径已删除独立的 typed required-action 能力裁决，统一由正式 `_decide_next_action()` 处理。
2. **唯一性证据**：AST 防恢复测试证明 `_decide_next_action()`、Qwen 决策证据写入和 canonical selection receipt 的运行调用点都只存在于共享 helper；动作后独立可信观察仍由 `_confirm_one_locked()` 保留，未与动作前观察混用。
3. **变化样本**：允许动作、Qwen blocked、fresh refresh、效果确认后观察、动作后同子目标续接、一次新观察纠正，以及设备不支持 `open_recent_apps` 时 0 次 Qwen/0 物理动作的 capability gap 均通过回放；开始路径在 capability gap 下仍保持 `qwen_decision=null/controller_decision=null`。
4. **验证结果**：最小正反/变化 `4/4`、编排与模块边界相关回归 `206/206`、本批唯一完整 Python 回归 `929/929`（`111.910s`）通过；静态编译和 `git diff --check` 通过。生产编排器由 `7092` 行降至 `7018` 行，净删除 `74` 行重复运行逻辑。
5. **边界与结论**：公开 API、模型调用次数、canonical 协议、风险边界、动作后验证、机械执行和真机语义均未改变；没有扩大到 scene observer、输入合同或 DeepSeek 图结构。

本批只运行离线测试，没有重载项目 API、调用在线模型、创建/恢复 session、执行真机动作或操作卖家 `main.exe`；因此这是内部简化与完整自动回归完成，不是新增真机验收证据。严格换行、多字段、普通发送和阶段 1–3 的既有真机结论均未改变。

用户批准的“运行链简化第二批”已完成，本批只删除经反向调用图证明不在正式运行链上的入口和其自循环测试：

1. **已删除旧路径**：移除 4 个从未被正式 adapter 调用的输入确认恢复 helper，以及无调用的 lineage 包装、编排器 context 取值器、`DeviceExecutor.action_kinds`内省、测试专用 orientation credential 铸造器和旧坐标解析包装。与之对应的直调测试同步删除，不保留 facade、兼容开关或第二权威。
2. **保留真实动态链路**：`vision_swipe_up/down/left/right` 虽然不存在静态显式调用，但 `RobotDeviceExecutor` 会通过动态方法名分发，因此已明确保留；真实标定建立/保存、覆盖边界校验、OCR grounding、canonical 选择和机械执行均未改变。
3. **防恢复证据**：新增源码断言，固定旧输入恢复 helper 和旧标定目标解析入口必须继续不存在。其他被静态扫描列为低引用的入口均已追到 Web 组合根、标定脚本、HTTP framework 回调或动态执行器的真实调用。
4. **规模与验证**：`poc/agent` 生产 Python 由 `45859` 行降至 `45383` 行，净删除 `476` 行；代码与测试差异为新增 `50`/删除 `1082`，净减少 `1032` 行。标定相关 `32/32`、受影响模块 `570/570`、本批唯一完整 Python 回归 `920/920`（`113.404s`）通过；静态编译和 `git diff --check` 通过。
5. **边界与结论**：公开 API、Qwen/DeepSeek 调用次数、串行截图闭环、canonical 协议、风险边界、动作后验证和真机语义均未改变。本批未重载项目 API、未调用在线模型、未执行真机动作，也未操作卖家 `main.exe`。

用户批准的“运行链简化第三批”已完成，本批收敛单步观察器内同一份 passive scene element wire schema：

1. **单一 schema 权威**：原观察器在 6 个运行分支分别复制同一份 8 字段 element 表，并在 5 个分支分别实现同一份嵌套 action-like key 递归检查。现只保留 `_PASSIVE_SCENE_ELEMENT_FIELDS`、`_ACTION_LIKE_WIRE_KEYS` 和一组共享 helper，局部副本已全部删除。
2. **业务语义不合并**：键盘模式审计、输入结构审计、focus-only surface、目标几何恢复和非输入键盘开关仍各自保留原业务条件；共享 helper 只回答“wire 形状是否精确、是否夹带动作指令”，不创建候选、不裁剪坐标、不放宽观察。
3. **正反与防恢复**：标准 8 字段 element 仍被接受；深层 list/dict 内夹带 `action=tap` 仍被拒绝；额外字段仍不是标准 passive shape。源码断言固定旧 `exact_fields`、`action_like` 和局部递归函数不得恢复。
4. **规模与验证**：观察器由 `7052` 行降至 `6912` 行，生产 `poc/agent` 由 `45383` 行降至 `45243` 行，净删除 `140` 行；代码与测试差异新增 `99`/删除 `195`，净减少 `96` 行。最小 `1/1`、观察器 `23/23`、受影响链 `527/527`、本批唯一完整 Python 回归 `921/921`（`113.873s`）通过；静态编译和 `git diff --check` 通过。
5. **边界与结论**：公开 API、单步 Qwen 调用次数、观察协议、canonical 候选、Controller、风险边界和真机语义均未改变。本批未重载项目 API、未调用在线模型、未执行真机动作，也未操作卖家 `main.exe`。

用户批准的“运行链简化第四批”已完成，本批收敛 typed 输入账本中 6 种待验证动作重复的输入表面绑定与记录组装：

1. **单一 pending 构造权威**：`input_verified_text`、中文预编辑、换行、键盘状态切换、IME 候选提交和逐字符 literal 过去各自复制页面、typed 字段、bounds、fingerprint 校验及 `TypedInputLineage` 组装；第四批先收敛为一套 pending 表面解析与记录构造，第五批又将记录构造提升为 pending/verified 共用的唯一权威，见下节。
2. **动作合同继续独立**：六个入口仍分别验证正文链、拼音、真实换行、键盘模式、候选绑定和逐字符键位；各自的 receipt 协议、source、错误信息与 digest 输入均未合并或改写。共同 helper 不生成动作、不接受新输入类型，也不改变 verified lineage 路径。
3. **正反、变化与防恢复**：已覆盖已知 App、unknown App + typed `input_field_id`、unknown App + unknown 字段拒绝、换行、中文预编辑、IME 候选和键盘状态切换；AST 断言固定六个 pending builder 必须复用唯一表面解析与记录构造，且不得重新直接构造 `TypedInputLineage`。
4. **规模与验证**：领域文件由 `1828` 行降至 `1756` 行，生产 `poc/agent` 由 `45243` 行降至 `45171` 行，净删除 `72` 行；代码与测试差异新增 `165`/删除 `200`，净减少 `35` 行。最小 `1/1`、输入账本 `37/37`、受影响链 `514/514`、本批唯一完整 Python 回归 `922/922`（`112.514s`）通过；静态编译和 `git diff --check` 通过。
5. **边界与结论**：公开 API、Qwen/DeepSeek 调用次数、canonical 协议、输入正文权威、风险边界、机械执行和真机语义均未改变。本批未重载项目 API、未调用在线模型、未执行真机动作，也未操作卖家 `main.exe`。

用户批准的“运行链简化第五批”已完成，本批继续收敛 typed 输入账本的正式 verified 记录路径：

1. **九个入口共用一份记录构造**：6 个 pending 与文字、换行、逐字符 3 个 verified builder 现在都只调用 `_build_input_lineage()`；`TypedInputLineage` 字段映射、digest、descriptor、时间和 `validate()` 不再由不同生命周期各复制一份。
2. **verified 表面解析只有一处**：动作后 App、screen、typed `input_field_id`、bounds 的选择与合法性现统一由 `_resolve_verified_input_surface()` 负责。普通文字/逐字符允许既有表面身份连续性，真实换行必须绑定同一 typed 字段；这两个不同的 fallback 成立条件仍留在动作入口，没有被宽化成一条模糊规则。
3. **防恢复与行为证据**：AST 断言覆盖全部 9 个入口，要求各自复用对应表面解析器与唯一记录构造器，并禁止重新直接构造 `TypedInputLineage`。既有测试继续覆盖已知/unknown App、typed 字段变化、screen fallback、软换行显示、真实尾随 LF、descriptor 和硬件 receipt。
4. **规模与验证**：领域文件由 `1756` 行降至 `1743` 行，生产 `poc/agent` 由 `45171` 行降至 `45158` 行，净删除 `13` 行；代码与测试差异新增 `140`/删除 `141`，总量净减少 `1` 行，同时删除三份 verified 记录组装权威。最小 `1/1`、输入账本 `37/37`、受影响链 `514/514`、本批唯一完整 Python 回归 `922/922`（`111.599s`）通过；静态编译和 `git diff --check` 通过。
5. **边界与结论**：公开 API、pending/verified lifecycle、receipt 内容、Qwen/DeepSeek 调用次数、canonical 协议、输入正文权威、风险边界、机械执行和真机语义均未改变。本批未重载项目 API、未调用在线模型、未执行真机动作，也未操作卖家 `main.exe`。

## 7. 本轮停止条件

- 发现旧模块仍被正式通用链路使用时，先提取通用能力再删除，不以 import 断裂换取表面清理。
- 相关测试失败时停止扩大删除范围，先定位通用链路回归。
- 不删除 `poc/output/web` 中的真实动作证据、`poc/evals` 回归夹具、`poc/.venv`、根目录 `录屏复核副本.mp4` 或任何用户文件。
- 不启动、停止、重启或操作卖家 `main.exe`。项目 API 只能在已授权范围内独立重载；真机仍须按官方客户端、全新观察和一次一动作合同执行。

## 8. 关键入口

- 正式 Web/API：`poc/web_app.py`
- DDD 会话领域合同：`poc/agent/domain/session.py`
- DDD 设备执行合同：`poc/agent/domain/device_execution.py`
- DDD 会话证据合同：`poc/agent/domain/session_evidence.py`
- DDD canonical 选择回执：`poc/agent/domain/canonical_selection.py`
- DDD 一次性确认作用域：`poc/agent/domain/confirmation_authority.py`
- DDD 已验证 App 表面血缘：`poc/agent/domain/app_surface_lineage.py`
- DDD 会话应用用例：`poc/agent/application/universal_agent_sessions.py`
- DDD 运行会话聚合：`poc/agent/application/runtime_session.py`
- DDD 进程内仓储：`poc/agent/infrastructure/in_memory_session_repository.py`
- DDD 设备执行实现：`poc/agent/infrastructure/device_executor.py`
- DDD 设备独占实现：`poc/agent/infrastructure/device_exclusivity.py`、`poc/agent/infrastructure/device_task_registry.py`
- DDD 文件系统证据存储：`poc/agent/infrastructure/file_system_evidence_store.py`
- DDD 每设备相机协调：`poc/agent/infrastructure/camera_coordinator.py`
- DeepSeek 任务图领域聚合：`poc/agent/domain/task_graph.py`
- DeepSeek 计划/重规划应用入口：`poc/agent/application/deepseek_task_graph.py`
- DeepSeek HTTP provider：`poc/agent/infrastructure/deepseek_intent_provider.py`
- 视觉模型领域合同：`poc/agent/domain/vision_model.py`
- 视觉模型环境配置：`poc/agent/infrastructure/environment_vision_model_config.py`
- DashScope 视觉模型 provider：`poc/agent/infrastructure/dashscope_vision_provider.py`
- Qwen 运行错误分类：`poc/agent/infrastructure/qwen_runtime_errors.py`
- 通用目标投影与安全清洗：`poc/agent/domain/generic_goal.py`
- 收件人绑定语义：`poc/agent/domain/message_intent.py`
- Qwen typed task context：`poc/agent/domain/qwen_task_context.py`
- 视觉证据值对象：`poc/agent/domain/visual_evidence.py`
- 本地图像测量：`poc/agent/infrastructure/observation_images.py`
- Windows OCR runtime：`poc/agent/infrastructure/windows_ocr_runtime.py`
- 一次性方向执行门：`poc/agent/infrastructure/orientation_safety.py`
- 可信观察领域快照：`poc/agent/domain/trusted_observation.py`
- 可信观察四帧 adapter：`poc/agent/infrastructure/trusted_observation_frames.py`
- 单步视觉 scene observer：`poc/agent/infrastructure/generic_scene_observer.py`
- 动作后观察领域合同：`poc/agent/domain/post_action_observation.py`
- 单动作 adapter 应用端口：`poc/agent/application/action_adapter.py`
- 单动作执行 adapter：`poc/agent/infrastructure/generic_action_adapter.py`
- 通用 Agent 编排应用入口：`poc/agent/application/universal_agent_orchestrator.py`
- 视觉模型会话用量账本：`poc/agent/application/vision_usage.py`
- DeepSeek 失败诊断持久化：`poc/agent/infrastructure/deepseek_failure_diagnostics.py`
- canonical 动作目录：`poc/agent/domain/canonical_action_protocol.py`
- Qwen 决策应用入口：`poc/agent/application/qwen_visual_decision.py`
- 通用控制器领域服务：`poc/agent/domain/universal_action_controller.py`
- 正式 API 客户端：`poc/local_agent_api_client.py`、`poc/agent_api_cli.py`
- 当前边界：`用户决策与协议边界.md`
- 历史索引：`历史文档索引.md`

## 9. 交接纪律

- 汇报必须分开写：内部机制修改、自动测试结果、真机实际结果、仍未完成目标。
- 测试通过不等于真机完成；历史报告重放不等于新 session 成功。
- 新失败先归类并构造通用最小复现，不增加 App 名称、固定步骤、坐标或截图特例。
- 一个时期每类判断只有一个正式权威；发现旧入口时直接退役，不再叠加兼容层。

## 10. 2026-08-22 当前换行修复

- session `76818b986b4f493c9ca2f3fd4e40cbeb` 已真实完成一次输入框聚焦，后续两次观察都得到稳定完整 QWERTY 七点，但模型整体 `keyboard.bounds=null`，第一段 `first` 在 0 新动作处被重复几何门拒绝；会话已取消，未输入字符。
- 用户已决定删除该重复否决。仅当本地多帧证据稳定全部七点并通过 QWERTY 结构校验时，观察器才据七点重建整体执行包络；单个 Enter、退格、字面键、模式/布局键的独立审计不变。无 App、截图或固定坐标特例。
- 最小正反 `5/5`、相关回归 `424/424`、完整 Python 回归 `967/967`（`35.818s`）通过；当前仅是内部机制和离线测试完成，换行真机仍未完成，多字段与普通发送尚未开始。
- 本批已本地提交且未推送，并启动项目 Uvicorn PID `13200`。官方类型化客户端确认 `service_version=0.2.0`、`openapi_verified=true`、controller/camera online、`busy=false`、0 活动任务/会话。下一步先取新观察，再从全新换行 session 一次一动作复验；不得启动、停止、重启或操作卖家 `main.exe`。

## 11. 2026-08-22 第一行成功后的当前状态

- session `d5c0c38e5e2145c29d1b2f73df710978` 已真实输入 `first`，回执 `matched/physical_actions=1`；新观察确认输入框值为 `first`，并有独立审计的右下角 `↵`，后置值 `first\n`。
- 该 session 没有继续：旧微步逻辑未推进 `input_first_line`，typed 动作语义也漏掉中文“换行键”，最终 blocked 后已取消；换行和 `second` 都未执行。
- `poc/agent/domain/task_semantic_ir.py` 已将“换行键/new line key”归一为 `press_enter`；`universal_agent_orchestrator.py` 只在下一确定性片段为 LF 且存在唯一正式 `press_enter` 后继时退出微步。单一长文本及无关后继保持原行为。
- 最小 `4/4`、相关回归 `182/182`、完整 Python `969/969`（`35.552s`）通过。代码与文档待本地提交并加载；之后先恢复空白页，再用一个全新 session 严格完成 `first`、换行、`second`。

## 12. 2026-08-22 当前页重置阻塞修复

- 两个相同重置请求均为 0 动作：DeepSeek 把唯一刷新节点写成 pending+active pointer，并错误增加无 payload `data_mutation` effect。手机仍保留 `first`。
- 当前页刷新归一化现会删除该伪 effect，并正确区分“不要发送/提交”的否定约束与真实正向外部效果；随后既有唯一前沿规则可安全激活刷新节点。无 App、截图或坐标特例。
- 相关 `212/212`、完整 Python `969/969`（`35.807s`）通过。下一步本地提交并加载后，先完成一次独立 audited reload，再从空白页建立全新换行 session。

## 13. 2026-08-22 本地键盘几何权威修复

- 严格换行 session `cd6f1173140b430f832710db114662c4` 只执行一次输入框聚焦；动作后真实画面显示输入框已聚焦且完整 QWERTY 已展开，但两轮输入结构审计分别把键盘压缩为 `y=870..1000`，以及返回 `y=1160..1900` 的纵向画布坐标。系统均以“可见键盘必须提供有效 bounds”拒绝，正式终态 `failed/physical_actions=1`，未输入 `first`、未点击换行、未输入 `second`。报告：`poc/output/web/generic_supervised_20260822_094312_cd6f1173/report.json`。
- 现场三帧本地 OCR 实际稳定识别顶行 `q/w/e/r` 与底行 `c/v/b/n/m`。旧复核器却在使用 OCR 前先要求 Qwen 的全部纵坐标位于 `0..1000`，使本地证据不可达；这是通用实现顺序缺陷，不是机械臂、网络或手机键盘故障。
- 新实现由多帧 OCR 独立拟合 QWERTY 水平键距和上下行中心，重建七点、整体执行包络与 Enter 行位置；Qwen 只保留“当前是 QWERTY”和“该键当前语义为 newline”的识别职责。模型使用更高 portrait Y grid 时，只允许用三行本地对应点拟合出的单一仿射变换原子归一化整份审计，不猜 `1920/2000` 等固定高度。
- 同一 fingerprint 中存在唯一、完整、typed 字段身份一致的粗输入框时，其本地粗框可替换专用审计的漂移框；0/2 个字段、字段 ID/标签冲突均不重绑，动作前独立 geometry audit 仍保留。Send/开始键、错误水平排列、缺少两帧稳定 OCR 和非 QWERTY 均不获得换行权限。
- 两份现场失败响应离线重放均恢复为 `input_field_1`、本地 anchors `q/p=708`、`a/l=780`、`z/m/backspace=852`，并在前缀为 `first` 时生成后置值 `first\n` 的唯一换行键。最小正反/变化 `5/5`、相关回归 `537/537`、完整 Python `996/996`（`36.153s`）通过；仅有既知测试子进程 `ResourceWarning`，对应进程已自然退出。
- 当前仅是通用修复与离线证据完成，尚未加载项目 API，也没有新的真机动作。下一步本地提交、不推送；只重载项目 API 后重新核对 OpenAPI、设备、controller/camera/busy、新观察与卖家 PID，再从空白页建立全新 session 严格完成 `first → ↵ → second`。卖家 `main.exe` 不得操作。

## 14. 2026-08-22 执行确认层 typed 字段续接修复

- 上批提交 `6e1aded` 加载后，全新 session `47cf47c3cd134e498b1ce3e49e260582` 在同一 `input_field_1` 内依次完成 `first` 与右下角 `↵`，两步均 `matched`，内容依次为 `first`、`first\n`，`physical_actions=2`。
- 第三步追加 `second` 在物理动作前停止：确认重观察把占位消失后的输入框判为 `application_text_input` 0 个匹配，终态 `needs_reobservation`，第三步 `request_physical_actions=0`，手机仍为 `first\n`。报告：`poc/output/web/generic_supervised_20260822_102932_47cf47c3/report.json`。
- 根因是执行确认层仍以旧标签和全量观察状态重绑，未消费规划层已有的 typed `input_field_id` 与授权 payload 前缀。新实现只在稳定同帧、唯一字段、非空 typed ID、当前值和本步后置值均为授权全文的精确前缀时续接，并仍须新鲜 crop 几何审计；错误前缀、重复或冲突字段继续拒绝。
- 最小正反/变化 `2/2`、相关回归 `367/367`、最终完整 Python 回归 `998/998`（`36.211s`）通过；既知 `ResourceWarning` PID `12856` 随后退出。当前仍只能声明内部修复与回归通过；必须加载后另建全新 session，才能重新验收 `first → ↵ → second`。
- 提交 `7a61c46` 已加载。独立刷新 session `6df6d89b225747b4a1872fe157dfde4a` 的唯一 reload 动作为 `matched/physical_actions=1`，动作后页面确为空白输入页；其 DeepSeek 动作后重规划因 `HTTP 402 Insufficient Balance` blocked，不影响刷新物理事实。
- 新严格 session `e424c727b3fa4f728d0ffdbc0d024662` 从唯一空白 `input_field_1` 建立。首次聚焦确认因本地帧差 `6.60 > 6.00` 在 0 动作处进入 `needs_reobservation`；官方 `next` 取得新观察 `obs_d7e5e4e8477044e8a472bbc6d019097b / c0f7389d6d4384ce13cf`，仍为空白唯一输入框，但随后 DeepSeek 再次因 402 blocked。正式 session 为 `physical_actions=0`，没有输入字符。
- 当前 controller/camera online、busy=false、0 活动任务/会话，卖家 `main.exe` PID 2144 未变。已达到同类外部模型余额故障两次停止门；恢复条件是 DeepSeek 账户余额可用，之后必须另建全新 session，不能恢复旧 authority。

## 15. 2026-08-22 动作后 typed 字段身份顺序修复

- 全新 session `6e8883c3b6474c34a530462e155d6e24` 依次完成聚焦、`first`、右下角 `↵` 和 `second` 四个真实动作；最终 observation `obs_6f70403062e24aa990ff13064cd65828 / a69e17af7e7e98a55cd6` 证明同一 `input_field_1` 的值逐字为 `first\nsecond`。
- 最后一步仍被判 mismatched：模型仅把 `screen_id` 从“通用动作具机验收页”改写为“通用动作真机验收页”。字段 ID、输入类型、多行属性、聚焦、QWERTY/direct-Latin、相机方向和授权后置值均一致，因此正式根因是自由文本页面名先于 typed 身份否决的通用顺序缺陷。
- Controller 现先计算 typed 字段身份；页面措辞漂移不能再覆盖该权威。不同字段 ID、不同真实包名和键盘模式变化仍拒绝；没有增加 App、页面、错别字、截图或坐标特例。
- 最小正反/变化 `4/4`、相关 `560/560`、完整 Python `999/999`（`36.944s`）通过，编译和差异检查通过。当前尚未加载修复；换行真机验收仍未完成，多字段和普通发送不得开始。

## 16. 2026-08-22 exact tap 唯一本地目标直推修复

- `4f180cb` 已加载。session `ed86b30deb3845d6aa22a0c672457eda` 的唯一系统 `back` 动作为 `matched/succeeded`，软键盘已收起且结果页保留；卖家 PID 2144 未变。
- 两个后续刷新 session `6764154de82746cd902c3353ad919d01`、`8a17997adfdc441c8941aacfcc867da1` 均有唯一、完整、本地独立几何审计的 `local_audited_reload_control_1`，但 Qwen 两次选择别的 tap choice，均在 `physical_actions=0` 处 blocked。达到同类合同两次门后已停止在线采样。
- 通用缺口是 `exact_tap_semantic` 直推只按 action 过滤，未消费已唯一成立的本地语义目标。新实现还要求唯一 canonical choice 的 `element_id` 等于 `target_local_candidate`；目录不含该目标继续拒绝，普通非 exact 任务仍由 Qwen 选择。
- 最小 `3/3`、相关 `562/562`、完整 Python `1001/1001`（`36.666s`）通过，编译和差异检查通过。当前尚未加载本批修复；换行、多字段和普通发送真机验收仍未完成。
- 提交 `c1d9d35` 已加载。exact reload session `2d3ef3e5cf73459182582821edba2f8e` 的唯一动作 matched 并恢复占位符；其动作后 DeepSeek 重规划失败不影响刷新物理事实。后续独立 `back` session `e31bb5da321740a9b5846ca722c498b1` 与 reload session `14e0177fb3274f6897aeeb50cc6f845e` 均 matched/succeeded，证明 exact tap 直推已在真服务生效。
- 两个全新严格换行 session `522ab89f80664d4283b4ff7067a3325b`、`102c8260eea343da9199d1e141a42c11` 均从空白页完成聚焦与 `first` 两个 matched 动作，并在 Enter 确认前连续得到相同 `geometry match 没有逐字复用 label+role`；均为 0 新动作，手机保留 `first`。前一 session 的唯一 `next` 还因未建立输入事务唯一本地目标终止；后一 session 已取消释放 authority。
- 已达到同类 geometry 模型合同连续两次停止门。当前 controller/camera online、busy=false、0 活动会话，卖家 PID 2144 未变。下一步只能离线审查两份 `confirmation_failure_step_3.json` 的通用 geometry 接口合同，不再在线采样；换行、多字段和普通发送真机验收仍未完成。

## 17. 2026-08-23 最新输入与发送状态

- IME 有用预编辑闭环已取得真机成功：session `2a71754482584fbdac5ef8216326f3af` 对唯一逐字候选 `freshsendproof` 执行一次点击，回执 `matched`，终态 `succeeded/physical_actions=1/active_subgoal=null`；正文已进入应用输入框，未发送。报告：`poc/output/web/generic_supervised_20260823_041248_2a717544/report.json`。
- 通用代码现禁止清除仍属于授权 payload 前缀的预编辑，并要求唯一候选几何。该批当时使用的“同帧粗值 + 唯一可编辑线索”正文恢复已于 2026-08-24 被第 21 节单一输入账本替代并从运行时代码物理删除；`465/465` 与 `1047/1047` 只保留为历史结果，不能覆盖当前权威。
- 普通发送尚未完成。旧值降级 session `9832be0906aa4c4f9defbbcb6937e769`、加载后结构化元素漏报 session `b297ee04d9224bca8b1895ae3254d9b9` 均为 0 动作 blocked；唯一一次后续 start 又因 DeepSeek effect contract HTTP 409 在 session 建立前失败。连续模型合同失败已触发停止门，不得继续换措辞或重复采样。
- 当前手机仍保留未发送的 `freshsendproof`；controller/camera online、busy=false、0 活动会话；卖家 `main.exe` PID `2144` 未被操作。当前工作区有本批代码、测试和三份文档修改，尚未提交、未推送。普通发送新 `succeeded` 证据与中文输入正式验收仍未完成。

## 18. 2026-08-23 普通发送闭环完成与当前边界

- 发送后只读验证不再继承已完成输入事务；只有活动输入节点才向视觉焦点投影 `input_text`。只读消息气泡/保存预览字面量不再编译为活动 `input.value_equals`。该修复解决 session `21f8d5f6bb2441a7a512f67789e7ca0d` 中“实际发送 matched、输入框已空，但旧输入值抢占最终气泡验证”的通用缺陷。
- 新增精确 IME 候选点击 lineage：同一 typed `input_field_id`、授权 payload、唯一逐字候选、前置 preedit/pinyin 链、独立几何和新鲜画面全部一致时，候选点击后的可见正文可维持输入框身份。现场 `loopok` 候选点击后原图可见正文已进入输入框，但对应 session `ca0ca7511e6841009b5f8409b1ffc518` 的正式终态仍为 mismatched/canceled，不能记作输入或中文验收成功。
- 两个后续 start 均为 0 动作合同失败；第二次仅把语义正确的 `effect_intents.expected_results` 输出成单字符串。达到两次停止门后离线增加确定性语法规范化：只把非空字符串原样包成单元素数组，不接受空白/null/数字/对象，不改变 effect 来源、授权或最终条件。
- 最终完整 Python 回归 `1058/1058`（`35.840s`）通过，无断言失败。
- 新普通发送 session `e5ef5dce84ac4270a194156e9e5d4d70` 已严格完成：从既有逐字正文 `loopok` 开始，不重新输入；只执行一次正式 `send_message`，回执 `receipt_c30cc9ebb2c84d819566af8f75bbfecb` matched；动作后新观察 `obs_ebda84e5c07b480ea52886109bdc29fe / 93eb4153bbe21be72b8d` 同时证明最新我方气泡为 `loopok`、输入框为空。最终 `succeeded/physical_actions=1/task_graph=completed/revision=4/active_subgoal=null`。报告：`poc/output/web/generic_supervised_20260823_131844_e5ef5dce/report.json`。
- 当前结论：**新的普通发送同会话成功证据已完成**。中文输入完整正式验收仍未完成；`loopok` 的可见上屏不能替代正式输入 session 成功。换行和多字段仍按当前台账中的既有正式结果判断，不因本次发送自动视为完成。
- 最终官方只读状态：项目 API PID `7404`，controller/camera online、busy=false、0 活动任务/会话；卖家 `main.exe` PID `2144` 始终未被操作。本批只允许本地提交，不推送。

## 19. 2026-08-23 多字段正式验收完成

- 多字段输入的通用事务链已补齐：IME 预编辑不推进字段；专用审计唯一成立时不再被遗漏前置字段的粗 scene 重复否决；本地 QWERTY 行几何支持方向明确的 `next`；IME 候选可绑定键盘顶部唯一水平候选栏。所有变化均为 typed 字段、键盘语义和几何合同，没有 App、页面、截图或坐标特例。
- 最终相关回归 `656/656`、完整 Python 回归 `1077/1077` 通过。当前项目 API PID `26808` 已加载修复；官方客户端确认 OpenAPI、controller、camera 正常，busy=false、0 活动任务/会话。卖家 `main.exe` PID `2144` 未被操作。
- 正式 session `e239a141d95242afbe2e3cbcdaf7182f` 从两个空字段开始，在同一任务图中完成主题 `first`、typed `subject → body` 的“下一步”切换、正文输入与最终核对。首次正文输入只产生错误预编辑 `econd`，该动作真实为 `mismatched`；Agent 没有推进字段，而是单独清除后重新输入 `second`。最终 `succeeded/physical_actions=7/task_graph=completed/revision=7/active_subgoal=null`，页面显示主题 `first`、正文 `second` 和验收通过，未发送、未提交。报告：`poc/output/web/generic_supervised_20260823_201639_e239a141/report.json`。
- 当前已完成：新的普通发送、多字段依次定位/输入/错误恢复/验证。当前剩余正式真机缺口只有严格换行成功终态和中文输入完整正式验收。后续仍须使用官方类型化客户端、一次一动作和新观察；不得恢复卖家文字对话框。

## 20. 2026-08-23 严格换行正式验收完成

- 四个通用修复提交为 `08cc560`、`075617f`、`e8fd1ba`、`ece259b`：确认阶段输入辅助键漏报恢复、空标签 newline 图标键规范化、IME 预编辑尾段下已提交前缀连续性，以及显式 screen/typed 字段在未知 App 页面上的 lineage 连续性。所有修复均依赖 typed `input_field_id`、授权 payload、字段/设备/页面身份与本地几何，没有增加 App、页面、截图、固定坐标或固定正文特例。
- 最终相关回归 `427/427`、完整 Python 回归 `1079/1079` 通过；测试产生的既知子进程警告对应 PID 均已确认退出。
- 全新 session `9989006ab2ca4cd497e6efac78990970` 从空白多行字段开始，依次真实执行聚焦、输入 `first`、确认候选、点击换行键、输入 `second`、确认候选。六个动作全部 matched；换行后同一 `input_field_1` 精确为 `first\n`，最终同一字段精确为 `first\nsecond`，无残留 IME 预编辑。
- 终态为 `succeeded/physical_actions=6/task_graph=completed/revision=4/active_subgoal=null`，无 verification errors。报告：`poc/output/web/generic_supervised_20260823_222522_9989006a/report.json`；完整会话：`poc/output/web/generic_supervised_20260823_222522_9989006a/session.json`。
- 当前权威结论：严格换行、多字段、新普通发送三项正式真机闭环均已完成；剩余正式真机缺口只有中文输入完整正式验收。最终官方只读状态为 controller/camera online、busy=false、0 活动任务/会话，项目 API PID `24812`；卖家 `main.exe` PID `2144` 未被操作。本批只本地提交，不推送。

## 21. 2026-08-24 输入状态账本单一权威

- 当前唯一正式输入真相是以 `input_field_id` 绑定的 typed 输入状态账本。compact 场景的自由摘要、输入值转写和粗框只可承担页面身份/审计选区职责，不能发布、恢复、清空或否决 committed value；专用输入审计是账本的视觉事实来源，不再与 compact 维护第二份正文。
- 正式中文 session `8eae52fcf060407f91a2df51975d719e` 已真实点击候选“你好”，但旧双权威把应用正文误投影为 `value=""`、`ime_preedit_text="你好"`，终态 `blocked/mismatched`。该失败已固化为“compact 报空或错误 + 动作后同字段精确片段 + 残留预测栏”的通用回归，不能记作中文输入完成。
- `generic_scene_observer.py` 现先按设备、App/screen、typed 字段与时效选择 pending lineage，再由专用审计校验同字段几何和动作后精确事实；候选点击回执、prior/expected、授权 payload 与唯一预测片段一致时只更新同一账本。专用审计失败时所有 compact 输入元素也会被移除，旧早退路径不能复活输入值。
- 已物理删除 compact 输入值提取、自由摘要/可编辑线索回填、换行投影及旧 `coarse_input_value` 接口；防恢复测试直接扫描生产源。没有增加 App、页面、截图、固定正文、固定坐标或固定步骤特例。
- 离线结果：最小/变化 `38/38`、观察器 `273/273`、输入/几何/Qwen/canonical/编排相关回归 `660/660`、完整 Python 回归 `1083/1083`（`43.873s`），静态编译与 `git diff --check` 通过。完整回归只有既知子进程 `ResourceWarning`，对应进程随后退出。
- 本批未加载或重启项目 API，未执行真机动作，也未操作卖家 `main.exe`。严格换行、多字段和新的普通发送三项既有正式成功结论不变；当前唯一真机缺口仍是中文输入完整正式验收。后续若继续真机，必须先用官方类型化客户端核对当前 OpenAPI、设备、controller/camera/busy、活动会话和新观察，再从全新 session 一次一动作执行；不得恢复卖家文字对话框。

## 22. 2026-08-24 Qwen Plus 调用成本收敛（历史过渡实现，会话硬门已于 2026-08-25 废除）

- 用户明确决定继续使用 `qwen3.7-plus`，不降级到 Flash，不设置备用模型或失败回退。该批曾建立 `16` 次请求/`50000` Token 的异常止损；该硬门已由用户于 2026-08-25 废除，只保留历史证据。六动作历史调用形状曾降到 `10` 次，随后由第 25 节收敛为每步一次。
- 历史五个中文 session 的 `6` 个物理动作产生 `65` 次成功 Qwen 请求、`174721` Token。根因是同一输入动作内重复进行 compact/input、确认整页、planned/fresh geometry、方向和动作后观察；不是 Qwen 模型档次、机械臂或中文本身。
- 新实现对同一设备、fingerprint、清洗后目标上下文和 typed lineage 的成功观察做有界精确复用；pending typed lineage 的动作后续接只继承已验证的 App/screen/system 身份，旧元素、正文、候选和坐标清空后由一份新 input audit 重建。不同设备/字段/上下文、无 pending lineage 或审计不能唯一重建时不复用。
- strict input 目标在 fresh 四帧通过稳定性、尺寸和全画面像素身份门后，不再重复整页观察与 planned/fresh 两次 Qwen 几何审计；普通控件保持旧严格路径。完整 QWERTY 三行经本地连续多帧 OCR 独立验证后，可签发绑定设备、scene、帧和单动作的一次性方向凭据；错误行序、缺行或非 QWERTY 回到正式方向审计。
- `qwen_usage.json`、`session.json` 和 `report.json` 保存逐请求 stage、fingerprint、provider request ID、网络尝试、prompt/completion/total Token、缓存命中和版本化费用估算。共享 provider 的请求与元数据结算已串行化，避免多设备把 request ID/Token 记入错误会话。当前统计只读，不再产生会话预算故障，也不触发模型降级。
- 离线调用形状为 `2（初始观察）+1（首次方向）+2（聚焦后观察）+5（typed 续接）=10` 次、构造 `28000` Token。相关回归 `432/432`、orchestrator/Web/API `209/209`、补充方向/promotion/账本 `51/51`；唯一一次完整 Python 回归 `1097/1097`（`43.363s`），编译与差异检查通过。既知测试子进程警告 PID `34056` 随后退出。
- 本批没有调用在线 Qwen、相机、Controller 或机械臂，没有重载项目 API，也没有操作卖家 `main.exe`；只读检查卖家 PID 仍为 `2144`。严格换行、多字段和普通发送既有成功证据不变；唯一正式真机缺口仍是中文输入 `你好\n世界` 的全新 session 成功终态。账户恢复后必须先用官方类型化客户端核对 OpenAPI、设备和新观察，再一次一动作验收并以新账本证明真实请求数/Token；当前离线 `10` 次结论不能冒充真机完成。

## 23. 2026-08-24 中文拼音续接成本修复与当前运行故障

- 账户恢复后的正式 session `e94d4ed0655148ba9302550b698351e1` 已完成聚焦和键入 `nihao` 两个 matched 动作；第二步后同一 typed 字段为应用值空、预编辑 `nihao`、唯一候选“你好”。当时已用 `10` 次 `qwen3.7-plus` 请求、`35420` Token，故在第三个物理动作前止损并取消，不能记作中文验收成功。
- 根因是中文拼音键入后未签发 pending typed lineage，以及无命名 App 的 `current_surface` 输入仍重复识别前台 App。提交 `5bd083a` 已以通用 typed 字段、确定性拼音、授权 payload 前缀、QWERTY 几何和 expected preedit/candidate 合同修复；命名 App 场景仍保留身份审计，没有增加 App、页面、截图、正文或坐标特例。
- 最终相关模块为 `35/35`、`276/276`、`128/128`，完整 Python 回归 `1100/1100`（`45.340s`），编译与差异检查通过；既知测试子进程 PID `27204` 已自然退出。提交已本地创建，未推送。
- 项目 Uvicorn 已加载提交，父 PID `10292`、服务 PID `20648`；官方 CLI 确认 `service_version=0.2.0`、`openapi_verified=true`、busy=false、0 活动任务/会话。连续两次官方状态均为 `controller_online=false、camera_online=false`，因此加载后 0 真机动作、0 新 session、0 新观察。
- 卖家 `main.exe` 当前为外部新进程 PID `3512`（启动于 `2026-08-24 12:47:58`），本任务没有启动、停止、重启或操作它。当前唯一阻塞是设备/相机离线运行故障。恢复后先用官方 CLI 核对 OpenAPI、设备、busy、活动会话和新 observation/fingerprint，再从全新 session 严格完成 `你好\n世界`，不能恢复已取消 session。

## 24. 2026-08-24 中文换行失败归约、导航证据修复与最新离线状态

- 正式中文 session `668ebb1421594d879d7a2bc8588fd346` 真实执行了聚焦、输入 `nihao`、点击唯一候选“你好”和 `press_enter` 四个物理动作。动作后帧与页面状态证明手机已显示 `你好\n`，但旧输入 lineage 因 `app_id=unknown` 把同一 typed 字段投影为 `actual=''`，随后会话达到 `11` 次 `qwen3.7-plus` 请求、`51587` Token 的硬门，正式终态为 `failed/physical_actions=4`；它不能记作中文验收成功。报告：`poc/output/web/generic_supervised_20260824_130158_668ebb14/report.json`。
- 提交 `cb8a35d` 统一未知 App 下的 typed 输入连续性：只有同一非空 `input_field_id`、授权 payload 精确前缀和稳定输入事务全部一致时，unknown App 才不再否决 pending/persistent text、newline 和 literal lineage；未知/错误字段仍拒绝。输入 lineage `39/39`、相关 `587/587`、完整 Python `1104/1104`（`53.303s`）通过。
- 页面恢复阶段没有产生可冒充成功的证据：reload session `c3addc4b82d2468c9d4d720139971360` 为 0 动作 blocked；Back session `b82698f2e70f4edc91d57a825770b98a` 的唯一 Back 物理回执 matched、键盘已收起，但 DeepSeek 动作后重规划失败使正式终态 blocked；link session `e63eed69bd8c46bca84b7ccd508b2a31` 仅因当前页出现“返回验收模式选择”链接而被错误写成 `succeeded/physical_actions=0`，当时画面仍是“通用动作真机验收页”。
- 提交 `190e636` 修复上述通用零动作误完成：链接、按钮或列表项只证明目标页面可进入，不能证明它已经是当前页面；只有 completion condition 自身被结构化 `screen_id/page_title/container` 身份或精确前台目标 App 证明时，才允许导航节点幂等零动作完成。变化样本保留“已经真实位于目标页”时的零动作完成；定向 `9/9`、相关 `219/219`、完整 Python `1105/1105`（`56.707s`）通过。
- 项目 API 已按用户授权只终止旧父/服务 PID `13708/23212` 并隐藏重启为父 PID `984`、服务 PID `288`。官方 CLI 确认 `service_version=0.2.0`、`openapi_verified=true`、control token 已加载、`busy=false`、0 活动任务/会话；连续两次状态仍为 `controller_online=false、camera_online=false`，所以重载后执行 0 个真机动作、0 个新 session、0 个新观察。
- 卖家 `main.exe` PID `3512` 始终存活且 Responding，本任务未启动、停止、重启或操作它。当前唯一正式真机缺口仍是全新同会话逐步得到 `你好\n世界`、全部动作 matched、最终 `succeeded/active_subgoal=null`；设备恢复前必须停在运行故障，不得用旧帧或上述失败会话代替新观察。

## 25. 2026-08-24 每个闭环步骤最多一次 Qwen 调用

- 用户明确要求保留 `qwen3.7-plus`，但每个闭环步骤最多调用一次。正式 Web 与评测入口现只由 `SingleStepGenericSceneObserver` 发出这一份 `single_step_observation` 请求；它同时给出页面事实和必要输入结构。随后 `QwenVisualDecisionObserver` 只是本地 canonical 目录选择器，不发起模型请求。候选映射、方向、帧身份、OCR、scope、机械可达与 receipt 均由本地确定性组件处理；不再分别联网做 compact、App 身份、geometry、orientation、input audit 或 visual action selection。
- 当时正式 session 账本版本为 `2026-08-24-single-step-qwen-usage-v2`，只接受 `single_step_observation`。旧 stage 在请求计数和联网前以 `vision_step_contract_violation` 拒绝；单次输出格式错误或候选不唯一也本地停止，不允许同一步远端重试。每步一次合同继续有效，但旧 `16` 次/`50000` Token 会话硬门现已废除；没有模型降级或备用模型。
- 本节记录的“唯一直接后继复用动作后观察”已被 2026-08-27 用户决定废止并从正式运行代码物理删除。当前单步调用合同只表示同一份当前子目标观察最多一次联网；动作后可验证刚执行动作，活动子目标变化时必须另建新观察步骤，不能用旧 `N+1` 口径恢复后继预取。
- 相关回归 `586/586`、单步观察/选择/方向/账本 `309/309`、Web/编排/用量 `222/222`；本批唯一完整 Python 回归 `1119/1119`（`44.617s`）通过，`git diff --check` 无空白错误。完整回归的既知子进程警告 PID `10108` 随后自然退出。
- 本批只做离线代码、测试和文档，没有调用在线 Qwen、相机、Controller 或机械臂，没有启动、停止或重载项目 API，也没有操作卖家 `main.exe`；只读检查确认 `main.exe` PID `3512` 仍 Responding。当前可声明 **每步一次机制已实现、相关测试与完整回归通过**，但服务尚未加载本批修改，新的真实 session 调用次数和中文完整成功仍未验收。后续如获准加载，必须先按官方类型化客户端核对 OpenAPI、设备、controller/camera/busy、活动会话和新 observation/fingerprint，再从全新 session 验收，不能恢复历史失败 session。

## 26. 2026-08-24 普通导航动作的一次新观察纠正

- 现场 session `6c4c1012f44c4f038561d4e83cf1604f` 的首个 `tap_semantic` 已真实执行，但动作后仍为 `launcher/home_screen`，回执 `mismatched`。旧自动循环停在 `needs_reobservation`、`proposal=null`、`automatic_loop_enabled=false`；网页因此长期显示“进行中”。Qwen 两次请求均成功、共 `8523` Token，controller/camera 在线且 busy=false，故根因不是网络、模型预算或设备离线，而是旧循环没有消费重观察状态。
- 当前唯一正式纠正合同为 `2026-08-24-fresh-observation-corrective-retry-v1`：普通 `read_only/navigation_only` 动作首次无预期语义变化后，强制失效旧 decision/candidate/几何/scope，取得一份新 observation，以新的单步 Qwen 结果和新一次性 scope执行最多一个纠正动作。新观察证明任务已推进时直接继续新子目标；第二次仍 mismatched 时终结为 `failed`，不能第三次点击或继续显示进行中。
- 自动纠正只覆盖普通导航物理动作；`input_verified_text`、`clear_verified_text`、`press_enter`、普通外部效果、登录和付款不会自动重复。该实现不依赖微信、App 名称、页面、截图、固定步骤或固定坐标，并保持“每个闭环步骤最多一次 Qwen”：纠正重观察是一个新的闭环步骤，不在原步骤追加请求。
- 新增正向、变化和反例：首个 mismatch 后新 observation + 新 scope 的纠正动作 matched 并完成；纠正动作再次 mismatch 后明确 failed 且只执行两次；输入与 external effect 不进入纠正。最小 `3/3`、编排/canonical/mock 相关 `185/185`、合并完整 Python 回归 `1127/1127`（`58.528s`）通过，`git diff --check` 和静态编译通过。完整回归的既知子进程警告 PID `5264` 随后自然退出。
- 本批尚未重载项目 API，也没有为验证本修复而执行新真机动作；因此当前只能声明 **一次新观察纠正已实现且完整回归通过**。现场旧 session 仍是旧运行代码产生的停滞证据，不能改写成新合同的真机成功；后续必须先加载本批，再用全新普通导航 session 验证一次真实纠正或正常首击成功。

## 26. 2026-08-24 系统鼠标角落抢占修复

- 已确认鼠标反复跳到左上角不是 Qwen、机械标定或手机动作坐标问题。通用截图和动作后处理此前会为清除卖家相机预览上的 PX/MM 悬浮层，把 Windows 系统鼠标移到卖家窗口外的桌面角落并永久留在那里；当前窗口布局使首选点落在左上角。
- 旧永久停放入口已从生产代码删除。新实现使用有作用域的临时鼠标租约：只有鼠标原本位于手机相机预览区域时才临时移开，截图/清除悬浮层结束或发生异常后恢复原位置；若用户在租约期间主动移动鼠标，则保留用户的新位置。鼠标原本在预览区域外时不移动、不等待。
- 通用截图、点击、Back/Home、滑动、长按、拖动、文字输入、清空和 XY 校准路径均已切换；没有增加 App、页面、截图、固定坐标或正文特例。真实机械点击仍可能短暂借用系统鼠标，这是卖家控制软件的执行方式，但动作结束后不再把鼠标留在屏幕角落。
- 最小回归 `82/82`、相关回归 `137/137`、完整 Python 回归 `1124/1124`（`57.029s`）通过，静态编译和 `git diff --check` 通过。变化样本覆盖鼠标已在预览外、用户中途移动和异常退出；完整回归警告对应测试子进程 PID `28972` 已自然退出。
- 本批自动测试没有调用在线 Qwen、相机、Controller 或机械臂；卖家 `main.exe` PID `3512` 未被操作且仍 Responding。已核验旧项目 API 为父/服务 PID `28168/30316`，但仅终止并隐藏重启这两个 PID 的提权请求被审批系统异常拒绝，命令没有执行；官方 CLI 随后确认旧服务仍在线、OpenAPI 已核验、controller/camera online、busy=false，并保留 session `6c4c1012f44c4f038561d4e83cf1604f`（`needs_reobservation`）。因此代码已修复但当前运行服务尚未加载，下一步只能在明确获准重载后操作这两个已核验 PID，不得操作 `main.exe` 或续跑旧会话。

## 27. 2026-08-24 新观察纠正加载与通用观察合同收敛

- 普通 `read_only/navigation_only` 首次 `mismatched` 后的一次新观察纠正已在提交 `ec2f13e` 实现：旧 decision/candidate/几何/scope 全部失效，取得新 observation 后才允许一个新 canonical 动作；第二次仍 mismatched 即 `failed`。输入、外部效果、登录和付款均不自动重复，不能盲点旧坐标。
- 加载后 session `b735906573aa47cbbe21ae83c181f771` 的首击真实 matched 并打开微信，但旧 App 前台校验只比较运行包名 `com.tencent.mm` 与语义 ID `wechat`，错误阻止任务推进。现已统一使用顶层 `foreground_app_id`、`app_id`、`screen_id` 绑定语义 App；不读取摘要或桌面子元素，不增加包名映射。
- session `7add250dce84430084ac3e7906d7698a` 证明单步 Qwen 完整返回输入事实结构、但唯一遗漏内层固定协议版本。当前只在事实字段集合恰好完整且仅缺该固定常量时确定性补齐；额外字段、其他缺字段和错误显式版本继续拒绝。
- session `c3ee0d4c` 证明旧精确标签归约器会把原本 `goal_relevant=false` 的越界同名外围元素晋升为目标。当前逐字标签只能在元素 bounds 先满足正式几何合同时改变目标归属；模型原本标为目标的无效几何仍严格失败，明确非目标的无效外围元素可删除。
- 最小与变化样本覆盖上述三类修复；场景观察/通用编排相关回归 `432/432`，本批最终完整 Python 回归 `1131/1131`（`44.377s`）通过，静态编译和 `git diff --check` 通过。完整回归警告 PID `29016` 随后自然退出。
- 最新服务已加载本批代码，官方客户端确认 OpenAPI、token、controller、camera 正常且 busy=false；当前项目 API 父/服务 PID 为 `13004/30620`，卖家 `main.exe` PID `3512` 全程未动且仍 Responding。
- 最后全新 session `026c17edd2034033875fdf0ba5c0d917` 为 0 物理动作：前三类修复均已生效，任务图正确识别微信前台，但又把“聊天列表中可见文件传输助手”错误当成“文件传输助手聊天页面已经进入”，跳过必要导航后在 `input_message` 阶段找不到输入框而 failed。该新缺口属于任务图完成证据的表面/页面身份混淆，不是纠正重试或几何修复失败；本批按止损规则未继续扩修。
- 因加载后的会话没有产生新的首个 `mismatched`，当前结论只能写：**一次新观察纠正已实现、完整回归通过并已加载；App 身份、单步内层协议和无效几何归约已有运行证据；新观察纠正本身尚缺一次新会话真实触发证据。** 下一批唯一缺口应是通用区分“入口可见”和“目标页面已进入”，修复后再用一个全新会话继续，不能恢复上述失败 session。

## 28. 2026-08-24 入口可见与具名目标页面完成证据分离

- session `026c17edd2034033875fdf0ba5c0d917` 的 0 动作失败已离线重放。画面为 `wechat_main_chat_list`，只存在 `role=list_item/label=文件传输助手` 的入口；旧 `_identity_anchor_is_grounded` 以“聊天”语义类别与 screen ID 中的 `chat` 相交，忽略“文件传输助手”限定身份，随后 App 身份又以短词/n-gram 相交，使 DeepSeek revision 3 错误完成 `find_recipient`。
- 当前页面完成要求同时绑定容器类别和具名限定身份。通用 `chat/conversation` 只能证明聊天类表面；具名联系人、订单、文件或设置子页面必须由结构化 `screen_id`、页面标题或页面级 container/text 证明。入口类 list item/button/icon/search result 永远不能替代目标页身份。
- App 表面判断拆成两项：完整 App 名称/ID出现可继续约束当前 App 与 verified lineage；只有移除完整 App 身份后仅剩打开/主界面/前台/可见等通用词时，前台事实才足以完成 App 节点。仍有具名子页面限定词时必须继续导航。
- 修复不包含微信、联系人、页面、坐标或固定步骤分支。正向/变化/反例覆盖普通聊天类别、具名会话、页面标题、App主界面、App名本身为通用类别、设置子页、浏览器 lineage 和 Launcher 入口；任务图/语义 IR/Qwen/编排相关回归 `265/265`、完整 Python 回归 `1134/1134`（`55.848s`）通过。完整回归警告 PID `25504` 随后自然退出；重新加载后的全新真机会话尚待本批末完成。

## 29. 2026-08-24 canonical App 表面身份收敛与最新离线状态

- 加载第 28 节修复后的全新 session `1847fd05a76d42d1a32bf02bb8cff099` 为 `blocked/physical_actions=0`。第 28 节已真实生效：revision 3 保留 `find_recipient` 为 active，没有再把聊天列表错误完成成具名会话页；画面唯一强目标为 `e1/list_item/文件传输助手`。
- 新阻塞来自 App 表面身份规则重复：任务图推进以结构化 `screen_id=wechat_main_chat_list` 正确认可当前微信前台；canonical 目录却只比较产品 ID `wechat`、运行包 `com.tencent.mm` 或精确标题，因标题为 `微信(137)` 而移除 `tap_semantic(e1)`，只留下 `home/back/wait_for_change`，单步本地选择因此停止。
- 当前 `canonical_action_protocol.scene_matches_target_app_surface()` 已成为统一 App 前台身份合同：顶层 `screen_id` 的有界身份词可以桥接不透明运行包与产品 ID；编排层零动作推进直接复用同一函数。摘要、正文、子元素和 Launcher 图标仍不参与。已有 receipt lineage 继续升级为精确运行包，不会被较弱结构化别名短路。
- 同一失败现场离线重放现在得到 `tap_semantic(e1)`、`back`、`wait_for_change`，唯一强目标确定性选择 `tap_semantic(e1)`。变化/反例覆盖不透明包名、无关 screen ID、Launcher 和 receipt lineage。相关回归 `197/197`、完整 Python 回归 `1135/1135`（`83.361s`）通过；完整回归仅有既知测试子进程 `ResourceWarning` PID `6220`，需确认其自然退出。
- 本节写入时尚未重载项目 API，也尚未执行修复后的全新真机 session。下一步只允许：确认测试子进程退出；静态编译与差异检查；核对当前项目 API/OpenAPI/设备/busy；仅重载项目 API而不操作卖家 `main.exe`；最后使用一个全新 session 继续同一通用发送闭环。不得恢复历史失败 session，也不得扩大重构。

## 30. 2026-08-24 具名会话导航成功与唯一空输入框融合修复

- 第 29 节修复加载后，全新 session `724e8a08bf644f6b9996940a8eab767e` 正确生成并执行 `tap_semantic(e1/文件传输助手)`。该动作回执 matched，动作后真实画面和单步响应均证明已进入“文件传输助手”会话页；因此 canonical App 表面身份修复已取得运行证据。
- 会话随后在同一动作后观察中 failed，终态 `physical_actions=1`，没有输入或发送。模型场景分支报告唯一输入 `e2/value=""/bounds=[120,910,780,960]`，输入结构分支同时报告唯一空应用输入且几何一致；旧审计因字段没有占位符、光标或标签而删除它，错误报“没有建立当前输入事务的唯一本地目标”。报告：`poc/output/web/generic_supervised_20260824_183022_724e8a08/report.json`。
- 当前通用修复只接受同一单步响应中“唯一、同值为空、双向高重合、完整可见、高置信且有视觉证据”的场景输入与输入结构交叉证明。输入结构仍独占最终值和几何，场景候选在投影前被删除；多字段、值冲突、几何不重合、无活动输入事务或无证据继续停止。
- 现场失败响应和四张动作后图片已离线重放，结果为唯一 `local_audited_input_1/value=""/goal_relevant=true`。正向、变化与反例最小测试通过；观察/动作选择/编排/协议相关 `481/481`、完整 Python 回归 `1137/1137`（`52.201s`）通过。完整回归警告的测试子进程 PID `31556` 已自然退出。
- 本节写入时修复尚未加载到项目 API，尚无新的输入或发送物理动作。下一步只做静态检查、精确核对并重载当前项目 API 进程（不得操作卖家 `main.exe`），再从全新 session 继续；不得恢复 session `724e...`，不得把离线成功写成真机输入/发送完成。

## 31. 2026-08-24 空输入视觉旧限制删除与最新状态

- 提交 `0065c55` 已加载，官方客户端确认 OpenAPI/token/controller/camera 正常、busy=false、0 活动会话；加载时只重启项目 Uvicorn，卖家 `main.exe` PID `3512` 未被操作。
- 加载后新 start `generic_supervised_20260824_184326_fde89cb1` 为 `failed/physical_actions=0/session=null`。任务图已在观察前正确推进到 `input_exact_text`，因此不是首节点焦点或具名页面幂等问题。失败画面仍为“文件传输助手”会话页，底部有完整浅色输入表面；Qwen 因旧合同要求空字段额外出现占位符、光标或高亮，返回 `application_inputs=[]`，观察随后按旧门失败。
- 当前已从正式提示删除该重复限制：完整边界或与周围 App chrome 可分辨的完整输入填充/轮廓即可作为空字段证据；占位符、光标和聚焦高亮为可选证据。任务文字、无解释空隙和相邻图标仍不能单独推断字段。本地唯一性、完整可见、坐标和置信门不变。
- 提示正反合同与输入观察回归 `284/284`、完整 Python 回归 `1137/1137`（`54.940s`）通过；完整回归警告的测试子进程 PID `22272` 已自然退出。本节写入时新提示尚未重载，尚无输入或发送物理动作。
- 下一步只能提交并重载本次小批次，官方核对设备与空闲状态后新建一个 session。若 Qwen 仍漏报同一空输入表面，按同类模型合同第二次失败门停止在线采样，转为一次性重新设计单步视觉输入合同；不得换措辞、恢复失败 session 或盲点。

## 32. 2026-08-24 空输入真机识别成功与键盘回执缺口修复

- 提交 `95a373f` 加载后，全新 session `edf738e5f6834dbf9be67fc278fcaa6d` 在同一真实会话页成功建立唯一 `local_audited_input_1/value=""/bounds=[0.10,0.91,0.60,0.96]`，证明第 31 节删除旧限制后，完整空输入表面已经在真服务生效。
- 第一个聚焦物理动作 matched。第二个模式切换物理动作后，新观察证明同一字段的 `keyboard_input_mode` 已由 `chinese_pinyin` 变为 `direct_latin`、值仍为空，且系统生成了下一唯一 `input_verified_text`；说明落点与物理切换成功。
- 正式回执却把第二步写成 mismatched，错误为“尚未实现的 typed transition expectation：element.state.keyboard_input_mode/equals”。根因是 canonical 已允许布局、输入模式、大小写三类状态谓词，Controller 的正式回执分支只列入允许集合却未实现消费。会话已主动取消释放 authority，终态 `cancelled/physical_actions=2`，没有输入或发送，不能记作完整闭环。
- 当前 Controller 已一次性实现三类键盘状态谓词，并从辅助键解析结果保留 `input_element_id`。动作后必须同字段 ID、同授权值、目标状态唯一一致。现场 step 2 前后观察离线重放现为 matched；相关 `423/423`、完整 Python `1138/1138`（`45.678s`）通过，警告 PID `15916` 已自然退出。
- 本节写入时回执修复尚未提交和重载。下一步只提交本批、静态核对、精确重载项目 API，再用一个全新 session 从当前已是 direct Latin 的字段状态继续完整输入与发送；不得恢复或改写 `edf738...`，也不得再次执行已经完成的模式切换。

## 33. 2026-08-24 拉丁前缀真机输入成功与已退役布局多跳方案

- 提交 `b814a7c` 加载后，全新 session `893115ea26af411ebec124b56055222e` 从已聚焦、空值、direct Latin 的同一字段生成 `input_verified_text`。唯一物理动作完成 `aaazjie` 字母键序；当时 Qwen 场景误把组合串当作应用值。后续第 34 节新观察证明它实际仍是输入法预编辑，因此本动作只能证明键序到达 IME，不能证明 App 正文已经提交。
- 会话仍在动作后观察阶段 failed，终态 `physical_actions=1`，没有中文、没有发送。下一字符是全角 `？`，当前画面只报告方向明确的 `qwerty -> numeric/123` 键；旧规划器只接受一步直达 symbol，错误丢失了完整输入观察并报“没有建立当前输入事务的唯一本地目标”。
- 该批曾采用 `qwerty <-> numeric <-> symbol` 并允许 `123` 作为符号中转。用户已于 2026-08-25 明确废除该方案；当前唯一正式路由见第 40 节，任何运行时、默认值或测试不得恢复本段旧路径。
- 当时现场失败响应和四帧离线重放曾生成 `qwerty -> numeric`；这只保留为历史失败证据，不能再作为当前合法动作或验收期望。
- 本节写入时多跳修复尚未提交和加载。下一步只提交、静态核对、重载项目 API，然后全新 session 从手机当前显示 `aaazjie` 的状态重新观察，不能再次输入该前缀或恢复失败 session；只有 dedicated input audit 逐字证明 committed App 值时才能当作前缀，否则必须按预编辑/候选状态继续或 0 动作停止。

## 34. 2026-08-24 IME 候选提交成功与可选字符键隔离

- 提交 `0692881` 加载后，全新 session `b30e9fbfbfba47dc87652af281a02515` 发现手机上的 `aaazjie` 实际仍是中文拼音预编辑，并非已提交应用值；系统没有重复输入，而是选择唯一同文 IME 候选 `aaazjie`。
- 候选点击后，动作后场景与 `application_inputs.text` 均逐字报告应用值 `aaazjie`、光标、direct Latin QWERTY，证明候选提交物理结果成功。正式会话仍因输入结构额外申报 `value=？/label=?` 的错误字符键而 failed，终态 `physical_actions=1`，未继续布局、中文或发送。
- 当前对结构完整但 label/value 语义不一致的可选字符键只做候选撤销，不再否决独立可信字段与布局事实；对象结构、类型和坐标错误继续严格拒绝。ASCII `?` 不会被当作全角 `？` 执行。
- 现场动作后响应与四帧离线重放当时保留了 `field_value=aaazjie`，但旧批次生成的 `qwerty -> numeric` 符号中转已退役；当前必须按第 40 节重新观察并选择英文模式与可见 symbol 键。
- 本节写入时该隔离修复尚未提交和加载。下一步只提交、重载项目 API并新建 session；必须从新观察的已提交 `aaazjie` 开始，不能重复候选点击或前缀输入。新会话预期首动作应为布局下一跳。

## 35. 2026-08-24 普通导航纠正与 App 启动终态修复

- 用户批准的通用纠正合同仍为 `2026-08-24-fresh-observation-corrective-retry-v1`：普通 `read_only/navigation_only` 动作第一次 `mismatched` 时废弃旧 candidate、bounds、scope 和观察，只允许依据新观察执行一次纠正；第二次仍不匹配即失败。输入、外部效果、登录和付款不进入自动纠正。正式运行链已加载该合同；本批最终成功样本第一次即 `matched`，所以 `corrective_retry_history=[]`，不能宣称真机触发过纠正分支。
- 历史 session `a58da606a989499eb6c66733b1719ea6` 已真实一次点击打开微信，动作后前台为 `com.tencent.mm`，但旧命名 App 身份门仍额外要求当前单步视觉架构已经退役的第二份 `controller_completion_evidence`，终态误判 `blocked`。该重复否决已删除；仍保留 session/task/revision、三个动作摘要、前后 observation/fingerprint、一次物理动作、`matched` 回执、Launcher 离开和正式 surface expectation 的完整绑定。
- session `013c6d13feef4122946d364101b9dbae` 再次真实打开微信，但动作后 Qwen 把底部控件输出为 `y2=1.03/1.04`，导致整份新观察被拒绝。单步观察现复用既有通用规则：非输入目标遇到越界的 goal-marked 元素批次时整批撤权，不裁剪、不产生错误落点，只保留独立的前台 App/screen 顶层事实；输入目标仍严格停止。
- 相关回归 `484/484`、最终完整 Python 回归 `1148/1148`（`45.465s`）通过；静态编译和 `git diff --check` 通过，既知测试子进程警告 PID `6060` 随后已退出。
- 加载后先以全新 exact Home session `68ab2e3dbd0b422b8ac2a5b8a749aba6` 一次动作返回桌面并 `succeeded`。随后全新普通目标“打开微信” session `5699d6f5d3844acebc5e8d26d96da955` 只执行一次 `tap_semantic`，receipt `matched`，前台从 Launcher 变为 `com.tencent.mm`，screen 为 `chat_file_transfer_assistant`，revision `1→2`，最终 `status=succeeded`、`active_subgoal=null`、`physical_actions=1`。正式报告：`poc/output/web/generic_supervised_20260824_195522_5699d6f5/report.json`。
- 当前项目 Uvicorn 父 PID `9168`、监听 PID `31516`；官方客户端确认 OpenAPI 已核对、controller/camera online、busy=false、无活动任务。卖家 `main.exe` 仍为 PID `3512`，本批未启动、停止、重启或点击其全屏/横竖屏控件。

## 36. 2026-08-25 正式运行链架构减法

- 正式链现只保留四个职责：DeepSeek 任务图、`2026-08-20-canonical-action-v1` 唯一动作目录、动作后 typed receipt 验证、机械 Controller。Qwen 每步只做一次远程画面结构化；后续动作选择是纯本地 canonical 选择，Qwen 不再输出或复核任务完成。
- 已物理删除 `generic_step_planner.py`、`element_geometry_audit.py`、旧多轮 `GenericSceneObserver`、在线二次动作选择/完成复核、旧几何凭据与 Controller 动作词风险黑名单；原 `test_generic_step_planner.py` 已改名为 `test_generic_action_adapter.py`。旧协议没有兼容开关、备用默认值或运行时入口。
- App 身份统一消费 canonical surface 合同；输入身份统一消费 typed input lineage；执行前只核对 fresh observation/scope、唯一目标、几何重叠与机械可达。发送、输入、关注、评论、删除、长按、拖动等普通动作词不再产生 Controller 风险否决；只有登录认证与付款交易进入正式确认策略。
- 最终离线验收：Python `820/820`、前端 `30/30` 通过，静态编译、差异检查和旧符号扫描通过。浏览器测试已禁止自动打开外部浏览器，既有子进程警告消失。本批没有调用在线模型、相机、Controller 或机械臂，也没有启动、停止、重载或操作卖家 `main.exe`；因此这是内部架构与自动测试完成，不是新增真机验收证据。

## 37. 2026-08-25 通用执行工程能力补齐

- 新增唯一零动作运行诊断 `GET /api/doctor/{device_id}`、官方客户端 `doctor` 命令和版本化报告；旧阶段预检脚本及测试已物理删除。诊断只读取模型、设备锁、Controller、camera、稳定帧、fingerprint 和 typed capability，固定 `physical_actions=0`。
- 新增 `DeviceExecutor` 作为 canonical 动作到 Robot transport 的唯一分发表。正式执行、Mock 与只读 Replay 共用同一 typed request/result；adapter 不再直接分派机械动作。Executor 与旧 `PhaseOneNavigationPolicy` 更名后的 `CanonicalActionPolicy` 均直接复用 canonical `SUPPORTED_ACTIONS`，没有第二份动作白名单。
- `double_tap` 已接入现有 `2026-08-20-canonical-action-v1` 全链。实现使用一次卖家 transport 请求设置 `click_count=2`，完成或异常后都恢复为 1；设备 registry 仍保持未晋级，只有新鲜真机报告同时证明事件 barrier、两次接触请求和恢复单击后才会启用，不以离线测试冒充真机能力。
- 新增三 App scope、三任务、九动作的只读任务序列 benchmark，结果 `3/3` 任务通过、`9/9` 动作匹配、`physical_actions=0`、`remote_model_calls=0`。Qwen usage 同时增加逐请求和汇总耗时字段，没有增加一次模型调用。
- 最终自动验证为 Python `836/836`、前端 `30/30`，静态编译、差异检查和旧运行入口扫描通过。全量回归中一度出现的五项失败同属旧测试 Robot 缺少点击 barrier 回执；只补齐测试替身正式合同后全部清零，生产链未加旧兼容。
- 官方客户端只读核对当前旧服务：OpenAPI/token 正常，controller/camera online、busy=false、默认设备 `device-local-01`；但 session `fcce25192ce34c24b6dbfb62f6a10993` 仍为 `awaiting_confirmation`。为不夺取控制权，本批没有重载项目 API、取消或续跑会话、调用在线模型或产生物理动作，也没有操作卖家 `main.exe`。
- 提交 `b965ab5` 已加载到交互桌面 Uvicorn，监听 PID `25444`。普通隐藏实例因非交互桌面导致 controller/camera offline，精确终止本任务刚创建的 PID `2064/26452` 后，改用一次性 `Interactive` 任务启动；临时任务已删除。卖家 `main.exe` 始终保持 PID `2288`、启动时间 `2026-08-25 13:24:59` 且未被操作。
- 官方客户端重新核对：OpenAPI/token 正常，controller/camera online、busy=false、stop=false、0 活动任务/会话。新 doctor 为 `2026-08-25-runtime-doctor-v1 / ready=true / physical_actions=0 / blockers=[]`，四帧 `810x1440` 且稳定；DeepSeek 与 Qwen 均已配置，Qwen 为 `qwen3.7-plus`、思考关闭。本次没有调用在线模型、创建 session 或执行机械臂动作。
- 当前交接动作：服务加载缺口已关闭。`double_tap` 仍保持 `requires_double_tap_live_acceptance`，只有存在明确且可验证目标时才单独做一次真机晋级；否则项目主线回到尚未完成的中文输入完整正式验收，不改变阶段 1–3、严格换行、多字段和普通发送的既有真机结论。

## 38. 2026-08-25 单步视觉坐标原子归一化与当前运行阻塞

- 原任务“打开微信，给文件传输助手发送 `aaazjie？你好`”的全新 session `075ca70dbd2e43e58b44362fd0c44f58` 已真实完成打开微信并进入具名会话页两个物理动作；动作后 Qwen 同时在 scene 和 input_structure 中把空输入框写为 `[120,1750,780,1830]`，旧输入内层解析因纵坐标超过 `1000` 终止。正式终态 `failed/physical_actions=2`，没有输入或发送。
- 当前正式单步观察协议升级为 `2026-08-25-single-step-qwen-observation-v2`，观察器为 `2026-08-25-single-step-scene-observer-v2`。外层响应必须声明唯一 `coordinate_space`；正常 `normalized_1000` 直接解析，严格等比例且显式声明精确宽高的 `image_grid` 会在任何 scene/input 解析前整份原子换算为 `0..1000`。无法证明、混合、比例不符或越出声明网格时仍为 0 新动作失败，不猜 `1920/2000` 等固定高度。
- 旧的 `_normalize_input_audit_pixel_coordinates` 已物理删除；它此前只处理 input_structure，且错误使用相机原图尺寸而不是实际发送给 Qwen 的 JPEG。现在由 `vision_agent._image_request_size()` 提供精确请求图片尺寸，scene 与 input_structure 不再有两个换算入口；canonical、Controller 与机械层仍只消费 `0..1000`。
- 现场 `1080×1920` 几何、`720×1280` 分辨率变化和错误宽高比、未声明越界、越出声明网格、scene/input 混用反例已固化。观察器相关 `347/347`、最终完整 Python `838/838`、静态编译与差异检查通过；测试没有调用在线模型、相机、Controller 或机械臂。
- 旧项目 Uvicorn PID `25444` 已被精确替换，当前服务明确报告 observer/protocol `v2`、busy=false、0 活动任务/会话。启动父 PID `2548`，实际监听 PID `28424`；卖家 `main.exe` PID `2288` 未被停止、重启或点击。
- 当前唯一运行阻塞：官方 doctor 为 `ready=false/physical_actions=0`，卖家进程虽然 Responding，但当前桌面没有该进程的顶层窗口，Controller 报 `client_size=[0,0]`、controller/camera offline。因没有新鲜手机画面，修复后的原任务尚未建立全新 session，也没有新的输入/发送真机证据。恢复条件是卖家控制端窗口重新出现在同一交互桌面且官方 doctor 重新变为 ready；恢复后只用原任务建立一个全新 session，不恢复或改写 `075ca...`。

## 39. 2026-08-25 Qwen 单会话预算硬门已废除并加载

- 坐标修复加载后的原任务新 session `917203ed3cb8496fb9668ae73acb1f45` 已执行 `6` 个物理动作。前五步正式 matched；第六步已真实切到 numeric 键盘，但动作后复核准备发起第七次 Qwen 请求时，前六次累计 `50163` Token 触发旧本地 `50000` Token 硬门。正式终态 `failed/physical_actions=6`，输入框保留 `aaazjie`，尚未输入 `？你好。` 或发送。报告：`poc/output/web/generic_supervised_20260825_144706_917203ed/report.json`。
- 用户已废除整个 session 的累计请求数和 Token 硬门。`vision_usage.py` 已物理删除 `16/50000` 阈值、预算异常类、请求前预算否决和剩余/耗尽输出；`qwen_runtime_errors.py` 不再分类旧预算错误。正式账本升级为 `2026-08-25-single-step-qwen-usage-v4`，只记录请求、Token、耗时和费用，明确 `session_limits_enforced=false`。
- 第 29 节每个闭环步骤最多一次 Qwen 仍有效；模型仍固定 `qwen3.7-plus`，不增加备用模型或自动降级。模型身份和旧联网 stage 检查保留，它们与资源预算无关。
- 离线验证：旧阈值变化样本在 `17` 次成功请求、累计 `51000` Token 后仍允许第 `18` 次请求；最小 `8/8`、相关 `173/173`、完整 Python `839/839`、前端 `30/30` 通过，静态编译、差异检查和生产源防恢复扫描通过。
- 项目 API 已只重载 Uvicorn，当前交互桌面父/监听 PID `24300/3248`；官方 bootstrap 为 `openapi_verified=true`，status 为 controller/camera online、busy=false、0 活动任务/会话，doctor 为 `ready=true/physical_actions=0/blockers=[]`。一次性 Interactive 任务已删除。卖家 `main.exe` 仍为 PID `2288`、启动时间不变且未被操作。
- 本批加载后没有建立新 session、调用在线 Qwen 或执行物理动作。因此当前可声明预算硬门已删除、自动测试通过并加载；不能改写 `917203ed...` 的历史失败，也不能声称原输入发送任务已经完成。后续如复验，必须用原目标建立全新 session，不能恢复旧 authority 或重复旧 session。

## 40. 2026-08-25 四类键盘输入唯一路由已实现，尚待真机加载验收

- 用户已否决 ADB/整段 Unicode 后端；正式输入继续使用真实软键盘、当前视觉标签和机械臂。拉丁字母要求 QWERTY+英文按键模式，键序后若仍为预编辑则点击逐字相同候选；中文要求 QWERTY+拼音并点击逐字相同候选；数字只通过 `123` 进入 numeric；其他可打印复杂字符先切英文，再通过当前画面独立可见的 `！？#`/`!?#` symbol 键进入符号层并点击逐字相同字符。
- 旧 `qwerty -> numeric -> symbol` 路径已从共享布局权威和测试期望删除，正式拓扑为 `numeric <-> qwerty <-> symbol`。因此下一字符为 `？` 时，只有 `123` 可见不会再产生误点；只有方向明确的 symbol 键才可成为下一动作。空格与真实换行不被当作复杂符号。
- 观察层现在为每一步计算同一份所需输入模式。符号若处于中文模式，只发布中英文切换；动作后新观察确认英文后，才发布 symbol 布局键；进入符号层的新观察确认精确 `？` 键后才发布字符动作。数字路径不受中英文模式阻挡。编排层使用同一 helper 核对 typed 后置状态，没有新增第二协议、App 分支、固定坐标或键盘品牌分支。
- Qwen 输入结构合同现为 `2026-08-25-input-structure-audit-v11`，要求完整枚举当前可见的 `123` 与独立 symbol 布局键，并把英文预编辑与 QWERTY 上方逐字相同候选分别报告；明确禁止用 `123` 代替符号键，也禁止用 `direct_latin` 模式名证明字母已提交。`2026-08-20-canonical-action-v1` 仍是唯一动作协议。
- 新增正向/变化/反例覆盖字母、中文、数字、复杂符号、模式切换、符号布局、精确字符及换行排除。输入观察/事务/编排回归 `224/224`、canonical/执行相关 `59/59`、完整 Python `854/854`（`26.126s`）通过，`git diff --check` 无空白错误。
- 项目 Uvicorn 已只替换旧父/监听 PID `27280/18304`，当前父/监听 PID 为 `9284/30020`，创建时间 `2026-08-25 17:02:11`。首次临时任务命令把本机枚举误写为 `InteractiveToken`，旧服务停止后未创建任务；随后改用系统实际支持的 `Interactive` 立即恢复。该错误属于 Agent 重载命令错误，不是项目、网络、手机或协议故障。
- 恢复后官方客户端确认 `openapi_verified=true`、controller/camera online、busy=false、0 活动任务/会话；doctor 为 `ready=true/physical_actions=0/blockers=[]`，临时任务已不存在。没有调用在线 Qwen、执行机械臂动作或操作卖家 `main.exe`。当前状态是“内部实现完成、完整自动回归通过并已加载”；原目标 `aaazjie？你好` 的新真机输入与发送终态仍未验收，不能写成真机完成。

## 41. 2026-08-25 英文预编辑逐字候选提交修复（尚未加载真机复验）

- 最新正式失败 session `ea9a0e1ab3de447681fb6d5993a65210` 已真实打开微信、进入文件传输助手、聚焦输入框并完成 `aaazjie` 字母键序，共 `4` 个物理动作。动作后真实帧中同一串字母出现两次：输入框内带下划线的 `aaazjie` 是未提交预编辑，QWERTY 字母行上方另有一个独立 `aaazjie` 候选。Qwen 的 dedicated input audit 正确返回应用 `text=""` 和预编辑 `aaazjie`，但漏报第二处候选为 `candidates=[]`，因此解析器按既有严格门停止。终态 `failed`；未点击候选、未继续 `？你好`、未发送。
- 根因是正式提示仍把 `direct_latin` 描述成直输路由，虽然 Controller、canonical 和 typed lineage 已具备英文预编辑候选分支，模型却仍可能据模式名漏枚举候选；候选点击后的残留预测栏归约又只接受 `chinese_pinyin`。这两处共同形成跨 App 的英文候选提交缺口，不是微信、正文、截图、坐标、机械臂范围或网络特例。
- Open-AutoGLM 通过临时切换 ADB Keyboard 并直接注入完整文本，随后恢复原键盘，因此绕过了可见 IME 候选事务；本项目禁止 ADB 且必须由机械臂操作真实软键盘，不能复制该 transport。当前适配仍保持一次一动作：字母键序后新观察；若应用值已逐字提交则继续，若同字段仍为 preedit 且存在唯一逐字候选则点击候选；点击后再用新观察验证 committed App 值。
- 输入结构合同升级为 `2026-08-25-input-structure-audit-v11`。提示明确 `direct_latin` 只表示拉丁按键模式，并要求把输入框内带下划线组合与 QWERTY 上方第二处同文字候选分别框出。receipt-bound 候选提交后的残留预测栏归约现同时接受 `direct_latin/chinese_pinyin`；typed `input_field_id`、授权 prior/fragment/expected、唯一候选、当前几何、App/页面和 fingerprint 门均未放宽。
- 正向样本覆盖 `aaazjie`；变化样本覆盖 `second`、已有换行前缀和多字段活动字段；缺失、错误、重复候选均保持拒绝。定向 `6/6`、输入/观察/canonical/Controller/编排相关 `420/420`、本批唯一完整 Python 回归 `856/856`（`22.957s`）通过。
- 本批没有调用在线 Qwen、重载项目 API、创建/恢复 session、调用相机/Controller 或执行真机动作，也没有操作卖家 `main.exe`。因此当前只可声明 **通用代码修复和自动回归完成**；运行服务尚未加载 v11，原目标仍须获准后只重载项目 API，并用一个全新 session 取得英文候选点击 matched、后续 `？你好` 与发送最终 succeeded，不能改写 `ea9a0e1a...` 的失败终态。

## 42. 2026-08-25 通用系统最近任务动作（离线相关回归已通过，尚未加载真机）

- 用户当前目标是 `Home → 打开 Android 最近任务 → 划掉微信卡片`；“清除微信”只指从最近任务列表移除卡片，不是强停、清数据、清缓存或删除聊天记录。手机已由此前全新 exact Home session `2270f7fec98d4a7b9b975f775613f7ff` 返回 launcher；此前自然语言任务误提出桌面分页滑动，exact `tap_semantic` 又因最近任务键是无文字系统区域而 0 动作 blocked，均未打开最近任务、未移除卡片。
- 对照 Open-AutoGLM 当前动作集合后，确认其同样只有 Tap/Swipe/Back/Home 等基础动作，没有可直接复用的第一类 Recents 动作。本项目真实缺口是 canonical/设备链没有表达 Android 最近任务键，而不是微信识别或模型措辞问题；用户已同意新增通用 `open_recent_apps`。
- `open_recent_apps` 已贯通 typed required action、exact graph/CLI、canonical candidate、Qwen 本地确定性选择、Controller typed transition、`DeviceExecutor`、Robot/Mock、设备 registry、OpenAPI 能力快照和观察提示。设备端只使用独立标定比率与现有单击事件栅栏，不接收模型坐标；动作后的唯一正式成功条件是新场景归类为 `surface.kind=recent_tasks`。
- 新增 `test_recent_apps_action.py`，覆盖中文/英文自然语义、浏览器“多任务页”反例、canonical/Qwen 同候选、系统页面精确分类、Controller 正反后置状态和一次单击凭据。定向 `5/5`、受影响模块 `476/476`、完整 Python 回归 `864/864` 通过；前端浏览器测试因当前 Node 环境缺少 `playwright` 未启动，本批没有安装依赖或改前端。
- 项目 API 已只替换旧 PID `208/14360`，当前启动父 PID `15620`、监听 PID `28280`；卖家 `main.exe` 仍为 PID `2288`，未被操作。官方 CLI 确认 OpenAPI、设备、相机、独占和四帧稳定性均正常，doctor 为 `ready=true/physical_actions=0` 且正式能力包含 `open_recent_apps`。
- 全新 session `ca59034b643142c99a1346fc5eb4d5df` 从 `launcher_home` 唯一选择 `open_recent_apps`。确认后执行 1 个系统点击，但动作后仍为 `launcher/home_screen`，typed `surface.kind=recent_tasks` 未满足，回执为 `mismatched/physical_actions=1`；系统准备的同动作自动纠正未被确认，会话随后取消并释放设备，没有原坐标重试。
- 动作前后截图显示首次设备点为基准画面 `(170,936)`，落在左侧最近任务图标附近但手机未接收。该比例是本批新加、此前未真机验证的设备标定，而 Home/Back 为独立已验收比例；根据用户此前“重新校正、范围向内放大”的决定，只把 `android_recents_x_ratio` 从 `0.315` 调整为同一按键内侧的 `0.330`，不改 App、截图、任务编排或 Qwen 坐标。相关 `95/95`、完整 Python `865/865` 通过，配置由执行时读取，没有再次重启服务。
- 纠正 session `e185750fabad4cb381c80480592cdf5f` 从新的 `launcher_home` observation/scope 执行一次 `(178,936)` 系统点击。动作后的四张真实图片都清楚显示系统最近任务背景、居中的微信卡片、卡片上方“微信”标题和底部全局 `×`；因此物理系统动作成功。单步 Qwen 却把卡片内部当成整个前台页面，输出 `foreground_app_id=com.tencent.mm、screen_id=chat_window`，还把系统全局 `×` 写成 App 内关闭按钮；Controller 因 `surface.kind=recent_tasks` 不成立而正确拒绝，正式终态仍是 `blocked/mismatched/physical_actions=1`。没有执行第三次系统点击，也没有划卡片或点全局 `×`。
- 只读尝试用自然任务“向上划掉唯一微信卡片”建立新任务时，正式投影在 0 动作处返回“任务图没有活动目标”并拒绝创建 session；这不能绕过系统容器身份缺口。Open-AutoGLM 当前做法是把 ADB 的 current-app、截图和动作历史交给模型直接生成 Tap/Swipe，没有本项目的 typed `surface.kind` 后置合同，因此没有可直接移植的严格解法。
- 候选通用修复尚待用户确认：仍只用本步同一份 Qwen 响应，把“系统最近任务容器”和“其中的 App 卡片”作为父子结构报告；canonical 只为唯一、完整可见、可移除的目标卡片生成 element-bound 向上 swipe，后置条件是同一卡片消失。不得增加第二次视觉调用、本地第二身份判断、微信分支、固定卡片坐标或点击底部全部关闭。相关正反例、一次完整回归通过并加载后，才允许一个全新 swipe session；动作后仍须新观察证明微信卡片消失。

## 43. 2026-08-26 最近任务卡片已清除与跨页面同名元素误判修复

- 第 42 节最后记录的是当时状态，已被本节后续真实证据更新。全新 session `56801f9ce21641d685e566746a87b1d1` 从可信 `system/system_recent_tasks` 场景唯一绑定 `e1/list_item/wechat_preview_card/微信`，执行一次 element-bound `swipe direction=left`。Controller 由卡片边界 `[0.29,0.31,0.71,0.83]` 解析归一化轨迹 `[0.605,0.57] → [0.08,0.57]`，卖家 transport 回执像素轨迹为 `[[485,814],[42,813]]`，`physical_actions=1`，没有自动重复。
- 动作后的四帧和新 observation 已稳定变为 `launcher/launcher_home`；原最近任务卡片不再存在，画面中的“微信”只是桌面 `role=icon/meaning=app_launcher_wechat` 图标。旧 Controller 仅凭新观察复用的 `element_id=e1` 或逐字同名“微信”，把桌面图标误认成已经划掉的最近任务卡片，因此该历史 receipt 仍如实保留为 `mismatched`，会话随后取消；不得把历史终态改写成 succeeded。原始报告：`poc/output/web/generic_supervised_20260826_004046_56801f9c/report.json`。
- 独立全新 session `228f8c04f5b74469bff744fe2ca790ce` 随后执行一次精确 typed `open_recent_apps`，receipt 为 `matched/physical_actions=1`，最终 `status=succeeded/active_subgoal=null`。新场景为 `system/system_recent_tasks`，唯一空状态文字逐字为“近期没有任何内容”，直接证明最近任务列表中已经没有该卡片。原始报告：`poc/output/web/generic_supervised_20260826_004353_228f8c04/report.json`；最终可信观察：同目录 `trusted_observation_step_2.json`。这是独立会话的真实结果证明，不反向改写前一 swipe 会话的历史回执。
- 通用修复已升级 Controller 为 `2026-08-26-universal-action-v17`：`element_id` 只在同一观察表面内有连续性；同一表面中，空间稳定重叠且 ID 或逐字标签一致的元素仍视为同一目标，即使模型的 role/meaning 措辞漂移；跨表面时，单独重复 ID 或同名标签不再构成同一元素，只有严格角色与含义连续的语义证据才继续保守阻止“已消失”结论。实现不包含 App 名、截图、固定坐标或任务步骤分支。
- 正向、反向和变化样本已覆盖“同页面同一卡片角色漂移仍应拒绝”“目标确实消失应通过”“切到桌面后同名且复用 ID 的应用图标不能冒充原卡片”。最小 `2/2`、相关 Controller/canonical/编排回归 `326/326`、本批唯一完整 Python 回归 `878/878`（`25.952s`）全部通过。
- 项目 API 已只替换旧父/监听 PID `24900/6060`，当前父/监听 PID 为 `26204/23736`，运行于交互 session 1；临时启动任务已删除。官方客户端确认 OpenAPI/token 正常，新服务明确加载 Controller `v17`，controller/camera online、busy=false、0 活动任务/会话，doctor 为 `ready=true/physical_actions=0/blockers=[]`。卖家 `main.exe` 始终为 PID `2288`、启动时间不变且未被操作。本批修复后没有再执行手机动作。

## 44. 2026-08-26 轻量 DDD 模块化单体第一批

- 用户批准采用轻量、渐进式 DDD 模块化单体，不拆微服务、不一次性重写；依赖方向和防双权威规则已写入 `AGENTS.md`。
- 第一条正式垂直切片是通用 Agent 会话生命周期。`poc/agent/domain` 定义会话/仓储合同，`application` 统一开始、查询、确认、重观察、自动推进、暂停与取消，`infrastructure` 提供唯一线程安全进程内仓储；`web_app.py` 只保留 HTTP/Pydantic 转换、认证和组合根职责。
- 旧 `generic_supervised_sessions`、`generic_supervised_session_lock` 与 Web 私有设备绑定判断已物理删除，没有双写、兼容 facade 或第二生命周期权威。
- 新边界测试 `5/5`、Web `74/74`、Orchestrator `156/156`、完整 Python `868/868`（`24.614s`）通过；前端合同合计 `28/28` 通过，静态编译、依赖扫描和 `git diff --check` 通过。
- 这是第一批真实迁移，不代表全部平铺模块已经迁完。没有重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`；运行服务仍可能是旧代码，后续真机前必须按官方客户端重新核对。

## 45. 2026-08-26 轻量 DDD 模块化单体第二批

- 第二条正式切片是设备执行与独占控制。`poc/agent/domain/device_execution.py` 定义 transport-neutral 的动作请求、执行结果、执行器端口和设备占用错误；`poc/agent/infrastructure` 实现 Robot/Replay 执行器、OS 进程租约和同设备任务注册表。
- 原根目录 `device_executor.py`、`device_exclusivity.py` 与 Orchestrator 内嵌 `DeviceTaskRegistry` 已物理删除。adapter、离线回放、能力验收、标定、Web 和测试均直接依赖新边界，没有旧路径转发、双实现或兼容 facade。
- Orchestrator 只依赖 domain 的 `DeviceTaskRegistryPort`，实际注册表由 Web/测试组合根显式注入；Robot 动作映射、物理动作计数和硬件事件回执保持不变，Replay 仍为 0 真机动作。同设备冲突、跨进程互斥、不同设备独立、同线程锁可重入以及非法动作参数在 transport 前拒绝均有直接回归。
- 最小与防恢复 `17/17`、受影响链 `497/497`、最终完整 Python 回归 `871/871`（`26.638s`）通过；前端合同合计 `28/28` 通过，静态编译、Node 静态检查、旧入口扫描和 `git diff --check` 通过。
- 本批没有迁移会话报告或 canonical 选择证据，因为它们不属于设备 transport 回执；也没有改变公开 API、模型、canonical、输入事务或真机语义。没有重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 46. 2026-08-26 轻量 DDD 模块化单体第三批

- 第三条正式切片是会话证据持久化。`poc/agent/domain/session_evidence.py` 定义证据存储端口与错误合同，`poc/agent/infrastructure/file_system_evidence_store.py` 提供唯一文件系统 JSON 原子写入实现。
- Orchestrator 内嵌 `AgentEvidenceStore`、旧存储错误定义和默认文件系统工厂已物理删除；Orchestrator 只依赖 domain 端口，Web、能力验收和测试组合根显式注入同一基础设施实现，不存在旧重导出、转发 facade 或双实现。
- 证据对象投影、全部文件名与 JSON 内容、同目录临时文件、`fsync`、原子替换和失败关闭行为保持不变。动作前/后写入失败、路径穿越、不可序列化与替换失败变化样本均通过；Web 对外仍为 HTTP `409/physical_actions=0`。
- 最小与防恢复 `8/8`、相关链 `304/304`、最终完整 Python 回归 `873/873`（`38.553s`）通过；前端合同合计 `28/28` 通过，静态编译、依赖扫描、Node 静态检查和 `git diff --check` 通过。
- 本批没有迁移 `CanonicalSelectionReceipt`，没有改变证据 schema、公开 API、模型、canonical、Controller、输入账本、设备执行或真机语义。没有重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`；下一批开始前仍须冻结一个新的真实业务切片并删除对应旧入口。

## 47. 2026-08-26 轻量 DDD 模块化单体第四批

- 第四条正式切片是 canonical 选择回执。`poc/agent/domain/canonical_selection.py` 现为唯一
  `CanonicalSelectionReceipt` 值对象和协议版本合同，并由 `to_dict()` 统一现有 JSON 投影。
- Orchestrator 尾部旧类、私有版本常量和重复字段拼装已物理删除；会话快照、确认前证据和终态报告
  直接消费同一个领域对象，不存在旧重导出、转发 facade 或双实现。
- 最小 `8/8`、相关链 `245/245`、完整 Python `874/874`（`27.126s`）通过；前端合同合计
  `28/28` 通过，静态编译、Node 静态检查、旧入口扫描和 `git diff --check` 通过。
- 本批没有改变 canonical 选择算法、证据 schema、公开 API、模型、Controller、输入账本、设备执行或
  真机语义，也没有重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`；下一批开始前
  仍须冻结一个新的真实业务切片并删除对应旧入口。

## 48. 2026-08-26 轻量 DDD 模块化单体第五批

- 第五条正式切片是动作与效果确认的一次性授权作用域。`poc/agent/domain/confirmation_authority.py`
  现为唯一 `ConfirmationAuthority` 与 `EffectConfirmationAuthority` 合同。
- Orchestrator 内嵌两类 authority 已物理删除；会话字段已收窄为领域类型，scope 字段与排序、漂移失效、
  重放拒绝、暂停/取消消费和服务端 intent preview 均保持不变，没有旧重导出、转发 facade 或双实现。
- 最小 `14/14`、相关链 `246/246`、完整 Python `875/875`（`28.818s`）通过；前端合同合计
  `28/28` 通过，静态编译、Node 静态检查、旧入口扫描和 `git diff --check` 通过。
- 本批没有改变确认类别、风险政策、公开 API、模型、canonical、Controller、输入账本、设备执行或真机
  语义，也没有重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`；下一批开始前仍须
  冻结一个新的真实业务切片并删除对应旧入口。

## 49. 2026-08-26 轻量 DDD 模块化单体第六批

- 第六条正式切片是已验证 App 表面血缘。`poc/agent/domain/app_surface_lineage.py` 现为唯一不可变
  `VerifiedAppSurfaceLineage` 和 10 字段 JSON 投影合同。
- Orchestrator 内嵌血缘类已物理删除；Qwen 上下文重绑定、动作后携带、页面身份验证、会话快照和终态
  报告继续消费同一个领域对象，不存在旧重导出、转发 facade 或双实现。
- 最小 `15/15`、相关链 `247/247`、完整 Python `876/876`（`29.144s`）通过；前端合同合计
  `28/28` 通过，静态编译、Node 静态检查、旧入口扫描和 `git diff --check` 通过。
- 本批没有改变 App/surface 匹配与升级规则、公开 API、模型、canonical、Controller、输入账本、设备执行
  或真机语义，也没有重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`；下一批开始前
  仍须冻结一个新的真实业务切片并删除对应旧入口。

## 50. 2026-08-26 轻量 DDD 模块化单体第七批

- 第七条正式切片是通用 Agent 运行会话聚合。`poc/agent/application/runtime_session.py` 现为唯一
  `UniversalAgentSessionState`、快照和动作后/纠正重试报告版本定义位置。
- Orchestrator 内嵌会话类和私有版本定义已物理删除；Orchestrator 只编排 application 聚合，测试从
  application 边界导入类型，不存在旧类、旧版本默认值或第二份快照实现。
- 最小 `11/11`、相关链 `248/248`、完整 Python `877/877`（`45.151s`）通过；前端合同合计
  `28/28` 通过，静态编译、Node 静态检查、旧入口扫描和 `git diff --check` 通过。
- 本批没有改变快照 schema、状态推进、公开 API、模型、canonical、Controller、输入账本、设备执行或
  真机语义，也没有重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`；下一批开始前仍须
  冻结一个新的真实业务切片并删除对应旧入口。

## 51. 2026-08-26 轻量 DDD 模块化单体第八批

- 第八条正式切片是每设备相机串行租约与预览缓存。`poc/agent/infrastructure/camera_coordinator.py`
  现为唯一 `DeviceCameraCoordinator` 与 `CameraPreviewUnavailable` 实现。
- Web 已物理删除内嵌类和 `BytesIO` 基础设施依赖；组合根为每设备建立独立实例，正式 Agent/doctor
  优先租约、JPEG 缓存和非阻塞 preview 行为保持不变，不存在 Web 转发类或双缓存。
- 最小 `5/5`、相关链 `381/381`、完整 Python `880/880`（`29.671s`）通过；前端合同合计
  `28/28` 通过，静态编译、Node 静态检查、旧入口扫描和 `git diff --check` 通过。
- 本批没有改变公开 API、四帧阈值、模型、canonical、Controller、输入账本、设备执行或真机语义，
  也没有重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`；下一批开始前仍须冻结一个
  新的真实业务切片并删除对应旧入口。

## 52. 2026-08-26 轻量 DDD 模块化单体第九批

- 第九条正式切片是设备注册表与控制器构造。`poc/agent/infrastructure/device_controller_registry.py`
  现为唯一设备 JSON 读取、Robot/Mock 控制器构造、设备描述和临时能力验收控制器实现。
- Web 已物理删除内嵌 `DeviceControllerRegistry`；可晋级动作集合由组合根显式注入，infrastructure 不反向
  导入能力验收或 Orchestrator。测试直接依赖新边界，不存在 Web 重导出、转发 facade 或第二份缓存。
- 最小 `8/8`、相关链 `409/409`、完整 Python `883/883`（`29.269s`）通过；前端合同合计
  `28/28` 通过，静态编译、Node 静态检查、旧入口扫描和 `git diff --check` 通过。
- 本批没有改变注册表 schema、设备配置、动作能力、公开 API、模型、canonical、Controller、输入账本、
  设备执行或真机语义，也没有重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`；下一批
  开始前仍须冻结一个新的真实业务切片并删除对应旧入口。

## 53. 2026-08-26 轻量 DDD 模块化单体第十批

- 第十条正式切片是每设备运行协调资源。`poc/agent/infrastructure/device_runtime_resources.py` 现为唯一
  每设备动作协调锁和相机协调器实例注册表。
- Web Runtime 已物理删除两组 guard/dict 和旧查询方法；采集、预览、doctor 与硬件租约直接消费同一
  infrastructure 实例，测试以替换注册表复位，不存在兼容属性、转发 facade 或第二份资源表。
- 最小 `10/10`、相关链 `413/413`、完整 Python `887/887`（`28.633s`）通过；前端合同合计
  `28/28` 通过，静态编译、Node 静态检查、旧入口扫描和 `git diff --check` 通过。
- 本批没有改变锁/缓存、公开 API、模型、canonical、Controller、输入账本、设备执行或真机语义，也没有
  重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`；下一批开始前仍须冻结一个新的真实
  业务切片并删除对应旧入口。

## 54. 2026-08-26 轻量 DDD 模块化单体第十一批

- 第十一条正式切片是视觉场景与语义动作领域语言。原根目录 `semantic_action.py`、`ui_scene.py` 已原样
  迁入 `poc/agent/domain/`，旧文件物理删除。
- canonical、Qwen、观察器、Controller、Orchestrator、Web 和测试全部直接依赖 domain 子模块；类型身份、
  协议、字段、阈值与序列化保持不变，不存在转发文件、旧导入或第二份类。
- 最小与类型/场景相关 `103/103`、视觉决策—控制—编排相关链 `613/613`、最终完整 Python
  `888/888`（`31.093s`）通过；前端合同合计 `28/28` 通过，静态编译、Node 静态检查、旧入口扫描和
  `git diff --check` 通过。
- 首次完整回归只暴露防恢复测试仍硬编码旧 `ui_scene.py` 路径，迁到新 domain 路径后最终回归通过；本批
  没有改变公开 API、模型、canonical、Controller、输入账本、设备执行或真机语义，也没有重载项目 API、
  调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 55. 2026-08-26 轻量 DDD 模块化单体第十二批

- 第十二条正式切片是确定性文字事务规划。原根目录 `text_input_utils.py` 与
  `verified_text_transaction.py` 已迁入 `poc/agent/domain/`，旧文件物理删除。
- 唯一 planner 改为领域内相对导入；canonical、lineage、observer、Controller、Robot、Orchestrator、
  能力验收和测试直接依赖 domain 子模块，不存在转发文件、旧绝对导入或第二套 planner。
- 最小 `15/15`、输入—canonical—控制—编排相关链 `623/623`、最终完整 Python `889/889`
  （`33.578s`）通过；前端合同合计 `28/28` 通过，静态编译、Node 静态检查、旧入口/文档路径扫描和
  `git diff --check` 通过。
- 首次完整回归只暴露旧链退役测试硬编码旧 `text_input_utils.py` 路径，迁到新 domain 路径后最终回归
  通过；本批没有改变文字规则、输入步骤、公开 API、模型、canonical、Controller、设备执行或真机语义，
  也没有重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 56. 2026-08-26 轻量 DDD 模块化单体第十三批

- 第十三条正式切片是 typed input lineage 分层。业务记录、精确值/字段/身份/TTL/bounds 规则和确定性
  构建迁入 `poc/agent/domain/input_value_lineage.py`；PIL 局部画面匹配和存储端口位于 application，
  按设备 JSON 原子持久化位于 infrastructure。
- 根目录 `poc/input_value_lineage.py` 已物理删除；adapter、observer、Controller、Web 和测试直接依赖
  新分层模块，Web 组合根只注入 `FileSystemTypedInputLineageStore`，不存在旧入口、转发 facade、双写、
  第二存储类或第二套 surface matcher。
- 最小与分层防恢复 `54/54`、输入—观察—控制—Web 相关链 `512/512`、最终完整 Python `890/890`
  （`36.666s`）通过；前端合同合计 `28/28` 通过，静态编译、依赖/旧入口扫描和 `git diff --check` 通过。
- 首次完整回归只有既有 Mock 四帧稳定采集偶发超时；独立复验 `18/18` 后最终全量通过，未添加生产
  补丁。本批没有改变 lineage 协议、局部画面算法、公开 API、模型、canonical、Controller、动作次数或
  真机语义，也没有重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 57. 2026-08-26 轻量 DDD 模块化单体第十四批

- 第十四条正式切片是设备动作能力。原根目录 `action_capabilities.py` 已原样迁入
  `poc/agent/domain/action_capabilities.py`，根文件物理删除。
- adapter、runtime doctor、Orchestrator 和测试全部直接依赖 domain 子模块；三处运行方法内动态导入
  已删除，不存在转发文件、兼容别名、第二份能力全集或第二套摘要/gap 实现。
- 最小与防恢复 `26/26`、adapter—doctor—编排—能力验收—Web 相关链 `475/475`、完整 Python
  `891/891`（`40.466s`）通过；前端合同合计 `28/28` 通过，静态编译、domain 依赖、旧入口/导入扫描和
  `git diff --check` 通过。
- 本批没有改变能力协议、已知动作、默认 gap、设备 profile、摘要、公开 API、模型、canonical、
  Controller、动作次数或真机语义，也没有重载项目 API、调用在线模型、执行真机动作或操作卖家
  `main.exe`。

## 58. 2026-08-26 轻量 DDD 模块化单体第十五批

- 第十五条正式切片是 canonical 可执行动作词汇。`poc/agent/domain/canonical_action_kinds.py` 现为唯一
  14 项动作集合；canonical 内旧 `SUPPORTED_ACTIONS` 定义已删除，不保留别名或重导出。
- canonical、Qwen、observer、Orchestrator、runtime session 与 device execution 直接依赖同一领域集合；
  domain/application 不再反向导入平铺 canonical，阻碍 task semantic IR 迁移的循环依赖已消失。
- 最小与防恢复 `30/30`、canonical—Qwen—观察—设备执行—编排—Web 相关链 `335/335`、完整 Python
  `892/892`（`39.562s`）通过；前端合同合计 `28/28` 通过，静态编译、旧定义/反向依赖扫描和
  `git diff --check` 通过。
- 本批没有改变动作集合成员、能力全集、候选/控制算法、公开 API、模型、输入事务、动作次数、风险边界
  或真机语义，也没有重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 59. 2026-08-26 轻量 DDD 模块化单体第十六批

- 第十六条正式切片是 TaskSemanticIR 与正式风险权威。原根模块纯业务部分已迁入
  `poc/agent/domain/task_semantic_ir.py`，根文件物理删除；唯一风险 JSON 文件读取迁入
  `poc/agent/infrastructure/file_system_risk_policy.py`。
- DeepSeek graph、canonical、Qwen、Orchestrator、Web 和测试直接消费同一 domain 类型/编译/裁决；
  domain 无 pathlib/I/O，不存在根转发、兼容别名、第二 loader 或第二套正式风险权威。
- 最小与防恢复 `60/60`、DeepSeek—canonical—Qwen—最近任务—编排—Web 相关链 `404/404`、完整
  Python `893/893`（`41.878s`）通过；前端合同合计 `28/28` 通过，静态编译、domain I/O、旧入口/导入
  扫描和 `git diff --check` 通过。
- 本批没有改变 IR/风险协议、字段、默认确认种类、编译/authority/preview、公开 API、模型、canonical、
  Controller、动作次数、风险边界或真机语义，也没有重载项目 API、调用在线模型、执行真机动作或操作
  卖家 `main.exe`。

## 60. 2026-08-26 轻量 DDD 模块化单体第十七批

- 第十七条正式切片是 canonical 动作目录值对象与确定性编译服务。原平铺
  `poc/canonical_action_protocol.py` 已物理迁入 `poc/agent/domain/canonical_action_protocol.py`，根文件
  删除且不保留 facade、兼容别名或第二套候选编译入口。
- Qwen、observer、adapter、Orchestrator、Web 与测试直接消费同一 domain 模块；内部依赖均为 domain
  相对导入，类型身份、根文件不存在、旧绝对导入为零和依赖方向均由防恢复测试固定。
- 最小与防恢复 `58/58`、canonical—Qwen—观察—适配—编排—Web 相关链 `466/466`、完整 Python
  `894/894`（`45.111s`）通过；前端合同合计 `28/28` 通过，静态编译、旧入口/导入扫描和
  `git diff --check` 通过。
- 本批没有改变 canonical 协议版本、字段、阈值、候选、catalog digest、expected result、公开 API、模型、
  Controller、动作次数、风险边界或真机语义，也没有重载项目 API、调用在线模型、执行真机动作或操作
  卖家 `main.exe`。

## 61. 2026-08-26 轻量 DDD 模块化单体第十八批

- 第十八条正式切片是通用目标只读投影与纯 JSON 对象解析。原平铺 `poc/generic_goal.py` 已物理迁入
  `poc/agent/domain/generic_goal.py`；根文件删除，不保留 facade、兼容别名或第二类型/解析入口。
- DeepSeek、application runtime session、adapter、Orchestrator 与测试直接消费同一 domain 模块；
  fenced JSON、无效/非对象 JSON、空键、不支持值及动作词普通数据变化样本均由测试固定。
- 最小与防恢复 `27/27`、DeepSeek—adapter—runtime session—编排—Mock—Web 相关链 `485/485`、完整
  Python `898/898`（`49.365s`）通过；前端合同合计 `28/28` 通过，静态编译、旧入口/导入扫描和
  `git diff --check` 通过。
- 本批没有改变目标投影协议、字段、校验、异常、DeepSeek 提示词/模型调用、公开 API、canonical、
  Controller、动作次数、风险边界或真机语义，也没有重载项目 API、调用在线模型、执行真机动作或操作
  卖家 `main.exe`。

## 62. 2026-08-26 轻量 DDD 模块化单体第十九批

- 第十九条正式切片是 DeepSeek 计划/重规划应用入口与风险策略装配。原平铺
  `poc/deepseek_task_graph.py` 已物理迁入 `poc/agent/application/deepseek_task_graph.py`；根文件删除，不
  保留 facade、兼容别名或第二类型身份。
- application 不再导入 infrastructure、pathlib 或读取文件；默认离线策略来自纯 domain 配置，正式 Web
  组合根继续加载既有风险策略 JSON 并显式注入。任务图纯领域聚合尚在该 application 模块内，下一批继续
  下沉，当前没有用 facade 制造虚假分层。
- 最小与防恢复 `110/110`、DeepSeek—TaskSemanticIR—capability—canonical—Qwen—编排—Mock—Web 相关链
  `405/405`、完整 Python `899/899`（`51.736s`）通过；前端合同合计 `28/28` 通过，静态编译、application
  反向依赖/文件读取与旧入口/导入扫描、`git diff --check` 通过。
- 本批没有改变任务图协议、字段、提示词、规范化、风险决定、模型调用次数、公开 API、Qwen、canonical、
  Controller、动作次数或真机语义，也没有重载项目 API、调用在线模型、执行真机动作或操作卖家
  `main.exe`。

## 63. 2026-08-26 轻量 DDD 模块化单体第二十批

- 第二十条正式切片是 typed 任务图领域聚合。`poc/agent/domain/task_graph.py` 现唯一拥有图协议、值对象、
  证据引用、全图校验、序列化、typed payload、exact builder 与确定性重规划投影；不依赖 application、
  infrastructure、Web、模型或设备。
- `poc/agent/application/deepseek_task_graph.py` 只保留 provider 端口、提示词、模型请求和 plan/replan 用例，
  正式导出仅 planner/provider；production 的任务图合同均直接依赖 domain，不保留 facade 或重复类型。
- 最小与防恢复 `111/111`、DeepSeek—TaskSemanticIR—capability—canonical—Qwen—编排—Mock—Web 相关链
  `406/406`、完整 Python `900/900`（`54.490s`）通过；前端合同合计 `28/28` 通过，静态编译、依赖方向、
  符号所有权、旧错误导入扫描和 `git diff --check` 通过。
- 本批没有改变图协议、字段、提示词、provider 请求/次数、风险决定、公开 API、Qwen、canonical、
  Controller、动作次数或真机语义，也没有重载项目 API、调用在线模型、执行真机动作或操作卖家
  `main.exe`。

## 64. 2026-08-26 轻量 DDD 模块化单体第二十一批

- 第二十一条正式切片是视觉模型共享错误、配置身份与环境装配。`poc/agent/domain/vision_model.py` 唯一拥有
  `VisionAgentError`、`VisionModelConfig`、协议常量和公开无密钥身份投影；domain 只依赖标准库。
- `poc/agent/infrastructure/environment_vision_model_config.py` 是唯一环境变量 loader；原平铺
  `poc/vision_model_config.py` 已物理删除。provider、Qwen、scene observer、adapter、Orchestrator、Web 与
  测试直接消费新分层合同，不存在 facade、兼容重导出、第二异常或第二 loader。
- 最小与防恢复 `48/48`、视觉配置—Qwen—scene—adapter—编排—Web 相关链 `481/481`、完整 Python
  `901/901`（`55.982s`）通过；前端合同合计 `28/28` 通过，静态编译、旧入口/导入/异常定义扫描和
  `git diff --check` 通过。
- 本批没有改变模型名称/地址、请求参数/次数、JSON 解析、公开 API、Qwen/scene/canonical/Controller、风险
  边界、设备执行、动作次数或真机语义，也没有重载项目 API、调用在线模型、执行真机动作或操作卖家
  `main.exe`。

## 65. 2026-08-26 轻量 DDD 模块化单体第二十二批

- 第二十二条正式切片是通用目标安全清洗与收件人绑定语义。原 `poc/message_intent.py` 已物理迁入
  `poc/agent/domain/message_intent.py`；原 scene observer 内嵌 `_safe_goal_context()` 已迁入
  `poc/agent/domain/generic_goal.py` 并成为唯一 `safe_goal_context()`。
- scene observer、Qwen 与测试直接消费 domain；根目录不保留 message-intent facade，观察器不保留第二
  清洗函数，Qwen 不重导出这些领域服务。收件人选择/页面身份和目标控制字段清洗判定保持不变。
- 最小与防恢复 `33/33`、目标语义—Qwen—scene—最近任务—编排—Web 相关链 `304/304`、完整 Python
  `902/902`（`59.254s`）通过；前端合同合计 `28/28` 通过，静态编译、旧文件/导入/定义扫描和
  `git diff --check` 通过。
- 本批没有改变 DeepSeek/Qwen 协议、提示词、模型调用、候选、canonical、Controller、API、风险边界、
  设备执行、动作次数或真机语义，也没有重载项目 API、调用在线模型、执行真机动作或操作卖家
  `main.exe`。

## 66. 2026-08-26 轻量 DDD 模块化单体第二十三批

- 第二十三条正式切片是 Qwen typed task context 领域合同。新
  `poc/agent/domain/qwen_task_context.py` 唯一拥有 `QwenTaskContext`、支持协议、task/device 格式、状态/
  execution class、scope/effect gate/TaskSemanticIR/input/recipient 校验和观察只读投影。
- `qwen_visual_decision.py` 已删除内嵌 context 类型、常量和辅助解析器；Orchestrator、离线评估与测试直接
  依赖 domain，provider 通过模块限定名消费且不重导出旧类名，不存在 facade 或第二类型身份。
- 最小与防恢复 `32/32`、Qwen—DeepSeek—TaskSemanticIR—scene—adapter—最近任务—编排—Web 相关链
  `532/532`、完整 Python `903/903`（`60.504s`）通过；前端合同合计 `28/28` 通过，静态编译、依赖、
  类型/常量所有权、旧导入/定义扫描和 `git diff --check` 通过。
- 本批没有改变 context 字段/校验/异常、DeepSeek 图、Qwen prompt/provider/请求次数、可信观察、决策/候选、
  canonical、Controller、API、风险边界、设备执行、动作次数或真机语义，也没有重载项目 API、调用在线
  模型、执行真机动作或操作卖家 `main.exe`。

## 67. 2026-08-27 轻量 DDD 模块化单体第二十四批

- 第二十四条正式切片是多帧稳定性/视觉遮挡证据与本地图像测量。新
  `poc/agent/domain/visual_evidence.py` 唯一拥有 `LocalFrameStability`、`VisualObstruction` 及序列化；
  domain 不依赖 PIL、环境或像素算法。
- 原 `poc/observation_images.py` 已物理迁入 `poc/agent/infrastructure/observation_images.py`；稳定性、
  清晰度、静态边缘差异与顶部遮挡共识算法保持，Qwen/scene 直接依赖 domain 证据，全部算法调用方直接
  依赖唯一 infrastructure 入口，不存在根 facade 或第二实现。
- 最小与防恢复 `35/35`、图像证据—Qwen—scene—adapter—doctor—编排—Web 相关链 `433/433`、完整
  Python `904/904`（`62.528s`）通过；前端合同合计 `28/28` 通过，静态编译、依赖、类型/算法所有权、
  旧文件/导入扫描和 `git diff --check` 通过。
- 本批没有改变数据字段/序列化、像素算法、阈值、环境配置、模型请求、canonical、Controller、API、风险
  边界、设备执行、动作次数或真机语义，也没有重载项目 API、调用在线模型、执行真机动作或操作卖家
  `main.exe`。

## 68. 2026-08-27 轻量 DDD 模块化单体第二十五批

- 第二十五条正式切片是可信观察领域快照、候选规范化与四帧 adapter。新
  `poc/agent/domain/trusted_observation.py` 唯一拥有 `TrustedObservation`、候选去重/冲突、局部目标裁决、
  system UI 投影与序列化；domain 不依赖 PIL、环境、UUID 或 infrastructure。
- 新 `poc/agent/infrastructure/trusted_observation_frames.py` 唯一负责四帧构建/新鲜度校验；本地 fingerprint
  位于 image infrastructure。Qwen 显式注入校验器，Web 组合根显式注入校验器/构建器，Orchestrator 不再
  反向导入 infrastructure；Qwen 不重导出观察类型或保留旧候选算法。
- 最小与防恢复 `41/41`、可信观察—Qwen—scene—adapter—编排—离线评估—Mock—Web 相关链 `443/443`、
  完整 Python `905/905`（`66.934s`）通过；前端合同合计 `28/28` 通过，静态编译、依赖、类型/算法所有权、
  旧导入/定义扫描和 `git diff --check` 通过。
- 本批没有改变观察字段/序列化、稳定/清晰/fingerprint/候选算法、Qwen prompt/provider/请求次数、选择结果、
  canonical、Controller、API、风险边界、设备执行、动作次数或真机语义，也没有重载项目 API、调用在线
  模型、执行真机动作或操作卖家 `main.exe`。

## 69. 2026-08-27 轻量 DDD 模块化单体第二十六批

- 第二十六条正式切片是 Qwen 单步视觉决策应用入口。原 `poc/qwen_visual_decision.py` 已物理迁入
  `poc/agent/application/qwen_visual_decision.py`；provider 调用、canonical choices、模型 payload、目标区域、
  blocked 决定与诊断指标保持同一实现。
- Web、离线评估和测试直接依赖 application；根目录无文件/facade/重导出/第二 observer。application 不导入
  infrastructure、文件系统、环境、scene observer、Web 或设备实现，Qwen context/可信观察继续直接依赖 domain。
- 最小与防恢复 `69/69`、Qwen—scene—adapter—编排—离线评估—最近任务—Mock—Web 相关链 `458/458`、
  完整 Python `906/906`（`69.468s`）通过；前端合同合计 `28/28` 通过，静态编译、application 依赖、
  observer/常量所有权、旧文件/导入扫描和 `git diff --check` 通过。
- 本批没有改变协议/字段、提示词、provider、请求参数/次数、候选/选择/blocked、诊断、canonical、
  Controller、API、风险边界、设备执行、动作次数或真机语义，也没有重载项目 API、调用在线模型、执行
  真机动作或操作卖家 `main.exe`。

## 70. 2026-08-27 轻量 DDD 模块化单体第二十七批

- 第二十七条正式切片是 canonical 动作确定性控制领域服务。原 `poc/universal_action_controller.py` 已物理
  迁入 `poc/agent/domain/universal_action_controller.py`；动作解析、局部落点、几何约束、文字事务、
  resolved action 与动作后证据验证保持同一实现和协议。
- Web、adapter、能力验收和测试直接依赖 domain；根目录无文件/facade/重导出/第二 Controller 或
  ResolvedAction。公开架构状态仍报告同一个逻辑权威名称 `universal_action_controller`。
- 最小与防恢复 `96/96`、Controller—UI scene—adapter—能力验收—编排—Mock—Web 相关链 `531/531`、
  完整 Python `907/907`（`70.559s`）通过；前端合同合计 `28/28` 通过，静态编译、domain 依赖、唯一类型
  所有权、旧根文件/导入扫描和 `git diff --check` 通过。
- 本批没有改变动作协议/规则、scene/canonical、几何/文字事务、异常文本、Qwen/DeepSeek、公开 API、风险
  边界、设备执行、动作次数或真机语义，也没有重载项目 API、调用在线模型、执行真机动作或操作卖家
  `main.exe`。

## 71. 2026-08-27 轻量 DDD 模块化单体第二十八批

- 第二十八条正式切片是本地单步视觉 scene observer。原 `poc/generic_scene_observer.py` 已物理迁入
  `poc/agent/infrastructure/generic_scene_observer.py`；PIL/本地图像测量、视觉 provider、OCR、缓存、prompt、
  scene 解析/规范化和专用输入结构审计保持同一实现与协议。
- Web 组合根、adapter、离线评估和测试直接依赖 infrastructure；根目录无文件/facade/重导出/第二 observer
  或 context，infrastructure 不反向依赖 Web、Orchestrator 或执行 adapter。
- 最小与防恢复 `88/88`、observer—输入 lineage—adapter—Qwen—编排—Mock—Web 相关链 `497/497`、
  完整 Python `908/908`（`85.669s`）通过；前端合同合计 `28/28` 通过，静态编译、基础设施边界、唯一类型
  所有权、旧根文件/导入扫描和 `git diff --check` 通过。
- 本批没有改变观察协议/字段、prompt/provider/模型请求次数、解析/审计/缓存、Qwen/DeepSeek、Controller、
  API、风险边界、设备执行、动作次数或真机语义，也没有重载项目 API、调用在线模型、执行真机动作或
  操作卖家 `main.exe`。

## 72. 2026-08-27 轻量 DDD 模块化单体第二十九批

- 第二十九条正式切片是单动作执行 adapter。原 `poc/generic_action_adapter.py` 已物理迁入
  `poc/agent/infrastructure/generic_action_adapter.py`；帧采集、本地 OCR/清晰度、方向凭证、typed lineage、
  Controller 解析/验证、设备执行和失败证据持久化保持同一实现。
- 为保持依赖方向，纯动作后观察上下文/阶段常量归入 domain，adapter 错误/Port 归入 application；
  Orchestrator 只消费 application/domain，具体 adapter 继续由 Web 组合根注入。根目录、旧 import/patch、
  第二类型定义和反向依赖均已删除。
- 最小与防恢复 `211/211`、adapter—Controller—observer—能力验收—编排—Mock—Web 相关链 `509/509`、
  完整 Python `909/909`（`78.705s`）通过；前端合同合计 `28/28` 通过，静态编译、依赖方向、唯一类型
  所有权、旧根文件/导入/patch 扫描和 `git diff --check` 通过。
- 本批没有改变动作/观察协议、Controller/canonical、OCR/方向/lineage/证据算法、设备请求、Qwen/DeepSeek、
  API、风险边界、动作次数或真机语义，也没有重载项目 API、调用在线模型、执行真机动作或操作卖家
  `main.exe`。

## 73. 2026-08-27 轻量 DDD 模块化单体第三十批

- 第三十条正式切片是通用 Agent 编排应用用例、视觉用量账本和 DeepSeek 失败诊断装配。原平铺
  `poc/universal_agent_orchestrator.py` 与 `poc/vision_usage.py` 已物理迁入 `poc/agent/application/`；原
  `poc/deepseek_failure_diagnostics.py` 已物理迁入 `poc/agent/infrastructure/`。
- Orchestrator 只消费 application/domain 与注入端口；已有报告读取也改由证据存储端口完成。Web 组合根显式
  注入文件系统诊断 writer，离线调用可使用无副作用空实现。根目录无文件/facade/重导出/旧 import/patch，
  不存在第二 Orchestrator、usage ledger 或诊断 writer。
- 最小与防恢复 `202/202`、DeepSeek—Qwen—scene—adapter—编排—Mock—doctor—能力验收—Web 相关链
  `578/578`、完整 Python `910/910`（`82.203s`）通过；前端合同合计 `28/28` 通过，静态编译、依赖方向、
  唯一类型/函数所有权、旧根文件/导入/patch 扫描和 `git diff --check` 通过。
- 本批没有改变任务/动作协议、编排顺序、模型 prompt/provider/请求次数、用量计算、canonical、Controller、
  API、风险边界、设备执行、动作次数或真机语义，也没有重载项目 API、调用在线模型、执行真机动作或
  操作卖家 `main.exe`。

## 74. 2026-08-27 轻量 DDD 模块化单体第三十一批

- 第三十一条正式切片是 DeepSeek HTTP provider。原 `poc/intent_provider.py` 已物理迁入
  `poc/agent/infrastructure/deepseek_intent_provider.py`；环境读取、请求/响应、状态、重试与异常保持同一实现。
- Web 组合根与测试直接依赖 infrastructure；application planner 继续只消费既有 provider port。根目录无
  文件/facade/重导出/第二 provider 或 error，旧 import 和 mock patch 路径均已删除。
- 最小与防恢复 `117/117`、DeepSeek planner—编排—doctor—Web 相关链 `361/361`、完整 Python
  `911/911`（`83.591s`）通过；前端合同合计 `28/28` 通过，静态编译、唯一类型所有权、旧根文件/导入/
  patch 扫描和 `git diff --check` 通过。
- 本批没有改变模型/URL、环境变量、请求体、thinking/temperature、超时/重试、响应解析、任务图、API、
  Qwen、canonical、Controller、风险边界、设备执行、动作次数或真机语义，也没有重载项目 API、调用在线
  模型、执行真机动作或操作卖家 `main.exe`。

## 75. 2026-08-27 轻量 DDD 模块化单体第三十二批

- 第三十二条正式切片是 DashScope 视觉模型 provider 与 Qwen 运行错误分类。原 `poc/vision_agent.py` 已物理
  迁入 `poc/agent/infrastructure/dashscope_vision_provider.py`，原 `poc/qwen_runtime_errors.py` 已物理迁入
  `poc/agent/infrastructure/qwen_runtime_errors.py`。
- Web、scene/adapter、离线评估和测试直接依赖 infrastructure；根目录无文件/facade/重导出/第二 provider
  或分类器，旧 import 和 mock patch 路径均已删除。公开状态中的 `vision_agent` 字段保持不变。
- 最小与防恢复 `85/85`、Qwen provider—scene—adapter—编排—doctor—Web 相关链 `465/465`、完整 Python
  `911/911`（`84.785s`）通过；前端合同合计 `28/28` 通过，静态编译、唯一类型/函数所有权、旧根文件/
  导入/patch 扫描和 `git diff --check` 通过。
- 本批没有改变视觉协议、模型配置/身份、图片尺寸/质量、请求体、重试、用量、错误分类、prompt、scene/Qwen
  决策、canonical、Controller、API、风险边界、设备执行、动作次数或真机语义，也没有重载项目 API、
  调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 76. 2026-08-27 轻量 DDD 模块化单体第三十三批

- 第三十三条正式切片是 Windows OCR runtime。原 `poc/ocr_runtime.py` 已物理迁入
  `poc/agent/infrastructure/windows_ocr_runtime.py`；临时图片、PowerShell 子进程、JSON/错误解析、缩放恢复
  和文字框匹配保持同一实现。
- 移动后资源根显式保持为 `poc/`，`OCR_SCRIPT` 仍指向原 `poc/windows_ocr.ps1`。单动作 adapter 直接依赖
  infrastructure；根目录无文件/facade/重导出/第二 OCR 实现或旧 import。
- 最小与防恢复 `167/167`、OCR—adapter—输入/QWERTY—编排—Web 相关链 `479/479`、完整 Python
  `912/912`（`86.150s`）通过；前端合同合计 `28/28` 通过，静态编译、资源路径、唯一类型/函数所有权、
  旧根文件/导入扫描和 `git diff --check` 通过。
- 本批没有改变 PowerShell/脚本、语言、图片缩放、子进程命令/超时、JSON、匹配/坐标、动作落点、API、
  模型、canonical、Controller、风险边界、设备执行、动作次数或真机语义，也没有启动 OCR 子进程、重载
  项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 77. 2026-08-27 轻量 DDD 模块化单体第三十四批

- 第三十四条正式切片是帧绑定的一次性方向执行门。原 `poc/orientation_safety.py` 已物理迁入
  `poc/agent/infrastructure/orientation_safety.py`；方向凭据、像素绑定、live seal 与 claim/consume 保持同一实现。
- Robot、device executor、adapter、能力验收和测试直接依赖 infrastructure；根目录无文件/facade/重导出、
  第二 credential/gate/seal 表或旧 import。
- 最小与防恢复 `58/58`、方向—Robot—device executor—adapter—能力验收—编排—Web 相关链 `493/493`、
  完整 Python `913/913`（`89.228s`）通过；前端合同合计 `28/28` 通过，静态编译、唯一类型/状态所有权、
  旧根文件/导入扫描和 `git diff --check` 通过。
- 本批没有改变凭据协议/字段、阈值、帧 fingerprint/视觉绑定、一次性授权、支持动作、Controller、API、
  风险边界、设备 transport、动作次数或真机语义，也没有重载项目 API、调用在线模型、执行真机动作或
  操作卖家 `main.exe`。

## 78. 2026-08-27 轻量 DDD 模块化单体第三十五批

- 第三十五条正式切片是触控标定基础设施。原 `poc/tap_calibration.py` 已物理迁入
  `poc/agent/infrastructure/tap_calibration.py`；仿射拟合、覆盖凸包、保存/加载、点击纠偏和系统边缘轨迹
  保持同一实现。
- 默认资源路径显式保持为原 `poc/tap_calibration.json`。Robot、能力验收、独立标定工具和测试直接依赖
  infrastructure；根目录无文件/facade/重导出、第二标定实现或旧 import/patch。
- 最小与防恢复 `76/76`、标定—方向—Robot—device executor—能力验收—adapter—编排—Web 相关链
  `539/539`、完整 Python `914/914`（`93.617s`）通过；前端合同合计 `28/28` 通过，静态编译、默认资源
  路径、唯一类型/函数所有权、旧根文件/导入/patch 扫描和 `git diff --check` 通过。
- 本批没有改变标定 JSON 路径/schema、版本/阈值、拟合/覆盖/纠偏算法、轨迹、Robot、API、模型、canonical、
  Controller、风险边界、设备 transport、动作次数或真机语义，也没有运行标定工具、重载项目 API、调用
  在线模型、执行真机动作或操作卖家 `main.exe`。

## 79. 2026-08-27 轻量 DDD 模块化单体第三十六批

- 第三十六条正式切片是卖家控制端 Win32 adapter。原 `poc/robot_gui_poc.py` 已物理迁入
  `poc/agent/infrastructure/seller_window_adapter.py`；窗口发现、DPI/客户区几何、相机截图、鼠标/键盘原语、
  事件栅栏和光标临时租约保持同一实现。
- 资源根仍为 `poc/`、输出根仍为 `poc/output`。Robot、标定/采集工具和测试直接依赖 infrastructure；
  根目录无文件/facade/重导出、第二 Win32 adapter 或旧 import/patch。
- 最小与防恢复 `66/66`、Win32/DPI—标定—方向—Robot—device executor—能力验收—adapter—编排—Web
  相关链 `560/560`、完整 Python `915/915`（`96.127s`）通过；前端合同合计 `28/28` 通过，静态编译、
  资源路径、唯一结构/函数所有权、旧根文件/导入/patch 扫描和 `git diff --check` 通过。
- 本批没有改变窗口标题、DPI、几何、相机截图/裁剪、鼠标/键盘事件、光标租约、事件栅栏、Robot、API、
  模型、canonical、Controller、风险边界、动作次数或真机语义，也没有捕获卖家窗口、重载项目 API、调用
  在线模型、执行真机动作或操作卖家 `main.exe`。

## 80. 2026-08-27 轻量 DDD 模块化单体第三十七批

- 第三十七条正式切片是 Robot/Mock controller 基础设施。原 `poc/robot_core.py` 已物理迁入
  `poc/agent/infrastructure/robot_controller.py`；设备状态、截图、动作原语、手机可见键位输入、配置读取、
  停止边界和本地回执保持同一实现。
- 默认配置仍指向 `poc/controller_config.json`，默认标定仍指向 `poc/tap_calibration.json`，Web 输出仍位于
  `poc/output/web`。设备 registry、scene/adapter、Web、工具和测试直接依赖 infrastructure；根目录无旧文件、
  facade、第二 controller/helper 身份或旧 import/patch。
- 最小与防恢复 `62/62`、controller—registry—scene—adapter—能力验收—编排—Web 相关链 `589/589`、
  完整 Python `916/916`（`98.330s`）通过；前端合同合计 `28/28` 通过，静态编译、默认资源路径、唯一
  controller/helper 身份、旧根文件/导入/patch 扫描和 `git diff --check` 通过。
- 本批没有改变配置/标定 JSON 路径或 schema、窗口/截图、动作/输入序列、方向门、stop、新任务清理、回执、
  API、模型、canonical、Controller、风险边界、动作次数或真机语义，也没有实例化真实 controller、重载项目
  API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 81. 2026-08-27 轻量 DDD 模块化单体第三十八批

- 第三十八条正式切片是能力验收动作资格领域合同与确定性任务图 planner。原
  `poc/capability_acceptance_planner.py` 已物理迁入
  `poc/agent/application/capability_acceptance_planner.py`；两个既有动作集合现由
  `poc/agent/domain/action_capabilities.py` 唯一定义。
- Web、验收 runtime/core 和测试直接依赖 application/domain；根目录无旧 planner、facade、旧 import、第二
  planner 或第二动作资格集合。现有验收 runtime/core 留作后续独立迁移切片，没有用本批包装或双写。
- 最小与防恢复 `48/48`、能力资格—planner—任务图—验收—编排—Web 相关链 `472/472`、完整 Python
  `917/917`（`101.318s`）通过；前端合同合计 `28/28` 通过，静态编译、唯一对象身份、旧根文件/导入/
  集合定义扫描和 `git diff --check` 通过。完整回归首次遇到一个既有 Mock 四帧采集时序波动，单项和最终
  完整回归均通过，未增加生产补丁或修改测试阈值。
- 本批没有改变动作集合成员、任务图文字/结构、验收执行/报告/晋级、公开 API、模型、canonical、Controller、
  风险边界、动作次数或真机语义，也没有重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 82. 2026-08-27 轻量 DDD 模块化单体第三十九批

- 第三十九条正式切片是能力验收报告与晋级基础设施。原 `poc/capability_acceptance.py` 已物理迁入
  `poc/agent/infrastructure/capability_acceptance.py`；标定/执行/四帧报告校验、live source 绑定、一次性
  promotion authority 和设备注册表原子晋级保持同一实现。
- Web、验收 runtime 和测试直接依赖 infrastructure；根目录无旧文件、facade、旧 import/patch、第二
  validator/promoter/authority，动作资格集合继续由 domain 唯一持有。
- 最小与防恢复 `208/208`、标定—方向—执行证据—验收 runtime—adapter—编排—Web 相关链 `540/540`、
  完整 Python `918/918`（`102.679s`）通过；前端合同合计 `28/28` 通过，静态编译、唯一类型/函数所有权、
  旧根文件/导入/patch 扫描和 `git diff --check` 通过。
- 本批没有改变报告版本/schema、动作资格集合、证据规则、authority 生命周期、锁/摘要/原子写、公开 API、
  模型、canonical、Controller、风险边界、动作次数或真机语义，也没有读取真实验收证据、写设备注册表、
  重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 83. 2026-08-27 轻量 DDD 模块化单体第四十批

- 第四十条正式切片是能力验收 file-backed runtime。原 `poc/capability_acceptance_runtime.py` 已物理迁入
  `poc/agent/infrastructure/capability_acceptance_runtime.py`；trial 状态/只读恢复、operation lock、一次
  动作确认、报告生成和独立 0 动作晋级保持同一实现。
- Web 和测试直接依赖 infrastructure；根目录无旧文件、facade、旧 import/patch 或第二 manager/trial 状态
  权威，controller/orchestrator/registry/revision provider 继续由组合根注入。
- 最小与防恢复 `68/68`、runtime—core/planner—adapter—编排—Mock—Web 相关链 `482/482`、完整 Python
  `919/919`（`104.884s`）通过；前端合同合计 `28/28` 通过，静态编译、唯一 manager/trial/helper 所有权、
  旧根文件/导入/patch 扫描和 `git diff --check` 通过。
- 本批没有改变 trial schema/目录、恢复规则、operation lock、一次确认/独立晋级顺序、物理动作上限、报告/
  registry、公开 API、模型、canonical、Controller、风险边界或真机语义，也没有创建真实 trial、写真实证据/
  注册表、重载项目 API、调用在线模型、执行真机动作或操作卖家 `main.exe`。

## 84. 2026-08-27 轻量 DDD 模块化单体第四十一批

- 第四十一条正式切片是 0 动作 runtime doctor 与模型身份复用。原 `poc/runtime_doctor.py` 已物理迁入
  `poc/agent/infrastructure/runtime_doctor.py`；controller/provider 状态、typed 能力快照、四帧稳定性、脱敏和
  blocker 投影保持同一实现。
- doctor 和 application 用量账本均复用 domain 的 `DEFAULT_VISION_MODEL=qwen3.7-plus`；模型未降级或更换，
  生产代码不再由 application 定义第二个同值字符串。Web/测试直接依赖 infrastructure，根目录无旧入口。
- 最小与防恢复 `57/57`、doctor—模型身份/用量—图像稳定性—能力快照—Qwen—Web 相关链 `172/172`、
  完整 Python `920/920`（`108.151s`）通过；前端合同合计 `28/28` 通过，静态编译、0 动作/脱敏/模型身份、
  旧根文件/导入/patch 扫描和 `git diff --check` 通过。
- 本批没有改变模型值、诊断 schema/文字、四帧数量/间隔/稳定性、脱敏、能力快照、公开 API、canonical、
  Controller、风险边界、动作次数或真机语义，也没有采集真实画面、重载项目 API、调用在线模型、执行真机
  动作或操作卖家 `main.exe`。

## 85. 2026-08-27 轻量 DDD 模块化单体第四十二批（迁移完成）

- 最终架构审计确认根目录只保留 8 个合法 interfaces/tools/evals：Web 组合根、官方类型化 API 客户端/CLI、
  两个离线评估入口、两个机械采集/标定工具和一个标定页面服务。精确 allowlist 会拒绝新增平铺业务模块，
  `agent` 正式包不得反向导入这些入口。
- allowlist 单项 `1/1`、官方客户端—离线评估—校准/采集—Web—架构相关链 `197/197`、完整 Python
  `921/921`（`114.360s`）通过；标准前端合同 `28/28`、校准页面合同 `4/4` 通过，8 个入口静态编译和
  `git diff --check` 通过。
- **本轮轻量、渐进式 DDD 模块化单体迁移完成。** 没有保留旧运行权威、转发 facade、兼容开关、空层或
  第二类型身份；以后新增正式业务切片直接进入 domain/application/infrastructure。
- 本批没有修改生产运行代码，也没有运行 CLI/评估/校准工具、启动页面服务、重载项目 API、调用在线模型、
  执行真机动作或操作卖家 `main.exe`。当前运行服务未加载本轮未重载代码，真机能力仍按独立证据报告。

## 86. 2026-08-28 20k 核心重构第二批

- `task_graph.py` 从 `3849` 行收敛到 `3124` 行；本批删除重复的本地 navigation 分类、typed record/wire
  序列化、单导航终态投影和重规划字段校验，未迁移实现、未保留兼容入口或第二任务权威。
- DeepSeek typed graph、exact input/action、本地两类确认、controller/visual typed evidence、active frontier、
  terminal exact-condition coverage 和 Qwen current-subgoal context 保持同一公开合同。
- 相关回归 `300/300`、完整 Python `922/922`（`112.327s`）通过，`git diff --check` 通过；当前生产总量
  `42912` 行，尚未达到 `<20000`，下一切片继续收敛单截图观察与 Qwen 链。
- 本批仅做离线重构；未重载项目 API、未调用在线模型、未创建真机会话、未执行机械动作，也未操作卖家
  `main.exe`。

## 87. 2026-08-28 20k 核心重构第三批

- 单步 Qwen 观察线协议只保留与实际请求图片等高的 `axis_grid`；生产解析入口已物理删除
  `normalized_1000`、`image_grid`、错误纵轴推断、平铺 UIScene 包装和缺失嵌套版本自动补写，不保留兼容
  开关或第二坐标权威。内部 canonical `0..1000`、同响应输入正文权威和每张新截图一次 Qwen 调用不变。
- `generic_scene_observer.py` 从 `6912` 行收敛到 `6701` 行；生产代码总量从 `42912` 降至 `42700`，仍未达到
  `<20000`。这只是观察切片的入口收敛，后续继续替换输入审计与 Qwen 本地选择的大型重复分支。
- 观察合同 `23/23`、观察—Qwen—输入账本—Adapter 相关回归 `194/194`、完整 Python `922/922`
  （`125.648s`）通过，`git diff --check` 通过；本批只做离线重构，未重载项目 API、调用在线模型、创建
  真机会话、执行机械动作或操作卖家 `main.exe`。

## 88. 2026-08-28 20k 核心重构第四批

- 单截图输入审计已物理删除 11 条旧 scene 修复入口、旧坐标/键盘几何恢复与第二视觉否决分支；typed
  `application_inputs[*].text` 仍是正文唯一权威，可选预编辑或候选坏数据不再推翻独立成立的应用输入事实。
- Qwen 只从本轮 canonical candidate 选择并复用同一 scope/target/effect 绑定；旧页面状态对象、重复动作
  effect 判断和重复 identity 遍历已经删除。没有建立兼容入口、第二动作目录或 App/截图/坐标特例。
- `generic_scene_observer.py` 从 `6701` 行收敛到 `5330` 行，`qwen_visual_decision.py` 从 `1529` 行收敛到
  `1067` 行；生产代码总量从 `42700` 降至 `40867`，尚未达到 `<20000`。
- 观察—Qwen—输入账本—Adapter 相关回归 `194/194`、完整 Python `922/922`（`149.215s`）通过，
  `git diff --check` 通过。本批只做离线重构，未重载项目 API、调用在线模型、创建真机会话、执行机械动作
  或操作卖家 `main.exe`；下一批从同一真实调用图继续收敛。

## 89. 2026-08-28 20k 核心重构第五批

- 新截图观察链已删除 `prior_scene`、无效 `ledger_input_value` 和三套 compact 输入/键盘剥离入口；当前帧
  只结合 typed 动作摘要与 typed 输入 lineage，不再把上一步模型 scene 传入本轮。
- 同一响应中可选 scene 输入框或键盘子结构不准确时，只撤销该可选几何，不再否决独立成立的
  `input_structure.application_inputs[*].text`；需要该几何的动作仍不会生成 canonical 候选。
- `generic_scene_observer.py` 从 `5330` 行收敛到 `4601` 行，Adapter 同步删除 12 行旧 continuation 分支；
  生产代码总量从 `40867` 降至 `40126`，尚未达到 `<20000`。
- 观察—Qwen—输入账本—Adapter 相关回归 `194/194`、完整 Python `922/922`（`99.509s`）通过，
  `git diff --check` 通过。本批只做离线重构，未重载项目 API、调用在线模型、创建真机会话、执行机械动作
  或操作卖家 `main.exe`；下一批进入 canonical/Controller/Adapter 规格收敛。

## 90. 2026-08-28 20k 核心重构第六批

- canonical catalog、Controller 与 Adapter 已共用同一元素绑定、formal transition、结果解析、pending lineage、
  方向诊断和 QWERTY OCR 锚点实现；已物理删除 Controller 的非 canonical 旧动作入口，没有保留兼容分支。
- Orchestrator 的零动作正向可见状态只由结构化目标与当前可信画面匹配一次；App 前台、系统 surface、页面
  身份、单/多元素、标题前缀和输入焦点不再分别维护可相互否决的推进链。值、缺失、外部效果与动作后结果
  的 typed evidence 边界不变。
- 四个生产模块合计净减 `646` 行，生产总量从 `40126` 降至 `39480`，尚未达到 `<20000`；相关回归
  `397/397`、完整 Python 回归 `922/922`（`112.600s`）通过，静态编译与 `git diff --check` 通过。
- 本批只做离线重构；未重载项目 API、调用在线模型、创建真机会话、执行机械动作或操作卖家 `main.exe`。
  下一批继续替换观察器千行级输入结构审计，不把本检查点误报为阶段完成。

## 91. 2026-08-28 20k 核心重构第七批

- 输入结构审计的键盘解析、字段匹配、活动/前置字段选择、输入元素发布和键盘控制发布已改为各一条正式路径；
  测试 provider 直接实现正式传输端口，生产模块不再保留捕获参数错误后降级调用的兼容回退。
- `generic_scene_observer.py` 从 `4601` 行降至 `4351` 行，生产总量从 `39480` 降至 `39230`，净减
  `250` 行，尚未达到 `<20000`。没有把代码移出统计目录、生成隐藏实现或改变模型/动作/输入权威。
- 观察—输入账本—Adapter—Qwen 相关回归 `194/194`、完整 Python 回归 `922/922`（`111.529s`）通过，
  静态编译和 `git diff --check` 通过。
- 本批只做离线重构；未重载项目 API、调用在线模型、创建真机会话、执行机械动作或操作卖家 `main.exe`。
  下一批进入 canonical catalog 的大型重复规格编译；当前 20k 阶段仍在进行中。

## 92. 2026-08-28 20k 核心重构第八批

- canonical catalog 已删除无运行时消费者的元素两两空间关系；精确文字、surface 和下一字段 relation 只在
  当前事实唯一绑定时生成。Controller 的 formal transition 改为单次 typed key 分派，输入辅助状态与聚焦
  证据不再由多条分支重复扫描。
- `canonical_action_protocol.py` 从 `2581` 行降至 `2275` 行，`universal_action_controller.py` 从 `2293`
  行降至 `2174` 行；生产总量从 `39230` 降至 `38805`，净减 `425` 行，尚未达到 `<20000`。
- canonical—Controller—Adapter—Qwen—最近任务相关回归 `236/236`、完整 Python 回归 `922/922`
  （`154.938s`）通过，静态编译和 `git diff --check` 通过；没有新增 App、截图、坐标或正文特例。
- 本批只做离线重构；未重载项目 API、调用在线模型、创建真机会话、执行机械动作或操作卖家 `main.exe`。
  下一批继续收敛 Adapter/Orchestrator，当前 20k 阶段仍在进行中。

## 93. 2026-08-28 20k 核心重构第九批

- Adapter 已删除永远只执行一次的动作后伪重试循环；一次稳定四帧只产生一次 Qwen 当前画面观察和一次
  Controller 验证，不匹配直接回到重规划。稳定性、清晰度和手机取景身份共用一个本地超时出口。
- 键盘几何准备、方向凭据授权、pending/verified 输入 lineage 与目标重绑定分别收敛为单路径；QWERTY OCR、
  输入、清空、换行、角色漂移和硬件门的原有通用合同保持不变。
- `generic_action_adapter.py` 从 `2594` 行降至 `2222` 行，生产总量从 `38805` 降至 `38433`，净减
  `372` 行，尚未达到 `<20000`。相关回归 `273/273`、完整 Python 回归 `922/922`（`106.158s`）通过，
  静态编译和 `git diff --check` 通过。
- 本批只做离线重构；未重载项目 API、调用在线模型、创建真机会话、执行机械动作或操作卖家 `main.exe`。
  下一批进入 Orchestrator，会继续物理删除旧推进分支，不把当前检查点误报为阶段完成。

## 94. 2026-08-28 20k 核心重构第十批

- Orchestrator 的 session 状态、动作清理、重新观察、可见推进、输入微动作、一次纠正记录和执行失败收口已
  各自归并为一条运行路径；没有新增兼容入口、第二任务/视觉/动作权威或 App/截图/坐标特例。
- `universal_agent_orchestrator.py` 从 `6705` 行降至 `6402` 行，生产代码总量从 `38433` 降至 `38130`，
  净减 `303` 行，尚未达到 `<20000`。一次动作后新截图、同帧 typed 输入正文权威、canonical selection、
  scope、receipt、最多一次普通导航纠正和两类确认边界保持不变。
- 关联实际测试项 `507/507`、完整 Python 回归 `922/922`（`111.188s`）通过，静态编译和
  `git diff --check` 通过。两次已退役测试模块名造成的 `FailedTest` 已明确归类为命令错误并按当前文件清单
  纠正，不属于产品代码失败。
- 本批只做离线重构；未重载项目 API、调用在线模型、创建真机会话、执行机械动作，也未启动、停止、重启
  或操作卖家 `main.exe`。下一批继续收敛当前截图观察切片，当前阶段仍在进行中。

## 95. 2026-08-28 20k 核心重构第十一批

- 当前活动子目标、typed 输入字段/事务/前置字段和观察投影已由一个只读目标视图解释；输入结构审计也只
  返回一个 typed 控件对象，元素发布不再重新拼装 15 项散装状态。没有新增动作、风险或视觉权威。
- `generic_scene_observer.py` 从 `4351` 行降至 `3994` 行，`generic_goal.py` 从 `134` 行增至 `281` 行；
  生产代码总量从 `38130` 降至 `37920`，净减 `210` 行，尚未达到 `<20000`。
- 相关回放 `240/240`、完整 Python 回归 `922/922`（`170.052s`）通过，静态编译和
  `git diff --check` 通过；一次错误的相对 `rg` 根路径只影响文件查找输出，实际测试正常通过，已纠正。
- 本批只做离线重构；未重载项目 API、调用在线模型、创建真机会话、执行机械动作或操作卖家 `main.exe`。
  下一批进入 task graph / semantic IR；当前 20k 阶段仍在进行中。

## 96. 2026-08-28 20k 核心重构第十二批

- task graph 对精确输入、当前页面刷新与最近任务卡片清除的本地 navigation 修正已合并为一次 payload
  遍历；DeepSeek application 不再串行调用三套复制/扫描实现。该入口仍只纠正模型分类，不产生动作。
- `task_graph.py` 从 `3124` 行降至 `3068` 行，`deepseek_task_graph.py` 从 `553` 行降至 `547` 行；
  生产代码总量从 `37920` 降至 `37858`，净减 `62` 行，尚未达到 `<20000`。
- DeepSeek—task semantic IR—最近任务—观察—Qwen—输入 lineage 相关回放 `157/157` 通过，静态编译
  通过。最终完整回归留到阶段终局执行；本批没有重载项目 API、调用在线模型、创建真机会话、执行机械
  动作或操作卖家 `main.exe`。
- 下一批转入 Orchestrator 千行级会话/单步生命周期重构；当前总量与阶段完成仍有实质差距，不能以本批
  测试通过宣称 20k 阶段完成。

## 97. 2026-08-28 20k 核心重构第十三批

- Orchestrator 内分散的当前帧 App/surface/命名页面/元素/标题/焦点判断已由 domain 的单一
  `VisibleGoalEvidence` 接管；编排器只保留取证、DeepSeek 推进与状态写入。canonical 与该评估器共用
  action vocabulary 和 UI scene 的低层事实，没有形成第二动作或视觉权威。
- pending/verified 输入 lineage 的事件栅栏、typed 输入 surface 和 record 构造已分别收敛为一个入口；原有
  拉丁、中文、候选、键盘状态、逐字、换行及四帧持久证据验证仍在，结构测试改为守护新单路径。
- 生产代码总量从 `37858` 降至 `37394`，净减 `464` 行；Orchestrator `6402→5652`、generic goal
  `281→667`、input lineage `1743→1646`。当前仍未达到 `<20000`。
- 输入—Adapter—Orchestrator—Qwen—依赖边界相关回放 `332/332` 通过；一次依赖方向失败已通过把 system/App surface
  事实下沉到 canonical vocabulary/UI scene 修复并复验。本批没有重载 API、调用在线模型、创建真机会话、
  执行机械动作或操作卖家 `main.exe`；下一批继续处理 App surface lineage 和单步生命周期。

## 98. 2026-08-28 20k 核心重构第十四批

- App 页面归属的 receipt 验证、lineage 创建/延续/刷新已由 domain 单一权威接管；Orchestrator 仅负责调用
  与统一错误出口，不再复制 session/task/device/revision、动作摘要、任务祖先和页面连续性判断。
- launcher 进入、功能页包名别名、后继节点延续和只读身份刷新等正向/变化样本仍成立；错误 scope、动作计数、
  非后代节点及不唯一标题仍会撤销证据。没有新增兼容入口或 App/截图/坐标特例。
- 生产代码总量从 `37394` 降至 `37354`，净减 `40` 行；相关回放 `332/332`、静态编译和
  `git diff --check` 通过。最终完整回归仍留到 `<20000` 后的终局批次。
- 本批没有重载项目 API、调用在线模型、创建真机会话、执行机械动作或操作卖家 `main.exe`；下一批继续
  物理删减千行级运行切片，当前阶段仍未完成。

## 99. 2026-08-28 20k 核心重构第十五批

- canonical 协议内 7 类 dataclass 的 wire 序列化已改为一个共享投影入口，只在各类型保留必要的省略和数值
  规范化差异；没有改变字段合同、candidate 编译、动作归属或执行语义。
- `canonical_action_protocol.py` 从 `2185` 行降至 `2141` 行，生产代码总量从 `37354` 降至 `37310`，净减
  `44` 行，尚未达到 `<20000`。相关回放 `217/217`、静态编译和 `git diff --check` 通过。
- 两次窄回归命令误引用已退役测试名，随后已按当前文件和类清单纠正；它们属于 Agent 命令错误，不是代码
  回归失败。本批没有重载 API、调用在线模型、创建真机会话、执行机械动作或操作卖家 `main.exe`。
