# 手机自动点击器项目最终目标（最高优先级）

确认日期：2026-08-11  
状态：用户已确认，作为项目产品方向和架构决策的最高优先级依据。

> 本文是项目最终目标的唯一权威入口。其他历史需求、交接文档、旧工作流或现有代码与本文冲突时，以本文为准。任何人不得在未取得用户明确同意的情况下，把项目重新缩减为微信、抖音或某一条命令的专用自动化。

## 1. 最终产品

本项目要实现一个可扩展到不同 App、不同自然语言表达和多台手机的通用视觉操作 Agent：

1. 用户在本机网页中用自然语言描述希望手机完成的目标。
2. DeepSeek理解目标、对象、约束、完成条件和风险，并动态拆分为高层子目标；任务执行中允许根据新画面调整剩余计划。
3. Qwen结合当前子目标和摄像头中的真实手机画面，判断当前页面、可见元素和状态，并提出当前唯一的下一视觉动作，包括语义目标、目标区域、预期变化和置信度。
4. 本地通用控制器只负责动作合法性、安全门禁、坐标转换和一次一动作约束，不负责写死某个 App 的业务流程。
5. 机械臂通过通用底层动作完成点击、滑动、返回、长按、拖动和虚拟键盘输入等操作。
6. 每次物理动作后必须重新观察并验证。若结果与预期不同，DeepSeek根据新状态重新规划剩余任务；若目标已经完成，则以当前可见证据结束。
7. 同一套循环必须能够扩展到新的 App。只要新目标能够由现有通用底层动作组合完成，就不应为该命令修改代码。
8. 架构从现在开始携带 `device_id` 和独立任务状态，为以后同时调度多台手机保留统一接口。

## 2. 唯一执行循环

```text
用户自然语言目标
  ↓
DeepSeek：理解目标并动态拆分高层子目标
  ↓
Qwen：结合当前子目标观察真实画面，提出一个下一视觉动作
  ↓
通用控制器：检查动作、风险、置信度和坐标边界
  ↓
机械臂：执行一个底层物理动作
  ↓
重新拍摄、观察和验证
  ├─ 符合预期：推进到下一子目标
  ├─ 不符合预期：根据新状态重新规划
  ├─ 外部状态动作：展示草稿并等待确认
  └─ 目标完成：保存可见证据并结束
```

循环本身可以固定，但用户目标、任务步骤、App和页面路径不能写死。

## 3. 模型与本地控制器的职责

### DeepSeek

- 理解用户不同措辞背后的真实目标。
- 输出结构化目标、高层子目标、约束、完成条件和风险动作。
- 根据Qwen观察到的新状态更新剩余任务，而不是坚持执行失效的旧步骤。
- 不输出机械坐标、系统Shell命令或绕过安全策略的动作。

### Qwen

- 读取摄像头中的真实手机画面。
- 结合当前子目标识别页面、文字、图标、输入框、键盘、弹层和元素状态。
- 每轮最多提出一个下一视觉动作，并给出目标区域、依据、预期变化和置信度。
- 不直接调用机械臂；其建议必须经过本地控制器检查。

### 本地通用控制器

- 保证同一时刻只执行一个动作，且使用确认前重新观察的新画面。
- 将视觉坐标转换为经过标定的机械臂坐标。
- 检查目标唯一性、画面稳定性、置信度、动作边界和账号影响。
- 不编排“抖音点赞流程”“微信加好友流程”等业务脚本。

### 机械臂动作层

- 只提供通用底层动作：点击、四向滑动、返回、长按、拖动、等待以及虚拟键盘输入。
- 动作层不知道“关注”“聊天”“拉群”等业务含义；这些结果由DeepSeek规划、Qwen观察和闭环验证共同实现。

## 4. 不得退化的红线

以下做法均视为偏离最终目标：

- 每出现一种用户说法就增加一个命令、正则表达式或接口。
- 为微信、抖音或某一页面继续堆独立的线性状态机，并把它当成产品主路径。
- 用“单次点赞成功”“单次上划成功”代替通用 Agent 已完成的证明。
- 因为当前测试场景是某个 App，就把通用协议改造成包含该 App 名称的流程协议。
- Qwen识别失败后不断增加针对某张截图的坐标补丁或视觉特判。
- 动作执行后不重新观察，仅以机械臂命令已发送判断成功。
- 为了追求“任意操作”而允许模型输出未经校验的裸坐标、连续自由动作或系统命令。

App专用知识只能作为可选视觉提示、语义别名、安全规则和测试样本，不能成为任务编排核心。

## 5. 开发决策检查

开始任何功能前必须回答：

1. 这项修改增加的是通用能力，还是只让一条固定命令通过？
2. 换一个 App 或换一种说法时，这项能力是否仍然可复用？
3. 新命令如果只由现有动作组合而成，是否可以不改代码直接执行？
4. 动作后是否有真实画面证据验证结果？
5. 是否破坏DeepSeek规划、Qwen视觉动作、本地控制器校验、机械臂执行的职责边界？

如果答案表明它正在形成单功能闭环，应立即停止实现，向用户说明偏离点并先调整架构。

## 6. 分阶段交付

1. **通用任务协议**：DeepSeek动态产生目标、子目标、约束、完成条件和风险。
2. **视觉动作协议**：Qwen依据当前子目标和真实画面提出唯一下一动作。
3. **通用动作执行**：机械臂稳定执行点击、滑动、返回、长按、拖动和输入。
4. **连续闭环与重规划**：动作后验证，异常时根据新状态调整剩余任务。
5. **通用网页控制台**：一个自然语言入口、动态计划、实时画面、执行轨迹、停止和风险确认。
6. **跨App陌生命令验收**：在没有预编排命令的情况下，通过已有动作组合完成多个 App 的新目标。
7. **多手机调度**：每台设备独立标定、队列、状态和锁，不同设备可并行，同一设备只能有一个物理控制任务。

## 7. 最终验收标准

项目不能以某个固定流程成功作为最终验收。至少需要证明：

- 用户输入项目中未预设过的自然语言命令，系统能现场理解和拆分。
- 同一目标换一种说法，仍能生成等价目标并完成任务。
- 系统能在至少三个不同 App 中组合执行多步任务，而不为每条命令新增代码。
- 起始页面、弹层或页面路径发生变化时，系统会依据新画面调整，而不是继续执行固定序列。
- 文字输入、导航、选择、发送前确认和结果验证能够进入同一个通用闭环。
- 代码测试、模型离线测试和真实机械臂验证分开记录；最终成功必须有真实画面和物理动作证据。
- 多手机版本能按 `device_id` 隔离状态、标定和任务，并防止同一设备被两个任务同时控制。

## 8. 当前任务的优先级

当前最优先工作不是继续优化某一次点赞或某一条客户寻找流程，而是把以下主路径真正变成默认运行方式：

```text
通用自然语言目标
→ DeepSeek动态任务图
→ Qwen目标驱动的视觉动作
→ 机械臂通用动作
→ 重新观察验证
→ 动态推进或重规划
```

旧微信、旧抖音工作流可以暂时保留作历史兼容和回归素材，但不得继续作为新功能的主要开发入口。
